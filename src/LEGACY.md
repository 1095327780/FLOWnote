# Legacy Source Notice

`src/` is legacy and read-only.

- Production runtime source of truth is `runtime/`.
- Do not add new features or fixes in `src/`.
- This directory will be removed in a follow-up cleanup release.
