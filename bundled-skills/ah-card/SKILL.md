---
name: ah-card
description: |
  永久笔记制卡技能：将阅读洞见、回顾结论、项目沉淀候选与归档经验转化为高质量永久笔记。制卡采用“引导式对话”，AI不直接代写定稿；必须经用户确认后再写入。
---

# AH Card

`ah-card` 在 FLOW 中承担加工核心，并承接回顾与项目中的高价值洞见。

## Architecture Decision

- 保持 `ah-think` 独立（服务 read/card/review 多场景）。
- 在 `ah-card` 内强制接入 Think Gate（条件触发）。
- 采用“引导式制卡”，禁止未确认直接落卡。

## Reusable Resources

- 来源适配：`references/source-adapters.md`
- 转卡判定：`references/card-triage.md`
- 思考绑定：`references/think-binding.md`
- 引导流程：`references/guided-card-flow.md`
- 卡片模板：`references/card-template.md`
- 同步规则：`references/index-sync-rules.md`
- 方法原则：`references/zettelkasten-principles.md`

## Skill Contract

### Inputs

- 候选洞见列表。
- 来源上下文（read/review/project/archive）。
- 用户选择的候选项与回答内容。

### Reads

- `Meta/.ai-memory/STATUS.md`
- `references/source-adapters.md`
- `references/card-triage.md`
- `references/think-binding.md`
- `references/guided-card-flow.md`
- `references/card-template.md`
- `references/index-sync-rules.md`
- `references/zettelkasten-principles.md`
- 若来源为项目：`04-沉淀候选/洞见候选.md`（或等效候选文件）

### Writes

- 永久笔记文件（仅用户确认后写入）。
- 主题页（📍）与领域页（🌱）同步更新结果。
- 来源回写（项目候选状态/回顾条目状态）。
- `STATUS.md` 的“卡片笔记”分区。

### Calls

- 协议入口：`Read ../ah-memory/SKILL.md`
- 思考引导：`Read ../ah-think/SKILL.md`
- 索引同步执行：`Read ../ah-index/SKILL.md`

### Return

- 已确认创建 / 暂缓 / 跳过清单。
- 每张卡片的来源追踪与链接摘要。
- 同步结果（主题页/领域页命中与待同步项）。
- 下一步建议（继续候选或修复待同步）。

### Failure Handling

- 候选模糊：先过 Think Gate，不直接落卡。
- 判断为项目局部信息：不转卡，返回“保留项目笔记”。
- 用户未确认定稿：只保留草稿，不写文件。
- 无可用来源：标记 `阻塞:<原因>` 并返回待补材料。
- 同步失败：保留已创建卡片，写 `待交接:ah-index`。

## Workflow

1. 读取状态，确认是否存在 `待交接:ah-card`。
2. 依据 `source-adapters.md` 标准化候选输入。
3. 使用 `card-triage.md` 做初筛（建议，不代替决策）。
4. 对不确定候选执行 Think Gate（`think-binding.md`）。
5. 按 `guided-card-flow.md` 逐条引导，收集用户表达。
6. 先给卡片草稿，用户确认后再按模板写入。
7. 按 `index-sync-rules.md` 调用 `ah-index(sync-card, sync_mode=strict)` 做同步。
8. 回写 `STATUS.md` 与来源状态，输出下一步动作。

## Quality Bar

- 一卡一事，标题必须为断言式陈述句。
- 制卡核心内容（观点/边界/应用）必须来自用户确认。
- 未经确认，禁止直接创建永久笔记文件。
- 每张卡片必须包含来源追踪与至少 2 条有效连接。
- 每张新卡必须触发索引同步或记录待同步原因。
