# Status Reporting

## 状态写入

在 `Meta/.ai-memory/STATUS.md` 的“回顾”分区：

- 启动巡检：`索引维护-{{日期}}: 进行中(1/M)`
- 巡检完成：`索引维护-{{日期}}: 已完成`
- 中断续跑：`进行中(N/M)`
- 异常阻塞：`阻塞:<原因>`

## 返回约束

返回最多 3 条建议：
1. 最高优先级修复动作
2. 次级修复或交接动作
3. 下次巡检触发条件

## 交接提示

- 大量可转化洞见：建议 `ah-card`
- 大量项目支撑缺口：建议 `ah-project/ah-archive`
