---
name: ah-project
description: |
  项目启动技能：把 O 层知识（领域页/主题页/永久笔记）映射到 W 层行动，并创建面向知识沉淀的项目目录（总览+执行记录+产出物+参考资料+沉淀候选）。用于新项目启动、复杂任务升级与行动闭环入口场景。
---

# AH Project

`ah-project` 在 FLOW 中承担 **O -> W 映射**，并为 **W -> F/L 回流**提前铺设沉淀结构。

## FLOW Position

- 输入：领域与主题知识、行动目标。
- 输出：可执行项目空间（执行与沉淀并重）。

## Reusable Resources

- 立项门禁：`references/project-gates.md`
- 知识绑定规则：`references/knowledge-binding.md`
- 领域页同步规则：`references/domain-sync-rules.md`
- 每日沉淀分流：`references/daily-routing.md`
- 文件夹蓝图：`references/folder-blueprint.md`
- 状态与交接协议：`references/handoff-spec.md`
- 项目总览模板：`assets/项目模板.md`
- 执行日志模板：`assets/项目执行日志模板.md`
- 洞见候选模板：`assets/洞见候选模板.md`

## Skill Contract

### Inputs

- 项目名称与目标。
- 领域归属、预计完成时间、优先级。
- 现有知识上下文（可选）。

### Reads

- `Meta/.ai-memory/STATUS.md`
- `references/project-gates.md`
- `references/knowledge-binding.md`
- `references/domain-sync-rules.md`
- `references/daily-routing.md`
- `references/folder-blueprint.md`
- `references/handoff-spec.md`
- `assets/项目模板.md`
- `assets/项目执行日志模板.md`
- `assets/洞见候选模板.md`

### Writes

- 新项目目录与子目录结构。
- `📍 项目总览.md`、`01-执行记录/执行日志.md`、`04-沉淀候选/洞见候选.md`。
- 相关领域页（`03-连接层/🌱*.md`）的项目入口同步结果。
- `STATUS.md` 的“项目”分区。

### Calls

- 协议入口：`Read ../ah-memory/SKILL.md`
- 索引同步执行：`Read ../ah-index/SKILL.md`
- 项目结项交接：`Read ../ah-archive/SKILL.md`

### Return

- 项目编号与目录路径。
- 成功标准与首批行动项。
- 领域页同步结果（命中页/跳过页/待同步项）。
- 每日沉淀分流规则（项目笔记 / 卡片候选 / 双写）。
- 明确的归档触发条件（何时调用 `ah-archive`）。

### Failure Handling

- 未通过立项门禁：不创建项目，返回最小补齐项。
- 关键字段缺失：写 `阻塞:<原因>` 并引导补齐。
- 知识绑定不足：允许创建，但标记“知识绑定待补齐”。
- 领域页同步失败：保留项目创建结果，写 `待交接:ah-index` 并返回待同步清单。

## Workflow

1. **Gate Check**：按 `project-gates.md` 判断是否应立项。
2. **Knowledge Binding**：按 `knowledge-binding.md` 绑定领域与支撑知识。
3. **Folder Init**：按 `folder-blueprint.md` 创建最小目录结构。
4. **Seed Files**：用 3 个模板生成总览、执行日志、洞见候选。
5. **Domain Sync**：按 `domain-sync-rules.md` 调用 `ah-index(sync-project, sync_mode=strict)` 同步领域页。
6. **Routing Rule**：按 `daily-routing.md` 写明每日沉淀去向。
7. **Status Update**：在 `STATUS.md` 写 `进行中(1/N)` 或 `阻塞:<原因>`。
8. **Handoff Plan**：写明何时转 `待交接:ah-archive`。

## Quality Bar

- 不创建“只有标题没有结构”的空项目目录。
- 保持目录简洁：固定 4 个子目录，不额外堆层级。
- 每个项目至少绑定 2 条可应用支撑知识。
- 每日执行内容必须可路由到“项目笔记/卡片候选”之一。
- 每个新项目必须触发一次领域页同步（成功或记录待同步原因）。
