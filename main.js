var __getOwnPropNames = Object.getOwnPropertyNames;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};

// runtime/i18n-locale-utils.js
var require_i18n_locale_utils = __commonJS({
  "runtime/i18n-locale-utils.js"(exports2, module2) {
    var SUPPORTED_UI_LOCALES = ["zh-CN", "en"];
    var DEFAULT_UI_LOCALE2 = "en";
    var UI_LANGUAGE_OPTIONS = ["auto", ...SUPPORTED_UI_LOCALES];
    function normalizeUiLanguage2(value) {
      const raw = String(value || "").trim().toLowerCase();
      if (raw === "auto") return "auto";
      if (raw === "zh-cn" || raw === "zh_cn" || raw === "zh") return "zh-CN";
      if (raw.startsWith("en")) return "en";
      return "auto";
    }
    function normalizeSupportedLocale2(value, fallback = DEFAULT_UI_LOCALE2) {
      const raw = String(value || "").trim().toLowerCase();
      if (raw === "zh-cn" || raw === "zh_cn" || raw === "zh" || raw.startsWith("zh-")) return "zh-CN";
      if (raw.startsWith("en")) return "en";
      if (fallback === null || fallback === void 0) return DEFAULT_UI_LOCALE2;
      return String(fallback);
    }
    function resolveLocaleFromNavigator2(navigatorLike, fallback = DEFAULT_UI_LOCALE2) {
      const nav = navigatorLike && typeof navigatorLike === "object" ? navigatorLike : null;
      const candidates = [];
      if (nav && Array.isArray(nav.languages)) {
        for (const item of nav.languages) {
          if (typeof item === "string" && item.trim()) candidates.push(item.trim());
        }
      }
      if (nav && typeof nav.language === "string" && nav.language.trim()) {
        candidates.push(nav.language.trim());
      }
      if (!candidates.length) return normalizeSupportedLocale2(fallback, fallback);
      for (const locale of candidates) {
        const normalized = normalizeSupportedLocale2(locale, "");
        if (SUPPORTED_UI_LOCALES.includes(normalized)) return normalized;
      }
      return normalizeSupportedLocale2(fallback, fallback);
    }
    function getMessageByPath2(messages, path) {
      if (!messages || typeof messages !== "object") return void 0;
      const keys = String(path || "").split(".");
      let cursor = messages;
      for (const key of keys) {
        if (!cursor || typeof cursor !== "object" || !(key in cursor)) return void 0;
        cursor = cursor[key];
      }
      return cursor;
    }
    function interpolateTemplate2(message, params = {}) {
      const template = String(message || "");
      return template.replace(/\{([a-zA-Z0-9_]+)\}/g, (_match, name) => {
        if (!Object.prototype.hasOwnProperty.call(params, name)) return "";
        const value = params[name];
        return value === void 0 || value === null ? "" : String(value);
      });
    }
    function createTranslator(options = {}) {
      const messages = options.messages && typeof options.messages === "object" ? options.messages : {};
      const getLocale = typeof options.getLocale === "function" ? options.getLocale : () => DEFAULT_UI_LOCALE2;
      const fallbackLocale = normalizeSupportedLocale2(options.fallbackLocale || DEFAULT_UI_LOCALE2);
      return function t(key, params = {}, runtimeOptions = {}) {
        const locale = normalizeSupportedLocale2(runtimeOptions.locale || getLocale(), fallbackLocale);
        const defaultValue = Object.prototype.hasOwnProperty.call(runtimeOptions, "defaultValue") ? runtimeOptions.defaultValue : key;
        const fromLocale = getMessageByPath2(messages[locale], key);
        const fromFallback = getMessageByPath2(messages[fallbackLocale], key);
        const selected = fromLocale !== void 0 ? fromLocale : fromFallback !== void 0 ? fromFallback : defaultValue;
        if (typeof selected !== "string") return String(selected);
        return interpolateTemplate2(selected, params);
      };
    }
    module2.exports = {
      SUPPORTED_UI_LOCALES,
      DEFAULT_UI_LOCALE: DEFAULT_UI_LOCALE2,
      UI_LANGUAGE_OPTIONS,
      normalizeUiLanguage: normalizeUiLanguage2,
      normalizeSupportedLocale: normalizeSupportedLocale2,
      resolveLocaleFromNavigator: resolveLocaleFromNavigator2,
      getMessageByPath: getMessageByPath2,
      interpolateTemplate: interpolateTemplate2,
      createTranslator
    };
  }
});

// runtime/runtime-locale-state.js
var require_runtime_locale_state = __commonJS({
  "runtime/runtime-locale-state.js"(exports2, module2) {
    var { normalizeSupportedLocale: normalizeSupportedLocale2, interpolateTemplate: interpolateTemplate2 } = require_i18n_locale_utils();
    var runtimeLocale = "en";
    function setRuntimeLocale2(locale) {
      runtimeLocale = normalizeSupportedLocale2(locale, "en");
      return runtimeLocale;
    }
    function getRuntimeLocale() {
      return runtimeLocale;
    }
    function rt(zhText, enText, params = {}, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || runtimeLocale, "en");
      const useZh = locale === "zh-CN";
      const template = useZh ? String(zhText !== void 0 ? zhText : enText || "") : String(enText !== void 0 ? enText : zhText || "");
      return interpolateTemplate2(template, params || {});
    }
    module2.exports = {
      setRuntimeLocale: setRuntimeLocale2,
      getRuntimeLocale,
      rt
    };
  }
});

// runtime/i18n-messages.js
var require_i18n_messages = __commonJS({
  "runtime/i18n-messages.js"(exports2, module2) {
    var I18N_MESSAGES2 = {
      "zh-CN": {
        commands: {
          open: "\u6253\u5F00",
          sendSelectedText: "\u53D1\u9001\u9009\u4E2D\u6587\u672C",
          newSession: "\u65B0\u5EFA\u4F1A\u8BDD",
          mobileQuickCapture: "\u5FEB\u901F\u6355\u83B7\u60F3\u6CD5"
        },
        notices: {
          pickTextFirst: "\u8BF7\u5148\u9009\u62E9\u6587\u672C",
          pluginLoadFailed: "FLOWnote \u52A0\u8F7D\u5931\u8D25: {message}",
          mobileLoadFailed: "FLOWnote \u79FB\u52A8\u7AEF\u52A0\u8F7D\u5931\u8D25: {message}",
          captureSaved: "\u2705 \u60F3\u6CD5\u5DF2\u6355\u83B7",
          captureFailed: "\u6355\u83B7\u5931\u8D25: {message}",
          needApiKeyFirst: "\u8BF7\u5148\u586B\u5199 API Key",
          languageAppliedReloadTip: "\u754C\u9762\u8BED\u8A00\u5DF2\u66F4\u65B0\u3002\u547D\u4EE4\u540D\u548C Ribbon \u63D0\u793A\u5C06\u5728\u91CD\u8F7D\u63D2\u4EF6\u540E\u751F\u6548\u3002"
        },
        errors: {
          localFsOnly: "\u4EC5\u652F\u6301\u672C\u5730\u6587\u4EF6\u7CFB\u7EDF Vault",
          aiMissingConfig: "AI \u670D\u52A1\u672A\u914D\u7F6E\uFF1A\u7F3A\u5C11 Base URL \u6216 API Key",
          aiResponseEmpty: "AI \u8FD4\u56DE\u5185\u5BB9\u4E3A\u7A7A",
          resolverUnsupported: "\u4E0D\u652F\u6301\u7684\u89E3\u6790\u670D\u52A1: {providerId}",
          resolverInvalidJson: "\u54CD\u5E94\u4E0D\u662F\u6709\u6548 JSON",
          resolverBodyEmpty: "\u6B63\u6587\u4E3A\u7A7A",
          resolverFailed: "\u89E3\u6790\u5931\u8D25",
          resolverFailedGeneral: "\u94FE\u63A5\u89E3\u6790\u5931\u8D25",
          resolverRateLimited: "\u9891\u7387\u53D7\u9650",
          resolverTimeout: "\u670D\u52A1\u8D85\u65F6",
          resolverUnavailable: "\u670D\u52A1\u4E0D\u53EF\u7528"
        },
        settings: {
          language: {
            name: "\u754C\u9762\u8BED\u8A00",
            desc: "\u9ED8\u8BA4\u8DDF\u968F\u8BBE\u5907\u8BED\u8A00\u3002\u5207\u6362\u540E\u754C\u9762\u5373\u65F6\u5237\u65B0\uFF1B\u547D\u4EE4\u540D\u4E0E Ribbon \u63D0\u793A\u91CD\u8F7D\u540E\u751F\u6548\u3002",
            optionAuto: "\u8DDF\u968F\u7CFB\u7EDF\uFF08\u63A8\u8350\uFF09",
            optionZhCN: "\u7B80\u4F53\u4E2D\u6587",
            optionEn: "English",
            reinstallPromptTitle: "\u91CD\u88C5\u5BF9\u5E94\u8BED\u8A00 Skills\uFF1F",
            reinstallPromptDesc: "\u5F53\u524D\u8BED\u8A00\u5DF2\u5207\u6362\u4E3A {language}\u3002\u662F\u5426\u73B0\u5728\u91CD\u88C5\u5BF9\u5E94\u8BED\u8A00\u7248\u672C\u7684\u5185\u7F6E Skills \u4E0E\u6A21\u677F\uFF1F",
            reinstallPromptConfirm: "\u7ACB\u5373\u91CD\u88C5",
            reinstallPromptCancel: "\u7A0D\u540E"
          },
          mobile: {
            intro: "\u914D\u7F6E AI \u670D\u52A1\u548C\u65E5\u8BB0\u8DEF\u5F84\uFF0C\u7528\u4E8E\u79FB\u52A8\u7AEF\u5FEB\u901F\u6355\u83B7\u60F3\u6CD5\u3002"
          },
          basic: {
            intro: "\u5E38\u7528\u60C5\u51B5\u4E0B\u53EA\u9700\u8981\u786E\u8BA4\u8FDE\u63A5\u72B6\u6001\u548C Provider \u767B\u5F55\u3002\u5176\u4F59\u9AD8\u7EA7\u9879\u4E00\u822C\u4FDD\u6301\u9ED8\u8BA4\u5373\u53EF\u3002",
            cliPathName: "FLOWnote CLI \u8DEF\u5F84\uFF08\u53EF\u9009\uFF09",
            cliPathDesc: "\u901A\u5E38\u7559\u7A7A\u3002\u63D2\u4EF6\u4F1A\u81EA\u52A8\u63A2\u6D4B\u3002Windows \u8BF7\u5148\u5728\u672C\u673A\u5B89\u88C5 Node.js\uFF0C\u518D\u6267\u884C npm install -g opencode-ai\u3002\u5FC5\u8981\u65F6\u4F18\u5148\u586B\u5199 opencode.exe \u6216 cli.js\uFF08\u4E0D\u8981\u586B opencode.cmd\uFF0C\u4E5F\u4E0D\u8981\u4F7F\u7528 WSL\uFF09\u3002",
            launchStrategyName: "\u8FDE\u63A5\u542F\u52A8\u65B9\u5F0F",
            launchStrategyDescWindows: "\u81EA\u52A8\uFF08\u63A8\u8350\uFF09\uFF1A\u81EA\u52A8\u68C0\u6D4B\u5E76\u8BB0\u5FC6\u6210\u529F\u7684\u672C\u673A\u8FDE\u63A5\u65B9\u5F0F\u3002\u624B\u52A8\u6A21\u5F0F\u4EC5\u4F7F\u7528 Windows \u672C\u673A\u5B89\u88C5\uFF1B\u4E0D\u518D\u652F\u6301 WSL\u3002",
            launchStrategyDesc: "\u81EA\u52A8\uFF08\u63A8\u8350\uFF09\uFF1A\u4F18\u5148\u4F7F\u7528\u4E0A\u6B21\u6210\u529F\u65B9\u5F0F\uFF1B\u5931\u8D25\u65F6\u81EA\u52A8\u56DE\u9000\u5230\u5176\u4ED6\u65B9\u5F0F\u3002",
            launchAuto: "\u81EA\u52A8\uFF08\u63A8\u8350\uFF09",
            launchNativeWindows: "Windows \u672C\u673A\u5B89\u88C5",
            launchWsl: "Windows WSL \u5B89\u88C5",
            launchNativeMac: "Mac \u672C\u673A\u5B89\u88C5",
            wslDistroName: "WSL \u53D1\u884C\u7248\uFF08\u53EF\u9009\uFF09",
            wslDistroDesc: "\u7559\u7A7A\u8868\u793A WSL \u9ED8\u8BA4\u53D1\u884C\u7248\u3002\u53EF\u586B Ubuntu / Debian \u7B49\u3002\u586B\u5199\u540E\u81EA\u52A8\u6A21\u5F0F\u4F1A\u4F18\u5148\u5C1D\u8BD5 WSL\u3002",
            skillInjectModeName: "\u6280\u80FD\u6CE8\u5165\u65B9\u5F0F",
            skillInjectModeDesc: "\u5F53\u4F60\u4F7F\u7528 /skill \u6307\u4EE4\u65F6\uFF0C\u63D2\u4EF6\u5982\u4F55\u628A\u6280\u80FD\u5185\u5BB9\u4F20\u7ED9\u6A21\u578B\u3002",
            skillInjectModeSummary: "\u6458\u8981\u6CE8\u5165\uFF08\u63A8\u8350\uFF09",
            skillInjectModeFull: "\u5168\u6587\u6CE8\u5165\uFF08\u66F4\u5B8C\u6574\u4F46\u66F4\u91CD\uFF09",
            skillInjectModeOff: "\u5173\u95ED\u6CE8\u5165\uFF08\u53EA\u53D1\u9001\u7528\u6237\u8F93\u5165\uFF09",
            advancedHeading: "\u9AD8\u7EA7\u8BBE\u7F6E",
            skillsDirName: "\u5185\u7F6E Skills \u5B89\u88C5\u76EE\u5F55",
            skillsDirDesc: "\u9ED8\u8BA4 .opencode/skills\u3002\u63D2\u4EF6\u4F1A\u81EA\u52A8\u5B89\u88C5/\u66F4\u65B0\u5185\u7F6E skills\uFF0C\u4F60\u4E5F\u53EF\u4EE5\u5728\u8BE5\u76EE\u5F55\u65B0\u589E\u6216\u4FEE\u6539 skills\uFF1B\u659C\u6760\u547D\u4EE4\u4F1A\u76F4\u63A5\u8BFB\u53D6\u6B64\u76EE\u5F55\u3002",
            reinstallSkillsName: "\u91CD\u65B0\u5B89\u88C5\u5185\u7F6E Skills \u4E0E\u6A21\u677F",
            reinstallSkillsDesc: "\u6309\u5F53\u524D\u754C\u9762\u8BED\u8A00\u5B89\u88C5/\u66F4\u65B0\u5185\u7F6E skills\uFF0C\u5E76\u5C06 Meta/\u6A21\u677F \u540C\u6B65\u5230\u5404 skill \u8D44\u6E90\u76EE\u5F55\u3002\u9047\u5230\u540C\u540D\u51B2\u7A81\u4F1A\u8BE2\u95EE\u66FF\u6362\u6216\u5FFD\u7565\u3002",
            reinstallSkillsNow: "\u7ACB\u5373\u91CD\u88C5",
            reinstallSkillsBusy: "\u91CD\u88C5\u4E2D...",
            reinstallSkillsSuccess: "\u91CD\u88C5\u5B8C\u6210\uFF1Askills {synced}/{total}\uFF0Ctemplates {syncedTemplates}/{totalTemplates}\uFF0C\u76EE\u5F55 {targetRoot}",
            reinstallSkillsSuccessWithMeta: "\u91CD\u88C5\u5B8C\u6210\uFF1Askills {synced}/{total}\uFF0Ctemplates {syncedTemplates}/{totalTemplates}\uFF0Cmeta {syncedMetaTemplates}/{totalMetaTemplates}\uFF0C\u76EE\u5F55 {targetRoot}",
            reinstallSkillsCanceled: "\u5DF2\u53D6\u6D88\u91CD\u88C5\u3002\u5DF2\u5904\u7406 skills {synced}/{total}\uFF0Ctemplates {syncedTemplates}/{totalTemplates}\u3002",
            reinstallSkillsFailed: "\u91CD\u88C5\u5931\u8D25\uFF1A{message}",
            resetTemplateBaselineName: "\u91CD\u7F6E\u6A21\u677F\u57FA\u7EBF",
            resetTemplateBaselineDesc: "\u4EC5\u5F53\u4F60\u9700\u8981\u6062\u590D\u9ED8\u8BA4\u6A21\u677F\u65F6\u4F7F\u7528\u3002\u4F1A\u628A\u5185\u7F6E\u6A21\u677F\u5199\u56DE Meta/\u6A21\u677F\uFF08\u51B2\u7A81\u53EF\u9010\u9879\u66FF\u6362\u6216\u5FFD\u7565\uFF09\u3002",
            resetTemplateBaselineNow: "\u91CD\u7F6E\u6A21\u677F",
            resetTemplateBaselineBusy: "\u91CD\u7F6E\u4E2D...",
            resetTemplateBaselineSuccess: "\u6A21\u677F\u91CD\u7F6E\u5B8C\u6210\uFF1A{synced}/{total}\uFF0C\u76EE\u5F55 {metaRoot}",
            resetTemplateBaselineCanceled: "\u5DF2\u53D6\u6D88\u6A21\u677F\u91CD\u7F6E\u3002\u5DF2\u5904\u7406 {synced}/{total}\u3002",
            resetTemplateBaselineFailed: "\u6A21\u677F\u91CD\u7F6E\u5931\u8D25\uFF1A{message}",
            contentConflictTitle: "\u53D1\u73B0\u540C\u540D\u51B2\u7A81",
            contentConflictDesc: "{kind} `{id}` \u5DF2\u5B58\u5728\u3002\u8BF7\u9009\u62E9\u5904\u7406\u65B9\u5F0F\u3002",
            contentConflictTarget: "\u76EE\u6807\uFF1A{path}",
            contentConflictSource: "\u6765\u6E90\uFF1A{path}",
            conflictKindSkill: "\u6280\u80FD",
            conflictKindTemplate: "\u6A21\u677F",
            conflictKindMetaTemplate: "Meta \u6A21\u677F",
            conflictReplace: "\u66FF\u6362",
            conflictSkip: "\u5FFD\u7565",
            conflictReplaceAll: "\u5168\u90E8\u66FF\u6362",
            conflictSkipAll: "\u5168\u90E8\u5FFD\u7565",
            conflictCancel: "\u53D6\u6D88",
            unknownError: "\u672A\u77E5\u9519\u8BEF",
            diagnosticsName: "\u8FDE\u63A5\u8BCA\u65AD",
            diagnosticsDesc: "\u68C0\u6D4B FLOWnote \u53EF\u6267\u884C\u6587\u4EF6\u4E0E\u8FDE\u63A5\u72B6\u6001\u3002",
            diagnosticsRun: "\u8FD0\u884C\u8BCA\u65AD",
            diagnosticsBusy: "\u6D4B\u8BD5\u4E2D...",
            diagnosticsOk: "\u8FDE\u63A5\u6B63\u5E38 ({mode})",
            diagnosticsFailed: "\u8FDE\u63A5\u5931\u8D25: {error}",
            autoMemoryRememberedWsl: "\u5DF2\u8BB0\u5FC6\uFF1AWSL{distro}",
            autoMemoryRememberedNative: "\u5DF2\u8BB0\u5FC6\uFF1A\u672C\u673A {command}",
            autoMemoryNone: "\u5F53\u524D\u672A\u8BB0\u5FC6\u6210\u529F\u8FDE\u63A5\u65B9\u5F0F\u3002",
            autoMemoryName: "\u81EA\u52A8\u8FDE\u63A5\u8BB0\u5FC6",
            autoMemoryDesc: "{rememberedText} \u6210\u529F\u8FDE\u63A5\u540E\u4F1A\u81EA\u52A8\u66F4\u65B0\u3002",
            autoMemoryReset: "\u91CD\u7F6E\u8BB0\u5FC6",
            autoMemoryResetDone: "\u5DF2\u6E05\u9664\u8BB0\u5FC6\u7684\u8FDE\u63A5\u65B9\u5F0F\u3002",
            autoMemoryResetFailed: "\u91CD\u7F6E\u5931\u8D25: {message}"
          },
          mobileCapture: {
            heading: "\u79FB\u52A8\u7AEF\u5FEB\u901F\u6355\u83B7",
            intro: "\u5728\u684C\u9762\u7AEF\u9884\u5148\u914D\u7F6E\u79FB\u52A8\u7AEF\u6355\u83B7\u8BBE\u7F6E\u3002\u540C\u6B65\u5230\u79FB\u52A8\u7AEF\u540E\u5373\u53EF\u4F7F\u7528\u3002",
            resolverHint: {
              tianapi: "\u9002\u5408\u57FA\u7840\u7F51\u9875\u6B63\u6587\u6293\u53D6\uFF1B\u52A8\u6001\u9875\u9762\u6216\u5F3A\u53CD\u722C\u9875\u9762\u53EF\u80FD\u5931\u8D25\u3002",
              showapi: "\u6309\u8C03\u7528\u8BA1\u8D39\uFF0C\u90E8\u5206\u5957\u9910\u6709\u514D\u8D39\u989D\u5EA6\uFF1B\u9002\u5408\u4F5C\u4E3A\u4F4E\u95E8\u69DB\u9009\u9879\u3002",
              gugudata: "\u8F93\u51FA Markdown \u8D28\u91CF\u8F83\u7A33\u5B9A\uFF1B\u5B98\u65B9\u5EFA\u8BAE\u63A7\u5236\u8BF7\u6C42\u9891\u7387\u3002"
            },
            resolverProvider: {
              tianapi: { name: "TianAPI", keyLabel: "TianAPI Key" },
              showapi: { name: "ShowAPI\uFF08\u4E07\u7EF4\u6613\u6E90\uFF09", keyLabel: "ShowAPI AppKey" },
              gugudata: { name: "\u5495\u5495\u6570\u636E", keyLabel: "\u5495\u5495\u6570\u636E AppKey" }
            }
          },
          providerAuth: {
            heading: "Provider \u767B\u5F55\u7BA1\u7406\uFF08OAuth / API Key\uFF09",
            intro: "\u7528\u4E8E\u5728\u63D2\u4EF6\u5185\u8FDE\u63A5\u5B98\u65B9/\u7B2C\u4E09\u65B9\u6A21\u578B provider\u3002\u5B8C\u6210\u540E\u4F1A\u5199\u5165\u5F53\u524D\u63D2\u4EF6\u8FD0\u884C\u65F6\u7684 FLOWnote \u51ED\u636E\u3002",
            refresh: "\u5237\u65B0 Provider \u72B6\u6001",
            expandAll: "\u5168\u90E8\u5C55\u5F00",
            collapseAll: "\u5168\u90E8\u6536\u8D77",
            searchPlaceholder: "\u641C\u7D22 Provider / ID / \u9274\u6743\u65B9\u5F0F / \u6A21\u578B\u6570",
            loading: "\u52A0\u8F7D\u4E2D...",
            notLoaded: "\u5C1A\u672A\u52A0\u8F7D Provider \u4FE1\u606F\u3002",
            loadedNoResult: "\u5DF2\u52A0\u8F7D {totalProviders} \u4E2A Provider\uFF0C\u641C\u7D22\u201C{query}\u201D\u65E0\u7ED3\u679C\u3002",
            loaded: "\u5DF2\u52A0\u8F7D {totalProviders} \u4E2A Provider\u3002",
            loadedWithCount: "\u5DF2\u52A0\u8F7D {totalProviders} \u4E2A Provider\uFF0C\u5F53\u524D\u663E\u793A {visibleCount} \u4E2A\uFF1B\u5DF2\u8FDE\u63A5 {connectedCount} \u4E2A\u3002",
            noMatch: "\u6CA1\u6709\u5339\u914D\u7684 Provider\uFF0C\u8BF7\u5C1D\u8BD5\u66F4\u6362\u5173\u952E\u8BCD\u3002",
            empty: "\u5F53\u524D\u6CA1\u6709\u53EF\u663E\u793A\u7684 Provider\u3002",
            groupCommon: "\u5E38\u7528",
            groupOthers: "\u5176\u4ED6",
            groupDomestic: "\u56FD\u4EA7\u5382\u5546",
            groupGlobal: "\u6D77\u5916\u5382\u5546",
            groupUnknownCountry: "\u5176\u4ED6/\u672A\u77E5\u56FD\u5BB6",
            refreshBusy: "\u5237\u65B0\u4E2D...",
            loadingProviders: "\u6B63\u5728\u8BFB\u53D6 Provider \u914D\u7F6E...",
            readFailed: "\u8BFB\u53D6\u5931\u8D25\uFF1A{message}",
            readFailedDetail: "\u8BFB\u53D6 Provider \u4FE1\u606F\u5931\u8D25\uFF1A{message}",
            connected: "\u5DF2\u8FDE\u63A5",
            disconnected: "\u672A\u8FDE\u63A5",
            providerMeta: "\u6A21\u578B\u6570\uFF1A{modelCount}\uFF1B\u9274\u6743\u65B9\u5F0F\uFF1A{methodText}",
            actionFailed: "\u64CD\u4F5C\u5931\u8D25\uFF1A{message}",
            oauthLogin: "OAuth \u767B\u5F55",
            pending: "\u5904\u7406\u4E2D...",
            setApiKey: "\u8BBE\u7F6E API Key",
            saving: "\u4FDD\u5B58\u4E2D...",
            clearLogin: "\u6E05\u9664\u767B\u5F55",
            clearing: "\u6E05\u9664\u4E2D...",
            clearTitle: "\u6E05\u9664 {providerName} \u767B\u5F55",
            clearDesc: "\u786E\u8BA4\u6E05\u9664\u8BE5 Provider \u7684\u767B\u5F55\u51ED\u636E\uFF1F",
            clearSubmit: "\u6E05\u9664",
            clearDone: "\u5DF2\u6E05\u9664\u767B\u5F55\u51ED\u636E\u3002",
            clearNotice: "{providerName} \u51ED\u636E\u5DF2\u6E05\u9664",
            pickOauthTitle: "\u9009\u62E9 {providerName} \u7684 OAuth \u767B\u5F55\u65B9\u5F0F",
            pickOauthDesc: "\u8BF7\u9009\u62E9\u4E00\u79CD OAuth \u9274\u6743\u65B9\u5F0F\u3002",
            continue: "\u7EE7\u7EED",
            invalidOauthMethod: "\u65E0\u6548\u7684 OAuth \u65B9\u5F0F\u5E8F\u53F7",
            oauthCanceled: "\u5DF2\u53D6\u6D88 OAuth \u767B\u5F55\u3002",
            oauthCreatingLink: "\u6B63\u5728\u521B\u5EFA\u6388\u6743\u94FE\u63A5...",
            oauthMissingUrl: "\u672A\u83B7\u53D6\u5230 OAuth \u6388\u6743\u94FE\u63A5",
            oauthOpened: "\u5DF2\u6253\u5F00 {providerName} \u6388\u6743\u9875\u9762",
            oauthCopied: "\u65E0\u6CD5\u81EA\u52A8\u6253\u5F00\u6D4F\u89C8\u5668\uFF0C\u6388\u6743\u94FE\u63A5\u5DF2\u590D\u5236\u5230\u526A\u8D34\u677F\u3002",
            oauthCallbackTitle: "{providerName} OAuth \u56DE\u8C03",
            oauthCallbackDesc: "\u8BF7\u5728\u6D4F\u89C8\u5668\u5B8C\u6210\u6388\u6743\u540E\u7C98\u8D34 code\u3002",
            oauthCallbackPlaceholder: "\u7C98\u8D34\u6388\u6743 code",
            submit: "\u63D0\u4EA4",
            oauthCodeMissing: "\u672A\u586B\u5199\u6388\u6743 code\uFF0C\u5DF2\u53D6\u6D88\u3002",
            oauthCompleteInBrowser: "\u8BF7\u5728\u6D4F\u89C8\u5668\u5B8C\u6210\u6388\u6743\u3002",
            oauthThenConfirm: "\u5B8C\u6210\u540E\u70B9\u51FB\u201C\u786E\u8BA4\u201D\u7EE7\u7EED\u3002",
            confirm: "\u786E\u8BA4",
            oauthSubmitting: "\u6B63\u5728\u63D0\u4EA4 OAuth \u56DE\u8C03...",
            oauthDone: "OAuth \u767B\u5F55\u5B8C\u6210\u3002",
            oauthSuccess: "{providerName} \u767B\u5F55\u6210\u529F",
            apiKeyTitle: "\u8BBE\u7F6E {providerName} API Key",
            apiKeyDesc: "\u8BF7\u8F93\u5165\u8BE5 Provider \u7684 API Key\uFF08\u4EC5\u4FDD\u5B58\u5728\u672C\u5730 FLOWnote \u51ED\u636E\u4E2D\uFF09\u3002",
            apiKeyPlaceholder: "\u8F93\u5165 API Key",
            save: "\u4FDD\u5B58",
            apiKeyCanceled: "\u5DF2\u53D6\u6D88 API Key \u8BBE\u7F6E\u3002",
            apiKeyEmpty: "API Key \u4E0D\u80FD\u4E3A\u7A7A\u3002",
            apiKeySaving: "\u6B63\u5728\u4FDD\u5B58 API Key...",
            apiKeySaved: "API Key \u5DF2\u4FDD\u5B58\u3002",
            apiKeySavedNotice: "{providerName} API Key \u5DF2\u4FDD\u5B58",
            noAuthMethods: "\u65E0\u53EF\u7528\u9274\u6743\u65B9\u5F0F"
          }
        },
        view: {
          header: {
            runtime: "\u804A\u5929\u8FD0\u884C\u65F6"
          },
          welcome: {
            greeting: "\u4ECA\u5929\u4F60\u60F3\u6574\u7406\u4EC0\u4E48\uFF1F",
            empty: "\u53D1\u9001\u4E00\u6761\u6D88\u606F\uFF0C\u6216\u8F93\u5165 / \u9009\u62E9\u6280\u80FD\u547D\u4EE4\u3002"
          },
          session: {
            current: "\u5F53\u524D\u4F1A\u8BDD\uFF1A{title}"
          },
          input: {
            placeholder: "\u8F93\u5165\u4F60\u7684\u6D88\u606F..."
          },
          settings: "\u8BBE\u7F6E"
        },
        mobile: {
          providers: {
            deepseek: "DeepSeek",
            qwen: "\u901A\u4E49\u5343\u95EE",
            moonshot: "Moonshot (Kimi)",
            zhipu: "\u667A\u8C31 (GLM)",
            siliconflow: "SiliconFlow",
            custom: "\u81EA\u5B9A\u4E49"
          },
          capture: {
            title: "\u{1F4A1} \u5FEB\u901F\u6355\u83B7",
            inputPlaceholder: "\u6B64\u523B\u5728\u60F3\u4EC0\u4E48\u2026",
            hintSend: "\u2318/Ctrl + Enter \u53D1\u9001",
            cancel: "\u53D6\u6D88",
            submit: "\u8BB0\u5F55",
            submitBusy: "\u8BB0\u5F55\u4E2D\u2026",
            statusAiCleanup: "\u{1F916} AI \u6E05\u7406\u4E2D\u2026",
            statusAiCleanupFailed: "\u26A0\uFE0F AI \u6E05\u7406\u5931\u8D25\uFF0C\u4F7F\u7528\u539F\u6587",
            statusResolver: "\u{1F517} \u89E3\u6790\u94FE\u63A5\u5185\u5BB9\u2026",
            statusResolverFailed: "\u26A0\uFE0F \u94FE\u63A5\u89E3\u6790\u5931\u8D25\uFF0C\u5DF2\u56DE\u9000\u539F\u6587\uFF1A{message}",
            statusWriteNote: "\u{1F4DD} \u5199\u5165\u65E5\u8BB0\u2026",
            emptyInput: "\u8BF7\u8F93\u5165\u5185\u5BB9"
          },
          settings: {
            providerName: "AI \u63D0\u4F9B\u5546",
            providerDesc: "\u9009\u62E9\u4E00\u4E2A\u9884\u8BBE\u63D0\u4F9B\u5546\uFF0C\u6216\u9009\u62E9\u81EA\u5B9A\u4E49\u586B\u5199\u5730\u5740\u3002",
            apiKeyName: "API Key",
            apiKeyDesc: "\u7528\u4E8E AI \u6E05\u7406\u4E0E\u94FE\u63A5\u89E3\u6790\u5931\u8D25\u65F6\u7684 AI \u56DE\u9000\u3002\u7559\u7A7A\u5219\u4E0D\u8D70 AI\u3002",
            providerKeyLinkPrefix: "\u524D\u5F80 {name} \u83B7\u53D6 \u2192",
            baseUrlName: "Base URL\uFF08\u53EF\u9009\uFF09",
            baseUrlDesc: "\u7559\u7A7A\u4F7F\u7528\u9884\u8BBE\u5730\u5740\u3002\u5F53\u524D\u751F\u6548: {value}",
            modelName: "\u6A21\u578B\u540D\uFF08\u53EF\u9009\uFF09",
            modelDesc: "\u7559\u7A7A\u4F7F\u7528\u9884\u8BBE\u6A21\u578B\u3002\u5F53\u524D\u751F\u6548: {value}",
            aiCleanupName: "\u542F\u7528 AI \u6E05\u7406",
            aiCleanupDesc: "\u5F00\u542F\u540E\u81EA\u52A8\u53BB\u9664\u8BED\u6C14\u8BCD\uFF08\u55EF\u3001\u554A\u3001\u90A3\u4E2A\u7B49\uFF09\u3002\u5173\u95ED\u5219\u76F4\u63A5\u8BB0\u5F55\u539F\u6587\u3002",
            urlSummaryName: "\u542F\u7528\u94FE\u63A5\u89E3\u6790",
            urlSummaryDesc: "\u4F18\u5148\u8D70\u56FD\u5185\u89E3\u6790\u670D\u52A1\uFF08\u5929\u805A/\u4E07\u7EF4\u6613\u6E90/\u5495\u5495\u6570\u636E\uFF09\uFF0C\u5931\u8D25\u540E\u81EA\u52A8\u56DE\u9000 AI\uFF0C\u518D\u56DE\u9000\u7EAF\u6587\u672C\u3002",
            resolverSwitchName: "\u89E3\u6790\u670D\u52A1\u603B\u5F00\u5173",
            resolverSwitchDesc: "\u5173\u95ED\u540E\u4E0D\u8BF7\u6C42\u4EFB\u4F55\u94FE\u63A5\u89E3\u6790\u670D\u52A1\u3002",
            resolverProviderName: "\u94FE\u63A5\u89E3\u6790\u670D\u52A1\u5546",
            resolverProviderDesc: "\u4E09\u9009\u4E00\u914D\u7F6E\u5373\u53EF\uFF0C\u63D2\u4EF6\u53EA\u4F1A\u4F7F\u7528\u5F53\u524D\u9009\u4E2D\u7684\u670D\u52A1\u5546\u3002",
            resolverEntryPrefix: "\u914D\u7F6E\u5165\u53E3\uFF1A",
            resolverBuyKey: "\u7533\u8BF7/\u8D2D\u4E70 Key",
            resolverDocs: "\u63A5\u53E3\u6587\u6863",
            resolverEntrySuffix: "\u3002\u82E5\u76EE\u6807\u7F51\u9875\u53CD\u722C\u6216\u52A8\u6001\u52A0\u8F7D\u5931\u8D25\uFF0C\u5C06\u81EA\u52A8\u964D\u7EA7\u5230 AI\uFF0C\u518D\u964D\u7EA7\u5230\u539F\u6587\u4FDD\u7559\u3002",
            timeoutName: "\u89E3\u6790\u8D85\u65F6(ms)",
            timeoutDesc: "\u5355\u6B21\u89E3\u6790\u8BF7\u6C42\u8D85\u65F6\uFF0C\u9ED8\u8BA4 25000\u3002",
            retriesName: "\u5931\u8D25\u91CD\u8BD5\u6B21\u6570",
            retriesDesc: "\u5355\u670D\u52A1\u91CD\u8BD5\u6B21\u6570\uFF0C\u9ED8\u8BA4 2\u3002",
            concurrencyName: "\u6700\u5927\u5E76\u53D1",
            concurrencyDesc: "\u5E76\u53D1\u89E3\u6790 URL \u4E0A\u9650\uFF0C\u9ED8\u8BA4 2\u3002",
            dailyPathName: "\u6BCF\u65E5\u7B14\u8BB0\u8DEF\u5F84",
            dailyPathDesc: "\u65E5\u8BB0\u6587\u4EF6\u5939\u7684\u76F8\u5BF9\u8DEF\u5F84\uFF08\u4E0D\u542B\u6587\u4EF6\u540D\uFF09\u3002",
            headerName: "\u8BB0\u5F55\u533A\u6807\u9898",
            headerDesc: "\u65E5\u8BB0\u4E2D\u7528\u4E8E\u5B58\u653E\u6355\u83B7\u5185\u5BB9\u7684\u533A\u5757\u6807\u9898\uFF08\u9ED8\u8BA4 ## \u8BB0\u5F55\uFF09\u3002",
            testName: "\u6D4B\u8BD5\u8FDE\u63A5",
            testDesc: "\u9A8C\u8BC1 AI \u670D\u52A1\u662F\u5426\u53EF\u7528\u3002",
            testBtn: "\u6D4B\u8BD5",
            testBusy: "\u6D4B\u8BD5\u4E2D..."
          },
          url: {
            statusProviderMissing: "\u26A0\uFE0F {providerName} \u672A\u914D\u7F6E Key\uFF0C\u5DF2\u56DE\u9000 AI",
            statusNoResolverOrAi: "\u26A0\uFE0F \u672A\u914D\u7F6E\u89E3\u6790\u6216 AI\uFF0C\u5DF2\u56DE\u9000\u7EAF\u6587\u672C",
            statusAiSummary: "\u{1F916} \u751F\u6210\u94FE\u63A5\u6458\u8981\u2026",
            statusFallbackAi: "\u26A0\uFE0F {hint}\uFF0C\u5DF2\u56DE\u9000 AI",
            statusFallbackPlain: "\u26A0\uFE0F {hint}\uFF0C\u5DF2\u56DE\u9000\u7EAF\u6587\u672C",
            statusResolverNoAi: "\u26A0\uFE0F \u5DF2\u89E3\u6790\u94FE\u63A5\u4F46\u672A\u914D\u7F6E AI\uFF0C\u5DF2\u56DE\u9000\u7EAF\u6587\u672C",
            statusAiSummaryFailed: "\u26A0\uFE0F AI \u6458\u8981\u5931\u8D25\uFF0C\u5DF2\u56DE\u9000\u7EAF\u6587\u672C",
            statusPartialResolverFailed: "\u26A0\uFE0F \u90E8\u5206\u94FE\u63A5\u89E3\u6790\u5931\u8D25\uFF0C\u5DF2\u4F7F\u7528\u53EF\u7528\u7ED3\u679C"
          },
          parser: {
            originalUrlPlaceholder: "\u539F\u59CBURL",
            originalTextPrefix: "\u539F\u6587",
            summaryFallback: "\u6682\u65E0\u6CD5\u89E3\u6790\uFF0C\u5DF2\u4FDD\u7559\u539F\u59CB\u94FE\u63A5",
            summaryPrefix: "\u94FE\u63A5\u6458\u8981",
            linkLabel: "\u94FE\u63A5{index}",
            untitled: "\uFF08\u65E0\u6807\u9898\uFF09",
            empty: "\uFF08\u7A7A\uFF09"
          },
          template: {
            daily: "# {{date}}\n\n## \u{1F4CB} \u4ECA\u65E5\u8BA1\u5212\n- [ ]\n\n## \u{1F4DD} \u4ECA\u65E5\u8BB0\u5F55\n\n### \u{1F4A1} \u60F3\u6CD5\u548C\u7075\u611F\n\n### \u{1F4D6} \u5B66\u4E60\u7B14\u8BB0\n\n## \u{1F504} \u6BCF\u65E5\u56DE\u987E\n- \u4ECA\u5929\u505A\u4E86\u4EC0\u4E48\uFF1A\n- \u660E\u5929\u8BA1\u5212\uFF1A\n",
            recordHeading: "## \u{1F4DD} \u4ECA\u65E5\u8BB0\u5F55"
          },
          prompts: {
            cleanup: "\u4F60\u662F\u4E00\u4E2A\u6587\u5B57\u6E05\u7406\u52A9\u624B\u3002\u4F60\u7684\u552F\u4E00\u4EFB\u52A1\u662F\u53BB\u9664\u53E3\u8BED\u4E2D\u7684\u8BED\u6C14\u8BCD\u548C\u586B\u5145\u8BCD\uFF08\u5982\uFF1A\u55EF\u3001\u554A\u3001\u90A3\u4E2A\u3001\u5C31\u662F\u3001\u7136\u540E\u3001\u5BF9\u3001\u54E6\u3001emmm\u3001\u989D \u7B49\uFF09\uFF0C\n\u8BA9\u53E5\u5B50\u66F4\u7B80\u6D01\u3002\n\u89C4\u5219\uFF1A\n1. \u53EA\u53BB\u9664\u8BED\u6C14\u8BCD\u548C\u586B\u5145\u8BCD\n2. \u4E0D\u8981\u6539\u5199\u3001\u6DA6\u8272\u6216\u7F8E\u5316\u539F\u6587\n3. \u4E0D\u8981\u6DFB\u52A0\u4EFB\u4F55\u65B0\u5185\u5BB9\n4. \u4E0D\u8981\u6539\u53D8\u539F\u6587\u7684\u610F\u601D\u548C\u8868\u8FBE\u65B9\u5F0F\n5. \u4FDD\u7559\u6240\u6709\u5B9E\u8D28\u5185\u5BB9\u548C\u539F\u59CB\u7528\u8BCD\n6. \u4FDD\u7559\u6240\u6709 URL \u94FE\u63A5\uFF0C\u539F\u6837\u8F93\u51FA\uFF0C\u7EDD\u5BF9\u4E0D\u8981\u6539\u52A8\u3001\u89E3\u91CA\u6216\u56DE\u590D URL \u5185\u5BB9\n7. \u76F4\u63A5\u8FD4\u56DE\u6E05\u7406\u540E\u7684\u6587\u672C\uFF0C\u4E0D\u8981\u4EFB\u4F55\u89E3\u91CA\u6216\u524D\u7F00",
            urlSummary: "\u4F60\u662F\u4E00\u4E2A\u94FE\u63A5\u6458\u8981\u52A9\u624B\u3002\u7528\u6237\u6587\u672C\u4E2D\u5305\u542B URL\uFF0C\u6211\u5DF2\u7ECF\u6293\u53D6\u4E86\u5BF9\u5E94\u9875\u9762\u5185\u5BB9\u3002\n\u8BF7\u8F93\u51FA\uFF1A\u4FDD\u7559\u539F\u6587\u4E0D\u6539\u52A8\uFF0C\u5E76\u5728\u672B\u5C3E\u8FFD\u52A0\u6458\u8981\u5217\u8868\u3002\n\u683C\u5F0F\uFF1A\u6BCF\u6761\u4E00\u884C `> \u{1F4CE} \u539F\u59CBURL - \u6458\u8981`\n\u89C4\u5219\uFF1A\n- \u5FC5\u987B\u4FDD\u7559\u539F\u6587\u4E2D\u7684\u6240\u6709\u539F\u59CB URL\uFF0C\u4E0D\u80FD\u66FF\u6362\u3001\u5220\u6539\u3001\u7F29\u77ED\n- \u6458\u8981\u4E0D\u8D85\u8FC7 50 \u5B57\n- \u4E0D\u8981\u6539\u52A8 URL \u4EE5\u5916\u7684\u539F\u6587\u5185\u5BB9\n- \u5982\u679C\u5185\u5BB9\u4E0D\u8DB3\uFF0C\u5199\u201C\u6682\u65E0\u6CD5\u89E3\u6790\uFF0C\u5DF2\u4FDD\u7559\u539F\u59CB\u94FE\u63A5\u201D\n- \u76F4\u63A5\u8FD4\u56DE\u5904\u7406\u540E\u7684\u5B8C\u6574\u6587\u672C\uFF0C\u4E0D\u8981\u89E3\u91CA",
            urlFallback: "\u4F60\u662F\u4E00\u4E2A\u94FE\u63A5\u964D\u7EA7\u52A9\u624B\u3002\n\u4EFB\u52A1\uFF1A\u5728\u65E0\u6CD5\u83B7\u53D6\u7F51\u9875\u6B63\u6587\u65F6\uFF0C\u5BF9\u539F\u6587\u505A\u6700\u5C0F\u5316\u5904\u7406\u3002\n\u89C4\u5219\uFF1A\n1. \u539F\u6587\u5FC5\u987B\u5B8C\u6574\u4FDD\u7559\uFF0C\u6240\u6709 URL \u5FC5\u987B\u4FDD\u7559\u539F\u6837\n2. \u4E0D\u5141\u8BB8\u6539\u5199\u539F\u6587\uFF0C\u4E0D\u5141\u8BB8\u7F16\u9020\u7F51\u9875\u5185\u5BB9\n3. \u4EC5\u53EF\u5728\u672B\u5C3E\u8FFD\u52A0\u63D0\u793A\u884C\uFF0C\u683C\u5F0F `> \u{1F4CE} \u539F\u59CBURL - \u6682\u65E0\u6CD5\u89E3\u6790\uFF0C\u5DF2\u4FDD\u7559\u539F\u59CB\u94FE\u63A5`\n4. \u76F4\u63A5\u8FD4\u56DE\u5904\u7406\u540E\u7684\u5B8C\u6574\u6587\u672C"
          }
        }
      },
      en: {
        commands: {
          open: "Open",
          sendSelectedText: "Send Selected Text",
          newSession: "New Session",
          mobileQuickCapture: "Quick Idea Capture"
        },
        notices: {
          pickTextFirst: "Select text first.",
          pluginLoadFailed: "FLOWnote failed to load: {message}",
          mobileLoadFailed: "FLOWnote mobile failed to load: {message}",
          captureSaved: "\u2705 Idea captured",
          captureFailed: "Capture failed: {message}",
          needApiKeyFirst: "Please fill API Key first.",
          languageAppliedReloadTip: "UI language updated. Command names and Ribbon tooltip apply after plugin reload."
        },
        errors: {
          localFsOnly: "Only local filesystem vaults are supported.",
          aiMissingConfig: "AI is not configured: missing Base URL or API Key.",
          aiResponseEmpty: "AI returned empty content.",
          resolverUnsupported: "Unsupported resolver provider: {providerId}",
          resolverInvalidJson: "Resolver response is not valid JSON.",
          resolverBodyEmpty: "Empty page content.",
          resolverFailed: "Resolver failed",
          resolverFailedGeneral: "URL resolver failed",
          resolverRateLimited: "Rate limited",
          resolverTimeout: "Resolver timed out",
          resolverUnavailable: "Resolver unavailable"
        },
        settings: {
          language: {
            name: "UI Language",
            desc: "Default follows device language. UI updates immediately; command names and Ribbon tooltip update after plugin reload.",
            optionAuto: "Follow System (Recommended)",
            optionZhCN: "\u7B80\u4F53\u4E2D\u6587",
            optionEn: "English",
            reinstallPromptTitle: "Reinstall language-specific Skills?",
            reinstallPromptDesc: "UI language is now {language}. Reinstall bundled Skills & Templates for this language now?",
            reinstallPromptConfirm: "Reinstall Now",
            reinstallPromptCancel: "Later"
          },
          mobile: {
            intro: "Configure AI and note paths for quick mobile idea capture."
          },
          basic: {
            intro: "In most cases, you only need to verify connection status and Provider auth. Keep other advanced options at default.",
            cliPathName: "FLOWnote CLI Path (Optional)",
            cliPathDesc: "Usually leave empty. The plugin auto-detects it. On Windows, install Node.js first, then run npm install -g opencode-ai. If needed, prefer opencode.exe or cli.js (do not use opencode.cmd or WSL).",
            launchStrategyName: "Connection Launch Strategy",
            launchStrategyDescWindows: "Auto (Recommended): detect and remember the working native launch path. Manual mode only uses native Windows installs; WSL is no longer supported.",
            launchStrategyDesc: "Auto (Recommended): try last successful method first and fallback automatically on failure.",
            launchAuto: "Auto (Recommended)",
            launchNativeWindows: "Native Windows Install",
            launchWsl: "Windows WSL Install",
            launchNativeMac: "Native Mac Install",
            wslDistroName: "WSL Distro (Optional)",
            wslDistroDesc: "Leave empty for default WSL distro. You can set Ubuntu / Debian etc. Filled value is preferred in auto mode.",
            skillInjectModeName: "Skill Injection Mode",
            skillInjectModeDesc: "How plugin injects skill content when you use /skill command.",
            skillInjectModeSummary: "Summary Injection (Recommended)",
            skillInjectModeFull: "Full Injection (More complete but heavier)",
            skillInjectModeOff: "Disable Injection (Send only user input)",
            advancedHeading: "Advanced Settings",
            skillsDirName: "Bundled Skills Install Directory",
            skillsDirDesc: "Default is .opencode/skills. Bundled skills are installed/updated automatically, and any skills you add or edit in this directory are available to slash commands.",
            reinstallSkillsName: "Reinstall Bundled Skills & Templates",
            reinstallSkillsDesc: "Install/update bundled skills for the current UI language and sync Meta/Templates into each skill resource folder. If conflicts exist, you can replace or skip.",
            reinstallSkillsNow: "Reinstall Now",
            reinstallSkillsBusy: "Reinstalling...",
            reinstallSkillsSuccess: "Reinstall complete: skills {synced}/{total}, templates {syncedTemplates}/{totalTemplates}, dir {targetRoot}",
            reinstallSkillsSuccessWithMeta: "Reinstall complete: skills {synced}/{total}, templates {syncedTemplates}/{totalTemplates}, meta {syncedMetaTemplates}/{totalMetaTemplates}, dir {targetRoot}",
            reinstallSkillsCanceled: "Reinstall canceled. Processed skills {synced}/{total}, templates {syncedTemplates}/{totalTemplates}.",
            reinstallSkillsFailed: "Reinstall failed: {message}",
            resetTemplateBaselineName: "Reset Template Baseline",
            resetTemplateBaselineDesc: "Use only when you need defaults restored. It writes built-in templates back to Meta/Templates (conflicts are replace/skip).",
            resetTemplateBaselineNow: "Reset Templates",
            resetTemplateBaselineBusy: "Resetting...",
            resetTemplateBaselineSuccess: "Template reset complete: {synced}/{total}, dir {metaRoot}",
            resetTemplateBaselineCanceled: "Template reset canceled. Processed {synced}/{total}.",
            resetTemplateBaselineFailed: "Template reset failed: {message}",
            contentConflictTitle: "Name Conflict Detected",
            contentConflictDesc: "{kind} `{id}` already exists. Choose how to proceed.",
            contentConflictTarget: "Target: {path}",
            contentConflictSource: "Source: {path}",
            conflictKindSkill: "Skill",
            conflictKindTemplate: "Template",
            conflictKindMetaTemplate: "Meta Template",
            conflictReplace: "Replace",
            conflictSkip: "Skip",
            conflictReplaceAll: "Replace All",
            conflictSkipAll: "Skip All",
            conflictCancel: "Cancel",
            unknownError: "Unknown error",
            diagnosticsName: "Connection Diagnostics",
            diagnosticsDesc: "Check FLOWnote executable and connection status.",
            diagnosticsRun: "Run Diagnostics",
            diagnosticsBusy: "Testing...",
            diagnosticsOk: "Connection healthy ({mode})",
            diagnosticsFailed: "Connection failed: {error}",
            autoMemoryRememberedWsl: "Remembered: WSL{distro}",
            autoMemoryRememberedNative: "Remembered: Native {command}",
            autoMemoryNone: "No successful connection method remembered yet.",
            autoMemoryName: "Auto Connection Memory",
            autoMemoryDesc: "{rememberedText} It updates automatically after successful connections.",
            autoMemoryReset: "Reset Memory",
            autoMemoryResetDone: "Remembered connection method has been cleared.",
            autoMemoryResetFailed: "Reset failed: {message}"
          },
          mobileCapture: {
            heading: "Mobile Quick Capture",
            intro: "Pre-configure mobile capture settings on desktop. It can be used on mobile after sync.",
            resolverHint: {
              tianapi: "Suitable for basic webpage content extraction; dynamic or anti-crawl pages may fail.",
              showapi: "Usage-based billing with some free quota on selected plans; good low-barrier option.",
              gugudata: "Stable Markdown quality output; official docs recommend rate control."
            },
            resolverProvider: {
              tianapi: { name: "TianAPI", keyLabel: "TianAPI Key" },
              showapi: { name: "ShowAPI", keyLabel: "ShowAPI AppKey" },
              gugudata: { name: "Gugudata", keyLabel: "Gugudata AppKey" }
            }
          },
          providerAuth: {
            heading: "Provider Auth Management (OAuth / API Key)",
            intro: "Connect official/third-party model providers inside the plugin. Credentials are written to FLOWnote runtime auth storage.",
            refresh: "Refresh Provider Status",
            expandAll: "Expand All",
            collapseAll: "Collapse All",
            searchPlaceholder: "Search Provider / ID / Auth Method / Model Count",
            loading: "Loading...",
            notLoaded: "Provider info has not been loaded yet.",
            loadedNoResult: 'Loaded {totalProviders} providers, no result for "{query}".',
            loaded: "Loaded {totalProviders} providers.",
            loadedWithCount: "Loaded {totalProviders} providers, showing {visibleCount}; connected {connectedCount}.",
            noMatch: "No matching provider. Try another keyword.",
            empty: "No providers to display.",
            groupCommon: "Common",
            groupOthers: "Others",
            groupDomestic: "Domestic Providers",
            groupGlobal: "Global Providers",
            groupUnknownCountry: "Other / Unknown Country",
            refreshBusy: "Refreshing...",
            loadingProviders: "Reading Provider configuration...",
            readFailed: "Read failed: {message}",
            readFailedDetail: "Failed to read Provider info: {message}",
            connected: "Connected",
            disconnected: "Disconnected",
            providerMeta: "Models: {modelCount}; Auth: {methodText}",
            actionFailed: "Action failed: {message}",
            oauthLogin: "OAuth Login",
            pending: "Processing...",
            setApiKey: "Set API Key",
            saving: "Saving...",
            clearLogin: "Clear Login",
            clearing: "Clearing...",
            clearTitle: "Clear {providerName} Login",
            clearDesc: "Clear credentials for this Provider?",
            clearSubmit: "Clear",
            clearDone: "Credentials cleared.",
            clearNotice: "{providerName} credentials cleared",
            pickOauthTitle: "Choose OAuth Method for {providerName}",
            pickOauthDesc: "Please choose one OAuth auth method.",
            continue: "Continue",
            invalidOauthMethod: "Invalid OAuth method index",
            oauthCanceled: "OAuth login canceled.",
            oauthCreatingLink: "Creating authorization link...",
            oauthMissingUrl: "Failed to get OAuth authorization URL",
            oauthOpened: "{providerName} authorization page opened",
            oauthCopied: "Cannot open browser automatically. Authorization URL copied to clipboard.",
            oauthCallbackTitle: "{providerName} OAuth Callback",
            oauthCallbackDesc: "Complete authorization in browser and paste the code.",
            oauthCallbackPlaceholder: "Paste authorization code",
            submit: "Submit",
            oauthCodeMissing: "Authorization code is empty. Canceled.",
            oauthCompleteInBrowser: "Complete authorization in browser.",
            oauthThenConfirm: 'After completion, click "Confirm" to continue.',
            confirm: "Confirm",
            oauthSubmitting: "Submitting OAuth callback...",
            oauthDone: "OAuth login complete.",
            oauthSuccess: "{providerName} login successful",
            apiKeyTitle: "Set {providerName} API Key",
            apiKeyDesc: "Enter Provider API Key (stored only in local FLOWnote credentials).",
            apiKeyPlaceholder: "Enter API Key",
            save: "Save",
            apiKeyCanceled: "API Key setup canceled.",
            apiKeyEmpty: "API Key cannot be empty.",
            apiKeySaving: "Saving API Key...",
            apiKeySaved: "API Key saved.",
            apiKeySavedNotice: "{providerName} API Key saved",
            noAuthMethods: "No auth methods"
          }
        },
        view: {
          header: {
            runtime: "Chat Runtime"
          },
          welcome: {
            greeting: "What would you like to organize today?",
            empty: "Send a message, or type / to pick a skill command."
          },
          session: {
            current: "Current session: {title}"
          },
          input: {
            placeholder: "Type your message..."
          },
          settings: "Settings"
        },
        mobile: {
          providers: {
            deepseek: "DeepSeek",
            qwen: "Qwen",
            moonshot: "Moonshot (Kimi)",
            zhipu: "Zhipu (GLM)",
            siliconflow: "SiliconFlow",
            custom: "Custom"
          },
          capture: {
            title: "\u{1F4A1} Quick Capture",
            inputPlaceholder: "What are you thinking now?",
            hintSend: "\u2318/Ctrl + Enter to send",
            cancel: "Cancel",
            submit: "Capture",
            submitBusy: "Capturing...",
            statusAiCleanup: "\u{1F916} Cleaning text...",
            statusAiCleanupFailed: "\u26A0\uFE0F AI cleanup failed, using original text",
            statusResolver: "\u{1F517} Resolving URLs...",
            statusResolverFailed: "\u26A0\uFE0F URL resolve failed, fallback to original: {message}",
            statusWriteNote: "\u{1F4DD} Writing daily note...",
            emptyInput: "Please input content."
          },
          settings: {
            providerName: "AI Provider",
            providerDesc: "Choose a preset provider or custom endpoint.",
            apiKeyName: "API Key",
            apiKeyDesc: "Used for AI cleanup and resolver fallback. Leave empty to skip AI.",
            providerKeyLinkPrefix: "Get key from {name} \u2192",
            baseUrlName: "Base URL (Optional)",
            baseUrlDesc: "Leave empty to use preset. Effective: {value}",
            modelName: "Model (Optional)",
            modelDesc: "Leave empty to use preset. Effective: {value}",
            aiCleanupName: "Enable AI Cleanup",
            aiCleanupDesc: "Automatically removes filler words. Disable to keep original text.",
            urlSummaryName: "Enable URL Resolve",
            urlSummaryDesc: "Use resolver first, then AI fallback, then plain text fallback.",
            resolverSwitchName: "Resolver Master Switch",
            resolverSwitchDesc: "Disable to skip all URL resolver requests.",
            resolverProviderName: "URL Resolver Provider",
            resolverProviderDesc: "Pick one provider. Plugin only uses selected provider.",
            resolverEntryPrefix: "Links:",
            resolverBuyKey: "Get/Buy Key",
            resolverDocs: "API Docs",
            resolverEntrySuffix: ". If page fetch fails, fallback to AI, then plain text.",
            timeoutName: "Timeout (ms)",
            timeoutDesc: "Single resolver request timeout, default 25000.",
            retriesName: "Retry Count",
            retriesDesc: "Retries per provider, default 2.",
            concurrencyName: "Max Concurrency",
            concurrencyDesc: "Max concurrent URL resolves, default 2.",
            dailyPathName: "Daily Note Path",
            dailyPathDesc: "Relative folder path for daily notes.",
            headerName: "Record Section Header",
            headerDesc: "Section header used for captured content (default: ## Records).",
            testName: "Test Connection",
            testDesc: "Verify AI service availability.",
            testBtn: "Test",
            testBusy: "Testing..."
          },
          url: {
            statusProviderMissing: "\u26A0\uFE0F {providerName} key missing, fallback to AI",
            statusNoResolverOrAi: "\u26A0\uFE0F Resolver and AI not configured, fallback to plain text",
            statusAiSummary: "\u{1F916} Generating URL summary...",
            statusFallbackAi: "\u26A0\uFE0F {hint}, fallback to AI",
            statusFallbackPlain: "\u26A0\uFE0F {hint}, fallback to plain text",
            statusResolverNoAi: "\u26A0\uFE0F URL resolved but AI not configured, fallback to plain text",
            statusAiSummaryFailed: "\u26A0\uFE0F AI summary failed, fallback to plain text",
            statusPartialResolverFailed: "\u26A0\uFE0F Some URLs failed, partial result applied"
          },
          parser: {
            originalUrlPlaceholder: "OriginalURL",
            originalTextPrefix: "Original",
            summaryFallback: "Unable to resolve, original URL preserved",
            summaryPrefix: "URL Summary",
            linkLabel: "Link {index}",
            untitled: "(Untitled)",
            empty: "(Empty)"
          },
          template: {
            daily: "# {{date}}\n\n## \u{1F4CB} Today Plan\n- [ ]\n\n## \u{1F4DD} Today Notes\n\n### \u{1F4A1} Ideas\n\n### \u{1F4D6} Learning Notes\n\n## \u{1F504} Daily Review\n- What I did today:\n- Plan for tomorrow:\n",
            recordHeading: "## \u{1F4DD} Today Notes"
          },
          prompts: {
            cleanup: "You are a text cleanup assistant. Your only task is to remove filler words from spoken text (such as um, uh, like, you know, etc.) and keep the sentence concise.\nRules:\n1. Only remove filler words\n2. Do not rewrite, polish, or beautify the text\n3. Do not add new content\n4. Keep original meaning and wording\n5. Preserve all substantive content\n6. Preserve all URLs exactly as-is\n7. Return cleaned text only, no explanation",
            urlSummary: 'You are a URL summary assistant. User text includes URLs and page content has been fetched.\nOutput the original text unchanged, and append summary lines.\nFormat: each line is `> \u{1F4CE} OriginalURL - Summary`\nRules:\n- Keep all original URLs unchanged\n- Summary <= 50 chars\n- Do not alter non-URL content\n- If insufficient content, use "Unable to resolve, original URL preserved"\n- Return full processed text without explanation',
            urlFallback: "You are a URL fallback assistant.\nTask: apply minimal processing when page content is unavailable.\nRules:\n1. Preserve original text and all URLs exactly\n2. Do not rewrite or fabricate page content\n3. You may only append lines like `> \u{1F4CE} OriginalURL - Unable to resolve, original URL preserved`\n4. Return processed full text directly"
          }
        }
      }
    };
    module2.exports = { I18N_MESSAGES: I18N_MESSAGES2 };
  }
});

// runtime/i18n-runtime.js
var require_i18n_runtime = __commonJS({
  "runtime/i18n-runtime.js"(exports2, module2) {
    function interpolateTemplate2(template, params = {}) {
      return String(template || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_match, name) => {
        if (!Object.prototype.hasOwnProperty.call(params, name)) return "";
        const value = params[name];
        return value === void 0 || value === null ? "" : String(value);
      });
    }
    function resolvePluginFromContext(context) {
      if (!context || typeof context !== "object") return null;
      if (context.plugin && typeof context.plugin === "object") return context.plugin;
      if (typeof context.t === "function" || typeof context.getEffectiveLocale === "function") return context;
      return null;
    }
    function tFromContext(context, key, fallback, params = {}, options = {}) {
      const plugin = resolvePluginFromContext(context);
      if (plugin && typeof plugin.t === "function") {
        return plugin.t(key, params, { ...options, defaultValue: fallback });
      }
      return interpolateTemplate2(fallback, params);
    }
    module2.exports = {
      interpolateTemplate: interpolateTemplate2,
      resolvePluginFromContext,
      tFromContext
    };
  }
});

// runtime/modals.js
var require_modals = __commonJS({
  "runtime/modals.js"(exports2, module2) {
    var { Modal: Modal2 } = require("obsidian");
    var { interpolateTemplate: interpolateTemplate2 } = require_i18n_runtime();
    function tr(tFn, key, fallback, params = {}) {
      if (typeof tFn === "function") return tFn(key, params, { defaultValue: fallback });
      return interpolateTemplate2(fallback, params);
    }
    function defaultFormatForDisplay(value, maxLen = 2200) {
      if (value === void 0 || value === null) return "";
      let out = "";
      if (typeof value === "string") {
        out = value;
      } else {
        try {
          out = JSON.stringify(value, null, 2);
        } catch {
          out = String(value);
        }
      }
      if (out.length <= maxLen) return out;
      return `${out.slice(0, maxLen)}
...(${out.length - maxLen} chars truncated)`;
    }
    var DiagnosticsModal = class extends Modal2 {
      constructor(app, result, t) {
        super(app);
        this.result = result;
        this.t = t;
      }
      onOpen() {
        const { contentEl } = this;
        contentEl.empty();
        contentEl.addClass("oc-diagnostics-modal");
        contentEl.createEl("h2", { text: tr(this.t, "modals.diagnostics.title", "FLOWnote Diagnostics") });
        if (!this.result) {
          contentEl.createEl("p", { text: tr(this.t, "modals.diagnostics.notRun", "Diagnostics has not run yet.") });
          return;
        }
        const conn = this.result.connection;
        const exe = this.result.executable;
        contentEl.createEl("h3", { text: tr(this.t, "modals.diagnostics.connection", "Connection") });
        contentEl.createEl("p", {
          text: conn.ok ? tr(this.t, "modals.diagnostics.connectionOk", "OK ({mode})", conn) : tr(this.t, "modals.diagnostics.connectionFailed", "Failed ({mode})", conn)
        });
        if (conn.error) contentEl.createEl("pre", { text: conn.error });
        contentEl.createEl("h3", { text: tr(this.t, "modals.diagnostics.executable", "Executable Detection") });
        contentEl.createEl("p", {
          text: exe.ok ? tr(this.t, "modals.diagnostics.executableFound", "Found: {path}", exe) : tr(this.t, "modals.diagnostics.executableMissing", "Not found")
        });
        if (exe.hint) contentEl.createEl("p", { text: exe.hint });
        const attempts = contentEl.createEl("details");
        attempts.createEl("summary", {
          text: tr(this.t, "modals.diagnostics.attempted", "Attempted Paths ({count})", { count: (exe.attempted || []).length })
        });
        attempts.createEl("pre", { text: (exe.attempted || []).join("\n") || tr(this.t, "modals.none", "(none)") });
      }
      onClose() {
        this.contentEl.empty();
      }
    };
    var PermissionRequestModal = class extends Modal2 {
      constructor(app, permission, onResolve, formatForDisplay, t) {
        super(app);
        this.permission = permission || {};
        this.onResolve = onResolve;
        this.formatForDisplay = typeof formatForDisplay === "function" ? formatForDisplay : defaultFormatForDisplay;
        this.t = t;
        this.resolved = false;
      }
      resolveAndClose(value) {
        if (this.resolved) return;
        this.resolved = true;
        if (typeof this.onResolve === "function") this.onResolve(value);
        this.close();
      }
      onOpen() {
        const { contentEl } = this;
        contentEl.empty();
        contentEl.addClass("oc-perm-modal");
        contentEl.createEl("h2", { text: tr(this.t, "modals.permission.title", "FLOWnote Permission Request") });
        const title = typeof this.permission.title === "string" && this.permission.title.trim() || tr(this.t, "modals.permission.defaultTitle", "Model requested a restricted operation");
        contentEl.createDiv({ cls: "oc-perm-title", text: title });
        const meta = [];
        if (typeof this.permission.type === "string" && this.permission.type) {
          meta.push(tr(this.t, "modals.permission.type", "Type: {value}", { value: this.permission.type }));
        }
        if (this.permission.pattern) {
          meta.push(tr(this.t, "modals.permission.pattern", "Pattern: {value}", {
            value: this.formatForDisplay(this.permission.pattern, 400)
          }));
        }
        if (meta.length) {
          contentEl.createEl("pre", { cls: "oc-perm-meta", text: meta.join("\n") });
        }
        const details = contentEl.createEl("details", { cls: "oc-perm-details" });
        details.createEl("summary", { text: tr(this.t, "modals.permission.metadata", "View full metadata") });
        details.createEl("pre", {
          text: this.formatForDisplay(this.permission.metadata || {}, 2400) || tr(this.t, "modals.empty", "(empty)")
        });
        const actions = contentEl.createDiv({ cls: "oc-perm-actions" });
        const rejectBtn = actions.createEl("button", { cls: "mod-muted", text: tr(this.t, "modals.permission.reject", "Reject") });
        const onceBtn = actions.createEl("button", { cls: "mod-cta", text: tr(this.t, "modals.permission.once", "Allow Once") });
        const alwaysBtn = actions.createEl("button", { text: tr(this.t, "modals.permission.always", "Always Allow (Session)") });
        rejectBtn.addEventListener("click", () => this.resolveAndClose("reject"));
        onceBtn.addEventListener("click", () => this.resolveAndClose("once"));
        alwaysBtn.addEventListener("click", () => this.resolveAndClose("always"));
      }
      onClose() {
        if (!this.resolved && typeof this.onResolve === "function") {
          this.onResolve(null);
        }
        this.contentEl.empty();
      }
    };
    var PromptAppendModal = class extends Modal2 {
      constructor(app, promptText, onSubmit, t) {
        super(app);
        this.promptText = String(promptText || "");
        this.onSubmit = onSubmit;
        this.t = t;
      }
      onOpen() {
        const { contentEl } = this;
        contentEl.empty();
        contentEl.addClass("oc-prompt-modal");
        contentEl.createEl("h2", { text: tr(this.t, "modals.append.title", "Model Requested Additional Input") });
        contentEl.createDiv({
          cls: "oc-prompt-desc",
          text: tr(
            this.t,
            "modals.append.desc",
            "FLOWnote requested more input via question/tool. You can edit and insert into the input box."
          )
        });
        const input = contentEl.createEl("textarea", {
          cls: "oc-prompt-input",
          text: this.promptText
        });
        const actions = contentEl.createDiv({ cls: "oc-prompt-actions" });
        const cancelBtn = actions.createEl("button", { cls: "mod-muted", text: tr(this.t, "modals.cancel", "Cancel") });
        const useBtn = actions.createEl("button", { cls: "mod-cta", text: tr(this.t, "modals.append.useInput", "Insert into Input") });
        cancelBtn.addEventListener("click", () => this.close());
        useBtn.addEventListener("click", () => {
          if (typeof this.onSubmit === "function") this.onSubmit(String(input.value || ""));
          this.close();
        });
        setTimeout(() => input.focus(), 0);
      }
      onClose() {
        this.contentEl.empty();
      }
    };
    var ModelSelectorModal = class extends Modal2 {
      constructor(app, options) {
        super(app);
        this.options = options || {};
        this.filterText = "";
        this.listEl = null;
      }
      onOpen() {
        const { contentEl } = this;
        contentEl.empty();
        contentEl.addClass("oc-model-modal");
        const tFn = this.options && typeof this.options.t === "function" ? this.options.t : null;
        contentEl.createEl("h2", { text: tr(tFn, "modals.model.title", "Select Model") });
        contentEl.createDiv({
          cls: "oc-model-modal-subtitle",
          text: tr(tFn, "modals.model.subtitle", "Use official model list from FLOWnote provider configuration")
        });
        const search = contentEl.createEl("input", {
          cls: "oc-model-search",
          attr: { type: "text", placeholder: tr(tFn, "modals.model.search", "Search provider/model...") }
        });
        search.addEventListener("input", () => {
          this.filterText = String(search.value || "").trim().toLowerCase();
          this.renderList();
        });
        const actions = contentEl.createDiv({ cls: "oc-model-modal-actions" });
        const refreshBtn = actions.createEl("button", { text: tr(tFn, "modals.model.refresh", "Refresh List") });
        refreshBtn.addEventListener("click", async () => {
          if (typeof this.options.onRefresh !== "function") return;
          refreshBtn.disabled = true;
          refreshBtn.setText(tr(tFn, "modals.model.refreshing", "Refreshing..."));
          try {
            const refreshed = await this.options.onRefresh();
            if (Array.isArray(refreshed)) this.options.models = refreshed;
          } finally {
            refreshBtn.disabled = false;
            refreshBtn.setText(tr(tFn, "modals.model.refresh", "Refresh List"));
            this.renderList();
          }
        });
        const clearBtn = actions.createEl("button", { text: tr(tFn, "modals.model.resetDefault", "Reset Default") });
        clearBtn.addEventListener("click", async () => {
          if (typeof this.options.onSelect === "function") await this.options.onSelect("");
          this.close();
        });
        this.listEl = contentEl.createDiv({ cls: "oc-model-list" });
        this.renderList();
      }
      renderList() {
        if (!this.listEl) return;
        this.listEl.empty();
        const models = Array.isArray(this.options.models) ? this.options.models : [];
        const filtered = models.filter((item) => {
          if (!this.filterText) return true;
          return String(item || "").toLowerCase().includes(this.filterText);
        });
        if (!filtered.length) {
          const tFn = this.options && typeof this.options.t === "function" ? this.options.t : null;
          this.listEl.createDiv({ cls: "oc-model-empty", text: tr(tFn, "modals.model.notFound", "No matching models") });
          return;
        }
        filtered.forEach((model) => {
          const row = this.listEl.createDiv({ cls: "oc-model-item" });
          if (model === this.options.currentModel) row.addClass("is-active");
          row.createDiv({ cls: "oc-model-item-id", text: model });
          const tFn = this.options && typeof this.options.t === "function" ? this.options.t : null;
          row.createDiv({
            cls: "oc-model-item-meta",
            text: model === this.options.currentModel ? tr(tFn, "modals.model.current", "Current") : tr(tFn, "modals.model.clickToSwitch", "Click to switch")
          });
          row.addEventListener("click", async () => {
            if (typeof this.options.onSelect === "function") await this.options.onSelect(model);
            this.close();
          });
        });
      }
      onClose() {
        this.contentEl.empty();
      }
    };
    module2.exports = {
      DiagnosticsModal,
      ModelSelectorModal,
      PermissionRequestModal,
      PromptAppendModal
    };
  }
});

// runtime/payload/markdown-utils.js
var require_markdown_utils = __commonJS({
  "runtime/payload/markdown-utils.js"(exports2, module2) {
    function normalizedRenderableText(text) {
      return String(text || "").replace(/[\u200B-\u200D\uFEFF]/g, "").trim();
    }
    function normalizeMarkdownSpacing(text) {
      const raw = String(text || "").replace(/\r\n?/g, "\n").replace(/[ \t]+\n/g, "\n").replace(/\n[ \t]+\n/g, "\n\n");
      if (!raw.trim()) return "";
      const segments = raw.split(/(```[\s\S]*?```)/g);
      const normalized = segments.map((segment) => {
        if (!segment) return "";
        if (segment.startsWith("```")) return segment;
        let out = segment;
        out = out.replace(/\n{3,}/g, "\n\n");
        out = out.replace(/\n{2,}(?=[ \t]*(?:[-*+]|\d+\.)\s)/g, "\n");
        out = out.replace(/(^|\n)([ \t]*(?:[-*+]|\d+\.)[^\n]*)\n{2,}(?=[ \t]*(?:[-*+]|\d+\.)\s)/g, "$1$2\n");
        out = out.replace(/\n{3,}/g, "\n\n");
        return out;
      });
      return normalized.join("").trim();
    }
    function normalizeMarkdownForDisplay(text) {
      return normalizeMarkdownSpacing(normalizedRenderableText(text));
    }
    module2.exports = {
      normalizedRenderableText,
      normalizeMarkdownSpacing,
      normalizeMarkdownForDisplay
    };
  }
});

// runtime/payload/assistant-parts-utils.js
var require_assistant_parts_utils = __commonJS({
  "runtime/payload/assistant-parts-utils.js"(exports2, module2) {
    var { normalizedRenderableText } = require_markdown_utils();
    function joinUniqueTextChunks(chunks) {
      const unique = [];
      const seen = /* @__PURE__ */ new Set();
      for (const chunk of Array.isArray(chunks) ? chunks : []) {
        const text = typeof chunk === "string" ? chunk.trim() : "";
        if (!text) continue;
        if (seen.has(text)) continue;
        seen.add(text);
        unique.push(text);
      }
      return unique.join("\n\n");
    }
    function stablePartKey(part, index) {
      if (!part || typeof part !== "object") return `invalid:${index}`;
      if (typeof part.id === "string" && part.id.trim()) return `id:${part.id.trim()}`;
      const type = typeof part.type === "string" && part.type.trim() ? part.type.trim() : "part";
      const messageId = typeof part.messageID === "string" && part.messageID.trim() ? part.messageID.trim() : "";
      const time = part.time && typeof part.time === "object" ? part.time : null;
      const startAt = time ? Number(time.start || time.created || 0) : 0;
      if (startAt > 0) return `${type}:${messageId}:${startAt}`;
      const textPreview = normalizedRenderableText(typeof part.text === "string" ? part.text : "").slice(0, 48);
      return `${type}:${messageId}:${textPreview}:${index}`;
    }
    function overlapSuffixPrefix(left, right, maxWindow = 2048) {
      const a = String(left || "");
      const b = String(right || "");
      const max = Math.min(a.length, b.length, Math.max(32, Number(maxWindow) || 2048));
      for (let len = max; len >= 16; len -= 1) {
        if (a.slice(a.length - len) === b.slice(0, len)) return len;
      }
      return 0;
    }
    function mergeSnapshotText(existingText, incomingText) {
      const existing = String(existingText || "");
      const incoming = String(incomingText || "");
      if (!incoming.trim()) return existing;
      if (!existing.trim()) return incoming;
      if (existing === incoming) return existing;
      if (incoming.includes(existing)) return incoming;
      if (existing.includes(incoming)) return existing;
      const existingTrimmed = existing.trim();
      const incomingTrimmed = incoming.trim();
      if (incomingTrimmed.startsWith(existingTrimmed) || incomingTrimmed.endsWith(existingTrimmed)) return incoming;
      if (existingTrimmed.startsWith(incomingTrimmed) || existingTrimmed.endsWith(incomingTrimmed)) return existing;
      const overlap = overlapSuffixPrefix(existing, incoming);
      if (overlap > 0) return `${existing}${incoming.slice(overlap)}`;
      const reverseOverlap = overlapSuffixPrefix(incoming, existing);
      if (reverseOverlap > 0) return `${incoming}${existing.slice(reverseOverlap)}`;
      return incoming.length >= existing.length ? incoming : existing;
    }
    function collectPartText(chunksByPart, part, index, text) {
      const value = String(text || "");
      if (!value.trim()) return;
      const key = stablePartKey(part, index);
      if (!chunksByPart.has(key)) {
        chunksByPart.set(key, value);
        return;
      }
      const existing = String(chunksByPart.get(key) || "");
      chunksByPart.set(key, mergeSnapshotText(existing, value));
    }
    function joinPartTextChunks(chunksByPart) {
      return Array.from(chunksByPart.values()).map((value) => String(value || "").trim()).filter(Boolean).join("\n\n");
    }
    function stringifyForDisplay(value, maxLen = 2200) {
      if (value === void 0 || value === null) return "";
      let out = "";
      if (typeof value === "string") {
        out = value;
      } else {
        try {
          out = JSON.stringify(value, null, 2);
        } catch {
          out = String(value);
        }
      }
      if (out.length <= maxLen) return out;
      return `${out.slice(0, maxLen)}
...(${out.length - maxLen} chars truncated)`;
    }
    function parseJsonObject(text) {
      if (typeof text !== "string") return null;
      const raw = text.trim();
      if (!raw || !raw.startsWith("{") && !raw.startsWith("[")) return null;
      try {
        const parsed = JSON.parse(raw);
        return parsed && typeof parsed === "object" ? parsed : null;
      } catch {
        return null;
      }
    }
    function makePreviewText(raw, maxLen = 320) {
      const text = typeof raw === "string" ? raw.trim() : "";
      if (!text) return "";
      const normalized = text.replace(/\s+/g, " ").trim();
      if (normalized.length <= maxLen) return normalized;
      return `${normalized.slice(0, maxLen)}...`;
    }
    function clampText(value, maxLen = 1200) {
      const raw = String(value || "");
      const limit = Math.max(64, Number(maxLen) || 1200);
      if (raw.length <= limit) return raw;
      return `${raw.slice(0, limit)}...(${raw.length - limit} chars truncated)`;
    }
    function normalizePatchPath(pathLike) {
      return String(pathLike || "").trim();
    }
    function normalizePatchAction(value) {
      const raw = String(value || "").trim().toLowerCase();
      if (!raw) return "";
      if (["a", "add", "added", "new", "create", "created"].includes(raw)) return "added";
      if (["m", "mod", "modify", "modified", "update", "updated", "change", "changed", "edit", "edited"].includes(raw)) {
        return "modified";
      }
      if (["d", "del", "delete", "deleted", "remove", "removed"].includes(raw)) return "deleted";
      if (["r", "ren", "rename", "renamed", "move", "moved"].includes(raw)) return "renamed";
      if (["c", "copy", "copied"].includes(raw)) return "copied";
      return "";
    }
    function normalizePatchFileEntry(entry) {
      if (entry === null || entry === void 0) return null;
      if (typeof entry === "string") {
        const text = normalizePatchPath(entry);
        if (!text) return null;
        return { path: clampText(text, 400) };
      }
      if (typeof entry !== "object") {
        const text = normalizePatchPath(entry);
        return text ? { path: clampText(text, 400) } : null;
      }
      const action = normalizePatchAction(entry.action || entry.status || entry.changeType || entry.op || entry.kind || entry.type);
      const from = normalizePatchPath(entry.from || entry.oldPath || entry.previousPath || entry.source || entry.src || "");
      const to = normalizePatchPath(entry.to || entry.newPath || entry.target || entry.dest || entry.dst || "");
      const path = normalizePatchPath(entry.path || entry.file || entry.filePath || entry.filename || entry.name || to || from || "");
      if (!path && !from && !to) return null;
      const out = {
        path: clampText(path, 400)
      };
      if (action) out.action = action;
      if (from) out.from = clampText(from, 400);
      if (to) out.to = clampText(to, 400);
      return out;
    }
    function formatPatchFileEntryForDetail(entry) {
      const item = entry && typeof entry === "object" ? entry : null;
      if (!item) return "";
      const from = normalizePatchPath(item.from || "");
      const to = normalizePatchPath(item.to || "");
      const path = normalizePatchPath(item.path || "");
      if (from && to && from !== to) return `${from} -> ${to}`;
      return path || to || from;
    }
    function compactJsonValue(value, depth = 0) {
      if (value === null || value === void 0) return value;
      if (typeof value === "string") return clampText(value, 800);
      if (typeof value === "number" || typeof value === "boolean") return value;
      if (typeof value !== "object") return String(value);
      if (depth >= 3) {
        if (Array.isArray(value)) return `Array(${value.length})`;
        return "Object(...)";
      }
      if (Array.isArray(value)) {
        const arr = value.slice(0, 24).map((item) => compactJsonValue(item, depth + 1));
        if (value.length > arr.length) arr.push(`...(${value.length - arr.length} more)`);
        return arr;
      }
      const out = {};
      const keys = Object.keys(value).slice(0, 40);
      for (const key of keys) {
        out[key] = compactJsonValue(value[key], depth + 1);
      }
      if (Object.keys(value).length > keys.length) {
        out.__truncated__ = `${Object.keys(value).length - keys.length} keys omitted`;
      }
      return out;
    }
    function compactPartRaw(part, type) {
      if (!part || typeof part !== "object") return null;
      const base = {
        id: typeof part.id === "string" ? part.id : "",
        type: typeof type === "string" ? type : typeof part.type === "string" ? part.type : "",
        messageID: typeof part.messageID === "string" ? part.messageID : "",
        sessionID: typeof part.sessionID === "string" ? part.sessionID : ""
      };
      const time = part.time && typeof part.time === "object" ? part.time : null;
      if (time) {
        base.time = {
          start: Number(time.start || 0) || void 0,
          end: Number(time.end || 0) || void 0,
          created: Number(time.created || 0) || void 0,
          completed: Number(time.completed || 0) || void 0
        };
      }
      if (base.type === "tool") {
        const state = part.state && typeof part.state === "object" ? part.state : {};
        const input = state.input && typeof state.input === "object" ? state.input : {};
        base.tool = typeof part.tool === "string" ? part.tool : "";
        base.state = {
          status: typeof state.status === "string" ? state.status : "",
          input: compactJsonValue(input, 0),
          error: clampText(state.error || "", 1200)
        };
        return base;
      }
      if (base.type === "patch") {
        base.hash = typeof part.hash === "string" ? part.hash : "";
        base.files = Array.isArray(part.files) ? part.files.slice(0, 200).map((item) => normalizePatchFileEntry(item)).filter(Boolean) : [];
        return base;
      }
      if (base.type === "file") {
        base.filename = typeof part.filename === "string" ? part.filename : "";
        base.url = typeof part.url === "string" ? clampText(part.url, 400) : "";
        base.mime = typeof part.mime === "string" ? part.mime : "";
        return base;
      }
      if (base.type === "retry") {
        base.attempt = Number.isFinite(part.attempt) ? Number(part.attempt) : void 0;
        base.error = clampText(extractErrorText(part.error), 1200);
        return base;
      }
      if (base.type === "reasoning") {
        return base;
      }
      if (base.type === "stream-text") {
        return base;
      }
      return base;
    }
    function humanizeToolName(toolName) {
      const raw = String(toolName || "").trim();
      if (!raw) return "";
      if (/^[a-z0-9_-]+$/i.test(raw)) {
        return raw.replace(/[_-]+/g, " ").trim();
      }
      return raw;
    }
    function upsertBlockById(blocks, indexById, block) {
      if (!block || typeof block !== "object") return;
      const id = typeof block.id === "string" ? block.id.trim() : "";
      if (!id) {
        blocks.push(block);
        return;
      }
      if (!indexById.has(id)) {
        indexById.set(id, blocks.length);
        blocks.push(block);
        return;
      }
      const index = Number(indexById.get(id));
      if (Number.isFinite(index) && index >= 0 && index < blocks.length) {
        blocks[index] = block;
      }
    }
    function toPartBlock(part, index) {
      if (!part || typeof part !== "object") return null;
      const type = typeof part.type === "string" ? part.type : "";
      if (!type || type === "text") return null;
      const toolCallId = type === "tool" && typeof part.callID === "string" && part.callID.trim() ? part.callID.trim() : "";
      const id = toolCallId ? `tool:${toolCallId}` : typeof part.id === "string" && part.id ? part.id : `${type}:${index}`;
      const block = {
        id,
        type,
        title: type,
        summary: "",
        detail: "",
        status: "",
        metadata: null,
        raw: compactPartRaw(part, type),
        preview: ""
      };
      if (type === "reasoning") {
        const reasoningText = typeof part.text === "string" ? part.text : "";
        const time = part.time && typeof part.time === "object" ? part.time : {};
        const completed = Number(time.completed || 0) > 0;
        block.title = "\u601D\u8003\u8FC7\u7A0B";
        block.status = completed ? "completed" : "running";
        block.summary = "";
        block.detail = reasoningText;
        block.preview = makePreviewText(reasoningText, 220);
        return block;
      }
      if (type === "stream-text") {
        const markdownText = typeof part.text === "string" ? part.text : "";
        const time = part.time && typeof part.time === "object" ? part.time : {};
        const completed = Number(time.completed || 0) > 0;
        block.title = "\u4E2D\u95F4\u8F93\u51FA";
        block.status = completed ? "completed" : "running";
        block.summary = "";
        block.detail = markdownText;
        block.preview = makePreviewText(markdownText, 220);
        return block;
      }
      if (type === "tool") {
        const toolName = typeof part.tool === "string" ? part.tool : "tool";
        const state = part.state && typeof part.state === "object" ? part.state : {};
        const status = typeof state.status === "string" ? state.status : "pending";
        const input = state.input && typeof state.input === "object" ? state.input : {};
        const outputText = typeof state.output === "string" ? state.output : "";
        const hasOutputText = Boolean(outputText.trim());
        const displayName = typeof state.title === "string" && state.title.trim() || (typeof input.skill === "string" && input.skill.trim() ? `Skill: ${input.skill.trim()}` : "") || humanizeToolName(toolName) || toolName;
        block.title = displayName;
        block.status = status;
        block.summary = `\u5DE5\u5177: ${toolName}`;
        block.callId = toolCallId;
        block.metadata = state.metadata || null;
        block.tool = toolName;
        block.toolInput = input;
        block.toolOutput = typeof state.output === "string" ? clampText(state.output, 4e3) : "";
        block.toolError = typeof state.error === "string" ? clampText(state.error, 1200) : "";
        block.attachments = Array.isArray(state.attachments) ? state.attachments : [];
        const chunks = [];
        if (toolName === "question" && Array.isArray(input.questions) && input.questions.length) {
          const lines = [];
          for (const question of input.questions) {
            if (!question || typeof question !== "object") continue;
            const q = typeof question.question === "string" ? question.question.trim() : "";
            if (!q) continue;
            lines.push(`\u95EE\u9898: ${q}`);
            if (Array.isArray(question.options) && question.options.length) {
              lines.push(
                `\u9009\u9879: ${question.options.map((opt) => {
                  if (opt && typeof opt === "object" && typeof opt.label === "string") {
                    return opt.label;
                  }
                  return String(opt);
                }).join(" / ")}`
              );
            }
          }
          if (lines.length) {
            chunks.push(lines.join("\n"));
          }
        }
        if (Object.keys(input).length > 0) {
          chunks.push("\u8F93\u5165:");
          chunks.push(stringifyForDisplay(input, 1400));
        }
        if (status === "completed") {
          if (hasOutputText) {
            chunks.push("\u8F93\u51FA:");
            chunks.push(stringifyForDisplay(outputText, 1800));
            block.preview = makePreviewText(outputText, 280);
            const parsedOutput = parseJsonObject(outputText);
            if (parsedOutput) {
              block.parsedOutput = parsedOutput;
            }
          }
          if (Array.isArray(state.attachments) && state.attachments.length) {
            const files = state.attachments.map((x) => x && (x.filename || x.url)).filter(Boolean).map(String);
            if (files.length) {
              chunks.push("\u9644\u4EF6:");
              chunks.push(files.map((f) => `- ${f}`).join("\n"));
            }
          }
        } else if (status === "error") {
          const errText = typeof state.error === "string" ? state.error : "";
          if (errText.trim()) {
            chunks.push("\u9519\u8BEF:");
            chunks.push(errText.trim());
            block.preview = makePreviewText(errText, 220);
          }
        } else if (hasOutputText) {
          chunks.push("\u8F93\u51FA(\u5B9E\u65F6):");
          chunks.push(stringifyForDisplay(outputText, 2e3));
          block.preview = makePreviewText(outputText, 280);
        }
        if (!block.preview) {
          const pathLike = typeof input.file_path === "string" && input.file_path || typeof input.path === "string" && input.path || typeof input.url === "string" && input.url || typeof input.command === "string" && input.command || "";
          if (pathLike) block.preview = makePreviewText(pathLike, 220);
        }
        if (!block.preview && status === "completed") {
          block.preview = "\u8BE5\u5DE5\u5177\u6B65\u9AA4\u5DF2\u5B8C\u6210\uFF0C\u4F46\u672C\u6B21\u672A\u8FD4\u56DE\u53EF\u5C55\u793A\u7684 output\u3002";
        }
        block.detail = chunks.join("\n");
        return block;
      }
      if (type === "step-start") {
        block.title = "\u6B65\u9AA4\u5F00\u59CB";
        block.status = "running";
        block.summary = typeof part.snapshot === "string" && part.snapshot ? "\u5DF2\u521B\u5EFA\u6B65\u9AA4\u5FEB\u7167" : "";
        return block;
      }
      if (type === "step-finish") {
        block.title = "\u6B65\u9AA4\u5B8C\u6210";
        block.status = "completed";
        const reason = typeof part.reason === "string" ? part.reason : "";
        block.summary = reason || "step finished";
        const tokens = part.tokens && typeof part.tokens === "object" ? part.tokens : {};
        const inputTokens = Number(tokens.input || 0);
        const outputTokens = Number(tokens.output || 0);
        const reasoningTokens = Number(tokens.reasoning || 0);
        const cost = Number(part.cost || 0);
        block.detail = `cost: ${cost}
tokens: in=${inputTokens}, out=${outputTokens}, reasoning=${reasoningTokens}`;
        return block;
      }
      if (type === "subtask") {
        block.title = `\u5B50\u4EFB\u52A1: ${typeof part.agent === "string" ? part.agent : "agent"}`;
        block.status = "running";
        block.summary = typeof part.description === "string" ? part.description : "";
        block.detail = typeof part.prompt === "string" ? part.prompt : "";
        return block;
      }
      if (type === "agent") {
        block.title = `Agent: ${typeof part.name === "string" ? part.name : "unknown"}`;
        block.status = "running";
        block.summary = "\u6B63\u5728\u6267\u884C\u5B50\u4EE3\u7406";
        if (part.source && typeof part.source === "object" && typeof part.source.value === "string") {
          block.detail = part.source.value;
        }
        return block;
      }
      if (type === "file") {
        const filename = typeof part.filename === "string" && part.filename ? part.filename : "";
        const url = typeof part.url === "string" ? part.url : "";
        block.title = `\u6587\u4EF6: ${filename || "attachment"}`;
        block.status = "completed";
        block.summary = typeof part.mime === "string" ? part.mime : "";
        block.detail = url;
        return block;
      }
      if (type === "patch") {
        block.title = "\u6587\u4EF6\u53D8\u66F4";
        block.status = "completed";
        const hash = typeof part.hash === "string" ? part.hash : "";
        const files = Array.isArray(part.files) ? part.files.slice(0, 200).map((item) => normalizePatchFileEntry(item)).filter(Boolean) : [];
        block.summary = hash ? `hash: ${hash.slice(0, 12)}` : "";
        block.detail = files.map((file) => `- ${formatPatchFileEntryForDetail(file)}`).filter(Boolean).join("\n");
        return block;
      }
      if (type === "retry") {
        block.title = `\u91CD\u8BD5 #${Number.isFinite(part.attempt) ? part.attempt : "?"}`;
        block.status = "error";
        block.summary = "\u8BF7\u6C42\u5931\u8D25\u540E\u91CD\u8BD5";
        block.detail = extractErrorText(part.error);
        return block;
      }
      if (type === "compaction") {
        block.title = "\u4E0A\u4E0B\u6587\u538B\u7F29";
        block.status = "completed";
        block.summary = part.auto ? "\u81EA\u52A8\u538B\u7F29" : "\u624B\u52A8\u538B\u7F29";
        return block;
      }
      if (type === "snapshot") {
        block.title = "\u4F1A\u8BDD\u5FEB\u7167";
        block.status = "completed";
        block.detail = typeof part.snapshot === "string" ? stringifyForDisplay(part.snapshot, 1200) : "";
        return block;
      }
      block.detail = stringifyForDisplay(part, 1e3);
      return block;
    }
    function blocksFingerprint(blocks) {
      const list = Array.isArray(blocks) ? blocks : [];
      try {
        return list.map((b) => {
          const detail = String(b && b.detail ? b.detail : "");
          const summary = String(b && b.summary ? b.summary : "").replace(/\s+/g, " ").trim();
          const title = String(b && b.title ? b.title : "").replace(/\s+/g, " ").trim();
          const detailHead = detail.slice(0, 48).replace(/\s+/g, " ").trim();
          const detailTail = detail.slice(-48).replace(/\s+/g, " ").trim();
          return [
            String(b && b.id || ""),
            String(b && b.type || ""),
            String(b && b.status || ""),
            title.slice(0, 64),
            summary.slice(0, 96),
            String(detail.length),
            detailHead,
            detailTail
          ].join("|");
        }).join("||");
      } catch {
        return String(list.length);
      }
    }
    function normalizeToolTargetKey(value) {
      const raw = String(value || "").trim();
      if (!raw) return "";
      return raw.replace(/\\/g, "/").toLowerCase();
    }
    function resolveToolTargetKey(input) {
      if (!input || typeof input !== "object") return "";
      const directCandidates = [
        input.filePath,
        input.file_path,
        input.filepath,
        input.path,
        input.target,
        input.url,
        input.command
      ];
      for (const value of directCandidates) {
        if (typeof value !== "string" || !value.trim()) continue;
        return normalizeToolTargetKey(value);
      }
      if (Array.isArray(input.paths) && input.paths.length) {
        const firstPath = input.paths.find((item) => typeof item === "string" && item.trim());
        if (firstPath) return normalizeToolTargetKey(firstPath);
      }
      return "";
    }
    function toolMetaKeyForPart(part, index) {
      const toolName = typeof part.tool === "string" && part.tool.trim() ? part.tool.trim().toLowerCase() : "tool";
      const state = part.state && typeof part.state === "object" ? part.state : {};
      const input = state.input && typeof state.input === "object" ? state.input : {};
      const targetKey = resolveToolTargetKey(input);
      if (targetKey) return `${toolName}:${targetKey}`;
      const fallbackId = typeof part.id === "string" && part.id.trim() ? part.id.trim() : `${index}`;
      return `${toolName}:id:${fallbackId}`;
    }
    function collectLatestToolErrorMeta(parts) {
      const latestByTarget = /* @__PURE__ */ new Map();
      for (let i = 0; i < parts.length; i += 1) {
        const part = parts[i];
        if (!part || typeof part !== "object" || part.type !== "tool") continue;
        const state = part.state && typeof part.state === "object" ? part.state : {};
        const status = typeof state.status === "string" ? state.status.trim().toLowerCase() : "";
        const toolName = typeof part.tool === "string" && part.tool.trim() ? part.tool.trim() : "tool";
        const key = toolMetaKeyForPart(part, i);
        if (status === "completed") {
          latestByTarget.set(key, { status: "completed", toolName, error: "" });
          continue;
        }
        const isErrorStatus = status === "error" || status === "blocked";
        const errorText = typeof state.error === "string" ? state.error.trim() : "";
        if (!isErrorStatus || !errorText) continue;
        latestByTarget.set(key, { status: "error", toolName, error: errorText });
      }
      const chunks = [];
      for (const row of latestByTarget.values()) {
        if (!row || row.status !== "error") continue;
        chunks.push(`${row.toolName}: ${row.error}`);
      }
      return chunks;
    }
    function extractAssistantParts(parts) {
      if (!Array.isArray(parts)) return { text: "", reasoning: "", meta: "", blocks: [] };
      const textChunks = /* @__PURE__ */ new Map();
      const reasoningChunks = /* @__PURE__ */ new Map();
      const metaChunks = [];
      const blocks = [];
      const blockIndexById = /* @__PURE__ */ new Map();
      for (let i = 0; i < parts.length; i += 1) {
        const part = parts[i];
        if (!part || typeof part !== "object") continue;
        const type = typeof part.type === "string" ? part.type : "";
        if (type === "text") {
          if (part.ignored === true) continue;
          if (typeof part.text === "string") collectPartText(textChunks, part, i, part.text);
          continue;
        }
        if (type === "reasoning") {
          if (typeof part.text === "string") collectPartText(reasoningChunks, part, i, part.text);
          const reasoningBlock = toPartBlock(part, i);
          if (reasoningBlock) upsertBlockById(blocks, blockIndexById, reasoningBlock);
          continue;
        }
        if (type === "retry") {
          const retryError = extractErrorText(part.error);
          if (retryError) metaChunks.push(`retry: ${retryError}`);
        }
        if (!type && typeof part.text === "string") {
          collectPartText(textChunks, part, i, part.text);
          continue;
        }
        const block = toPartBlock(part, i);
        if (block) {
          upsertBlockById(blocks, blockIndexById, block);
        }
      }
      return {
        text: joinPartTextChunks(textChunks),
        reasoning: joinPartTextChunks(reasoningChunks),
        meta: joinUniqueTextChunks([...metaChunks, ...collectLatestToolErrorMeta(parts)]),
        blocks
      };
    }
    function extractErrorText(error) {
      const appendHint = (message2) => {
        const msg = String(message2 || "").trim();
        if (!msg) return "";
        if (/OAuth authentication is currently not allowed/i.test(msg)) {
          return `${msg}\uFF08\u53EF\u8F93\u5165 /models \u6216\u901A\u8FC7\u6A21\u578B\u4E0B\u62C9\u9009\u62E9\u5B98\u65B9\u6A21\u578B\uFF1B\u5982\u9700\u7B2C\u4E09\u65B9\u6A21\u578B\u8BF7\u5728\u8BBE\u7F6E\u7684 Provider \u767B\u5F55\u7BA1\u7406\u91CC\u5B8C\u6210\u8FDE\u63A5\uFF09`;
        }
        if (/(status\s*=\s*401|code['"]?\s*[:=]\s*401|unauthorized|user not found)/i.test(msg)) {
          return `${msg}\uFF08\u9274\u6743\u5931\u8D25\uFF1A\u8BF7\u5728\u8BBE\u7F6E \u2192 Provider \u767B\u5F55\u7BA1\u7406\u91CC\u91CD\u65B0\u767B\u5F55\u5F53\u524D Provider\u3002Windows \u8BF7\u786E\u8BA4\u4F60\u4F7F\u7528\u7684\u662F\u672C\u673A Node.js \u5B89\u88C5\u7684 OpenCode\uFF0C\u800C\u4E0D\u662F WSL \u5B89\u88C5\uFF09`;
        }
        return msg;
      };
      if (typeof error === "string") {
        return appendHint(error);
      }
      if (!error || typeof error !== "object") return "";
      const name = typeof error.name === "string" ? error.name : "AssistantError";
      const data = error.data && typeof error.data === "object" ? error.data : {};
      const root = error && typeof error === "object" ? error : {};
      const message = typeof data.message === "string" && data.message.trim() || typeof root.message === "string" && root.message.trim() || typeof data.responseBody === "string" && data.responseBody.trim() || typeof root.responseBody === "string" && root.responseBody.trim() || "";
      const providerId = typeof data.providerID === "string" && data.providerID.trim() || typeof root.providerID === "string" && root.providerID.trim() || "";
      const provider = providerId ? ` provider=${providerId}` : "";
      const statusCode = Number.isFinite(data.statusCode) && Number(data.statusCode) || Number.isFinite(root.statusCode) && Number(root.statusCode) || Number.isFinite(root.status) && Number(root.status) || 0;
      const status = statusCode ? ` status=${statusCode}` : "";
      if (message) {
        return `${name}${status}${provider}: ${appendHint(message)}`;
      }
      try {
        return `${name}${status}${provider}: ${JSON.stringify(error)}`;
      } catch {
        return `${name}${status}${provider}`;
      }
    }
    function looksLikeInternalPromptLeak(text) {
      const value = String(text || "").trim();
      if (!value) return false;
      if (/\[analyze-mode\]/i.test(value) && /ANALYSIS MODE\./i.test(value)) return true;
      if (/你当前要遵循技能/.test(value) && /技能摘要[:：]/.test(value) && /用户请求如下[:：]/.test(value)) return true;
      return false;
    }
    function extractAssistantPayloadFromEnvelope(envelope) {
      if (!envelope || typeof envelope !== "object") return { text: "", reasoning: "", meta: "", blocks: [] };
      const info = envelope.info && typeof envelope.info === "object" ? envelope.info : {};
      const role = typeof info.role === "string" ? info.role.trim().toLowerCase() : "";
      const errorText = extractErrorText(info.error);
      if (role && role !== "assistant") {
        if (errorText) {
          return { text: `\u6A21\u578B\u8FD4\u56DE\u9519\u8BEF\uFF1A${errorText}`, reasoning: "", meta: errorText, blocks: [] };
        }
        return { text: "", reasoning: "", meta: "", blocks: [] };
      }
      const extracted = extractAssistantParts(Array.isArray(envelope.parts) ? envelope.parts : []);
      let text = extracted.text;
      const reasoning = extracted.reasoning;
      const meta = extracted.meta;
      const blocks = extracted.blocks || [];
      if (!text && errorText) {
        text = `\u6A21\u578B\u8FD4\u56DE\u9519\u8BEF\uFF1A${errorText}`;
      }
      if (!text && info.summary && typeof info.summary === "object") {
        const title = typeof info.summary.title === "string" ? info.summary.title.trim() : "";
        const body = typeof info.summary.body === "string" ? info.summary.body.trim() : "";
        const out = [title, body].filter(Boolean).join("\n\n");
        if (out) text = out;
      }
      if (text && looksLikeInternalPromptLeak(text)) {
        text = "";
      }
      return { text, reasoning, meta, blocks };
    }
    function formatSessionStatusText(status) {
      if (!status || typeof status !== "object") return "unknown";
      const nested = status.status && typeof status.status === "object" ? status.status : status.state && typeof status.state === "object" ? status.state : null;
      const typeRaw = typeof status.type === "string" && status.type.trim() || typeof status.status === "string" && status.status.trim() || typeof status.state === "string" && status.state.trim() || nested && typeof nested.type === "string" && nested.type.trim() || nested && typeof nested.status === "string" && nested.status.trim() || nested && typeof nested.state === "string" && nested.state.trim() || "";
      const type = String(typeRaw || "").toLowerCase();
      const messageRaw = typeof status.message === "string" && status.message.trim() || typeof status.error === "string" && status.error.trim() || typeof status.reason === "string" && status.reason.trim() || nested && typeof nested.message === "string" && nested.message.trim() || nested && typeof nested.error === "string" && nested.error.trim() || nested && typeof nested.reason === "string" && nested.reason.trim() || "";
      const message = String(messageRaw || "").replace(/\s+/g, " ").trim();
      if (type === "idle") return "idle";
      if (type === "busy") return "busy";
      if (type === "retry") {
        const attempt = Number.isFinite(status.attempt) ? status.attempt : nested && Number.isFinite(nested.attempt) ? nested.attempt : "?";
        return `retry(attempt=${attempt}${message ? `, message=${message}` : ""})`;
      }
      if (type) return message ? `${type}(message=${message})` : type;
      if (message) return `unknown(message=${message})`;
      return "unknown";
    }
    module2.exports = {
      joinUniqueTextChunks,
      mergeSnapshotText,
      stringifyForDisplay,
      parseJsonObject,
      makePreviewText,
      toPartBlock,
      blocksFingerprint,
      extractAssistantParts,
      extractErrorText,
      looksLikeInternalPromptLeak,
      extractAssistantPayloadFromEnvelope,
      formatSessionStatusText,
      normalizedRenderableText
    };
  }
});

// runtime/payload/payload-state-utils.js
var require_payload_state_utils = __commonJS({
  "runtime/payload/payload-state-utils.js"(exports2, module2) {
    var { normalizedRenderableText } = require_markdown_utils();
    function hasRenderablePayload(payload) {
      const p = payload && typeof payload === "object" ? payload : {};
      const textLen = normalizedRenderableText(p.text || "").length;
      const reasoningLen = String(p.reasoning || "").trim().length;
      const blocksLen = Array.isArray(p.blocks) ? p.blocks.length : 0;
      return textLen > 0 || reasoningLen > 0 || blocksLen > 0;
    }
    function hasSufficientPayload(payload) {
      const p = payload && typeof payload === "object" ? payload : {};
      const textLen = normalizedRenderableText(p.text || "").length;
      const reasoningLen = String(p.reasoning || "").trim().length;
      const blocksLen = Array.isArray(p.blocks) ? p.blocks.length : 0;
      if (textLen > 1) return true;
      if (blocksLen > 0) return true;
      if (reasoningLen > 40) return true;
      return false;
    }
    function isIntermediateToolCallPayload(payload) {
      const p = payload && typeof payload === "object" ? payload : {};
      const text = normalizedRenderableText(p.text || "");
      const blocks = Array.isArray(p.blocks) ? p.blocks : [];
      const hasToolBlock = blocks.some((b) => b && b.type === "tool");
      const hasToolCallsFinish = blocks.some(
        (b) => b && b.type === "step-finish" && String(b.summary || "").trim().toLowerCase() === "tool-calls"
      );
      if (!hasToolBlock || !hasToolCallsFinish) return false;
      if (!text) return true;
      if (/^\d+$/.test(text)) return true;
      if (text.length <= 1) return true;
      return false;
    }
    function hasTerminalStepFinish(payload) {
      const p = payload && typeof payload === "object" ? payload : {};
      const blocks = Array.isArray(p.blocks) ? p.blocks : [];
      for (const b of blocks) {
        if (!b || b.type !== "step-finish") continue;
        const reason = String(b.summary || "").trim().toLowerCase();
        if (!reason) continue;
        if (reason === "tool-calls") continue;
        return true;
      }
      return false;
    }
    function payloadLooksInProgress(payload) {
      const p = payload && typeof payload === "object" ? payload : {};
      const blocks = Array.isArray(p.blocks) ? p.blocks : [];
      const hasRenderableText = normalizedRenderableText(p.text || "").length > 1;
      const hasReasoningText = String(p.reasoning || "").trim().length > 0;
      const hasToolInFlight = blocks.some((b) => {
        if (!b || b.type !== "tool") return false;
        const status = String(b.status || "").trim().toLowerCase();
        if (!status) return true;
        return status !== "completed" && status !== "error";
      });
      const hasStepStart = blocks.some((b) => b && b.type === "step-start");
      const hasTerminalFinish = hasTerminalStepFinish(payload);
      const hasToolCallsFinish = blocks.some(
        (b) => b && b.type === "step-finish" && String(b.summary || "").trim().toLowerCase() === "tool-calls"
      );
      if (hasTerminalFinish) return false;
      if (hasToolCallsFinish && !hasRenderableText) return true;
      if (hasToolInFlight) return true;
      if (hasStepStart && !hasRenderableText && !hasReasoningText) return true;
      return false;
    }
    function hasTerminalPayload(payload) {
      const p = payload && typeof payload === "object" ? payload : {};
      if (hasTerminalStepFinish(p)) return true;
      if (payloadLooksInProgress(p)) return false;
      if (normalizedRenderableText(p.text || "").length > 1) return true;
      return false;
    }
    function responseRichnessScore(payload) {
      const p = payload && typeof payload === "object" ? payload : {};
      const text = normalizedRenderableText(p.text || "");
      const reasoning = String(p.reasoning || "").trim();
      const blocks = Array.isArray(p.blocks) ? p.blocks : [];
      const meta = String(p.meta || "").trim();
      const hasFallbackText = /^\(无文本返回：session\.status=/.test(text);
      const terminal = hasTerminalPayload(p);
      const inProgress = payloadLooksInProgress(p);
      let score = 0;
      score += Math.min(text.length, 400) * 1.4;
      score += Math.min(reasoning.length, 300) * 0.45;
      score += Math.min(blocks.length, 10) * 8;
      score += Math.min(meta.length, 200) * 0.15;
      if (terminal) score += 120;
      if (inProgress) score -= 40;
      if (hasFallbackText) score -= 30;
      return score;
    }
    function mergeBlockLists(primaryBlocks, secondaryBlocks) {
      const out = [];
      const indexByKey = /* @__PURE__ */ new Map();
      const statusRank = (value) => {
        const status = String(value || "").trim().toLowerCase();
        if (status === "error") return 4;
        if (status === "completed") return 3;
        if (status === "running") return 2;
        if (status === "pending") return 1;
        return 0;
      };
      const lists = [Array.isArray(primaryBlocks) ? primaryBlocks : [], Array.isArray(secondaryBlocks) ? secondaryBlocks : []];
      for (const list of lists) {
        for (let idx = 0; idx < list.length; idx += 1) {
          const block = list[idx];
          if (!block || typeof block !== "object") continue;
          const key = String(block.id || "") || `${String(block.type || "block")}::${String(block.title || "")}::${String(block.summary || "")}::${idx}`;
          if (!indexByKey.has(key)) {
            indexByKey.set(key, out.length);
            out.push(block);
            continue;
          }
          const existingIndex = Number(indexByKey.get(key));
          const existing = out[existingIndex];
          const existingRank = statusRank(existing && existing.status);
          const nextRank = statusRank(block.status);
          const existingDetailLen = String(existing && existing.detail ? existing.detail : "").length;
          const nextDetailLen = String(block.detail || "").length;
          const shouldReplace = nextRank > existingRank || nextRank === existingRank && nextDetailLen > existingDetailLen;
          if (shouldReplace) {
            out[existingIndex] = block;
          }
        }
      }
      return out;
    }
    function mergeAssistantPayload(preferred, fallback) {
      if (!fallback) return preferred;
      if (!preferred) return fallback;
      const p = preferred && typeof preferred === "object" ? preferred : {};
      const f = fallback && typeof fallback === "object" ? fallback : {};
      const pMessageId = String(p.messageId || "").trim();
      const fMessageId = String(f.messageId || "").trim();
      if (pMessageId && fMessageId && pMessageId !== fMessageId) {
        return preferred;
      }
      return {
        messageId: pMessageId || fMessageId || "",
        text: normalizedRenderableText(p.text || "") ? String(p.text || "") : String(f.text || ""),
        reasoning: String(p.reasoning || "").trim() ? String(p.reasoning || "") : String(f.reasoning || ""),
        meta: String(p.meta || "").trim() ? String(p.meta || "") : String(f.meta || ""),
        blocks: mergeBlockLists(p.blocks, f.blocks),
        completed: Boolean(p.completed) || Boolean(f.completed)
      };
    }
    function chooseRicherResponse(primary, secondary) {
      if (!secondary) return primary;
      if (!primary) return secondary;
      const p = primary && typeof primary === "object" ? primary : {};
      const s = secondary && typeof secondary === "object" ? secondary : {};
      const pTextLen = normalizedRenderableText(p.text || "").length;
      const sTextLen = normalizedRenderableText(s.text || "").length;
      const pTerminal = hasTerminalPayload(p);
      const sTerminal = hasTerminalPayload(s);
      const pIntermediate = isIntermediateToolCallPayload(p);
      const sIntermediate = isIntermediateToolCallPayload(s);
      if (sTerminal && !pTerminal) return mergeAssistantPayload(secondary, primary);
      if (pTerminal && !sTerminal) return mergeAssistantPayload(primary, secondary);
      if (pIntermediate && sTextLen > 1 && pTextLen <= 1) return mergeAssistantPayload(secondary, primary);
      if (sIntermediate && pTextLen > 1 && sTextLen <= 1) return mergeAssistantPayload(primary, secondary);
      if (sTextLen > 0 && pTextLen === 0) return mergeAssistantPayload(secondary, primary);
      if (pTextLen > 0 && sTextLen === 0) return mergeAssistantPayload(primary, secondary);
      if (pTerminal && sTerminal) {
        const textGap = Math.abs(pTextLen - sTextLen);
        const textGapThreshold = 24;
        if (textGap >= textGapThreshold) {
          if (sTextLen > pTextLen) return mergeAssistantPayload(secondary, primary);
          if (pTextLen > sTextLen) return mergeAssistantPayload(primary, secondary);
        }
      }
      const pScore = responseRichnessScore(primary);
      const sScore = responseRichnessScore(secondary);
      return sScore >= pScore ? mergeAssistantPayload(secondary, primary) : mergeAssistantPayload(primary, secondary);
    }
    module2.exports = {
      hasRenderablePayload,
      hasSufficientPayload,
      isIntermediateToolCallPayload,
      hasTerminalStepFinish,
      payloadLooksInProgress,
      hasTerminalPayload,
      responseRichnessScore,
      mergeBlockLists,
      mergeAssistantPayload,
      chooseRicherResponse
    };
  }
});

// runtime/assistant-payload-utils.js
var require_assistant_payload_utils = __commonJS({
  "runtime/assistant-payload-utils.js"(exports2, module2) {
    var assistantParts = require_assistant_parts_utils();
    var markdownUtils = require_markdown_utils();
    var payloadState = require_payload_state_utils();
    module2.exports = {
      ...assistantParts,
      ...markdownUtils,
      ...payloadState
    };
  }
});

// runtime/chat/chat-orchestrator.js
var require_chat_orchestrator = __commonJS({
  "runtime/chat/chat-orchestrator.js"(exports2, module2) {
    var { Notice: Notice2 } = require("obsidian");
    var { tFromContext } = require_i18n_runtime();

    function tr(view, key, fallback, params = {}) {
      return tFromContext(view, key, fallback, params);
    }

    function createMessageId(prefix) {
      return `${prefix}-${Date.now()}-${Math.random().toString(16).slice(2, 8)}`;
    }

    function collectUserLinkedContextFiles(view, hideUserMessage) {
      if (hideUserMessage || !view || typeof view.getLinkedContextFilePaths !== "function") return [];
      const rawPaths = view.getLinkedContextFilePaths();
      if (!Array.isArray(rawPaths) || !rawPaths.length) return [];
      const seen = new Set();
      const normalized = [];
      rawPaths.forEach((rawPath) => {
        const next = String(rawPath || "").trim().replace(/^\/+/, "");
        if (!next || seen.has(next)) return;
        seen.add(next);
        normalized.push(next);
      });
      return normalized;
    }

    function mountPendingDraft(view, sessionId, userText, hideUserMessage, linkedContextFiles = []) {
      const userMessage = { id: createMessageId("msg"), role: "user", text: userText, createdAt: Date.now() };
      if (Array.isArray(linkedContextFiles) && linkedContextFiles.length) {
        userMessage.linkedContextFiles = linkedContextFiles.slice();
      }
      const draftId = createMessageId("msg");
      const draft = {
        id: draftId,
        role: "assistant",
        text: "",
        reasoning: "",
        meta: "",
        blocks: [],
        createdAt: Date.now(),
        pending: true,
        error: "",
      };

      if (!hideUserMessage) {
        view.plugin.sessionStore.appendMessage(sessionId, userMessage);
      }
      view.plugin.sessionStore.appendMessage(sessionId, draft);

      if (typeof view.setForceBottomWindow === "function") {
        view.setForceBottomWindow(12000);
      }
      view.autoScrollEnabled = true;
      if (typeof view.renderMessages === "function") {
        view.renderMessages({ forceBottom: true });
      }
      if (typeof view.refreshHistoryMenu === "function") {
        view.refreshHistoryMenu();
      }
      if (typeof view.scheduleScrollMessagesToBottom === "function") {
        view.scheduleScrollMessagesToBottom(true);
      }

      return { draftId };
    }

    function renderDraftBlocks(view, draftId) {
      const messages = view.elements.messages;
      if (!messages) return;

      const target = view.findMessageRow(draftId);
      if (!target) return;

      const currentDraft = view.plugin
        .sessionStore
        .getActiveMessages()
        .find((msg) => msg && msg.id === draftId);
      if (!currentDraft) return;

      view.renderAssistantBlocks(target, currentDraft);
      view.removeStandaloneReasoningContainer(target);
      view.reorderAssistantMessageLayout(target);
    }

    function isQuestionToolBlock(block) {
      if (!block || typeof block !== "object") return false;
      if (String(block.type || "").trim().toLowerCase() !== "tool") return false;
      const toolName = String(block.tool || "").trim().toLowerCase();
      if (toolName === "question") return true;
      const raw = block.raw && typeof block.raw === "object" ? block.raw : {};
      const candidate =
        String(raw.tool || "").trim().toLowerCase()
        || String(raw.name || "").trim().toLowerCase()
        || String(raw.type || "").trim().toLowerCase();
      return candidate === "question";
    }

    function hasQuestionToolBlock(blocks) {
      const list = Array.isArray(blocks) ? blocks : [];
      return list.some((block) => isQuestionToolBlock(block));
    }

    function createTransportHandlers(view, sessionId, draftId) {
      const queueFrame = (flush, state) => {
        if (!state || typeof state !== "object") return;
        if (state.scheduled) return;
        if (typeof requestAnimationFrame === "function") {
          state.scheduled = requestAnimationFrame(() => {
            state.scheduled = 0;
            flush();
          });
          return;
        }
        flush();
      };

      const tokenState = { latest: "", scheduled: 0 };
      const reasoningState = { latest: "", scheduled: 0 };
      const blockState = { latest: [], scheduled: 0 };
      const questionRefreshState = { lastAt: 0 };

      const flushToken = () => {
        const currentDraft = view.plugin
          .sessionStore
          .getActiveMessages()
          .find((msg) => msg && msg.id === draftId);
        if (!currentDraft || !currentDraft.pending) return;
        const partial = String(tokenState.latest || "");
        view.plugin.sessionStore.updateAssistantDraft(sessionId, draftId, partial);
        const refreshedDraft = view.plugin
          .sessionStore
          .getActiveMessages()
          .find((msg) => msg && msg.id === draftId);
        if (!refreshedDraft || !refreshedDraft.pending) return;
        const displayedText = currentDraft && typeof currentDraft.text === "string"
          ? String((refreshedDraft && refreshedDraft.text) || "")
          : partial;

        if (displayedText.trim()) {
          view.setRuntimeStatus(tr(view, "view.runtime.generating", "Generating response..."), "working");
        }

        const messages = view.elements.messages;
        if (!messages) return;
        const target = view.findMessageRow(draftId);
        if (target) {
          const body = target.querySelector(".oc-message-content");
          if (body) {
            const hasStreamTextBlocks = Array.isArray(refreshedDraft.blocks) && refreshedDraft.blocks.some((block) => String((block && block.type) || "").trim().toLowerCase() === "stream-text");
            if (hasStreamTextBlocks) {
              body.empty();
            } else if (displayedText.trim() && typeof view.renderMarkdownSafely === "function") {
              view.renderMarkdownSafely(body, displayedText, () => {
                if (typeof view.enhanceCodeBlocks === "function") view.enhanceCodeBlocks(body);
              });
            } else {
              body.textContent = displayedText;
            }
          }
        }
        view.scheduleScrollMessagesToBottom();
      };

      const flushReasoning = () => {
        const currentDraft = view.plugin
          .sessionStore
          .getActiveMessages()
          .find((msg) => msg && msg.id === draftId);
        if (!currentDraft || !currentDraft.pending) return;
        const partialReasoning = String(reasoningState.latest || "");
        view.plugin.sessionStore.updateAssistantDraft(sessionId, draftId, undefined, partialReasoning);
        const refreshedDraft = view.plugin
          .sessionStore
          .getActiveMessages()
          .find((msg) => msg && msg.id === draftId);
        if (!refreshedDraft || !refreshedDraft.pending) return;
        const displayedReasoning = currentDraft && typeof currentDraft.reasoning === "string"
          ? String((refreshedDraft && refreshedDraft.reasoning) || "")
          : partialReasoning;

        if (displayedReasoning.trim()) {
          view.setRuntimeStatus(tr(view, "view.runtime.reasoning", "Model is reasoning..."), "working");
        }

        const messages = view.elements.messages;
        if (!messages) return;
        const target = view.findMessageRow(draftId);
        if (!target) return;

        const hasReasoningBlocks = view.hasReasoningBlock(refreshedDraft && refreshedDraft.blocks);
        if (hasReasoningBlocks && refreshedDraft) {
          view.removeStandaloneReasoningContainer(target);
        } else {
          const reasoningBody = view.ensureReasoningContainer(target, true);
          if (reasoningBody) reasoningBody.textContent = displayedReasoning || "...";
        }
        view.scheduleScrollMessagesToBottom();
      };

      const flushBlocks = () => {
        const currentDraft = view.plugin
          .sessionStore
          .getActiveMessages()
          .find((msg) => msg && msg.id === draftId);
        if (!currentDraft || !currentDraft.pending) return;
        const blocks = Array.isArray(blockState.latest) ? blockState.latest : [];
        view.plugin.sessionStore.updateAssistantDraft(sessionId, draftId, undefined, undefined, undefined, blocks);
        const runtimeStatus = view.runtimeStatusFromBlocks(blocks);
        if (runtimeStatus && runtimeStatus.text) {
          view.setRuntimeStatus(runtimeStatus.text, runtimeStatus.tone);
        }

        if (
          hasQuestionToolBlock(blocks)
          && typeof view.refreshPendingQuestionRequests === "function"
          && Date.now() - Number(questionRefreshState.lastAt || 0) >= 300
        ) {
          questionRefreshState.lastAt = Date.now();
          void view.refreshPendingQuestionRequests({
            minIntervalMs: 300,
            silent: true,
          }).catch(() => {});
        }

        renderDraftBlocks(view, draftId);
        view.renderInlineQuestionPanel(view.plugin.sessionStore.getActiveMessages());
        view.scheduleScrollMessagesToBottom();
      };

      return {
        onToken: (partial) => {
          tokenState.latest = String(partial || "");
          queueFrame(flushToken, tokenState);
        },

        onReasoning: (partialReasoning) => {
          reasoningState.latest = String(partialReasoning || "");
          queueFrame(flushReasoning, reasoningState);
        },

        onBlocks: (blocks) => {
          blockState.latest = Array.isArray(blocks) ? blocks : [];
          queueFrame(flushBlocks, blockState);
        },

        onPermissionRequest: async (permission) => {
          view.setRuntimeStatus(tr(view, "view.permission.waiting", "Waiting for permission confirmation..."), "info");
          const decision = await view.showPermissionRequestModal(permission || {});
          if (!decision) return "reject";
          if (decision === "always" || decision === "once" || decision === "reject") {
            return decision;
          }
          return "reject";
        },

        onQuestionRequest: (questionRequest) => {
          const request = view.upsertPendingQuestionRequest(questionRequest || {});
          if (!request) return;
          if (view.plugin && typeof view.plugin.log === "function") {
            view.plugin.log(`question requested ${JSON.stringify({
              id: request.id,
              sessionId: request.sessionId,
              count: Array.isArray(request.questions) ? request.questions.length : 0,
            })}`);
          }
          view.setRuntimeStatus(tr(view, "view.question.answerInPanel", "Please answer in the panel below."), "info");
          view.renderInlineQuestionPanel(view.plugin.sessionStore.getActiveMessages());
        },

        onQuestionResolved: (info) => {
          const sessionIdFromEvent = String((info && info.sessionId) || "").trim();
          const requestIdFromEvent = String((info && info.requestId) || "").trim();
          if (requestIdFromEvent) {
            view.removePendingQuestionRequest(sessionIdFromEvent || sessionId, requestIdFromEvent);
          }
          view.renderInlineQuestionPanel(view.plugin.sessionStore.getActiveMessages());
        },

        onPromptAppend: (appendText) => {
          view.setRuntimeStatus(tr(view, "view.promptAppend.waiting", "Waiting for additional input..."), "info");
          if (view.hasVisibleQuestionToolCard()) {
            view.setRuntimeStatus(tr(view, "view.question.answerAndSubmit", "Please answer and submit in the panel below."), "info");
            return;
          }
          view.showPromptAppendModal(appendText);
        },

        onToast: (toast) => {
          view.handleToastEvent(toast || {});
        },
      };
    }

    function finalizeAssistantDraft(view, sessionId, draftId, response) {
      view.plugin.sessionStore.finalizeAssistantDraft(
        sessionId,
        draftId,
        {
          messageId: response.messageId || "",
          text: response.text || "",
          reasoning: response.reasoning || "",
          meta: response.meta || "",
          blocks: Array.isArray(response.blocks) ? response.blocks : [],
        },
        /error|failed|失败|status=\d{3}/i.test(String(response.meta || "")) ? String(response.meta || "") : "",
      );
    }

    async function handlePromptFailure(view, sessionId, draftId, error) {
      const msg = error instanceof Error ? error.message : String(error);
      const isSilentAbort = view.silentAbortBudget > 0 && view.isAbortLikeError(msg);

      if (isSilentAbort) {
        view.silentAbortBudget = Math.max(0, Number(view.silentAbortBudget || 0) - 1);
        const existing = (view.plugin.sessionStore.state().messagesBySession[sessionId] || []).find((x) => x && x.id === draftId);
        view.plugin.sessionStore.finalizeAssistantDraft(
          sessionId,
          draftId,
          {
            text: existing && typeof existing.text === "string" ? existing.text : "",
            reasoning: existing && typeof existing.reasoning === "string" ? existing.reasoning : "",
            meta: existing && typeof existing.meta === "string" ? existing.meta : "",
            blocks: existing && Array.isArray(existing.blocks) ? existing.blocks : [],
          },
          "",
        );
        view.setRuntimeStatus(tr(view, "view.question.waiting", "Waiting for question answers..."), "info");
        return { shouldRerenderModelPicker: false };
      }

      let shouldRerenderModelPicker = false;
      const activeModel = String(view.selectedModel || view.plugin.settings.defaultModel || "").trim();
      if (activeModel && view.plugin && typeof view.plugin.markModelUnavailable === "function") {
        const tracked = view.plugin.markModelUnavailable(activeModel, msg);
        if (tracked && tracked.hidden) {
          shouldRerenderModelPicker = true;
          if (String(view.plugin.settings.defaultModel || "").trim() === activeModel) {
            view.selectedModel = "";
            view.plugin.settings.defaultModel = "";
            try {
              await view.plugin.saveSettings();
            } catch {
            }
          }
          new Notice2(tr(view, "view.model.hiddenUnavailable", "Model may be unavailable and has been hidden: {model}", { model: activeModel }));
        }
      }

      view.setRuntimeStatus(tr(view, "view.request.failed", "Request failed: {message}", { message: msg }), "error");
      view.plugin.sessionStore.finalizeAssistantDraft(
        sessionId,
        draftId,
        tr(view, "view.request.failed", "Request failed: {message}", { message: msg }),
        msg,
      );
      new Notice(msg);
      return { shouldRerenderModelPicker };
    }

    async function finalizePromptCycle(view, shouldRerenderModelPicker) {
      view.currentAbort = null;
      view.setBusy(false);
      await view.plugin.persistState();

      if (typeof view.setForceBottomWindow === "function") {
        view.setForceBottomWindow(6000);
      }
      view.autoScrollEnabled = true;

      if (shouldRerenderModelPicker) {
        view.render();
        return;
      }

      if (typeof view.renderMessages === "function") {
        view.renderMessages({ forceBottom: true });
      }
      if (typeof view.refreshHistoryMenu === "function") {
        view.refreshHistoryMenu();
      }
      if (typeof view.scheduleScrollMessagesToBottom === "function") {
        view.scheduleScrollMessagesToBottom(true);
      }
    }

    async function runSendPrompt(view, userText, options = {}) {
      const requestOptions = options && typeof options === "object" ? options : {};
      const forceSessionId = typeof requestOptions.sessionId === "string" ? requestOptions.sessionId.trim() : "";
      const hideUserMessage = Boolean(requestOptions.hideUserMessage);

      const modelSlash = view.parseModelSlashCommand(userText);
      if (modelSlash) {
        await view.handleModelSlashCommand(userText, modelSlash);
        return;
      }

      const skillSelectorSlash = view.parseSkillSelectorSlashCommand(userText);
      if (skillSelectorSlash) {
        view.openSkillSelector();
        return;
      }

      const skillMatch = view.resolveSkillFromPrompt(userText);

      const st = view.plugin.sessionStore.state();
      let sessionId = forceSessionId || st.activeSessionId;
      if (forceSessionId && st.activeSessionId !== forceSessionId) {
        view.plugin.sessionStore.setActiveSession(forceSessionId);
        view.render();
      }

      if (!sessionId) {
        const session = await view.plugin.createSession("");
        sessionId = session.id;
        view.plugin.sessionStore.setActiveSession(sessionId);
        view.render();
      }

      const linkedContextFiles = collectUserLinkedContextFiles(view, hideUserMessage);
      const { draftId } = mountPendingDraft(view, sessionId, userText, hideUserMessage, linkedContextFiles);
      if (!hideUserMessage) {
        if (typeof view.clearLinkedContextFiles === "function") {
          view.clearLinkedContextFiles({ closePicker: true });
        } else if (Array.isArray(view.linkedContextFiles)) {
          view.linkedContextFiles = [];
          if (typeof view.refreshLinkedContextIndicators === "function") {
            view.refreshLinkedContextIndicators();
          }
        }
      }

      view.currentAbort = new AbortController();
      view.setBusy(true);
      view.setRuntimeStatus(tr(view, "view.runtime.waitingResponse", "Waiting for FLOWnote response..."), "working");
      let shouldRerenderModelPicker = false;

      try {
        const skillCommand = skillMatch && typeof skillMatch.command === "string"
          ? String(skillMatch.command).trim()
          : "";
        const useNativeSkillCommand = Boolean(skillMatch && skillMatch.skill && skillCommand);
        let prompt = "";
        if (useNativeSkillCommand) {
          let commandArgs = String(skillMatch.promptText || "").trim();
          if (typeof view.composePromptWithLinkedFiles === "function") {
            commandArgs = await view.composePromptWithLinkedFiles(commandArgs, { linkedPaths: linkedContextFiles });
          }
          prompt = commandArgs ? `${skillCommand} ${commandArgs}` : skillCommand;
        } else {
          prompt = view.plugin.skillService.buildInjectedPrompt(
            skillMatch.skill,
            view.plugin.settings.skillInjectMode,
            skillMatch.promptText || userText,
          );
          if (typeof view.composePromptWithLinkedFiles === "function") {
            prompt = await view.composePromptWithLinkedFiles(prompt, { linkedPaths: linkedContextFiles });
          }
        }

        const response = await view.plugin.opencodeClient.sendMessage({
          sessionId,
          prompt,
          signal: view.currentAbort.signal,
          ...createTransportHandlers(view, sessionId, draftId),
        });

        finalizeAssistantDraft(view, sessionId, draftId, response);
      } catch (error) {
        const out = await handlePromptFailure(view, sessionId, draftId, error);
        shouldRerenderModelPicker = Boolean(out && out.shouldRerenderModelPicker);
      } finally {
        await finalizePromptCycle(view, shouldRerenderModelPicker);
      }
    }

    module2.exports = {
      runSendPrompt,
    };
  }
});

// runtime/view/command-router.js
var require_command_router = __commonJS({
  "runtime/view/command-router.js"(exports2, module2) {
    var { Notice: Notice2 } = require("obsidian");
    var { tFromContext } = require_i18n_runtime();
    function tr(view, key, fallback, params = {}) {
      return tFromContext(view, key, fallback, params);
    }
    function uid(prefix) {
      return `${prefix}-${Date.now()}-${Math.random().toString(16).slice(2, 8)}`;
    }
    function syncInlineModelSelectLabelFromSelect(view) {
      const modelSelect = view && view.elements && view.elements.modelSelect;
      const modelSelectText = view && view.elements && view.elements.modelSelectText;
      if (!modelSelect || !modelSelectText) return;
      const option = modelSelect.options && modelSelect.selectedIndex >= 0 ? modelSelect.options[modelSelect.selectedIndex] : null;
      const displayText = String(option && option.text || "").trim() || tr(view, "view.model.placeholder", "Model");
      modelSelectText.textContent = displayText;
      modelSelectText.setAttribute("title", displayText);
    }
    function getSkillPrimaryDescription(skill) {
      if (!skill) return tr(this, "view.skill.primaryFallback", "Select a skill to see its primary description.");
      const cleanInline = (line) => String(line || "").trim().replace(/^[-*]\s+/, "").replace(/^>\s+/, "").replace(/\*\*(.*?)\*\*/g, "$1").replace(/`([^`]+)`/g, "$1").trim();
      const directRaw = String(skill.description || "").trim();
      const isBlockMarker = /^([>|][+-]?)$/.test(directRaw);
      if (!isBlockMarker && directRaw) {
        const directLines = directRaw.split(/\r?\n/).map((line) => cleanInline(line)).filter((line) => line && !/^[-:| ]+$/.test(line));
        if (directLines.length) return directLines[0];
      }
      const lines = String(skill.summary || "").split(/\r?\n/).map((line) => line.trim()).filter(Boolean).map((line) => {
        if (!line.startsWith("|")) return line.replace(/^[-*]\s+/, "");
        const cells = line.split("|").map((cell) => cell.trim().replace(/^[-*]\s+/, "")).filter(Boolean);
        const picked = cells.find((cell) => !/^[-:]+$/.test(cell) && !/^(name|名称|命令|command|技能|skill|功能|作用|描述|description)$/i.test(cell) && !cell.startsWith("/"));
        return picked || "";
      }).map((line) => cleanInline(line)).filter((line) => line && !/^#{1,6}\s/.test(line) && !/^```/.test(line) && !/^[-:| ]+$/.test(line));
      return lines[0] || tr(this, "view.skill.noDescription", "No skill description");
    }
    function getSkillBriefDescription(skill) {
      const primary = String(this.getSkillPrimaryDescription(skill) || "").replace(/\s+/g, " ").trim();
      if (!primary || primary === tr(this, "view.skill.noDescription", "No skill description")) return "";
      let brief = primary;
      const namePrefix = String(skill && skill.name ? skill.name : "").trim();
      if (namePrefix && brief.toLowerCase().startsWith(namePrefix.toLowerCase())) {
        brief = brief.slice(namePrefix.length).trim();
        brief = brief.replace(/^[(（][^)）]+[)）]\s*[-—:：]?\s*/, "");
        brief = brief.replace(/^[-—:：\s]+/, "");
      }
      if (!brief) brief = primary;
      const delimiters = ["\uFF1A", ":", "\u3002", "\uFF1B", ";", "\uFF0C", ",", "\uFF08", "("];
      let cutIndex = brief.length;
      delimiters.forEach((delimiter) => {
        const index = brief.indexOf(delimiter);
        if (index > 0 && index < cutIndex) cutIndex = index;
      });
      brief = brief.slice(0, cutIndex).trim();
      if (brief.length > 24) brief = `${brief.slice(0, 24)}\u2026`;
      return brief;
    }
    function parseModelSlashCommand(text) {
      const input = String(text || "").trim();
      if (!input.startsWith("/")) return null;
      const raw = input.slice(1).trim();
      if (!raw) return null;
      const firstSpace = raw.indexOf(" ");
      const cmd = (firstSpace >= 0 ? raw.slice(0, firstSpace) : raw).trim().toLowerCase();
      const args = (firstSpace >= 0 ? raw.slice(firstSpace + 1) : "").trim();
      if (!["models", "model", "modle"].includes(cmd)) return null;
      return { command: "models", args };
    }
    function parseSkillSelectorSlashCommand(text) {
      const input = String(text || "").trim();
      if (!input.startsWith("/")) return null;
      if (input === "/") return { command: "skills" };
      const raw = input.slice(1).trim();
      if (!raw) return null;
      const firstSpace = raw.indexOf(" ");
      const cmd = (firstSpace >= 0 ? raw.slice(0, firstSpace) : raw).trim().toLowerCase();
      const args = (firstSpace >= 0 ? raw.slice(firstSpace + 1) : "").trim();
      if (!["skills", "skill"].includes(cmd)) return null;
      if (args) return null;
      return { command: "skills" };
    }
    function resolveSkillFromPrompt(userText) {
      const input = String(userText || "").trim();
      if (!input.startsWith("/")) return { skill: null, promptText: input };
      const raw = input.slice(1).trim();
      if (!raw) return { skill: null, promptText: input };
      const firstSpace = raw.indexOf(" ");
      const cmd = (firstSpace >= 0 ? raw.slice(0, firstSpace) : raw).trim();
      const cmdLower = cmd.toLowerCase();
      if (!cmdLower) return { skill: null, promptText: input };
      if (["models", "model", "modle"].includes(cmdLower)) {
        return { skill: null, promptText: input };
      }
      const skillService = this.plugin && this.plugin.skillService;
      if (skillService && typeof skillService.loadSkills === "function") {
        try {
          skillService.loadSkills();
        } catch {
        }
      }
      const skills = skillService && typeof skillService.getSkills === "function" ? skillService.getSkills() : [];
      const findSkillByToken = (token) => skills.find((item) => {
        const needle = String(token || "").toLowerCase();
        if (!needle) return false;
        const id = String(item.id || "").toLowerCase();
        const name = String(item.name || "").toLowerCase();
        return id === needle || name === needle;
      });
      const buildPromptText = (skill2, promptTextRaw) => {
        const promptText = String(promptTextRaw || "").trim();
        if (promptText) return promptText;
        return tr(
          this,
          "view.skill.defaultPrompt",
          "Please handle this task with skill {name}.",
          { name: skill2.name || skill2.id }
        );
      };
      if (["skills", "skill"].includes(cmdLower)) {
        const nestedRaw = (firstSpace >= 0 ? raw.slice(firstSpace + 1) : "").trim();
        if (!nestedRaw) return { skill: null, promptText: input };
        const nestedSpace = nestedRaw.indexOf(" ");
        const nestedCmd = (nestedSpace >= 0 ? nestedRaw.slice(0, nestedSpace) : nestedRaw).trim();
        const nestedPromptRaw = (nestedSpace >= 0 ? nestedRaw.slice(nestedSpace + 1) : "").trim();
        const nestedSkill = findSkillByToken(nestedCmd);
        if (!nestedSkill) return { skill: null, promptText: input };
        return {
          skill: nestedSkill,
          promptText: buildPromptText(nestedSkill, nestedPromptRaw),
          command: `/${nestedSkill.id}`
        };
      }
      const skill = findSkillByToken(cmdLower);
      if (!skill) return { skill: null, promptText: input };
      const rest = (firstSpace >= 0 ? raw.slice(firstSpace + 1) : "").trim();
      return {
        skill,
        promptText: buildPromptText(skill, rest),
        command: `/${skill.id}`
      };
    }
    function openSkillSelector() {
      const skillService = this.plugin && this.plugin.skillService;
      if (skillService && typeof skillService.loadSkills === "function") {
        try {
          skillService.loadSkills();
        } catch {
        }
      }
      const skills = skillService && typeof skillService.getSkills === "function" ? skillService.getSkills() : [];
      if (!skills.length) {
        new Notice2(tr(this, "view.skill.noneFound", "No available skills found. Check Skills directory settings."));
        return;
      }
      const input = this.elements && this.elements.input;
      if (!input || input.disabled) {
        new Notice2(tr(this, "view.skill.notReady", "Input area is not ready yet. Try again shortly."));
        return;
      }
      const beforeCursor = String(input.value || "").slice(0, Number(input.selectionStart || 0));
      if (!/^\s*\/[^\s]*$/.test(beforeCursor)) {
        input.value = "/";
        input.selectionStart = input.selectionEnd = 1;
      }
      input.focus();
      if (typeof this.syncLinkedContextPickerFromInputMention === "function") {
        this.syncLinkedContextPickerFromInputMention();
      }
      this.setRuntimeStatus(tr(this, "view.skill.selectHint", "Type / to choose a skill command."), "info");
    }
    async function refreshModelList() {
      if (this.plugin && typeof this.plugin.loadModelCatalogFromTerminalOutput === "function") {
        const cachedModels = this.plugin.loadModelCatalogFromTerminalOutput();
        if (Array.isArray(cachedModels) && cachedModels.length && typeof this.updateModelSelectOptions === "function") {
          this.updateModelSelectOptions();
        }
      }
      if (this.plugin && typeof this.plugin.refreshModelCatalog === "function") {
        return this.plugin.refreshModelCatalog();
      }
      const models = await this.plugin.opencodeClient.listModels();
      this.plugin.cachedModels = Array.isArray(models) ? models : [];
      if (typeof this.updateModelSelectOptions === "function") this.updateModelSelectOptions();
      return this.plugin.cachedModels;
    }
    async function ensureActiveSession() {
      const st = this.plugin.sessionStore.state();
      if (st.activeSessionId) return st.activeSessionId;
      const session = await this.plugin.createSession("");
      this.plugin.sessionStore.setActiveSession(session.id);
      await this.plugin.persistState();
      return session.id;
    }
    async function applyModelSelection(modelID, options = {}) {
      const normalized = String(modelID || "").trim();
      const previous = String(this.selectedModel || "");
      const previousSetting = String(this.plugin.settings.defaultModel || "");
      const availableModels = Array.isArray(this.plugin.cachedModels) ? this.plugin.cachedModels : [];
      if (normalized && availableModels.length && !availableModels.includes(normalized)) {
        throw new Error(tr(this, "view.model.unavailable", "Model unavailable or not authorized: {model}", { model: normalized }));
      }
      this.selectedModel = normalized;
      this.plugin.settings.defaultModel = normalized;
      await this.plugin.saveSettings();
      if (this.elements.modelSelect) {
        this.elements.modelSelect.value = normalized;
      }
      syncInlineModelSelectLabelFromSelect(this);
      try {
        if (normalized) {
          await this.plugin.opencodeClient.setDefaultModel({ model: normalized });
          if (!options.silentNotice) new Notice2(tr(this, "view.model.switched", "Switched model: {model}", { model: normalized }));
          return tr(this, "view.model.switched", "Switched model: {model}", { model: normalized });
        }
        if (!options.silentNotice) new Notice2(tr(this, "view.model.resetAuto", "Reset to automatic model selection by FLOWnote."));
        return tr(this, "view.model.resetAuto", "Reset to automatic model selection by FLOWnote.");
      } catch (e) {
        this.selectedModel = previous;
        this.plugin.settings.defaultModel = previousSetting;
        await this.plugin.saveSettings();
        if (this.elements.modelSelect) this.elements.modelSelect.value = previous;
        syncInlineModelSelectLabelFromSelect(this);
        throw e;
      }
    }
    async function openModelSelector(sessionId) {
      let select = this.elements.modelSelect;
      if (!select) {
        this.render();
        select = this.elements.modelSelect;
      }
      if (typeof this.updateModelSelectOptions === "function") this.updateModelSelectOptions();
      if (!select || select.disabled) {
        new Notice2(tr(this, "view.model.notReady", "Model picker is not ready yet. Try again shortly."));
        return;
      }
      void this.refreshModelList().catch((error) => {
        const message = error instanceof Error ? error.message : String(error);
        if (typeof this.plugin.log === "function") this.plugin.log(`refresh model list failed: ${message}`);
      });
      select.focus();
      if (typeof select.showPicker === "function") {
        try {
          select.showPicker();
          return;
        } catch {
        }
      }
      this.setRuntimeStatus(tr(this, "view.model.selectHint", "Please choose a model from the picker."), "info");
    }
    async function handleModelSlashCommand(userText, parsed) {
      const sessionId = await this.ensureActiveSession();
      this.plugin.sessionStore.appendMessage(sessionId, {
        id: uid("msg"),
        role: "user",
        text: userText,
        createdAt: Date.now()
      });
      if (!parsed.args) {
        this.appendAssistantMessage(sessionId, tr(this, "view.model.selectHint", "Please choose a model from the picker."), "");
        await this.plugin.persistState();
        this.renderMessages();
        this.refreshHistoryMenu();
        await this.openModelSelector(sessionId);
        return;
      }
      try {
        const text = await this.applyModelSelection(parsed.args, { silentNotice: true });
        this.appendAssistantMessage(sessionId, text, "");
      } catch (e) {
        const msg = e instanceof Error ? e.message : String(e);
        this.appendAssistantMessage(
          sessionId,
          tr(this, "view.model.switchFailed", "Model switch failed: {message}", { message: msg }),
          msg
        );
        new Notice2(tr(this, "view.model.switchFailed", "Model switch failed: {message}", { message: msg }));
      }
      await this.plugin.persistState();
      this.renderMessages();
      this.refreshHistoryMenu();
    }
    module2.exports = { commandRouterMethods: {
      getSkillPrimaryDescription,
      getSkillBriefDescription,
      parseModelSlashCommand,
      parseSkillSelectorSlashCommand,
      resolveSkillFromPrompt,
      openSkillSelector,
      refreshModelList,
      ensureActiveSession,
      applyModelSelection,
      openModelSelector,
      handleModelSlashCommand
    } };
  }
});

// runtime/domain/session-title.js
var require_session_title = __commonJS({
  "runtime/domain/session-title.js"(exports2, module2) {
    function normalizeSessionTitleInput(value) {
      return String(value || "").replace(/\s+/g, " ").trim();
    }
    function isPlaceholderSessionTitle(title) {
      const normalized = normalizeSessionTitleInput(title).toLowerCase();
      if (!normalized) return true;
      if (normalized === "\u65B0\u4F1A\u8BDD" || normalized === "\u672A\u547D\u540D\u4F1A\u8BDD" || normalized === "new session" || normalized === "untitled" || normalized === "untitled session") {
        return true;
      }
      return /^(new session|untitled(?: session)?|新会话|未命名会话)(?:\s*[-:：].*)?$/.test(normalized);
    }
    function deriveSessionTitleFromPrompt(prompt) {
      let text = normalizeSessionTitleInput(prompt);
      if (!text) return "";
      if (text.startsWith("/")) {
        const firstSpace = text.indexOf(" ");
        if (firstSpace > 1) {
          const rest = normalizeSessionTitleInput(text.slice(firstSpace + 1));
          text = rest || text.slice(1);
        } else {
          text = text.slice(1);
        }
      }
      text = text.replace(/^[\s:：\-—]+/, "");
      if (!text) return "";
      const maxLen = 28;
      return text.length > maxLen ? `${text.slice(0, maxLen)}\u2026` : text;
    }
    function resolveSessionDisplayTitle(session, fallback = "\u672A\u547D\u540D\u4F1A\u8BDD") {
      if (!session || typeof session !== "object") return fallback;
      const current = normalizeSessionTitleInput(session.title);
      if (current && !isPlaceholderSessionTitle(current)) return current;
      const inferred = deriveSessionTitleFromPrompt(session.lastUserPrompt || "");
      return inferred || current || fallback;
    }
    module2.exports = {
      normalizeSessionTitleInput,
      isPlaceholderSessionTitle,
      deriveSessionTitleFromPrompt,
      resolveSessionDisplayTitle
    };
  }
});

// runtime/view/layout/shared-utils.js
var require_shared_utils = __commonJS({
  "runtime/view/layout/shared-utils.js"(exports2, module2) {
    var { setIcon } = require("obsidian");
    var {
      normalizeSessionTitleInput: normalizeSessionTitleFromDomain,
      isPlaceholderSessionTitle: isPlaceholderSessionTitleFromDomain,
      deriveSessionTitleFromPrompt: deriveSessionTitleFromPromptFromDomain,
      resolveSessionDisplayTitle
    } = require_session_title();
    var { tFromContext } = require_i18n_runtime();
    var LINKED_CONTEXT_ALLOWED_EXTENSIONS = /* @__PURE__ */ new Set(["md", "canvas", "txt", "json", "csv", "yaml", "yml"]);
    var LINKED_CONTEXT_MAX_FILES = 12;
    var LINKED_CONTEXT_MAX_CHARS_PER_FILE = 9e3;
    var LINKED_CONTEXT_MAX_TOTAL_CHARS = 36e3;
    var LINKED_CONTEXT_PICKER_MAX_ITEMS = 120;
    function tr(view, key, fallback, params = {}) {
      return tFromContext(view, key, fallback, params);
    }
    function normalizeLinkedContextPath(value) {
      return String(value || "").trim().replace(/^\/+/, "");
    }
    function displayNameFromPath(pathValue) {
      const path = normalizeLinkedContextPath(pathValue);
      if (!path) return "";
      const parts = path.split("/");
      return parts[parts.length - 1] || path;
    }
    function isLinkableContextFile(file) {
      if (!file || typeof file !== "object") return false;
      const ext = String(file.extension || "").trim().toLowerCase();
      if (!ext) return false;
      return LINKED_CONTEXT_ALLOWED_EXTENSIONS.has(ext);
    }
    function isLinkableContextFolder(target) {
      if (!target || typeof target !== "object") return false;
      const pathValue = normalizeLinkedContextPath(target.path || "");
      if (!pathValue) return false;
      if (Array.isArray(target.children)) return true;
      const ext = String(target.extension || "").trim().toLowerCase();
      if (ext) return false;
      return !/\.[A-Za-z0-9_-]+$/.test(pathValue);
    }
    function createLinkableContextEntry(file) {
      if (!file || typeof file !== "object") return null;
      const filePath = normalizeLinkedContextPath(file.path || file.file?.path || file.name || "");
      if (!filePath) return null;
      const fallbackName = displayNameFromPath(filePath);
      const basename = String(file.basename || "").trim();
      const derivedName = basename || String(file.name || "").trim() || fallbackName;
      const name = derivedName || fallbackName || filePath;
      const stat = file.stat && typeof file.stat === "object" ? file.stat : null;
      const mtime = Number(stat && stat.mtime ? stat.mtime : 0);
      return {
        path: filePath,
        name,
        mtime,
        search: `${name} ${filePath}`.toLowerCase()
      };
    }
    function openSettings() {
      this.app.setting.open();
      this.app.setting.openTabById(this.plugin.manifest.id);
    }
    function buildIconButton(parent, icon, label, onClick, cls = "") {
      const btn = parent.createEl("button", { cls: `oc-icon-btn ${cls}`.trim() });
      setIcon(btn, icon);
      btn.setAttr("aria-label", label);
      btn.setAttr("title", label);
      btn.addEventListener("click", onClick);
      return btn;
    }
    function createSvgNode(tag, attrs = {}) {
      const node = document.createElementNS("http://www.w3.org/2000/svg", tag);
      Object.entries(attrs).forEach(([k, v]) => node.setAttribute(k, String(v)));
      return node;
    }
    function isFreeModel(modelName) {
      const raw = String(modelName || "").trim().toLowerCase();
      if (!raw) return false;
      const modelId = raw.includes("/") ? raw.slice(raw.indexOf("/") + 1) : raw;
      return /(?:^|[-_:.\/])free$/.test(modelId);
    }
    function extractModelProvider(modelName) {
      const raw = String(modelName || "").trim().toLowerCase();
      if (!raw) return "Unspecified Provider";
      const slashIndex = raw.indexOf("/");
      if (slashIndex <= 0) return "Unspecified Provider";
      return raw.slice(0, slashIndex) || "Unspecified Provider";
    }
    function splitModelsByFree(models) {
      const uniq = [...new Set((Array.isArray(models) ? models : []).map((m) => String(m || "").trim()).filter(Boolean))];
      uniq.sort((a, b) => a.localeCompare(b));
      return uniq.reduce((acc, model) => {
        if (isFreeModel(model)) {
          acc.free.push(model);
          return acc;
        }
        const provider = extractModelProvider(model);
        if (!acc.byProvider[provider]) acc.byProvider[provider] = [];
        acc.byProvider[provider].push(model);
        return acc;
      }, { free: [], byProvider: {} });
    }
    function appendGroupedModelOptions(selectEl, models, view) {
      const grouped = splitModelsByFree(models);
      if (grouped.free.length) {
        const freeGroup = selectEl.createEl("optgroup", {
          attr: { label: tr(view, "view.model.freeGroup", "Free Models ({count})", { count: grouped.free.length }) }
        });
        grouped.free.forEach((m) => freeGroup.createEl("option", { value: m, text: m }));
      }
      Object.entries(grouped.byProvider).sort(([a], [b]) => a.localeCompare(b)).forEach(([provider, providerModels]) => {
        const providerGroup = selectEl.createEl("optgroup", {
          attr: { label: `${provider} (${providerModels.length})` }
        });
        providerModels.forEach((m) => providerGroup.createEl("option", { value: m, text: m }));
      });
    }
    function updateModelSelectOptions() {
      const modelSelect = this.elements && this.elements.modelSelect;
      if (!modelSelect) return;
      const selectedBefore = String(this.selectedModel || modelSelect.value || "");
      const models = Array.isArray(this.plugin && this.plugin.cachedModels) ? this.plugin.cachedModels : [];
      const normalizedModels = [...new Set(models.map((model) => String(model || "").trim()).filter(Boolean))];
      modelSelect.empty();
      modelSelect.createEl("option", { value: "", text: tr(this, "view.model.placeholder", "Model") });
      appendGroupedModelOptions(modelSelect, normalizedModels, this);
      const canRestoreSelection = selectedBefore && normalizedModels.includes(selectedBefore);
      modelSelect.value = canRestoreSelection ? selectedBefore : "";
      this.selectedModel = modelSelect.value;
      syncInlineModelSelectLabel.call(this);
    }
    function syncInlineModelSelectLabel() {
      const modelSelect = this.elements && this.elements.modelSelect;
      const modelSelectText = this.elements && this.elements.modelSelectText;
      if (!modelSelect || !modelSelectText) return;
      const option = modelSelect.options && modelSelect.selectedIndex >= 0 ? modelSelect.options[modelSelect.selectedIndex] : null;
      const displayText = String(option && option.text || "").trim() || tr(this, "view.model.placeholder", "Model");
      modelSelectText.textContent = displayText;
      modelSelectText.setAttribute("title", displayText);
    }
    function renderSidebarToggleIcon(button) {
      if (!button) return;
      button.empty();
      button.classList.toggle("is-collapsed", Boolean(this.isSidebarCollapsed));
      try {
        setIcon(button, this.isSidebarCollapsed ? "panel-left-open" : "panel-left-close");
      } catch {
        setIcon(button, this.isSidebarCollapsed ? "chevrons-right" : "chevrons-left");
      }
    }
    function scrollMessagesTo(target) {
      const messages = this.elements.messages;
      if (!messages) return;
      if (target === "top") {
        this.autoScrollEnabled = false;
        if (typeof this.setForceBottomWindow === "function") this.setForceBottomWindow(0);
        if (typeof this.withProgrammaticScroll === "function") {
          this.withProgrammaticScroll(messages, () => {
            messages.scrollTop = 0;
          });
          return;
        }
        messages.scrollTop = 0;
        return;
      }
      this.autoScrollEnabled = true;
      if (typeof this.scheduleScrollMessagesToBottom === "function") {
        this.scheduleScrollMessagesToBottom(true);
        return;
      }
      messages.scrollTop = messages.scrollHeight;
    }
    function toggleSidebarCollapsed() {
      this.isSidebarCollapsed = !this.isSidebarCollapsed;
      this.render();
    }
    function normalizeSessionTitle(value) {
      return normalizeSessionTitleFromDomain(value);
    }
    function isPlaceholderSessionTitle(title) {
      return isPlaceholderSessionTitleFromDomain(title);
    }
    function deriveSessionTitleFromPrompt(prompt) {
      return deriveSessionTitleFromPromptFromDomain(prompt);
    }
    function sessionDisplayTitle(session) {
      return resolveSessionDisplayTitle(session, tr(this, "view.session.untitled", "Untitled Session"));
    }
    function activeSessionLabel() {
      const st = this.plugin.sessionStore.state();
      const session = st.sessions.find((s) => s.id === st.activeSessionId);
      if (!session) return tr(this, "view.session.noneSelected", "No session selected");
      return this.sessionDisplayTitle(session);
    }
    function formatSessionMetaTime(timestamp) {
      if (!timestamp) return "";
      const date = new Date(timestamp);
      if (Number.isNaN(date.getTime())) return "";
      const now = /* @__PURE__ */ new Date();
      if (date.toDateString() === now.toDateString()) {
        return date.toLocaleTimeString(void 0, { hour: "2-digit", minute: "2-digit", hour12: false });
      }
      return date.toLocaleDateString(void 0, { month: "short", day: "numeric" });
    }
    var sharedLayoutMethods = {
      openSettings,
      buildIconButton,
      createSvgNode,
      updateModelSelectOptions,
      renderSidebarToggleIcon,
      scrollMessagesTo,
      toggleSidebarCollapsed,
      normalizeSessionTitle,
      isPlaceholderSessionTitle,
      deriveSessionTitleFromPrompt,
      sessionDisplayTitle,
      activeSessionLabel,
      formatSessionMetaTime
    };
    module2.exports = {
      LINKED_CONTEXT_MAX_FILES,
      LINKED_CONTEXT_MAX_CHARS_PER_FILE,
      LINKED_CONTEXT_MAX_TOTAL_CHARS,
      LINKED_CONTEXT_PICKER_MAX_ITEMS,
      tr,
      normalizeLinkedContextPath,
      displayNameFromPath,
      isLinkableContextFile,
      isLinkableContextFolder,
      createLinkableContextEntry,
      sharedLayoutMethods
    };
  }
});

// runtime/view/layout/header-methods.js
var require_header_methods = __commonJS({
  "runtime/view/layout/header-methods.js"(exports2, module2) {
    var { Notice: Notice2, setIcon } = require("obsidian");
    var { tr } = require_shared_utils();
    var FLOWNOTE_ICON_ID2 = "flownote-journal-glow";
    function render() {
      this.clearInlineQuestionWidget(true);
      const container = this.contentEl || this.containerEl.children[1] || this.containerEl;
      container.empty();
      container.addClass("oc-root", "oc-surface");
      this.root = container;
      const shell = container.createDiv({ cls: "oc-shell" });
      const header = shell.createDiv({ cls: "oc-header" });
      this.renderHeader(header);
      const body = shell.createDiv({ cls: "oc-body" });
      const main = body.createDiv({ cls: "oc-main" });
      this.elements.body = body;
      this.elements.main = main;
      this.renderMain(main);
    }
    function renderHeader(header) {
      header.empty();
      const brand = header.createDiv({ cls: "oc-brand" });
      const logo = brand.createDiv({ cls: "oc-brand-logo" });
      setIcon(logo, FLOWNOTE_ICON_ID2);
      brand.createDiv({ cls: "oc-brand-title", text: "FLOWnote" });
      const actions = header.createDiv({ cls: "oc-header-actions" });
      actions.createDiv({ cls: "oc-header-meta", text: tr(this, "view.header.runtime", "Chat Runtime") });
      const newBtn = this.buildIconButton(
        actions,
        "plus",
        tr(this, "view.session.new", "New session"),
        async () => {
          try {
            const session = await this.plugin.createSession("");
            this.plugin.sessionStore.setActiveSession(session.id);
            await this.plugin.persistState();
            this.closeHistoryMenu();
            this.render();
          } catch (e) {
            new Notice2(e instanceof Error ? e.message : String(e));
          }
        },
        "oc-header-btn"
      );
      newBtn.setAttr("type", "button");
      const historyContainer = actions.createDiv({ cls: "oc-history-container" });
      const historyBtn = historyContainer.createEl("button", {
        cls: "oc-icon-btn oc-header-btn oc-history-toggle"
      });
      setIcon(historyBtn, "history");
      historyBtn.setAttr("type", "button");
      historyBtn.setAttr("aria-label", tr(this, "view.session.history", "Session history"));
      historyBtn.setAttr("title", tr(this, "view.session.history", "Session history"));
      const historyMenu = historyContainer.createDiv({ cls: "oc-history-menu" });
      historyMenu.addEventListener("click", (event) => event.stopPropagation());
      this.elements.historyMenu = historyMenu;
      historyBtn.addEventListener("click", (event) => {
        event.preventDefault();
        event.stopPropagation();
        this.toggleHistoryMenu();
      });
      if (!this.historyMenuDocumentBound) {
        this.historyMenuDocumentBound = true;
        this.registerDomEvent(document, "click", () => this.closeHistoryMenu());
      }
      this.refreshHistoryMenu();
    }
    var headerMethods = {
      render,
      renderHeader
    };
    module2.exports = { headerMethods };
  }
});

// runtime/view/layout/sidebar-methods.js
var require_sidebar_methods = __commonJS({
  "runtime/view/layout/sidebar-methods.js"(exports2, module2) {
    var { Notice: Notice2, setIcon } = require("obsidian");
    var { tr } = require_shared_utils();
    function renderSidebar(side) {
      if (!side) return;
      side.empty();
      const sessions = this.plugin.sessionStore.state().sessions;
      const active = this.plugin.sessionStore.state().activeSessionId;
      const header = side.createDiv({ cls: "oc-history-header" });
      header.createSpan({ text: tr(this, "view.session.heading", "Sessions") });
      header.createSpan({
        cls: "oc-history-count",
        text: tr(this, "view.session.count", "{count} sessions", { count: sessions.length })
      });
      const list = side.createDiv({ cls: "oc-history-list" });
      if (!sessions.length) {
        list.createDiv({ cls: "oc-history-empty", text: tr(this, "view.session.empty", 'No sessions yet. Click "+" to start.') });
        return;
      }
      sessions.forEach((s) => {
        const displayTitle = this.sessionDisplayTitle(s);
        const item = list.createDiv({ cls: "oc-session-item", attr: { title: displayTitle } });
        if (s.id === active) item.addClass("is-active");
        item.addEventListener("click", async () => {
          if (item.hasClass("is-renaming")) return;
          this.closeHistoryMenu();
          this.plugin.sessionStore.setActiveSession(s.id);
          this.render();
          try {
            if (typeof this.plugin.ensureSessionMessagesLoaded === "function") {
              await this.plugin.ensureSessionMessagesLoaded(s.id, { force: false });
            }
          } catch (error) {
            this.plugin.log(
              `load session history failed: ${error instanceof Error ? error.message : String(error)}`
            );
          }
          await this.plugin.persistState();
          this.render();
        });
        const iconEl = item.createDiv({ cls: "oc-session-item-icon" });
        setIcon(iconEl, s.id === active ? "message-square-dot" : "message-square");
        const content = item.createDiv({ cls: "oc-session-item-content" });
        const titleEl = content.createDiv({ cls: "oc-session-title", text: displayTitle });
        titleEl.setAttr("title", displayTitle);
        if (s.lastUserPrompt) {
          content.createDiv({ cls: "oc-session-preview", text: s.lastUserPrompt, attr: { title: s.lastUserPrompt } });
        }
        content.createDiv({
          cls: "oc-session-meta",
          text: s.id === active ? tr(this, "view.session.currentShort", "Current session") : this.formatSessionMetaTime(s.updatedAt)
        });
        const actions = item.createDiv({ cls: "oc-session-item-actions" });
        const renameBtn = actions.createEl("button", { cls: "oc-session-item-action" });
        renameBtn.setAttr("type", "button");
        renameBtn.setAttr("aria-label", tr(this, "view.session.rename", "Rename session"));
        renameBtn.setAttr("title", tr(this, "view.session.rename", "Rename session"));
        setIcon(renameBtn, "pencil");
        renameBtn.addEventListener("click", (event) => {
          event.preventDefault();
          event.stopPropagation();
          if (item.hasClass("is-renaming")) return;
          item.addClass("is-renaming");
          const input = document.createElement("input");
          input.type = "text";
          input.className = "oc-session-rename-input";
          input.value = displayTitle;
          titleEl.replaceWith(input);
          input.focus();
          input.select();
          const stop = (ev) => ev.stopPropagation();
          input.addEventListener("click", stop);
          input.addEventListener("mousedown", stop);
          let finished = false;
          const finishRename = async (commit) => {
            if (finished) return;
            finished = true;
            item.removeClass("is-renaming");
            if (!commit) {
              this.render();
              return;
            }
            const normalized = this.normalizeSessionTitle(input.value || "");
            if (!normalized) {
              new Notice2(tr(this, "view.session.renameEmpty", "Session name cannot be empty"));
              this.render();
              return;
            }
            const renamed = this.plugin.sessionStore.renameSession(s.id, normalized);
            if (!renamed) {
              new Notice2(tr(this, "view.session.renameMissing", "Session to rename was not found"));
              this.render();
              return;
            }
            await this.plugin.persistState();
            this.refreshHistoryMenu();
            this.refreshCurrentSessionContext();
          };
          input.addEventListener("blur", () => {
            void finishRename(true);
          }, { once: true });
          input.addEventListener("keydown", (ev) => {
            if (ev.key === "Enter" && !ev.isComposing) {
              ev.preventDefault();
              input.blur();
              return;
            }
            if (ev.key === "Escape" && !ev.isComposing) {
              ev.preventDefault();
              void finishRename(false);
            }
          });
        });
        const deleteBtn = actions.createEl("button", { cls: "oc-session-item-action is-danger" });
        deleteBtn.setAttr("type", "button");
        deleteBtn.setAttr("aria-label", tr(this, "view.session.delete", "Delete session"));
        deleteBtn.setAttr("title", tr(this, "view.session.delete", "Delete session"));
        setIcon(deleteBtn, "trash-2");
        deleteBtn.addEventListener("click", async (event) => {
          event.preventDefault();
          event.stopPropagation();
          const confirmed = window.confirm(tr(this, "view.session.deleteConfirm", 'Delete session "{title}"?', { title: displayTitle }));
          if (!confirmed) return;
          const removed = typeof this.plugin.deleteSession === "function" ? await this.plugin.deleteSession(s.id) : this.plugin.sessionStore.removeSession(s.id);
          if (!removed) {
            new Notice2(tr(this, "view.session.deleteFailed", "Delete failed: session not found"));
            return;
          }
          if (typeof this.plugin.deleteSession !== "function") {
            await this.plugin.persistState();
          }
          this.closeHistoryMenu();
          this.render();
        });
      });
    }
    function closeHistoryMenu() {
      const menu = this.elements && this.elements.historyMenu;
      if (!menu) return;
      menu.removeClass("visible");
    }
    function toggleHistoryMenu() {
      const menu = this.elements && this.elements.historyMenu;
      if (!menu) return;
      const isVisible = menu.hasClass("visible");
      if (isVisible) {
        menu.removeClass("visible");
        return;
      }
      this.refreshHistoryMenu();
      menu.addClass("visible");
    }
    function refreshHistoryMenu() {
      const menu = this.elements && this.elements.historyMenu;
      if (!menu) return;
      this.renderSidebar(menu);
    }
    function refreshCurrentSessionContext() {
      const labelEl = this.elements && this.elements.currentSessionLabel;
      if (!labelEl) return;
      labelEl.textContent = tr(this, "view.session.current", "Current session: {title}", { title: this.activeSessionLabel() });
    }
    var sidebarMethods = {
      renderSidebar,
      closeHistoryMenu,
      toggleHistoryMenu,
      refreshHistoryMenu,
      refreshCurrentSessionContext
    };
    module2.exports = { sidebarMethods };
  }
});

// runtime/view/layout/linked-context-drop-methods.js
var require_linked_context_drop_methods = __commonJS({
  "runtime/view/layout/linked-context-drop-methods.js"(exports2, module2) {
    var { Notice: Notice2 } = require("obsidian");
    var {
      LINKED_CONTEXT_MAX_FILES,
      tr,
      normalizeLinkedContextPath,
      isLinkableContextFile,
      isLinkableContextFolder
    } = require_shared_utils();
    var LINKED_CONTEXT_DROP_TEXT_TYPES = [
      "text/plain",
      "text/uri-list"
    ];
    var LINKED_CONTEXT_DROP_EXTENSION_CANDIDATES = ["md", "canvas", "txt", "json", "csv", "yaml", "yml"];
    function isLinkableContextTarget(target) {
      return isLinkableContextFile(target) || isLinkableContextFolder(target);
    }
    function toggleElementClass(el, className, enabled) {
      if (!el || !className) return;
      if (typeof el.toggleClass === "function") {
        el.toggleClass(className, Boolean(enabled));
        return;
      }
      if (el.classList && typeof el.classList.toggle === "function") {
        el.classList.toggle(className, Boolean(enabled));
      }
    }
    function normalizeComparablePath(pathValue) {
      const normalized = String(pathValue || "").trim().replace(/\\/g, "/").replace(/\/+/g, "/");
      if (normalized.length > 1 && normalized.endsWith("/")) {
        return normalized.slice(0, -1);
      }
      return normalized;
    }
    function splitDropTextTokens(rawText) {
      return String(rawText || "").split(/\r?\n+/).map((token) => String(token || "").trim()).filter(Boolean).map((token) => token.replace(/^[-*]\s+/, "").trim()).filter(Boolean);
    }
    function extractWikiLinkTargets(rawText) {
      const text = String(rawText || "");
      const targets = [];
      const re = /\[\[([^\]]+)\]\]/g;
      let match = re.exec(text);
      while (match) {
        targets.push(String(match[1] || "").trim());
        match = re.exec(text);
      }
      return targets;
    }
    function extractPathFromObsidianUri(rawValue) {
      const value = String(rawValue || "").trim();
      if (!value || !/^obsidian:\/\//i.test(value)) return "";
      try {
        const parsed = new URL(value);
        if (parsed.protocol !== "obsidian:") return "";
        const fromQuery = [
          parsed.searchParams.get("file"),
          parsed.searchParams.get("path"),
          parsed.searchParams.get("url"),
          parsed.searchParams.get("name")
        ].map((item) => String(item || "").trim()).find(Boolean);
        if (fromQuery) return fromQuery;
        const pathname = String(parsed.pathname || "").trim().replace(/^\/+/, "");
        if (pathname) return pathname;
        return "";
      } catch {
        const match = value.match(/[?&](?:file|path|url|name)=([^&#]+)/i);
        if (match && match[1]) {
          return String(match[1] || "").trim();
        }
        return "";
      }
    }
    function decodeMaybe(value) {
      const text = String(value || "");
      if (!text) return "";
      try {
        return decodeURIComponent(text);
      } catch {
        return text;
      }
    }
    function normalizeDropCandidateToken(rawToken) {
      let token = String(rawToken || "").trim();
      if (!token) return "";
      if (token.startsWith("<") && token.endsWith(">")) {
        token = token.slice(1, -1).trim();
      }
      token = token.replace(/^['"]+|['"]+$/g, "").trim();
      if (!token) return "";
      token = token.replace(/\\#/g, "#").replace(/\\\|/g, "|");
      if (token.includes("|")) token = token.split("|")[0];
      if (token.includes("#")) token = token.split("#")[0];
      return token.trim();
    }
    function pushUniqueString(list, seen, value) {
      const text = String(value || "").trim();
      if (!text || seen.has(text)) return;
      seen.add(text);
      list.push(text);
    }
    function hasFileExtension(pathValue) {
      const path = String(pathValue || "").trim();
      if (!path) return false;
      const slashIndex = path.lastIndexOf("/");
      const filename = slashIndex >= 0 ? path.slice(slashIndex + 1) : path;
      return /\.[A-Za-z0-9_-]+$/.test(filename);
    }
    function buildPathResolutionCandidates(view, rawPath) {
      const out = [];
      const seen = /* @__PURE__ */ new Set();
      const base = String(rawPath || "").trim();
      if (!base) return out;
      pushUniqueString(out, seen, normalizeLinkedContextPath(base));
      pushUniqueString(out, seen, normalizeLinkedContextPath(decodeMaybe(base)));
      pushUniqueString(out, seen, normalizeLinkedContextPath(base.replace(/\\/g, "/")));
      pushUniqueString(out, seen, normalizeLinkedContextPath(decodeMaybe(base.replace(/\\/g, "/"))));
      const vault = view && view.app && view.app.vault;
      const vaultName = vault && typeof vault.getName === "function" ? String(vault.getName() || "").trim() : "";
      if (vaultName) {
        const lowerVault = vaultName.toLowerCase();
        out.slice().forEach((candidate) => {
          const lowerCandidate = candidate.toLowerCase();
          if (!lowerCandidate.startsWith(`${lowerVault}/`)) return;
          pushUniqueString(out, seen, candidate.slice(vaultName.length + 1));
        });
      }
      out.slice().forEach((candidate) => {
        if (hasFileExtension(candidate)) return;
        LINKED_CONTEXT_DROP_EXTENSION_CANDIDATES.forEach((ext) => {
          pushUniqueString(out, seen, `${candidate}.${ext}`);
        });
      });
      return out;
    }
    function normalizeMatchingKey(value) {
      return String(value || "").normalize("NFC").trim().replace(/\\/g, "/").replace(/^\/+/, "").replace(/\/+/g, "/");
    }
    function removeKnownExtension(pathValue) {
      const path = normalizeMatchingKey(pathValue);
      if (!path) return "";
      const slashIndex = path.lastIndexOf("/");
      const head = slashIndex >= 0 ? path.slice(0, slashIndex + 1) : "";
      const tail = slashIndex >= 0 ? path.slice(slashIndex + 1) : path;
      const dotIndex = tail.lastIndexOf(".");
      if (dotIndex <= 0) return path;
      const ext = tail.slice(dotIndex + 1).toLowerCase();
      if (!LINKED_CONTEXT_DROP_EXTENSION_CANDIDATES.includes(ext)) return path;
      return `${head}${tail.slice(0, dotIndex)}`;
    }
    function resolveByLinkpathScan(view, rawPath) {
      const vault = view && view.app && view.app.vault;
      if (!vault || typeof vault.getFiles !== "function") return "";
      const metadataCache = view && view.app && view.app.metadataCache;
      const sourcePath = (() => {
        const workspace = view && view.app && view.app.workspace;
        const activeFile = workspace && typeof workspace.getActiveFile === "function" ? workspace.getActiveFile() : null;
        return String(activeFile && activeFile.path ? activeFile.path : "");
      })();
      const target = normalizeMatchingKey(rawPath);
      if (!target) return "";
      const targetNoExt = removeKnownExtension(target);
      const targetLower = target.toLowerCase();
      const targetNoExtLower = targetNoExt.toLowerCase();
      const files = vault.getFiles().filter((file) => isLinkableContextFile(file));
      const exactPath = [];
      const pathWithoutExt = [];
      const linktextExact = [];
      files.forEach((file) => {
        const filePath = normalizeMatchingKey(file.path || "");
        if (!filePath) return;
        if (filePath.toLowerCase() === targetLower) {
          exactPath.push(filePath);
          return;
        }
        if (removeKnownExtension(filePath).toLowerCase() === targetNoExtLower) {
          pathWithoutExt.push(filePath);
          return;
        }
        if (metadataCache && typeof metadataCache.fileToLinktext === "function") {
          const linktext = normalizeMatchingKey(metadataCache.fileToLinktext(file, sourcePath, true));
          if (linktext && linktext.toLowerCase() === targetLower) {
            linktextExact.push(filePath);
          }
        }
      });
      const pickUnique = (items) => items.length === 1 ? normalizeLinkedContextPath(items[0]) : "";
      return pickUnique(exactPath) || pickUnique(pathWithoutExt) || pickUnique(linktextExact);
    }
    function resolveAbsolutePathToVaultPath(view, absolutePath) {
      const vault = view && view.app && view.app.vault;
      const adapter = vault && vault.adapter;
      const getBasePath = adapter && typeof adapter.getBasePath === "function" ? adapter.getBasePath.bind(adapter) : null;
      if (!getBasePath) return "";
      const basePath = normalizeComparablePath(getBasePath());
      const inputPath = normalizeComparablePath(absolutePath);
      if (!basePath || !inputPath) return "";
      const baseLower = basePath.toLowerCase();
      const inputLower = inputPath.toLowerCase();
      const prefix = `${baseLower}/`;
      if (!inputLower.startsWith(prefix)) return "";
      const relative = inputPath.slice(basePath.length + 1);
      return normalizeLinkedContextPath(relative);
    }
    function resolveDropTokenToLinkablePath(rawToken) {
      const vault = this.app && this.app.vault;
      if (!vault || typeof vault.getAbstractFileByPath !== "function") return "";
      const tryResolveByPath = (pathValue) => {
        const normalized = normalizeLinkedContextPath(pathValue);
        if (!normalized) return "";
        const target = vault.getAbstractFileByPath(normalized);
        if (!isLinkableContextTarget(target)) return "";
        return normalizeLinkedContextPath(target.path || normalized);
      };
      const token = normalizeDropCandidateToken(rawToken);
      if (!token) return "";
      const decodedToken = decodeMaybe(token);
      const directCandidates = buildPathResolutionCandidates(this, token);
      for (const candidate of directCandidates) {
        const directPath = tryResolveByPath(candidate);
        if (directPath) return directPath;
      }
      if (/^(?:[A-Za-z]:[\\/]|\/|\\\\)/.test(decodedToken)) {
        const relative = resolveAbsolutePathToVaultPath(this, decodedToken);
        const relativeCandidates = buildPathResolutionCandidates(this, relative);
        for (const candidate of relativeCandidates) {
          const relativePath = tryResolveByPath(candidate);
          if (relativePath) return relativePath;
        }
      }
      const metadataCache = this.app && this.app.metadataCache;
      if (metadataCache && typeof metadataCache.getFirstLinkpathDest === "function") {
        const workspace = this.app && this.app.workspace;
        const activeFile = workspace && typeof workspace.getActiveFile === "function" ? workspace.getActiveFile() : null;
        const sourcePath = String(activeFile && activeFile.path ? activeFile.path : "");
        const linkpathCandidates = buildPathResolutionCandidates(this, decodedToken);
        for (const linkpath of linkpathCandidates) {
          const resolved = metadataCache.getFirstLinkpathDest(linkpath, sourcePath);
          if (!isLinkableContextTarget(resolved)) continue;
          return normalizeLinkedContextPath(resolved.path);
        }
      }
      const scanned = resolveByLinkpathScan(this, decodedToken);
      if (scanned) return scanned;
      return "";
    }
    function collectDropCandidateTokens(dataTransfer) {
      const rawTokens = [];
      const rawSeen = /* @__PURE__ */ new Set();
      const pushRaw = (value) => {
        const token = String(value || "").trim();
        if (!token || rawSeen.has(token)) return;
        rawSeen.add(token);
        rawTokens.push(token);
      };
      const fileList = dataTransfer && dataTransfer.files ? Array.from(dataTransfer.files) : [];
      fileList.forEach((file) => {
        if (!file || typeof file !== "object") return;
        pushRaw(file.path);
        pushRaw(file.name);
      });
      LINKED_CONTEXT_DROP_TEXT_TYPES.forEach((type) => {
        let value = "";
        try {
          value = dataTransfer.getData(type);
        } catch {
          value = "";
        }
        pushRaw(value);
      });
      const types = dataTransfer && dataTransfer.types ? Array.from(dataTransfer.types) : [];
      types.filter((type) => /obsidian/i.test(String(type || ""))).forEach((type) => {
        let value = "";
        try {
          value = dataTransfer.getData(type);
        } catch {
          value = "";
        }
        pushRaw(value);
      });
      const expanded = [];
      const expandedSeen = /* @__PURE__ */ new Set();
      const pushExpanded = (value) => {
        const token = String(value || "").trim();
        if (!token || expandedSeen.has(token)) return;
        expandedSeen.add(token);
        expanded.push(token);
      };
      rawTokens.forEach((rawToken) => {
        pushExpanded(rawToken);
        splitDropTextTokens(rawToken).forEach((token) => pushExpanded(token));
        extractWikiLinkTargets(rawToken).forEach((token) => pushExpanded(token));
        pushExpanded(extractPathFromObsidianUri(rawToken));
      });
      return expanded;
    }
    function extractLinkedContextPathsFromDropEvent(event) {
      const dataTransfer = event && event.dataTransfer;
      if (!dataTransfer) return [];
      const tokens = collectDropCandidateTokens(dataTransfer);
      if (!tokens.length) return [];
      const resolved = [];
      const seen = /* @__PURE__ */ new Set();
      tokens.forEach((token) => {
        const next = resolveDropTokenToLinkablePath.call(this, token);
        if (!next || seen.has(next)) return;
        seen.add(next);
        resolved.push(next);
      });
      return resolved;
    }
    function looksLikeLinkedContextDropEvent(event) {
      const dataTransfer = event && event.dataTransfer;
      if (!dataTransfer) return false;
      const fileList = dataTransfer.files ? Array.from(dataTransfer.files) : [];
      if (fileList.length > 0) return true;
      const types = dataTransfer.types ? Array.from(dataTransfer.types).map((type) => String(type || "").toLowerCase()) : [];
      if (types.some((type) => type.includes("obsidian"))) return true;
      if (types.includes("text/uri-list")) {
        let uriList = "";
        try {
          uriList = String(dataTransfer.getData("text/uri-list") || "");
        } catch {
          uriList = "";
        }
        if (/^obsidian:\/\//i.test(uriList.trim()) || /^file:\/\//i.test(uriList.trim())) return true;
      }
      let textPlain = "";
      try {
        textPlain = String(dataTransfer.getData("text/plain") || "");
      } catch {
        textPlain = "";
      }
      return /^obsidian:\/\//i.test(textPlain.trim()) || /^file:\/\//i.test(textPlain.trim());
    }
    function setLinkedContextDropActive(isActive) {
      const inputWrapper = this.elements && this.elements.inputWrapper;
      toggleElementClass(inputWrapper, "is-drop-target", Boolean(isActive));
    }
    function handleLinkedContextInputDragOver(event) {
      const droppedPaths = this.extractLinkedContextPathsFromDropEvent(event);
      const shouldHandle = droppedPaths.length > 0 || looksLikeLinkedContextDropEvent(event);
      if (!shouldHandle) {
        this.setLinkedContextDropActive(false);
        return false;
      }
      event.preventDefault();
      if (event.dataTransfer) {
        try {
          event.dataTransfer.dropEffect = "copy";
        } catch {
        }
      }
      this.setLinkedContextDropActive(true);
      return true;
    }
    function handleLinkedContextInputDragLeave(event) {
      const currentTarget = event && event.currentTarget;
      const relatedTarget = event && event.relatedTarget;
      if (currentTarget && relatedTarget && typeof currentTarget.contains === "function" && currentTarget.contains(relatedTarget)) {
        return false;
      }
      this.setLinkedContextDropActive(false);
      return true;
    }
    function handleLinkedContextInputDrop(event) {
      const droppedPaths = this.extractLinkedContextPathsFromDropEvent(event);
      const shouldHandle = droppedPaths.length > 0 || looksLikeLinkedContextDropEvent(event);
      this.setLinkedContextDropActive(false);
      if (!shouldHandle) return false;
      event.preventDefault();
      event.stopPropagation();
      if (!droppedPaths.length) {
        new Notice2(tr(this, "view.context.dropUnsupported", "Unable to link dropped file automatically. Please try selecting it with @."));
        const inputEl2 = this.elements && this.elements.input;
        if (inputEl2 && typeof inputEl2.focus === "function") {
          inputEl2.focus();
        }
        return true;
      }
      const linkedPaths = this.getLinkedContextFilePaths().slice();
      const linkedSet = new Set(linkedPaths);
      let added = 0;
      let skippedByLimit = 0;
      droppedPaths.forEach((pathValue) => {
        if (!pathValue || linkedSet.has(pathValue)) return;
        if (linkedPaths.length >= LINKED_CONTEXT_MAX_FILES) {
          skippedByLimit += 1;
          return;
        }
        linkedPaths.push(pathValue);
        linkedSet.add(pathValue);
        added += 1;
      });
      if (added > 0) {
        this.linkedContextFiles = linkedPaths;
        this.refreshLinkedContextIndicators();
      }
      if (skippedByLimit > 0) {
        new Notice2(tr(this, "view.context.maxFiles", "You can link up to {count} context items at once.", {
          count: LINKED_CONTEXT_MAX_FILES
        }));
      }
      const inputEl = this.elements && this.elements.input;
      if (inputEl && typeof inputEl.focus === "function") {
        inputEl.focus();
      }
      return true;
    }
    var linkedContextDropMethods = {
      extractLinkedContextPathsFromDropEvent,
      setLinkedContextDropActive,
      handleLinkedContextInputDragOver,
      handleLinkedContextInputDragLeave,
      handleLinkedContextInputDrop
    };
    module2.exports = { linkedContextDropMethods };
  }
});

// runtime/view/layout/linked-context-methods.js
var require_linked_context_methods = __commonJS({
  "runtime/view/layout/linked-context-methods.js"(exports2, module2) {
    var { Notice: Notice2, setIcon } = require("obsidian");
    var {
      LINKED_CONTEXT_MAX_FILES,
      LINKED_CONTEXT_MAX_CHARS_PER_FILE,
      LINKED_CONTEXT_MAX_TOTAL_CHARS,
      LINKED_CONTEXT_PICKER_MAX_ITEMS,
      tr,
      normalizeLinkedContextPath,
      displayNameFromPath,
      isLinkableContextFile,
      isLinkableContextFolder,
      createLinkableContextEntry
    } = require_shared_utils();
    var { linkedContextDropMethods } = require_linked_context_drop_methods();
    function getLinkedContextFilePaths() {
      if (!Array.isArray(this.linkedContextFiles)) this.linkedContextFiles = [];
      const seen = /* @__PURE__ */ new Set();
      const normalized = [];
      for (const rawPath of this.linkedContextFiles) {
        const next = normalizeLinkedContextPath(rawPath);
        if (!next || seen.has(next)) continue;
        seen.add(next);
        normalized.push(next);
      }
      this.linkedContextFiles = normalized;
      return normalized;
    }
    function listLinkableVaultFiles() {
      const vault = this.app && this.app.vault;
      if (!vault || typeof vault.getFiles !== "function") return [];
      const entries = vault.getFiles().filter((file) => isLinkableContextFile(file)).map((file) => createLinkableContextEntry(file)).filter(Boolean);
      entries.sort((a, b) => String(a.path || "").localeCompare(String(b.path || ""), void 0, { sensitivity: "base" }));
      return entries;
    }
    function isLinkedContextFolderPath(pathValue) {
      const normalizedPath = normalizeLinkedContextPath(pathValue);
      if (!normalizedPath) return false;
      const vault = this.app && this.app.vault;
      if (!vault || typeof vault.getAbstractFileByPath !== "function") return false;
      const target = vault.getAbstractFileByPath(normalizedPath);
      return isLinkableContextFolder(target);
    }
    function refreshLinkedContextIndicators() {
      const contextRow = this.elements && this.elements.contextRow;
      const fileIndicator = this.elements && this.elements.fileIndicator;
      const selectionIndicator = this.elements && this.elements.selectionIndicator;
      if (!contextRow || !fileIndicator || !selectionIndicator) return;
      const linkedPaths = this.getLinkedContextFilePaths();
      fileIndicator.empty();
      if (!linkedPaths.length) {
        contextRow.toggleClass("has-content", false);
        fileIndicator.style.display = "none";
        selectionIndicator.textContent = "";
        selectionIndicator.setAttr("title", "");
        return;
      }
      linkedPaths.forEach((pathValue) => {
        const isFolder = isLinkedContextFolderPath.call(this, pathValue);
        const chip = fileIndicator.createDiv({
          cls: "oc-context-file-chip",
          attr: { title: pathValue }
        });
        if (isFolder) chip.addClass("is-folder");
        const iconEl = chip.createSpan({ cls: "oc-context-file-chip-icon" });
        setIcon(iconEl, isFolder ? "folder" : "file-text");
        chip.createSpan({
          cls: "oc-context-file-chip-name",
          text: displayNameFromPath(pathValue)
        });
        const removeBtn = chip.createEl("button", {
          cls: "oc-context-file-chip-remove",
          text: "\xD7"
        });
        removeBtn.setAttr("type", "button");
        removeBtn.setAttr("aria-label", tr(this, "view.context.removeFile", "Remove linked context"));
        removeBtn.setAttr("title", tr(this, "view.context.removeFile", "Remove linked context"));
        removeBtn.addEventListener("click", (event) => {
          event.preventDefault();
          event.stopPropagation();
          this.removeLinkedContextFile(pathValue);
        });
      });
      contextRow.toggleClass("has-content", true);
      fileIndicator.style.display = "flex";
      const counterText = tr(this, "view.context.fileCount", "{count} linked context item(s)", { count: linkedPaths.length });
      selectionIndicator.textContent = counterText;
      selectionIndicator.setAttr("title", counterText);
    }
    function toggleLinkedContextFile(pathOrFile) {
      const normalizedPath = normalizeLinkedContextPath(pathOrFile && pathOrFile.path ? pathOrFile.path : pathOrFile);
      if (!normalizedPath) return;
      const linkedPaths = this.getLinkedContextFilePaths();
      const existingIndex = linkedPaths.indexOf(normalizedPath);
      if (existingIndex >= 0) {
        linkedPaths.splice(existingIndex, 1);
        this.linkedContextFiles = linkedPaths;
        this.refreshLinkedContextIndicators();
        return;
      }
      if (linkedPaths.length >= LINKED_CONTEXT_MAX_FILES) {
        new Notice2(tr(this, "view.context.maxFiles", "You can link up to {count} context items at once.", {
          count: LINKED_CONTEXT_MAX_FILES
        }));
        return;
      }
      linkedPaths.push(normalizedPath);
      this.linkedContextFiles = linkedPaths;
      this.refreshLinkedContextIndicators();
    }
    function removeLinkedContextFile(pathValue) {
      const normalizedPath = normalizeLinkedContextPath(pathValue);
      if (!normalizedPath) return;
      const linkedPaths = this.getLinkedContextFilePaths().filter((item) => item !== normalizedPath);
      this.linkedContextFiles = linkedPaths;
      this.refreshLinkedContextIndicators();
    }
    function clearLinkedContextFiles(options = {}) {
      const linkedPaths = this.getLinkedContextFilePaths();
      if (!linkedPaths.length) {
        if (options && options.closePicker) {
          this.closeLinkedContextFilePicker();
        }
        return;
      }
      this.linkedContextFiles = [];
      this.refreshLinkedContextIndicators();
      if (options && options.closePicker) {
        this.closeLinkedContextFilePicker();
      }
    }
    function ensureLinkedContextPickerState() {
      if (!this.linkedContextFilePicker || typeof this.linkedContextFilePicker !== "object") {
        this.linkedContextFilePicker = {
          visible: false,
          mode: "button",
          mentionStart: -1,
          query: "",
          selectedIndex: 0,
          entries: [],
          filtered: [],
          lastLoadAt: 0,
          rootEl: null,
          searchEl: null,
          listEl: null,
          emptyEl: null
        };
      }
      return this.linkedContextFilePicker;
    }
    function ensureSlashCommandPickerState() {
      if (!this.slashCommandPicker || typeof this.slashCommandPicker !== "object") {
        this.slashCommandPicker = {
          visible: false,
          mode: "mention",
          mentionStart: -1,
          query: "",
          selectedIndex: 0,
          entries: [],
          filtered: [],
          lastLoadAt: 0,
          rootEl: null,
          listEl: null
        };
      }
      return this.slashCommandPicker;
    }
    function listSlashCommandEntries() {
      const entries = [];
      const seen = /* @__PURE__ */ new Set();
      const pushEntry = (raw) => {
        if (!raw || typeof raw !== "object") return;
        const command = String(raw.command || "").trim();
        if (!command || !command.startsWith("/")) return;
        const normalized = command.toLowerCase();
        if (seen.has(normalized)) return;
        seen.add(normalized);
        const name = String(raw.name || "").trim();
        const description = String(raw.description || "").trim();
        const insert = String(raw.insert || `${command} `);
        entries.push({
          type: String(raw.type || "command"),
          command,
          name,
          description,
          insert,
          order: Number(raw.order || 0),
          search: `${command.slice(1)} ${name} ${description}`.toLowerCase()
        });
      };
      const skillService = this.plugin && this.plugin.skillService;
      if (skillService && typeof skillService.loadSkills === "function") {
        try {
          skillService.loadSkills();
        } catch {
        }
      }
      const skills = skillService && typeof skillService.getSkills === "function" ? skillService.getSkills() : [];
      skills.slice().sort((a, b) => String(a && a.id ? a.id : "").localeCompare(String(b && b.id ? b.id : ""))).forEach((skill) => {
        const id = String(skill && skill.id ? skill.id : "").trim();
        if (!id) return;
        const command = `/${id}`;
        const name = String(skill && skill.name ? skill.name : id).trim() || id;
        const description = typeof this.getSkillPrimaryDescription === "function" ? String(this.getSkillPrimaryDescription(skill) || "") : "";
        pushEntry({
          type: "skill",
          command,
          name,
          description,
          insert: `${command} `,
          order: 10
        });
      });
      return entries;
    }
    function detectSlashCommandQuery() {
      const inputEl = this.elements && this.elements.input;
      if (!inputEl) return null;
      const text = String(inputEl.value || "");
      const cursor = Number(inputEl.selectionStart || 0);
      const beforeCursor = text.slice(0, cursor);
      const match = beforeCursor.match(/^\s*\/([^\s]*)$/);
      if (!match) return null;
      const slashIndex = beforeCursor.lastIndexOf("/");
      if (slashIndex < 0) return null;
      return {
        start: slashIndex,
        cursor,
        query: String(match[1] || "")
      };
    }
    function closeSlashCommandPicker(options = {}) {
      const picker = ensureSlashCommandPickerState.call(this);
      picker.visible = false;
      picker.mode = "mention";
      picker.mentionStart = -1;
      picker.query = "";
      picker.selectedIndex = 0;
      picker.filtered = [];
      if (picker.rootEl && picker.rootEl.isConnected) {
        picker.rootEl.remove();
      }
      picker.rootEl = null;
      picker.listEl = null;
      if (options && options.focusInput && this.elements && this.elements.input) {
        this.elements.input.focus();
      }
    }
    function filterSlashCommandPickerEntries(picker) {
      const query = String(picker.query || "").trim().toLowerCase();
      const base = Array.isArray(picker.entries) ? picker.entries.slice() : [];
      const matchEntry = (entry) => {
        if (!entry || typeof entry !== "object") return false;
        if (!query) return true;
        return String(entry.search || "").includes(query);
      };
      const scoreEntry = (entry) => {
        const commandText = String(entry.command || "").replace(/^\//, "").toLowerCase();
        const nameText = String(entry.name || "").toLowerCase();
        const startsCommand = query ? commandText.startsWith(query) : false;
        const startsName = query ? nameText.startsWith(query) : false;
        return {
          startsCommand,
          startsName,
          order: Number(entry.order || 0),
          command: commandText,
          name: nameText
        };
      };
      const filtered = base.filter((entry) => matchEntry(entry)).sort((a, b) => {
        const aScore = scoreEntry(a);
        const bScore = scoreEntry(b);
        if (aScore.startsCommand !== bScore.startsCommand) return aScore.startsCommand ? -1 : 1;
        if (aScore.startsName !== bScore.startsName) return aScore.startsName ? -1 : 1;
        if (aScore.order !== bScore.order) return aScore.order - bScore.order;
        if (aScore.command !== bScore.command) return aScore.command.localeCompare(bScore.command);
        return aScore.name.localeCompare(bScore.name);
      }).slice(0, LINKED_CONTEXT_PICKER_MAX_ITEMS);
      picker.filtered = filtered;
      if (!filtered.length) {
        picker.selectedIndex = 0;
        return;
      }
      const maxIndex = filtered.length - 1;
      picker.selectedIndex = Math.max(0, Math.min(maxIndex, Number(picker.selectedIndex || 0)));
    }
    function moveSlashCommandPickerSelection(delta) {
      const picker = ensureSlashCommandPickerState.call(this);
      if (!picker.visible || !Array.isArray(picker.filtered) || !picker.filtered.length) return;
      const maxIndex = picker.filtered.length - 1;
      picker.selectedIndex = Math.max(0, Math.min(maxIndex, Number(picker.selectedIndex || 0) + Number(delta || 0)));
      renderSlashCommandPickerList.call(this);
    }
    function selectSlashCommandPickerEntry(index, options = {}) {
      const picker = ensureSlashCommandPickerState.call(this);
      const item = Array.isArray(picker.filtered) ? picker.filtered[index] : null;
      if (!item || !item.command) return;
      const fromMention = options && Object.prototype.hasOwnProperty.call(options, "fromMention") ? Boolean(options.fromMention) : true;
      if (fromMention && this.elements && this.elements.input) {
        const inputEl = this.elements.input;
        const text = String(inputEl.value || "");
        let start = Number(picker.mentionStart || -1);
        let end = Number(inputEl.selectionStart || 0);
        if (start < 0 || end < start || text.charAt(start) !== "/") {
          const mention = detectSlashCommandQuery.call(this);
          if (mention) {
            start = mention.start;
            end = mention.cursor;
          }
        }
        if (start >= 0 && end >= start) {
          const before = text.slice(0, start);
          const after = text.slice(end).replace(/^\s+/, "");
          const insertText = String(item.insert || `${item.command} `);
          inputEl.value = `${before}${insertText}${after}`;
          const nextCursor = before.length + insertText.length;
          inputEl.selectionStart = inputEl.selectionEnd = nextCursor;
        }
      }
      const closeAfterSelect = options && Object.prototype.hasOwnProperty.call(options, "close") ? Boolean(options.close) : true;
      if (closeAfterSelect) {
        closeSlashCommandPicker.call(this, { focusInput: true });
      } else {
        renderSlashCommandPickerList.call(this);
      }
    }
    function renderSlashCommandPickerList() {
      const picker = ensureSlashCommandPickerState.call(this);
      if (!picker.visible || !picker.listEl || !picker.rootEl) return;
      filterSlashCommandPickerEntries.call(this, picker);
      picker.listEl.empty();
      if (!picker.filtered.length) {
        picker.listEl.createDiv({
          cls: "oc-context-file-picker-empty",
          text: tr(this, "view.command.picker.empty", "No matching commands.")
        });
        return;
      }
      picker.filtered.forEach((entry, index) => {
        const item = picker.listEl.createDiv({
          cls: "oc-context-file-picker-item oc-slash-command-item",
          attr: { title: [entry.command, entry.name, entry.description].filter(Boolean).join(" - ") }
        });
        if (index === picker.selectedIndex) item.addClass("is-selected");
        const textWrap = item.createDiv({ cls: "oc-context-file-picker-item-text" });
        textWrap.createDiv({ cls: "oc-slash-command-item-command", text: entry.command });
        const meta = textWrap.createDiv({ cls: "oc-slash-command-item-meta" });
        if (entry.name) {
          meta.createSpan({ cls: "oc-slash-command-item-name", text: entry.name });
        }
        if (entry.description) {
          meta.createSpan({ cls: "oc-slash-command-item-desc", text: entry.description });
        }
        item.addEventListener("mouseenter", () => {
          if (picker.selectedIndex === index) return;
          picker.selectedIndex = index;
          const previousSelected = picker.listEl ? picker.listEl.querySelector(".oc-slash-command-item.is-selected") : null;
          if (previousSelected && previousSelected !== item) {
            previousSelected.classList.remove("is-selected");
          }
          item.classList.add("is-selected");
        });
        item.addEventListener("mousedown", (event) => {
          event.stopPropagation();
        });
        item.addEventListener("click", (event) => {
          event.stopPropagation();
          selectSlashCommandPickerEntry.call(this, index, { close: true, fromMention: true });
        });
      });
    }
    function renderSlashCommandPicker() {
      const picker = ensureSlashCommandPickerState.call(this);
      if (!picker.visible) {
        closeSlashCommandPicker.call(this);
        return;
      }
      const inputWrapper = this.elements && this.elements.inputWrapper;
      if (!inputWrapper) return;
      if (!picker.rootEl || !picker.rootEl.isConnected) {
        picker.rootEl = inputWrapper.createDiv({ cls: "oc-context-file-picker oc-slash-command-picker is-mention-mode" });
        picker.rootEl.addEventListener("mousedown", (event) => {
          event.stopPropagation();
        });
        picker.rootEl.addEventListener("click", (event) => {
          event.stopPropagation();
        });
        picker.listEl = picker.rootEl.createDiv({ cls: "oc-context-file-picker-list" });
      }
      renderSlashCommandPickerList.call(this);
    }
    function syncSlashCommandPickerFromInputMention() {
      const picker = ensureSlashCommandPickerState.call(this);
      const mention = detectSlashCommandQuery.call(this);
      if (!mention) {
        if (picker.visible && picker.mode === "mention") {
          closeSlashCommandPicker.call(this);
        }
        return;
      }
      const linkedPicker = this.ensureLinkedContextPickerState();
      if (linkedPicker.visible && linkedPicker.mode === "mention") {
        this.closeLinkedContextFilePicker({ preserveSlash: true });
      }
      const now = Date.now();
      const shouldReloadEntries = !Array.isArray(picker.entries) || !picker.entries.length || !picker.visible || picker.mode !== "mention" || now - Number(picker.lastLoadAt || 0) > 5e3;
      if (shouldReloadEntries) {
        picker.entries = listSlashCommandEntries.call(this);
        picker.lastLoadAt = now;
      }
      if (!picker.entries.length) {
        if (picker.visible) {
          closeSlashCommandPicker.call(this);
        }
        return;
      }
      const isDifferentQuery = picker.query !== mention.query;
      picker.mode = "mention";
      picker.mentionStart = mention.start;
      picker.query = mention.query;
      picker.visible = true;
      if (isDifferentQuery || !picker.filtered.length) {
        picker.selectedIndex = 0;
      }
      this.ensureLinkedContextPickerDocumentBinding();
      renderSlashCommandPicker.call(this);
    }
    function handleSlashCommandInputKeydown(event) {
      const picker = ensureSlashCommandPickerState.call(this);
      if (!picker.visible || picker.mode !== "mention") return false;
      if (event.key === "ArrowDown") {
        event.preventDefault();
        moveSlashCommandPickerSelection.call(this, 1);
        return true;
      }
      if (event.key === "ArrowUp") {
        event.preventDefault();
        moveSlashCommandPickerSelection.call(this, -1);
        return true;
      }
      if ((event.key === "Enter" || event.key === "Tab") && !event.isComposing) {
        event.preventDefault();
        if (Array.isArray(picker.filtered) && picker.filtered.length) {
          selectSlashCommandPickerEntry.call(this, picker.selectedIndex, { close: true, fromMention: true });
          return true;
        }
        closeSlashCommandPicker.call(this, { focusInput: true });
        return true;
      }
      if (event.key === "Escape" && !event.isComposing) {
        event.preventDefault();
        closeSlashCommandPicker.call(this, { focusInput: true });
        return true;
      }
      return false;
    }
    function ensureLinkedContextPickerDocumentBinding() {
      if (this.linkedContextFilePickerDocumentBound) return;
      this.linkedContextFilePickerDocumentBound = true;
      this.registerDomEvent(document, "click", (event) => {
        const target = event && event.target ? event.target : null;
        if (typeof Node !== "undefined" && !(target instanceof Node)) return;
        const picker = this.ensureLinkedContextPickerState();
        if (picker.visible) {
          if (!(picker.rootEl && picker.rootEl.contains(target)) && !(this.elements && this.elements.attachFileBtn && this.elements.attachFileBtn.contains(target))) {
            this.closeLinkedContextFilePicker({ preserveSlash: true });
          }
        }
        const slashPicker = ensureSlashCommandPickerState.call(this);
        if (slashPicker.visible) {
          const inputEl = this.elements && this.elements.input;
          const insideSlash = Boolean(slashPicker.rootEl && slashPicker.rootEl.contains(target)) || Boolean(inputEl && target === inputEl);
          if (!insideSlash) {
            closeSlashCommandPicker.call(this);
          }
        }
      });
    }
    function closeLinkedContextFilePicker(options = {}) {
      const picker = this.ensureLinkedContextPickerState();
      picker.visible = false;
      picker.mode = "button";
      picker.mentionStart = -1;
      picker.query = "";
      picker.selectedIndex = 0;
      picker.filtered = [];
      if (picker.rootEl && picker.rootEl.isConnected) {
        picker.rootEl.remove();
      }
      picker.rootEl = null;
      picker.searchEl = null;
      picker.listEl = null;
      picker.emptyEl = null;
      if (!(options && options.preserveSlash)) {
        closeSlashCommandPicker.call(this);
      }
      if (options && options.focusInput && this.elements && this.elements.input) {
        this.elements.input.focus();
      }
    }
    function detectLinkedContextMentionQuery() {
      const inputEl = this.elements && this.elements.input;
      if (!inputEl) return null;
      const text = String(inputEl.value || "");
      const cursor = Number(inputEl.selectionStart || 0);
      const beforeCursor = text.slice(0, cursor);
      const atIndex = beforeCursor.lastIndexOf("@");
      if (atIndex < 0) return null;
      const charBefore = atIndex > 0 ? beforeCursor.charAt(atIndex - 1) : " ";
      if (atIndex > 0 && !/\s/.test(charBefore)) return null;
      const token = beforeCursor.slice(atIndex + 1);
      if (/\s/.test(token)) return null;
      return {
        start: atIndex,
        cursor,
        query: token
      };
    }
    function syncLinkedContextPickerFromInputMention() {
      syncSlashCommandPickerFromInputMention.call(this);
      const picker = this.ensureLinkedContextPickerState();
      const mention = this.detectLinkedContextMentionQuery();
      if (!mention) {
        if (picker.visible && picker.mode === "mention") {
          this.closeLinkedContextFilePicker();
        }
        return;
      }
      const now = Date.now();
      const shouldReloadEntries = !Array.isArray(picker.entries) || !picker.entries.length || !picker.visible || picker.mode !== "mention" || now - Number(picker.lastLoadAt || 0) > 5e3;
      if (shouldReloadEntries) {
        picker.entries = this.listLinkableVaultFiles();
        picker.lastLoadAt = now;
      }
      if (!picker.entries.length) {
        if (picker.visible && picker.mode === "mention") {
          this.closeLinkedContextFilePicker();
        }
        return;
      }
      closeSlashCommandPicker.call(this);
      const isDifferentQuery = picker.query !== mention.query;
      picker.mode = "mention";
      picker.mentionStart = mention.start;
      picker.query = mention.query;
      picker.visible = true;
      if (isDifferentQuery || !picker.filtered.length) {
        picker.selectedIndex = 0;
      }
      this.ensureLinkedContextPickerDocumentBinding();
      this.renderLinkedContextFilePicker();
    }
    function handleLinkedContextInputKeydown(event) {
      if (handleSlashCommandInputKeydown.call(this, event)) return true;
      const picker = this.ensureLinkedContextPickerState();
      if (!picker.visible || picker.mode !== "mention") return false;
      if (event.key === "ArrowDown") {
        event.preventDefault();
        this.moveLinkedContextPickerSelection(1);
        return true;
      }
      if (event.key === "ArrowUp") {
        event.preventDefault();
        this.moveLinkedContextPickerSelection(-1);
        return true;
      }
      if ((event.key === "Enter" || event.key === "Tab") && !event.isComposing) {
        event.preventDefault();
        if (Array.isArray(picker.filtered) && picker.filtered.length) {
          this.selectLinkedContextPickerEntry(picker.selectedIndex, { close: true, fromMention: true });
          return true;
        }
        this.closeLinkedContextFilePicker({ focusInput: true });
        return true;
      }
      if (event.key === "Escape" && !event.isComposing) {
        event.preventDefault();
        this.closeLinkedContextFilePicker({ focusInput: true });
        return true;
      }
      return false;
    }
    function filterLinkedContextPickerEntries(picker) {
      const query = String(picker.query || "").trim().toLowerCase();
      const linkedSet = new Set(this.getLinkedContextFilePaths());
      const base = Array.isArray(picker.entries) ? picker.entries.slice() : [];
      const filtered = base.filter((entry) => {
        if (!entry || typeof entry !== "object") return false;
        if (!query) return true;
        return String(entry.search || "").includes(query);
      }).sort((a, b) => {
        const aName = String(a.name || "").toLowerCase();
        const bName = String(b.name || "").toLowerCase();
        const aStarts = query ? aName.startsWith(query) : false;
        const bStarts = query ? bName.startsWith(query) : false;
        if (aStarts && !bStarts) return -1;
        if (!aStarts && bStarts) return 1;
        const aLinked = linkedSet.has(a.path);
        const bLinked = linkedSet.has(b.path);
        if (aLinked && !bLinked) return -1;
        if (!aLinked && bLinked) return 1;
        const aMtime = Number(a.mtime || 0);
        const bMtime = Number(b.mtime || 0);
        if (aMtime !== bMtime) return bMtime - aMtime;
        return String(a.path || "").localeCompare(String(b.path || ""), void 0, { sensitivity: "base" });
      }).slice(0, LINKED_CONTEXT_PICKER_MAX_ITEMS);
      picker.filtered = filtered;
      if (!filtered.length) {
        picker.selectedIndex = 0;
        return;
      }
      const maxIndex = filtered.length - 1;
      picker.selectedIndex = Math.max(0, Math.min(maxIndex, Number(picker.selectedIndex || 0)));
    }
    function moveLinkedContextPickerSelection(delta) {
      const picker = this.ensureLinkedContextPickerState();
      if (!picker.visible || !Array.isArray(picker.filtered) || !picker.filtered.length) return;
      const maxIndex = picker.filtered.length - 1;
      picker.selectedIndex = Math.max(0, Math.min(maxIndex, Number(picker.selectedIndex || 0) + Number(delta || 0)));
      this.renderLinkedContextFilePickerList();
    }
    function selectLinkedContextPickerEntry(index, options = {}) {
      const picker = this.ensureLinkedContextPickerState();
      const item = Array.isArray(picker.filtered) ? picker.filtered[index] : null;
      if (!item || !item.path) return;
      const fromMention = Boolean(options && options.fromMention);
      if (fromMention && this.elements && this.elements.input) {
        const inputEl = this.elements.input;
        const text = String(inputEl.value || "");
        let start = Number(picker.mentionStart || -1);
        let end = Number(inputEl.selectionStart || 0);
        if (start < 0 || end < start || text.charAt(start) !== "@") {
          const mention = this.detectLinkedContextMentionQuery();
          if (mention) {
            start = mention.start;
            end = mention.cursor;
          }
        }
        if (start >= 0 && end >= start) {
          const before = text.slice(0, start);
          const after = text.slice(end).replace(/^\s+/, "");
          const needsSpace = Boolean(before && after && !/\s$/.test(before));
          const next = `${before}${needsSpace ? " " : ""}${after}`;
          const nextCursor = before.length + (needsSpace ? 1 : 0);
          inputEl.value = next;
          inputEl.selectionStart = inputEl.selectionEnd = nextCursor;
        }
      }
      this.toggleLinkedContextFile(item.path);
      const closeAfterSelect = options && Object.prototype.hasOwnProperty.call(options, "close") ? Boolean(options.close) : true;
      if (closeAfterSelect) {
        this.closeLinkedContextFilePicker({ focusInput: true });
      } else {
        this.renderLinkedContextFilePickerList();
      }
    }
    function renderLinkedContextFilePickerList() {
      const picker = this.ensureLinkedContextPickerState();
      if (!picker.visible || !picker.listEl || !picker.rootEl) return;
      this.filterLinkedContextPickerEntries(picker);
      picker.listEl.empty();
      if (!picker.filtered.length) {
        picker.listEl.createDiv({
          cls: "oc-context-file-picker-empty",
          text: tr(this, "view.context.picker.empty", "No matching files.")
        });
        return;
      }
      const linkedSet = new Set(this.getLinkedContextFilePaths());
      picker.filtered.forEach((entry, index) => {
        const item = picker.listEl.createDiv({
          cls: "oc-context-file-picker-item",
          attr: { title: entry.path }
        });
        if (index === picker.selectedIndex) item.addClass("is-selected");
        if (linkedSet.has(entry.path)) item.addClass("is-linked");
        const textWrap = item.createDiv({ cls: "oc-context-file-picker-item-text" });
        textWrap.createDiv({ cls: "oc-context-file-picker-item-name", text: entry.name || displayNameFromPath(entry.path) || entry.path });
        textWrap.createDiv({ cls: "oc-context-file-picker-item-path", text: entry.path });
        if (linkedSet.has(entry.path)) {
          item.createDiv({ cls: "oc-context-file-picker-item-meta", text: tr(this, "view.context.picker.linked", "Linked") });
        }
        item.addEventListener("mouseenter", () => {
          if (picker.selectedIndex === index) return;
          picker.selectedIndex = index;
          const previousSelected = picker.listEl ? picker.listEl.querySelector(".oc-context-file-picker-item.is-selected") : null;
          if (previousSelected && previousSelected !== item) {
            previousSelected.classList.remove("is-selected");
          }
          item.classList.add("is-selected");
        });
        item.addEventListener("mousedown", (event) => {
          event.stopPropagation();
        });
        item.addEventListener("click", (event) => {
          event.stopPropagation();
          this.selectLinkedContextPickerEntry(index, { close: true, fromMention: picker.mode === "mention" });
        });
      });
    }
    function renderLinkedContextFilePicker() {
      const picker = this.ensureLinkedContextPickerState();
      if (!picker.visible) {
        this.closeLinkedContextFilePicker();
        return;
      }
      const inputWrapper = this.elements && this.elements.inputWrapper;
      if (!inputWrapper) return;
      if (!picker.rootEl || !picker.rootEl.isConnected) {
        picker.rootEl = inputWrapper.createDiv({ cls: "oc-context-file-picker" });
        picker.rootEl.addEventListener("mousedown", (event) => {
          event.stopPropagation();
        });
        picker.rootEl.addEventListener("click", (event) => {
          event.stopPropagation();
        });
        picker.searchEl = picker.rootEl.createEl("input", {
          cls: "oc-context-file-picker-search",
          attr: {
            type: "text",
            placeholder: tr(this, "view.context.picker.search", "Search Obsidian files...")
          }
        });
        picker.searchEl.addEventListener("input", () => {
          picker.query = String(picker.searchEl.value || "");
          picker.selectedIndex = 0;
          this.renderLinkedContextFilePickerList();
        });
        picker.searchEl.addEventListener("keydown", (event) => {
          if (event.key === "ArrowDown") {
            event.preventDefault();
            this.moveLinkedContextPickerSelection(1);
            return;
          }
          if (event.key === "ArrowUp") {
            event.preventDefault();
            this.moveLinkedContextPickerSelection(-1);
            return;
          }
          if ((event.key === "Enter" || event.key === "Tab") && !event.isComposing) {
            event.preventDefault();
            this.selectLinkedContextPickerEntry(picker.selectedIndex, { close: true, fromMention: picker.mode === "mention" });
            return;
          }
          if (event.key === "Escape" && !event.isComposing) {
            event.preventDefault();
            this.closeLinkedContextFilePicker({ focusInput: true });
          }
        });
        picker.listEl = picker.rootEl.createDiv({ cls: "oc-context-file-picker-list" });
      }
      const mentionMode = picker.mode === "mention";
      if (picker.rootEl) {
        picker.rootEl.toggleClass("is-mention-mode", mentionMode);
      }
      if (picker.searchEl) {
        picker.searchEl.value = String(picker.query || "");
        picker.searchEl.style.display = mentionMode ? "none" : "";
      }
      this.renderLinkedContextFilePickerList();
    }
    function openLinkedContextFilePicker() {
      closeSlashCommandPicker.call(this);
      const picker = this.ensureLinkedContextPickerState();
      if (picker.visible) {
        if (picker.mode === "mention") {
          picker.mode = "button";
          picker.mentionStart = -1;
          picker.query = "";
          picker.selectedIndex = 0;
          if (!Array.isArray(picker.entries) || !picker.entries.length) {
            picker.entries = this.listLinkableVaultFiles();
          }
          this.renderLinkedContextFilePicker();
          if (picker.searchEl) picker.searchEl.focus();
          return;
        }
        this.closeLinkedContextFilePicker({ focusInput: true });
        return;
      }
      const entries = this.listLinkableVaultFiles();
      if (!entries.length) {
        new Notice2(tr(this, "view.context.noFiles", "No linkable vault files found."));
        return;
      }
      picker.entries = entries;
      picker.lastLoadAt = Date.now();
      picker.mode = "button";
      picker.mentionStart = -1;
      picker.query = "";
      picker.selectedIndex = 0;
      picker.visible = true;
      this.ensureLinkedContextPickerDocumentBinding();
      this.renderLinkedContextFilePicker();
      if (picker.searchEl) picker.searchEl.focus();
    }
    async function buildLinkedContextPromptBlock(options = {}) {
      const candidatePaths = options && Array.isArray(options.linkedPaths) ? options.linkedPaths : this.getLinkedContextFilePaths();
      const linkedPaths = candidatePaths.map((pathValue) => normalizeLinkedContextPath(pathValue)).filter(Boolean);
      if (!linkedPaths.length) return "";
      const vault = this.app && this.app.vault;
      if (!vault || typeof vault.getAbstractFileByPath !== "function") return "";
      const sections = [
        "Additional context from user-linked Obsidian targets (@):",
        "Use these files/folders as reference context when answering the request."
      ];
      let totalChars = 0;
      let includedFiles = 0;
      let includedFolders = 0;
      let skipped = 0;
      for (const pathValue of linkedPaths) {
        const target = vault.getAbstractFileByPath(pathValue);
        if (!target || typeof target.path !== "string") {
          skipped += 1;
          continue;
        }
        if (isLinkableContextFolder(target)) {
          includedFolders += 1;
          sections.push("");
          sections.push(`<<<FLOWNOTE_FOLDER path="${target.path}">>>`);
          sections.push("[Folder path only; file contents are not attached.]");
          sections.push("<<<END_FLOWNOTE_FOLDER>>>");
          continue;
        }
        if (!isLinkableContextFile(target)) {
          skipped += 1;
          continue;
        }
        let rawText = "";
        try {
          rawText = typeof vault.cachedRead === "function" ? await vault.cachedRead(target) : await vault.read(target);
        } catch {
          skipped += 1;
          continue;
        }
        const content = String(rawText || "");
        const remain = LINKED_CONTEXT_MAX_TOTAL_CHARS - totalChars;
        if (remain <= 0) {
          skipped += 1;
          continue;
        }
        const allowed = Math.min(remain, LINKED_CONTEXT_MAX_CHARS_PER_FILE);
        const snippet = content.slice(0, allowed);
        const truncated = content.length > snippet.length;
        totalChars += snippet.length;
        includedFiles += 1;
        sections.push(``);
        sections.push(`<<<FLOWNOTE_FILE path="${target.path}">>>`);
        sections.push(snippet);
        if (truncated) {
          sections.push(`[Truncated to ${snippet.length} chars]`);
        }
        sections.push("<<<END_FLOWNOTE_FILE>>>");
      }
      if (!includedFiles && !includedFolders) return "";
      if (skipped > 0) {
        sections.push(``);
        sections.push(`[Note] ${skipped} linked target(s) were skipped due to read errors or size limits.`);
      }
      return sections.join("\n");
    }
    async function composePromptWithLinkedFiles(basePrompt, options = {}) {
      const prompt = String(basePrompt || "");
      if (!prompt) return prompt;
      const candidatePaths = options && Array.isArray(options.linkedPaths) ? options.linkedPaths : this.getLinkedContextFilePaths();
      const linkedPaths = candidatePaths.map((pathValue) => normalizeLinkedContextPath(pathValue)).filter(Boolean);
      if (!linkedPaths.length) return prompt;
      try {
        const contextBlock = await this.buildLinkedContextPromptBlock({ linkedPaths });
        if (!contextBlock) return prompt;
        return `${contextBlock}

User request:
${prompt}`;
      } catch (error) {
        if (this.plugin && typeof this.plugin.log === "function") {
          this.plugin.log(`compose linked context prompt failed: ${error instanceof Error ? error.message : String(error)}`);
        }
        return prompt;
      }
    }
    var linkedContextMethods = {
      getLinkedContextFilePaths,
      listLinkableVaultFiles,
      refreshLinkedContextIndicators,
      toggleLinkedContextFile,
      removeLinkedContextFile,
      clearLinkedContextFiles,
      ...linkedContextDropMethods,
      ensureLinkedContextPickerState,
      ensureLinkedContextPickerDocumentBinding,
      closeLinkedContextFilePicker,
      detectLinkedContextMentionQuery,
      syncLinkedContextPickerFromInputMention,
      handleLinkedContextInputKeydown,
      filterLinkedContextPickerEntries,
      moveLinkedContextPickerSelection,
      selectLinkedContextPickerEntry,
      renderLinkedContextFilePickerList,
      renderLinkedContextFilePicker,
      openLinkedContextFilePicker,
      buildLinkedContextPromptBlock,
      composePromptWithLinkedFiles
    };
    module2.exports = { linkedContextMethods };
  }
});

// runtime/view/layout/main-composer-methods.js
var require_main_composer_methods = __commonJS({
  "runtime/view/layout/main-composer-methods.js"(exports2, module2) {
    var { Notice: Notice2, setIcon } = require("obsidian");
    var { tr } = require_shared_utils();
    var OPENCODE_DOCS_URL = "https://opencode.ai/docs";
    function normalizeDiagnosticsResult(result) {
      const raw = result && typeof result === "object" ? result : {};
      const connection = raw.connection && typeof raw.connection === "object" ? raw.connection : {};
      const executable = raw.executable && typeof raw.executable === "object" ? raw.executable : {};
      return {
        connection: {
          ok: Boolean(connection.ok),
          mode: String(connection.mode || "sdk"),
          error: String(connection.error || "").trim()
        },
        executable: {
          ok: Boolean(executable.ok),
          path: String(executable.path || "").trim(),
          hint: String(executable.hint || "").trim()
        }
      };
    }
    function connectionCheckCommands() {
      if (typeof process !== "undefined" && process && process.platform === "win32") {
        return ["opencode --version", "where opencode"];
      }
      return ["opencode --version", "which opencode"];
    }
    function windowsInstallGuideCommands() {
      return [
        "node -v",
        "npm -v",
        "npm install -g opencode-ai",
        "opencode --version",
        "where opencode"
      ];
    }
    function isLikelyMissingOpenCode(result) {
      const normalized = normalizeDiagnosticsResult(result);
      if (!normalized.executable.ok) return true;
      const err = normalized.connection.error.toLowerCase();
      if (!err) return false;
      return /not found|command not found|enoent|executable not found|未找到|找不到/.test(err);
    }
    function isLikelyWindowsWslInstallIssue(result) {
      const isWindows = typeof process !== "undefined" && process && process.platform === "win32";
      if (!isWindows) return false;
      const normalized = normalizeDiagnosticsResult(result);
      const err = normalized.connection.error.toLowerCase();
      return /wsl(?:\.exe|\(|:|\/)|windows\s*\+\s*wsl/.test(err);
    }
    async function copyText(text) {
      const value = String(text || "");
      if (!value) return false;
      try {
        if (typeof navigator !== "undefined" && navigator.clipboard && typeof navigator.clipboard.writeText === "function") {
          await navigator.clipboard.writeText(value);
          return true;
        }
      } catch {
      }
      return false;
    }
    function renderConnectionStatusPopoverContent(view, result) {
      const popover = view.elements && view.elements.statusPopover;
      if (!popover) return;
      const hasResult = Boolean(result && typeof result === "object" && result.connection && result.executable);
      const normalized = normalizeDiagnosticsResult(result);
      popover.empty();
      const title = popover.createDiv({ cls: "oc-connection-popover-title" });
      const body = popover.createDiv({ cls: "oc-connection-popover-body" });
      const appendLine = (text) => {
        if (!text) return;
        body.createDiv({ cls: "oc-connection-popover-line", text: String(text) });
      };
      const appendCommand = (cmd) => {
        if (!cmd) return;
        const line = body.createDiv({ cls: "oc-connection-popover-line" });
        line.createEl("code", { text: String(cmd) });
      };
      const appendLink = (label, url) => {
        if (!label || !url) return;
        const line = body.createDiv({ cls: "oc-connection-popover-line" });
        const anchor = line.createEl("a", { cls: "oc-copyable-link", text: String(label), href: String(url) });
        anchor.target = "_blank";
        anchor.rel = "noopener noreferrer";
      };
      const appendCopyableDetails = (text) => {
        const value = String(text || "").trim();
        if (!value) return;
        const panel = body.createDiv({ cls: "oc-copyable-panel oc-copyable-panel--compact" });
        const actions = panel.createDiv({ cls: "oc-copyable-actions" });
        const copyBtn = actions.createEl("button", { text: "\u590D\u5236\u8BE6\u60C5" });
        copyBtn.type = "button";
        copyBtn.addEventListener("click", async () => {
          const copied = await copyText(value);
          new Notice2(copied ? "\u8BE6\u60C5\u5DF2\u590D\u5236" : "\u590D\u5236\u5931\u8D25\uFF0C\u8BF7\u624B\u52A8\u9009\u62E9\u6587\u672C\u590D\u5236");
        });
        const area = panel.createEl("textarea", {
          cls: "oc-copyable-textarea",
          attr: {
            readonly: "true",
            spellcheck: "false",
            "aria-label": "\u8FDE\u63A5\u8BCA\u65AD\u8BE6\u60C5"
          }
        });
        area.value = value;
        area.rows = Math.min(12, Math.max(4, value.split(/\r?\n/).length));
        area.addEventListener("focus", () => area.select());
        area.addEventListener("click", () => area.select());
      };
      const appendCheckCommands = () => {
        connectionCheckCommands().forEach((cmd) => appendCommand(cmd));
      };
      const appendWindowsInstallGuide = () => {
        appendLine("\u8BF7\u5728 Windows \u672C\u673A\u7528 Node.js \u5B89\u88C5 OpenCode\uFF0C\u4E0D\u8981\u4F7F\u7528 WSL\uFF1A");
        windowsInstallGuideCommands().forEach((cmd) => appendCommand(cmd));
        appendLink("\u5B98\u65B9\u5B89\u88C5\u6587\u6863", OPENCODE_DOCS_URL);
      };
      if (!hasResult) {
        title.setText("\u6B63\u5728\u68C0\u6D4B OpenCode \u8FDE\u63A5\u72B6\u6001");
        appendLine("\u70B9\u51FB\u7EFF\u8272\u72B6\u6001\u70B9\u4F1A\u81EA\u52A8\u5237\u65B0\u8FDE\u63A5\u72B6\u6001\u3002");
        appendLine("\u5982\u679C\u957F\u65F6\u95F4\u65E0\u6CD5\u8FDE\u63A5\uFF0C\u53EF\u5148\u68C0\u67E5\u672C\u673A\u5B89\u88C5\uFF1A");
        appendCheckCommands();
        return;
      }
      if (normalized.connection.ok) {
        title.setText("OpenCode\u6210\u529F\u8FDE\u63A5");
        appendLine(`\u8FDE\u63A5\u6A21\u5F0F\uFF1A${normalized.connection.mode.toUpperCase()}`);
        if (normalized.executable.path) appendLine(`\u6267\u884C\u8DEF\u5F84\uFF1A${normalized.executable.path}`);
        return;
      }
      if (isLikelyMissingOpenCode(normalized)) {
        title.setText("OpenCode\u8FDE\u63A5\u5931\u8D25\uFF1A\u672A\u68C0\u6D4B\u5230\u53EF\u7528\u5B89\u88C5");
        if (typeof process !== "undefined" && process && process.platform === "win32") {
          appendWindowsInstallGuide();
          appendLine("\u5B89\u88C5\u540E\u91CD\u542F Obsidian\uFF0C\u518D\u70B9\u51FB\u72B6\u6001\u70B9\u5237\u65B0\u8FDE\u63A5\u3002");
        } else {
          appendLine("\u8BF7\u5148\u5728\u7EC8\u7AEF\u68C0\u67E5 OpenCode \u662F\u5426\u5B89\u88C5\u6B63\u5E38\uFF1A");
          appendCheckCommands();
        }
        appendCopyableDetails([
          normalized.executable.hint ? `\u63D0\u793A\uFF1A${normalized.executable.hint}` : "",
          normalized.connection.error ? `\u9519\u8BEF\uFF1A${normalized.connection.error}` : ""
        ].filter(Boolean).join("\n\n"));
        return;
      }
      if (isLikelyWindowsWslInstallIssue(normalized)) {
        title.setText("OpenCode\u8FDE\u63A5\u5931\u8D25\uFF1A\u68C0\u6D4B\u5230 WSL \u5B89\u88C5");
        appendLine("\u8BF7\u4F7F\u7528\u8FBE.js\u8FDB\u884C\u5B89\u88C5\u3002");
        appendWindowsInstallGuide();
        appendLine("\u5B89\u88C5\u540E\u91CD\u542F Obsidian\uFF0C\u518D\u70B9\u51FB\u72B6\u6001\u70B9\u5237\u65B0\u8FDE\u63A5\u3002");
        appendCopyableDetails(normalized.connection.error ? `\u9519\u8BEF\uFF1A${normalized.connection.error}` : "");
        return;
      }
      title.setText("OpenCode\u8FDE\u63A5\u5931\u8D25");
      appendLine("\u53EF\u5148\u6267\u884C\u4EE5\u4E0B\u547D\u4EE4\u68C0\u67E5\u8FDE\u63A5\uFF1A");
      appendCheckCommands();
      appendCopyableDetails(normalized.connection.error ? `\u9519\u8BEF\uFF1A${normalized.connection.error}` : "");
    }
    function closeConnectionStatusPopover(view) {
      const popover = view.elements && view.elements.statusPopover;
      if (!popover) return;
      popover.removeClass("is-open");
      if (view.elements && view.elements.statusDot) {
        view.elements.statusDot.setAttribute("aria-expanded", "false");
      }
    }
    function openConnectionStatusPopover(view) {
      const popover = view.elements && view.elements.statusPopover;
      if (!popover) return;
      popover.addClass("is-open");
      if (view.elements && view.elements.statusDot) {
        view.elements.statusDot.setAttribute("aria-expanded", "true");
      }
    }
    async function refreshConnectionStatusPopover(view, force = true) {
      const diagnosticsService = view && view.plugin ? view.plugin.diagnosticsService : null;
      if (!diagnosticsService || typeof diagnosticsService.runCached !== "function") {
        renderConnectionStatusPopoverContent(view, null);
        return;
      }
      const result = await diagnosticsService.runCached(0, Boolean(force));
      view.applyStatus(result);
      renderConnectionStatusPopoverContent(view, result);
    }
    function renderMain(main) {
      main.empty();
      if (typeof this.closeLinkedContextFilePicker === "function") {
        this.closeLinkedContextFilePicker();
      }
      const toolbar = main.createDiv({ cls: "oc-toolbar" });
      const toolbarLeft = toolbar.createDiv({ cls: "oc-toolbar-left" });
      const toolbarRight = toolbar.createDiv({ cls: "oc-toolbar-right" });
      const connectionIndicator = toolbarLeft.createDiv({ cls: "oc-connection-indicator" });
      this.elements.statusDot = connectionIndicator.createDiv({ cls: "oc-connection-dot warn" });
      this.elements.statusDot.setAttr("role", "button");
      this.elements.statusDot.setAttr("tabindex", "0");
      this.elements.statusDot.setAttr("aria-haspopup", "dialog");
      this.elements.statusDot.setAttr("aria-expanded", "false");
      this.elements.statusDot.setAttribute("aria-label", tr(this, "view.connection.unknown", "Connection status unknown"));
      this.elements.statusDot.setAttribute("title", tr(this, "view.connection.unknown", "Connection status unknown"));
      this.elements.statusPopover = connectionIndicator.createDiv({ cls: "oc-connection-popover" });
      renderConnectionStatusPopoverContent(this, this.latestDiagnosticsResult || null);
      const toggleConnectionPopover = () => {
        const popover = this.elements && this.elements.statusPopover;
        if (!popover) return;
        if (popover.hasClass("is-open")) {
          closeConnectionStatusPopover(this);
          return;
        }
        openConnectionStatusPopover(this);
        renderConnectionStatusPopoverContent(this, this.latestDiagnosticsResult || null);
        void refreshConnectionStatusPopover(this, true).catch((error) => {
          if (this.plugin && typeof this.plugin.log === "function") {
            this.plugin.log(`refresh connection popover failed: ${error instanceof Error ? error.message : String(error)}`);
          }
          renderConnectionStatusPopoverContent(this, this.latestDiagnosticsResult || null);
        });
      };
      const onDotActivate = (event) => {
        event.preventDefault();
        event.stopPropagation();
        toggleConnectionPopover();
      };
      this.elements.statusDot.addEventListener("click", onDotActivate);
      this.elements.statusDot.addEventListener("keydown", (event) => {
        if (event.key === "Enter" || event.key === " ") {
          onDotActivate(event);
        }
      });
      connectionIndicator.addEventListener("click", (event) => event.stopPropagation());
      if (!this.connectionPopoverDocumentBound) {
        this.connectionPopoverDocumentBound = true;
        this.registerDomEvent(document, "click", () => {
          closeConnectionStatusPopover(this);
        });
      }
      const settingsBtn = this.buildIconButton(toolbarRight, "settings", tr(this, "view.settings", "Settings"), () => this.openSettings());
      settingsBtn.addClass("oc-toolbar-btn");
      const messagesWrapper = main.createDiv({ cls: "oc-messages-wrapper" });
      this.elements.messages = messagesWrapper.createDiv({ cls: "oc-messages oc-messages-focusable", attr: { tabindex: "0" } });
      this.bindMessagesScrollTracking();
      this.elements.inlineQuestionHost = messagesWrapper.createDiv({ cls: "oc-inline-question-host" });
      this.renderMessages();
      const navSidebar = messagesWrapper.createDiv({ cls: "oc-nav-sidebar visible" });
      const topBtn = navSidebar.createEl("button", { cls: "oc-nav-btn oc-nav-btn-top" });
      topBtn.setAttr("type", "button");
      topBtn.setAttr("aria-label", tr(this, "view.scroll.top", "Scroll to top"));
      topBtn.setAttr("title", tr(this, "view.scroll.topShort", "Top"));
      try {
        setIcon(topBtn, "chevron-up");
      } catch {
        topBtn.setText("\u2191");
      }
      topBtn.addEventListener("click", () => this.scrollMessagesTo("top"));
      const bottomBtn = navSidebar.createEl("button", { cls: "oc-nav-btn oc-nav-btn-bottom" });
      bottomBtn.setAttr("type", "button");
      bottomBtn.setAttr("aria-label", tr(this, "view.scroll.bottom", "Scroll to bottom"));
      bottomBtn.setAttr("title", tr(this, "view.scroll.bottomShort", "Bottom"));
      try {
        setIcon(bottomBtn, "chevron-down");
      } catch {
        bottomBtn.setText("\u2193");
      }
      bottomBtn.addEventListener("click", () => this.scrollMessagesTo("bottom"));
      const contextFooter = main.createDiv({ cls: "oc-context-footer" });
      this.elements.currentSessionLabel = contextFooter.createDiv({
        cls: "oc-context-session",
        text: tr(this, "view.session.current", "Current session: {title}", { title: this.activeSessionLabel() })
      });
      const composer = main.createDiv({ cls: "oc-composer" });
      this.elements.composer = composer;
      const inputContainer = composer.createDiv({ cls: "oc-input-container" });
      const inputWrapper = inputContainer.createDiv({ cls: "oc-input-wrapper" });
      this.elements.inputContainer = inputContainer;
      this.elements.inputWrapper = inputWrapper;
      const contextRow = inputWrapper.createDiv({ cls: "oc-context-row" });
      const fileIndicator = contextRow.createDiv({ cls: "oc-file-indicator" });
      const selectionIndicator = contextRow.createDiv({
        cls: "oc-selection-indicator",
        text: ""
      });
      contextRow.toggleClass("has-content", false);
      fileIndicator.empty();
      selectionIndicator.empty();
      this.elements.contextRow = contextRow;
      this.elements.fileIndicator = fileIndicator;
      this.elements.selectionIndicator = selectionIndicator;
      this.elements.input = inputWrapper.createEl("textarea", {
        cls: "oc-input",
        attr: { placeholder: tr(this, "view.input.placeholder", "Type your message...") }
      });
      this.elements.input.addEventListener("keydown", (ev) => {
        if (typeof this.handleLinkedContextInputKeydown === "function" && this.handleLinkedContextInputKeydown(ev)) {
          return;
        }
        if ((ev.metaKey || ev.ctrlKey) && ev.key === "Enter") {
          ev.preventDefault();
          this.handleSend();
        }
      });
      this.elements.input.addEventListener("input", () => {
        if (typeof this.syncLinkedContextPickerFromInputMention === "function") {
          this.syncLinkedContextPickerFromInputMention();
        }
      });
      this.elements.input.addEventListener("click", () => {
        if (typeof this.syncLinkedContextPickerFromInputMention === "function") {
          this.syncLinkedContextPickerFromInputMention();
        }
      });
      const bindLinkedContextDropEvents = (targetEl) => {
        if (!targetEl || typeof targetEl.addEventListener !== "function") return;
        targetEl.addEventListener("dragenter", (event) => {
          if (typeof this.handleLinkedContextInputDragOver === "function") {
            this.handleLinkedContextInputDragOver(event);
          }
        });
        targetEl.addEventListener("dragover", (event) => {
          if (typeof this.handleLinkedContextInputDragOver === "function") {
            this.handleLinkedContextInputDragOver(event);
          }
        });
        targetEl.addEventListener("dragleave", (event) => {
          if (typeof this.handleLinkedContextInputDragLeave === "function") {
            this.handleLinkedContextInputDragLeave(event);
          }
        });
        targetEl.addEventListener("drop", (event) => {
          if (typeof this.handleLinkedContextInputDrop === "function") {
            this.handleLinkedContextInputDrop(event);
          }
        });
      };
      bindLinkedContextDropEvents(inputContainer);
      bindLinkedContextDropEvents(this.elements.input);
      const inputToolbar = inputWrapper.createDiv({ cls: "oc-input-toolbar" });
      const inputToolbarLeft = inputToolbar.createDiv({ cls: "oc-input-toolbar-left" });
      const inputToolbarRight = inputToolbar.createDiv({ cls: "oc-actions oc-actions-right" });
      this.elements.attachFileBtn = inputToolbarLeft.createEl("button", { cls: "mod-muted oc-context-link-btn" });
      this.elements.attachFileBtn.setAttr("type", "button");
      this.elements.attachFileBtn.setAttr("aria-label", tr(this, "view.context.attach", "Link Obsidian file context (@)"));
      this.elements.attachFileBtn.setAttr("title", tr(this, "view.context.attach", "Link Obsidian file context (@)"));
      try {
        setIcon(this.elements.attachFileBtn, "plus");
      } catch {
        this.elements.attachFileBtn.setText("+");
      }
      this.elements.attachFileBtn.addEventListener("click", () => this.openLinkedContextFilePicker());
      const modelSelectWrap = inputToolbarLeft.createDiv({ cls: "oc-model-select-inline-wrap" });
      const modelSelectText = modelSelectWrap.createSpan({
        cls: "oc-model-select-inline-text",
        text: tr(this, "view.model.placeholder", "Model")
      });
      modelSelectWrap.createSpan({ cls: "oc-model-select-inline-caret", text: "\u25BE" });
      const modelSelect = modelSelectWrap.createEl("select", {
        cls: "oc-model-select-inline",
        attr: { "aria-label": tr(this, "view.model.selectTitle", "Select model") }
      });
      this.elements.modelSelect = modelSelect;
      this.elements.modelSelectText = modelSelectText;
      this.updateModelSelectOptions();
      modelSelect.addEventListener("change", async () => {
        try {
          await this.applyModelSelection(modelSelect.value);
        } catch (e) {
          const msg = e instanceof Error ? e.message : String(e);
          modelSelect.value = this.selectedModel || "";
          new Notice2(tr(this, "view.model.switchFailed", "Model switch failed: {message}", { message: msg }));
        }
      });
      this.elements.cancelBtn = inputToolbarRight.createEl("button", { cls: "mod-muted oc-cancel-btn", text: tr(this, "view.action.cancel", "Cancel") });
      this.elements.sendBtn = inputToolbarRight.createEl("button", { cls: "mod-cta oc-send-btn" });
      this.elements.sendBtn.setAttr("type", "button");
      this.elements.sendBtn.setAttr("aria-label", tr(this, "view.action.send", "Send"));
      this.elements.sendBtn.setAttr("title", tr(this, "view.action.sendShortcut", "Send (Ctrl/Cmd + Enter)"));
      try {
        setIcon(this.elements.sendBtn, "arrow-up");
      } catch {
        this.elements.sendBtn.setText("\u2191");
      }
      this.elements.cancelBtn.setAttr("type", "button");
      this.elements.cancelBtn.setAttr("aria-label", tr(this, "view.action.cancel", "Cancel"));
      this.elements.cancelBtn.disabled = true;
      this.elements.sendBtn.addEventListener("click", () => this.handleSend());
      this.elements.cancelBtn.addEventListener("click", () => this.cancelSending());
      this.refreshLinkedContextIndicators();
      this.renderInlineQuestionPanel(this.plugin.sessionStore.getActiveMessages());
      const diagnosticsService = this.plugin && this.plugin.diagnosticsService;
      if (diagnosticsService) {
        const cached = diagnosticsService.getLastResult();
        if (cached) {
          this.applyStatus(cached);
          renderConnectionStatusPopoverContent(this, cached);
        }
        if (typeof diagnosticsService.runCached === "function") {
          void diagnosticsService.runCached(1e4, false).then((result) => {
            if (result) {
              this.applyStatus(result);
              renderConnectionStatusPopoverContent(this, result);
            }
          }).catch((error) => {
            if (this.plugin && typeof this.plugin.log === "function") {
              this.plugin.log(`view diagnostics refresh failed: ${error instanceof Error ? error.message : String(error)}`);
            }
          });
        }
      }
    }
    function applyStatus(result) {
      const dot = this.elements.statusDot;
      if (!dot) return;
      this.latestDiagnosticsResult = result && typeof result === "object" ? result : null;
      dot.removeClass("ok", "error", "warn");
      if (!result || !result.connection) {
        dot.addClass("warn");
        dot.setAttribute("aria-label", tr(this, "view.connection.unknown", "Connection status unknown"));
        dot.setAttribute("title", tr(this, "view.connection.unknown", "Connection status unknown"));
        return;
      }
      if (result.connection.ok) {
        dot.addClass("ok");
        const label2 = tr(this, "view.connection.ok", "Connected ({mode})", result.connection);
        dot.setAttribute("aria-label", label2);
        dot.setAttribute("title", label2);
        return;
      }
      dot.addClass("error");
      const label = tr(this, "view.connection.error", "Connection error ({mode})", result.connection);
      dot.setAttribute("aria-label", label);
      dot.setAttribute("title", label);
    }
    var mainComposerMethods = {
      renderMain,
      applyStatus
    };
    module2.exports = { mainComposerMethods };
  }
});

// runtime/view/layout-renderer.js
var require_layout_renderer = __commonJS({
  "runtime/view/layout-renderer.js"(exports2, module2) {
    var { sharedLayoutMethods } = require_shared_utils();
    var { headerMethods } = require_header_methods();
    var { sidebarMethods } = require_sidebar_methods();
    var { linkedContextMethods } = require_linked_context_methods();
    var { mainComposerMethods } = require_main_composer_methods();
    var layoutRendererMethods = {
      ...sharedLayoutMethods,
      ...sidebarMethods,
      ...linkedContextMethods,
      ...headerMethods,
      ...mainComposerMethods
    };
    module2.exports = { layoutRendererMethods };
  }
});

// runtime/view/message/scroll-methods.js
var require_scroll_methods = __commonJS({
  "runtime/view/message/scroll-methods.js"(exports2, module2) {
    var AUTO_SCROLL_REENABLE_THRESHOLD = 24;
    var AUTO_SCROLL_STICKY_THRESHOLD = 120;
    var PROGRAMMATIC_SCROLL_SUPPRESS_MS = 140;
    var FORCE_BOTTOM_MIN_MS = 0;
    var FORCE_BOTTOM_DEFAULT_MS = 0;
    var MANUAL_INTENT_WINDOW_MS = 1200;
    function isMessagesNearBottom(threshold = AUTO_SCROLL_REENABLE_THRESHOLD) {
      const container = this.elements && this.elements.messages;
      if (!container) return true;
      const distance = container.scrollHeight - container.scrollTop - container.clientHeight;
      return distance <= Math.max(0, Number(threshold) || 0);
    }
    function normalizeForceBottomMs(durationMs) {
      const raw = Number(durationMs);
      if (!Number.isFinite(raw) || raw <= FORCE_BOTTOM_MIN_MS) return FORCE_BOTTOM_DEFAULT_MS;
      return Math.max(FORCE_BOTTOM_MIN_MS, Math.floor(raw));
    }
    function setForceBottomWindow(durationMs = FORCE_BOTTOM_DEFAULT_MS) {
      const ms = normalizeForceBottomMs(durationMs);
      this.forceBottomUntil = ms > 0 ? Date.now() + ms : 0;
    }
    function hasActiveForceBottom() {
      const until = Number(this.forceBottomUntil || 0);
      if (until <= Date.now()) {
        this.forceBottomUntil = 0;
        return false;
      }
      return true;
    }
    function markManualScrollIntent() {
      this.lastManualScrollIntentAt = Date.now();
    }
    function hasRecentManualScrollIntent(windowMs = MANUAL_INTENT_WINDOW_MS) {
      const last = Number(this.lastManualScrollIntentAt || 0);
      if (!last) return false;
      return Date.now() - last <= Math.max(0, Number(windowMs) || 0);
    }
    function suppressProgrammaticScrollEvents() {
      this.ignoreMessageScrollEventsUntil = Date.now() + PROGRAMMATIC_SCROLL_SUPPRESS_MS;
    }
    function withProgrammaticScroll(container, callback) {
      if (!container || typeof callback !== "function") return;
      this.suppressProgrammaticScrollEvents();
      callback();
      requestAnimationFrame(() => {
        this.suppressProgrammaticScrollEvents();
        this.autoScrollEnabled = this.isMessagesNearBottom(AUTO_SCROLL_STICKY_THRESHOLD);
      });
    }
    function shouldAutoScrollMessages() {
      if (this.hasActiveForceBottom()) return true;
      if (typeof this.autoScrollEnabled !== "boolean") return true;
      return this.autoScrollEnabled;
    }
    function bindMessagesScrollTracking() {
      const container = this.elements && this.elements.messages;
      if (!container) return;
      if (this.messagesScrollEl === container && typeof this.messagesScrollHandler === "function") return;
      this.unbindMessagesScrollTracking();
      const onManualIntent = () => {
        this.markManualScrollIntent();
      };
      const onKeyDown = (event) => {
        const key = String(event && event.key || "");
        if (!key) return;
        if (["ArrowUp", "ArrowDown", "PageUp", "PageDown", "Home", "End", " "].includes(key)) {
          this.markManualScrollIntent();
        }
      };
      const onScroll = () => {
        const ignoreUntil = Number(this.ignoreMessageScrollEventsUntil || 0);
        if (ignoreUntil > Date.now()) return;
        const nearBottom = this.isMessagesNearBottom(AUTO_SCROLL_REENABLE_THRESHOLD);
        if (nearBottom) {
          this.autoScrollEnabled = true;
          return;
        }
        if (this.hasRecentManualScrollIntent()) {
          this.setForceBottomWindow(0);
          this.autoScrollEnabled = false;
          return;
        }
        if (!this.autoScrollEnabled) return;
        this.autoScrollEnabled = true;
      };
      container.addEventListener("wheel", onManualIntent, { passive: true });
      container.addEventListener("touchstart", onManualIntent, { passive: true });
      container.addEventListener("pointerdown", onManualIntent, { passive: true });
      container.addEventListener("keydown", onKeyDown);
      container.addEventListener("scroll", onScroll, { passive: true });
      this.messagesScrollEl = container;
      this.messagesScrollHandler = onScroll;
      this.messagesIntentHandler = onManualIntent;
      this.messagesKeyDownHandler = onKeyDown;
      this.autoScrollEnabled = this.isMessagesNearBottom(AUTO_SCROLL_STICKY_THRESHOLD);
    }
    function unbindMessagesScrollTracking() {
      if (this.messagesScrollEl && typeof this.messagesIntentHandler === "function") {
        this.messagesScrollEl.removeEventListener("wheel", this.messagesIntentHandler);
        this.messagesScrollEl.removeEventListener("touchstart", this.messagesIntentHandler);
        this.messagesScrollEl.removeEventListener("pointerdown", this.messagesIntentHandler);
      }
      if (this.messagesScrollEl && typeof this.messagesKeyDownHandler === "function") {
        this.messagesScrollEl.removeEventListener("keydown", this.messagesKeyDownHandler);
      }
      if (this.messagesScrollEl && typeof this.messagesScrollHandler === "function") {
        this.messagesScrollEl.removeEventListener("scroll", this.messagesScrollHandler);
      }
      this.messagesScrollEl = null;
      this.messagesScrollHandler = null;
      this.messagesIntentHandler = null;
      this.messagesKeyDownHandler = null;
      if (this.pendingScrollRaf) {
        cancelAnimationFrame(this.pendingScrollRaf);
        this.pendingScrollRaf = 0;
      }
    }
    function scheduleScrollMessagesToBottom(force = false) {
      const container = this.elements && this.elements.messages;
      if (!container) return;
      const shouldForce = Boolean(force || this.hasActiveForceBottom());
      if (shouldForce) this.autoScrollEnabled = true;
      if (!shouldForce && !this.shouldAutoScrollMessages()) return;
      if (this.pendingScrollRaf) cancelAnimationFrame(this.pendingScrollRaf);
      this.pendingScrollRaf = requestAnimationFrame(() => {
        this.pendingScrollRaf = 0;
        const latestContainer = this.elements && this.elements.messages;
        if (!latestContainer) return;
        const shouldForceNow = Boolean(force || this.hasActiveForceBottom());
        if (shouldForceNow) this.autoScrollEnabled = true;
        if (!shouldForceNow && !this.shouldAutoScrollMessages()) return;
        this.withProgrammaticScroll(latestContainer, () => {
          latestContainer.scrollTop = latestContainer.scrollHeight;
        });
      });
    }
    var scrollMethods = {
      isMessagesNearBottom,
      setForceBottomWindow,
      hasActiveForceBottom,
      markManualScrollIntent,
      hasRecentManualScrollIntent,
      suppressProgrammaticScrollEvents,
      withProgrammaticScroll,
      shouldAutoScrollMessages,
      bindMessagesScrollTracking,
      unbindMessagesScrollTracking,
      scheduleScrollMessagesToBottom
    };
    module2.exports = { scrollMethods };
  }
});

// runtime/view/message/dom-utils.js
var require_dom_utils = __commonJS({
  "runtime/view/message/dom-utils.js"(exports2, module2) {
    var { setIcon } = require("obsidian");
    var TOOL_ICON_MAP = {
      read: "file-text",
      write: "file-pen",
      edit: "file-pen",
      bash: "terminal-square",
      ls: "folder-tree",
      glob: "search",
      grep: "search-code",
      web_search: "globe",
      web_fetch: "globe",
      skill: "sparkles",
      question: "circle-help",
      todo_write: "list-checks"
    };
    function setNodeText(node, text) {
      if (!node) return;
      const value = String(text || "");
      if (typeof node.setText === "function") {
        node.setText(value);
      } else {
        node.textContent = value;
      }
    }
    function emptyNode(node) {
      if (!node) return;
      if (typeof node.empty === "function") {
        node.empty();
      } else {
        node.innerHTML = "";
      }
    }
    function safeSetIcon(el, iconName) {
      if (!el) return;
      try {
        setIcon(el, iconName);
      } catch {
        try {
          setIcon(el, "circle");
        } catch {
        }
      }
    }
    async function copyTextToClipboard(text) {
      await navigator.clipboard.writeText(String(text || ""));
    }
    function applyCopyGlyph(el) {
      if (!el) return;
      el.innerHTML = "";
      const svg = document.createElementNS("http://www.w3.org/2000/svg", "svg");
      svg.setAttribute("viewBox", "0 0 24 24");
      svg.setAttribute("aria-hidden", "true");
      svg.setAttribute("focusable", "false");
      const rectBack = document.createElementNS("http://www.w3.org/2000/svg", "rect");
      rectBack.setAttribute("x", "9");
      rectBack.setAttribute("y", "9");
      rectBack.setAttribute("width", "11");
      rectBack.setAttribute("height", "11");
      rectBack.setAttribute("rx", "2");
      rectBack.setAttribute("fill", "none");
      rectBack.setAttribute("stroke", "currentColor");
      rectBack.setAttribute("stroke-width", "1.8");
      const rectFront = document.createElementNS("http://www.w3.org/2000/svg", "path");
      rectFront.setAttribute("d", "M7 15H6a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2h7a2 2 0 0 1 2 2v1");
      rectFront.setAttribute("fill", "none");
      rectFront.setAttribute("stroke", "currentColor");
      rectFront.setAttribute("stroke-width", "1.8");
      rectFront.setAttribute("stroke-linecap", "round");
      rectFront.setAttribute("stroke-linejoin", "round");
      svg.appendChild(rectBack);
      svg.appendChild(rectFront);
      el.appendChild(svg);
    }
    function showCopyFeedback(el, restore) {
      if (!el) return;
      const prev = el.getAttribute("data-copying");
      if (prev === "1") return;
      el.setAttribute("data-copying", "1");
      el.classList.add("copied");
      el.textContent = "copied!";
      setTimeout(() => {
        el.removeAttribute("data-copying");
        el.classList.remove("copied");
        if (typeof restore === "function") restore();
      }, 1400);
    }
    function resolveToolIconName(toolName) {
      const key = String(toolName || "").trim().toLowerCase();
      return TOOL_ICON_MAP[key] || "wrench";
    }
    function applyToolStatusIcon(el, status) {
      if (!el) return;
      const normalizedStatus = String(status || "pending").trim().toLowerCase();
      el.className = "oc-tool-status";
      el.classList.add(`status-${normalizedStatus}`);
      el.empty();
      if (normalizedStatus === "completed") {
        safeSetIcon(el, "check");
        return;
      }
      if (normalizedStatus === "error") {
        safeSetIcon(el, "x");
        return;
      }
      if (normalizedStatus === "running") {
        safeSetIcon(el, "loader-circle");
        return;
      }
      safeSetIcon(el, "clock3");
    }
    var domUtils = {
      setNodeText,
      emptyNode,
      safeSetIcon,
      copyTextToClipboard,
      applyCopyGlyph,
      showCopyFeedback,
      resolveToolIconName,
      applyToolStatusIcon
    };
    module2.exports = { domUtils };
  }
});

// runtime/view/message/markdown-methods.js
var require_markdown_methods = __commonJS({
  "runtime/view/message/markdown-methods.js"(exports2, module2) {
    var { Notice: Notice2, MarkdownRenderer, Keymap } = require("obsidian");
    var { normalizeMarkdownForDisplay } = require_assistant_payload_utils();
    var { domUtils } = require_dom_utils();
    var { tFromContext } = require_i18n_runtime();
    var {
      setNodeText,
      emptyNode,
      copyTextToClipboard,
      applyCopyGlyph,
      showCopyFeedback
    } = domUtils;
    var MARKDOWN_RENDER_STATE = /* @__PURE__ */ new WeakMap();
    function markdownRenderKey(text) {
      const raw = String(text || "");
      return `${raw.length}:${raw.slice(0, 120)}`;
    }
    function ensureCodeWrapper(pre) {
      if (!pre || !pre.parentElement) return null;
      if (pre.parentElement.classList.contains("oc-code-wrapper")) {
        return pre.parentElement;
      }
      const wrapper = document.createElement("div");
      wrapper.className = "oc-code-wrapper";
      pre.parentElement.insertBefore(wrapper, pre);
      wrapper.appendChild(pre);
      return wrapper;
    }
    function ensureCodeCopyButton(wrapper, codeText, context) {
      if (!wrapper) return;
      let btn = wrapper.querySelector(".oc-code-copy-btn");
      if (!btn) {
        btn = document.createElement("button");
        btn.className = "oc-code-copy-btn";
        btn.type = "button";
        btn.setAttribute("aria-label", tFromContext(context, "view.message.copyCode", "Copy code"));
        wrapper.appendChild(btn);
      }
      applyCopyGlyph(btn);
      btn.onclick = async (event) => {
        event.preventDefault();
        event.stopPropagation();
        try {
          await copyTextToClipboard(codeText || "");
          showCopyFeedback(btn, () => {
            applyCopyGlyph(btn);
          });
        } catch {
          new Notice2(tFromContext(context, "view.message.copyFailed", "Copy failed"));
        }
      };
    }
    function enhanceCodeBlocks(container) {
      if (!container) return;
      container.querySelectorAll("pre").forEach((pre) => {
        const wrapper = ensureCodeWrapper(pre);
        if (!wrapper) return;
        const codeEl = pre.querySelector("code");
        const codeText = codeEl ? codeEl.innerText : pre.innerText;
        ensureCodeCopyButton(wrapper, codeText, this);
        const obsidianCopy = wrapper.querySelector(".copy-code-button");
        if (obsidianCopy) obsidianCopy.remove();
      });
    }
    function attachCodeCopyButtons(container) {
      this.enhanceCodeBlocks(container);
    }
    function getMarkdownRenderSourcePath() {
      const file = this.app && this.app.workspace && typeof this.app.workspace.getActiveFile === "function" ? this.app.workspace.getActiveFile() : null;
      return file && typeof file.path === "string" ? file.path : "";
    }
    function decodeLinkText(value) {
      const text = String(value || "");
      if (!text) return "";
      try {
        return decodeURIComponent(text);
      } catch {
        return text;
      }
    }
    function resolveObsidianUriTarget(href) {
      const raw = String(href || "").trim();
      if (!raw) return "";
      let url;
      try {
        url = new URL(raw);
      } catch {
        return "";
      }
      if (String(url.protocol || "").toLowerCase() !== "obsidian:") return "";
      const path = decodeLinkText(url.searchParams.get("path") || "");
      if (path) return path;
      const file = decodeLinkText(url.searchParams.get("file") || "");
      return file;
    }
    function resolveInternalLinkText(anchorEl) {
      if (!anchorEl) return "";
      const dataHref = String(anchorEl.getAttribute("data-href") || "").trim();
      if (dataHref) return decodeLinkText(dataHref);
      const href = String(anchorEl.getAttribute("href") || "").trim();
      if (!href) return "";
      if (href.startsWith("#")) return "";
      const obsidianTarget = resolveObsidianUriTarget(href);
      if (obsidianTarget) return obsidianTarget;
      if (/^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(href)) return "";
      return decodeLinkText(href.replace(/^\.\//, ""));
    }
    function attachInternalLinkHandlers(container) {
      if (!container || !this.app || !this.app.workspace) return;
      const sourcePath = this.getMarkdownRenderSourcePath();
      container.querySelectorAll("a").forEach((anchorEl) => {
        if (!anchorEl || anchorEl.dataset.ocLinkBound === "1") return;
        anchorEl.dataset.ocLinkBound = "1";
        const linktext = this.resolveInternalLinkText(anchorEl);
        if (!linktext) return;
        anchorEl.addEventListener("click", (evt) => {
          if (!evt || evt.button !== 0 && evt.button !== 1) return;
          evt.preventDefault();
          evt.stopPropagation();
          const modEvent = typeof Keymap !== "undefined" && Keymap && typeof Keymap.isModEvent === "function" ? Keymap.isModEvent(evt) : Boolean(evt.metaKey || evt.ctrlKey);
          const openInNewLeaf = Boolean(modEvent || evt.button === 1);
          void this.app.workspace.openLinkText(linktext, sourcePath, openInNewLeaf);
        });
        anchorEl.addEventListener("mouseover", (evt) => {
          this.app.workspace.trigger("hover-link", {
            event: evt,
            source: "flownote",
            hoverParent: this,
            targetEl: anchorEl,
            linktext
          });
        });
      });
    }
    function renderMarkdownSafely(container, markdownText, onRendered) {
      if (!container) return;
      const text = String(markdownText || "");
      if (!text.trim()) {
        MARKDOWN_RENDER_STATE.delete(container);
        emptyNode(container);
        return;
      }
      const key = markdownRenderKey(text);
      const previous = MARKDOWN_RENDER_STATE.get(container);
      if (previous && previous.rendered && previous.key === key) return;
      const version = Number(previous && previous.version ? previous.version : 0) + 1;
      MARKDOWN_RENDER_STATE.set(container, {
        key,
        version,
        rendered: false
      });
      const staging = document.createElement("div");
      const sourcePath = this.getMarkdownRenderSourcePath();
      MarkdownRenderer.render(this.app, text, staging, sourcePath, this.plugin).then(() => {
        const latest = MARKDOWN_RENDER_STATE.get(container);
        if (!latest || latest.version !== version) return;
        emptyNode(container);
        while (staging.firstChild) {
          container.appendChild(staging.firstChild);
        }
        MARKDOWN_RENDER_STATE.set(container, {
          key,
          version,
          rendered: true
        });
        this.attachInternalLinkHandlers(container);
        if (typeof onRendered === "function") onRendered();
        const shouldForceBottom = Boolean(
          this.currentAbort || typeof this.hasActiveForceBottom === "function" && this.hasActiveForceBottom()
        );
        this.scheduleScrollMessagesToBottom(shouldForceBottom);
      }).catch(() => {
        const latest = MARKDOWN_RENDER_STATE.get(container);
        if (!latest || latest.version !== version) return;
        emptyNode(container);
        setNodeText(container, text);
        MARKDOWN_RENDER_STATE.set(container, {
          key,
          version,
          rendered: true
        });
        const shouldForceBottom = Boolean(
          this.currentAbort || typeof this.hasActiveForceBottom === "function" && this.hasActiveForceBottom()
        );
        this.scheduleScrollMessagesToBottom(shouldForceBottom);
      });
    }
    var markdownMethods = {
      attachCodeCopyButtons,
      attachInternalLinkHandlers,
      getMarkdownRenderSourcePath,
      resolveInternalLinkText,
      renderMarkdownSafely,
      enhanceCodeBlocks
    };
    module2.exports = {
      MARKDOWN_RENDER_STATE,
      markdownMethods
    };
  }
});

// runtime/view/message/block-utils.js
var require_block_utils = __commonJS({
  "runtime/view/message/block-utils.js"(exports2, module2) {
    var { tFromContext } = require_i18n_runtime();
    const INTERNAL_NOISY_TOOL_NAMES = new Set([
      "background_output",
      "background_cancel",
    ]);
    const PATCH_DEFAULT_CONTEXT_LINES = 3;
    const PATCH_DEFAULT_MAX_MATRIX_CELLS = 120000;
    const PATCH_DEFAULT_MAX_RENDERED_LINES = 320;

    function ensureReasoningContainer(row, openByDefault) {
      let details = row.querySelector(".oc-message-reasoning");
      if (!details) {
        details = document.createElement("details");
        details.addClass("oc-message-reasoning");
        details.createEl("summary", { text: tFromContext(this, "view.block.reasoning", "Reasoning (collapsible)") });
        details.createDiv({ cls: "oc-message-reasoning-body" });
        const body = row.querySelector(".oc-message-content");
        if (body && body.parentElement === row) {
          row.insertBefore(details, body);
        } else {
          row.appendChild(details);
        }
      }
      if (openByDefault) details.open = true;
      return details.querySelector(".oc-message-reasoning-body");
    }

    function ensureBlocksContainer(row) {
      let container = row.querySelector(".oc-part-list");
      if (!container) {
        container = row.createDiv({ cls: "oc-part-list" });
      }
      return container;
    }

    function reorderAssistantMessageLayout(row) {
      if (!row) return;
      const body = row.querySelector(".oc-message-content");
      if (!body || body.parentElement !== row) return;

      const reasoning = row.querySelector(".oc-message-reasoning");
      if (reasoning && reasoning.parentElement === row) {
        row.insertBefore(reasoning, body);
      }

      const parts = row.querySelector(".oc-part-list");
      if (parts && parts.parentElement === row) {
        row.insertBefore(parts, body);
      }

      const meta = row.querySelector(".oc-message-meta");
      if (meta && meta.parentElement === row) {
        row.insertBefore(meta, body);
      }

      row.appendChild(body);
    }

    function normalizeBlockStatus(status) {
      const value = String(status || "").trim().toLowerCase();
      if (["completed", "running", "pending", "error"].includes(value)) return value;
      return "pending";
    }

    function resolveDisplayBlockStatus(block, messagePending) {
      const status = this.normalizeBlockStatus(block && block.status);
      if (status === "error" || status === "completed") return status;
      if (messagePending) return status;
      return "completed";
    }

    function blockTypeLabel(type) {
      const value = String(type || "").trim();
      const map = {
        tool: tFromContext(this, "view.block.tool", "Tool"),
        subtask: tFromContext(this, "view.block.subtask", "Subtask"),
        agent: tFromContext(this, "view.block.agent", "Agent"),
        file: tFromContext(this, "view.block.file", "File"),
        patch: tFromContext(this, "view.block.patch", "Patch"),
        retry: tFromContext(this, "view.block.retry", "Retry"),
        compaction: tFromContext(this, "view.block.compaction", "Compaction"),
        snapshot: tFromContext(this, "view.block.snapshot", "Snapshot"),
      };
      return map[value] || value || tFromContext(this, "view.block.output", "Output");
    }

    function blockStatusLabel(status) {
      const value = this.normalizeBlockStatus(status);
      if (value === "completed") return tFromContext(this, "view.block.status.completed", "Completed");
      if (value === "running") return tFromContext(this, "view.block.status.running", "Running");
      if (value === "error") return tFromContext(this, "view.block.status.error", "Failed");
      return tFromContext(this, "view.block.status.pending", "Pending");
    }

    function truncateSummaryText(text, maxLength = 88) {
      const raw = String(text || "").trim();
      if (!raw) return "";
      return raw.length > maxLength ? `${raw.slice(0, maxLength)}...` : raw;
    }

    function normalizePatchChangeType(value) {
      const raw = String(value || "").trim().toLowerCase();
      if (!raw) return "unknown";
      if (["a", "add", "added", "new", "create", "created"].includes(raw)) return "added";
      if (["m", "mod", "modify", "modified", "update", "updated", "change", "changed", "edit", "edited"].includes(raw)) {
        return "modified";
      }
      if (["d", "del", "delete", "deleted", "remove", "removed"].includes(raw)) return "deleted";
      if (["r", "ren", "rename", "renamed", "move", "moved"].includes(raw)) return "renamed";
      if (["c", "copy", "copied"].includes(raw)) return "copied";
      return "unknown";
    }

    function patchChangeLabel(changeType) {
      const type = normalizePatchChangeType(changeType);
      if (type === "added") return tFromContext(this, "view.block.patchAction.added", "Added");
      if (type === "modified") return tFromContext(this, "view.block.patchAction.modified", "Modified");
      if (type === "deleted") return tFromContext(this, "view.block.patchAction.deleted", "Deleted");
      if (type === "renamed") return tFromContext(this, "view.block.patchAction.renamed", "Renamed");
      if (type === "copied") return tFromContext(this, "view.block.patchAction.copied", "Copied");
      return tFromContext(this, "view.block.patchLabel", "File change");
    }

    function normalizePatchPath(pathLike) {
      return String(pathLike || "").trim();
    }

    function splitPatchTextLines(value) {
      const text = String(value || "").replace(/\r/g, "");
      if (!text) return [];
      return text.split("\n");
    }

    function countPatchLineChanges(lines) {
      let added = 0;
      let removed = 0;
      for (const line of Array.isArray(lines) ? lines : []) {
        const type = line && typeof line === "object" ? String(line.type || "") : "";
        if (type === "insert") added += 1;
        if (type === "delete") removed += 1;
      }
      return { added, removed };
    }

    function clampPatchDiffLines(lines, maxRenderedLines) {
      const source = Array.isArray(lines) ? lines : [];
      const limitRaw = Number(maxRenderedLines);
      const limit = Number.isFinite(limitRaw) && limitRaw > 0
        ? Math.max(24, Math.floor(limitRaw))
        : PATCH_DEFAULT_MAX_RENDERED_LINES;
      if (source.length <= limit) {
        return {
          lines: source,
          truncated: false,
          hiddenLineCount: 0,
        };
      }

      const head = Math.max(8, Math.floor(limit * 0.58));
      const tail = Math.max(8, limit - head - 1);
      const keptHead = source.slice(0, head);
      const keptTail = source.slice(source.length - tail);
      const hiddenLineCount = Math.max(0, source.length - keptHead.length - keptTail.length);
      const markerLine = {
        type: "omitted",
        text: `... ${hiddenLineCount} lines omitted ...`,
      };
      return {
        lines: [...keptHead, markerLine, ...keptTail],
        truncated: true,
        hiddenLineCount,
      };
    }

    function buildFallbackPatchLines(beforeLines, afterLines) {
      const lines = [];
      for (const text of beforeLines) {
        lines.push({ type: "delete", text: String(text || "") });
      }
      for (const text of afterLines) {
        lines.push({ type: "insert", text: String(text || "") });
      }
      return lines;
    }

    function buildPatchLineDiff(beforeText, afterText, options = {}) {
      const beforeLines = splitPatchTextLines(beforeText);
      const afterLines = splitPatchTextLines(afterText);
      const rows = beforeLines.length;
      const cols = afterLines.length;
      const maxMatrixCellsRaw = Number(options.maxMatrixCells);
      const maxMatrixCells = Number.isFinite(maxMatrixCellsRaw) && maxMatrixCellsRaw > 0
        ? Math.max(1024, Math.floor(maxMatrixCellsRaw))
        : PATCH_DEFAULT_MAX_MATRIX_CELLS;
      const maxRenderedLinesRaw = Number(options.maxRenderedLines);
      const maxRenderedLines = Number.isFinite(maxRenderedLinesRaw) && maxRenderedLinesRaw > 0
        ? Math.max(24, Math.floor(maxRenderedLinesRaw))
        : PATCH_DEFAULT_MAX_RENDERED_LINES;

      if (!rows && !cols) {
        return {
          lines: [],
          truncated: false,
          hiddenLineCount: 0,
          matrixLimited: false,
        };
      }

      const matrixCells = rows * cols;
      if (matrixCells > maxMatrixCells) {
        const fallbackLines = buildFallbackPatchLines(beforeLines, afterLines);
        const limitedFallback = clampPatchDiffLines(fallbackLines, maxRenderedLines);
        return {
          lines: limitedFallback.lines,
          truncated: limitedFallback.truncated,
          hiddenLineCount: limitedFallback.hiddenLineCount,
          matrixLimited: true,
        };
      }

      const width = cols + 1;
      const matrix = new Uint32Array((rows + 1) * width);

      for (let row = rows - 1; row >= 0; row -= 1) {
        for (let col = cols - 1; col >= 0; col -= 1) {
          const idx = row * width + col;
          if (beforeLines[row] === afterLines[col]) {
            matrix[idx] = matrix[(row + 1) * width + (col + 1)] + 1;
          } else {
            const down = matrix[(row + 1) * width + col];
            const right = matrix[row * width + (col + 1)];
            matrix[idx] = down >= right ? down : right;
          }
        }
      }

      const lines = [];
      let row = 0;
      let col = 0;
      while (row < rows && col < cols) {
        const beforeLine = beforeLines[row];
        const afterLine = afterLines[col];
        if (beforeLine === afterLine) {
          lines.push({ type: "equal", text: beforeLine });
          row += 1;
          col += 1;
          continue;
        }
        const down = matrix[(row + 1) * width + col];
        const right = matrix[row * width + (col + 1)];
        if (down >= right) {
          lines.push({ type: "delete", text: beforeLine });
          row += 1;
        } else {
          lines.push({ type: "insert", text: afterLine });
          col += 1;
        }
      }
      while (row < rows) {
        lines.push({ type: "delete", text: beforeLines[row] });
        row += 1;
      }
      while (col < cols) {
        lines.push({ type: "insert", text: afterLines[col] });
        col += 1;
      }

      const limited = clampPatchDiffLines(lines, maxRenderedLines);
      return {
        lines: limited.lines,
        truncated: limited.truncated,
        hiddenLineCount: limited.hiddenLineCount,
        matrixLimited: false,
      };
    }

    function splitPatchDiffHunks(diffLines, options = {}) {
      const lines = Array.isArray(diffLines) ? diffLines : [];
      if (!lines.length) return [];

      const contextLinesRaw = Number(options.contextLines);
      const contextLines = Number.isFinite(contextLinesRaw) && contextLinesRaw >= 0
        ? Math.min(24, Math.floor(contextLinesRaw))
        : PATCH_DEFAULT_CONTEXT_LINES;

      const changedIndices = [];
      for (let index = 0; index < lines.length; index += 1) {
        const line = lines[index];
        const type = line && typeof line === "object" ? String(line.type || "") : "";
        if (type !== "equal") {
          changedIndices.push(index);
        }
      }
      if (!changedIndices.length) return [];

      const ranges = [];
      for (const changedIndex of changedIndices) {
        const start = Math.max(0, changedIndex - contextLines);
        const end = Math.min(lines.length - 1, changedIndex + contextLines);
        const previous = ranges[ranges.length - 1];
        if (previous && start <= previous.end + 1) {
          previous.end = Math.max(previous.end, end);
        } else {
          ranges.push({ start, end });
        }
      }

      const hunks = [];
      for (const range of ranges) {
        let oldStart = 1;
        let newStart = 1;
        for (let i = 0; i < range.start; i += 1) {
          const type = String((lines[i] && lines[i].type) || "");
          if (type === "equal" || type === "delete") oldStart += 1;
          if (type === "equal" || type === "insert") newStart += 1;
        }
        hunks.push({
          lines: lines.slice(range.start, range.end + 1),
          oldStart,
          newStart,
        });
      }
      return hunks;
    }

    function normalizePatchDiffEntry(entry) {
      if (!entry || typeof entry !== "object") return null;
      const file = normalizePatchPath(
        entry.file || entry.path || entry.filePath || entry.filename || entry.name || entry.to || entry.newPath || "",
      );
      const before = typeof entry.before === "string"
        ? entry.before
        : typeof entry.old === "string"
          ? entry.old
          : typeof entry.previous === "string"
            ? entry.previous
            : "";
      const after = typeof entry.after === "string"
        ? entry.after
        : typeof entry.new === "string"
          ? entry.new
          : typeof entry.current === "string"
            ? entry.current
            : "";
      if (!file && !before && !after) return null;

      const parseCount = (value) => {
        const num = Number(value);
        if (!Number.isFinite(num) || num < 0) return 0;
        return Math.max(0, Math.floor(num));
      };

      return {
        file,
        before,
        after,
        additions: parseCount(entry.additions),
        deletions: parseCount(entry.deletions),
      };
    }

    function inferPatchActionFromDiff(diffEntry, fallbackEntry) {
      const fallbackAction = normalizePatchChangeType(fallbackEntry && fallbackEntry.action);
      if (fallbackAction !== "unknown") return fallbackAction;
      const diff = diffEntry && typeof diffEntry === "object" ? diffEntry : {};
      const before = String(diff.before || "");
      const after = String(diff.after || "");
      const beforeHasText = before.length > 0;
      const afterHasText = after.length > 0;
      if (beforeHasText && !afterHasText) return "deleted";
      if (!beforeHasText && afterHasText) return "added";

      const additions = Number(diff.additions || 0);
      const deletions = Number(diff.deletions || 0);
      if (deletions > 0 && additions <= 0) return "deleted";
      if (additions > 0 && deletions <= 0) return "added";
      return "modified";
    }

    function countPatchDiffStats(diffEntry, diffLines = []) {
      const diff = diffEntry && typeof diffEntry === "object" ? diffEntry : {};
      const additions = Number(diff.additions);
      const deletions = Number(diff.deletions);
      const hasExplicitCounts = Number.isFinite(additions) || Number.isFinite(deletions);
      if (hasExplicitCounts) {
        return {
          added: Number.isFinite(additions) && additions > 0 ? Math.floor(additions) : 0,
          removed: Number.isFinite(deletions) && deletions > 0 ? Math.floor(deletions) : 0,
        };
      }
      return countPatchLineChanges(diffLines);
    }

    function fileNameOnly(pathLike) {
      const raw = String(pathLike || "").trim();
      if (!raw) return "";
      const normalized = raw.replace(/\\/g, "/");
      const parts = normalized.split("/");
      return parts[parts.length - 1] || normalized;
    }

    function pickToolInput(block) {
      if (!block || typeof block !== "object") return null;
      if (block.toolInput && typeof block.toolInput === "object") return block.toolInput;
      if (block.raw && block.raw.state && block.raw.state.input && typeof block.raw.state.input === "object") {
        return block.raw.state.input;
      }
      if (block.raw && block.raw.input && typeof block.raw.input === "object") return block.raw.input;
      return null;
    }

    function inferToolSummary(block, toolName) {
      const normalizedTool = String(toolName || "").trim().toLowerCase();
      const rawSummary = String((block && (block.preview || block.summary)) || "").trim();
      const summary = /^工具:\s*/.test(rawSummary) ? "" : rawSummary;
      if (summary) return truncateSummaryText(summary);

      const input = pickToolInput(block);
      if (!input) return "";

      if (["read", "write", "edit"].includes(normalizedTool)) {
        const pathText = input.filePath || input.file_path || input.path || input.target_path || "";
        return truncateSummaryText(fileNameOnly(pathText));
      }

      if (normalizedTool === "bash") {
        const command = input.command || input.cmd || input.script || "";
        return truncateSummaryText(command, 72);
      }

      if (normalizedTool === "web_search") {
        return truncateSummaryText(input.query || input.keyword || "", 72);
      }

      if (normalizedTool === "web_fetch") {
        return truncateSummaryText(input.url || input.uri || "", 72);
      }

      if (normalizedTool === "ls") {
        return truncateSummaryText(fileNameOnly(input.path || "."), 72);
      }

      if (normalizedTool === "grep" || normalizedTool === "glob") {
        return truncateSummaryText(input.pattern || input.query || "", 72);
      }

      if (normalizedTool === "skill") {
        return truncateSummaryText(input.skill || input.name || "", 72);
      }

      return "";
    }

    function parsePatchFromToPayload(rawText) {
      const text = String(rawText || "").trim();
      if (!text) return null;

      const arrow = text.match(/^(.+?)\s*->\s*(.+)$/);
      if (arrow && arrow[1] && arrow[2]) {
        return {
          from: normalizePatchPath(arrow[1]),
          to: normalizePatchPath(arrow[2]),
        };
      }

      const tabParts = text.split(/\t+/).map((part) => normalizePatchPath(part)).filter(Boolean);
      if (tabParts.length >= 2) {
        return {
          from: tabParts[0],
          to: tabParts[tabParts.length - 1],
        };
      }

      const spacedParts = text.split(/\s{2,}/).map((part) => normalizePatchPath(part)).filter(Boolean);
      if (spacedParts.length >= 2) {
        return {
          from: spacedParts[0],
          to: spacedParts[spacedParts.length - 1],
        };
      }

      return null;
    }

    function normalizePatchFileEntry(entry) {
      if (entry === null || entry === undefined) return null;

      if (typeof entry === "string") {
        const text = normalizePatchPath(entry);
        if (!text) return null;

        const statusMatch = text.match(/^([ACDMRTUXB\?])(?:\d+)?\s+(.+)$/i);
        if (statusMatch) {
          const code = String(statusMatch[1] || "").toUpperCase();
          const payload = normalizePatchPath(statusMatch[2]);
          const parsedPair = parsePatchFromToPayload(payload);
          if (code === "R") {
            if (parsedPair) {
              return {
                action: "renamed",
                from: parsedPair.from,
                to: parsedPair.to,
                path: parsedPair.to || parsedPair.from,
              };
            }
            return { action: "renamed", path: payload };
          }
          if (code === "C") {
            if (parsedPair) {
              return {
                action: "copied",
                from: parsedPair.from,
                to: parsedPair.to,
                path: parsedPair.to || parsedPair.from,
              };
            }
            return { action: "copied", path: payload };
          }

          const actionMap = {
            A: "added",
            M: "modified",
            D: "deleted",
            T: "modified",
            U: "modified",
            X: "modified",
            B: "modified",
            "?": "unknown",
          };
          return {
            action: actionMap[code] || "unknown",
            path: payload,
          };
        }

        const renameFrom = text.match(/^rename from\s+(.+)$/i);
        if (renameFrom && renameFrom[1]) {
          return {
            action: "renamed",
            from: normalizePatchPath(renameFrom[1]),
          };
        }
        const renameTo = text.match(/^rename to\s+(.+)$/i);
        if (renameTo && renameTo[1]) {
          return {
            action: "renamed",
            to: normalizePatchPath(renameTo[1]),
            path: normalizePatchPath(renameTo[1]),
          };
        }

        const prefixed = text.match(/^\[?([a-zA-Z]+)\]?\s*:\s*(.+)$/);
        if (prefixed && prefixed[1] && prefixed[2]) {
          return {
            action: normalizePatchChangeType(prefixed[1]),
            path: normalizePatchPath(prefixed[2]),
          };
        }

        const pair = parsePatchFromToPayload(text);
        if (pair) {
          return {
            action: "renamed",
            from: pair.from,
            to: pair.to,
            path: pair.to || pair.from,
          };
        }

        return { action: "unknown", path: text };
      }

      if (typeof entry !== "object") {
        const raw = normalizePatchPath(entry);
        return raw ? { action: "unknown", path: raw } : null;
      }

      const rawAction = entry.action || entry.status || entry.changeType || entry.op || entry.kind || entry.type;
      const from = normalizePatchPath(entry.from || entry.oldPath || entry.previousPath || entry.source || entry.src || "");
      const to = normalizePatchPath(entry.to || entry.newPath || entry.target || entry.dest || entry.dst || "");
      let pathValue = normalizePatchPath(
        entry.path || entry.file || entry.filePath || entry.filename || entry.name || to || from || "",
      );
      let action = normalizePatchChangeType(rawAction);

      if (action === "unknown" && from && to && from !== to) action = "renamed";
      if (action === "renamed" && !pathValue) pathValue = to || from;
      if (action === "deleted" && !pathValue) pathValue = from;
      if (action === "added" && !pathValue) pathValue = to;
      if (!pathValue && !from && !to) return null;

      return {
        action,
        path: pathValue,
        from: from || "",
        to: to || "",
      };
    }

    function patchFileDisplayPath(entry) {
      const item = entry && typeof entry === "object" ? entry : {};
      const action = normalizePatchChangeType(item.action);
      const from = normalizePatchPath(item.from || "");
      const to = normalizePatchPath(item.to || "");
      const pathText = normalizePatchPath(item.path || "");

      if ((action === "renamed" || action === "copied") && from && to) {
        return `${from} -> ${to}`;
      }
      return pathText || to || from || "";
    }

    function extractPatchFileEntries(block, detailText = "") {
      const fileEntries = [];
      let pendingRenameFrom = "";

      const inputEntries = (() => {
        if (block && block.raw && Array.isArray(block.raw.files) && block.raw.files.length) {
          return block.raw.files;
        }
        return String(detailText || "")
          .split(/\r?\n/)
          .map((line) => line.replace(/^\s*-\s*/, "").trim())
          .filter(Boolean);
      })();

      for (const sourceEntry of inputEntries) {
        const normalized = normalizePatchFileEntry(sourceEntry);
        if (!normalized) continue;

        const from = normalizePatchPath(normalized.from || "");
        const to = normalizePatchPath(normalized.to || "");
        const pathText = normalizePatchPath(normalized.path || "");
        const action = normalizePatchChangeType(normalized.action);

        if (action === "renamed" && from && !to && !pathText) {
          pendingRenameFrom = from;
          continue;
        }
        if (action === "renamed" && !from && (to || pathText) && pendingRenameFrom) {
          const resolvedTo = to || pathText;
          fileEntries.push({
            action: "renamed",
            from: pendingRenameFrom,
            to: resolvedTo,
            path: resolvedTo,
          });
          pendingRenameFrom = "";
          continue;
        }

        const entry = {
          action,
          from,
          to,
          path: pathText || to || from,
        };
        if (!entry.path && !entry.from && !entry.to) continue;
        fileEntries.push(entry);
      }

      if (pendingRenameFrom) {
        fileEntries.push({
          action: "unknown",
          path: pendingRenameFrom,
          from: "",
          to: "",
        });
      }

      return fileEntries;
    }

    function summarizePatchChanges(entries) {
      const list = Array.isArray(entries) ? entries : [];
      if (!list.length) return "";
      return `${list.length} 个文件变更`;
    }

    function withInferredPatchActions(entries, message, patchBlockIndex) {
      void message;
      void patchBlockIndex;
      const list = Array.isArray(entries) ? entries : [];
      return list;
    }

    function extractPatchFiles(block, detailText = "") {
      return extractPatchFileEntries(block, detailText)
        .map((entry) => patchFileDisplayPath(entry))
        .filter(Boolean);
    }

    function patchHash(block) {
      if (block && block.raw && typeof block.raw.hash === "string" && block.raw.hash.trim()) {
        return block.raw.hash.trim();
      }
      const summary = String((block && block.summary) || "").trim();
      const match = summary.match(/hash:\s*([a-f0-9]+)/i);
      return match && match[1] ? match[1] : "";
    }

    function toolDisplayName(block) {
      if (!block || typeof block !== "object") return "";
      if (typeof block.tool === "string" && block.tool.trim()) return block.tool.trim();
      const summary = typeof block.summary === "string" ? block.summary.trim() : "";
      const summaryMatch = summary.match(/^工具:\s*(.+)$/);
      if (summaryMatch && summaryMatch[1]) return summaryMatch[1].trim();
      const title = typeof block.title === "string" ? block.title.trim() : "";
      return title;
    }

    function visibleAssistantBlocks(rawBlocks) {
      const blocks = Array.isArray(rawBlocks) ? rawBlocks : [];
      return blocks.filter((block) => {
        if (!block || typeof block !== "object") return false;
        const type = String(block.type || "").trim().toLowerCase();
        if (!type) return false;
        if (type === "step-start" || type === "step-finish") return false;
        if (type === "tool") {
          const toolName = String((block.tool || "")).trim().toLowerCase();
          const status = normalizeBlockStatus(block.status);
          if (INTERNAL_NOISY_TOOL_NAMES.has(toolName) && status !== "error") return false;
        }
        return true;
      });
    }

    function hasReasoningBlock(rawBlocks) {
      const blocks = this.visibleAssistantBlocks(rawBlocks);
      return blocks.some((block) => String((block && block.type) || "").trim().toLowerCase() === "reasoning");
    }

    function removeStandaloneReasoningContainer(row) {
      if (!row) return;
      const reasoning = row.querySelector(".oc-message-reasoning:not(.oc-part-reasoning)");
      if (reasoning && reasoning.parentElement) {
        reasoning.parentElement.removeChild(reasoning);
      }
    }

    function findMessageRow(messageId) {
      if (!this.elements.messages || !messageId) return null;
      const rows = this.elements.messages.querySelectorAll(".oc-message");
      for (const row of rows) {
        if (row && row.dataset && row.dataset.messageId === messageId) return row;
      }
      return null;
    }


    const blockUtilsMethods = {
      ensureReasoningContainer,
      ensureBlocksContainer,
      reorderAssistantMessageLayout,
      normalizeBlockStatus,
      resolveDisplayBlockStatus,
      blockTypeLabel,
      blockStatusLabel,
      inferToolSummary,
      toolDisplayName,
      visibleAssistantBlocks,
      hasReasoningBlock,
      removeStandaloneReasoningContainer,
      findMessageRow,
    };

    const blockUtilsInternal = {
      truncateSummaryText,
      normalizePatchPath,
      normalizePatchChangeType,
      patchChangeLabel,
      normalizePatchFileEntry,
      normalizePatchDiffEntry,
      inferPatchActionFromDiff,
      buildPatchLineDiff,
      splitPatchDiffHunks,
      countPatchDiffStats,
      patchFileDisplayPath,
      extractPatchFileEntries,
      summarizePatchChanges,
      withInferredPatchActions,
      extractPatchFiles,
      patchHash,
    };

    module2.exports = {
      blockUtilsMethods,
      blockUtilsInternal,
    };
  }
});

// runtime/view/message/block-render-methods.js
var require_block_render_methods = __commonJS({
  "runtime/view/message/block-render-methods.js"(exports2, module2) {
    var { normalizeMarkdownForDisplay } = require_assistant_payload_utils();
    var { MARKDOWN_RENDER_STATE } = require_markdown_methods();
    var { domUtils } = require_dom_utils();
    var { blockUtilsMethods, blockUtilsInternal } = require_block_utils();
    var { tFromContext } = require_i18n_runtime();

    const {
      setNodeText,
      safeSetIcon,
      resolveToolIconName,
      applyToolStatusIcon,
    } = domUtils;

    const {
      inferToolSummary,
    } = blockUtilsMethods;

    const {
      truncateSummaryText,
      normalizePatchPath,
      normalizePatchChangeType,
      extractPatchFileEntries,
      summarizePatchChanges,
      patchChangeLabel,
      patchFileDisplayPath,
      normalizePatchDiffEntry,
      inferPatchActionFromDiff,
      buildPatchLineDiff,
      splitPatchDiffHunks,
      countPatchDiffStats,
    } = blockUtilsInternal;

    const PATCH_CONTEXT_LINES = 3;
    const PATCH_MAX_MATRIX_CELLS = 120000;
    const PATCH_MAX_RENDERED_LINES = 320;
    const FILE_MUTATION_TOOL_NAMES = new Set(["write", "edit", "multiedit"]);

    function clampRenderText(value, maxLen = 12000) {
      const raw = String(value || "");
      const limit = Math.max(512, Number(maxLen) || 12000);
      if (raw.length <= limit) return raw;
      return `${raw.slice(0, limit)}\n\n...(truncated ${raw.length - limit} chars)`;
    }

    function normalizeComparablePath(value) {
      return String(value || "")
        .trim()
        .replace(/\\/g, "/")
        .replace(/\/+/g, "/")
        .replace(/^\/+/, "")
        .toLowerCase();
    }

    function isAbsolutePath(pathValue) {
      const raw = String(pathValue || "").trim();
      if (!raw) return false;
      return /^(?:[A-Za-z]:[\\/]|\/|\\\\)/.test(raw);
    }

    function normalizeComparableFsPath(pathValue) {
      const normalized = String(pathValue || "")
        .trim()
        .replace(/\\/g, "/")
        .replace(/\/+/g, "/");
      if (normalized.length > 1 && normalized.endsWith("/")) return normalized.slice(0, -1);
      return normalized;
    }

    function getVaultBasePath(view) {
      const vault = view && view.app && view.app.vault;
      const adapter = vault && vault.adapter;
      const getBasePath = adapter && typeof adapter.getBasePath === "function"
        ? adapter.getBasePath.bind(adapter)
        : null;
      if (!getBasePath) return "";
      return normalizeComparableFsPath(getBasePath());
    }

    function isPathInsideDirectory(pathValue, directory) {
      const target = normalizeComparableFsPath(pathValue).toLowerCase();
      const base = normalizeComparableFsPath(directory).toLowerCase();
      if (!target || !base) return false;
      return target === base || target.startsWith(`${base}/`);
    }

    function getCurrentPluginAbsoluteDir(view) {
      const basePath = getVaultBasePath(view);
      if (!basePath) return "";

      const plugin = view && view.plugin && typeof view.plugin === "object" ? view.plugin : null;
      const manifest = plugin && plugin.manifest && typeof plugin.manifest === "object" ? plugin.manifest : {};
      const manifestDir = String(manifest.dir || "").trim();
      const pluginId = String(manifest.id || "").trim();

      if (manifestDir) {
        const candidate = isAbsolutePath(manifestDir)
          ? normalizeComparableFsPath(manifestDir)
          : normalizeComparableFsPath(`${basePath}/${manifestDir}`);
        if (candidate) return candidate;
      }
      if (pluginId) return normalizeComparableFsPath(`${basePath}/.obsidian/plugins/${pluginId}`);
      return "";
    }

    function getCurrentPluginVaultRelativeDir(view) {
      const plugin = view && view.plugin && typeof view.plugin === "object" ? view.plugin : null;
      const manifest = plugin && plugin.manifest && typeof plugin.manifest === "object" ? plugin.manifest : {};
      const manifestDir = String(manifest.dir || "").trim().replace(/\\/g, "/");
      const pluginId = String(manifest.id || "").trim();

      if (manifestDir) {
        if (isAbsolutePath(manifestDir)) {
          const asRelative = resolveAbsolutePathToVaultPath(view, manifestDir);
          if (asRelative) return normalizePatchPath(asRelative).replace(/^\/+/, "");
        } else if (manifestDir.includes("/")) {
          return normalizePatchPath(manifestDir).replace(/^\/+/, "");
        } else if (pluginId) {
          return `.obsidian/plugins/${pluginId}`;
        }
      }
      if (pluginId) return `.obsidian/plugins/${pluginId}`;
      return "";
    }

    function isPathInsideCurrentPluginDirectory(view, pathValue) {
      const target = normalizeComparableFsPath(pathValue);
      if (!target || !isAbsolutePath(target)) return false;
      const pluginDir = getCurrentPluginAbsoluteDir(view);
      if (!pluginDir) return false;
      return isPathInsideDirectory(target, pluginDir);
    }

    function resolveAbsolutePathToVaultPath(view, absolutePath) {
      const basePath = getVaultBasePath(view);
      const inputPath = normalizeComparableFsPath(absolutePath);
      if (!basePath || !inputPath) return "";
      const baseLower = basePath.toLowerCase();
      const inputLower = inputPath.toLowerCase();
      const prefix = `${baseLower}/`;
      if (!inputLower.startsWith(prefix)) return "";
      return normalizePatchPath(inputPath.slice(basePath.length + 1));
    }

    function collectComparablePatchPaths(view, pathValue) {
      const raw = normalizePatchPath(pathValue).replace(/\\/g, "/");
      if (!raw) return [];
      const keys = new Set();
      keys.add(normalizeComparablePath(raw));

      if (isAbsolutePath(raw)) {
        const relative = resolveAbsolutePathToVaultPath(view, raw);
        if (relative) keys.add(normalizeComparablePath(relative));
      } else {
        keys.add(normalizeComparablePath(raw.replace(/^\/+/, "")));
      }

      return Array.from(keys).filter(Boolean);
    }

    function resolvePatchLinkPath(view, rawPath) {
      const vault = view && view.app && view.app.vault;
      if (!vault || typeof vault.getAbstractFileByPath !== "function") return "";
      const normalized = normalizePatchPath(rawPath).replace(/\\/g, "/");
      if (!normalized) return "";

      const tryPath = (candidate) => {
        const pathValue = normalizePatchPath(candidate).replace(/^\/+/, "");
        if (!pathValue) return "";
        const target = vault.getAbstractFileByPath(pathValue);
        if (target && !Array.isArray(target.children)) {
          return normalizePatchPath(target.path || pathValue);
        }
        return "";
      };

      const direct = tryPath(normalized);
      if (direct) return direct;

      const relative = isAbsolutePath(normalized)
        ? resolveAbsolutePathToVaultPath(view, normalized)
        : normalized.replace(/^\/+/, "");
      const resolvedRelative = tryPath(relative);
      if (resolvedRelative) return resolvedRelative;

      const metadataCache = view && view.app && view.app.metadataCache;
      if (metadataCache && typeof metadataCache.getFirstLinkpathDest === "function") {
        const sourcePath = typeof view.getMarkdownRenderSourcePath === "function"
          ? String(view.getMarkdownRenderSourcePath() || "")
          : "";
        const byLink = metadataCache.getFirstLinkpathDest(relative, sourcePath);
        if (byLink && typeof byLink.path === "string" && byLink.path) {
          return normalizePatchPath(byLink.path);
        }
        const basename = String(relative || normalized).split("/").pop();
        if (basename) {
          const byBasename = metadataCache.getFirstLinkpathDest(basename, sourcePath);
          if (byBasename && typeof byBasename.path === "string" && byBasename.path) {
            return normalizePatchPath(byBasename.path);
          }
        }
      }

      if (typeof vault.getFiles === "function") {
        const rel = String(relative || normalized).replace(/^\/+/, "");
        const basename = rel.split("/").pop();
        const allFiles = vault.getFiles();
        const match = allFiles.find((file) => {
          if (!file || typeof file.path !== "string") return false;
          const filePath = normalizePatchPath(file.path).replace(/\\/g, "/");
          if (!filePath) return false;
          if (rel && filePath === rel) return true;
          if (rel && filePath.endsWith(`/${rel}`)) return true;
          if (basename && filePath.endsWith(`/${basename}`)) return true;
          return false;
        });
        if (match && typeof match.path === "string" && match.path) {
          return normalizePatchPath(match.path);
        }
      }
      return "";
    }

    function resolvePatchPathInfo(view, displayPath, targetPath) {
      const rawDisplayPath = normalizePatchPath(displayPath).replace(/\\/g, "/");
      const rawTargetPath = normalizePatchPath(targetPath || rawDisplayPath).replace(/\\/g, "/");
      const relativeDisplay = isAbsolutePath(rawDisplayPath)
        ? resolveAbsolutePathToVaultPath(view, rawDisplayPath)
        : rawDisplayPath.replace(/^\/+/, "");
      const linkPath = resolvePatchLinkPath(view, rawTargetPath);
      return {
        rawPath: rawDisplayPath || rawTargetPath,
        displayPath: relativeDisplay || rawDisplayPath || rawTargetPath,
        linkPath,
        isLinkable: Boolean(linkPath),
      };
    }

    function ensurePatchDiffStateStore(view) {
      if (!(view.patchDiffCache instanceof Map)) view.patchDiffCache = new Map();
      if (!(view.patchDiffInflight instanceof Map)) view.patchDiffInflight = new Map();
    }

    function resolvePatchMessageId(message, raw) {
      const candidates = [
        raw && raw.messageID,
        raw && raw.messageId,
        message && message.messageID,
        message && message.messageId,
        message && message.remoteMessageId,
        message && message.serverMessageId,
        message && message.sdkMessageId,
      ];
      for (const candidate of candidates) {
        const normalized = String(candidate || "").trim();
        if (!normalized) continue;
        return normalized;
      }
      return "";
    }

    function getPatchDiffRequest(view, block, message) {
      const raw = block && block.raw && typeof block.raw === "object" ? block.raw : {};
      const sessionId = String(
        raw.sessionID
        || raw.sessionId
        || "",
      ).trim();
      const messageId = resolvePatchMessageId(message, raw);
      if (!sessionId || !messageId) return null;
      return {
        key: `${sessionId}::msg:${messageId}`,
        sessionId,
        messageId,
      };
    }

    function rerenderPatchMessage(view, messageId) {
      const targetId = String(messageId || "").trim();
      if (!targetId || !view || !view.plugin || !view.plugin.sessionStore) return;
      const row = typeof view.findMessageRow === "function" ? view.findMessageRow(targetId) : null;
      if (!row) return;
      const messages = view.plugin.sessionStore.getActiveMessages();
      const message = Array.isArray(messages) ? messages.find((item) => item && item.id === targetId) : null;
      if (!message) return;
      const parent = row.parentElement;
      if (parent && typeof view.renderMessageItem === "function") {
        const nextSibling = row.nextSibling;
        row.remove();
        view.renderMessageItem(parent, message);
        const inserted = parent.lastElementChild;
        if (inserted && nextSibling && inserted !== nextSibling) {
          parent.insertBefore(inserted, nextSibling);
        }
        return;
      }
      view.renderAssistantBlocks(row, message);
      view.renderAssistantMeta(row, message);
      view.reorderAssistantMessageLayout(row);
    }

    function requestPatchDiff(view, block, message) {
      const plugin = view && view.plugin;
      const client = plugin && plugin.opencodeClient;
      if (!client || typeof client.getSessionDiff !== "function") return;
      const request = getPatchDiffRequest(view, block, message);
      if (!request || !request.key) return;
      const { key, sessionId, messageId } = request;

      ensurePatchDiffStateStore(view);
      const cached = view.patchDiffCache.get(key);
      if (cached && cached.status === "pending") return;
      if (cached && cached.status === "ready") return;
      if (view.patchDiffInflight.has(key)) return;

      view.patchDiffCache.set(key, {
        status: "pending",
        items: [],
        error: "",
        fetchedAt: 0,
      });

      const task = Promise.resolve()
        .then(async () => {
          const payload = await client.getSessionDiff({
            sessionId,
            messageId,
          });
          return (Array.isArray(payload) ? payload : [])
            .map((entry) => normalizePatchDiffEntry(entry))
            .filter(Boolean);
        })
        .then((normalized) => {
          view.patchDiffCache.set(key, {
            status: "ready",
            items: normalized,
            error: "",
            fetchedAt: Date.now(),
          });
        })
        .catch((error) => {
          view.patchDiffCache.set(key, {
            status: "error",
            items: [],
            error: error instanceof Error ? error.message : String(error || ""),
            fetchedAt: Date.now(),
          });
        })
        .finally(() => {
          view.patchDiffInflight.delete(key);
          rerenderPatchMessage(view, message && message.id);
        });

      view.patchDiffInflight.set(key, task);
    }

    function buildPatchRenderItems(view, patchEntries, diffEntries) {
      const entries = Array.isArray(patchEntries) ? patchEntries : [];
      const diffs = Array.isArray(diffEntries) ? diffEntries : [];

      const diffByKey = new Map();
      diffs.forEach((diff) => {
        if (isHiddenPatchDiff(view, diff)) return;
        for (const key of collectComparablePatchPaths(view, diff.file)) {
          if (!diffByKey.has(key)) diffByKey.set(key, diff);
        }
      });

      const used = new Set();
      const items = [];
      entries.forEach((entry) => {
        if (isHiddenPatchEntry(view, entry)) return;
        const candidates = [
          ...(collectComparablePatchPaths(view, entry && entry.path)),
          ...(collectComparablePatchPaths(view, entry && entry.to)),
          ...(collectComparablePatchPaths(view, entry && entry.from)),
        ];
        let matchedDiff = null;
        for (const key of candidates) {
          if (!key) continue;
          const target = diffByKey.get(key);
          if (target) {
            matchedDiff = target;
            used.add(target);
            break;
          }
        }
        items.push({ entry, diff: matchedDiff });
      });

      diffs.forEach((diff) => {
        if (isHiddenPatchDiff(view, diff)) return;
        if (used.has(diff)) return;
        items.push({ entry: null, diff });
      });

      return items;
    }

    function pathContainsDotPrefixedFolder(view, pathValue) {
      const normalized = normalizePatchPath(pathValue).replace(/\\/g, "/").trim();
      if (!normalized) return false;
      if (isPathInsideCurrentPluginDirectory(view, normalized)) return false;
      const relativeLike = isAbsolutePath(normalized)
        ? (resolveAbsolutePathToVaultPath(view, normalized) || normalized)
        : normalized.replace(/^\/+/, "");
      if (isPathInsideDirectory(relativeLike, getCurrentPluginVaultRelativeDir(view))) return false;
      const segments = String(relativeLike).split("/").filter(Boolean);
      for (const segment of segments) {
        if (segment === "." || segment === "..") continue;
        if (segment.startsWith(".")) return true;
      }
      return false;
    }

    function isHiddenPatchEntry(view, entry) {
      const item = entry && typeof entry === "object" ? entry : {};
      return pathContainsDotPrefixedFolder(view, item.path)
        || pathContainsDotPrefixedFolder(view, item.to)
        || pathContainsDotPrefixedFolder(view, item.from);
    }

    function isHiddenPatchDiff(view, diff) {
      const item = diff && typeof diff === "object" ? diff : {};
      return pathContainsDotPrefixedFolder(view, item.file);
    }

    function resolvePatchDisplayPaths(entry, diff) {
      const displayPathRaw = entry
        ? patchFileDisplayPath(entry)
        : normalizePatchPath(diff && diff.file);
      const targetPathRaw = entry
        ? (entry.to || entry.path || entry.from || displayPathRaw)
        : normalizePatchPath(diff && diff.file);
      return {
        displayPathRaw,
        targetPathRaw,
      };
    }

    function collectPatchSummaryPaths(view, items) {
      const list = Array.isArray(items) ? items : [];
      const seen = new Set();
      const out = [];
      for (const item of list) {
        const entry = item && item.entry ? item.entry : null;
        const diff = item && item.diff ? item.diff : null;
        const { displayPathRaw, targetPathRaw } = resolvePatchDisplayPaths(entry, diff);
        const displayPath = displayPathRaw || tFromContext(view, "view.block.pathMissing", "(path missing)");
        if (!displayPath && !targetPathRaw) continue;
        const pathInfo = resolvePatchPathInfo(view, displayPath, targetPathRaw);
        const dedupeKey = String(pathInfo.linkPath || pathInfo.displayPath || displayPath).trim();
        if (!dedupeKey || seen.has(dedupeKey)) continue;
        seen.add(dedupeKey);
        out.push({
          label: pathInfo.displayPath || displayPath,
          pathInfo,
        });
      }
      return out;
    }

    function renderPatchPath(container, text, pathInfo, view) {
      const label = String(text || "");
      const info = pathInfo && typeof pathInfo === "object" ? pathInfo : {};
      if (!info.isLinkable) {
        container.createSpan({ cls: "oc-patch-file-path", text: label });
        return;
      }
      const anchor = container.createEl("a", {
        cls: "oc-patch-file-path oc-patch-file-link internal-link",
        text: label,
        attr: {
          href: info.linkPath,
          "data-href": info.linkPath,
          title: info.rawPath || label,
        },
      });
      if (view && view.app && view.app.workspace && typeof view.app.workspace.openLinkText === "function") {
        anchor.addEventListener("click", (evt) => {
          if (!evt || (evt.button !== 0 && evt.button !== 1)) return;
          evt.preventDefault();
          evt.stopPropagation();
          const modEvent = typeof Keymap !== "undefined" && Keymap && typeof Keymap.isModEvent === "function"
            ? Keymap.isModEvent(evt)
            : Boolean(evt.metaKey || evt.ctrlKey);
          const sourcePath = typeof view.getMarkdownRenderSourcePath === "function"
            ? String(view.getMarkdownRenderSourcePath() || "")
            : "";
          const openInNewLeaf = Boolean(modEvent || evt.button === 1);
          void view.app.workspace.openLinkText(info.linkPath, sourcePath, openInNewLeaf);
        });
      }
    }

    function renderPatchFallbackFileList(container, entries, view) {
      const list = container.createDiv({ cls: "oc-tool-file-list" });
      entries.forEach((entry) => {
        const action = normalizePatchChangeType(entry && entry.action);
        const label = patchChangeLabel.call(view, action);
        const displayPath = patchFileDisplayPath(entry) || tFromContext(view, "view.block.pathMissing", "(path missing)");
        const targetPath = entry && (entry.to || entry.path || entry.from) ? (entry.to || entry.path || entry.from) : displayPath;
        const pathInfo = resolvePatchPathInfo(view, displayPath, targetPath);

        const item = list.createDiv({ cls: "oc-tool-file-item" });
        item.setAttr("data-change-type", action || "unknown");
        item.setAttr("title", pathInfo.rawPath || displayPath);
        item.createSpan({ cls: "oc-patch-file-action", text: `[${label}] ` });
        renderPatchPath(item, pathInfo.displayPath || displayPath, pathInfo, view);
      });
      if (typeof view.attachInternalLinkHandlers === "function") {
        view.attachInternalLinkHandlers(list);
      }
    }

    function renderPatchHunkLine(container, line) {
      const item = line && typeof line === "object" ? line : {};
      const type = String(item.type || "equal");
      const row = container.createDiv({ cls: "oc-patch-line" });
      row.setAttr("data-line-type", type);
      row.addClass(`is-${type}`);

      const prefix = row.createSpan({ cls: "oc-patch-line-prefix" });
      if (type === "insert") prefix.setText("+");
      else if (type === "delete") prefix.setText("-");
      else if (type === "omitted") prefix.setText("…");
      else prefix.setText(" ");

      row.createSpan({
        cls: "oc-patch-line-text",
        text: String(item.text || " "),
      });
    }

    function renderPatchDiffDetails(content, view, items) {
      if (!items.length) return false;

      const files = content.createDiv({ cls: "oc-patch-files" });
      items.forEach((item) => {
        const entry = item && item.entry ? item.entry : null;
        const diff = item && item.diff ? item.diff : null;
        const action = normalizePatchChangeType(entry && entry.action) !== "unknown"
          ? normalizePatchChangeType(entry && entry.action)
          : inferPatchActionFromDiff(diff, entry);
        const actionLabel = patchChangeLabel.call(view, action);
        const { displayPathRaw, targetPathRaw } = resolvePatchDisplayPaths(entry, diff);
        const displayPath = displayPathRaw || tFromContext(view, "view.block.pathMissing", "(path missing)");
        const pathInfo = resolvePatchPathInfo(view, displayPath, targetPathRaw);

        const fileDetails = files.createDiv({ cls: "oc-patch-file-details" });
        const summary = fileDetails.createDiv({ cls: "oc-patch-file-summary" });
        summary.createSpan({ cls: "oc-patch-file-action", text: `[${actionLabel}]` });

        const pathWrap = summary.createSpan({ cls: "oc-patch-file-summary-path" });
        renderPatchPath(pathWrap, pathInfo.displayPath || displayPath, pathInfo, view);
        summary.setAttr("title", pathInfo.rawPath || displayPath);

        const fileBody = fileDetails.createDiv({ cls: "oc-patch-file-body" });
        if (!diff) {
          fileBody.createDiv({
            cls: "oc-tool-result-text",
            text: tFromContext(view, "view.block.patchNoDiffForFile", "No line-level diff for this file."),
          });
          return;
        }

        const diffRender = buildPatchLineDiff(diff.before, diff.after, {
          maxMatrixCells: PATCH_MAX_MATRIX_CELLS,
          maxRenderedLines: PATCH_MAX_RENDERED_LINES,
        });
        const lines = Array.isArray(diffRender.lines) ? diffRender.lines : [];
        const stats = countPatchDiffStats(diff, lines);
        const statsText = `${stats.added > 0 ? `+${stats.added}` : "+0"} ${stats.removed > 0 ? `-${stats.removed}` : "-0"}`;
        summary.createSpan({ cls: "oc-patch-file-stats", text: statsText });

        const hunks = splitPatchDiffHunks(lines, { contextLines: PATCH_CONTEXT_LINES });
        if (!hunks.length) {
          fileBody.createDiv({
            cls: "oc-tool-result-text",
            text: tFromContext(view, "view.block.patchNoLineChanges", "No line changes detected."),
          });
        } else {
          hunks.forEach((hunk, hunkIndex) => {
            if (hunkIndex > 0) {
              fileBody.createDiv({ cls: "oc-patch-hunk-separator", text: "..." });
            }
            const hunkEl = fileBody.createDiv({ cls: "oc-patch-hunk" });
            hunkEl.createDiv({
              cls: "oc-patch-hunk-header",
              text: `@@ -${Number(hunk.oldStart || 1)} +${Number(hunk.newStart || 1)} @@`,
            });
            for (const line of hunk.lines) {
              renderPatchHunkLine(hunkEl, line);
            }
          });
        }

        if (diffRender.truncated) {
          const suffix = diffRender.matrixLimited
            ? tFromContext(view, "view.block.patchLargeFileFallback", "Large file fallback mode enabled.")
            : tFromContext(view, "view.block.patchLineTruncated", "Diff output was truncated.");
          fileBody.createDiv({
            cls: "oc-patch-truncated-note",
            text: `${suffix} ${tFromContext(view, "view.block.patchHiddenLines", "{count} lines hidden.", { count: Number(diffRender.hiddenLineCount || 0) })}`,
          });
        }
      });

      if (typeof view.attachInternalLinkHandlers === "function") {
        view.attachInternalLinkHandlers(files);
      }
      return true;
    }

    function renderReasoningPart(container, block, messagePending) {
      const details = container.createEl("details", { cls: "oc-thinking-block oc-part-reasoning" });
      const status = this.resolveDisplayBlockStatus(block, messagePending);
      details.addClass(`is-${status}`);
      details.setAttr("data-part-type", "reasoning");

      const summary = details.createEl("summary", { cls: "oc-thinking-header" });
      summary.createSpan({ cls: "oc-thinking-label", text: tFromContext(this, "view.block.reasoningShort", "Thought") });
      summary.createSpan({
        cls: "oc-thinking-state",
        text: this.blockStatusLabel(status),
      });

      const body = details.createDiv({ cls: "oc-thinking-content" });
      const detailText = clampRenderText(typeof block.detail === "string" ? block.detail : "", 16000);
      const content = normalizeMarkdownForDisplay(detailText);
      if (content) {
        if (messagePending) {
          MARKDOWN_RENDER_STATE.delete(body);
          setNodeText(body, content);
        } else {
          this.renderMarkdownSafely(body, content, () => {
            this.enhanceCodeBlocks(body);
          });
        }
      } else {
        MARKDOWN_RENDER_STATE.delete(body);
        setNodeText(body, tFromContext(this, "view.block.empty", "..."));
      }

      details.open = Boolean(messagePending && (status === "running" || status === "pending"));
    }

    function pickToolInputForRender(block) {
      if (!block || typeof block !== "object") return null;
      if (block.toolInput && typeof block.toolInput === "object") return block.toolInput;
      if (block.raw && block.raw.state && block.raw.state.input && typeof block.raw.state.input === "object") {
        return block.raw.state.input;
      }
      if (block.raw && block.raw.input && typeof block.raw.input === "object") return block.raw.input;
      return null;
    }
    
    function extractQuotedPathFromText(text) {
      const raw = String(text || "");
      if (!raw) return "";
      const patterns = [
        /"(?:filePath|file_path|path|target_path)"\s*:\s*"([^"]+)"/i,
        /\b(?:Updated|Wrote|Read)\s+([A-Za-z]:[\\/][^\n]+|\/[^\n]+)/i,
        /([A-Za-z]:[\\/][^\s"']+|\/[^\s"']+)/,
      ];
      for (const pattern of patterns) {
        const match = raw.match(pattern);
        if (match && match[1]) return normalizePatchPath(match[1]);
      }
      return "";
    }
    
    function inferToolFilePath(block, toolName, summaryText, detailText) {
      const normalizedTool = String(toolName || "").trim().toLowerCase();
      const input = pickToolInputForRender(block);
      const fromInput = normalizePatchPath(
        input && (
          input.filePath
          || input.file_path
          || input.path
          || input.target_path
          || input.targetPath
          || input.filename
          || input.file
        ),
      );
      if (fromInput) return fromInput;
    
      if (!["read", "write", "edit", "multiedit", "todowrite"].includes(normalizedTool)) return "";
      const fromSummary = extractQuotedPathFromText(summaryText);
      if (fromSummary) return fromSummary;
      return extractQuotedPathFromText(detailText);
    }
    
    function stripToolPathFromSummary(summaryText, filePath) {
      const summary = String(summaryText || "").trim();
      const path = String(filePath || "").trim();
      if (!summary) return "";
      if (!path) return summary;
      const escaped = path.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");
      const stripped = summary
        .replace(new RegExp(escaped, "ig"), "")
        .replace(/\s{2,}/g, " ")
        .replace(/[·•\-:\s]+$/, "")
        .trim();
      return stripped || summary;
    }
    
    function pickToolOutputForRender(block) {
      if (!block || typeof block !== "object") return "";
      if (typeof block.toolOutput === "string" && block.toolOutput.trim()) return block.toolOutput;
      const raw = block.raw && typeof block.raw === "object" ? block.raw : {};
      const state = raw.state && typeof raw.state === "object" ? raw.state : {};
      if (typeof state.output === "string" && state.output.trim()) return state.output;
      if (typeof raw.output === "string" && raw.output.trim()) return raw.output;
      return "";
    }
    
    function inferMutationActionFromText(toolName, text) {
      const normalizedTool = String(toolName || "").trim().toLowerCase();
      const raw = String(text || "");
      if (/\b(deleted|removed|remove)\b/i.test(raw)) return "deleted";
      if (/\b(renamed|rename|moved|move)\b/i.test(raw)) return "renamed";
      if (/\b(created|create|added|add)\b/i.test(raw)) return "added";
      if (normalizedTool === "write") return "modified";
      return "modified";
    }
    
    function isMutationToolCall(block, toolName, filePath, detailText, summaryText) {
      const normalizedTool = String(toolName || "").trim().toLowerCase();
      if (!filePath) return false;
      if (FILE_MUTATION_TOOL_NAMES.has(normalizedTool)) return true;
      const combined = [
        String(summaryText || ""),
        String(detailText || ""),
        String(pickToolOutputForRender(block) || ""),
      ].join("\n");
      return /\b(updated|modified|created|wrote|deleted|removed|patched|renamed)\b/i.test(combined);
    }
    
    function findMatchingDiffEntryForPath(view, diffEntries, pathValue) {
      const targetKeys = new Set(collectComparablePatchPaths(view, pathValue));
      if (!targetKeys.size) return null;
      for (const diff of Array.isArray(diffEntries) ? diffEntries : []) {
        for (const key of collectComparablePatchPaths(view, diff && diff.file)) {
          if (targetKeys.has(key)) return diff;
        }
      }
      return null;
    }
    
    function buildMutationFallbackEntry(toolName, filePath, detailText, summaryText) {
      const path = normalizePatchPath(filePath);
      if (!path) return null;
      return {
        action: inferMutationActionFromText(toolName, `${summaryText || ""}\n${detailText || ""}`),
        path,
      };
    }
    
    function collectToolLocalDiffEntries(block, view) {
      const metadata = block && block.metadata && typeof block.metadata === "object"
        ? block.metadata
        : {};
      const rawEntries = [];
      const filediff = metadata.filediff && typeof metadata.filediff === "object" ? metadata.filediff : null;
      if (filediff) rawEntries.push(filediff);
    
      const filediffs = Array.isArray(metadata.filediffs) ? metadata.filediffs : [];
      for (const item of filediffs) rawEntries.push(item);
    
      const normalized = [];
      for (const entry of rawEntries) {
        const diff = normalizePatchDiffEntry(entry);
        if (!diff) continue;
        if (isHiddenPatchDiff(view, diff)) continue;
        normalized.push(diff);
      }
      return normalized;
    }
    
    function renderToolPart(container, block, messagePending, message) {
      const status = this.resolveDisplayBlockStatus(block, messagePending);
      const toolName = this.toolDisplayName(block) || tFromContext(this, "view.block.tool", "tool");
      const normalizedToolName = String((block && block.tool) || toolName || "tool").trim().toLowerCase();
      let summaryText = inferToolSummary(block, normalizedToolName || toolName);
      const questionItems = normalizedToolName === "question"
        ? this.extractQuestionItemsFromBlock(block)
        : [];
      if (!summaryText && questionItems.length) {
        const firstQuestion = String((questionItems[0] && questionItems[0].question) || "").trim();
        const suffix = questionItems.length > 1 ? ` (+${questionItems.length - 1})` : "";
        summaryText = truncateSummaryText(`${firstQuestion}${suffix}`);
      }
      const detailText = clampRenderText(String((block && block.detail) || "").trim(), 12000);
    
      const details = container.createEl("details", { cls: "oc-tool-call" });
      details.addClass(`is-${status}`);
      details.setAttr("data-part-type", "tool");
      details.setAttr("data-tool-name", normalizedToolName || "tool");
      details.open = messagePending ? (status === "running" || status === "error") : status === "error";
    
      const header = details.createEl("summary", { cls: "oc-tool-header" });
      const iconEl = header.createSpan({ cls: "oc-tool-icon" });
      safeSetIcon(iconEl, resolveToolIconName(normalizedToolName || toolName));
    
      header.createSpan({ cls: "oc-tool-name", text: toolName });
    
      const summaryEl = header.createSpan({ cls: "oc-tool-summary" });
      const linkedFilePathRaw = inferToolFilePath(block, normalizedToolName, summaryText, detailText);
      const linkedFilePath = pathContainsDotPrefixedFolder(this, linkedFilePathRaw) ? "" : linkedFilePathRaw;
      const linkedPathInfo = linkedFilePath
        ? resolvePatchPathInfo(this, linkedFilePath, linkedFilePath)
        : null;
      const summaryLabel = stripToolPathFromSummary(summaryText, linkedFilePath);
      summaryEl.setText(summaryLabel);
    
      if (linkedPathInfo && linkedPathInfo.displayPath) {
        if (summaryLabel) {
          header.createSpan({ cls: "oc-patch-summary-divider", text: "·" });
        }
        const pathWrap = header.createSpan({ cls: "oc-patch-summary-path oc-tool-summary-path" });
        renderPatchPath(pathWrap, linkedPathInfo.displayPath, linkedPathInfo, this);
      }
    
      const statusEl = header.createSpan({ cls: "oc-tool-status" });
      applyToolStatusIcon(statusEl, status);
    
      const content = details.createDiv({ cls: "oc-tool-content" });
      const shouldRenderMutationCard = isMutationToolCall(block, normalizedToolName, linkedFilePath, detailText, summaryText);
    
      if (shouldRenderMutationCard) {
        details.addClass("oc-tool-patch");
        content.addClass("oc-tool-patch-content");
    
        const localDiffEntries = collectToolLocalDiffEntries(block, this);
        const hasLocalDiff = localDiffEntries.length > 0;
    
        let diffState = null;
        if (!hasLocalDiff) {
          ensurePatchDiffStateStore(this);
          const patchRequest = getPatchDiffRequest(this, block, message);
          const patchKey = patchRequest && patchRequest.key ? patchRequest.key : "";
          diffState = patchKey ? this.patchDiffCache.get(patchKey) : null;
          if (!messagePending && patchKey && !diffState) {
            requestPatchDiff(this, block, message);
            diffState = this.patchDiffCache.get(patchKey) || null;
          }
        }
    
        const sessionDiffEntries = diffState && diffState.status === "ready" && Array.isArray(diffState.items)
          ? diffState.items.filter((diff) => !isHiddenPatchDiff(this, diff))
          : [];
        const diffEntries = hasLocalDiff ? localDiffEntries : sessionDiffEntries;
        const matchedDiff = findMatchingDiffEntryForPath(this, diffEntries, linkedFilePath)
          || (diffEntries.length === 1 ? diffEntries[0] : null);
        const fallbackEntry = buildMutationFallbackEntry(normalizedToolName, linkedFilePath, detailText, summaryText);
        const renderItems = matchedDiff || fallbackEntry ? [{ entry: fallbackEntry, diff: matchedDiff }] : [];
        const renderedDiff = renderItems.length
          ? renderPatchDiffDetails(content, this, renderItems)
          : false;
    
        if (!renderedDiff && fallbackEntry) {
          renderPatchFallbackFileList(content, [fallbackEntry], this);
        } else if (!renderedDiff) {
          content.createDiv({ cls: "oc-tool-result-text", text: tFromContext(this, "view.block.patchNoDetail", "No patch details detected") });
        }
    
        if (!hasLocalDiff && diffState && diffState.status === "pending") {
          content.createDiv({
            cls: "oc-tool-result-text oc-patch-load-status",
            text: tFromContext(this, "view.block.patchLoadingDiff", "Loading line-level diff..."),
          });
        }
        if (!hasLocalDiff && diffState && diffState.status === "error") {
          content.createDiv({
            cls: "oc-tool-result-text oc-patch-load-status is-warning",
            text: tFromContext(this, "view.block.patchDiffFallback", "Line-level diff unavailable; showing file list."),
          });
        }
        return;
      }
    
      if (questionItems.length) {
        content.createDiv({
          cls: "oc-question-inline-note",
          text: tFromContext(this, "view.question.answerInPanel", "Please answer in the panel below."),
        });
        const firstQuestion = String((questionItems[0] && questionItems[0].question) || "").trim();
        if (firstQuestion) {
          content.createDiv({
            cls: "oc-question-inline-prompt",
            text: firstQuestion,
          });
        }
      }
    
      if (detailText) {
        const pre = content.createEl("pre", { cls: "oc-tool-detail", text: detailText });
        pre.setAttr("dir", "auto");
      } else if (summaryText) {
        content.createDiv({ cls: "oc-tool-result-text", text: summaryText });
      } else {
        content.createDiv({ cls: "oc-tool-result-text", text: tFromContext(this, "view.block.noResult", "No result yet") });
      }
    }
    
    function renderPatchPart(container, block, messagePending, message, blockIndex) {
      void blockIndex;
      const status = this.resolveDisplayBlockStatus(block, messagePending);
      const detailText = clampRenderText(String((block && block.detail) || "").trim(), 12000);
      const entries = extractPatchFileEntries(block, detailText).filter((entry) => !isHiddenPatchEntry(this, entry));

      ensurePatchDiffStateStore(this);
      const patchRequest = getPatchDiffRequest(this, block, message);
      const patchKey = patchRequest && patchRequest.key ? patchRequest.key : "";
      let diffState = patchKey ? this.patchDiffCache.get(patchKey) : null;
      if (!messagePending && patchKey && !diffState) {
        requestPatchDiff(this, block, message);
        diffState = this.patchDiffCache.get(patchKey) || null;
      }

      const diffEntries = diffState && diffState.status === "ready" && Array.isArray(diffState.items)
        ? diffState.items.filter((diff) => !isHiddenPatchDiff(this, diff))
        : [];
      const renderItems = buildPatchRenderItems(this, entries, diffEntries);
      if (!messagePending && diffState && diffState.status === "ready" && !renderItems.length) return;
      const changeSummary = summarizePatchChanges(entries);
      const summaryText = changeSummary
        || (renderItems.length ? `${renderItems.length} 个文件变更` : "")
        || tFromContext(this, "view.block.patchLabel", "File change");
      const summaryPaths = collectPatchSummaryPaths(this, renderItems);

      const details = container.createEl("details", { cls: "oc-tool-call oc-tool-patch" });
      details.addClass(`is-${status}`);
      details.setAttr("data-part-type", "patch");
      details.setAttr("data-tool-name", "patch");
      details.open = messagePending ? (status === "running" || status === "error") : status === "error";

      const header = details.createEl("summary", { cls: "oc-tool-header" });
      const iconEl = header.createSpan({ cls: "oc-tool-icon" });
      safeSetIcon(iconEl, "git-commit-horizontal");
      header.createSpan({ cls: "oc-tool-name", text: tFromContext(this, "view.block.patchLabel", "File change") });
      header.createSpan({ cls: "oc-tool-summary", text: summaryText });
      if (summaryPaths.length) {
        header.createSpan({ cls: "oc-patch-summary-divider", text: "·" });
        const summaryPathWrap = header.createSpan({ cls: "oc-patch-summary-path" });
        const primaryPath = summaryPaths.find((item) => item && item.pathInfo && item.pathInfo.isLinkable) || summaryPaths[0];
        renderPatchPath(summaryPathWrap, primaryPath.label, primaryPath.pathInfo, this);
        if (summaryPaths.length > 1) {
          header.createSpan({ cls: "oc-patch-summary-more", text: `+${summaryPaths.length - 1}` });
        }
      }
      const expandEl = header.createSpan({ cls: "oc-patch-expand-indicator" });
      safeSetIcon(expandEl, "chevron-right");
      const statusEl = header.createSpan({ cls: "oc-tool-status" });
      applyToolStatusIcon(statusEl, status);

      const content = details.createDiv({ cls: "oc-tool-content oc-tool-patch-content" });
      const renderedDiff = diffEntries.length
        ? renderPatchDiffDetails(content, this, renderItems)
        : false;
      if (!renderedDiff && entries.length) {
        renderPatchFallbackFileList(content, entries, this);
      } else if (!renderedDiff) {
        content.createDiv({ cls: "oc-tool-result-text", text: tFromContext(this, "view.block.patchNoDetail", "No patch details detected") });
      }

      if (diffState && diffState.status === "pending") {
        content.createDiv({
          cls: "oc-tool-result-text oc-patch-load-status",
          text: tFromContext(this, "view.block.patchLoadingDiff", "Loading line-level diff..."),
        });
      }
      if (diffState && diffState.status === "error") {
        content.createDiv({
          cls: "oc-tool-result-text oc-patch-load-status is-warning",
          text: tFromContext(this, "view.block.patchDiffFallback", "Line-level diff unavailable; showing file list."),
        });
      }
    }

    function renderGenericPart(container, block, messagePending) {
      const card = container.createDiv({ cls: "oc-part-card" });
      const status = this.resolveDisplayBlockStatus(block, messagePending);
      card.addClass(`is-${status}`);
      card.setAttr("data-part-type", String((block && block.type) || ""));

      const head = card.createDiv({ cls: "oc-part-head" });
      head.createDiv({
        cls: "oc-part-type",
        text: this.blockTypeLabel(block && block.type),
      });
      head.createDiv({
        cls: "oc-part-status",
        text: this.blockStatusLabel(status),
      });

      const title = typeof block.title === "string" ? block.title.trim() : "";
      if (title) card.createDiv({ cls: "oc-part-title", text: title });

      const summary = clampRenderText(typeof block.summary === "string" ? block.summary.trim() : "", 2400);
      if (summary) card.createDiv({ cls: "oc-part-summary", text: summary });

      const preview = clampRenderText(typeof block.preview === "string" ? block.preview.trim() : "", 3200);
      if (preview) card.createDiv({ cls: "oc-part-preview", text: preview });

      const detail = clampRenderText(typeof block.detail === "string" ? block.detail.trim() : "", 12000);
      if (detail) {
        const details = card.createEl("details", { cls: "oc-part-details" });
        details.createEl("summary", { text: tFromContext(this, "view.block.viewDetail", "View details") });
        details.createEl("pre", { cls: "oc-part-detail", text: detail });
      }
    }

    function renderStreamTextPart(container, block, messagePending) {
      const card = container.createDiv({ cls: "oc-stream-text-part" });
      const status = this.resolveDisplayBlockStatus(block, messagePending);
      card.addClass(`is-${status}`);
      card.setAttr("data-part-type", "stream-text");

      const body = card.createDiv({ cls: "oc-stream-text-content" });
      const markdown = normalizeMarkdownForDisplay(
        clampRenderText(
          typeof block.detail === "string" && block.detail
            ? block.detail
            : (typeof block.text === "string" ? block.text : ""),
          16000,
        ),
      );
      if (markdown.trim()) {
        this.renderMarkdownSafely(body, markdown, () => {
          this.enhanceCodeBlocks(body);
        });
      } else {
        body.setText("...");
      }
    }

    function renderAssistantBlocks(row, message) {
      const messagePending = Boolean(message && message.pending);
      const blocks = this
        .visibleAssistantBlocks(message.blocks)
        .filter((block) => {
          const type = String((block && block.type) || "").trim().toLowerCase();
          if (type !== "stream-text") return true;
          return messagePending;
        });
      const container = this.ensureBlocksContainer(row);
      container.empty();
      if (!blocks.length) {
        container.toggleClass("is-empty", true);
        return;
      }
      container.toggleClass("is-empty", false);

      blocks.forEach((block, blockIndex) => {
        const type = String((block && block.type) || "").trim().toLowerCase();
        if (type === "reasoning") {
          this.renderReasoningPart(container, block, messagePending);
          return;
        }
        if (type === "tool") {
          this.renderToolPart(container, block, messagePending, message);
          return;
        }
        if (type === "patch") {
          this.renderPatchPart(container, block, messagePending, message, blockIndex);
          return;
        }
        if (type === "stream-text") {
          this.renderStreamTextPart(container, block, messagePending);
          return;
        }

        this.renderGenericPart(container, block, messagePending);
      });
    }

    function renderAssistantMeta(row, message) {
      const metaText = clampRenderText(typeof message.meta === "string" ? message.meta.trim() : "", 8000);
      if (!metaText) return;
      const pre = row.createEl("pre", { cls: "oc-message-meta", text: metaText });
      if (/error|failed|失败|status=\d{3}/i.test(metaText)) {
        pre.addClass("is-error");
      }
    }


    const blockRenderMethods = {
      renderReasoningPart,
      renderToolPart,
      renderPatchPart,
      renderStreamTextPart,
      renderGenericPart,
      renderAssistantBlocks,
      renderAssistantMeta,
    };

    module2.exports = { blockRenderMethods };
  }
});

// runtime/view/message/message-list-methods.js
var require_message_list_methods = __commonJS({
  "runtime/view/message/message-list-methods.js"(exports2, module2) {
    var { Notice: Notice2 } = require("obsidian");
    var { normalizeMarkdownForDisplay } = require_assistant_payload_utils();
    var { domUtils } = require_dom_utils();
    var { tFromContext } = require_i18n_runtime();
    var {
      safeSetIcon,
      copyTextToClipboard,
      applyCopyGlyph,
      showCopyFeedback
    } = domUtils;
    var MAX_RENDER_MARKDOWN_CHARS = 24e3;
    var MAX_RENDER_REASONING_CHARS = 16e3;
    function clampRenderText(value, maxLen = MAX_RENDER_MARKDOWN_CHARS) {
      const raw = String(value || "");
      const limit = Math.max(512, Number(maxLen) || MAX_RENDER_MARKDOWN_CHARS);
      if (raw.length <= limit) return raw;
      return `${raw.slice(0, limit)}

...(truncated ${raw.length - limit} chars)`;
    }
    function renderMessages(options = {}) {
      const container = this.elements.messages;
      if (!container) return;
      const cfg = options && typeof options === "object" ? options : {};
      const forceBottom = Boolean(cfg.forceBottom);
      if (forceBottom) this.autoScrollEnabled = true;
      this.bindMessagesScrollTracking();
      const shouldStickToBottom = forceBottom || this.shouldAutoScrollMessages();
      const prevScrollTop = Number(container.scrollTop || 0);
      container.empty();
      const messages = this.plugin.sessionStore.getActiveMessages();
      this.pruneQuestionAnswerStates(messages);
      if (!messages.length) {
        const welcome = container.createDiv({ cls: "oc-welcome" });
        welcome.createDiv({
          cls: "oc-welcome-greeting",
          text: tFromContext(this, "view.welcome.greeting", "What would you like to organize today?")
        });
        welcome.createDiv({
          cls: "oc-empty",
          text: tFromContext(this, "view.welcome.empty", "Send a message, or type / to pick a skill command.")
        });
        this.renderInlineQuestionPanel(messages);
        if (shouldStickToBottom) {
          this.scheduleScrollMessagesToBottom(true);
        } else {
          container.scrollTop = 0;
        }
        return;
      }
      messages.forEach((m) => this.renderMessageItem(container, m));
      this.syncRuntimeStatusToPendingTail();
      this.renderInlineQuestionPanel(messages);
      if (shouldStickToBottom) {
        this.scheduleScrollMessagesToBottom(true);
      } else {
        const maxTop = Math.max(0, container.scrollHeight - container.clientHeight);
        container.scrollTop = Math.max(0, Math.min(prevScrollTop, maxTop));
      }
    }
    function renderUserActions(row, message) {
      const actions = row.createDiv({ cls: "oc-user-msg-actions" });
      const copyBtn = actions.createEl("button", { cls: "oc-inline-action" });
      copyBtn.setAttr("type", "button");
      safeSetIcon(copyBtn, "copy");
      if (!copyBtn.querySelector("svg")) copyBtn.setText("C");
      copyBtn.setAttr("aria-label", tFromContext(this, "view.message.copy", "Copy message"));
      copyBtn.setAttr("title", tFromContext(this, "view.message.copy", "Copy message"));
      copyBtn.addEventListener("click", async () => {
        await copyTextToClipboard(message.text || "");
        new Notice2(tFromContext(this, "view.message.copied", "Message copied"));
      });
      const retryBtn = actions.createEl("button", { cls: "oc-inline-action" });
      retryBtn.setAttr("type", "button");
      safeSetIcon(retryBtn, "rotate-ccw");
      if (!retryBtn.querySelector("svg")) retryBtn.setText("\u21BA");
      retryBtn.setAttr("aria-label", tFromContext(this, "view.message.retry", "Retry from this message"));
      retryBtn.setAttr("title", tFromContext(this, "view.message.retry", "Retry from this message"));
      retryBtn.addEventListener("click", async () => {
        await this.sendPrompt(message.text || "");
      });
    }
    function addTextCopyButton(textBlock, sourceText) {
      if (!textBlock || textBlock.querySelector(".oc-text-copy-btn")) return;
      const copyBtn = textBlock.createEl("button", { cls: "oc-text-copy-btn" });
      copyBtn.setAttr("type", "button");
      copyBtn.setAttr("aria-label", tFromContext(this, "view.message.copyBlock", "Copy text block"));
      applyCopyGlyph(copyBtn);
      copyBtn.addEventListener("click", async (event) => {
        event.preventDefault();
        event.stopPropagation();
        try {
          await copyTextToClipboard(sourceText || "");
          showCopyFeedback(copyBtn, () => {
            applyCopyGlyph(copyBtn);
          });
        } catch {
          new Notice2(tFromContext(this, "view.message.copyFailed", "Copy failed"));
        }
      });
    }
    function normalizeMessageLinkedContextFiles(message) {
      const rawPaths = Array.isArray(message && message.linkedContextFiles) ? message.linkedContextFiles : [];
      const seen = /* @__PURE__ */ new Set();
      const normalized = [];
      rawPaths.forEach((rawPath) => {
        const next = String(rawPath || "").trim().replace(/^\/+/, "");
        if (!next || seen.has(next)) return;
        seen.add(next);
        normalized.push(next);
      });
      return normalized;
    }
    function linkedContextDisplayName(pathValue) {
      const parts = String(pathValue || "").split("/");
      return parts.length ? parts[parts.length - 1] || String(pathValue || "") : String(pathValue || "");
    }
    function isLinkedContextFolderPath(view, pathValue) {
      const normalized = String(pathValue || "").trim().replace(/^\/+/, "");
      if (!normalized) return false;
      const vault = view && view.app && view.app.vault;
      if (!vault || typeof vault.getAbstractFileByPath !== "function") return false;
      const target = vault.getAbstractFileByPath(normalized);
      return Boolean(target && Array.isArray(target.children));
    }
    function renderUserLinkedContextFiles(row, message) {
      const linkedFiles = normalizeMessageLinkedContextFiles(message);
      if (!linkedFiles.length) return;
      const panel = row.createDiv({ cls: "oc-message-context-files" });
      panel.createDiv({
        cls: "oc-message-context-label",
        text: tFromContext(this, "view.message.linkedFiles", "Linked context")
      });
      const chips = panel.createDiv({ cls: "oc-message-context-list" });
      linkedFiles.forEach((pathValue) => {
        const isFolder = isLinkedContextFolderPath(this, pathValue);
        const chip = isFolder ? chips.createDiv({
          cls: "oc-message-context-chip is-folder",
          attr: { title: pathValue }
        }) : chips.createEl("a", {
          cls: "oc-message-context-chip internal-link",
          attr: {
            href: pathValue,
            "data-href": pathValue,
            title: pathValue
          }
        });
        const iconEl = chip.createSpan({ cls: "oc-message-context-chip-icon" });
        safeSetIcon(iconEl, isFolder ? "folder" : "file-text");
        chip.createSpan({
          cls: "oc-message-context-chip-name",
          text: linkedContextDisplayName(pathValue)
        });
      });
      if (typeof this.attachInternalLinkHandlers === "function") {
        this.attachInternalLinkHandlers(panel);
      }
    }
    function renderMessageItem(parent, message) {
      const row = parent.createDiv({ cls: ["oc-message", `oc-message-${message.role}`] });
      row.dataset.messageId = message.id || "";
      if (message.pending) row.addClass("is-pending");
      const head = row.createDiv({ cls: "oc-msg-head" });
      head.createDiv({ cls: "oc-msg-role", text: message.role.toUpperCase() });
      if (message.error) head.createDiv({ cls: "oc-msg-error", text: message.error });
      const body = row.createDiv({ cls: "oc-message-content", attr: { dir: "auto" } });
      if (message.pending) {
        let pendingText = typeof message.text === "string" ? message.text : "";
        const hasPendingText = Boolean(String(pendingText || "").trim());
        const hasStreamTextBlocks = message.role === "assistant" && this.visibleAssistantBlocks(message.blocks).some((block) => String((block && block.type) || "").trim().toLowerCase() === "stream-text");
        const runtimeText = String(this.runtimeStatusState && this.runtimeStatusState.text || "").trim();
        const runtimeTone = String(this.runtimeStatusState && this.runtimeStatusState.tone || "info");
        const canShowRuntimeStatus = message.role === "assistant" && !hasPendingText && Boolean(runtimeText);
        body.removeClass("oc-runtime-tail", "is-info", "is-working", "is-error");
        if (canShowRuntimeStatus) {
          pendingText = runtimeText;
          body.addClass("oc-runtime-tail");
          if (runtimeTone === "error") body.addClass("is-error");
          else if (runtimeTone === "working") body.addClass("is-working");
          else body.addClass("is-info");
        }
        if (message.role === "assistant" && hasPendingText && hasStreamTextBlocks) {
          body.empty();
        } else if (message.role === "assistant" && pendingText.trim()) {
          const pendingMarkdown = normalizeMarkdownForDisplay(
            clampRenderText(pendingText, MAX_RENDER_MARKDOWN_CHARS)
          );
          this.renderMarkdownSafely(body, pendingMarkdown, () => {
            this.enhanceCodeBlocks(body);
          });
        } else {
          body.setText(pendingText);
        }
        const hasReasoningBlocks2 = this.hasReasoningBlock(message.blocks);
        if (message.role === "assistant" && message.reasoning && !hasReasoningBlocks2) {
          const reasoningBody = this.ensureReasoningContainer(row, true);
          if (reasoningBody) reasoningBody.textContent = clampRenderText(message.reasoning, MAX_RENDER_REASONING_CHARS);
        } else if (message.role === "assistant" && hasReasoningBlocks2) {
          this.removeStandaloneReasoningContainer(row);
        }
        if (message.role === "assistant") {
          this.renderAssistantBlocks(row, message);
          this.renderAssistantMeta(row, message);
          this.reorderAssistantMessageLayout(row);
        }
        return;
      }
      const textForRender = normalizeMarkdownForDisplay(clampRenderText(message.text || "", MAX_RENDER_MARKDOWN_CHARS));
      const hasReasoning = Boolean(message.reasoning && String(message.reasoning).trim());
      const hasReasoningBlocks = this.hasReasoningBlock(message.blocks);
      const hasBlocks = this.visibleAssistantBlocks(message.blocks).length > 0;
      const fallbackText = hasReasoning || hasBlocks ? "(\u7ED3\u6784\u5316\u8F93\u51FA\u5DF2\u8FD4\u56DE\uFF0C\u53EF\u5C55\u5F00\u4E0B\u65B9\u8BE6\u60C5\u67E5\u770B\u3002)" : "";
      const localizedFallback = hasReasoning || hasBlocks ? tFromContext(this, "view.message.structuredFallback", "(Structured output returned. Expand details below.)") : "";
      const finalText = textForRender || localizedFallback;
      if (finalText) {
        const textBlock = body.createDiv({ cls: "oc-text-block" });
        this.renderMarkdownSafely(textBlock, finalText, () => {
          this.enhanceCodeBlocks(textBlock);
          if (message.role === "assistant") {
            this.addTextCopyButton(textBlock, finalText);
          }
        });
      }
      if (message.role === "assistant" && hasReasoning && !hasReasoningBlocks) {
        const reasoningBody = this.ensureReasoningContainer(row, !textForRender);
        if (reasoningBody) {
          const reasoningText = normalizeMarkdownForDisplay(
            clampRenderText(message.reasoning || "", MAX_RENDER_REASONING_CHARS)
          );
          this.renderMarkdownSafely(reasoningBody, reasoningText, () => {
            this.enhanceCodeBlocks(reasoningBody);
          });
        }
      } else if (message.role === "assistant" && hasReasoningBlocks) {
        this.removeStandaloneReasoningContainer(row);
      }
      if (message.role === "assistant") {
        this.renderAssistantBlocks(row, message);
        this.renderAssistantMeta(row, message);
        this.reorderAssistantMessageLayout(row);
      }
      if (message.role === "user") {
        this.renderUserLinkedContextFiles(row, message);
        this.renderUserActions(row, message);
      }
    }
    var messageListMethods = {
      renderMessages,
      renderUserActions,
      addTextCopyButton,
      renderUserLinkedContextFiles,
      renderMessageItem
    };
    module2.exports = { messageListMethods };
  }
});

// runtime/view/message-renderer.js
var require_message_renderer = __commonJS({
  "runtime/view/message-renderer.js"(exports2, module2) {
    var { scrollMethods } = require_scroll_methods();
    var { markdownMethods } = require_markdown_methods();
    var { blockUtilsMethods } = require_block_utils();
    var { blockRenderMethods } = require_block_render_methods();
    var { messageListMethods } = require_message_list_methods();
    var messageRendererMethods = {
      ...scrollMethods,
      ...messageListMethods,
      ...markdownMethods,
      ...blockUtilsMethods,
      ...blockRenderMethods
    };
    module2.exports = { messageRendererMethods };
  }
});

// runtime/view/question/parse-methods.js
var require_parse_methods = __commonJS({
  "runtime/view/question/parse-methods.js"(exports2, module2) {
    function parseMaybeJsonObject(raw) {
      if (typeof raw !== "string") return null;
      const text = raw.trim();
      if (!text || !text.startsWith("{") && !text.startsWith("[")) return null;
      try {
        const parsed = JSON.parse(text);
        return parsed && typeof parsed === "object" ? parsed : null;
      } catch {
        return null;
      }
    }
    function splitQuestionOptionString(raw) {
      const text = String(raw || "").trim();
      if (!text) return [];
      const byLines = text.split(/\r?\n/).map((line) => line.trim()).filter(Boolean);
      if (byLines.length > 1) return byLines;
      if (text.includes(" / ")) {
        return text.split(" / ").map((part) => part.trim()).filter(Boolean);
      }
      if (text.includes("\u3001")) {
        return text.split("\u3001").map((part) => part.trim()).filter(Boolean);
      }
      return [text];
    }
    function normalizeQuestionOption(raw) {
      if (raw && typeof raw === "object") {
        const obj = raw;
        const label2 = typeof obj.label === "string" && obj.label.trim() || typeof obj.value === "string" && obj.value.trim() || typeof obj.text === "string" && obj.text.trim() || typeof obj.name === "string" && obj.name.trim() || "";
        const description = typeof obj.description === "string" && obj.description.trim() || typeof obj.desc === "string" && obj.desc.trim() || typeof obj.hint === "string" && obj.hint.trim() || "";
        if (!label2) return null;
        return { label: label2, description };
      }
      const label = String(raw || "").trim();
      if (!label) return null;
      return { label, description: "" };
    }
    function parseQuestionOptions(rawOptions) {
      const collected = [];
      const pushOption = (value) => {
        const normalized = this.normalizeQuestionOption(value);
        if (normalized) collected.push(normalized);
      };
      if (Array.isArray(rawOptions)) {
        rawOptions.forEach((value) => pushOption(value));
      } else if (rawOptions && typeof rawOptions === "object") {
        const obj = rawOptions;
        if (Array.isArray(obj.options)) {
          obj.options.forEach((value) => pushOption(value));
        } else if (Array.isArray(obj.choices)) {
          obj.choices.forEach((value) => pushOption(value));
        } else if (Array.isArray(obj.items)) {
          obj.items.forEach((value) => pushOption(value));
        } else {
          Object.entries(obj).forEach(([key, value]) => {
            if (typeof value === "string" && value.trim()) {
              pushOption({ label: key, description: value.trim() });
              return;
            }
            if (value === true || value === false || typeof value === "number") {
              pushOption({ label: key, description: String(value) });
              return;
            }
            pushOption(value);
          });
        }
      } else if (typeof rawOptions === "string") {
        this.splitQuestionOptionString(rawOptions).forEach((value) => pushOption(value));
      }
      const deduped = [];
      const seen = /* @__PURE__ */ new Set();
      for (const option of collected) {
        const label = String(option.label || "").trim();
        if (!label || seen.has(label)) continue;
        seen.add(label);
        deduped.push({
          label,
          description: String(option.description || "").trim()
        });
      }
      return deduped;
    }
    function normalizeQuestionItem(rawItem, index) {
      const obj = rawItem && typeof rawItem === "object" ? rawItem : { question: String(rawItem || "") };
      const resolveMultiSelect = (value) => {
        const source = value && typeof value === "object" ? value : {};
        const selectionMode = String(
          source.selectionMode !== void 0 ? source.selectionMode : source.selection_mode !== void 0 ? source.selection_mode : source.type !== void 0 ? source.type : source.kind
        ).trim().toLowerCase();
        const maxSelectedRaw = Number(
          source.maxSelections !== void 0 ? source.maxSelections : source.maxSelection !== void 0 ? source.maxSelection : source.max_select !== void 0 ? source.max_select : source.max_selectable !== void 0 ? source.max_selectable : source.maxChoices !== void 0 ? source.maxChoices : source.max_choices !== void 0 ? source.max_choices : 0
        );
        const modeIsMulti = ["multiple", "multi", "multi-select", "multi_select", "checkbox", "checkboxes"].includes(selectionMode);
        const maxSelected = Number.isFinite(maxSelectedRaw) ? maxSelectedRaw : 0;
        return Boolean(
          source.multiSelect || source.multiple || source.allowMultiple || source.allow_multiple || source.multi_select || source.isMulti || source.is_multi || modeIsMulti || maxSelected > 1
        );
      };
      const question = typeof obj.question === "string" && obj.question.trim() || typeof obj.prompt === "string" && obj.prompt.trim() || typeof obj.ask === "string" && obj.ask.trim() || typeof obj.query === "string" && obj.query.trim() || typeof obj.content === "string" && obj.content.trim() || typeof obj.text === "string" && obj.text.trim() || typeof obj.title === "string" && obj.title.trim() || typeof obj.name === "string" && obj.name.trim() || "";
      if (!question) return null;
      const options = this.parseQuestionOptions(
        obj.options !== void 0 ? obj.options : obj.choices !== void 0 ? obj.choices : obj.items !== void 0 ? obj.items : obj.answers !== void 0 ? obj.answers : obj.selections !== void 0 ? obj.selections : obj.values !== void 0 ? obj.values : obj.select_options !== void 0 ? obj.select_options : obj.selectOptions !== void 0 ? obj.selectOptions : obj.candidates
      );
      return {
        question,
        header: typeof obj.header === "string" && obj.header.trim() || `Q${index + 1}`,
        options,
        multiSelect: resolveMultiSelect(obj)
      };
    }
    function normalizeQuestionInput(rawInput) {
      const parsedFromString = this.parseMaybeJsonObject(rawInput);
      const input = parsedFromString || rawInput;
      let rawQuestions = [];
      if (Array.isArray(input)) {
        rawQuestions = input;
      } else if (input && typeof input === "object") {
        if (Array.isArray(input.questions)) {
          rawQuestions = input.questions;
        } else if (typeof input.questions === "string") {
          const parsedQuestions = this.parseMaybeJsonObject(input.questions);
          if (Array.isArray(parsedQuestions)) rawQuestions = parsedQuestions;
        } else if (input.questions && typeof input.questions === "object") {
          rawQuestions = Object.values(input.questions);
        } else if (typeof input.question === "string" || typeof input.prompt === "string" || typeof input.text === "string") {
          rawQuestions = [input];
        }
      }
      const normalized = [];
      const seenQuestion = /* @__PURE__ */ new Set();
      for (let i = 0; i < rawQuestions.length; i += 1) {
        const item = this.normalizeQuestionItem(rawQuestions[i], i);
        if (!item) continue;
        const qKey = String(item.question || "").trim();
        if (!qKey || seenQuestion.has(qKey)) continue;
        seenQuestion.add(qKey);
        normalized.push(item);
      }
      return normalized;
    }
    function parseQuestionsFromDetailText(detailText) {
      const text = String(detailText || "").trim();
      if (!text) return [];
      const lines = text.split(/\r?\n/);
      const questions = [];
      let current = null;
      const pushCurrent = () => {
        if (!current || !String(current.question || "").trim()) return;
        current.options = this.parseQuestionOptions(current.options);
        questions.push(current);
        current = null;
      };
      for (const rawLine of lines) {
        const line = String(rawLine || "").trim();
        if (!line) continue;
        if (line.startsWith("\u95EE\u9898:")) {
          pushCurrent();
          current = {
            question: line.replace(/^问题:\s*/, "").trim(),
            header: `Q${questions.length + 1}`,
            options: [],
            multiSelect: false
          };
          continue;
        }
        if (line.startsWith("\u9009\u9879:")) {
          if (!current) continue;
          const optionText = line.replace(/^选项:\s*/, "").trim();
          current.options = this.splitQuestionOptionString(optionText);
        }
      }
      pushCurrent();
      return questions.filter((item) => item && String(item.question || "").trim());
    }
    function extractQuestionItemsFromBlock(block) {
      if (!block || typeof block !== "object") return [];
      const sources = [];
      if (block.toolInput !== void 0) sources.push(block.toolInput);
      if (block.raw && block.raw.state && block.raw.state.input !== void 0) sources.push(block.raw.state.input);
      if (block.raw && block.raw.input !== void 0) sources.push(block.raw.input);
      for (const source of sources) {
        const normalized = this.normalizeQuestionInput(source);
        if (normalized.length) return normalized;
      }
      if (typeof block.detail === "string" && block.detail.trim()) {
        const fromDetail = this.parseQuestionsFromDetailText(block.detail);
        if (fromDetail.length) return fromDetail;
      }
      return [];
    }
    function buildFallbackQuestionItemsFromRequest(request) {
      const req = request && typeof request === "object" ? request : {};
      let question = "";
      if (typeof req.question === "string" && req.question.trim()) {
        question = req.question.trim();
      } else if (typeof req.prompt === "string" && req.prompt.trim()) {
        question = req.prompt.trim();
      } else if (typeof req.title === "string" && req.title.trim()) {
        question = req.title.trim();
      }
      if (!question) {
        question = "FLOWnote \u9700\u8981\u4F60\u7684\u8F93\u5165\uFF0C\u8BF7\u586B\u5199\u540E\u63D0\u4EA4\u7EE7\u7EED\u3002";
      }
      return [
        {
          header: "Q1",
          question,
          options: [],
          multiSelect: false
        }
      ];
    }
    var parseMethods = {
      parseMaybeJsonObject,
      splitQuestionOptionString,
      normalizeQuestionOption,
      parseQuestionOptions,
      normalizeQuestionItem,
      normalizeQuestionInput,
      parseQuestionsFromDetailText,
      extractQuestionItemsFromBlock,
      buildFallbackQuestionItemsFromRequest
    };
    module2.exports = { parseMethods };
  }
});

// runtime/question-runtime.js
var require_question_runtime = __commonJS({
  "runtime/question-runtime.js"(exports2, module2) {
    function questionTextSignature(questions) {
      const list = Array.isArray(questions) ? questions : [];
      return list.map((item) => {
        if (!item || typeof item !== "object") return "";
        return String(item.question || "").trim().toLowerCase();
      }).filter(Boolean).join("||");
    }
    function questionRequestMapKey(sessionId, requestId) {
      const sid = String(sessionId || "").trim();
      const rid = String(requestId || "").trim();
      if (!sid || !rid) return "";
      return `${sid}::${rid}`;
    }
    function getQuestionRequestInteractionKey(sessionId, requestId) {
      const sid = String(sessionId || "").trim();
      const rid = String(requestId || "").trim();
      if (!sid || !rid) return "";
      return `${sid}::question-request::${rid}`;
    }
    function normalizeQuestionRequest(raw, normalizeQuestionInput) {
      if (!raw || typeof raw !== "object") return null;
      const obj = raw;
      const requestId = typeof obj.id === "string" && obj.id.trim() || typeof obj.requestID === "string" && obj.requestID.trim() || "";
      const sessionId = typeof obj.sessionID === "string" && obj.sessionID.trim() || typeof obj.sessionId === "string" && obj.sessionId.trim() || "";
      if (!requestId || !sessionId) return null;
      const parser = typeof normalizeQuestionInput === "function" ? normalizeQuestionInput : () => [];
      const questions = parser(obj.questions !== void 0 ? obj.questions : obj.input);
      const tool = obj.tool && typeof obj.tool === "object" ? obj.tool : {};
      return {
        id: requestId,
        sessionId,
        questions,
        questionSignature: questionTextSignature(questions),
        tool: {
          messageID: typeof tool.messageID === "string" ? tool.messageID : "",
          callID: typeof tool.callID === "string" ? tool.callID : ""
        },
        updatedAt: Date.now()
      };
    }
    function upsertPendingQuestionRequest(pendingQuestionRequests, raw, normalizeQuestionInput) {
      if (!(pendingQuestionRequests instanceof Map)) return null;
      const normalized = normalizeQuestionRequest(raw, normalizeQuestionInput);
      if (!normalized) return null;
      const key = questionRequestMapKey(normalized.sessionId, normalized.id);
      if (!key) return null;
      const previous = pendingQuestionRequests.get(key);
      pendingQuestionRequests.set(
        key,
        previous ? {
          ...previous,
          ...normalized,
          updatedAt: Date.now()
        } : normalized
      );
      return pendingQuestionRequests.get(key) || null;
    }
    function removePendingQuestionRequest(pendingQuestionRequests, sessionId, requestId) {
      if (!(pendingQuestionRequests instanceof Map)) return;
      const key = questionRequestMapKey(sessionId, requestId);
      if (!key) return;
      pendingQuestionRequests.delete(key);
    }
    function findPendingQuestionRequest(pendingQuestionRequests, interaction) {
      if (!(pendingQuestionRequests instanceof Map) || !pendingQuestionRequests.size) return null;
      const sessionId = String(interaction && interaction.sessionId || "").trim();
      if (!sessionId) return null;
      const pending = [];
      for (const request of pendingQuestionRequests.values()) {
        if (!request || request.sessionId !== sessionId) continue;
        pending.push(request);
      }
      if (!pending.length) return null;
      const interactionMessageId = String(
        interaction && interaction.message && (interaction.message.id || interaction.message.messageID || interaction.message.messageId) || ""
      ).trim();
      const interactionCallId = String(
        interaction && interaction.block && (interaction.block.id || interaction.block.raw && interaction.block.raw.id) || ""
      ).trim();
      const interactionSig = questionTextSignature(interaction && interaction.questions ? interaction.questions : []);
      pending.sort((a, b) => Number(b.updatedAt || 0) - Number(a.updatedAt || 0));
      const byToolStrict = pending.find((request) => {
        const tool = request.tool || {};
        if (!tool.callID || !interactionCallId) return false;
        if (tool.callID !== interactionCallId) return false;
        if (tool.messageID && interactionMessageId && tool.messageID !== interactionMessageId) return false;
        return true;
      });
      if (byToolStrict) return byToolStrict;
      const byToolMessage = pending.find((request) => {
        const tool = request.tool || {};
        return Boolean(tool.messageID && interactionMessageId && tool.messageID === interactionMessageId);
      });
      if (byToolMessage) return byToolMessage;
      const bySignature = pending.find((request) => request.questionSignature && request.questionSignature === interactionSig);
      if (bySignature) return bySignature;
      return pending[0] || null;
    }
    function tokenizeQuestionAnswer(rawAnswer) {
      const text = String(rawAnswer || "").trim();
      if (!text) return [];
      return text.split(/\s*,\s*/).map((item) => item.trim()).filter(Boolean);
    }
    function buildQuestionAnswerArrays(questions, result, state) {
      const list = Array.isArray(questions) ? questions : [];
      const resultMap = result && typeof result === "object" ? result : {};
      const stateAnswers = state && state.answers && typeof state.answers === "object" ? state.answers : {};
      return list.map((question, index) => {
        const q = question && typeof question === "object" ? question : {};
        const qText = String(q.question || "").trim();
        const optionLabels = new Set(
          (Array.isArray(q.options) ? q.options : []).map((opt) => {
            if (opt && typeof opt === "object") return String(opt.label || "").trim();
            return String(opt || "").trim();
          }).filter(Boolean)
        );
        const answerText = qText && typeof resultMap[qText] === "string" ? String(resultMap[qText] || "") : "";
        const tokens = tokenizeQuestionAnswer(answerText);
        const selected = [];
        const extras = [];
        for (const token of tokens) {
          if (optionLabels.has(token)) selected.push(token);
          else extras.push(token);
        }
        if (!tokens.length) {
          const stateAnswer = stateAnswers[index];
          if (stateAnswer && typeof stateAnswer === "object") {
            const base = typeof stateAnswer.value === "string" ? stateAnswer.value.trim() : "";
            const custom = typeof stateAnswer.custom === "string" ? stateAnswer.custom.trim() : "";
            if (base) selected.push(base);
            if (custom) extras.push(custom);
          }
        }
        const merged = [...selected, ...extras].map((item) => String(item || "").trim()).filter(Boolean);
        if (!merged.length && answerText.trim()) return [answerText.trim()];
        return merged;
      });
    }
    module2.exports = {
      buildQuestionAnswerArrays,
      findPendingQuestionRequest,
      getQuestionRequestInteractionKey,
      normalizeQuestionRequest,
      questionRequestMapKey,
      questionTextSignature,
      removePendingQuestionRequest,
      tokenizeQuestionAnswer,
      upsertPendingQuestionRequest
    };
  }
});

// runtime/view/question/request-state-methods.js
var require_request_state_methods = __commonJS({
  "runtime/view/question/request-state-methods.js"(exports2, module2) {
    var {
      findPendingQuestionRequest: findPendingQuestionRequestRuntime,
      getQuestionRequestInteractionKey: getQuestionRequestInteractionKeyRuntime,
      normalizeQuestionRequest: normalizeQuestionRequestRuntime,
      questionRequestMapKey: questionRequestMapKeyRuntime,
      questionTextSignature: questionTextSignatureRuntime,
      removePendingQuestionRequest: removePendingQuestionRequestRuntime,
      upsertPendingQuestionRequest: upsertPendingQuestionRequestRuntime
    } = require_question_runtime();
    function questionTextSignature(questions) {
      return questionTextSignatureRuntime(questions);
    }
    function questionRequestMapKey(sessionId, requestId) {
      return questionRequestMapKeyRuntime(sessionId, requestId);
    }
    function getQuestionRequestInteractionKey(sessionId, requestId) {
      return getQuestionRequestInteractionKeyRuntime(sessionId, requestId);
    }
    function normalizeQuestionRequest(raw) {
      return normalizeQuestionRequestRuntime(raw, (input) => this.normalizeQuestionInput(input));
    }
    function upsertPendingQuestionRequest(raw) {
      return upsertPendingQuestionRequestRuntime(
        this.pendingQuestionRequests,
        raw,
        (input) => this.normalizeQuestionInput(input)
      );
    }
    function removePendingQuestionRequest(sessionId, requestId) {
      removePendingQuestionRequestRuntime(this.pendingQuestionRequests, sessionId, requestId);
    }
    function findPendingQuestionRequest(interaction) {
      return findPendingQuestionRequestRuntime(this.pendingQuestionRequests, interaction);
    }
    function getQuestionAnswerState(key, totalQuestions) {
      const total = Math.max(1, Number(totalQuestions) || 1);
      if (!key) return { total, answers: {}, submitted: false, sending: false };
      const existing = this.questionAnswerStates.get(key);
      if (existing && Number(existing.total) === total) {
        return existing;
      }
      const next = { total, answers: {}, submitted: false, sending: false };
      this.questionAnswerStates.set(key, next);
      return next;
    }
    function pruneQuestionAnswerStates(messages) {
      if (!(this.questionAnswerStates instanceof Map) || !this.questionAnswerStates.size) return;
      const activeSessionId = String(this.plugin.sessionStore.state().activeSessionId || "").trim();
      const keepKeys = /* @__PURE__ */ new Set();
      if (activeSessionId && this.pendingQuestionRequests instanceof Map) {
        for (const request of this.pendingQuestionRequests.values()) {
          if (!request || request.sessionId !== activeSessionId) continue;
          const questions = Array.isArray(request.questions) && request.questions.length ? request.questions : this.buildFallbackQuestionItemsFromRequest(request);
          if (!questions.length) continue;
          const requestKey = this.getQuestionRequestInteractionKey(activeSessionId, request.id);
          if (requestKey) keepKeys.add(requestKey);
        }
      }
      for (const key of this.questionAnswerStates.keys()) {
        if (!keepKeys.has(key)) this.questionAnswerStates.delete(key);
      }
      if (this.questionSubmitAt instanceof Map) {
        for (const key of this.questionSubmitAt.keys()) {
          if (!keepKeys.has(key)) this.questionSubmitAt.delete(key);
        }
      }
    }
    async function refreshPendingQuestionRequests(options = {}) {
      if (!this.plugin || !this.plugin.opencodeClient || typeof this.plugin.opencodeClient.listQuestions !== "function") {
        return;
      }
      const activeSessionId = String(this.plugin.sessionStore.state().activeSessionId || "").trim();
      if (!activeSessionId) return;
      const now = Date.now();
      const minIntervalMs = Math.max(400, Number(options.minIntervalMs || 1400));
      const force = Boolean(options.force);
      const silent = Boolean(options.silent);
      if (!force && this.pendingQuestionRefreshInflight) return this.pendingQuestionRefreshInflight;
      if (!force && now - Number(this.pendingQuestionRefreshAt || 0) < minIntervalMs) return;
      const refreshPromise = (async () => {
        this.pendingQuestionRefreshAt = Date.now();
        const listed = await this.plugin.opencodeClient.listQuestions({
          signal: this.currentAbort ? this.currentAbort.signal : void 0
        });
        if (!Array.isArray(listed)) return;
        const keep = /* @__PURE__ */ new Set();
        for (const raw of listed) {
          const normalized = this.upsertPendingQuestionRequest(raw);
          if (!normalized || normalized.sessionId !== activeSessionId) continue;
          const requestKey = this.questionRequestMapKey(activeSessionId, normalized.id);
          if (requestKey) keep.add(requestKey);
        }
        if (this.pendingQuestionRequests instanceof Map) {
          for (const [mapKey, request] of this.pendingQuestionRequests.entries()) {
            if (!request || request.sessionId !== activeSessionId) continue;
            if (!keep.has(mapKey)) this.pendingQuestionRequests.delete(mapKey);
          }
        }
        if (!silent) {
          this.renderInlineQuestionPanel(this.plugin.sessionStore.getActiveMessages());
        }
      })();
      this.pendingQuestionRefreshInflight = refreshPromise;
      try {
        await refreshPromise;
      } finally {
        if (this.pendingQuestionRefreshInflight === refreshPromise) {
          this.pendingQuestionRefreshInflight = null;
        }
      }
    }
    var requestStateMethods = {
      questionTextSignature,
      questionRequestMapKey,
      getQuestionRequestInteractionKey,
      normalizeQuestionRequest,
      upsertPendingQuestionRequest,
      removePendingQuestionRequest,
      findPendingQuestionRequest,
      getQuestionAnswerState,
      pruneQuestionAnswerStates,
      refreshPendingQuestionRequests
    };
    module2.exports = { requestStateMethods };
  }
});

// runtime/view/question/submission-methods.js
var require_submission_methods = __commonJS({
  "runtime/view/question/submission-methods.js"(exports2, module2) {
    var {
      buildQuestionAnswerArrays: buildQuestionAnswerArraysRuntime,
      tokenizeQuestionAnswer: tokenizeQuestionAnswerRuntime
    } = require_question_runtime();
    var { tFromContext } = require_i18n_runtime();
    function buildQuestionAnswerPayload(questions, state) {
      const list = Array.isArray(questions) ? questions : [];
      const answers = state && state.answers ? state.answers : {};
      const lines = [];
      for (let index = 0; index < list.length; index += 1) {
        const question = list[index] && typeof list[index] === "object" ? list[index] : {};
        const answer = answers[index];
        if (!answer || typeof answer.value !== "string" || !answer.value.trim()) continue;
        const qid = typeof question.id === "string" && question.id.trim() || `question_${index + 1}`;
        const custom = typeof answer.custom === "string" ? answer.custom.trim() : "";
        const content = custom ? `${answer.value.trim()} | ${custom}` : answer.value.trim();
        lines.push(`${qid}: ${content}`);
      }
      return lines.join("\n");
    }
    function tokenizeQuestionAnswer(rawAnswer) {
      return tokenizeQuestionAnswerRuntime(rawAnswer);
    }
    function buildQuestionAnswerArrays(questions, result, state) {
      return buildQuestionAnswerArraysRuntime(questions, result, state);
    }
    async function submitQuestionAnswers(interactionKey, questions, state, directPayload = "", options = {}) {
      if (!state || state.submitted || state.sending) return;
      if (interactionKey) {
        const now = Date.now();
        const lastAt = Number(this.questionSubmitAt.get(interactionKey) || 0);
        if (now - lastAt < 1200) return;
        this.questionSubmitAt.set(interactionKey, now);
      }
      const payload = String(directPayload || "").trim() || this.buildQuestionAnswerPayload(questions, state);
      if (!payload.trim()) return;
      state.sending = true;
      state.submitted = true;
      this.renderMessages();
      try {
        const sessionId = typeof options.sessionId === "string" ? options.sessionId.trim() : "";
        let requestId = typeof options.requestId === "string" ? options.requestId.trim() : "";
        const providedAnswers = Array.isArray(options.questionAnswers) ? options.questionAnswers : null;
        if (!requestId && interactionKey && interactionKey.includes("::question-request::")) {
          const parsedId = interactionKey.split("::question-request::")[1];
          if (parsedId) requestId = String(parsedId).trim();
        }
        if (!requestId) {
          const pending = this.findPendingQuestionRequest({
            key: interactionKey,
            sessionId,
            questions,
            message: options.message || null,
            block: options.block || null
          });
          if (pending && pending.id) requestId = pending.id;
        }
        if (!requestId) {
          const listed = await this.plugin.opencodeClient.listQuestions({ signal: this.currentAbort ? this.currentAbort.signal : void 0 });
          if (Array.isArray(listed)) {
            for (const req of listed) this.upsertPendingQuestionRequest(req);
          }
          const refreshed = this.findPendingQuestionRequest({
            key: interactionKey,
            sessionId,
            questions,
            message: options.message || null,
            block: options.block || null
          });
          if (refreshed && refreshed.id) requestId = refreshed.id;
        }
        if (!requestId) {
          throw new Error(tFromContext(this, "view.question.requestMissing", "No pending question request ID found"));
        }
        const answers = providedAnswers && providedAnswers.length ? providedAnswers : this.buildQuestionAnswerArrays(questions, null, state);
        await this.plugin.opencodeClient.replyQuestion({
          requestId,
          sessionId,
          answers,
          signal: this.currentAbort ? this.currentAbort.signal : void 0
        });
        this.removePendingQuestionRequest(sessionId, requestId);
        this.setRuntimeStatus(tFromContext(this, "view.question.submitted", "Answers submitted, waiting for model to continue..."), "info");
      } catch (e) {
        state.submitted = false;
        const msg = e instanceof Error ? e.message : String(e);
        this.setRuntimeStatus(tFromContext(this, "view.question.submitFailed", "Failed to submit answers: {message}", { message: msg }), "error");
      } finally {
        state.sending = false;
        if (interactionKey) this.questionAnswerStates.set(interactionKey, state);
        this.renderMessages();
      }
    }
    var submissionMethods = {
      buildQuestionAnswerPayload,
      tokenizeQuestionAnswer,
      buildQuestionAnswerArrays,
      submitQuestionAnswers
    };
    module2.exports = { submissionMethods };
  }
});

// runtime/inline-ask-user-question-panel.js
var require_inline_ask_user_question_panel = __commonJS({
  "runtime/inline-ask-user-question-panel.js"(exports2, module2) {
    var HINTS_TEXT = "Enter to select \xB7 Tab/Arrow keys to navigate \xB7 Esc to cancel";
    var HINTS_TEXT_IMMEDIATE = "Enter to select \xB7 Arrow keys to navigate \xB7 Esc to cancel";
    var InlineAskUserQuestionPanel = class {
      constructor(containerEl, input, resolve, signal, config) {
        this.containerEl = containerEl;
        this.input = input || {};
        this.resolveCallback = typeof resolve === "function" ? resolve : () => {
        };
        this.signal = signal;
        this.resolved = false;
        this.config = {
          title: config && config.title || "Claude has a question",
          headerEl: config && config.headerEl ? config.headerEl : null,
          showCustomInput: config && typeof config.showCustomInput === "boolean" ? config.showCustomInput : true,
          immediateSelect: config && typeof config.immediateSelect === "boolean" ? config.immediateSelect : false
        };
        this.questions = [];
        this.answers = /* @__PURE__ */ new Map();
        this.customInputs = /* @__PURE__ */ new Map();
        this.activeTabIndex = 0;
        this.focusedItemIndex = 0;
        this.isInputFocused = false;
        this.rootEl = null;
        this.tabBar = null;
        this.contentArea = null;
        this.tabElements = [];
        this.currentItems = [];
        this.abortHandler = null;
        this.boundKeyDown = this.handleKeyDown.bind(this);
      }
      render() {
        this.rootEl = this.containerEl.createDiv({ cls: "claudian-ask-question-inline" });
        this.rootEl.toggleClass("is-immediate", Boolean(this.config.immediateSelect));
        const titleEl = this.rootEl.createDiv({ cls: "claudian-ask-inline-title" });
        titleEl.setText(this.config.title);
        if (this.config.headerEl) {
          this.rootEl.appendChild(this.config.headerEl);
        }
        this.questions = this.parseQuestions();
        if (!this.questions.length) {
          this.handleResolve(null);
          return;
        }
        if (this.config.immediateSelect && (this.questions.length !== 1 || !Array.isArray(this.questions[0].options) || !this.questions[0].options.length)) {
          this.config.immediateSelect = false;
        }
        for (let i = 0; i < this.questions.length; i += 1) {
          this.answers.set(i, /* @__PURE__ */ new Set());
          this.customInputs.set(i, "");
        }
        if (!this.config.immediateSelect) {
          this.tabBar = this.rootEl.createDiv({ cls: "claudian-ask-tab-bar" });
          this.renderTabBar();
        }
        this.contentArea = this.rootEl.createDiv({ cls: "claudian-ask-content" });
        this.renderTabContent();
        this.rootEl.setAttribute("tabindex", "0");
        this.rootEl.addEventListener("keydown", this.boundKeyDown);
        requestAnimationFrame(() => {
          if (!this.rootEl) return;
          this.rootEl.focus();
          this.rootEl.scrollIntoView({ block: "nearest", behavior: "smooth" });
        });
        if (this.signal) {
          this.abortHandler = () => this.handleResolve(null);
          this.signal.addEventListener("abort", this.abortHandler, { once: true });
        }
      }
      destroy(silent = false) {
        if (silent) {
          if (this.resolved) return;
          this.resolved = true;
          if (this.rootEl) this.rootEl.removeEventListener("keydown", this.boundKeyDown);
          if (this.signal && this.abortHandler) {
            this.signal.removeEventListener("abort", this.abortHandler);
            this.abortHandler = null;
          }
          if (this.rootEl) this.rootEl.remove();
          return;
        }
        this.handleResolve(null);
      }
      parseQuestions() {
        const raw = this.input && Array.isArray(this.input.questions) ? this.input.questions : [];
        return raw.filter((q) => q && typeof q === "object" && typeof q.question === "string").map((q, idx) => ({
          question: String(q.question || ""),
          header: typeof q.header === "string" ? q.header.slice(0, 12) : `Q${idx + 1}`,
          options: this.deduplicateOptions((Array.isArray(q.options) ? q.options : []).map((o) => this.coerceOption(o))),
          multiSelect: Boolean(q.multiSelect)
        })).filter((q) => String(q.question || "").trim().length > 0);
      }
      coerceOption(opt) {
        if (opt && typeof opt === "object") {
          const obj = opt;
          const label = this.extractLabel(obj);
          const description = typeof obj.description === "string" ? obj.description : "";
          return { label, description };
        }
        return { label: typeof opt === "string" ? opt : String(opt), description: "" };
      }
      deduplicateOptions(options) {
        const seen = /* @__PURE__ */ new Set();
        return options.filter((o) => {
          if (!o || typeof o.label !== "string") return false;
          const label = o.label.trim();
          if (!label) return false;
          if (seen.has(label)) return false;
          seen.add(label);
          o.label = label;
          return true;
        });
      }
      extractLabel(obj) {
        if (typeof obj.label === "string") return obj.label;
        if (typeof obj.value === "string") return obj.value;
        if (typeof obj.text === "string") return obj.text;
        if (typeof obj.name === "string") return obj.name;
        return String(obj);
      }
      renderTabBar() {
        if (!this.tabBar) return;
        this.tabBar.empty();
        this.tabElements = [];
        for (let idx = 0; idx < this.questions.length; idx += 1) {
          const answered = this.isQuestionAnswered(idx);
          const tab = this.tabBar.createSpan({ cls: "claudian-ask-tab" });
          tab.createSpan({ text: this.questions[idx].header, cls: "claudian-ask-tab-label" });
          tab.createSpan({ text: answered ? " \u2713" : "", cls: "claudian-ask-tab-tick" });
          tab.setAttribute("title", this.questions[idx].question);
          if (idx === this.activeTabIndex) tab.addClass("is-active");
          if (answered) tab.addClass("is-answered");
          tab.addEventListener("click", () => this.switchTab(idx));
          this.tabElements.push(tab);
        }
        const allAnswered = this.questions.every((_, i) => this.isQuestionAnswered(i));
        const submitTab = this.tabBar.createSpan({ cls: "claudian-ask-tab" });
        submitTab.createSpan({ text: allAnswered ? "\u2713 " : "", cls: "claudian-ask-tab-submit-check" });
        submitTab.createSpan({ text: "Submit", cls: "claudian-ask-tab-label" });
        if (this.activeTabIndex === this.questions.length) submitTab.addClass("is-active");
        submitTab.addEventListener("click", () => this.switchTab(this.questions.length));
        this.tabElements.push(submitTab);
      }
      isQuestionAnswered(idx) {
        return this.answers.get(idx).size > 0 || this.customInputs.get(idx).trim().length > 0;
      }
      switchTab(index) {
        const clamped = Math.max(0, Math.min(index, this.questions.length));
        if (clamped === this.activeTabIndex) return;
        this.activeTabIndex = clamped;
        this.focusedItemIndex = 0;
        this.isInputFocused = false;
        if (!this.config.immediateSelect) this.renderTabBar();
        this.renderTabContent();
        if (this.rootEl) this.rootEl.focus();
      }
      renderTabContent() {
        if (!this.contentArea) return;
        this.contentArea.empty();
        this.currentItems = [];
        if (this.activeTabIndex < this.questions.length) {
          this.renderQuestionTab(this.activeTabIndex);
        } else {
          this.renderSubmitTab();
        }
      }
      renderQuestionTab(idx) {
        const q = this.questions[idx];
        const isMulti = q.multiSelect;
        const selected = this.answers.get(idx);
        this.contentArea.createDiv({
          text: q.question,
          cls: "claudian-ask-question-text"
        });
        const listEl = this.contentArea.createDiv({ cls: "claudian-ask-list" });
        for (let optIdx = 0; optIdx < q.options.length; optIdx += 1) {
          const option = q.options[optIdx];
          const isFocused = optIdx === this.focusedItemIndex;
          const isSelected = selected.has(option.label);
          const row = listEl.createDiv({ cls: "claudian-ask-item" });
          if (isFocused) row.addClass("is-focused");
          if (isSelected) row.addClass("is-selected");
          row.createSpan({ text: isFocused ? "\u203A" : "\xA0", cls: "claudian-ask-cursor" });
          row.createSpan({ text: `${optIdx + 1}. `, cls: "claudian-ask-item-num" });
          if (isMulti) this.renderMultiSelectCheckbox(row, isSelected);
          const labelBlock = row.createDiv({ cls: "claudian-ask-item-content" });
          const labelRow = labelBlock.createDiv({ cls: "claudian-ask-label-row" });
          labelRow.createSpan({ text: option.label, cls: "claudian-ask-item-label" });
          if (!isMulti && isSelected) {
            labelRow.createSpan({ text: " \u2713", cls: "claudian-ask-check-mark" });
          }
          if (option.description) {
            labelBlock.createDiv({ text: option.description, cls: "claudian-ask-item-desc" });
          }
          row.addEventListener("click", () => {
            this.focusedItemIndex = optIdx;
            this.updateFocusIndicator();
            this.selectOption(idx, option.label);
          });
          this.currentItems.push(row);
        }
        if (this.config.showCustomInput) {
          const customIdx = q.options.length;
          const customFocused = customIdx === this.focusedItemIndex;
          const customText = this.customInputs.get(idx) || "";
          const hasCustomText = customText.trim().length > 0;
          const customRow = listEl.createDiv({ cls: "claudian-ask-item claudian-ask-custom-item" });
          if (customFocused) customRow.addClass("is-focused");
          customRow.createSpan({ text: customFocused ? "\u203A" : "\xA0", cls: "claudian-ask-cursor" });
          customRow.createSpan({ text: `${customIdx + 1}. `, cls: "claudian-ask-item-num" });
          if (isMulti) this.renderMultiSelectCheckbox(customRow, hasCustomText);
          const inputEl = customRow.createEl("input", {
            type: "text",
            cls: "claudian-ask-custom-text",
            placeholder: "Type something.",
            value: customText
          });
          inputEl.addEventListener("input", () => {
            this.customInputs.set(idx, inputEl.value);
            if (!isMulti && inputEl.value.trim()) {
              selected.clear();
              this.updateOptionVisuals(idx);
            }
            this.updateTabIndicators();
          });
          inputEl.addEventListener("focus", () => {
            this.isInputFocused = true;
          });
          inputEl.addEventListener("blur", () => {
            this.isInputFocused = false;
          });
          this.currentItems.push(customRow);
        }
        this.contentArea.createDiv({
          text: this.config.immediateSelect ? HINTS_TEXT_IMMEDIATE : HINTS_TEXT,
          cls: "claudian-ask-hints"
        });
      }
      renderSubmitTab() {
        this.contentArea.createDiv({
          text: "Review your answers",
          cls: "claudian-ask-review-title"
        });
        const reviewEl = this.contentArea.createDiv({ cls: "claudian-ask-review" });
        for (let idx = 0; idx < this.questions.length; idx += 1) {
          const q = this.questions[idx];
          const answerText = this.getAnswerText(idx);
          const pairEl = reviewEl.createDiv({ cls: "claudian-ask-review-pair" });
          pairEl.createDiv({ text: `${idx + 1}.`, cls: "claudian-ask-review-num" });
          const bodyEl = pairEl.createDiv({ cls: "claudian-ask-review-body" });
          bodyEl.createDiv({ text: q.question, cls: "claudian-ask-review-q-text" });
          bodyEl.createDiv({
            text: answerText || "Not answered",
            cls: answerText ? "claudian-ask-review-a-text" : "claudian-ask-review-empty"
          });
          pairEl.addEventListener("click", () => this.switchTab(idx));
        }
        this.contentArea.createDiv({
          text: "Ready to submit your answers?",
          cls: "claudian-ask-review-prompt"
        });
        const actionsEl = this.contentArea.createDiv({ cls: "claudian-ask-list" });
        const allAnswered = this.questions.every((_, i) => this.isQuestionAnswered(i));
        const submitRow = actionsEl.createDiv({ cls: "claudian-ask-item" });
        if (this.focusedItemIndex === 0) submitRow.addClass("is-focused");
        if (!allAnswered) submitRow.addClass("is-disabled");
        submitRow.createSpan({ text: this.focusedItemIndex === 0 ? "\u203A" : "\xA0", cls: "claudian-ask-cursor" });
        submitRow.createSpan({ text: "1. ", cls: "claudian-ask-item-num" });
        submitRow.createSpan({ text: "Submit answers", cls: "claudian-ask-item-label" });
        submitRow.addEventListener("click", () => {
          this.focusedItemIndex = 0;
          this.updateFocusIndicator();
          this.handleSubmit();
        });
        this.currentItems.push(submitRow);
        const cancelRow = actionsEl.createDiv({ cls: "claudian-ask-item" });
        if (this.focusedItemIndex === 1) cancelRow.addClass("is-focused");
        cancelRow.createSpan({ text: this.focusedItemIndex === 1 ? "\u203A" : "\xA0", cls: "claudian-ask-cursor" });
        cancelRow.createSpan({ text: "2. ", cls: "claudian-ask-item-num" });
        cancelRow.createSpan({ text: "Cancel", cls: "claudian-ask-item-label" });
        cancelRow.addEventListener("click", () => {
          this.focusedItemIndex = 1;
          this.handleResolve(null);
        });
        this.currentItems.push(cancelRow);
        this.contentArea.createDiv({
          text: HINTS_TEXT,
          cls: "claudian-ask-hints"
        });
      }
      getAnswerText(idx) {
        const selected = this.answers.get(idx);
        const custom = this.customInputs.get(idx);
        const parts = [];
        if (selected.size > 0) parts.push([...selected].join(", "));
        if (custom.trim()) parts.push(custom.trim());
        return parts.join(", ");
      }
      selectOption(qIdx, label) {
        const q = this.questions[qIdx];
        const selected = this.answers.get(qIdx);
        const isMulti = q.multiSelect;
        if (isMulti) {
          if (selected.has(label)) selected.delete(label);
          else selected.add(label);
        } else {
          selected.clear();
          selected.add(label);
          this.customInputs.set(qIdx, "");
        }
        this.updateOptionVisuals(qIdx);
        if (this.config.immediateSelect && !isMulti) {
          const result = {};
          result[q.question] = label;
          this.handleResolve(result);
          return;
        }
        this.updateTabIndicators();
        if (!isMulti) this.switchTab(this.activeTabIndex + 1);
      }
      renderMultiSelectCheckbox(parent, checked) {
        parent.createSpan({
          text: checked ? "[\u2713] " : "[ ] ",
          cls: `claudian-ask-check${checked ? " is-checked" : ""}`
        });
      }
      updateOptionVisuals(qIdx) {
        const q = this.questions[qIdx];
        const selected = this.answers.get(qIdx);
        const isMulti = q.multiSelect;
        for (let i = 0; i < q.options.length; i += 1) {
          const item = this.currentItems[i];
          if (!item) continue;
          const isSelected = selected.has(q.options[i].label);
          item.toggleClass("is-selected", isSelected);
          if (isMulti) {
            const checkSpan = item.querySelector(".claudian-ask-check");
            if (checkSpan) {
              checkSpan.textContent = isSelected ? "[\u2713] " : "[ ] ";
              checkSpan.toggleClass("is-checked", isSelected);
            }
          } else {
            const labelRow = item.querySelector(".claudian-ask-label-row");
            const existingMark = item.querySelector(".claudian-ask-check-mark");
            if (isSelected && !existingMark && labelRow) {
              labelRow.createSpan({ text: " \u2713", cls: "claudian-ask-check-mark" });
            } else if (!isSelected && existingMark) {
              existingMark.remove();
            }
          }
        }
      }
      updateFocusIndicator() {
        for (let i = 0; i < this.currentItems.length; i += 1) {
          const item = this.currentItems[i];
          const cursor = item.querySelector(".claudian-ask-cursor");
          if (i === this.focusedItemIndex) {
            item.addClass("is-focused");
            if (cursor) cursor.textContent = "\u203A";
            item.scrollIntoView({ block: "nearest" });
            if (item.classList && item.classList.contains("claudian-ask-custom-item")) {
              const input = item.querySelector(".claudian-ask-custom-text");
              if (input) {
                input.focus();
                this.isInputFocused = true;
              }
            }
          } else {
            item.removeClass("is-focused");
            if (cursor) cursor.textContent = "\xA0";
            if (item.classList && item.classList.contains("claudian-ask-custom-item")) {
              const input = item.querySelector(".claudian-ask-custom-text");
              if (input && document.activeElement === input) {
                input.blur();
                this.isInputFocused = false;
              }
            }
          }
        }
      }
      updateTabIndicators() {
        for (let idx = 0; idx < this.questions.length; idx += 1) {
          const tab = this.tabElements[idx];
          if (!tab) continue;
          const tick = tab.querySelector(".claudian-ask-tab-tick");
          const answered = this.isQuestionAnswered(idx);
          tab.toggleClass("is-answered", answered);
          if (tick) tick.textContent = answered ? " \u2713" : "";
        }
        const submitTab = this.tabElements[this.questions.length];
        if (submitTab) {
          const submitCheck = submitTab.querySelector(".claudian-ask-tab-submit-check");
          const allAnswered = this.questions.every((_, i) => this.isQuestionAnswered(i));
          if (submitCheck) submitCheck.textContent = allAnswered ? "\u2713 " : "";
        }
      }
      handleNavigationKey(e, maxFocusIndex) {
        switch (e.key) {
          case "ArrowDown":
            e.preventDefault();
            e.stopPropagation();
            this.focusedItemIndex = Math.min(this.focusedItemIndex + 1, maxFocusIndex);
            this.updateFocusIndicator();
            return true;
          case "ArrowUp":
            e.preventDefault();
            e.stopPropagation();
            this.focusedItemIndex = Math.max(this.focusedItemIndex - 1, 0);
            this.updateFocusIndicator();
            return true;
          case "ArrowLeft":
            if (this.config.immediateSelect) return false;
            e.preventDefault();
            e.stopPropagation();
            this.switchTab(this.activeTabIndex - 1);
            return true;
          case "Tab":
            if (this.config.immediateSelect) return false;
            e.preventDefault();
            e.stopPropagation();
            if (e.shiftKey) this.switchTab(this.activeTabIndex - 1);
            else this.switchTab(this.activeTabIndex + 1);
            return true;
          case "Escape":
            e.preventDefault();
            e.stopPropagation();
            this.handleResolve(null);
            return true;
          default:
            return false;
        }
      }
      handleKeyDown(e) {
        if (this.isInputFocused) {
          if (e.key === "Escape") {
            e.preventDefault();
            e.stopPropagation();
            this.isInputFocused = false;
            if (document.activeElement) document.activeElement.blur();
            if (this.rootEl) this.rootEl.focus();
            return;
          }
          if (e.key === "Tab" || e.key === "Enter") {
            e.preventDefault();
            e.stopPropagation();
            this.isInputFocused = false;
            if (document.activeElement) document.activeElement.blur();
            if (e.key === "Tab" && e.shiftKey) this.switchTab(this.activeTabIndex - 1);
            else this.switchTab(this.activeTabIndex + 1);
            return;
          }
          return;
        }
        if (this.config.immediateSelect) {
          const q2 = this.questions[this.activeTabIndex];
          const maxIdx = q2.options.length - 1;
          if (this.handleNavigationKey(e, maxIdx)) return;
          if (e.key === "Enter") {
            e.preventDefault();
            e.stopPropagation();
            if (this.focusedItemIndex <= maxIdx) {
              this.selectOption(this.activeTabIndex, q2.options[this.focusedItemIndex].label);
            }
          }
          return;
        }
        const isSubmitTab = this.activeTabIndex === this.questions.length;
        const q = this.questions[this.activeTabIndex];
        const maxFocusIndex = isSubmitTab ? 1 : this.config.showCustomInput ? q.options.length : q.options.length - 1;
        if (this.handleNavigationKey(e, maxFocusIndex)) return;
        if (isSubmitTab) {
          if (e.key === "Enter") {
            e.preventDefault();
            e.stopPropagation();
            if (this.focusedItemIndex === 0) this.handleSubmit();
            else this.handleResolve(null);
          }
          return;
        }
        if (e.key === "ArrowRight") {
          e.preventDefault();
          e.stopPropagation();
          this.switchTab(this.activeTabIndex + 1);
          return;
        }
        if (e.key === "Enter") {
          e.preventDefault();
          e.stopPropagation();
          if (this.focusedItemIndex < q.options.length) {
            this.selectOption(this.activeTabIndex, q.options[this.focusedItemIndex].label);
          } else if (this.config.showCustomInput) {
            this.isInputFocused = true;
            const input = this.contentArea.querySelector(".claudian-ask-custom-text");
            if (input) input.focus();
          }
        }
      }
      handleSubmit() {
        const allAnswered = this.questions.every((_, i) => this.isQuestionAnswered(i));
        if (!allAnswered) return;
        const result = {};
        for (let i = 0; i < this.questions.length; i += 1) {
          result[this.questions[i].question] = this.getAnswerText(i);
        }
        this.handleResolve(result);
      }
      handleResolve(result) {
        if (this.resolved) return;
        this.resolved = true;
        if (this.rootEl) this.rootEl.removeEventListener("keydown", this.boundKeyDown);
        if (this.signal && this.abortHandler) {
          this.signal.removeEventListener("abort", this.abortHandler);
          this.abortHandler = null;
        }
        if (this.rootEl) this.rootEl.remove();
        this.resolveCallback(result);
      }
    };
    module2.exports = { InlineAskUserQuestionPanel };
  }
});

// runtime/view/question/inline-panel-methods.js
var require_inline_panel_methods = __commonJS({
  "runtime/view/question/inline-panel-methods.js"(exports2, module2) {
    var { InlineAskUserQuestionPanel } = require_inline_ask_user_question_panel();
    var { tFromContext } = require_i18n_runtime();
    function clearInlineQuestionWidget(silent = true) {
      if (this.inlineQuestionWidget && typeof this.inlineQuestionWidget.destroy === "function") {
        this.inlineQuestionWidget.destroy(silent);
      }
      this.inlineQuestionWidget = null;
      this.inlineQuestionKey = "";
      if (this.elements.inlineQuestionHost) {
        this.elements.inlineQuestionHost.removeClass("is-active");
        this.elements.inlineQuestionHost.empty();
      }
      if (this.elements.composer) {
        this.elements.composer.removeClass("is-inline-hidden");
      }
    }
    function formatInlineQuestionPayload(questions, result) {
      const list = Array.isArray(questions) ? questions : [];
      const answerMap = result && typeof result === "object" ? result : {};
      const lines = [];
      for (const question of list) {
        if (!question || typeof question !== "object") continue;
        const qText = typeof question.question === "string" ? question.question.trim() : "";
        if (!qText) continue;
        const answer = typeof answerMap[qText] === "string" ? answerMap[qText].trim() : "";
        if (!answer) continue;
        if (list.length === 1) lines.push(answer);
        else lines.push(`${qText}: ${answer}`);
      }
      if (!lines.length) {
        for (const raw of Object.values(answerMap)) {
          const answer = typeof raw === "string" ? raw.trim() : "";
          if (answer) lines.push(answer);
        }
      }
      return lines.join("\n");
    }
    async function submitInlineQuestionResult(interaction, result) {
      if (!interaction || !interaction.key) return;
      const state = interaction.state || this.getQuestionAnswerState(interaction.key, interaction.questions.length);
      if (state.submitted || state.sending) return;
      const payload = this.formatInlineQuestionPayload(interaction.questions, result);
      if (!String(payload || "").trim()) return;
      const questionAnswers = this.buildQuestionAnswerArrays(interaction.questions, result, state);
      await this.submitQuestionAnswers(interaction.key, interaction.questions, state, payload, {
        sessionId: interaction.sessionId,
        requestId: interaction.requestId || "",
        questionAnswers,
        message: interaction.message || null,
        block: interaction.block || null
      });
    }
    function extractQuestionRequestIdFromBlock(block) {
      const source = block && typeof block === "object" ? block : {};
      const raw = source.raw && typeof source.raw === "object" ? source.raw : {};
      const state = raw.state && typeof raw.state === "object" ? raw.state : {};
      const request = state.request && typeof state.request === "object" ? state.request : {};
      const candidates = [
        source.requestID,
        source.requestId,
        raw.requestID,
        raw.requestId,
        raw.id,
        state.requestID,
        state.requestId,
        request.id,
        request.requestID,
        request.requestId
      ];
      for (const value of candidates) {
        const normalized = String(value || "").trim();
        if (normalized) return normalized;
      }
      return "";
    }
    function findQuestionInteractionFromLatestAssistantBlock(messages, activeSessionId) {
      const list = Array.isArray(messages) ? messages : [];
      const hasActiveRequest = Boolean(this.currentAbort && this.currentAbort.signal && !this.currentAbort.signal.aborted);
      const now = Date.now();
      for (let messageIndex = list.length - 1; messageIndex >= 0; messageIndex -= 1) {
        const message = list[messageIndex];
        if (!message || message.role !== "assistant") continue;
        const messageCreatedAt = Number(message.createdAt || 0);
        const isRecent = messageCreatedAt > 0 ? now - messageCreatedAt <= 18e4 : false;
        if (!message.pending && !hasActiveRequest && !isRecent) break;
        const blocks = Array.isArray(message.blocks) ? message.blocks : [];
        for (let blockIndex = blocks.length - 1; blockIndex >= 0; blockIndex -= 1) {
          const block = blocks[blockIndex];
          if (!block || typeof block !== "object") continue;
          if (String(block.type || "").trim().toLowerCase() !== "tool") continue;
          if (String(block.tool || "").trim().toLowerCase() !== "question") continue;
          const status = String(block.status || "").trim().toLowerCase();
          if (status === "error") continue;
          const questions = this.extractQuestionItemsFromBlock(block);
          if (!questions.length) continue;
          const requestId = this.extractQuestionRequestIdFromBlock(block);
          const fallbackKey = `${activeSessionId}::question-block::${String(message.id || messageIndex)}::${String(block.id || blockIndex)}`;
          const key = requestId ? this.getQuestionRequestInteractionKey(activeSessionId, requestId) : fallbackKey;
          const state = this.getQuestionAnswerState(key, questions.length);
          if (state.submitted || state.sending) continue;
          return {
            key,
            sessionId: activeSessionId,
            message,
            block,
            questions,
            state,
            requestId
          };
        }
      }
      return null;
    }
    function findActiveQuestionInteraction(messages) {
      const activeSessionId = String(this.plugin.sessionStore.state().activeSessionId || "").trim();
      if (!activeSessionId) return null;
      if (!(this.pendingQuestionRequests instanceof Map)) {
        return this.findQuestionInteractionFromLatestAssistantBlock(messages, activeSessionId);
      }
      const pending = [];
      for (const request of this.pendingQuestionRequests.values()) {
        if (!request || request.sessionId !== activeSessionId) continue;
        pending.push(request);
      }
      if (!pending.length) return null;
      pending.sort((a, b) => Number(b.updatedAt || 0) - Number(a.updatedAt || 0));
      for (const request of pending) {
        const questions = Array.isArray(request.questions) && request.questions.length ? request.questions : this.buildFallbackQuestionItemsFromRequest(request);
        if (!questions.length) continue;
        const key = this.getQuestionRequestInteractionKey(activeSessionId, request.id);
        if (!key) continue;
        const state = this.getQuestionAnswerState(key, questions.length);
        if (state.submitted) continue;
        return {
          key,
          sessionId: activeSessionId,
          message: null,
          block: null,
          questions,
          state,
          requestId: request.id
        };
      }
      const fallback = this.findQuestionInteractionFromLatestAssistantBlock(messages, activeSessionId);
      if (fallback && typeof this.refreshPendingQuestionRequests === "function" && Date.now() - Number(this.pendingQuestionFallbackRefreshAt || 0) >= 300) {
        this.pendingQuestionFallbackRefreshAt = Date.now();
        void this.refreshPendingQuestionRequests({
          minIntervalMs: 300,
          silent: true
        }).catch(() => {
        });
      }
      return fallback;
    }
    function renderInlineQuestionPanel(messages) {
      if (!this.elements.inlineQuestionHost || !this.elements.composer) return;
      if (this.inlineQuestionWidget && this.inlineQuestionWidget.rootEl && !this.inlineQuestionWidget.rootEl.isConnected) {
        this.inlineQuestionWidget = null;
        this.inlineQuestionKey = "";
      }
      const interaction = this.findActiveQuestionInteraction(messages);
      if (!interaction) {
        this.clearInlineQuestionWidget(true);
        return;
      }
      this.elements.composer.addClass("is-inline-hidden");
      this.elements.inlineQuestionHost.addClass("is-active");
      if (this.inlineQuestionWidget && this.inlineQuestionKey === interaction.key) {
        return;
      }
      this.clearInlineQuestionWidget(true);
      this.inlineQuestionKey = interaction.key;
      this.inlineQuestionWidget = new InlineAskUserQuestionPanel(
        this.elements.inlineQuestionHost,
        { questions: interaction.questions },
        (result) => {
          if (!result) {
            this.clearInlineQuestionWidget(true);
            this.setRuntimeStatus(tFromContext(this, "view.question.canceled", "Question response canceled"), "info");
            return;
          }
          void this.submitInlineQuestionResult(interaction, result);
        },
        this.currentAbort ? this.currentAbort.signal : void 0,
        {
          title: tFromContext(this, "view.question.title", "FLOWnote has a question"),
          showCustomInput: true,
          immediateSelect: interaction.questions.length === 1 && Array.isArray(interaction.questions[0].options) && interaction.questions[0].options.length > 0 && !Boolean(interaction.questions[0].multiSelect)
        }
      );
      this.inlineQuestionWidget.render();
    }
    function prefillComposerInput(text, options = {}) {
      const inputEl = this.elements.input;
      if (!inputEl) return;
      const content = String(text || "").trim();
      if (!content) return;
      const current = String(inputEl.value || "");
      inputEl.value = current && !current.endsWith("\n") ? `${current}
${content}` : `${current}${content}`;
      inputEl.focus();
      if (options.sendNow) {
        void this.handleSend();
      }
    }
    function hasVisibleQuestionToolCard() {
      const messages = this.plugin.sessionStore.getActiveMessages();
      return Boolean(this.findActiveQuestionInteraction(messages));
    }
    var inlinePanelMethods = {
      clearInlineQuestionWidget,
      formatInlineQuestionPayload,
      submitInlineQuestionResult,
      extractQuestionRequestIdFromBlock,
      findQuestionInteractionFromLatestAssistantBlock,
      findActiveQuestionInteraction,
      renderInlineQuestionPanel,
      prefillComposerInput,
      hasVisibleQuestionToolCard
    };
    module2.exports = { inlinePanelMethods };
  }
});

// runtime/view/question-flow.js
var require_question_flow = __commonJS({
  "runtime/view/question-flow.js"(exports2, module2) {
    var { parseMethods } = require_parse_methods();
    var { requestStateMethods } = require_request_state_methods();
    var { submissionMethods } = require_submission_methods();
    var { inlinePanelMethods } = require_inline_panel_methods();
    var questionFlowMethods = {
      ...parseMethods,
      ...requestStateMethods,
      ...submissionMethods,
      ...inlinePanelMethods
    };
    module2.exports = { questionFlowMethods };
  }
});

// runtime/view/runtime-status.js
var require_runtime_status = __commonJS({
  "runtime/view/runtime-status.js"(exports2, module2) {
    var { tFromContext } = require_i18n_runtime();
    function runtimeStatusFromBlocks(rawBlocks) {
      const blocks = this.visibleAssistantBlocks(rawBlocks);
      const reasoningBlocks = blocks.filter((block) => block && String(block.type || "").trim().toLowerCase() === "reasoning");
      const reasoningRunning = reasoningBlocks.some((block) => {
        const status = this.normalizeBlockStatus(block && block.status);
        return status === "running" || status === "pending";
      });
      if (reasoningRunning) {
        return { tone: "working", text: tFromContext(this, "view.runtime.reasoning", "Model is reasoning...") };
      }
      const tools = blocks.filter((block) => block && String(block.type || "").trim().toLowerCase() === "tool");
      if (!tools.length) {
        if (reasoningBlocks.length) {
          return { tone: "info", text: tFromContext(this, "view.runtime.reasoningDone", "Reasoning complete, preparing response...") };
        }
        return null;
      }
      const names = [...new Set(tools.map((block) => this.toolDisplayName(block)).filter(Boolean))];
      const shortNames = names.slice(0, 3).join(", ");
      const suffix = names.length > 3 ? "\u2026" : "";
      const statusText = shortNames || tFromContext(this, "view.runtime.tool", "Tools");
      const running = tools.some((block) => {
        const status = this.normalizeBlockStatus(block && block.status);
        return status === "running" || status === "pending";
      });
      if (running) {
        return {
          tone: "working",
          text: tFromContext(this, "view.runtime.toolRunning", "Running: {statusText}{suffix}", { statusText, suffix })
        };
      }
      const failed = tools.some((block) => this.normalizeBlockStatus(block && block.status) === "error");
      if (failed) {
        return {
          tone: "error",
          text: tFromContext(this, "view.runtime.toolFailed", "Tool failed: {statusText}{suffix}", { statusText, suffix })
        };
      }
      return { tone: "info", text: tFromContext(this, "view.runtime.toolDone", "Tool finished, preparing response...") };
    }
    function syncRuntimeStatusToPendingTail() {
      const container = this.elements.messages;
      if (!container) return;
      const rows = container.querySelectorAll(".oc-message-assistant.is-pending");
      if (!rows || !rows.length) return;
      const row = rows[rows.length - 1];
      if (!row) return;
      const body = row.querySelector(".oc-message-content");
      if (!body) return;
      const messageId = String(row.dataset && row.dataset.messageId || "").trim();
      const draft = messageId ? this.plugin.sessionStore.getActiveMessages().find((msg) => msg && msg.id === messageId) : null;
      const draftText = draft && typeof draft.text === "string" ? draft.text.trim() : "";
      body.removeClass("oc-runtime-tail", "is-info", "is-working", "is-error");
      if (draftText) return;
      const statusText = String(this.runtimeStatusState && this.runtimeStatusState.text || "").trim();
      body.setText(statusText);
      if (!statusText) return;
      body.addClass("oc-runtime-tail");
      const tone = String(this.runtimeStatusState && this.runtimeStatusState.tone || "info");
      if (tone === "error") body.addClass("is-error");
      else if (tone === "working") body.addClass("is-working");
      else body.addClass("is-info");
    }
    function setRuntimeStatus(text, tone = "info") {
      const normalizedText = String(text || "").trim();
      const normalizedTone = tone === "error" || tone === "working" ? tone : "info";
      this.runtimeStatusState = { text: normalizedText, tone: normalizedTone };
      this.syncRuntimeStatusToPendingTail();
    }
    module2.exports = { runtimeStatusMethods: {
      runtimeStatusFromBlocks,
      syncRuntimeStatusToPendingTail,
      setRuntimeStatus
    } };
  }
});

// runtime/flownote-view.js
var require_flownote_view = __commonJS({
  "runtime/flownote-view.js"(exports2, module2) {
    var {
      ItemView,
      Notice: Notice2
    } = require("obsidian");
    var {
      PermissionRequestModal,
      PromptAppendModal
    } = require_modals();
    var {
      stringifyForDisplay
    } = require_assistant_payload_utils();
    var { runSendPrompt } = require_chat_orchestrator();
    var { tFromContext } = require_i18n_runtime();
    var { commandRouterMethods } = require_command_router();
    var { layoutRendererMethods } = require_layout_renderer();
    var { messageRendererMethods } = require_message_renderer();
    var { questionFlowMethods } = require_question_flow();
    var { runtimeStatusMethods } = require_runtime_status();
    var VIEW_TYPE = "flownote-view";
    var FLOWNOTE_ICON_ID2 = "flownote-journal-glow";
    function uid(prefix) {
      return `${prefix}-${Date.now()}-${Math.random().toString(16).slice(2, 8)}`;
    }
    var FLOWnoteAssistantView = class extends ItemView {
      constructor(leaf, plugin) {
        super(leaf);
        this.plugin = plugin;
        this.root = null;
        this.elements = {};
        this.currentAbort = null;
        this.selectedModel = "";
        this.isSidebarCollapsed = false;
        this.questionAnswerStates = /* @__PURE__ */ new Map();
        this.questionSubmitAt = /* @__PURE__ */ new Map();
        this.pendingQuestionRequests = /* @__PURE__ */ new Map();
        this.inlineQuestionWidget = null;
        this.inlineQuestionKey = "";
        this.silentAbortBudget = 0;
        this.runtimeStatusState = { text: "", tone: "info" };
        this.autoScrollEnabled = true;
        this.messagesScrollEl = null;
        this.messagesScrollHandler = null;
        this.messagesIntentHandler = null;
        this.messagesKeyDownHandler = null;
        this.pendingScrollRaf = 0;
        this.ignoreMessageScrollEventsUntil = 0;
        this.forceBottomUntil = 0;
        this.lastManualScrollIntentAt = 0;
        this.linkedContextFiles = [];
        this.patchDiffCache = /* @__PURE__ */ new Map();
        this.patchDiffInflight = /* @__PURE__ */ new Map();
      }
      getViewType() {
        return VIEW_TYPE;
      }
      getDisplayText() {
        return "FLOWnote";
      }
      getIcon() {
        return FLOWNOTE_ICON_ID2;
      }
      async onOpen() {
        this.selectedModel = this.plugin.settings.defaultModel || "";
        this.render();
      }
      onClose() {
        this.clearInlineQuestionWidget(true);
        if (typeof this.closeLinkedContextFilePicker === "function") {
          this.closeLinkedContextFilePicker();
        }
        this.unbindMessagesScrollTracking();
        this.forceBottomUntil = 0;
        this.lastManualScrollIntentAt = 0;
        if (this.currentAbort) {
          this.currentAbort.abort();
          this.currentAbort = null;
        }
      }
      appendAssistantMessage(sessionId, text, error = "") {
        this.plugin.sessionStore.appendMessage(sessionId, {
          id: uid("msg"),
          role: "assistant",
          text: String(text || ""),
          error: String(error || ""),
          pending: false,
          createdAt: Date.now()
        });
      }
      showPermissionRequestModal(permission) {
        return new Promise((resolve) => {
          const modal = new PermissionRequestModal(
            this.app,
            permission,
            (answer) => resolve(answer || null),
            stringifyForDisplay,
            typeof this.plugin.t === "function" ? this.plugin.t.bind(this.plugin) : null
          );
          modal.open();
        });
      }
      showPromptAppendModal(appendText) {
        const modal = new PromptAppendModal(this.app, appendText, (value) => {
          this.prefillComposerInput(value);
        }, typeof this.plugin.t === "function" ? this.plugin.t.bind(this.plugin) : null);
        modal.open();
      }
      handleToastEvent(toast) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        const title = typeof toast.title === "string" ? toast.title.trim() : "";
        const message = typeof toast.message === "string" ? toast.message.trim() : "";
        const text = [title, message].filter(Boolean).join(": ") || t("view.toastFallback", "FLOWnote \u63D0\u793A");
        new Notice2(text, 4e3);
      }
      isAbortLikeError(message) {
        const text = String(message || "").toLowerCase();
        return /abort|aborted|cancelled|canceled|用户取消/.test(text);
      }
      async handleSend() {
        const input = this.elements.input;
        if (!input) return;
        const text = String(input.value || "").trim();
        if (!text) return;
        input.value = "";
        await this.sendPrompt(text);
      }
      async sendPrompt(userText, options = {}) {
        return runSendPrompt(this, userText, options);
      }
      cancelSending() {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        if (this.currentAbort) {
          this.currentAbort.abort();
          this.currentAbort = null;
          this.setBusy(false);
          new Notice2(t("view.sendCanceled", "\u5DF2\u53D6\u6D88\u53D1\u9001"));
        }
      }
      setBusy(isBusy) {
        if (this.elements.sendBtn) this.elements.sendBtn.disabled = isBusy;
        if (this.elements.cancelBtn) this.elements.cancelBtn.disabled = !isBusy;
        if (this.elements.attachFileBtn) this.elements.attachFileBtn.disabled = isBusy;
        if (this.elements.modelSelect) this.elements.modelSelect.disabled = isBusy;
        if (this.elements.input) this.elements.input.disabled = isBusy;
        if (isBusy && typeof this.closeLinkedContextFilePicker === "function") {
          this.closeLinkedContextFilePicker();
        }
        if (this.root) {
          this.root.toggleClass("is-busy", isBusy);
        }
        if (!isBusy) {
          this.setRuntimeStatus("", "info");
        }
      }
    };
    Object.assign(
      FLOWnoteAssistantView.prototype,
      commandRouterMethods,
      layoutRendererMethods,
      messageRendererMethods,
      questionFlowMethods,
      runtimeStatusMethods
    );
    module2.exports = {
      VIEW_TYPE,
      FLOWnoteAssistantView
    };
  }
});

// runtime/flownote-client.js
var require_flownote_client = __commonJS({
  "runtime/flownote-client.js"(exports2, module2) {
    var { rt } = (() => {
      try {
        return require_runtime_locale_state();
      } catch (_e) {
        return {
          rt: (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
            const value = params[k];
            return value === void 0 || value === null ? "" : String(value);
          })
        };
      }
    })();
    var FLOWnoteClient = class {
      constructor(options) {
        if (!options || typeof options.SdkTransport !== "function") {
          throw new Error(rt(
            "FLOWnoteClient \u9700\u8981\u6CE8\u5165 SdkTransport \u6784\u9020\u5668",
            "FLOWnoteClient requires injected SdkTransport constructor"
          ));
        }
        this.SdkTransport = options.SdkTransport;
        this.settings = options.settings;
        this.logger = options.logger || (() => {
        });
        this.sdk = new this.SdkTransport(options);
        this.lastMode = "sdk";
        this.lastError = "";
      }
      updateSettings(settings) {
        this.settings = settings;
        this.sdk.updateSettings(settings);
      }
      primary() {
        return this.sdk;
      }
      async withTransport(actionName, fn) {
        const primary = this.primary();
        const primaryMode = "sdk";
        try {
          const out = await fn(primary);
          this.logger(`action=${actionName} mode=${primaryMode} ok`);
          this.lastMode = primaryMode;
          this.lastError = "";
          return out;
        } catch (e1) {
          this.logger(`action=${actionName} mode=${primaryMode} err=${e1 instanceof Error ? e1.message : String(e1)}`);
          this.lastError = e1 instanceof Error ? e1.message : String(e1);
          throw e1;
        }
      }
      testConnection() {
        return this.withTransport("testConnection", (t) => t.testConnection());
      }
      listSessions() {
        return this.withTransport("listSessions", (t) => t.listSessions());
      }
      listSessionMessages(options = {}) {
        return this.withTransport("listSessionMessages", (t) => t.listSessionMessages(options));
      }
      getSessionDiff(options = {}) {
        return this.withTransport("getSessionDiff", (t) => t.getSessionDiff(options));
      }
      createSession(title) {
        return this.withTransport("createSession", (t) => t.createSession(title));
      }
      listModels() {
        return this.withTransport("listModels", (t) => t.listModels());
      }
      setDefaultModel(options) {
        return this.withTransport("setDefaultModel", (t) => t.setDefaultModel(options));
      }
      sendMessage(options) {
        return this.withTransport("sendMessage", (t) => t.sendMessage(options));
      }
      listQuestions(options = {}) {
        return this.withTransport("listQuestions", (t) => t.listQuestions(options));
      }
      replyQuestion(options) {
        return this.withTransport("replyQuestion", (t) => t.replyQuestion(options));
      }
      replyPermission(options) {
        return this.withTransport("replyPermission", (t) => t.replyPermission(options));
      }
      listProviders() {
        return this.withTransport("listProviders", (t) => t.listProviders());
      }
      listProviderAuthMethods() {
        return this.withTransport("listProviderAuthMethods", (t) => t.listProviderAuthMethods());
      }
      authorizeProviderOauth(options) {
        return this.withTransport("authorizeProviderOauth", (t) => t.authorizeProviderOauth(options));
      }
      completeProviderOauth(options) {
        return this.withTransport("completeProviderOauth", (t) => t.completeProviderOauth(options));
      }
      setProviderApiKeyAuth(options) {
        return this.withTransport("setProviderApiKeyAuth", (t) => t.setProviderApiKeyAuth(options));
      }
      clearProviderAuth(options) {
        return this.withTransport("clearProviderAuth", (t) => t.clearProviderAuth(options));
      }
      async stop() {
        await this.sdk.stop();
      }
    };
    module2.exports = { FLOWnoteClient };
  }
});

// runtime/http-utils.js
var require_http_utils = __commonJS({
  "runtime/http-utils.js"(exports2, module2) {
    var http = require("http");
    var https = require("https");
    var { rt } = (() => {
      try {
        return require_runtime_locale_state();
      } catch (_e) {
        return {
          rt: (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
            const value = params[k];
            return value === void 0 || value === null ? "" : String(value);
          })
        };
      }
    })();
    function nodeHttpRequestJson(url, method, body, timeoutMs, signal, options = {}) {
      const parsed = new URL(url);
      const isHttps = parsed.protocol === "https:";
      const client = isHttps ? https : http;
      const trace = options && typeof options.trace === "function" ? options.trace : null;
      const payload = body === void 0 ? void 0 : JSON.stringify(body);
      const headers = {
        accept: "application/json, text/plain, */*"
      };
      if (payload !== void 0) {
        headers["content-type"] = "application/json";
        headers["content-length"] = Buffer.byteLength(payload);
      }
      return new Promise((resolve, reject) => {
        let settled = false;
        const finish = (fn, value) => {
          if (settled) return;
          settled = true;
          if (signal && onAbort) signal.removeEventListener("abort", onAbort);
          fn(value);
        };
        let onAbort = null;
        if (trace) trace(`HTTP ${method} ${parsed.pathname}${parsed.search}`);
        const req = client.request(
          {
            protocol: parsed.protocol,
            hostname: parsed.hostname,
            port: parsed.port || (isHttps ? 443 : 80),
            path: `${parsed.pathname}${parsed.search}`,
            method,
            headers
          },
          (res) => {
            const chunks = [];
            res.on("data", (chunk) => chunks.push(chunk));
            res.on("end", () => {
              const text = Buffer.concat(chunks).toString("utf8");
              if (trace) trace(`HTTP ${method} ${parsed.pathname} -> ${res.statusCode}`);
              finish(resolve, {
                status: Number(res.statusCode || 0),
                text
              });
            });
          }
        );
        req.on("error", (err) => finish(reject, err));
        req.setTimeout(timeoutMs, () => {
          req.destroy(new Error(rt("\u8BF7\u6C42\u8D85\u65F6 ({timeoutMs}ms)", "Request timed out ({timeoutMs}ms)", { timeoutMs })));
        });
        if (signal) {
          onAbort = () => req.destroy(new Error(rt("\u7528\u6237\u53D6\u6D88\u4E86\u8BF7\u6C42", "Request was cancelled by user")));
          if (signal.aborted) {
            req.destroy(new Error(rt("\u7528\u6237\u53D6\u6D88\u4E86\u8BF7\u6C42", "Request was cancelled by user")));
          } else {
            signal.addEventListener("abort", onAbort, { once: true });
          }
        }
        if (payload !== void 0) req.write(payload);
        req.end();
      });
    }
    function nodeHttpRequestSse(url, timeoutMs, signal, handlers, options = {}) {
      const parsed = new URL(url);
      const isHttps = parsed.protocol === "https:";
      const client = isHttps ? https : http;
      const maxBufferChars = 2 * 1024 * 1024;
      const trace = options && typeof options.trace === "function" ? options.trace : null;
      return new Promise((resolve, reject) => {
        let settled = false;
        let buffer = "";
        const finish = (fn, value) => {
          if (settled) return;
          settled = true;
          if (signal && onAbort) signal.removeEventListener("abort", onAbort);
          fn(value);
        };
        const consumeSseChunk = (chunk) => {
          const textChunk = String(chunk || "").replace(/\r/g, "");
          if (!textChunk) return;
          buffer += textChunk;
          if (buffer.length > maxBufferChars) {
            finish(reject, new Error(rt(
              "SSE \u7F13\u51B2\u533A\u8D85\u9650 ({maxBufferChars})",
              "SSE buffer exceeded limit ({maxBufferChars})",
              { maxBufferChars }
            )));
            req.destroy();
            return;
          }
          let splitIndex = buffer.indexOf("\n\n");
          while (splitIndex >= 0) {
            const block = buffer.slice(0, splitIndex);
            buffer = buffer.slice(splitIndex + 2);
            splitIndex = buffer.indexOf("\n\n");
            if (!block.trim()) continue;
            const lines = block.split("\n");
            const dataLines = [];
            let eventName = "";
            let eventId = "";
            let retry = "";
            for (const line of lines) {
              if (line.startsWith("data:")) {
                dataLines.push(line.replace(/^data:\s*/, ""));
              } else if (line.startsWith("event:")) {
                eventName = line.replace(/^event:\s*/, "");
              } else if (line.startsWith("id:")) {
                eventId = line.replace(/^id:\s*/, "");
              } else if (line.startsWith("retry:")) {
                retry = line.replace(/^retry:\s*/, "");
              }
            }
            if (!dataLines.length) continue;
            const rawData = dataLines.join("\n");
            let payload = rawData;
            try {
              payload = JSON.parse(rawData);
            } catch {
              payload = rawData;
            }
            if (handlers && typeof handlers.onEvent === "function") {
              handlers.onEvent(payload, { event: eventName, id: eventId, retry });
            }
            if (handlers && typeof handlers.shouldStop === "function" && handlers.shouldStop()) {
              finish(resolve);
              req.destroy();
              return;
            }
          }
        };
        let onAbort = null;
        const req = client.request(
          {
            protocol: parsed.protocol,
            hostname: parsed.hostname,
            port: parsed.port || (isHttps ? 443 : 80),
            path: `${parsed.pathname}${parsed.search}`,
            method: "GET",
            headers: {
              accept: "text/event-stream"
            }
          },
          (res) => {
            const status = Number(res.statusCode || 0);
            if (status < 200 || status >= 300) {
              const chunks = [];
              res.on("data", (chunk) => chunks.push(chunk));
              res.on("end", () => {
                const body = Buffer.concat(chunks).toString("utf8");
                finish(reject, new Error(rt(
                  "SSE \u8BF7\u6C42\u5931\u8D25 ({status}): {body}",
                  "SSE request failed ({status}): {body}",
                  { status, body }
                )));
              });
              return;
            }
            if (trace) trace(`HTTP GET ${parsed.pathname} -> ${status} (sse)`);
            res.on("data", consumeSseChunk);
            res.on("end", () => finish(resolve));
            res.on("error", (err) => finish(reject, err));
          }
        );
        req.on("error", (err) => finish(reject, err));
        req.setTimeout(timeoutMs, () => req.destroy(new Error(rt(
          "SSE \u8D85\u65F6 ({timeoutMs}ms)",
          "SSE timed out ({timeoutMs}ms)",
          { timeoutMs }
        ))));
        if (signal) {
          onAbort = () => req.destroy(new Error(rt("\u7528\u6237\u53D6\u6D88\u4E86\u8BF7\u6C42", "Request was cancelled by user")));
          if (signal.aborted) {
            req.destroy(new Error(rt("\u7528\u6237\u53D6\u6D88\u4E86\u8BF7\u6C42", "Request was cancelled by user")));
          } else {
            signal.addEventListener("abort", onAbort, { once: true });
          }
        }
        req.end();
      });
    }
    function createLinkedAbortController(signal) {
      const controller = new AbortController();
      let off = () => {
      };
      if (signal) {
        if (signal.aborted) {
          controller.abort();
        } else {
          const onAbort = () => controller.abort();
          signal.addEventListener("abort", onAbort, { once: true });
          off = () => signal.removeEventListener("abort", onAbort);
        }
      }
      return { controller, detach: off };
    }
    module2.exports = {
      nodeHttpRequestJson,
      nodeHttpRequestSse,
      createLinkedAbortController
    };
  }
});

// runtime/transports/shared/command-utils.js
var require_command_utils = __commonJS({
  "runtime/transports/shared/command-utils.js"(exports2, module2) {
    function parseModel(defaultModel) {
      const model = String(defaultModel || "").trim();
      if (!model.includes("/")) return void 0;
      const [providerID, modelID] = model.split("/");
      if (!providerID || !modelID) return void 0;
      return { providerID, modelID };
    }
    function parseCommandModel(defaultModel) {
      const model = String(defaultModel || "").trim();
      if (!model.includes("/")) return void 0;
      return model;
    }
    function normalizeSlashCommandName(commandName) {
      const normalized = String(commandName || "").trim().replace(/^\//, "").toLowerCase();
      if (normalized === "modle") return "model";
      return normalized;
    }
    function availableCommandSet(list) {
      const set = /* @__PURE__ */ new Set();
      for (const item of list || []) {
        const name = String(item && item.name ? item.name : "").replace(/^\//, "").trim().toLowerCase();
        if (name) set.add(name);
      }
      return set;
    }
    function resolveCommandFromSet(commandName, names) {
      const normalized = normalizeSlashCommandName(commandName);
      if (!normalized) return { use: false, command: "" };
      const set = names instanceof Set ? names : /* @__PURE__ */ new Set();
      if (!set.size) {
        return { use: true, command: normalized };
      }
      if (set.has(normalized)) {
        return { use: true, command: normalized };
      }
      if (normalized === "model" && set.has("models")) return { use: true, command: "models" };
      if (normalized === "models" && set.has("model")) return { use: true, command: "model" };
      return { use: false, command: normalized };
    }
    function parseSlashCommand(prompt) {
      const text = String(prompt || "").trim();
      if (!text.startsWith("/")) return null;
      if (text.length <= 1) return null;
      const withoutSlash = text.slice(1).trim();
      if (!withoutSlash) return null;
      const firstSpace = withoutSlash.indexOf(" ");
      if (firstSpace < 0) {
        return { command: withoutSlash, arguments: "" };
      }
      return {
        command: withoutSlash.slice(0, firstSpace).trim(),
        arguments: withoutSlash.slice(firstSpace + 1).trim()
      };
    }
    function normalizeTimestampMs(value) {
      const raw = Number(value || 0);
      if (!Number.isFinite(raw) || raw <= 0) return 0;
      if (raw >= 1e14) return Math.floor(raw / 1e3);
      if (raw >= 1e12) return Math.floor(raw);
      if (raw >= 1e9) return Math.floor(raw * 1e3);
      return Math.floor(raw);
    }
    function messageInfo(item) {
      if (!item || typeof item !== "object") return null;
      if (item.info && typeof item.info === "object") return item.info;
      if (item.message && typeof item.message === "object") {
        if (item.message.info && typeof item.message.info === "object") return item.message.info;
        return item.message;
      }
      return item;
    }
    function messageRole(item) {
      const info = messageInfo(item);
      if (!info || typeof info !== "object") return "";
      if (typeof info.role === "string" && info.role.trim()) return info.role.trim().toLowerCase();
      if (typeof info.type === "string" && info.type.trim()) return info.type.trim().toLowerCase();
      return "";
    }
    function messageHasError(item) {
      const info = messageInfo(item);
      if (!info || typeof info !== "object") return false;
      if (typeof info.error === "string" && info.error.trim()) return true;
      if (info.error && typeof info.error === "object") return true;
      return false;
    }
    function messageCreatedAt(item) {
      const info = messageInfo(item);
      if (!info || typeof info !== "object") return 0;
      const time = info.time && typeof info.time === "object" ? info.time : {};
      const created = normalizeTimestampMs(
        time.created || time.updated || info.created || info.updated || 0
      );
      return created;
    }
    function findLatestAssistantMessage(messages, startedAt) {
      const list = Array.isArray(messages) ? messages : [];
      const candidates = list.filter((item) => messageRole(item) === "assistant").filter((item) => {
        const created = messageCreatedAt(item);
        return !startedAt || created === 0 || created >= startedAt - 1e3;
      }).sort((a, b) => {
        const ta = messageCreatedAt(a);
        const tb = messageCreatedAt(b);
        return tb - ta;
      });
      if (candidates.length) return candidates[0];
      const errorCandidates = list.filter((item) => messageHasError(item)).sort((a, b) => {
        const ta = messageCreatedAt(a);
        const tb = messageCreatedAt(b);
        return tb - ta;
      });
      return errorCandidates[0] || null;
    }
    module2.exports = {
      parseModel,
      parseCommandModel,
      normalizeSlashCommandName,
      availableCommandSet,
      resolveCommandFromSet,
      parseSlashCommand,
      findLatestAssistantMessage
    };
  }
});

// runtime/transports/shared/completion-signals.js
var require_completion_signals = __commonJS({
  "runtime/transports/shared/completion-signals.js"(exports2, module2) {
    function isObject(value) {
      return Boolean(value) && typeof value === "object";
    }
    function extractNestedStatusObject(status) {
      if (!isObject(status)) return null;
      if (isObject(status.status)) return status.status;
      if (isObject(status.state)) return status.state;
      return null;
    }
    function extractSessionStatusType(status) {
      if (!isObject(status)) return "";
      if (typeof status.type === "string" && status.type.trim()) return status.type.trim().toLowerCase();
      if (typeof status.status === "string" && status.status.trim()) return status.status.trim().toLowerCase();
      if (typeof status.state === "string" && status.state.trim()) return status.state.trim().toLowerCase();
      const nested = extractNestedStatusObject(status);
      if (nested && typeof nested.type === "string" && nested.type.trim()) return nested.type.trim().toLowerCase();
      if (nested && typeof nested.status === "string" && nested.status.trim()) return nested.status.trim().toLowerCase();
      if (nested && typeof nested.state === "string" && nested.state.trim()) return nested.state.trim().toLowerCase();
      return "";
    }
    function extractSessionStatusHint(status) {
      if (!isObject(status)) return "";
      const nested = extractNestedStatusObject(status);
      const text = [
        extractSessionStatusType(status),
        typeof status.message === "string" ? status.message : "",
        typeof status.error === "string" ? status.error : "",
        typeof status.reason === "string" ? status.reason : "",
        nested && typeof nested.message === "string" ? nested.message : "",
        nested && typeof nested.error === "string" ? nested.error : "",
        nested && typeof nested.reason === "string" ? nested.reason : ""
      ].filter(Boolean).join(" ");
      return String(text || "").trim();
    }
    function isSessionStatusActive(status) {
      const type = extractSessionStatusType(status);
      if (type && ["busy", "retry", "running", "queued", "in_progress", "in-progress", "processing"].includes(type)) {
        return true;
      }
      const hint = extractSessionStatusHint(status);
      if (!hint) return false;
      return /(busy|retry|running|queued|in[ -]?progress|processing|thinking|generating|waiting)/i.test(hint);
    }
    function isSessionStatusTerminal(status) {
      const type = extractSessionStatusType(status);
      if (type && [
        "idle",
        "done",
        "completed",
        "complete",
        "failed",
        "error",
        "aborted",
        "cancelled",
        "timeout",
        "stopped"
      ].includes(type)) {
        return true;
      }
      const hint = extractSessionStatusHint(status);
      if (!hint) return false;
      return /(idle|done|completed|complete|failed|error|aborted|cancelled|timeout|stopped|no pending)/i.test(hint);
    }
    function isAssistantMessageCompletedInfo(info) {
      if (!isObject(info)) return false;
      const completedAt = isObject(info.time) ? Number(info.time.completed || 0) : 0;
      if (completedAt > 0) return true;
      return String(info.finish || "").trim().length > 0;
    }
    function isAssistantMessageEnvelopeCompleted(envelope) {
      const info = isObject(envelope) && isObject(envelope.info) ? envelope.info : null;
      if (!info) return false;
      const role = String(info.role || "").trim().toLowerCase();
      if (role && role !== "assistant") return false;
      return isAssistantMessageCompletedInfo(info);
    }
    function extractSessionIdFromEventProperties(properties) {
      const props = isObject(properties) ? properties : {};
      if (typeof props.sessionID === "string" && props.sessionID.trim()) return props.sessionID.trim();
      if (typeof props.sessionId === "string" && props.sessionId.trim()) return props.sessionId.trim();
      if (typeof props.id === "string" && props.id.trim()) return props.id.trim();
      const info = isObject(props.info) ? props.info : null;
      if (!info) return "";
      if (typeof info.sessionID === "string" && info.sessionID.trim()) return info.sessionID.trim();
      if (typeof info.sessionId === "string" && info.sessionId.trim()) return info.sessionId.trim();
      if (typeof info.id === "string" && info.id.trim()) return info.id.trim();
      return "";
    }
    function extractSessionStatusFromEventProperties(properties) {
      const props = isObject(properties) ? properties : {};
      if (isObject(props.status) || typeof props.status === "string") return props.status;
      const info = isObject(props.info) ? props.info : null;
      if (!info) return null;
      if (isObject(info.status) || typeof info.status === "string") return info.status;
      if (isObject(info.state) || typeof info.state === "string") return info.state;
      return null;
    }
    module2.exports = {
      extractSessionStatusType,
      extractSessionStatusHint,
      isSessionStatusActive,
      isSessionStatusTerminal,
      isAssistantMessageCompletedInfo,
      isAssistantMessageEnvelopeCompleted,
      extractSessionIdFromEventProperties,
      extractSessionStatusFromEventProperties
    };
  }
});

// runtime/transports/shared/event-reducer.js
var require_event_reducer = __commonJS({
  "runtime/transports/shared/event-reducer.js"(exports2, module2) {
    var {
      toPartBlock,
      blocksFingerprint,
      extractErrorText,
      mergeSnapshotText
    } = require_assistant_payload_utils();
    var {
      isAssistantMessageCompletedInfo,
      extractSessionIdFromEventProperties,
      extractSessionStatusFromEventProperties,
      extractSessionStatusType
    } = require_completion_signals();
    var { rt } = (() => {
      try {
        return require_runtime_locale_state();
      } catch (_e) {
        return {
          rt: (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
            const value = params[k];
            return value === void 0 || value === null ? "" : String(value);
          })
        };
      }
    })();
    function normalizeTimestampMs(value) {
      const raw = Number(value || 0);
      if (!Number.isFinite(raw) || raw <= 0) return 0;
      if (raw >= 1e14) return Math.floor(raw / 1e3);
      if (raw >= 1e12) return Math.floor(raw);
      if (raw >= 1e9) return Math.floor(raw * 1e3);
      return Math.floor(raw);
    }
    function parseSkillInfoFromInstructionText(text) {
      const raw = String(text || "");
      if (!raw.includes("<skill-instruction>")) return null;
      const baseDirMatch = raw.match(/Base directory for this skill:\s*([^\n\r]+)/i);
      const baseDirectory = baseDirMatch && baseDirMatch[1] ? String(baseDirMatch[1]).trim() : "";
      if (!baseDirectory) return null;
      const normalizedPath = baseDirectory.replace(/\\/g, "/");
      const skillMatch = normalizedPath.match(/\/skills\/([^/]+)\/?/i);
      const skillId = skillMatch && skillMatch[1] ? String(skillMatch[1]).trim() : "";
      if (!skillId) return null;
      return { skillId, baseDirectory };
    }
    function createTransportEventReducer(options) {
      const cfg = options && typeof options === "object" ? options : {};
      const sessionId = String(cfg.sessionId || "").trim();
      const startedAt = Number(cfg.startedAt || 0);
      const permissionEventTypes = new Set(
        Array.isArray(cfg.permissionEventTypes) && cfg.permissionEventTypes.length ? cfg.permissionEventTypes.map((item) => String(item || "").trim()).filter(Boolean) : ["permission.asked"]
      );
      const textByPart = /* @__PURE__ */ new Map();
      const reasoningByPart = /* @__PURE__ */ new Map();
      const blockPartById = /* @__PURE__ */ new Map();
      const partKindById = /* @__PURE__ */ new Map();
      const partAliasToCanonicalId = /* @__PURE__ */ new Map();
      const promptedPermissionIds = /* @__PURE__ */ new Set();
      const promptedQuestionIds = /* @__PURE__ */ new Set();
      const activeQuestionIds = /* @__PURE__ */ new Set();
      const userTextByPart = /* @__PURE__ */ new Map();
      const stickyBlockIds = /* @__PURE__ */ new Set();
      let messageId = "";
      let activeMessageCreatedAt = 0;
      let latestUserMessageId = "";
      let text = "";
      let reasoning = "";
      let meta = "";
      let blocks = [];
      let blocksKey = blocksFingerprint(blocks);
      let done = false;
      let completionSeen = false;
      let idleSeen = false;
      let timelineTextSegment = 0;
      let activeTimelineTextPartId = "";
      const call = (fn, ...args) => {
        if (typeof fn !== "function") return;
        fn(...args);
      };
      const overlapSuffixPrefix = (leftText, rightText, maxWindow = 2048) => {
        const left = String(leftText || "");
        const right = String(rightText || "");
        const max = Math.min(left.length, right.length, Math.max(16, Number(maxWindow) || 2048));
        for (let len = max; len >= 1; len -= 1) {
          if (left.slice(left.length - len) === right.slice(0, len)) return len;
        }
        return 0;
      };
      const mergeDeltaText = (existingText, deltaText) => {
        const existing = String(existingText || "");
        const incoming = String(deltaText || "");
        if (!incoming) return existing;
        if (!existing) return incoming;
        if (existing === incoming) return existing;
        if (incoming.startsWith(existing)) return incoming;
        if (existing.endsWith(incoming)) return existing;
        const overlap = overlapSuffixPrefix(existing, incoming, 2048);
        if (overlap > 0) return `${existing}${incoming.slice(overlap)}`;
        return `${existing}${incoming}`;
      };
      const resetTimelineTextState = () => {
        timelineTextSegment = 0;
        activeTimelineTextPartId = "";
      };
      const closeTimelineTextPart = () => {
        activeTimelineTextPartId = "";
      };
      const ensureTimelineTextPartId = () => {
        if (!messageId) return "";
        if (activeTimelineTextPartId && blockPartById.has(activeTimelineTextPartId)) {
          return activeTimelineTextPartId;
        }
        timelineTextSegment += 1;
        const partId = `stream-text:${messageId}:${timelineTextSegment}`;
        const now = Date.now();
        blockPartById.set(partId, {
          id: partId,
          type: "stream-text",
          sessionID: sessionId,
          messageID: messageId,
          text: "",
          time: { start: now }
        });
        partKindById.set(partId, "stream-text");
        activeTimelineTextPartId = partId;
        return partId;
      };
      const updateTimelineTextPart = (delta = "", snapshotText = "") => {
        const partId = ensureTimelineTextPartId();
        if (!partId) return false;
        const currentPart = blockPartById.get(partId) || {
          id: partId,
          type: "stream-text",
          sessionID: sessionId,
          messageID: messageId,
          text: "",
          time: { start: Date.now() }
        };
        const currentText = String(currentPart.text || "");
        let nextText = currentText;
        if (delta) nextText = mergeDeltaText(nextText, delta);
        if (typeof snapshotText === "string" && snapshotText.length) {
          nextText = mergeSnapshotText(nextText, snapshotText);
        }
        if (nextText === currentText) return false;
        const nextPart = Object.assign({}, currentPart, {
          messageID: messageId || currentPart.messageID || "",
          text: nextText,
          time: Object.assign({}, currentPart.time || {}, {
            end: Date.now()
          })
        });
        blockPartById.set(partId, nextPart);
        partKindById.set(partId, "stream-text");
        return true;
      };
      const joinPartText = (map) => Array.from(map.values()).map((v) => String(v || "")).filter((v) => v.length > 0).join("\n\n");
      const updateText = () => {
        const next = joinPartText(textByPart);
        if (next !== text) {
          text = next;
          call(cfg.onToken, text);
        }
      };
      const updateReasoning = () => {
        const next = joinPartText(reasoningByPart);
        if (next !== reasoning) {
          reasoning = next;
          call(cfg.onReasoning, reasoning);
        }
      };
      const retainStickyBlocks = () => {
        const retained = [];
        for (const [id, part] of blockPartById.entries()) {
          if (!stickyBlockIds.has(id)) continue;
          retained.push([id, part]);
        }
        return retained;
      };
      const updateBlocks = () => {
        const nextBlocks = Array.from(blockPartById.values()).map((part, idx) => toPartBlock(part, idx)).filter(Boolean);
        const nextBlocksKey = blocksFingerprint(nextBlocks);
        if (nextBlocksKey !== blocksKey) {
          blocks = nextBlocks;
          blocksKey = nextBlocksKey;
          call(cfg.onBlocks, blocks);
        }
      };
      const resetActiveMessageContent = () => {
        const stickyBlocks = retainStickyBlocks();
        textByPart.clear();
        reasoningByPart.clear();
        blockPartById.clear();
        partKindById.clear();
        partAliasToCanonicalId.clear();
        resetTimelineTextState();
        for (const [id, part] of stickyBlocks) {
          blockPartById.set(id, part);
          partKindById.set(id, String(part && part.type ? part.type : ""));
        }
        completionSeen = false;
        idleSeen = false;
        text = "";
        reasoning = "";
        blocks = stickyBlocks.map((entry, idx) => toPartBlock(entry[1], idx)).filter(Boolean);
        blocksKey = blocksFingerprint(blocks);
        call(cfg.onToken, "");
        call(cfg.onReasoning, "");
        call(cfg.onBlocks, blocks);
      };
      const consumePermissionEvent = (event) => {
        const props = event.properties && typeof event.properties === "object" ? event.properties : {};
        const permission = props.request && typeof props.request === "object" ? props.request : props;
        const permId = permission && typeof permission.id === "string" && permission.id || typeof props.permissionID === "string" && props.permissionID || "";
        const permSession = permission && typeof permission.sessionID === "string" && permission.sessionID || typeof props.sessionID === "string" && props.sessionID || "";
        if (!permId || permSession && permSession !== sessionId) return;
        if (promptedPermissionIds.has(permId)) return;
        promptedPermissionIds.add(permId);
        call(cfg.onPermissionRequest, permission || {}, permId);
      };
      const consumeQuestionAsked = (event) => {
        const props = event.properties && typeof event.properties === "object" ? event.properties : {};
        const request = props.request && typeof props.request === "object" ? props.request : props;
        const requestId = request && typeof request.id === "string" && request.id || typeof props.requestID === "string" && props.requestID || "";
        const reqSession = request && typeof request.sessionID === "string" && request.sessionID || typeof props.sessionID === "string" && props.sessionID || "";
        if (!requestId || reqSession && reqSession !== sessionId) return;
        activeQuestionIds.add(requestId);
        if (promptedQuestionIds.has(requestId)) return;
        promptedQuestionIds.add(requestId);
        call(cfg.onQuestionRequest, request || {});
      };
      const consumeQuestionResolved = (event) => {
        const props = event.properties && typeof event.properties === "object" ? event.properties : {};
        const requestId = typeof props.requestID === "string" ? props.requestID : "";
        const reqSession = typeof props.sessionID === "string" ? props.sessionID : "";
        if (reqSession && reqSession !== sessionId) return;
        if (requestId) activeQuestionIds.delete(requestId);
        else activeQuestionIds.clear();
        call(cfg.onQuestionResolved, {
          requestId,
          sessionId: reqSession || sessionId,
          rejected: event.type === "question.rejected",
          answers: Array.isArray(props.answers) ? props.answers : []
        });
      };
      const consumePromptAppend = (event) => {
        const props = event.properties && typeof event.properties === "object" ? event.properties : {};
        const appendText = typeof props.text === "string" ? props.text : "";
        if (!appendText) return;
        call(cfg.onPromptAppend, appendText);
      };
      const consumeToast = (event) => {
        const props = event.properties && typeof event.properties === "object" ? event.properties : {};
        call(cfg.onToast, {
          title: typeof props.title === "string" ? props.title : "",
          message: typeof props.message === "string" ? props.message : "",
          variant: typeof props.variant === "string" ? props.variant : "info"
        });
      };
      const consumeSessionError = (event) => {
        const props = event.properties && typeof event.properties === "object" ? event.properties : {};
        if (props.sessionID && props.sessionID !== sessionId) return;
        const err = extractErrorText(props.error);
        if (err) {
          meta = err;
          if (!text) {
            text = rt("\u6A21\u578B\u8FD4\u56DE\u9519\u8BEF\uFF1A{err}", "Model returned an error: {err}", { err });
            call(cfg.onToken, text);
          }
        }
        completionSeen = true;
        done = true;
      };
      const consumeMessageUpdated = (event) => {
        const info = event.properties && event.properties.info && typeof event.properties.info === "object" ? event.properties.info : null;
        if (!info || info.sessionID !== sessionId) return;
        if (info.role === "user") {
          if (typeof info.id !== "string" || !info.id) return;
          const created2 = info.time ? normalizeTimestampMs(info.time.created || 0) : 0;
          if (created2 > 0 && created2 < startedAt - 1e3) return;
          latestUserMessageId = info.id;
          return;
        }
        if (info.role !== "assistant") return;
        if (typeof info.id !== "string" || !info.id) return;
        const created = info.time ? normalizeTimestampMs(info.time.created || 0) : 0;
        if (created > 0 && created < startedAt - 1e3) return;
        if (!messageId || created >= activeMessageCreatedAt) {
          if (messageId && messageId !== info.id) {
            resetActiveMessageContent();
          }
          messageId = info.id;
          activeMessageCreatedAt = created;
        }
        if (info.id !== messageId) return;
        const err = extractErrorText(info.error);
        if (err) {
          meta = err;
          if (!text) {
            text = rt("\u6A21\u578B\u8FD4\u56DE\u9519\u8BEF\uFF1A{err}", "Model returned an error: {err}", { err });
            call(cfg.onToken, text);
          }
        }
        if (isAssistantMessageCompletedInfo(info)) {
          completionSeen = true;
          if (idleSeen) done = true;
        }
      };
      const tryCaptureSkillInstruction = (part, delta = "") => {
        if (!part || part.type !== "text") return;
        if (typeof part.messageID !== "string" || !part.messageID) return;
        if (latestUserMessageId && part.messageID !== latestUserMessageId) return;
        const partId = typeof part.id === "string" && part.id ? part.id : `user:${part.messageID}`;
        const current = userTextByPart.get(partId) || "";
        let next = current;
        if (delta) next = mergeDeltaText(next, delta);
        if (typeof part.text === "string") next = mergeSnapshotText(next, part.text);
        if (next !== current) userTextByPart.set(partId, next);
        const skillInfo = parseSkillInfoFromInstructionText(next);
        if (!skillInfo) return;
        const toolPartId = `skill-instruction:${part.messageID}`;
        if (blockPartById.has(toolPartId)) return;
        const now = Date.now();
        const skillPart = {
          id: toolPartId,
          sessionID: sessionId,
          messageID: part.messageID,
          type: "tool",
          callID: toolPartId,
          tool: "skill",
          state: {
            status: "completed",
            title: `Skill: ${skillInfo.skillId}`,
            input: {
              skill: skillInfo.skillId,
              source: "skill-instruction",
              baseDirectory: skillInfo.baseDirectory
            },
            output: `Official skill context injected: ${skillInfo.skillId}`,
            metadata: {
              source: "skill-instruction"
            },
            time: {
              start: now,
              end: now
            }
          }
        };
        stickyBlockIds.add(toolPartId);
        partKindById.set(toolPartId, "tool");
        blockPartById.set(toolPartId, skillPart);
        updateBlocks();
      };
      const resolveCanonicalPartId = (part) => {
        if (!part || typeof part !== "object") return "";
        const type = typeof part.type === "string" ? part.type : "";
        if (type === "tool") {
          const callId = typeof part.callID === "string" ? part.callID.trim() : "";
          if (callId) return `tool:${callId}`;
        }
        const rawId = typeof part.id === "string" ? part.id.trim() : "";
        if (rawId) return rawId;
        const messageRef = typeof part.messageID === "string" ? part.messageID : "unknown";
        return `${type || "part"}:${messageRef}`;
      };
      const tryAdoptActiveAssistantMessageId = (candidateId, partType = "") => {
        const nextId = String(candidateId || "").trim();
        if (!nextId) return false;
        if (messageId) return nextId === messageId;
        if (latestUserMessageId && nextId === latestUserMessageId) return false;
        const normalizedType = String(partType || "").trim().toLowerCase();
        if (normalizedType === "text" && !latestUserMessageId) return false;
        messageId = nextId;
        activeMessageCreatedAt = Math.max(activeMessageCreatedAt || 0, startedAt || Date.now());
        return true;
      };
      const consumeMessagePartUpdated = (event) => {
        const props = event.properties && typeof event.properties === "object" ? event.properties : {};
        const part = props.part && typeof props.part === "object" ? props.part : null;
        if (!part || typeof part.sessionID !== "string" || part.sessionID !== sessionId) return;
        const delta = typeof props.delta === "string" ? props.delta : "";
        if (part.time) {
          const partStart = normalizeTimestampMs(part.time.start || 0);
          if (partStart > 0 && partStart < startedAt - 1e3) return;
        }
        tryCaptureSkillInstruction(part, delta);
        const partMessageId = typeof part.messageID === "string" ? part.messageID : "";
        if (!messageId) {
          const adopted = tryAdoptActiveAssistantMessageId(partMessageId, part.type);
          if (!adopted) return;
        }
        if (!partMessageId || partMessageId !== messageId) return;
        const rawPartId = typeof part.id === "string" ? part.id.trim() : "";
        const partId = resolveCanonicalPartId(part);
        if (rawPartId && rawPartId !== partId) {
          partAliasToCanonicalId.set(rawPartId, partId);
        }
        partKindById.set(partId, String(part.type || ""));
        if (part.type === "text") {
          idleSeen = false;
          const timelineChanged = updateTimelineTextPart(
            delta,
            typeof part.text === "string" ? part.text : ""
          );
          if (part.ignored === true) {
            textByPart.delete(partId);
          } else {
            const current = textByPart.get(partId) || "";
            let next = current;
            if (delta) next = mergeDeltaText(next, delta);
            if (typeof part.text === "string") next = mergeSnapshotText(next, part.text);
            if (next !== current) {
              textByPart.set(partId, next);
            }
          }
          updateText();
          if (timelineChanged) updateBlocks();
          return;
        }
        if (part.type === "reasoning") {
          idleSeen = false;
          closeTimelineTextPart();
          const current = reasoningByPart.get(partId) || "";
          let next = current;
          if (delta) next = mergeDeltaText(next, delta);
          if (typeof part.text === "string") next = mergeSnapshotText(next, part.text);
          if (next !== current) {
            reasoningByPart.set(partId, next);
          }
          blockPartById.set(partId, Object.assign({}, part, { text: next || current }));
          updateReasoning();
          updateBlocks();
          return;
        }
        idleSeen = false;
        closeTimelineTextPart();
        blockPartById.set(partId, part);
        updateBlocks();
      };
      const consumeMessagePartDelta = (event) => {
        const props = event.properties && typeof event.properties === "object" ? event.properties : {};
        const sid = typeof props.sessionID === "string" ? props.sessionID : "";
        if (sid && sid !== sessionId) return;
        const msgId = typeof props.messageID === "string" ? props.messageID : "";
        if (!messageId) {
          const adopted = tryAdoptActiveAssistantMessageId(msgId, "");
          if (!adopted) return;
        }
        if (msgId && msgId !== messageId) return;
        const rawPartId = typeof props.partID === "string" ? props.partID.trim() : "";
        if (!rawPartId) return;
        const field = typeof props.field === "string" ? props.field.trim().toLowerCase() : "";
        if (field && !/(\.|^)text$/.test(field)) return;
        const delta = typeof props.delta === "string" ? props.delta : "";
        if (!delta) return;
        const partId = partAliasToCanonicalId.get(rawPartId) || rawPartId;
        const knownType = String(partKindById.get(partId) || "");
        if (knownType === "reasoning") {
          idleSeen = false;
          closeTimelineTextPart();
          const current2 = reasoningByPart.get(partId) || "";
          const next2 = mergeDeltaText(current2, delta);
          if (next2 !== current2) {
            reasoningByPart.set(partId, next2);
            const prevPart = blockPartById.get(partId);
            if (prevPart && typeof prevPart === "object") {
              blockPartById.set(partId, Object.assign({}, prevPart, { text: next2 }));
            }
            updateReasoning();
            updateBlocks();
          }
          return;
        }
        idleSeen = false;
        const timelineChanged = updateTimelineTextPart(delta, "");
        const current = textByPart.get(partId) || "";
        const next = mergeDeltaText(current, delta);
        if (next !== current) {
          textByPart.set(partId, next);
          updateText();
        }
        if (timelineChanged) updateBlocks();
      };
      const consumeMessagePartRemoved = (event) => {
        const props = event.properties && typeof event.properties === "object" ? event.properties : {};
        const sid = typeof props.sessionID === "string" ? props.sessionID : "";
        if (sid && sid !== sessionId) return;
        const rawPartId = typeof props.partID === "string" ? props.partID : "";
        if (!rawPartId) return;
        const partId = partAliasToCanonicalId.get(rawPartId) || rawPartId;
        const partType = String(partKindById.get(partId) || "");
        if (partType === "text") {
          textByPart.delete(partId);
          updateText();
        } else if (partType === "reasoning") {
          updateReasoning();
        } else {
          updateBlocks();
        }
      };
      function consume(rawEvent) {
        const event = rawEvent && typeof rawEvent === "object" ? rawEvent : null;
        if (!event || typeof event.type !== "string") return done;
        if (event.type === "message.part.updated") {
          consumeMessagePartUpdated(event);
          return done;
        }
        if (event.type === "message.part.delta") {
          consumeMessagePartDelta(event);
          return done;
        }
        if (event.type === "message.part.removed") {
          consumeMessagePartRemoved(event);
          return done;
        }
        if (event.type === "message.updated") {
          consumeMessageUpdated(event);
          return done;
        }
        if (permissionEventTypes.has(event.type)) {
          consumePermissionEvent(event);
          return done;
        }
        if (event.type === "question.asked") {
          consumeQuestionAsked(event);
          return done;
        }
        if (event.type === "question.replied" || event.type === "question.rejected") {
          consumeQuestionResolved(event);
          return done;
        }
        if (event.type === "tui.prompt.append") {
          consumePromptAppend(event);
          return done;
        }
        if (event.type === "tui.toast.show") {
          consumeToast(event);
          return done;
        }
        if (event.type === "session.error") {
          consumeSessionError(event);
          return done;
        }
        if (event.type === "session.idle") {
          const sid = extractSessionIdFromEventProperties(event.properties);
          if (sid === sessionId) {
            if (activeQuestionIds.size > 0) return done;
            const hasAnyPayload = Boolean(String(text || "").trim() || String(reasoning || "").trim() || String(meta || "").trim() || Array.isArray(blocks) && blocks.length > 0);
            if (completionSeen || !hasAnyPayload && !messageId) {
              done = true;
            } else {
              idleSeen = true;
            }
          }
          return done;
        }
        if (event.type === "session.status") {
          const sid = extractSessionIdFromEventProperties(event.properties);
          const status = extractSessionStatusType(
            extractSessionStatusFromEventProperties(event.properties)
          );
          if (sid === sessionId && status === "idle") {
            if (activeQuestionIds.size > 0) return done;
            const hasAnyPayload = Boolean(String(text || "").trim() || String(reasoning || "").trim() || String(meta || "").trim() || Array.isArray(blocks) && blocks.length > 0);
            if (completionSeen || !hasAnyPayload && !messageId) {
              done = true;
            } else {
              idleSeen = true;
            }
          }
        }
        return done;
      }
      function snapshot() {
        return {
          messageId,
          text,
          reasoning,
          meta,
          blocks,
          completed: Boolean(done && completionSeen)
        };
      }
      function isDone() {
        return done;
      }
      return {
        consume,
        snapshot,
        isDone
      };
    }
    module2.exports = {
      createTransportEventReducer
    };
  }
});

// runtime/transports/shared/finalizer.js
var require_finalizer = __commonJS({
  "runtime/transports/shared/finalizer.js"(exports2, module2) {
    var {
      blocksFingerprint,
      chooseRicherResponse,
      formatSessionStatusText,
      hasRenderablePayload,
      normalizedRenderableText,
      payloadLooksInProgress,
      responseRichnessScore
    } = require_assistant_payload_utils();
    var {
      extractSessionStatusHint,
      isSessionStatusTerminal
    } = require_completion_signals();
    var { rt } = (() => {
      try {
        return require_runtime_locale_state();
      } catch (_e) {
        return {
          rt: (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
            const value = params[k];
            return value === void 0 || value === null ? "" : String(value);
          })
        };
      }
    })();
    function buildProgressKey(messageId, payload) {
      const p = payload && typeof payload === "object" ? payload : {};
      const text = normalizedRenderableText(p.text || "");
      const reasoning = String(p.reasoning || "").trim();
      const blocks = Array.isArray(p.blocks) ? p.blocks : [];
      const typeStatusSummary = (() => {
        const counter = /* @__PURE__ */ new Map();
        for (const block of blocks) {
          if (!block || typeof block !== "object") continue;
          const type = String(block.type || "block").trim() || "block";
          const status = String(block.status || "").trim().toLowerCase() || "-";
          const key = `${type}:${status}`;
          counter.set(key, Number(counter.get(key) || 0) + 1);
        }
        return Array.from(counter.entries()).sort((a, b) => String(a[0]).localeCompare(String(b[0]))).map(([key, count]) => `${key}=${count}`).join(",");
      })();
      return [
        String(messageId || ""),
        `score=${responseRichnessScore(p)}`,
        `text=${text.length}`,
        `reasoning=${reasoning.length}`,
        `blocks=${blocks.length}`,
        `shape=${typeStatusSummary}`,
        `fp=${blocksFingerprint(blocks)}`
      ].join("|");
    }
    function statusIndicatesTerminalNoWork(status) {
      return isSessionStatusTerminal(status);
    }
    function extractStatusHint(status) {
      return extractSessionStatusHint(status);
    }
    function buildStatusErrorPayload(statusHint, payload) {
      const hint = String(statusHint || "").trim();
      if (!hint) return payload;
      const current = payload && typeof payload === "object" ? payload : {};
      if (hasRenderablePayload(current)) return current;
      return {
        text: rt("\u6A21\u578B\u8FD4\u56DE\u9519\u8BEF\uFF1A{hint}", "Model returned an error: {hint}", { hint }),
        reasoning: "",
        meta: hint,
        blocks: []
      };
    }
    async function pollAssistantPayload(options) {
      const cfg = options && typeof options === "object" ? options : {};
      const quietTimeoutMs = Math.max(1e4, Number(cfg.quietTimeoutMs || 12e4));
      const maxTotalMs = Math.max(quietTimeoutMs * 3, Number(cfg.maxTotalMs || 10 * 60 * 1e3));
      const latestIntervalMs = Math.max(220, Number(cfg.latestIntervalMs || 1100));
      const requireTerminalStatusOnCompletion = Boolean(cfg.requireTerminalStatusOnCompletion);
      const defaultNoMessageTimeoutMs = Math.max(
        12e3,
        Math.min(45e3, Math.floor(quietTimeoutMs * 0.5))
      );
      const configuredNoMessageTimeoutMs = Number(cfg.noMessageTimeoutMs);
      const noMessageTimeoutMs = Number.isFinite(configuredNoMessageTimeoutMs) && configuredNoMessageTimeoutMs > 0 ? configuredNoMessageTimeoutMs : defaultNoMessageTimeoutMs;
      const terminalNoPayloadGraceMs = Math.max(2500, Math.min(noMessageTimeoutMs, 2e4));
      const startedAt = Date.now();
      let lastProgressAt = startedAt;
      let messageId = String(cfg.initialMessageId || "");
      let payload = cfg.initialPayload && typeof cfg.initialPayload === "object" ? cfg.initialPayload : { text: "", reasoning: "", meta: "", blocks: [] };
      let progressKey = buildProgressKey(messageId, payload);
      let pollCount = 0;
      let lastStatus = null;
      let lastMessageCreated = 0;
      let timedOut = false;
      let completionSeen = false;
      let byIdSeen = false;
      let lastLatestAt = 0;
      let questionPending = false;
      let lastQuestionCheckAt = 0;
      const emit = () => {
        if (typeof cfg.onToken === "function") cfg.onToken(String(payload.text || ""));
        if (typeof cfg.onReasoning === "function") cfg.onReasoning(String(payload.reasoning || ""));
        if (typeof cfg.onBlocks === "function") cfg.onBlocks(Array.isArray(payload.blocks) ? payload.blocks : []);
      };
      const markProgress = () => {
        const nextKey = buildProgressKey(messageId, payload);
        if (nextKey !== progressKey) {
          progressKey = nextKey;
          lastProgressAt = Date.now();
          emit();
        }
      };
      const isComplete = () => {
        if (!hasRenderablePayload(payload)) return false;
        if (payloadLooksInProgress(payload)) return false;
        if (!completionSeen) return false;
        if (!requireTerminalStatusOnCompletion) return true;
        if (statusIndicatesTerminalNoWork(lastStatus)) return true;
        if (typeof cfg.getSessionStatus !== "function") return true;
        const elapsedMs = Date.now() - startedAt;
        const statusGraceMs = Math.max(2500, Math.min(12e3, Math.floor(quietTimeoutMs * 0.15)));
        if (!lastStatus && pollCount >= 4 && elapsedMs >= statusGraceMs) return true;
        return false;
      };
      const checkQuestionPending = async (force = false) => {
        if (typeof cfg.isQuestionPending !== "function") return false;
        const now = Date.now();
        if (!force && now - lastQuestionCheckAt < 800) return questionPending;
        lastQuestionCheckAt = now;
        try {
          questionPending = Boolean(await cfg.isQuestionPending(cfg.signal));
        } catch {
          questionPending = false;
        }
        return questionPending;
      };
      while (Date.now() - startedAt < maxTotalMs && Date.now() - lastProgressAt < quietTimeoutMs) {
        if (cfg.signal && cfg.signal.aborted) {
          throw new Error(rt("\u7528\u6237\u53D6\u6D88\u4E86\u8BF7\u6C42", "Request was cancelled by user"));
        }
        pollCount += 1;
        if (pollCount % 4 === 0 && !messageId && !hasRenderablePayload(payload) && await checkQuestionPending()) {
          lastProgressAt = Date.now();
        }
        let byIdUpdated = false;
        if (messageId && typeof cfg.getByMessageId === "function") {
          try {
            const byId = await cfg.getByMessageId(messageId, cfg.signal);
            if (byId && byId.payload) {
              byIdSeen = true;
              const before = payload;
              payload = chooseRicherResponse(payload, byId.payload);
              if (byId.completed) completionSeen = true;
              if (byId.messageId) messageId = String(byId.messageId || messageId);
              if (payload !== before) {
                markProgress();
                byIdUpdated = true;
              }
            }
          } catch {
          }
        }
        const now = Date.now();
        const shouldFetchLatest = typeof cfg.getLatest === "function" && (!messageId || !byIdSeen || !hasRenderablePayload(payload) || !byIdUpdated || now - lastLatestAt >= latestIntervalMs);
        if (shouldFetchLatest) {
          lastLatestAt = now;
          try {
            const latest = await cfg.getLatest(cfg.signal);
            if (latest && latest.payload) {
              if (latest.messageId && (!latest.createdAt || Number(latest.createdAt) >= lastMessageCreated)) {
                messageId = String(latest.messageId);
                if (latest.createdAt) lastMessageCreated = Number(latest.createdAt || 0);
              }
              const before = payload;
              payload = chooseRicherResponse(payload, latest.payload);
              if (latest.completed) completionSeen = true;
              if (payload !== before || latest.messageId) markProgress();
            }
          } catch {
          }
        }
        if (pollCount % 2 === 0 && typeof cfg.getSessionStatus === "function") {
          lastStatus = await cfg.getSessionStatus(cfg.signal);
          const statusHint = extractStatusHint(lastStatus);
          if (/401|unauthorized|user not found|forbidden|invalid api key|authentication/i.test(statusHint)) {
            payload = buildStatusErrorPayload(statusHint, payload);
            markProgress();
            break;
          }
          if (statusIndicatesTerminalNoWork(lastStatus)) {
            const hasRenderable = hasRenderablePayload(payload);
            if (!hasRenderable) {
              if (await checkQuestionPending(true)) {
                lastProgressAt = Date.now();
              } else {
                const elapsedMs = Date.now() - startedAt;
                const allowShortTimeoutFastExit = noMessageTimeoutMs <= 2e3;
                if (messageId || allowShortTimeoutFastExit || elapsedMs >= terminalNoPayloadGraceMs) {
                  break;
                }
              }
            } else {
            }
          }
        }
        if (!messageId && !hasRenderablePayload(payload) && Date.now() - startedAt >= noMessageTimeoutMs) {
          if (await checkQuestionPending()) {
            lastProgressAt = Date.now();
          } else if (statusIndicatesTerminalNoWork(lastStatus)) {
            break;
          } else if (!lastStatus && typeof cfg.getSessionStatus !== "function") {
            break;
          }
        }
        if (isComplete()) break;
        await cfg.sleep(220);
      }
      if (Date.now() - startedAt >= maxTotalMs || Date.now() - lastProgressAt >= quietTimeoutMs) {
        timedOut = true;
      }
      return {
        messageId,
        payload,
        lastStatus,
        completed: completionSeen,
        timedOut,
        startedAt,
        lastProgressAt,
        quietTimeoutMs,
        maxTotalMs
      };
    }
    async function ensureRenderablePayload(options) {
      const cfg = options && typeof options === "object" ? options : {};
      const payload = cfg.payload && typeof cfg.payload === "object" ? cfg.payload : { text: "", reasoning: "", meta: "", blocks: [] };
      if (hasRenderablePayload(payload)) return payload;
      const status = cfg.lastStatus || (typeof cfg.getSessionStatus === "function" ? await cfg.getSessionStatus(cfg.signal) : null);
      const statusText = formatSessionStatusText(status);
      return {
        ...payload,
        text: rt(
          "(\u65E0\u6587\u672C\u8FD4\u56DE\uFF1Asession.status={statusText}\u3002\u53EF\u80FD\u662F\u6A21\u578B\u4ECD\u5728\u6392\u961F/\u6267\u884C\uFF0C\u6216\u5F53\u524D\u6A21\u578B\u4E0D\u53EF\u7528\uFF1B\u8BF7\u5728 FLOWnote \u8BCA\u65AD\u4E2D\u67E5\u770B\u72B6\u6001\u8BE6\u60C5\u3002)",
          "(No text returned: session.status={statusText}. The model may still be queued/running, or current model is unavailable; check FLOWnote diagnostics for details.)",
          { statusText }
        )
      };
    }
    module2.exports = {
      pollAssistantPayload,
      ensureRenderablePayload
    };
  }
});

// runtime/vendor/opencode-sdk-v2-client.cjs
var require_opencode_sdk_v2_client = __commonJS({
  "runtime/vendor/opencode-sdk-v2-client.cjs"(exports2, module2) {
    var __defProp = Object.defineProperty;
    var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
    var __getOwnPropNames2 = Object.getOwnPropertyNames;
    var __hasOwnProp = Object.prototype.hasOwnProperty;
    var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
    var __export = (target, all) => {
      for (var name in all)
        __defProp(target, name, { get: all[name], enumerable: true });
    };
    var __copyProps = (to, from, except, desc) => {
      if (from && typeof from === "object" || typeof from === "function") {
        for (let key of __getOwnPropNames2(from))
          if (!__hasOwnProp.call(to, key) && key !== except)
            __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
      }
      return to;
    };
    var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
    var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
    var sdk_v2_client_entry_exports = {};
    __export(sdk_v2_client_entry_exports, {
      createOpencodeClient: () => createOpencodeClient
    });
    module2.exports = __toCommonJS(sdk_v2_client_entry_exports);
    var createSseClient = ({ onRequest, onSseError, onSseEvent, responseTransformer, responseValidator, sseDefaultRetryDelay, sseMaxRetryAttempts, sseMaxRetryDelay, sseSleepFn, url, ...options }) => {
      let lastEventId;
      const sleep = sseSleepFn ?? ((ms) => new Promise((resolve) => setTimeout(resolve, ms)));
      const createStream = async function* () {
        let retryDelay = sseDefaultRetryDelay ?? 3e3;
        let attempt = 0;
        const signal = options.signal ?? new AbortController().signal;
        while (true) {
          if (signal.aborted)
            break;
          attempt++;
          const headers = options.headers instanceof Headers ? options.headers : new Headers(options.headers);
          if (lastEventId !== void 0) {
            headers.set("Last-Event-ID", lastEventId);
          }
          try {
            const requestInit = {
              redirect: "follow",
              ...options,
              body: options.serializedBody,
              headers,
              signal
            };
            let request = new Request(url, requestInit);
            if (onRequest) {
              request = await onRequest(url, requestInit);
            }
            const _fetch = options.fetch ?? globalThis.fetch;
            const response = await _fetch(request);
            if (!response.ok)
              throw new Error(`SSE failed: ${response.status} ${response.statusText}`);
            if (!response.body)
              throw new Error("No body in SSE response");
            const reader = response.body.pipeThrough(new TextDecoderStream()).getReader();
            let buffer = "";
            const abortHandler = () => {
              try {
                reader.cancel();
              } catch {
              }
            };
            signal.addEventListener("abort", abortHandler);
            try {
              while (true) {
                const { done, value } = await reader.read();
                if (done)
                  break;
                buffer += value;
                buffer = buffer.replace(/\r\n/g, "\n").replace(/\r/g, "\n");
                const chunks = buffer.split("\n\n");
                buffer = chunks.pop() ?? "";
                for (const chunk of chunks) {
                  const lines = chunk.split("\n");
                  const dataLines = [];
                  let eventName;
                  for (const line of lines) {
                    if (line.startsWith("data:")) {
                      dataLines.push(line.replace(/^data:\s*/, ""));
                    } else if (line.startsWith("event:")) {
                      eventName = line.replace(/^event:\s*/, "");
                    } else if (line.startsWith("id:")) {
                      lastEventId = line.replace(/^id:\s*/, "");
                    } else if (line.startsWith("retry:")) {
                      const parsed = Number.parseInt(line.replace(/^retry:\s*/, ""), 10);
                      if (!Number.isNaN(parsed)) {
                        retryDelay = parsed;
                      }
                    }
                  }
                  let data;
                  let parsedJson = false;
                  if (dataLines.length) {
                    const rawData = dataLines.join("\n");
                    try {
                      data = JSON.parse(rawData);
                      parsedJson = true;
                    } catch {
                      data = rawData;
                    }
                  }
                  if (parsedJson) {
                    if (responseValidator) {
                      await responseValidator(data);
                    }
                    if (responseTransformer) {
                      data = await responseTransformer(data);
                    }
                  }
                  onSseEvent?.({
                    data,
                    event: eventName,
                    id: lastEventId,
                    retry: retryDelay
                  });
                  if (dataLines.length) {
                    yield data;
                  }
                }
              }
            } finally {
              signal.removeEventListener("abort", abortHandler);
              reader.releaseLock();
            }
            break;
          } catch (error) {
            onSseError?.(error);
            if (sseMaxRetryAttempts !== void 0 && attempt >= sseMaxRetryAttempts) {
              break;
            }
            const backoff = Math.min(retryDelay * 2 ** (attempt - 1), sseMaxRetryDelay ?? 3e4);
            await sleep(backoff);
          }
        }
      };
      const stream = createStream();
      return { stream };
    };
    var separatorArrayExplode = (style) => {
      switch (style) {
        case "label":
          return ".";
        case "matrix":
          return ";";
        case "simple":
          return ",";
        default:
          return "&";
      }
    };
    var separatorArrayNoExplode = (style) => {
      switch (style) {
        case "form":
          return ",";
        case "pipeDelimited":
          return "|";
        case "spaceDelimited":
          return "%20";
        default:
          return ",";
      }
    };
    var separatorObjectExplode = (style) => {
      switch (style) {
        case "label":
          return ".";
        case "matrix":
          return ";";
        case "simple":
          return ",";
        default:
          return "&";
      }
    };
    var serializeArrayParam = ({ allowReserved, explode, name, style, value }) => {
      if (!explode) {
        const joinedValues2 = (allowReserved ? value : value.map((v) => encodeURIComponent(v))).join(separatorArrayNoExplode(style));
        switch (style) {
          case "label":
            return `.${joinedValues2}`;
          case "matrix":
            return `;${name}=${joinedValues2}`;
          case "simple":
            return joinedValues2;
          default:
            return `${name}=${joinedValues2}`;
        }
      }
      const separator = separatorArrayExplode(style);
      const joinedValues = value.map((v) => {
        if (style === "label" || style === "simple") {
          return allowReserved ? v : encodeURIComponent(v);
        }
        return serializePrimitiveParam({
          allowReserved,
          name,
          value: v
        });
      }).join(separator);
      return style === "label" || style === "matrix" ? separator + joinedValues : joinedValues;
    };
    var serializePrimitiveParam = ({ allowReserved, name, value }) => {
      if (value === void 0 || value === null) {
        return "";
      }
      if (typeof value === "object") {
        throw new Error("Deeply-nested arrays/objects aren\u2019t supported. Provide your own `querySerializer()` to handle these.");
      }
      return `${name}=${allowReserved ? value : encodeURIComponent(value)}`;
    };
    var serializeObjectParam = ({ allowReserved, explode, name, style, value, valueOnly }) => {
      if (value instanceof Date) {
        return valueOnly ? value.toISOString() : `${name}=${value.toISOString()}`;
      }
      if (style !== "deepObject" && !explode) {
        let values = [];
        Object.entries(value).forEach(([key, v]) => {
          values = [...values, key, allowReserved ? v : encodeURIComponent(v)];
        });
        const joinedValues2 = values.join(",");
        switch (style) {
          case "form":
            return `${name}=${joinedValues2}`;
          case "label":
            return `.${joinedValues2}`;
          case "matrix":
            return `;${name}=${joinedValues2}`;
          default:
            return joinedValues2;
        }
      }
      const separator = separatorObjectExplode(style);
      const joinedValues = Object.entries(value).map(([key, v]) => serializePrimitiveParam({
        allowReserved,
        name: style === "deepObject" ? `${name}[${key}]` : key,
        value: v
      })).join(separator);
      return style === "label" || style === "matrix" ? separator + joinedValues : joinedValues;
    };
    var PATH_PARAM_RE = /\{[^{}]+\}/g;
    var defaultPathSerializer = ({ path, url: _url }) => {
      let url = _url;
      const matches = _url.match(PATH_PARAM_RE);
      if (matches) {
        for (const match of matches) {
          let explode = false;
          let name = match.substring(1, match.length - 1);
          let style = "simple";
          if (name.endsWith("*")) {
            explode = true;
            name = name.substring(0, name.length - 1);
          }
          if (name.startsWith(".")) {
            name = name.substring(1);
            style = "label";
          } else if (name.startsWith(";")) {
            name = name.substring(1);
            style = "matrix";
          }
          const value = path[name];
          if (value === void 0 || value === null) {
            continue;
          }
          if (Array.isArray(value)) {
            url = url.replace(match, serializeArrayParam({ explode, name, style, value }));
            continue;
          }
          if (typeof value === "object") {
            url = url.replace(match, serializeObjectParam({
              explode,
              name,
              style,
              value,
              valueOnly: true
            }));
            continue;
          }
          if (style === "matrix") {
            url = url.replace(match, `;${serializePrimitiveParam({
              name,
              value
            })}`);
            continue;
          }
          const replaceValue = encodeURIComponent(style === "label" ? `.${value}` : value);
          url = url.replace(match, replaceValue);
        }
      }
      return url;
    };
    var getUrl = ({ baseUrl, path, query, querySerializer, url: _url }) => {
      const pathUrl = _url.startsWith("/") ? _url : `/${_url}`;
      let url = (baseUrl ?? "") + pathUrl;
      if (path) {
        url = defaultPathSerializer({ path, url });
      }
      let search = query ? querySerializer(query) : "";
      if (search.startsWith("?")) {
        search = search.substring(1);
      }
      if (search) {
        url += `?${search}`;
      }
      return url;
    };
    function getValidRequestBody(options) {
      const hasBody = options.body !== void 0;
      const isSerializedBody = hasBody && options.bodySerializer;
      if (isSerializedBody) {
        if ("serializedBody" in options) {
          const hasSerializedBody = options.serializedBody !== void 0 && options.serializedBody !== "";
          return hasSerializedBody ? options.serializedBody : null;
        }
        return options.body !== "" ? options.body : null;
      }
      if (hasBody) {
        return options.body;
      }
      return void 0;
    }
    var getAuthToken = async (auth, callback) => {
      const token = typeof callback === "function" ? await callback(auth) : callback;
      if (!token) {
        return;
      }
      if (auth.scheme === "bearer") {
        return `Bearer ${token}`;
      }
      if (auth.scheme === "basic") {
        return `Basic ${btoa(token)}`;
      }
      return token;
    };
    var jsonBodySerializer = {
      bodySerializer: (body) => JSON.stringify(body, (_key, value) => typeof value === "bigint" ? value.toString() : value)
    };
    var createQuerySerializer = ({ parameters = {}, ...args } = {}) => {
      const querySerializer = (queryParams) => {
        const search = [];
        if (queryParams && typeof queryParams === "object") {
          for (const name in queryParams) {
            const value = queryParams[name];
            if (value === void 0 || value === null) {
              continue;
            }
            const options = parameters[name] || args;
            if (Array.isArray(value)) {
              const serializedArray = serializeArrayParam({
                allowReserved: options.allowReserved,
                explode: true,
                name,
                style: "form",
                value,
                ...options.array
              });
              if (serializedArray)
                search.push(serializedArray);
            } else if (typeof value === "object") {
              const serializedObject = serializeObjectParam({
                allowReserved: options.allowReserved,
                explode: true,
                name,
                style: "deepObject",
                value,
                ...options.object
              });
              if (serializedObject)
                search.push(serializedObject);
            } else {
              const serializedPrimitive = serializePrimitiveParam({
                allowReserved: options.allowReserved,
                name,
                value
              });
              if (serializedPrimitive)
                search.push(serializedPrimitive);
            }
          }
        }
        return search.join("&");
      };
      return querySerializer;
    };
    var getParseAs = (contentType) => {
      if (!contentType) {
        return "stream";
      }
      const cleanContent = contentType.split(";")[0]?.trim();
      if (!cleanContent) {
        return;
      }
      if (cleanContent.startsWith("application/json") || cleanContent.endsWith("+json")) {
        return "json";
      }
      if (cleanContent === "multipart/form-data") {
        return "formData";
      }
      if (["application/", "audio/", "image/", "video/"].some((type) => cleanContent.startsWith(type))) {
        return "blob";
      }
      if (cleanContent.startsWith("text/")) {
        return "text";
      }
      return;
    };
    var checkForExistence = (options, name) => {
      if (!name) {
        return false;
      }
      if (options.headers.has(name) || options.query?.[name] || options.headers.get("Cookie")?.includes(`${name}=`)) {
        return true;
      }
      return false;
    };
    var setAuthParams = async ({ security, ...options }) => {
      for (const auth of security) {
        if (checkForExistence(options, auth.name)) {
          continue;
        }
        const token = await getAuthToken(auth, options.auth);
        if (!token) {
          continue;
        }
        const name = auth.name ?? "Authorization";
        switch (auth.in) {
          case "query":
            if (!options.query) {
              options.query = {};
            }
            options.query[name] = token;
            break;
          case "cookie":
            options.headers.append("Cookie", `${name}=${token}`);
            break;
          case "header":
          default:
            options.headers.set(name, token);
            break;
        }
      }
    };
    var buildUrl = (options) => getUrl({
      baseUrl: options.baseUrl,
      path: options.path,
      query: options.query,
      querySerializer: typeof options.querySerializer === "function" ? options.querySerializer : createQuerySerializer(options.querySerializer),
      url: options.url
    });
    var mergeConfigs = (a, b) => {
      const config = { ...a, ...b };
      if (config.baseUrl?.endsWith("/")) {
        config.baseUrl = config.baseUrl.substring(0, config.baseUrl.length - 1);
      }
      config.headers = mergeHeaders(a.headers, b.headers);
      return config;
    };
    var headersEntries = (headers) => {
      const entries = [];
      headers.forEach((value, key) => {
        entries.push([key, value]);
      });
      return entries;
    };
    var mergeHeaders = (...headers) => {
      const mergedHeaders = new Headers();
      for (const header of headers) {
        if (!header) {
          continue;
        }
        const iterator = header instanceof Headers ? headersEntries(header) : Object.entries(header);
        for (const [key, value] of iterator) {
          if (value === null) {
            mergedHeaders.delete(key);
          } else if (Array.isArray(value)) {
            for (const v of value) {
              mergedHeaders.append(key, v);
            }
          } else if (value !== void 0) {
            mergedHeaders.set(key, typeof value === "object" ? JSON.stringify(value) : value);
          }
        }
      }
      return mergedHeaders;
    };
    var Interceptors = class {
      constructor() {
        __publicField(this, "fns", []);
      }
      clear() {
        this.fns = [];
      }
      eject(id) {
        const index = this.getInterceptorIndex(id);
        if (this.fns[index]) {
          this.fns[index] = null;
        }
      }
      exists(id) {
        const index = this.getInterceptorIndex(id);
        return Boolean(this.fns[index]);
      }
      getInterceptorIndex(id) {
        if (typeof id === "number") {
          return this.fns[id] ? id : -1;
        }
        return this.fns.indexOf(id);
      }
      update(id, fn) {
        const index = this.getInterceptorIndex(id);
        if (this.fns[index]) {
          this.fns[index] = fn;
          return id;
        }
        return false;
      }
      use(fn) {
        this.fns.push(fn);
        return this.fns.length - 1;
      }
    };
    var createInterceptors = () => ({
      error: new Interceptors(),
      request: new Interceptors(),
      response: new Interceptors()
    });
    var defaultQuerySerializer = createQuerySerializer({
      allowReserved: false,
      array: {
        explode: true,
        style: "form"
      },
      object: {
        explode: true,
        style: "deepObject"
      }
    });
    var defaultHeaders = {
      "Content-Type": "application/json"
    };
    var createConfig = (override = {}) => ({
      ...jsonBodySerializer,
      headers: defaultHeaders,
      parseAs: "auto",
      querySerializer: defaultQuerySerializer,
      ...override
    });
    var createClient = (config = {}) => {
      let _config = mergeConfigs(createConfig(), config);
      const getConfig = () => ({ ..._config });
      const setConfig = (config2) => {
        _config = mergeConfigs(_config, config2);
        return getConfig();
      };
      const interceptors = createInterceptors();
      const beforeRequest = async (options) => {
        const opts = {
          ..._config,
          ...options,
          fetch: options.fetch ?? _config.fetch ?? globalThis.fetch,
          headers: mergeHeaders(_config.headers, options.headers),
          serializedBody: void 0
        };
        if (opts.security) {
          await setAuthParams({
            ...opts,
            security: opts.security
          });
        }
        if (opts.requestValidator) {
          await opts.requestValidator(opts);
        }
        if (opts.body !== void 0 && opts.bodySerializer) {
          opts.serializedBody = opts.bodySerializer(opts.body);
        }
        if (opts.body === void 0 || opts.serializedBody === "") {
          opts.headers.delete("Content-Type");
        }
        const url = buildUrl(opts);
        return { opts, url };
      };
      const request = async (options) => {
        const { opts, url } = await beforeRequest(options);
        const requestInit = {
          redirect: "follow",
          ...opts,
          body: getValidRequestBody(opts)
        };
        let request2 = new Request(url, requestInit);
        for (const fn of interceptors.request.fns) {
          if (fn) {
            request2 = await fn(request2, opts);
          }
        }
        const _fetch = opts.fetch;
        let response;
        try {
          response = await _fetch(request2);
        } catch (error2) {
          let finalError2 = error2;
          for (const fn of interceptors.error.fns) {
            if (fn) {
              finalError2 = await fn(error2, void 0, request2, opts);
            }
          }
          finalError2 = finalError2 || {};
          if (opts.throwOnError) {
            throw finalError2;
          }
          return opts.responseStyle === "data" ? void 0 : {
            error: finalError2,
            request: request2,
            response: void 0
          };
        }
        for (const fn of interceptors.response.fns) {
          if (fn) {
            response = await fn(response, request2, opts);
          }
        }
        const result = {
          request: request2,
          response
        };
        if (response.ok) {
          const parseAs = (opts.parseAs === "auto" ? getParseAs(response.headers.get("Content-Type")) : opts.parseAs) ?? "json";
          if (response.status === 204 || response.headers.get("Content-Length") === "0") {
            let emptyData;
            switch (parseAs) {
              case "arrayBuffer":
              case "blob":
              case "text":
                emptyData = await response[parseAs]();
                break;
              case "formData":
                emptyData = new FormData();
                break;
              case "stream":
                emptyData = response.body;
                break;
              case "json":
              default:
                emptyData = {};
                break;
            }
            return opts.responseStyle === "data" ? emptyData : {
              data: emptyData,
              ...result
            };
          }
          let data;
          switch (parseAs) {
            case "arrayBuffer":
            case "blob":
            case "formData":
            case "text":
              data = await response[parseAs]();
              break;
            case "json": {
              const text = await response.text();
              data = text ? JSON.parse(text) : {};
              break;
            }
            case "stream":
              return opts.responseStyle === "data" ? response.body : {
                data: response.body,
                ...result
              };
          }
          if (parseAs === "json") {
            if (opts.responseValidator) {
              await opts.responseValidator(data);
            }
            if (opts.responseTransformer) {
              data = await opts.responseTransformer(data);
            }
          }
          return opts.responseStyle === "data" ? data : {
            data,
            ...result
          };
        }
        const textError = await response.text();
        let jsonError;
        try {
          jsonError = JSON.parse(textError);
        } catch {
        }
        const error = jsonError ?? textError;
        let finalError = error;
        for (const fn of interceptors.error.fns) {
          if (fn) {
            finalError = await fn(error, response, request2, opts);
          }
        }
        finalError = finalError || {};
        if (opts.throwOnError) {
          throw finalError;
        }
        return opts.responseStyle === "data" ? void 0 : {
          error: finalError,
          ...result
        };
      };
      const makeMethodFn = (method) => (options) => request({ ...options, method });
      const makeSseFn = (method) => async (options) => {
        const { opts, url } = await beforeRequest(options);
        return createSseClient({
          ...opts,
          body: opts.body,
          headers: opts.headers,
          method,
          onRequest: async (url2, init) => {
            let request2 = new Request(url2, init);
            for (const fn of interceptors.request.fns) {
              if (fn) {
                request2 = await fn(request2, opts);
              }
            }
            return request2;
          },
          serializedBody: getValidRequestBody(opts),
          url
        });
      };
      return {
        buildUrl,
        connect: makeMethodFn("CONNECT"),
        delete: makeMethodFn("DELETE"),
        get: makeMethodFn("GET"),
        getConfig,
        head: makeMethodFn("HEAD"),
        interceptors,
        options: makeMethodFn("OPTIONS"),
        patch: makeMethodFn("PATCH"),
        post: makeMethodFn("POST"),
        put: makeMethodFn("PUT"),
        request,
        setConfig,
        sse: {
          connect: makeSseFn("CONNECT"),
          delete: makeSseFn("DELETE"),
          get: makeSseFn("GET"),
          head: makeSseFn("HEAD"),
          options: makeSseFn("OPTIONS"),
          patch: makeSseFn("PATCH"),
          post: makeSseFn("POST"),
          put: makeSseFn("PUT"),
          trace: makeSseFn("TRACE")
        },
        trace: makeMethodFn("TRACE")
      };
    };
    var extraPrefixesMap = {
      $body_: "body",
      $headers_: "headers",
      $path_: "path",
      $query_: "query"
    };
    var extraPrefixes = Object.entries(extraPrefixesMap);
    var buildKeyMap = (fields, map) => {
      if (!map) {
        map = /* @__PURE__ */ new Map();
      }
      for (const config of fields) {
        if ("in" in config) {
          if (config.key) {
            map.set(config.key, {
              in: config.in,
              map: config.map
            });
          }
        } else if ("key" in config) {
          map.set(config.key, {
            map: config.map
          });
        } else if (config.args) {
          buildKeyMap(config.args, map);
        }
      }
      return map;
    };
    var stripEmptySlots = (params) => {
      for (const [slot, value] of Object.entries(params)) {
        if (value && typeof value === "object" && !Object.keys(value).length) {
          delete params[slot];
        }
      }
    };
    var buildClientParams = (args, fields) => {
      const params = {
        body: {},
        headers: {},
        path: {},
        query: {}
      };
      const map = buildKeyMap(fields);
      let config;
      for (const [index, arg] of args.entries()) {
        if (fields[index]) {
          config = fields[index];
        }
        if (!config) {
          continue;
        }
        if ("in" in config) {
          if (config.key) {
            const field = map.get(config.key);
            const name = field.map || config.key;
            if (field.in) {
              ;
              params[field.in][name] = arg;
            }
          } else {
            params.body = arg;
          }
        } else {
          for (const [key, value] of Object.entries(arg ?? {})) {
            const field = map.get(key);
            if (field) {
              if (field.in) {
                const name = field.map || key;
                params[field.in][name] = value;
              } else {
                params[field.map] = value;
              }
            } else {
              const extra = extraPrefixes.find(([prefix]) => key.startsWith(prefix));
              if (extra) {
                const [prefix, slot] = extra;
                params[slot][key.slice(prefix.length)] = value;
              } else if ("allowExtra" in config && config.allowExtra) {
                for (const [slot, allowed] of Object.entries(config.allowExtra)) {
                  if (allowed) {
                    ;
                    params[slot][key] = value;
                    break;
                  }
                }
              }
            }
          }
        }
      }
      stripEmptySlots(params);
      return params;
    };
    var client = createClient(createConfig({ baseUrl: "http://localhost:4096" }));
    var HeyApiClient = class {
      constructor(args) {
        __publicField(this, "client");
        this.client = args?.client ?? client;
      }
    };
    var HeyApiRegistry = class {
      constructor() {
        __publicField(this, "defaultKey", "default");
        __publicField(this, "instances", /* @__PURE__ */ new Map());
      }
      get(key) {
        const instance = this.instances.get(key ?? this.defaultKey);
        if (!instance) {
          throw new Error(`No SDK client found. Create one with "new OpencodeClient()" to fix this error.`);
        }
        return instance;
      }
      set(value, key) {
        this.instances.set(key ?? this.defaultKey, value);
      }
    };
    var Config = class extends HeyApiClient {
      /**
       * Get global configuration
       *
       * Retrieve the current global OpenCode configuration settings and preferences.
       */
      get(options) {
        return (options?.client ?? this.client).get({
          url: "/global/config",
          ...options
        });
      }
      /**
       * Update global configuration
       *
       * Update global OpenCode configuration settings and preferences.
       */
      update(parameters, options) {
        const params = buildClientParams([parameters], [{ args: [{ key: "config", map: "body" }] }]);
        return (options?.client ?? this.client).patch({
          url: "/global/config",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
    };
    var Global = class extends HeyApiClient {
      constructor() {
        super(...arguments);
        __publicField(this, "_config");
      }
      /**
       * Get health
       *
       * Get health information about the OpenCode server.
       */
      health(options) {
        return (options?.client ?? this.client).get({
          url: "/global/health",
          ...options
        });
      }
      /**
       * Get global events
       *
       * Subscribe to global events from the OpenCode system using server-sent events.
       */
      event(options) {
        return (options?.client ?? this.client).sse.get({
          url: "/global/event",
          ...options
        });
      }
      /**
       * Dispose instance
       *
       * Clean up and dispose all OpenCode instances, releasing all resources.
       */
      dispose(options) {
        return (options?.client ?? this.client).post({
          url: "/global/dispose",
          ...options
        });
      }
      get config() {
        return this._config ?? (this._config = new Config({ client: this.client }));
      }
    };
    var Auth = class extends HeyApiClient {
      /**
       * Remove auth credentials
       *
       * Remove authentication credentials
       */
      remove(parameters, options) {
        const params = buildClientParams([parameters], [{ args: [{ in: "path", key: "providerID" }] }]);
        return (options?.client ?? this.client).delete({
          url: "/auth/{providerID}",
          ...options,
          ...params
        });
      }
      /**
       * Set auth credentials
       *
       * Set authentication credentials
       */
      set(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "providerID" },
              { key: "auth", map: "body" }
            ]
          }
        ]);
        return (options?.client ?? this.client).put({
          url: "/auth/{providerID}",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
    };
    var Project = class extends HeyApiClient {
      /**
       * List all projects
       *
       * Get a list of projects that have been opened with OpenCode.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/project",
          ...options,
          ...params
        });
      }
      /**
       * Get current project
       *
       * Retrieve the currently active project that OpenCode is working with.
       */
      current(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/project/current",
          ...options,
          ...params
        });
      }
      /**
       * Update project
       *
       * Update project properties such as name, icon, and commands.
       */
      update(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "projectID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "name" },
              { in: "body", key: "icon" },
              { in: "body", key: "commands" }
            ]
          }
        ]);
        return (options?.client ?? this.client).patch({
          url: "/project/{projectID}",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
    };
    var Pty = class extends HeyApiClient {
      /**
       * List PTY sessions
       *
       * Get a list of all active pseudo-terminal (PTY) sessions managed by OpenCode.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/pty",
          ...options,
          ...params
        });
      }
      /**
       * Create PTY session
       *
       * Create a new pseudo-terminal (PTY) session for running shell commands and processes.
       */
      create(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "command" },
              { in: "body", key: "args" },
              { in: "body", key: "cwd" },
              { in: "body", key: "title" },
              { in: "body", key: "env" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/pty",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Remove PTY session
       *
       * Remove and terminate a specific pseudo-terminal (PTY) session.
       */
      remove(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "ptyID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).delete({
          url: "/pty/{ptyID}",
          ...options,
          ...params
        });
      }
      /**
       * Get PTY session
       *
       * Retrieve detailed information about a specific pseudo-terminal (PTY) session.
       */
      get(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "ptyID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/pty/{ptyID}",
          ...options,
          ...params
        });
      }
      /**
       * Update PTY session
       *
       * Update properties of an existing pseudo-terminal (PTY) session.
       */
      update(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "ptyID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "title" },
              { in: "body", key: "size" }
            ]
          }
        ]);
        return (options?.client ?? this.client).put({
          url: "/pty/{ptyID}",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Connect to PTY session
       *
       * Establish a WebSocket connection to interact with a pseudo-terminal (PTY) session in real-time.
       */
      connect(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "ptyID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/pty/{ptyID}/connect",
          ...options,
          ...params
        });
      }
    };
    var Config2 = class extends HeyApiClient {
      /**
       * Get configuration
       *
       * Retrieve the current OpenCode configuration settings and preferences.
       */
      get(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/config",
          ...options,
          ...params
        });
      }
      /**
       * Update configuration
       *
       * Update OpenCode configuration settings and preferences.
       */
      update(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { key: "config", map: "body" }
            ]
          }
        ]);
        return (options?.client ?? this.client).patch({
          url: "/config",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * List config providers
       *
       * Get a list of all configured AI providers and their default models.
       */
      providers(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/config/providers",
          ...options,
          ...params
        });
      }
    };
    var Tool = class extends HeyApiClient {
      /**
       * List tool IDs
       *
       * Get a list of all available tool IDs, including both built-in tools and dynamically registered tools.
       */
      ids(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/experimental/tool/ids",
          ...options,
          ...params
        });
      }
      /**
       * List tools
       *
       * Get a list of available tools with their JSON schema parameters for a specific provider and model combination.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "provider" },
              { in: "query", key: "model" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/experimental/tool",
          ...options,
          ...params
        });
      }
    };
    var Worktree = class extends HeyApiClient {
      /**
       * Remove worktree
       *
       * Remove a git worktree and delete its branch.
       */
      remove(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { key: "worktreeRemoveInput", map: "body" }
            ]
          }
        ]);
        return (options?.client ?? this.client).delete({
          url: "/experimental/worktree",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * List worktrees
       *
       * List all sandbox worktrees for the current project.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/experimental/worktree",
          ...options,
          ...params
        });
      }
      /**
       * Create worktree
       *
       * Create a new git worktree for the current project and run any configured startup scripts.
       */
      create(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { key: "worktreeCreateInput", map: "body" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/experimental/worktree",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Reset worktree
       *
       * Reset a worktree branch to the primary default branch.
       */
      reset(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { key: "worktreeResetInput", map: "body" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/experimental/worktree/reset",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
    };
    var Workspace = class extends HeyApiClient {
      /**
       * Remove workspace
       *
       * Remove an existing workspace.
       */
      remove(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "id" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).delete({
          url: "/experimental/workspace/{id}",
          ...options,
          ...params
        });
      }
      /**
       * Create workspace
       *
       * Create a workspace for the current project.
       */
      create(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "id" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "branch" },
              { in: "body", key: "config" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/experimental/workspace/{id}",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * List workspaces
       *
       * List all workspaces.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/experimental/workspace",
          ...options,
          ...params
        });
      }
    };
    var Session = class extends HeyApiClient {
      /**
       * List sessions
       *
       * Get a list of all OpenCode sessions across projects, sorted by most recently updated. Archived sessions are excluded by default.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "roots" },
              { in: "query", key: "start" },
              { in: "query", key: "cursor" },
              { in: "query", key: "search" },
              { in: "query", key: "limit" },
              { in: "query", key: "archived" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/experimental/session",
          ...options,
          ...params
        });
      }
    };
    var Resource = class extends HeyApiClient {
      /**
       * Get MCP resources
       *
       * Get all available MCP resources from connected servers. Optionally filter by name.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/experimental/resource",
          ...options,
          ...params
        });
      }
    };
    var Experimental = class extends HeyApiClient {
      constructor() {
        super(...arguments);
        __publicField(this, "_workspace");
        __publicField(this, "_session");
        __publicField(this, "_resource");
      }
      get workspace() {
        return this._workspace ?? (this._workspace = new Workspace({ client: this.client }));
      }
      get session() {
        return this._session ?? (this._session = new Session({ client: this.client }));
      }
      get resource() {
        return this._resource ?? (this._resource = new Resource({ client: this.client }));
      }
    };
    var Session2 = class extends HeyApiClient {
      /**
       * List sessions
       *
       * Get a list of all OpenCode sessions, sorted by most recently updated.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "roots" },
              { in: "query", key: "start" },
              { in: "query", key: "search" },
              { in: "query", key: "limit" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/session",
          ...options,
          ...params
        });
      }
      /**
       * Create session
       *
       * Create a new OpenCode session for interacting with AI assistants and managing conversations.
       */
      create(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "parentID" },
              { in: "body", key: "title" },
              { in: "body", key: "permission" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Get session status
       *
       * Retrieve the current status of all sessions, including active, idle, and completed states.
       */
      status(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/session/status",
          ...options,
          ...params
        });
      }
      /**
       * Delete session
       *
       * Delete a session and permanently remove all associated data, including messages and history.
       */
      delete(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).delete({
          url: "/session/{sessionID}",
          ...options,
          ...params
        });
      }
      /**
       * Get session
       *
       * Retrieve detailed information about a specific OpenCode session.
       */
      get(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/session/{sessionID}",
          ...options,
          ...params
        });
      }
      /**
       * Update session
       *
       * Update properties of an existing session, such as title or other metadata.
       */
      update(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "title" },
              { in: "body", key: "time" }
            ]
          }
        ]);
        return (options?.client ?? this.client).patch({
          url: "/session/{sessionID}",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Get session children
       *
       * Retrieve all child sessions that were forked from the specified parent session.
       */
      children(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/session/{sessionID}/children",
          ...options,
          ...params
        });
      }
      /**
       * Get session todos
       *
       * Retrieve the todo list associated with a specific session, showing tasks and action items.
       */
      todo(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/session/{sessionID}/todo",
          ...options,
          ...params
        });
      }
      /**
       * Initialize session
       *
       * Analyze the current application and create an AGENTS.md file with project-specific agent configurations.
       */
      init(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "modelID" },
              { in: "body", key: "providerID" },
              { in: "body", key: "messageID" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/init",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Fork session
       *
       * Create a new session by forking an existing session at a specific message point.
       */
      fork(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "messageID" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/fork",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Abort session
       *
       * Abort an active session and stop any ongoing AI processing or command execution.
       */
      abort(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/abort",
          ...options,
          ...params
        });
      }
      /**
       * Unshare session
       *
       * Remove the shareable link for a session, making it private again.
       */
      unshare(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).delete({
          url: "/session/{sessionID}/share",
          ...options,
          ...params
        });
      }
      /**
       * Share session
       *
       * Create a shareable link for a session, allowing others to view the conversation.
       */
      share(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/share",
          ...options,
          ...params
        });
      }
      /**
       * Get message diff
       *
       * Get the file changes (diff) that resulted from a specific user message in the session.
       */
      diff(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "messageID" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/session/{sessionID}/diff",
          ...options,
          ...params
        });
      }
      /**
       * Summarize session
       *
       * Generate a concise summary of the session using AI compaction to preserve key information.
       */
      summarize(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "providerID" },
              { in: "body", key: "modelID" },
              { in: "body", key: "auto" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/summarize",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Get session messages
       *
       * Retrieve all messages in a session, including user prompts and AI responses.
       */
      messages(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "limit" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/session/{sessionID}/message",
          ...options,
          ...params
        });
      }
      /**
       * Send message
       *
       * Create and send a new message to a session, streaming the AI response.
       */
      prompt(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "messageID" },
              { in: "body", key: "model" },
              { in: "body", key: "agent" },
              { in: "body", key: "noReply" },
              { in: "body", key: "tools" },
              { in: "body", key: "format" },
              { in: "body", key: "system" },
              { in: "body", key: "variant" },
              { in: "body", key: "parts" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/message",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Delete message
       *
       * Permanently delete a specific message (and all of its parts) from a session. This does not revert any file changes that may have been made while processing the message.
       */
      deleteMessage(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "path", key: "messageID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).delete({
          url: "/session/{sessionID}/message/{messageID}",
          ...options,
          ...params
        });
      }
      /**
       * Get message
       *
       * Retrieve a specific message from a session by its message ID.
       */
      message(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "path", key: "messageID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/session/{sessionID}/message/{messageID}",
          ...options,
          ...params
        });
      }
      /**
       * Send async message
       *
       * Create and send a new message to a session asynchronously, starting the session if needed and returning immediately.
       */
      promptAsync(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "messageID" },
              { in: "body", key: "model" },
              { in: "body", key: "agent" },
              { in: "body", key: "noReply" },
              { in: "body", key: "tools" },
              { in: "body", key: "format" },
              { in: "body", key: "system" },
              { in: "body", key: "variant" },
              { in: "body", key: "parts" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/prompt_async",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Send command
       *
       * Send a new command to a session for execution by the AI assistant.
       */
      command(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "messageID" },
              { in: "body", key: "agent" },
              { in: "body", key: "model" },
              { in: "body", key: "arguments" },
              { in: "body", key: "command" },
              { in: "body", key: "variant" },
              { in: "body", key: "parts" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/command",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Run shell command
       *
       * Execute a shell command within the session context and return the AI's response.
       */
      shell(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "agent" },
              { in: "body", key: "model" },
              { in: "body", key: "command" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/shell",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Revert message
       *
       * Revert a specific message in a session, undoing its effects and restoring the previous state.
       */
      revert(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "messageID" },
              { in: "body", key: "partID" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/revert",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Restore reverted messages
       *
       * Restore all previously reverted messages in a session.
       */
      unrevert(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/unrevert",
          ...options,
          ...params
        });
      }
    };
    var Part = class extends HeyApiClient {
      /**
       * Delete a part from a message
       */
      delete(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "path", key: "messageID" },
              { in: "path", key: "partID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).delete({
          url: "/session/{sessionID}/message/{messageID}/part/{partID}",
          ...options,
          ...params
        });
      }
      /**
       * Update a part in a message
       */
      update(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "path", key: "messageID" },
              { in: "path", key: "partID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { key: "part", map: "body" }
            ]
          }
        ]);
        return (options?.client ?? this.client).patch({
          url: "/session/{sessionID}/message/{messageID}/part/{partID}",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
    };
    var Permission = class extends HeyApiClient {
      /**
       * Respond to permission
       *
       * Approve or deny a permission request from the AI assistant.
       *
       * @deprecated
       */
      respond(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "sessionID" },
              { in: "path", key: "permissionID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "response" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/session/{sessionID}/permissions/{permissionID}",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Respond to permission request
       *
       * Approve or deny a permission request from the AI assistant.
       */
      reply(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "requestID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "reply" },
              { in: "body", key: "message" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/permission/{requestID}/reply",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * List pending permissions
       *
       * Get all pending permission requests across all sessions.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/permission",
          ...options,
          ...params
        });
      }
    };
    var Question = class extends HeyApiClient {
      /**
       * List pending questions
       *
       * Get all pending question requests across all sessions.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/question",
          ...options,
          ...params
        });
      }
      /**
       * Reply to question request
       *
       * Provide answers to a question request from the AI assistant.
       */
      reply(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "requestID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "answers" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/question/{requestID}/reply",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Reject question request
       *
       * Reject a question request from the AI assistant.
       */
      reject(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "requestID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/question/{requestID}/reject",
          ...options,
          ...params
        });
      }
    };
    var Oauth = class extends HeyApiClient {
      /**
       * OAuth authorize
       *
       * Initiate OAuth authorization for a specific AI provider to get an authorization URL.
       */
      authorize(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "providerID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "method" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/provider/{providerID}/oauth/authorize",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * OAuth callback
       *
       * Handle the OAuth callback from a provider after user authorization.
       */
      callback(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "providerID" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "method" },
              { in: "body", key: "code" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/provider/{providerID}/oauth/callback",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
    };
    var Provider = class extends HeyApiClient {
      constructor() {
        super(...arguments);
        __publicField(this, "_oauth");
      }
      /**
       * List providers
       *
       * Get a list of all available AI providers, including both available and connected ones.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/provider",
          ...options,
          ...params
        });
      }
      /**
       * Get provider auth methods
       *
       * Retrieve available authentication methods for all AI providers.
       */
      auth(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/provider/auth",
          ...options,
          ...params
        });
      }
      get oauth() {
        return this._oauth ?? (this._oauth = new Oauth({ client: this.client }));
      }
    };
    var Find = class extends HeyApiClient {
      /**
       * Find text
       *
       * Search for text patterns across files in the project using ripgrep.
       */
      text(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "pattern" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/find",
          ...options,
          ...params
        });
      }
      /**
       * Find files
       *
       * Search for files or directories by name or pattern in the project directory.
       */
      files(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "query" },
              { in: "query", key: "dirs" },
              { in: "query", key: "type" },
              { in: "query", key: "limit" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/find/file",
          ...options,
          ...params
        });
      }
      /**
       * Find symbols
       *
       * Search for workspace symbols like functions, classes, and variables using LSP.
       */
      symbols(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "query" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/find/symbol",
          ...options,
          ...params
        });
      }
    };
    var File = class extends HeyApiClient {
      /**
       * List files
       *
       * List files and directories in a specified path.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "path" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/file",
          ...options,
          ...params
        });
      }
      /**
       * Read file
       *
       * Read the content of a specified file.
       */
      read(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "query", key: "path" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/file/content",
          ...options,
          ...params
        });
      }
      /**
       * Get file status
       *
       * Get the git status of all files in the project.
       */
      status(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/file/status",
          ...options,
          ...params
        });
      }
    };
    var Auth2 = class extends HeyApiClient {
      /**
       * Remove MCP OAuth
       *
       * Remove OAuth credentials for an MCP server
       */
      remove(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "name" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).delete({
          url: "/mcp/{name}/auth",
          ...options,
          ...params
        });
      }
      /**
       * Start MCP OAuth
       *
       * Start OAuth authentication flow for a Model Context Protocol (MCP) server.
       */
      start(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "name" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/mcp/{name}/auth",
          ...options,
          ...params
        });
      }
      /**
       * Complete MCP OAuth
       *
       * Complete OAuth authentication for a Model Context Protocol (MCP) server using the authorization code.
       */
      callback(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "name" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "code" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/mcp/{name}/auth/callback",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Authenticate MCP OAuth
       *
       * Start OAuth flow and wait for callback (opens browser)
       */
      authenticate(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "name" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/mcp/{name}/auth/authenticate",
          ...options,
          ...params
        });
      }
    };
    var Mcp = class extends HeyApiClient {
      constructor() {
        super(...arguments);
        __publicField(this, "_auth");
      }
      /**
       * Get MCP status
       *
       * Get the status of all Model Context Protocol (MCP) servers.
       */
      status(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/mcp",
          ...options,
          ...params
        });
      }
      /**
       * Add MCP server
       *
       * Dynamically add a new Model Context Protocol (MCP) server to the system.
       */
      add(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "name" },
              { in: "body", key: "config" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/mcp",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Connect an MCP server
       */
      connect(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "name" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/mcp/{name}/connect",
          ...options,
          ...params
        });
      }
      /**
       * Disconnect an MCP server
       */
      disconnect(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "path", key: "name" },
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/mcp/{name}/disconnect",
          ...options,
          ...params
        });
      }
      get auth() {
        return this._auth ?? (this._auth = new Auth2({ client: this.client }));
      }
    };
    var Control = class extends HeyApiClient {
      /**
       * Get next TUI request
       *
       * Retrieve the next TUI (Terminal User Interface) request from the queue for processing.
       */
      next(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/tui/control/next",
          ...options,
          ...params
        });
      }
      /**
       * Submit TUI response
       *
       * Submit a response to the TUI request queue to complete a pending request.
       */
      response(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { key: "body", map: "body" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/control/response",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
    };
    var Tui = class extends HeyApiClient {
      constructor() {
        super(...arguments);
        __publicField(this, "_control");
      }
      /**
       * Append TUI prompt
       *
       * Append prompt to the TUI
       */
      appendPrompt(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "text" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/append-prompt",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Open help dialog
       *
       * Open the help dialog in the TUI to display user assistance information.
       */
      openHelp(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/open-help",
          ...options,
          ...params
        });
      }
      /**
       * Open sessions dialog
       *
       * Open the session dialog
       */
      openSessions(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/open-sessions",
          ...options,
          ...params
        });
      }
      /**
       * Open themes dialog
       *
       * Open the theme dialog
       */
      openThemes(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/open-themes",
          ...options,
          ...params
        });
      }
      /**
       * Open models dialog
       *
       * Open the model dialog
       */
      openModels(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/open-models",
          ...options,
          ...params
        });
      }
      /**
       * Submit TUI prompt
       *
       * Submit the prompt
       */
      submitPrompt(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/submit-prompt",
          ...options,
          ...params
        });
      }
      /**
       * Clear TUI prompt
       *
       * Clear the prompt
       */
      clearPrompt(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/clear-prompt",
          ...options,
          ...params
        });
      }
      /**
       * Execute TUI command
       *
       * Execute a TUI command (e.g. agent_cycle)
       */
      executeCommand(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "command" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/execute-command",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Show TUI toast
       *
       * Show a toast notification in the TUI
       */
      showToast(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "title" },
              { in: "body", key: "message" },
              { in: "body", key: "variant" },
              { in: "body", key: "duration" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/show-toast",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Publish TUI event
       *
       * Publish a TUI event
       */
      publish(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { key: "body", map: "body" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/publish",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * Select session
       *
       * Navigate the TUI to display the specified session.
       */
      selectSession(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "sessionID" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/tui/select-session",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      get control() {
        return this._control ?? (this._control = new Control({ client: this.client }));
      }
    };
    var Instance = class extends HeyApiClient {
      /**
       * Dispose instance
       *
       * Clean up and dispose the current OpenCode instance, releasing all resources.
       */
      dispose(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/instance/dispose",
          ...options,
          ...params
        });
      }
    };
    var Path = class extends HeyApiClient {
      /**
       * Get paths
       *
       * Retrieve the current working directory and related path information for the OpenCode instance.
       */
      get(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/path",
          ...options,
          ...params
        });
      }
    };
    var Vcs = class extends HeyApiClient {
      /**
       * Get VCS info
       *
       * Retrieve version control system (VCS) information for the current project, such as git branch.
       */
      get(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/vcs",
          ...options,
          ...params
        });
      }
    };
    var Command = class extends HeyApiClient {
      /**
       * List commands
       *
       * Get a list of all available commands in the OpenCode system.
       */
      list(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/command",
          ...options,
          ...params
        });
      }
    };
    var App = class extends HeyApiClient {
      /**
       * Write log
       *
       * Write a log entry to the server logs with specified level and metadata.
       */
      log(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" },
              { in: "body", key: "service" },
              { in: "body", key: "level" },
              { in: "body", key: "message" },
              { in: "body", key: "extra" }
            ]
          }
        ]);
        return (options?.client ?? this.client).post({
          url: "/log",
          ...options,
          ...params,
          headers: {
            "Content-Type": "application/json",
            ...options?.headers,
            ...params.headers
          }
        });
      }
      /**
       * List agents
       *
       * Get a list of all available AI agents in the OpenCode system.
       */
      agents(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/agent",
          ...options,
          ...params
        });
      }
      /**
       * List skills
       *
       * Get a list of all available skills in the OpenCode system.
       */
      skills(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/skill",
          ...options,
          ...params
        });
      }
    };
    var Lsp = class extends HeyApiClient {
      /**
       * Get LSP status
       *
       * Get LSP server status
       */
      status(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/lsp",
          ...options,
          ...params
        });
      }
    };
    var Formatter = class extends HeyApiClient {
      /**
       * Get formatter status
       *
       * Get formatter status
       */
      status(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).get({
          url: "/formatter",
          ...options,
          ...params
        });
      }
    };
    var Event = class extends HeyApiClient {
      /**
       * Subscribe to events
       *
       * Get events
       */
      subscribe(parameters, options) {
        const params = buildClientParams([parameters], [
          {
            args: [
              { in: "query", key: "directory" },
              { in: "query", key: "workspace" }
            ]
          }
        ]);
        return (options?.client ?? this.client).sse.get({
          url: "/event",
          ...options,
          ...params
        });
      }
    };
    var _OpencodeClient = class _OpencodeClient2 extends HeyApiClient {
      constructor(args) {
        super(args);
        __publicField(this, "_global");
        __publicField(this, "_auth");
        __publicField(this, "_project");
        __publicField(this, "_pty");
        __publicField(this, "_config");
        __publicField(this, "_tool");
        __publicField(this, "_worktree");
        __publicField(this, "_experimental");
        __publicField(this, "_session");
        __publicField(this, "_part");
        __publicField(this, "_permission");
        __publicField(this, "_question");
        __publicField(this, "_provider");
        __publicField(this, "_find");
        __publicField(this, "_file");
        __publicField(this, "_mcp");
        __publicField(this, "_tui");
        __publicField(this, "_instance");
        __publicField(this, "_path");
        __publicField(this, "_vcs");
        __publicField(this, "_command");
        __publicField(this, "_app");
        __publicField(this, "_lsp");
        __publicField(this, "_formatter");
        __publicField(this, "_event");
        _OpencodeClient2.__registry.set(this, args?.key);
      }
      get global() {
        return this._global ?? (this._global = new Global({ client: this.client }));
      }
      get auth() {
        return this._auth ?? (this._auth = new Auth({ client: this.client }));
      }
      get project() {
        return this._project ?? (this._project = new Project({ client: this.client }));
      }
      get pty() {
        return this._pty ?? (this._pty = new Pty({ client: this.client }));
      }
      get config() {
        return this._config ?? (this._config = new Config2({ client: this.client }));
      }
      get tool() {
        return this._tool ?? (this._tool = new Tool({ client: this.client }));
      }
      get worktree() {
        return this._worktree ?? (this._worktree = new Worktree({ client: this.client }));
      }
      get experimental() {
        return this._experimental ?? (this._experimental = new Experimental({ client: this.client }));
      }
      get session() {
        return this._session ?? (this._session = new Session2({ client: this.client }));
      }
      get part() {
        return this._part ?? (this._part = new Part({ client: this.client }));
      }
      get permission() {
        return this._permission ?? (this._permission = new Permission({ client: this.client }));
      }
      get question() {
        return this._question ?? (this._question = new Question({ client: this.client }));
      }
      get provider() {
        return this._provider ?? (this._provider = new Provider({ client: this.client }));
      }
      get find() {
        return this._find ?? (this._find = new Find({ client: this.client }));
      }
      get file() {
        return this._file ?? (this._file = new File({ client: this.client }));
      }
      get mcp() {
        return this._mcp ?? (this._mcp = new Mcp({ client: this.client }));
      }
      get tui() {
        return this._tui ?? (this._tui = new Tui({ client: this.client }));
      }
      get instance() {
        return this._instance ?? (this._instance = new Instance({ client: this.client }));
      }
      get path() {
        return this._path ?? (this._path = new Path({ client: this.client }));
      }
      get vcs() {
        return this._vcs ?? (this._vcs = new Vcs({ client: this.client }));
      }
      get command() {
        return this._command ?? (this._command = new Command({ client: this.client }));
      }
      get app() {
        return this._app ?? (this._app = new App({ client: this.client }));
      }
      get lsp() {
        return this._lsp ?? (this._lsp = new Lsp({ client: this.client }));
      }
      get formatter() {
        return this._formatter ?? (this._formatter = new Formatter({ client: this.client }));
      }
      get event() {
        return this._event ?? (this._event = new Event({ client: this.client }));
      }
    };
    __publicField(_OpencodeClient, "__registry", new HeyApiRegistry());
    var OpencodeClient = _OpencodeClient;
    function createOpencodeClient(config) {
      if (!config?.fetch) {
        const customFetch = (req) => {
          req.timeout = false;
          return fetch(req);
        };
        config = {
          ...config,
          fetch: customFetch
        };
      }
      if (config?.directory) {
        const isNonASCII = /[^\x00-\x7F]/.test(config.directory);
        const encodedDirectory = isNonASCII ? encodeURIComponent(config.directory) : config.directory;
        config.headers = {
          ...config.headers,
          "x-opencode-directory": encodedDirectory
        };
      }
      const client2 = createClient(config);
      return new OpencodeClient({ client: client2 });
    }
  }
});

// runtime/executable-resolver.js
var require_executable_resolver = __commonJS({
  "runtime/executable-resolver.js"(exports2, module2) {
    var fs = require("fs");
    var path = require("path");
    var os = require("os");
    var { execFile } = require("child_process");
    var { rt } = (() => {
      try {
        return require_runtime_locale_state();
      } catch (_e) {
        return {
          rt: (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
            const value = params[k];
            return value === void 0 || value === null ? "" : String(value);
          })
        };
      }
    })();
    function expandHome(p) {
      if (!p) return p;
      let out = String(p || "").trim();
      if (!out) return out;
      if (out === "~") out = os.homedir();
      else if (out.startsWith("~/")) out = path.join(os.homedir(), out.slice(2));
      out = out.replace(/%([^%]+)%/g, (full, name) => {
        const key = String(name || "").trim();
        if (!key) return full;
        return Object.prototype.hasOwnProperty.call(process.env, key) ? String(process.env[key]) : full;
      });
      return out;
    }
    function isWindowsCommandWrapperPath(filePath) {
      return /\.(cmd|bat)$/i.test(String(filePath || "").trim());
    }
    function isNodeScriptPath(filePath) {
      return /\.(mjs|cjs|js)$/i.test(String(filePath || "").trim());
    }
    function isRegularFile(filePath) {
      try {
        return fs.statSync(filePath).isFile();
      } catch {
        return false;
      }
    }
    function isLikelyNodeScriptFile(filePath) {
      const target = String(filePath || "").trim();
      if (!target || !isRegularFile(target)) return false;
      if (isNodeScriptPath(target)) return true;
      if (isWindowsCommandWrapperPath(target)) return false;
      try {
        const fd = fs.openSync(target, "r");
        try {
          const chunk = Buffer.alloc(160);
          const size = fs.readSync(fd, chunk, 0, chunk.length, 0);
          if (size <= 0) return false;
          const head = chunk.toString("utf8", 0, size);
          return /^\s*#!.*\bnode(?:\.exe)?\b/i.test(head);
        } finally {
          fs.closeSync(fd);
        }
      } catch {
        return false;
      }
    }
    function resolvesToWindowsExecutableAlias(filePath) {
      try {
        const resolved = fs.realpathSync(filePath);
        const ext = path.extname(String(resolved || "")).toLowerCase();
        return [".exe", ".com", ".cmd", ".bat", ".ps1"].includes(ext);
      } catch {
        return false;
      }
    }
    function isExecutable(filePath) {
      if (process.platform === "win32") {
        if (!isRegularFile(filePath)) return false;
        const ext = path.extname(String(filePath || "")).toLowerCase();
        if (!ext) return isLikelyNodeScriptFile(filePath) || resolvesToWindowsExecutableAlias(filePath);
        if (isNodeScriptPath(filePath)) return true;
        return [".exe", ".cmd", ".bat", ".com", ".ps1"].includes(ext);
      }
      try {
        fs.accessSync(filePath, fs.constants.X_OK);
        return true;
      } catch {
        return false;
      }
    }
    function normalizeWindowsWrapperScriptPath(rawScriptPath, wrapperPath) {
      const raw = String(rawScriptPath || "").trim().replace(/^["']+|["']+$/g, "");
      if (!raw) return "";
      const wrapperDir = path.dirname(String(wrapperPath || ""));
      if (!wrapperDir) return "";
      let candidate = raw.replace(/%~dp0/gi, `${wrapperDir}\\`);
      candidate = candidate.replace(/%dp0%/gi, `${wrapperDir}\\`);
      candidate = candidate.replace(/\\/g, path.sep).replace(/\//g, path.sep);
      if (!path.isAbsolute(candidate)) candidate = path.resolve(wrapperDir, candidate);
      return path.normalize(candidate);
    }
    function resolveWindowsWrapperNodeScript(wrapperPath) {
      const wrapper = String(wrapperPath || "").trim();
      if (!wrapper || !isWindowsCommandWrapperPath(wrapper)) return "";
      if (!fs.existsSync(wrapper)) return "";
      let content = "";
      try {
        content = String(fs.readFileSync(wrapper, "utf8") || "");
      } catch {
        return "";
      }
      if (!content) return "";
      const patterns = [
        /node(?:\.exe)?\s+"([^"\r\n]+?)"/gi,
        /"%~dp0\\node\.exe"\s+"([^"\r\n]+?)"/gi,
        /"%~dp0([^"\r\n]+?)"/gi,
        /%~dp0([^\s\r\n]+)/gi,
        /"([^"\r\n]*node_modules[^"\r\n]+?)"/gi
      ];
      for (const pattern of patterns) {
        let match = null;
        while ((match = pattern.exec(content)) !== null) {
          const candidate = normalizeWindowsWrapperScriptPath(match[1], wrapper);
          if (candidate && isLikelyNodeScriptFile(candidate)) return candidate;
        }
      }
      return "";
    }
    function splitPathEntries(envPath) {
      const sep = process.platform === "win32" ? ";" : ":";
      return String(envPath || "").split(sep).filter(Boolean);
    }
    function findInPath(binaryName) {
      const out = [];
      const name = String(binaryName || "").trim();
      if (!name) return out;
      const windowsExts = process.platform === "win32" ? String(process.env.PATHEXT || ".EXE;.CMD;.BAT;.COM").split(";").map((ext) => ext.trim().toLowerCase()).filter(Boolean) : [];
      const hasKnownWinExt = process.platform === "win32" && windowsExts.some((ext) => name.toLowerCase().endsWith(ext));
      for (const p of splitPathEntries(process.env.PATH)) {
        const base = path.join(p, name);
        out.push(base);
        if (process.platform === "win32") {
          if (!hasKnownWinExt) {
            windowsExts.forEach((ext) => out.push(`${base}${ext.toLowerCase()}`));
          }
        }
      }
      return out;
    }
    function which(binaryName) {
      const cmd = process.platform === "win32" ? "where" : "which";
      return new Promise((resolve) => {
        execFile(cmd, [binaryName], { timeout: 2e3 }, (error, stdout) => {
          if (error) return resolve([]);
          resolve(
            String(stdout || "").split(/\r?\n/).map((x) => x.trim()).filter(Boolean)
          );
        });
      });
    }
    function resolveNodeExecutablePath(scriptPath = "") {
      const candidates = [];
      if (process.platform === "win32") {
        const scriptDir = path.dirname(String(scriptPath || ""));
        if (scriptDir) candidates.push(path.join(scriptDir, "node.exe"));
        const home = os.homedir();
        const userProfile = process.env.USERPROFILE || home;
        const localAppData = process.env.LOCALAPPDATA || path.join(home, "AppData", "Local");
        const programFiles = process.env.ProgramFiles || "C:\\Program Files";
        const programFilesX86 = process.env["ProgramFiles(x86)"] || "C:\\Program Files (x86)";
        candidates.push(
          path.join(userProfile, "scoop", "apps", "nodejs-lts", "current", "node.exe"),
          path.join(userProfile, "scoop", "apps", "nodejs", "current", "node.exe"),
          path.join(programFiles, "nodejs", "node.exe"),
          path.join(programFilesX86, "nodejs", "node.exe"),
          path.join(localAppData, "Programs", "nodejs", "node.exe"),
          ...findInPath("node.exe"),
          ...findInPath("node")
        );
      } else {
        candidates.push(...findInPath("node"));
      }
      const normalized = [...new Set(candidates.map((item) => String(item || "").trim()).filter(Boolean))];
      const existing = normalized.find((candidate) => {
        if (!fs.existsSync(candidate)) return false;
        if (process.platform === "win32" && isWindowsCommandWrapperPath(candidate)) return false;
        return true;
      });
      if (existing) return existing;
      return process.platform === "win32" ? "node.exe" : "node";
    }
    function isWindowsWslPath(filePath) {
      const target = String(filePath || "").trim();
      if (!target) return false;
      const base = path.basename(target).toLowerCase();
      return base === "wsl" || base === "wsl.exe";
    }
    function windowsCandidatePriority(candidate) {
      const normalized = String(candidate || "").trim();
      const ext = path.extname(normalized).toLowerCase();
      if (isWindowsCommandWrapperPath(normalized)) return 50;
      if (ext === ".exe" || ext === ".com") return 0;
      if (isNodeScriptPath(normalized)) return 10;
      if (!ext) return 20;
      return 30;
    }
    var ExecutableResolver = class {
      buildPlatformCandidates() {
        if (process.platform !== "win32") {
          return [expandHome("~/.opencode/bin/opencode")];
        }
        const home = os.homedir();
        const appData = process.env.APPDATA || path.join(home, "AppData", "Roaming");
        const localAppData = process.env.LOCALAPPDATA || path.join(home, "AppData", "Local");
        const userProfile = process.env.USERPROFILE || home;
        const npmNodeModules = path.join(appData, "npm", "node_modules");
        return [
          path.join(home, ".opencode", "bin", "opencode.exe"),
          path.join(userProfile, ".opencode", "bin", "opencode.exe"),
          path.join(localAppData, "Programs", "opencode", "opencode.exe"),
          path.join(localAppData, "Microsoft", "WinGet", "Links", "opencode.exe"),
          path.join(localAppData, "Microsoft", "WinGet", "Links", "opencode"),
          path.join(npmNodeModules, "@opencode-ai", "opencode", "dist", "cli.js"),
          path.join(npmNodeModules, "@opencode-ai", "opencode", "cli.js"),
          path.join(npmNodeModules, "@opencode-ai", "opencode", "bin", "opencode.js"),
          path.join(npmNodeModules, "opencode-ai", "bin", "opencode"),
          path.join(npmNodeModules, "opencode", "dist", "cli.js"),
          path.join(npmNodeModules, "opencode", "cli.js"),
          path.join(npmNodeModules, "opencode", "bin", "opencode.js"),
          path.join(home, ".opencode", "bin", "opencode.cmd"),
          path.join(userProfile, ".opencode", "bin", "opencode.cmd"),
          path.join(appData, "npm", "opencode.cmd"),
          path.join(appData, "npm", "opencode")
        ];
      }
      buildMissingHint() {
        if (process.platform === "win32") {
          const home = os.homedir();
          return [
            rt("\u672A\u627E\u5230\u53EF\u6267\u884C\u6587\u4EF6\u3002", "Executable not found."),
            rt(
              "\u8BF7\u5148\u5728 Windows \u672C\u673A\u5B89\u88C5 Node.js\uFF0C\u7136\u540E\u6267\u884C npm install -g opencode-ai\u3002\u5FC5\u8981\u65F6\u53EF\u5728\u8BBE\u7F6E\u91CC\u586B\u5199 FLOWnote CLI \u7684\u7EDD\u5BF9\u8DEF\u5F84\uFF08\u4F18\u5148 .exe\uFF0C\u5176\u6B21 cli.js \u6216 bin/opencode\uFF09\uFF0C\u4F8B\u5982\uFF1A",
              "Install Node.js on native Windows first, then run npm install -g opencode-ai. If needed, set the absolute FLOWnote CLI path in settings (prefer .exe, then cli.js or bin/opencode), for example:"
            ),
            `${path.join(home, ".opencode", "bin", "opencode.exe")}`,
            `${path.join(home, "AppData", "Roaming", "npm", "node_modules", "opencode-ai", "bin", "opencode")}`,
            rt(
              "Windows \u4E0B\u8BF7\u907F\u514D\u586B\u5199 opencode.cmd \u5305\u88C5\u811A\u672C\u3002",
              "On Windows, avoid using the opencode.cmd wrapper script."
            ),
            rt(
              "\u4E0D\u8981\u4F7F\u7528 WSL \u5B89\u88C5 OpenCode\uFF1B\u8BF7\u6539\u4E3A\u5728 Windows \u672C\u673A\u7528 Node.js \u5B89\u88C5\u3002",
              "Do not install OpenCode via WSL; install it on native Windows with Node.js instead."
            )
          ].join(" ");
        }
        return rt(
          "\u672A\u627E\u5230\u53EF\u6267\u884C\u6587\u4EF6\u3002\u8BF7\u5728\u8BBE\u7F6E\u91CC\u586B\u5199\u7EDD\u5BF9\u8DEF\u5F84\uFF0C\u4F8B\u5982 {example}",
          "Executable not found. Set the absolute path in settings, e.g. {example}",
          { example: expandHome("~/.opencode/bin/opencode") }
        );
      }
      async resolve(cliPath, options = {}) {
        const attempted = [];
        const candidates = [];
        const onlyCliPath = Boolean(options && options.onlyCliPath);
        const explicitCliPath = String(cliPath || "").trim();
        if (explicitCliPath) candidates.push(expandHome(explicitCliPath));
        if (!onlyCliPath) {
          candidates.push(...findInPath("opencode"));
          candidates.push(...this.buildPlatformCandidates());
          candidates.push(...await which("opencode"));
        }
        const unique = [...new Set(candidates.map((c) => String(c || "").trim()).filter(Boolean))];
        const ordered = process.platform === "win32" ? unique.map((item, index) => ({ item, index })).sort((a, b) => {
          const aExplicit = explicitCliPath && a.index === 0 ? -100 : 0;
          const bExplicit = explicitCliPath && b.index === 0 ? -100 : 0;
          const delta = windowsCandidatePriority(a.item) + aExplicit - (windowsCandidatePriority(b.item) + bExplicit);
          if (delta !== 0) return delta;
          return a.index - b.index;
        }).map((item) => item.item) : unique;
        for (const c of ordered) {
          attempted.push(c);
          if (process.platform === "win32" && isWindowsWslPath(c)) continue;
          if (!fs.existsSync(c)) continue;
          if (!isExecutable(c)) continue;
          if (process.platform === "win32" && isWindowsCommandWrapperPath(c)) {
            const scriptPath = resolveWindowsWrapperNodeScript(c);
            if (!scriptPath) continue;
            return {
              ok: true,
              path: scriptPath,
              attempted,
              kind: "node-script",
              sourcePath: c,
              nodePath: resolveNodeExecutablePath(scriptPath)
        };
      }
      if (process.platform === "win32" && isNodeScriptPath(c)) {
        return {
          ok: true,
              path: c,
              attempted,
          kind: "node-script",
          nodePath: resolveNodeExecutablePath(c)
        };
      }
      if (process.platform === "win32" && isLikelyNodeScriptFile(c)) {
        return {
          ok: true,
          path: c,
          attempted,
          kind: "node-script",
          nodePath: resolveNodeExecutablePath(c)
        };
      }
      return { ok: true, path: c, attempted, kind: "native" };
    }
    return {
      ok: false,
      path: "",
          attempted,
          hint: this.buildMissingHint()
        };
      }
    };
    module2.exports = {
      ExecutableResolver,
      isNodeScriptPath,
      isLikelyNodeScriptFile,
      isWindowsCommandWrapperPath,
      resolveWindowsWrapperNodeScript,
      resolveNodeExecutablePath
    };
  }
});

// runtime/transports/compat/path-wsl-utils.js
var require_path_wsl_utils = __commonJS({
  "runtime/transports/compat/path-wsl-utils.js"(exports2, module2) {
    function createPathWslMethods(deps = {}) {
      const {
        fs,
        path,
        os,
        execFileSync,
        OPENCODE_SERVE_ARGS,
        WSL_RUNTIME_WORKSPACE_DIR,
        WSL_XDG_DATA_DIR,
        WSL_XDG_CONFIG_DIR,
        WSL_XDG_STATE_DIR,
        WSL_XDG_CACHE_DIR,
        SQLITE_LOCK_RE: SQLITE_LOCK_RE2,
        isWindowsPlatform,
        isWindowsCommandWrapper,
        quotePosixShellArg,
        toWslPath,
        toWslPathWithFallback,
        parseWindowsDrivePath,
        parseWslMountPath,
        shortStableHash
      } = deps;
      class PathWslMethods {
        normalizeDirectoryForService(directory) {
          const raw = String(directory || "").trim();
          if (!raw) return raw;
          if (this.launchContext && this.launchContext.mode === "wsl") {
            let resolved = this.resolveWslDirectory(raw);
            if (this.isWslMountPath(resolved)) {
              const mountAccessible = this.probeWslDirectory(resolved);
              if (!mountAccessible) {
                const fallback = this.getDefaultWslWorkspaceDir();
                this.log(`wsl mount directory inaccessible, fallback to workspace: ${resolved} -> ${fallback}`);
                this.ensureWslFallbackWorkspaceMirror(raw, fallback);
                resolved = fallback;
              }
            }
            const normalized = this.normalizeWslWorkspaceDirectory(resolved);
            if (normalized && normalized !== raw) {
              this.wslDirectoryCache.set(raw, normalized);
            }
            return normalized;
          }
          return raw;
        }
        getActiveWslDistro() {
          const fromLaunch = this.launchContext && this.launchContext.mode === "wsl" ? String(this.launchContext.distro || "").trim() : "";
          if (fromLaunch) return fromLaunch;
          const fromSettings = String(this.settings && this.settings.wslDistro ? this.settings.wslDistro : "").trim();
          return fromSettings || "Ubuntu";
        }
        toWindowsWslUncPath(wslPath, distro = "") {
          const target = String(wslPath || "").trim();
          if (!target.startsWith("/")) return "";
          const pickedDistro = String(distro || this.getActiveWslDistro() || "").trim();
          if (!pickedDistro) return "";
          const tail = target.replace(/^\/+/, "").replace(/\//g, "\\");
          return `\\\\wsl$\\${pickedDistro}\\${tail}`;
        }
        copyFileIfNeeded(srcFile, dstFile) {
          try {
            const srcStat = fs.statSync(srcFile);
            if (!srcStat.isFile()) return;
            let shouldCopy = true;
            try {
              const dstStat = fs.statSync(dstFile);
              if (dstStat.isFile() && Number(dstStat.size) === Number(srcStat.size) && Math.floor(Number(dstStat.mtimeMs || 0)) === Math.floor(Number(srcStat.mtimeMs || 0))) {
                shouldCopy = false;
              }
            } catch {
            }
            if (!shouldCopy) return;
            fs.mkdirSync(path.dirname(dstFile), { recursive: true });
            fs.copyFileSync(srcFile, dstFile);
            try {
              fs.utimesSync(dstFile, srcStat.atime, srcStat.mtime);
            } catch {
            }
          } catch {
          }
        }
        copyTreeIfNeeded(srcPath, dstPath) {
          let stat;
          try {
            stat = fs.statSync(srcPath);
          } catch {
            return;
          }
          if (!stat) return;
          if (stat.isFile()) {
            this.copyFileIfNeeded(srcPath, dstPath);
            return;
          }
          if (!stat.isDirectory()) return;
          try {
            fs.mkdirSync(dstPath, { recursive: true });
          } catch {
            return;
          }
          let entries = [];
          try {
            entries = fs.readdirSync(srcPath, { withFileTypes: true });
          } catch {
            return;
          }
          for (const entry of entries) {
            if (!entry || !entry.name) continue;
            const name = String(entry.name || "");
            if (!name || name === ".DS_Store") continue;
            const childSrc = path.join(srcPath, name);
            const childDst = path.join(dstPath, name);
            if (entry.isDirectory()) {
              this.copyTreeIfNeeded(childSrc, childDst);
            } else if (entry.isFile()) {
              this.copyFileIfNeeded(childSrc, childDst);
            }
          }
        }
        ensureWslFallbackWorkspaceMirror(sourceVaultPath, fallbackWorkspacePath) {
          if (!isWindowsPlatform()) return;
          if (!this.launchContext || this.launchContext.mode !== "wsl") return;
          const sourceRoot = String(sourceVaultPath || "").trim();
          const fallback = String(fallbackWorkspacePath || "").trim();
          if (!sourceRoot || !fallback.startsWith("/")) return;
          if (!path.isAbsolute(sourceRoot)) return;
          if (!fs.existsSync(sourceRoot)) return;
          const now = Date.now();
          const mirrorKey = `${sourceRoot}=>${fallback}`;
          if (this.wslFallbackMirrorKey === mirrorKey && now - Number(this.wslFallbackMirrorAt || 0) < 5e3) {
            return;
          }
          const distro = this.getActiveWslDistro();
          const uncRoot = this.toWindowsWslUncPath(fallback, distro);
          if (!uncRoot) return;
          const mirrorItems = [
            "Meta/.ai-memory",
            "Meta/\u6A21\u677F",
            "skills",
            "AGENTS.md",
            "README.md"
          ];
          let copiedAny = false;
          for (const rel of mirrorItems) {
            const src = path.join(sourceRoot, rel);
            if (!fs.existsSync(src)) continue;
            const dst = path.join(uncRoot, rel);
            this.copyTreeIfNeeded(src, dst);
            copiedAny = true;
          }
          this.wslFallbackMirrorAt = now;
          this.wslFallbackMirrorKey = mirrorKey;
          if (copiedAny) {
            this.log(`wsl fallback workspace mirrored from vault: ${sourceRoot} -> ${fallback}`);
          }
        }
        isWslMountPath(value) {
          return /^\/mnt\/[a-zA-Z]\//.test(String(value || "").trim());
        }
        readWindowsDriveMap() {
          if (!isWindowsPlatform()) return /* @__PURE__ */ new Map();
          const now = Date.now();
          if (this.windowsDriveMapCache && now - Number(this.windowsDriveMapCache.at || 0) <= 6e4) {
            return this.windowsDriveMapCache.map;
          }
          const map = /* @__PURE__ */ new Map();
          try {
            const output = String(execFileSync("cmd.exe", ["/d", "/s", "/c", "subst"], {
              encoding: "utf8",
              windowsHide: true,
              timeout: 1500
            }) || "");
            output.split(/\r?\n/).forEach((line) => {
              const match = String(line || "").match(/^\s*([a-zA-Z]):\\:\s*=>\s*(.+?)\s*$/);
              if (!match) return;
              const drive = String(match[1] || "").toLowerCase();
              const target = String(match[2] || "").trim().replace(/^\\\\\?\\/, "");
              if (!drive || !target) return;
              map.set(drive, target);
            });
          } catch {
          }
          this.windowsDriveMapCache = {
            at: now,
            map
          };
          return map;
        }
        resolveWindowsAliasPath(windowsPath) {
          if (!isWindowsPlatform()) return "";
          const parsed = parseWindowsDrivePath(windowsPath);
          if (!parsed || parsed.drive === "c") return "";
          const driveMap = this.readWindowsDriveMap();
          const mappedRoot = String(driveMap.get(parsed.drive) || "").trim();
          if (!mappedRoot) return "";
          const tail = String(parsed.tail || "").replace(/\//g, path.sep);
          return tail ? path.join(mappedRoot, tail) : mappedRoot;
        }
        buildWslDirectoryCandidates(rawDirectory) {
          const raw = String(rawDirectory || "").trim();
          const candidates = [];
          const push = (value) => {
            const next = String(value || "").trim();
            if (!next || candidates.includes(next)) return;
            candidates.push(next);
          };
          if (!raw) return candidates;
          const parsedWindows = parseWindowsDrivePath(raw);
          const parsedMount = parseWslMountPath(raw);
          if (raw.startsWith("/")) push(raw);
          if (parsedWindows) {
            const tail = String(parsedWindows.tail || "").replace(/\//g, path.sep);
            const mappedAlias = this.resolveWindowsAliasPath(raw);
            if (mappedAlias) push(toWslPath(mappedAlias));
            push(toWslPathWithFallback(raw));
            push(toWslPath(raw));
            if (tail) {
              const mappedRoot = String(this.readWindowsDriveMap().get(parsedWindows.drive) || "").trim();
              if (mappedRoot) push(toWslPath(path.join(mappedRoot, tail)));
              const userProfile = process.env.USERPROFILE || os.homedir();
              if (userProfile) push(toWslPath(path.join(userProfile, tail)));
            }
            return candidates;
          }
          if (parsedMount) {
            const tail = String(parsedMount.tail || "").replace(/\//g, path.sep);
            if (tail) {
              const mappedRoot = String(this.readWindowsDriveMap().get(parsedMount.drive) || "").trim();
              if (mappedRoot) push(toWslPath(path.join(mappedRoot, tail)));
              const userProfile = process.env.USERPROFILE || os.homedir();
              if (userProfile) push(toWslPath(path.join(userProfile, tail)));
            }
            push(toWslPathWithFallback(raw));
            return candidates;
          }
          push(toWslPathWithFallback(raw));
          push(toWslPath(raw));
          return candidates;
        }
        probeWslDirectory(directory) {
          if (!isWindowsPlatform()) return true;
          const target = String(directory || "").trim();
          if (!target) return false;
          if (!target.startsWith("/")) return false;
          const args = [];
          const distro = this.launchContext && this.launchContext.mode === "wsl" ? String(this.launchContext.distro || "").trim() : "";
          if (distro) args.push("-d", distro);
          args.push("-e", "sh", "-lc", `[ -d ${quotePosixShellArg(target)} ] && printf __OK__`);
          try {
            const out = String(execFileSync("wsl.exe", args, {
              encoding: "utf8",
              windowsHide: true,
              timeout: 2e3,
              cwd: process.env.USERPROFILE || os.homedir() || process.cwd()
            }) || "");
            return out.includes("__OK__");
          } catch {
            return false;
          }
        }
        resolveWslDirectory(rawDirectory) {
          const raw = String(rawDirectory || "").trim();
          if (!raw) return raw;
          const cached = String(this.wslDirectoryCache.get(raw) || "").trim();
          if (cached) return cached;
          const candidates = this.buildWslDirectoryCandidates(raw);
          if (!candidates.length) return raw;
          let resolved = candidates[0];
          for (const candidate of candidates) {
            if (this.probeWslDirectory(candidate)) {
              resolved = candidate;
              break;
            }
          }
          this.wslDirectoryCache.set(raw, resolved);
          if (resolved !== candidates[0]) {
            this.log(`wsl directory mapped: ${candidates[0]} -> ${resolved}`);
          }
          return resolved;
        }
        getWslHomeDirectory() {
          if (this.wslUserHomeDir) return this.wslUserHomeDir;
          const fromUserHome = String(this.launchContext && this.launchContext.wslUserHome ? this.launchContext.wslUserHome : "").trim();
          if (fromUserHome.startsWith("/")) {
            this.wslUserHomeDir = fromUserHome;
            return this.wslUserHomeDir;
          }
          const fromLaunch = String(this.launchContext && this.launchContext.wslHome ? this.launchContext.wslHome : "").trim();
          if (fromLaunch.startsWith("/")) {
            const idx = fromLaunch.lastIndexOf("/");
            if (idx > 0) {
              this.wslUserHomeDir = fromLaunch.slice(0, idx);
              return this.wslUserHomeDir;
            }
          }
          const args = [];
          const distro = this.launchContext && this.launchContext.mode === "wsl" ? String(this.launchContext.distro || "").trim() : String(this.settings && this.settings.wslDistro ? this.settings.wslDistro : "").trim();
          if (distro) args.push("-d", distro);
          args.push("-e", "sh", "-lc", `printf '%s' "$HOME"`);
          try {
            const out = String(execFileSync("wsl.exe", args, {
              encoding: "utf8",
              windowsHide: true,
              timeout: 2e3,
              cwd: process.env.USERPROFILE || os.homedir() || process.cwd()
            }) || "").trim();
            if (out.startsWith("/")) {
              this.wslUserHomeDir = out;
              return this.wslUserHomeDir;
            }
          } catch {
          }
          this.wslUserHomeDir = "/home";
          return this.wslUserHomeDir;
        }
        ensureWslDirectory(dirPath) {
          if (!isWindowsPlatform()) return;
          const target = String(dirPath || "").trim();
          if (!target.startsWith("/")) return;
          const args = [];
          const distro = this.launchContext && this.launchContext.mode === "wsl" ? String(this.launchContext.distro || "").trim() : String(this.settings && this.settings.wslDistro ? this.settings.wslDistro : "").trim();
          if (distro) args.push("-d", distro);
          args.push("-e", "sh", "-lc", `mkdir -p ${quotePosixShellArg(target)} >/dev/null 2>&1`);
          try {
            execFileSync("wsl.exe", args, {
              encoding: "utf8",
              windowsHide: true,
              timeout: 2500,
              cwd: process.env.USERPROFILE || os.homedir() || process.cwd()
            });
          } catch {
          }
        }
        getDefaultWslWorkspaceDir() {
          if (this.wslWorkspaceDir) return this.wslWorkspaceDir;
          const homeDir = this.getWslHomeDirectory();
          const workspace = `${homeDir}/${WSL_RUNTIME_WORKSPACE_DIR}`;
          this.ensureWslDirectory(workspace);
          this.wslWorkspaceDir = workspace;
          this.log(`wsl mount directory detected, use local workspace: ${workspace}`);
          return workspace;
        }
        normalizeWslWorkspaceDirectory(inputDir) {
          const raw = String(inputDir || "").trim();
          if (!raw) return raw;
          const normalized = raw.replace(/\\/g, "/").replace(/\/+$/, "");
          const needsMigration = /\/\.opencode-runtime\/workspace$/i.test(normalized) || /\/\.opencode-workspace$/i.test(normalized);
          if (!needsMigration) return raw;
          const fallback = this.getDefaultWslWorkspaceDir();
          this.log(`wsl legacy workspace mapped: ${raw} -> ${fallback}`);
          return fallback;
        }
        buildWslDirectoryHint(detailText) {
          if (!this.launchContext || this.launchContext.mode !== "wsl") return "";
          const detail = String(detailText || "");
          if (!/enoent|no such file|cannot find/i.test(detail)) return "";
          if (!/config\.json|\/mnt\/[a-z]\//i.test(detail)) return "";
          return `
\u63D0\u793A: \u5F53\u524D Vault \u8DEF\u5F84\u5728 WSL \u4E0D\u53EF\u8BBF\u95EE\uFF0C\u63D2\u4EF6\u4F1A\u81EA\u52A8\u6539\u7528 WSL \u672C\u5730\u5DE5\u4F5C\u76EE\u5F55\u3002`;
        }
        buildWslSqliteHint(detailText) {
          if (!this.launchContext || this.launchContext.mode !== "wsl") return "";
          const detail = String(detailText || "");
          if (!SQLITE_LOCK_RE2.test(detail)) return "";
          const dataHome = String(this.launchContext.wslHome || "").trim();
          const suffix = dataHome ? ` \u5F53\u524D XDG_DATA_HOME=${dataHome}\u3002` : "";
          return `
\u63D0\u793A: \u68C0\u6D4B\u5230 SQLite/\u6587\u4EF6\u9501\u95EE\u9898\u3002FLOWnote \u5B98\u65B9\u5C06\u6570\u636E\u5E93\u5B58\u653E\u5728 XDG_DATA_HOME\uFF08\u9ED8\u8BA4 ~/.local/share/opencode\uFF09\u3002\u63D2\u4EF6\u5DF2\u6539\u4E3A\u72EC\u7ACB XDG_DATA_HOME\uFF0C\u4EE5\u907F\u514D\u4E0E\u5176\u4ED6 FLOWnote \u8FDB\u7A0B\u7ADE\u4E89\u540C\u4E00\u4E2A\u6570\u636E\u5E93\u3002${suffix}`;
        }
        buildWslServeCommand(_runtimeHome) {
          const args = OPENCODE_SERVE_ARGS.map((arg) => quotePosixShellArg(arg)).join(" ");
          const overrideDataHome = String(this.wslDataHomeOverride || "").trim();
          const dataHomeExpr = overrideDataHome.startsWith("/") ? quotePosixShellArg(overrideDataHome) : `"${`$HOME/${WSL_XDG_DATA_DIR}`}"`;
          return [
            'cd "$HOME" >/dev/null 2>&1 || true;',
            "OPENCODE_BIN='';",
            'for c in "$HOME/.opencode/bin/opencode" /home/*/.opencode/bin/opencode /root/.opencode/bin/opencode "$HOME/.local/bin/opencode" "/usr/local/bin/opencode" "/usr/bin/opencode"; do',
            '  if [ -x "$c" ]; then OPENCODE_BIN="$c"; break; fi;',
            "done;",
            'if [ -z "$OPENCODE_BIN" ] && command -v opencode >/dev/null 2>&1; then',
            '  CAND="$(command -v opencode 2>/dev/null)";',
            '  case "$CAND" in',
            "    ''|*.exe|*:*|*\\\\*) ;;",
            '    *) OPENCODE_BIN="$CAND" ;;',
            "  esac;",
            "fi;",
            'if [ -z "$OPENCODE_BIN" ]; then',
            '  echo "opencode not found in WSL PATH or common install dirs" 1>&2;',
            "  exit 127;",
            "fi;",
            'echo "[FLOWnote] WSL HOME=$HOME" 1>&2;',
            `XDG_DATA_HOME_DIR=${dataHomeExpr};`,
            `XDG_CONFIG_HOME_DIR="$HOME/${WSL_XDG_CONFIG_DIR}";`,
            `XDG_STATE_HOME_DIR="$HOME/${WSL_XDG_STATE_DIR}";`,
            `XDG_CACHE_HOME_DIR="$HOME/${WSL_XDG_CACHE_DIR}";`,
            'SRC_DATA_DIR="$HOME/.local/share/opencode";',
            'TARGET_DATA_DIR="$XDG_DATA_HOME_DIR/opencode";',
            'mkdir -p "$XDG_DATA_HOME_DIR" "$XDG_CONFIG_HOME_DIR" "$XDG_STATE_HOME_DIR" "$XDG_CACHE_HOME_DIR" >/dev/null 2>&1 || true;',
            'mkdir -p "$TARGET_DATA_DIR" >/dev/null 2>&1 || true;',
            'if [ -d "$SRC_DATA_DIR" ] && [ "$SRC_DATA_DIR" != "$TARGET_DATA_DIR" ]; then',
            '  cp -R "$SRC_DATA_DIR"/. "$TARGET_DATA_DIR"/ >/dev/null 2>&1 || true;',
            '  find "$TARGET_DATA_DIR" -type f \\( -name "*.lock" -o -name "*.wal" -o -name "*.shm" \\) -delete >/dev/null 2>&1 || true;',
            "fi;",
            'echo "[FLOWnote] WSL XDG_DATA_HOME=$XDG_DATA_HOME_DIR" 1>&2;',
            'echo "[FLOWnote] WSL XDG_CONFIG_HOME=$XDG_CONFIG_HOME_DIR" 1>&2;',
            `exec env XDG_DATA_HOME="$XDG_DATA_HOME_DIR" XDG_CONFIG_HOME="$XDG_CONFIG_HOME_DIR" XDG_STATE_HOME="$XDG_STATE_HOME_DIR" XDG_CACHE_HOME="$XDG_CACHE_HOME_DIR" "$OPENCODE_BIN" ${args}`
          ].join(" ");
        }
        useWslDataHomeFallback() {
          if (!this.launchContext || this.launchContext.mode !== "wsl") return false;
          const homeDir = String(this.getWslHomeDirectory() || "").trim();
          if (!homeDir.startsWith("/")) return false;
          const stamp = Date.now().toString(36);
          const vaultTag = shortStableHash(this.vaultPath || "vault").slice(0, 8);
          const next = `${homeDir}/${WSL_XDG_DATA_DIR}-${vaultTag}-${stamp}`;
          if (!next || next === this.wslDataHomeOverride) return false;
          this.wslDataHomeOverride = next;
          this.log(`wsl sqlite fallback XDG_DATA_HOME=${next}`);
          return true;
        }
        parseWslCliPath(cliPath) {
          const raw = String(cliPath || "").trim();
          if (!raw) return { useWsl: false, distro: "" };
          const lower = raw.toLowerCase();
          if (lower.startsWith("wsl://")) {
            return { useWsl: true, distro: raw.slice("wsl://".length).trim() };
          }
          if (lower.startsWith("wsl:")) {
            return { useWsl: true, distro: raw.slice("wsl:".length).trim() };
          }
          const normalized = raw.replace(/\\/g, "/");
          const base = normalized.split("/").filter(Boolean).pop() || "";
          if (base.toLowerCase() === "wsl" || base.toLowerCase() === "wsl.exe") {
            return { useWsl: true, distro: "" };
          }
          return { useWsl: false, distro: "" };
        }
      }
      const methods = {};
      for (const key of Object.getOwnPropertyNames(PathWslMethods.prototype)) {
        if (key === "constructor") continue;
        methods[key] = PathWslMethods.prototype[key];
      }
      return methods;
    }
    module2.exports = { createPathWslMethods };
  }
});

// runtime/transports/compat/launch-attempt-methods.js
var require_launch_attempt_methods = __commonJS({
  "runtime/transports/compat/launch-attempt-methods.js"(exports2, module2) {
    function createLaunchAttemptMethods(deps = {}) {
      const {
        os,
        path,
        process: process2,
        OPENCODE_SERVE_ARGS,
        isWindowsPlatform,
        isWindowsCommandWrapper,
        isNodeScriptPath,
        resolveWindowsWrapperNodeScript,
        resolveNodeExecutablePath,
        toWslPath
      } = deps;
      class LaunchAttemptMethods {
        createWslAttempt(runtimeHome, distro = "", shellName = "sh") {
          const pickedDistro = String(distro || "").trim();
          const pickedShell = String(shellName || "sh").trim() || "sh";
          const args = [];
          if (pickedDistro) args.push("-d", pickedDistro);
          args.push("-e", pickedShell, "-lc", this.buildWslServeCommand(runtimeHome));
          const wslLaunchCwd = process2.env.USERPROFILE || os.homedir() || process2.cwd();
          return {
            label: pickedDistro ? `wsl(${pickedDistro}, ${pickedShell})` : `wsl(${pickedShell})`,
            command: "wsl.exe",
            args,
            options: {
              cwd: wslLaunchCwd,
              env: { ...process2.env },
              shell: false
            },
            mode: "wsl",
            directory: toWslPath(this.vaultPath) || this.vaultPath,
            distro: pickedDistro
          };
        }
        pushWslAttempts(list, seen, runtimeHome, distro = "") {
          this.pushLaunchAttempt(list, seen, this.createWslAttempt(runtimeHome, distro, "bash"));
          this.pushLaunchAttempt(list, seen, this.createWslAttempt(runtimeHome, distro, "sh"));
        }
        pushLaunchAttempt(list, seen, attempt) {
          const item = attempt && typeof attempt === "object" ? attempt : null;
          if (!item) return;
          const command = String(item.command || "").trim();
          if (!command) return;
          const args = Array.isArray(item.args) ? item.args.map((v) => String(v || "")) : [];
          const shellFlag = item.options && item.options.shell ? "1" : "0";
          const key = `${command}|${shellFlag}|${args.join("")}`;
          if (seen.has(key)) return;
          seen.add(key);
          list.push({
            label: String(item.label || command),
            command,
            args,
            options: item.options && typeof item.options === "object" ? item.options : {},
            mode: item.mode === "wsl" ? "wsl" : "native",
            directory: String(item.directory || this.vaultPath),
            distro: String(item.distro || "").trim(),
            remember: item.remember !== false
          });
        }
        normalizeLaunchStrategy(value) {
          const mode = String(value || "").trim().toLowerCase();
          const compact = mode.replace(/[\s_-]+/g, "");
          if (mode === "native" || compact === "windowsnative" || compact === "macnative" || compact === "local") return "native";
          return "auto";
        }
        normalizeLaunchProfile(profile) {
          if (!profile || typeof profile !== "object") return null;
          const mode = String(profile.mode || "").trim().toLowerCase() === "native" ? "native" : "";
          const command = String(profile.command || "").trim();
          const args = Array.isArray(profile.args) ? profile.args.map((item) => String(item || "")) : [];
          const shell = Boolean(profile.shell);
          const distro = String(profile.distro || "").trim();
          if (mode !== "native" || !command) return null;
          return { mode, command, args, shell, distro };
        }
        resolvePreferredLaunchProfile() {
          if (typeof this.getPreferredLaunch !== "function") return null;
          try {
            return this.normalizeLaunchProfile(this.getPreferredLaunch());
          } catch {
            return null;
          }
        }
        buildLaunchProfileFromAttempt(attempt) {
          const item = attempt && typeof attempt === "object" ? attempt : null;
          if (!item) return null;
          if (item.remember === false) return null;
          if (item.mode !== "native") return null;
          const command = String(item.command || "").trim();
          if (!command) return null;
          return {
            mode: "native",
            command,
            args: Array.isArray(item.args) ? item.args.map((value) => String(value || "")) : [],
            shell: Boolean(item.options && item.options.shell),
            distro: ""
          };
        }
        notifyLaunchSuccess(attempt) {
          const profile = this.buildLaunchProfileFromAttempt(attempt);
          if (!profile || typeof this.onLaunchSuccess !== "function") return;
          try {
            Promise.resolve(this.onLaunchSuccess(profile)).catch(() => {
            });
          } catch {
          }
        }
        pushPreferredLaunchAttempt(list, seen, preferred, runtimeHome) {
          const profile = this.normalizeLaunchProfile(preferred);
          if (!profile) return;
          this.pushLaunchAttempt(list, seen, {
            label: `${profile.command} (remembered)`,
            command: profile.command,
            args: Array.isArray(profile.args) && profile.args.length ? profile.args : OPENCODE_SERVE_ARGS,
            options: {
              cwd: this.vaultPath,
              env: { ...process2.env, OPENCODE_HOME: runtimeHome },
              shell: Boolean(profile.shell)
            },
            mode: "native",
            directory: this.vaultPath
          });
        }
        isNodeScriptLaunchPath(resolvedPath, resolvedKind = "") {
          if (String(resolvedKind || "").trim().toLowerCase() === "node-script") return true;
          return isNodeScriptPath(resolvedPath);
        }
        pushNodeScriptAttempts(list, seen, scriptPath, baseOptions, preferredNode = "", sourcePath = "") {
          const script = String(scriptPath || "").trim();
          if (!script) return;
          const nodeCandidates = [
            String(preferredNode || "").trim(),
            String(resolveNodeExecutablePath(script) || "").trim(),
            isWindowsPlatform() ? "node.exe" : "node",
            "node"
          ];
          const dedupedNodes = [...new Set(nodeCandidates.filter(Boolean))];
          const baseName = path.basename(script);
          const sourceLabel = String(sourcePath || "").trim();
          dedupedNodes.forEach((nodeCommand, index) => {
            this.pushLaunchAttempt(list, seen, {
              label: sourceLabel && index === 0 ? `${baseName} via ${nodeCommand} (${path.basename(sourceLabel)})` : `${baseName} via ${nodeCommand}`,
              command: nodeCommand,
              args: [script, ...OPENCODE_SERVE_ARGS],
              options: { ...baseOptions, shell: false },
              mode: "native",
              directory: this.vaultPath
            });
          });
        }
        buildLaunchAttempts(resolved, runtimeHome) {
          const attempts = [];
          const seen = /* @__PURE__ */ new Set();
          const strategy = this.normalizeLaunchStrategy(this.settings && this.settings.launchStrategy);
          const preferred = strategy === "auto" ? this.resolvePreferredLaunchProfile() : null;
          const baseEnv = { ...process2.env, OPENCODE_HOME: runtimeHome };
          const baseOptions = {
            cwd: this.vaultPath,
            env: baseEnv
          };
          const resolvedPath = String(resolved && resolved.path ? resolved.path : "").trim();
          const resolvedKind = String(resolved && resolved.kind ? resolved.kind : "").trim();
          const resolvedNodePath = String(resolved && resolved.nodePath ? resolved.nodePath : "").trim();
          const resolvedSourcePath = String(resolved && resolved.sourcePath ? resolved.sourcePath : "").trim();
          if (preferred) {
            this.pushPreferredLaunchAttempt(attempts, seen, preferred, runtimeHome);
          }
          if (resolvedPath) {
            const wrapperScript = isWindowsPlatform() && isWindowsCommandWrapper(resolvedPath) ? resolveWindowsWrapperNodeScript(resolvedPath) : "";
            const nodeScriptPath = this.isNodeScriptLaunchPath(resolvedPath, resolvedKind) ? resolvedPath : wrapperScript;
            if (nodeScriptPath) {
              this.pushNodeScriptAttempts(
                attempts,
                seen,
                nodeScriptPath,
                baseOptions,
                resolvedNodePath,
                resolvedSourcePath || (wrapperScript ? resolvedPath : "")
              );
            } else if (!(isWindowsPlatform() && isWindowsCommandWrapper(resolvedPath))) {
              this.pushLaunchAttempt(attempts, seen, {
                label: resolvedPath,
                command: resolvedPath,
                args: OPENCODE_SERVE_ARGS,
                options: { ...baseOptions, shell: false },
                mode: "native",
                directory: this.vaultPath
              });
            }
          }
          const allowAutoFallback = Boolean(this.settings.autoDetectCli || !String(this.settings.cliPath || "").trim());
          if (allowAutoFallback) {
            if (isWindowsPlatform()) {
              this.pushLaunchAttempt(attempts, seen, {
                label: "opencode.exe (PATH)",
                command: "opencode.exe",
                args: OPENCODE_SERVE_ARGS,
                options: { ...baseOptions, shell: false },
                mode: "native",
                directory: this.vaultPath
              });
              this.pushLaunchAttempt(attempts, seen, {
                label: "opencode (PATH)",
                command: "opencode",
                args: OPENCODE_SERVE_ARGS,
                options: { ...baseOptions, shell: false },
                mode: "native",
                directory: this.vaultPath
              });
            } else {
              this.pushLaunchAttempt(attempts, seen, {
                label: "opencode (PATH)",
                command: "opencode",
                args: OPENCODE_SERVE_ARGS,
                options: { ...baseOptions, shell: false },
                mode: "native",
                directory: this.vaultPath
              });
            }
          }
          return attempts;
        }
      }
      const methods = {};
      for (const key of Object.getOwnPropertyNames(LaunchAttemptMethods.prototype)) {
        if (key === "constructor") continue;
        methods[key] = LaunchAttemptMethods.prototype[key];
      }
      return methods;
    }
    module2.exports = { createLaunchAttemptMethods };
  }
});

// runtime/transports/compat/process-lifecycle-methods.js
var require_process_lifecycle_methods = __commonJS({
  "runtime/transports/compat/process-lifecycle-methods.js"(exports2, module2) {
    function createProcessLifecycleMethods(deps = {}) {
      const {
        fs,
        path,
        spawn,
        process: process2,
        STARTUP_TIMEOUT_MS,
        WSL_STARTUP_TIMEOUT_MS,
        collectOutputTail,
        appendOutputHint,
        rt = (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
          const v = params[k];
          return v === void 0 || v === null ? "" : String(v);
        })
      } = deps;
      class ProcessLifecycleMethods {
        async startProcessWithAttempt(attempt) {
          const launch = attempt && typeof attempt === "object" ? attempt : {};
          const label = String(launch.label || launch.command || "unknown");
          return new Promise((resolve, reject) => {
            let settled = false;
            let startupTimeout = null;
            const startupTimeoutMs = launch.mode === "wsl" ? WSL_STARTUP_TIMEOUT_MS : STARTUP_TIMEOUT_MS;
            let onOutput = null;
            let onStderr = null;
            let onError = null;
            let onExit = null;
            const outputTail = [];
            let detectedWslDataHome = "";
            let detectedWslUserHome = "";
            const finish = (error, url) => {
              if (settled) return;
              settled = true;
              if (startupTimeout) {
                clearTimeout(startupTimeout);
                startupTimeout = null;
              }
              if (this.process) {
                if (onOutput) {
                  try {
                    this.process.stdout.removeListener("data", onOutput);
                  } catch {
                  }
                  try {
                    this.process.stderr.removeListener("data", onStderr || onOutput);
                  } catch {
                  }
                }
                if (onError) {
                  try {
                    this.process.removeListener("error", onError);
                  } catch {
                  }
                }
                if (onExit) {
                  try {
                    this.process.removeListener("exit", onExit);
                  } catch {
                  }
                }
              }
              if (error) {
                this.cleanupProcessOnBootFailure();
                reject(error);
                return;
              }
              this.launchContext = {
                mode: launch.mode === "wsl" ? "wsl" : "native",
                directory: String(launch.directory || this.vaultPath),
                label,
                distro: String(launch.distro || "").trim(),
                wslHome: detectedWslDataHome,
                wslUserHome: detectedWslUserHome
              };
              this.attachProcessExitMonitor();
              resolve(url);
            };
            try {
              this.process = spawn(launch.command, Array.isArray(launch.args) ? launch.args : [], launch.options || {});
            } catch (e) {
              finish(new Error(rt(
                "\u65E0\u6CD5\u542F\u52A8 FLOWnote \u670D\u52A1 ({label}): {message}",
                "Failed to start FLOWnote service ({label}): {message}",
                { label, message: e instanceof Error ? e.message : String(e) }
              )));
              return;
            }
            const inspectOutput = (source, chunk) => {
              collectOutputTail(outputTail, source, chunk);
              const text = String(chunk || "");
              const dataHomeMatch = text.match(/\[flownote\]\s+WSL XDG_DATA_HOME=([^\r\n]+)/);
              if (dataHomeMatch && dataHomeMatch[1]) {
                detectedWslDataHome = String(dataHomeMatch[1]).trim();
              }
              const homeMatch = text.match(/\[flownote\]\s+WSL HOME=([^\r\n]+)/);
              if (homeMatch && homeMatch[1]) {
                detectedWslUserHome = String(homeMatch[1]).trim();
              }
              const match = text.match(/https?:\/\/(?:127\.0\.0\.1|localhost|\[::1\]):\d+/i);
              if (match) {
                this.baseUrl = match[0];
                finish(null, this.baseUrl);
              }
            };
            onOutput = (chunk) => inspectOutput("stdout", chunk);
            onStderr = (chunk) => inspectOutput("stderr", chunk);
            onError = (err) => {
              const message = err instanceof Error ? err.message : String(err);
              finish(new Error(appendOutputHint(rt(
                "\u65E0\u6CD5\u542F\u52A8 FLOWnote \u670D\u52A1 ({label}): {message}",
                "Failed to start FLOWnote service ({label}): {message}",
                { label, message }
              ), outputTail)));
            };
            onExit = (code) => {
              if (this.baseUrl) {
                this.clearProcessState();
                return;
              }
              const exitCode = Number(code);
              const armHint = exitCode === 3221225477 ? rt(
                "\uFF08Windows 0xC0000005\uFF1A\u8FDB\u7A0B\u5D29\u6E83\uFF0C\u5E38\u89C1\u4E8E\u67B6\u6784\u4E0D\u5339\u914D\u6216\u8FD0\u884C\u65F6\u5F02\u5E38\uFF09",
                "(Windows 0xC0000005: process crash, often caused by architecture mismatch or runtime failure)"
              ) : "";
              finish(new Error(appendOutputHint(
                rt(
                  "FLOWnote \u670D\u52A1\u63D0\u524D\u9000\u51FA ({label})\uFF0C\u9000\u51FA\u7801: {code}{armHint}",
                  "FLOWnote service exited early ({label}), exit code: {code}{armHint}",
                  { label, code: String(code), armHint }
                ),
                outputTail
              )));
            };
            this.process.stdout.on("data", onOutput);
            this.process.stderr.on("data", onStderr);
            this.process.on("error", onError);
            this.process.on("exit", onExit);
            startupTimeout = setTimeout(() => {
              if (!this.baseUrl) {
                finish(new Error(appendOutputHint(
                  rt(
                    "\u7B49\u5F85 FLOWnote \u670D\u52A1\u542F\u52A8\u8D85\u65F6 ({label}, {startupTimeoutMs}ms)",
                    "Timed out waiting for FLOWnote service startup ({label}, {startupTimeoutMs}ms)",
                    { label, startupTimeoutMs }
                  ),
                  outputTail
                )));
              }
            }, startupTimeoutMs);
          });
        }
        async startWithFallbacks() {
          const runtimeHome = path.join(this.vaultPath, this.settings.opencodeHomeDir || ".opencode-runtime");
          fs.mkdirSync(runtimeHome, { recursive: true });
          const resolved = await this.resolveExecutable();
          const attempts = this.buildLaunchAttempts(resolved, runtimeHome);
          if (!attempts.length) {
            const hint2 = resolved && resolved.hint ? resolved.hint : rt("opencode \u672A\u627E\u5230", "opencode not found");
            throw new Error(rt(
              "\u65E0\u6CD5\u542F\u52A8 FLOWnote \u670D\u52A1: {hint}",
              "Failed to start FLOWnote service: {hint}",
              { hint: hint2 }
            ));
          }
          const failed = [];
          for (const attempt of attempts) {
            try {
              const url = await this.startProcessWithAttempt(attempt);
              this.log(`opencode launch ok: ${attempt.label} -> ${url}`);
              this.notifyLaunchSuccess(attempt);
              return url;
            } catch (e) {
              const message = e instanceof Error ? e.message : String(e);
              failed.push(`${attempt.label}: ${message}`);
              this.log(`opencode launch failed: ${attempt.label}: ${message}`);
            }
          }
          const hint = resolved && resolved.hint ? `
${rt("\u63D0\u793A", "Hint")}: ${resolved.hint}` : "";
          throw new Error(rt(
            "\u65E0\u6CD5\u542F\u52A8 FLOWnote \u670D\u52A1\uFF0C\u5DF2\u5C1D\u8BD5 {count} \u79CD\u65B9\u5F0F:\n{failed}{hint}",
            "Failed to start FLOWnote service after {count} attempts:\n{failed}{hint}",
            { count: attempts.length, failed: failed.join("\n"), hint }
          ));
        }
        clearProcessState() {
          this.process = null;
          this.baseUrl = "";
          this.bootPromise = null;
          this.processExitListener = null;
          this.wslDirectoryCache.clear();
          this.wslWorkspaceDir = "";
          this.wslUserHomeDir = "";
          this.wslRequestChain = Promise.resolve();
          this.launchContext = {
            mode: "native",
            directory: this.vaultPath,
            label: "",
            distro: "",
            wslHome: "",
            wslUserHome: ""
          };
        }
        cleanupProcessOnBootFailure() {
          if (this.processExitListener && this.process) {
            try {
              this.process.removeListener("exit", this.processExitListener);
            } catch {
            }
            this.processExitListener = null;
          }
          if (this.process) {
            try {
              this.process.kill();
            } catch {
            }
          }
          this.clearProcessState();
        }
        attachProcessExitMonitor() {
          if (!this.process) return;
          if (this.processExitListener) {
            try {
              this.process.removeListener("exit", this.processExitListener);
            } catch {
            }
          }
          this.processExitListener = () => {
            this.clearProcessState();
          };
          this.process.once("exit", this.processExitListener);
        }
        async ensureStarted() {
          if (this.baseUrl) return this.baseUrl;
          if (this.bootPromise) return this.bootPromise;
          this.bootPromise = this.startWithFallbacks().catch((e) => {
            this.bootPromise = null;
            throw e;
          });
          return this.bootPromise;
        }
        isWslSqliteLockError(status, detailText) {
          if (!this.launchContext || this.launchContext.mode !== "wsl") return false;
          if (!(Number(status) >= 500)) return false;
          const detail = String(detailText || "");
          return SQLITE_LOCK_RE.test(detail);
        }
        async stop() {
          if (this.process) this.process.kill();
          this.clearProcessState();
        }
      }
      const methods = {};
      for (const key of Object.getOwnPropertyNames(ProcessLifecycleMethods.prototype)) {
        if (key === "constructor") continue;
        methods[key] = ProcessLifecycleMethods.prototype[key];
      }
      return methods;
    }
    module2.exports = { createProcessLifecycleMethods };
  }
});

// runtime/transports/compat/request-session-methods.js
var require_request_session_methods = __commonJS({
  "runtime/transports/compat/request-session-methods.js"(exports2, module2) {
    function createRequestSessionMethods(deps = {}) {
      const {
        URL: URL2,
        nodeHttpRequestJson,
        parseModel,
        parseCommandModel,
        availableCommandSet,
        resolveCommandFromSet,
        findLatestAssistantMessage,
        looksLikeRetryableConnectionError,
        rt = (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
          const v = params[k];
          return v === void 0 || v === null ? "" : String(v);
        })
      } = deps;
      class RequestSessionMethods {
        ensureSessionAliasMap() {
          if (!(this.sessionAliasMap instanceof Map)) {
            this.sessionAliasMap = /* @__PURE__ */ new Map();
          }
          return this.sessionAliasMap;
        }
        resolveSessionAlias(sessionId) {
          const original = String(sessionId || "").trim();
          if (!original) return "";
          const map = this.ensureSessionAliasMap();
          const seen = /* @__PURE__ */ new Set();
          let current = original;
          while (current && map.has(current) && !seen.has(current)) {
            seen.add(current);
            const next = String(map.get(current) || "").trim();
            if (!next) break;
            current = next;
          }
          return current || original;
        }
        rememberSessionAlias(fromSessionId, toSessionId, source = "") {
          const from = String(fromSessionId || "").trim();
          const toRaw = String(toSessionId || "").trim();
          if (!from || !toRaw || from === toRaw) return;
          const to = this.resolveSessionAlias(toRaw);
          if (!to || from === to) return;
          const map = this.ensureSessionAliasMap();
          const previous = String(map.get(from) || "").trim();
          if (previous === to) return;
          map.set(from, to);
          if (source) {
            this.log(`session alias set ${JSON.stringify({ from, to, source })}`);
          }
        }
        ensureSessionDirectoryHintMap() {
          if (!(this.sessionDirectoryHints instanceof Map)) {
            this.sessionDirectoryHints = /* @__PURE__ */ new Map();
          }
          return this.sessionDirectoryHints;
        }
        rememberSessionDirectoryHint(sessionId, directory, source = "") {
          const sidRaw = String(sessionId || "").trim();
          const sid = this.resolveSessionAlias(sidRaw) || sidRaw;
          const rawDirectory = String(directory || "").trim();
          if (!sid || !rawDirectory) return;
          const normalized = String(this.normalizeDirectoryForService(rawDirectory) || rawDirectory).trim();
          if (!normalized) return;
          const map = this.ensureSessionDirectoryHintMap();
          const previous = String(map.get(sid) || "").trim();
          if (previous === normalized) return;
          map.set(sid, normalized);
          if (source) {
            this.log(`session directory hint set ${JSON.stringify({ sessionId: sid, directory: normalized, source })}`);
          }
        }
        getSessionScopedDirectory(sessionId) {
          const sidRaw = String(sessionId || "").trim();
          const sid = this.resolveSessionAlias(sidRaw) || sidRaw;
          if (!sid) return this.vaultPath;
          const map = this.ensureSessionDirectoryHintMap();
          const hinted = String(map.get(sid) || "").trim();
          return hinted || this.vaultPath;
        }
        buildSessionDirectoryQuery(sessionId, query = {}) {
          const next = query && typeof query === "object" ? { ...query } : {};
          const hasDirectory = Object.prototype.hasOwnProperty.call(next, "directory") && String(next.directory || "").trim().length > 0;
          if (!hasDirectory) {
            const scoped = this.getSessionScopedDirectory(sessionId);
            if (String(scoped || "").trim()) next.directory = scoped;
          }
          return next;
        }
        collectSessionDirectoryCandidates(sessionId, preferredDirectory = "") {
          const candidates = [];
          const seen = /* @__PURE__ */ new Set();
          const push = (value) => {
            const raw = String(value || "").trim();
            if (!raw) return;
            if (seen.has(raw)) return;
            seen.add(raw);
            candidates.push(raw);
          };
          push(preferredDirectory);
          push(this.getSessionScopedDirectory(sessionId));
          push(this.vaultPath);
          if (this.launchContext && this.launchContext.mode === "wsl") {
            push(this.launchContext.directory);
            if (typeof this.getDefaultWslWorkspaceDir === "function") {
              push(this.getDefaultWslWorkspaceDir());
            }
            if (typeof this.resolveWslDirectory === "function") {
              push(this.resolveWslDirectory(this.vaultPath));
            }
            if (typeof this.buildWslDirectoryCandidates === "function") {
              const extras = this.buildWslDirectoryCandidates(this.vaultPath);
              for (const item of extras) push(item);
            }
          }
          return candidates;
        }
        withWslRequestLock(task) {
          const run = typeof task === "function" ? task : async () => null;
          const chain = Promise.resolve(this.wslRequestChain).catch(() => {
          }).then(() => run());
          this.wslRequestChain = chain.catch(() => {
          });
          return chain;
        }
        async request(method, endpoint, body, query = {}, signal, retryState = {}) {
          const state = {
            wslSqliteRetried: Boolean(retryState && retryState.wslSqliteRetried),
            wslQueued: Boolean(retryState && retryState.wslQueued),
            connectionRetried: Boolean(retryState && retryState.connectionRetried)
          };
          if (this.launchContext && this.launchContext.mode === "wsl" && !state.wslQueued) {
            return this.withWslRequestLock(() => this.request(method, endpoint, body, query, signal, {
              ...state,
              wslQueued: true
            }));
          }
          const baseUrl = await this.ensureStarted();
          const url = new URL2(baseUrl + endpoint);
          for (const [k, v] of Object.entries(query || {})) {
            const value = k === "directory" ? this.normalizeDirectoryForService(v) : v;
            if (value !== void 0 && value !== null && String(value).length > 0) {
              url.searchParams.set(k, String(value));
            }
          }
          let resp;
          try {
            resp = await nodeHttpRequestJson(
              url.toString(),
              method,
              body,
              this.settings.requestTimeoutMs,
              signal,
              { trace: (line) => this.log(line) }
            );
          } catch (error) {
            const message = error instanceof Error ? error.message : String(error);
            if (!state.connectionRetried && looksLikeRetryableConnectionError(message)) {
              this.log(`request connection reset, restart service and retry once: ${message}`);
              this.cleanupProcessOnBootFailure();
              return this.request(method, endpoint, body, query, signal, {
                ...state,
                connectionRetried: true
              });
            }
            const hint = `${this.buildWslDirectoryHint(message)}${this.buildWslSqliteHint(message)}`;
            throw new Error(rt(
              "FLOWnote \u8FDE\u63A5\u5931\u8D25: {message}{hint}",
              "FLOWnote connection failed: {message}{hint}",
              { message, hint }
            ));
          }
          const text = resp.text;
          let parsed = null;
          try {
            parsed = JSON.parse(text);
          } catch {
            parsed = null;
          }
          if (resp.status < 200 || resp.status >= 300) {
            const detail = parsed ? JSON.stringify(parsed) : text;
            if (!state.wslSqliteRetried && this.isWslSqliteLockError(resp.status, detail) && this.useWslDataHomeFallback()) {
              this.cleanupProcessOnBootFailure();
              return this.request(method, endpoint, body, query, signal, {
                ...state,
                wslSqliteRetried: true
              });
            }
            const hint = `${this.buildWslDirectoryHint(detail)}${this.buildWslSqliteHint(detail)}`;
            throw new Error(rt(
              "FLOWnote \u8BF7\u6C42\u5931\u8D25 ({status}): {detail}{hint}",
              "FLOWnote request failed ({status}): {detail}{hint}",
              { status: resp.status, detail, hint }
            ));
          }
          return parsed;
        }
        parseModel() {
          return parseModel(this.settings.defaultModel);
        }
        parseCommandModel() {
          return parseCommandModel(this.settings.defaultModel);
        }
        extractSessionId(candidate) {
          const payload = candidate && typeof candidate === "object" ? candidate : null;
          if (!payload) return "";
          const direct = String(payload.id || payload.sessionID || payload.sessionId || "").trim();
          if (direct) return direct;
          const nestedCandidates = [
            payload.session,
            payload.data,
            payload.result,
            payload.payload,
            payload.item
          ];
          for (const nested of nestedCandidates) {
            if (!nested || typeof nested !== "object") continue;
            const nestedId = String(nested.id || nested.sessionID || nested.sessionId || "").trim();
            if (nestedId) return nestedId;
          }
          return "";
        }
        normalizeSessionStatus(candidate) {
          if (typeof candidate === "string") {
            const type = candidate.trim().toLowerCase();
            return type ? { type } : null;
          }
          if (!candidate || typeof candidate !== "object") return null;
          if (candidate.status && typeof candidate.status === "object" && candidate.status !== candidate) {
            const nested = this.normalizeSessionStatus(candidate.status);
            if (nested) return nested;
          }
          if (candidate.state && typeof candidate.state === "object" && candidate.state !== candidate) {
            const nested = this.normalizeSessionStatus(candidate.state);
            if (nested) return nested;
          }
          const next = { ...candidate };
          if (!next.type && typeof next.status === "string") next.type = next.status;
          if (!next.type && typeof next.state === "string") next.type = next.state;
          if (typeof next.type === "string") next.type = next.type.trim().toLowerCase();
          return next;
        }
        normalizeMessageEnvelope(envelope, depth = 0) {
          if (depth > 3) return null;
          const item = envelope && typeof envelope === "object" ? envelope : null;
          if (!item) return null;
          if (item.info && typeof item.info === "object") {
            return {
              info: item.info,
              parts: Array.isArray(item.parts) ? item.parts : []
            };
          }
          if (item.message && typeof item.message === "object") {
            return this.normalizeMessageEnvelope(item.message, depth + 1);
          }
          if (item.payload && typeof item.payload === "object") {
            return this.normalizeMessageEnvelope(item.payload, depth + 1);
          }
          const looksLikeInfo = Boolean(
            item.id || item.role || item.sessionID || item.sessionId || item.parentID || item.modelID
          );
          if (looksLikeInfo) {
            return {
              info: item,
              parts: Array.isArray(item.parts) ? item.parts : []
            };
          }
          return null;
        }
        extractMessageList(payload) {
          if (Array.isArray(payload)) {
            return payload.map((item) => this.normalizeMessageEnvelope(item)).filter(Boolean);
          }
          if (!payload || typeof payload !== "object") return [];
          const arrayCandidates = [
            payload.messages,
            payload.items,
            payload.list,
            payload.results,
            payload.result,
            payload.data,
            payload.message
          ];
          for (const candidate of arrayCandidates) {
            if (!Array.isArray(candidate)) continue;
            return candidate.map((item) => this.normalizeMessageEnvelope(item)).filter(Boolean);
          }
          const singleCandidates = [
            payload.message,
            payload.data,
            payload.item,
            payload.result,
            payload.payload,
            payload
          ];
          for (const candidate of singleCandidates) {
            const normalized = this.normalizeMessageEnvelope(candidate);
            if (normalized) return [normalized];
          }
          return [];
        }
        createMessageListFetchState(options = {}) {
          const fallbackCooldownMs = Number(options.fallbackCooldownMs);
          return {
            useUnbounded: Boolean(options.useUnbounded),
            lastFallbackAt: 0,
            fallbackCooldownMs: Number.isFinite(fallbackCooldownMs) && fallbackCooldownMs >= 0 ? fallbackCooldownMs : 1500
          };
        }
        async fetchSessionMessages(sessionId, options = {}) {
          const requestedSessionId = String(sessionId || "").trim();
          const targetSessionId = this.resolveSessionAlias(requestedSessionId) || requestedSessionId;
          const signal = options.signal;
          const startedAt = Number(options.startedAt || 0);
          const requireRecentTail = Boolean(options.requireRecentTail);
          const state = options.state && typeof options.state === "object" ? options.state : null;
          const rawLimit = Number(options.limit || 50);
          const limit = Number.isFinite(rawLimit) && rawLimit > 0 ? Math.max(1, Math.floor(rawLimit)) : 50;
          const shouldPickLatest = startedAt > 0 || requireRecentTail;
          const fetchList = async (query) => {
            const listRes = await this.request(
              "GET",
              `/session/${encodeURIComponent(targetSessionId)}/message`,
              void 0,
              query,
              signal
            );
            return this.extractMessageList(listRes && listRes.data ? listRes.data : listRes);
          };
          const fetchListWithDirectoryFallback = async (baseQuery = {}) => {
            const primaryDirectory = String(
              Object.prototype.hasOwnProperty.call(baseQuery, "directory") ? baseQuery.directory : this.getSessionScopedDirectory(sessionId)
            ).trim();
            const fallbackEnabled = Boolean(
              options.allowDirectoryFallback !== false && this.launchContext && this.launchContext.mode === "wsl"
            );
            const candidates = fallbackEnabled ? this.collectSessionDirectoryCandidates(sessionId, primaryDirectory) : [primaryDirectory || this.vaultPath];
            let firstError = null;
            let firstList = null;
            let firstDirectory = "";
            for (let i = 0; i < candidates.length; i += 1) {
              const candidate = String(candidates[i] || "").trim();
              const query = { ...baseQuery, directory: candidate };
              try {
                const list = await fetchList(query);
                if (i === 0) {
                  firstList = list;
                  firstDirectory = candidate;
                }
                if (Array.isArray(list) && list.length > 0) {
                  this.rememberSessionDirectoryHint(targetSessionId, candidate, i > 0 ? "message-list-fallback" : "message-list");
                  if (i > 0) {
                    this.log(`message list directory fallback hit ${JSON.stringify({
                      sessionId: targetSessionId,
                      directory: String(this.normalizeDirectoryForService(candidate) || candidate),
                      size: list.length
                    })}`);
                  }
                  return { list, directory: candidate, source: i > 0 ? "fallback" : "primary" };
                }
              } catch (error) {
                if (!firstError) firstError = error;
              }
            }
            if (Array.isArray(firstList)) {
              return { list: firstList, directory: firstDirectory, source: "primary-empty" };
            }
            if (firstError) throw firstError;
            return { list: [], directory: primaryDirectory, source: "empty" };
          };
          const pickLatest = (messages) => {
            if (!shouldPickLatest) return null;
            return this.findLatestAssistantMessage(messages, startedAt);
          };
          if (state && state.useUnbounded) {
            const unboundedResult2 = await fetchListWithDirectoryFallback(this.buildSessionDirectoryQuery(sessionId));
            const unbounded2 = unboundedResult2.list;
            return {
              list: unbounded2,
              latest: pickLatest(unbounded2),
              strategy: unboundedResult2.source === "fallback" ? "unbounded-directory-fallback" : "unbounded"
            };
          }
          const limitedResult = await fetchListWithDirectoryFallback(this.buildSessionDirectoryQuery(sessionId, { limit }));
          const limited = limitedResult.list;
          const limitedLatest = pickLatest(limited);
          const needUnbounded = limited.length >= limit && (requireRecentTail || startedAt > 0 && !limitedLatest);
          if (!needUnbounded) {
            return {
              list: limited,
              latest: limitedLatest,
              strategy: limitedResult.source === "fallback" ? "limited-directory-fallback" : "limited"
            };
          }
          if (state && state.fallbackCooldownMs > 0 && state.lastFallbackAt > 0) {
            const sinceLastFallback = Date.now() - Number(state.lastFallbackAt || 0);
            if (sinceLastFallback < state.fallbackCooldownMs) {
              return {
                list: limited,
                latest: limitedLatest,
                strategy: "limited-cooldown"
              };
            }
          }
          if (state) state.lastFallbackAt = Date.now();
          const unboundedResult = await fetchListWithDirectoryFallback(this.buildSessionDirectoryQuery(sessionId));
          const unbounded = unboundedResult.list;
          const unboundedLatest = pickLatest(unbounded);
          const shouldPreferUnbounded = Boolean(
            unbounded.length > limited.length || !limitedLatest && unboundedLatest
          );
          if (state && shouldPreferUnbounded) {
            state.useUnbounded = true;
            this.log(`message list switched to unbounded fetch session=${targetSessionId} limited=${limited.length} full=${unbounded.length}`);
          }
          return shouldPreferUnbounded ? {
            list: unbounded,
            latest: unboundedLatest,
            strategy: unboundedResult.source === "fallback" ? "unbounded-directory-fallback" : "unbounded"
          } : {
            list: limited,
            latest: limitedLatest,
            strategy: limitedResult.source === "fallback" ? "limited-directory-fallback" : "limited"
          };
        }
        async getSessionStatus(sessionId, signal) {
          const requestedSessionId = String(sessionId || "").trim();
          const targetSessionId = this.resolveSessionAlias(requestedSessionId) || requestedSessionId;
          try {
            const res = await this.request(
              "GET",
              "/session/status",
              void 0,
              this.buildSessionDirectoryQuery(targetSessionId),
              signal
            );
            const payload = res && res.data ? res.data : res;
            if (!payload || typeof payload !== "object") return null;
            if (Object.prototype.hasOwnProperty.call(payload, targetSessionId)) {
              const normalized2 = this.normalizeSessionStatus(payload[targetSessionId]);
              if (normalized2) return normalized2;
            }
            if (payload.sessions && typeof payload.sessions === "object" && Object.prototype.hasOwnProperty.call(payload.sessions, targetSessionId)) {
              const normalized2 = this.normalizeSessionStatus(payload.sessions[targetSessionId]);
              if (normalized2) return normalized2;
            }
            if (Array.isArray(payload)) {
              if (!payload.length) return { type: "idle" };
              const row = payload.find((item) => {
                if (!item || typeof item !== "object") return false;
                return item.id === targetSessionId || item.sessionID === targetSessionId || item.sessionId === targetSessionId;
              });
              const normalized2 = this.normalizeSessionStatus(row);
              if (normalized2) return normalized2;
            }
            if (payload.sessionID === targetSessionId || payload.sessionId === targetSessionId || payload.id === targetSessionId) {
              const normalized2 = this.normalizeSessionStatus(payload);
              if (normalized2) return normalized2;
            }
            const normalized = this.normalizeSessionStatus(payload);
            if (normalized && (normalized.type || normalized.message || normalized.reason || normalized.error)) {
              return normalized;
            }
            const hasKnownRootShape = Boolean(
              Object.prototype.hasOwnProperty.call(payload, "type") || Object.prototype.hasOwnProperty.call(payload, "status") || Object.prototype.hasOwnProperty.call(payload, "state") || Object.prototype.hasOwnProperty.call(payload, "message") || Object.prototype.hasOwnProperty.call(payload, "error") || Object.prototype.hasOwnProperty.call(payload, "reason") || Object.prototype.hasOwnProperty.call(payload, "id") || Object.prototype.hasOwnProperty.call(payload, "sessionID") || Object.prototype.hasOwnProperty.call(payload, "sessionId")
            );
            if (!hasKnownRootShape) {
              return { type: "idle" };
            }
            return null;
          } catch {
            return null;
          }
        }
        findLatestAssistantMessage(messages, startedAt) {
          return findLatestAssistantMessage(messages, startedAt);
        }
        async testConnection() {
          await this.request("GET", "/path", void 0, { directory: this.vaultPath });
          return { ok: true, mode: "compat" };
        }
        async listSessions() {
          const res = await this.request("GET", "/session", void 0, { directory: this.vaultPath });
          const payload = res && res.data ? res.data : res || [];
          const sessions = Array.isArray(payload) ? payload : Array.isArray(payload.sessions) ? payload.sessions : [];
          for (const row of sessions) {
            if (!row || typeof row !== "object") continue;
            const sid = String(row.id || row.sessionID || row.sessionId || "").trim();
            const directory = String(row.directory || "").trim();
            if (!sid || !directory) continue;
            this.rememberSessionDirectoryHint(sid, directory, "list-sessions");
          }
          if (Array.isArray(payload)) return payload;
          if (Array.isArray(payload.sessions)) return payload.sessions;
          return [];
        }
        async listSessionMessages(options = {}) {
          const requestedSessionId = String(options.sessionId || "").trim();
          const sessionId = this.resolveSessionAlias(requestedSessionId) || requestedSessionId;
          if (!sessionId) return [];
          const rawLimit = Number(options.limit || 200);
          const limit = Number.isFinite(rawLimit) && rawLimit > 0 ? Math.max(1, Math.floor(rawLimit)) : 200;
          const fetched = await this.fetchSessionMessages(sessionId, {
            signal: options.signal,
            limit,
            requireRecentTail: true,
            state: this.createMessageListFetchState()
          });
          return Array.isArray(fetched && fetched.list) ? fetched.list : [];
        }
        async createSession(title) {
          const fallbackEnabled = Boolean(this.launchContext && this.launchContext.mode === "wsl");
          const candidates = fallbackEnabled ? this.collectSessionDirectoryCandidates("", this.vaultPath) : [String(this.vaultPath || "").trim()];
          let firstPayload = null;
          let firstError = null;
          for (let i = 0; i < candidates.length; i += 1) {
            const directory = String(candidates[i] || "").trim();
            if (!directory) continue;
            try {
              const res = await this.request(
                "POST",
                "/session",
                title ? { title } : {},
                { directory }
              );
              const payload = res && res.data ? res.data : res;
              if (firstPayload === null) firstPayload = payload;
              const sessionId = this.extractSessionId(payload);
              if (!sessionId) continue;
              const out = payload && typeof payload === "object" ? { ...payload } : { id: sessionId };
              if (!String(out.id || "").trim()) out.id = sessionId;
              this.rememberSessionDirectoryHint(
                sessionId,
                directory,
                i > 0 ? "create-session-fallback" : "create-session"
              );
              return out;
            } catch (error) {
              if (!firstError) firstError = error;
            }
          }
          if (firstPayload !== null) {
            const sessionId = this.extractSessionId(firstPayload);
            if (sessionId && firstPayload && typeof firstPayload === "object" && !String(firstPayload.id || "").trim()) {
              return { ...firstPayload, id: sessionId };
            }
            return firstPayload;
          }
          if (firstError) throw firstError;
          return {};
        }
        async listModels() {
          try {
            const res = await this.request("GET", "/config/providers", void 0, { directory: this.vaultPath });
            const payload = res && res.data ? res.data : res || [];
            const providers = Array.isArray(payload) ? payload : Array.isArray(payload.providers) ? payload.providers : [];
            const out = [];
            for (const p of providers) {
              const models = p.models || {};
              for (const key of Object.keys(models)) out.push(`${p.id}/${key}`);
            }
            return out.sort();
          } catch {
            return [];
          }
        }
        async listProviders() {
          const res = await this.request("GET", "/provider", void 0, { directory: this.vaultPath });
          const payload = res && res.data ? res.data : res || {};
          const all = Array.isArray(payload.all) ? payload.all : [];
          const connected = Array.isArray(payload.connected) ? payload.connected : [];
          const defaults = payload && typeof payload.default === "object" && payload.default ? payload.default : {};
          return {
            all,
            connected,
            default: defaults
          };
        }
        async listProviderAuthMethods() {
          const res = await this.request("GET", "/provider/auth", void 0, { directory: this.vaultPath });
          const payload = res && res.data ? res.data : res || {};
          return payload && typeof payload === "object" ? payload : {};
        }
        async authorizeProviderOauth(options = {}) {
          const providerID = String(options.providerID || "").trim();
          if (!providerID) throw new Error(rt("providerID \u4E0D\u80FD\u4E3A\u7A7A", "providerID is required"));
          const method = Number(options.method);
          if (!Number.isFinite(method) || method < 0) throw new Error(rt("OAuth method \u65E0\u6548", "Invalid OAuth method"));
          const res = await this.request(
            "POST",
            `/provider/${encodeURIComponent(providerID)}/oauth/authorize`,
            { method: Number(method) },
            { directory: this.vaultPath },
            options.signal
          );
          return res && res.data ? res.data : res;
        }
        async completeProviderOauth(options = {}) {
          const providerID = String(options.providerID || "").trim();
          if (!providerID) throw new Error(rt("providerID \u4E0D\u80FD\u4E3A\u7A7A", "providerID is required"));
          const method = Number(options.method);
          if (!Number.isFinite(method) || method < 0) throw new Error(rt("OAuth method \u65E0\u6548", "Invalid OAuth method"));
          const body = { method: Number(method) };
          const code = String(options.code || "").trim();
          if (code) body.code = code;
          const res = await this.request(
            "POST",
            `/provider/${encodeURIComponent(providerID)}/oauth/callback`,
            body,
            { directory: this.vaultPath },
            options.signal
          );
          const payload = res && Object.prototype.hasOwnProperty.call(res, "data") ? res.data : res;
          return payload === void 0 ? true : Boolean(payload);
        }
        async setProviderApiKeyAuth(options = {}) {
          const providerID = String(options.providerID || "").trim();
          if (!providerID) throw new Error(rt("providerID \u4E0D\u80FD\u4E3A\u7A7A", "providerID is required"));
          const key = String(options.key || "").trim();
          if (!key) throw new Error(rt("API Key \u4E0D\u80FD\u4E3A\u7A7A", "API Key is required"));
          await this.request(
            "PUT",
            `/auth/${encodeURIComponent(providerID)}`,
            { type: "api", key },
            void 0,
            options.signal
          );
          return true;
        }
        async clearProviderAuth(options = {}) {
          const providerID = String(options.providerID || "").trim();
          if (!providerID) throw new Error(rt("providerID \u4E0D\u80FD\u4E3A\u7A7A", "providerID is required"));
          await this.request(
            "DELETE",
            `/auth/${encodeURIComponent(providerID)}`,
            void 0,
            void 0,
            options.signal
          );
          return true;
        }
        async listCommands() {
          const now = Date.now();
          if (now - this.commandCache.at < 3e4 && this.commandCache.items.length) {
            return this.commandCache.items;
          }
          try {
            const res = await this.request("GET", "/command", void 0, { directory: this.vaultPath });
            const payload = res && res.data ? res.data : res || [];
            const items = Array.isArray(payload) ? payload : Array.isArray(payload.commands) ? payload.commands : [];
            this.commandCache = {
              at: now,
              items: Array.isArray(items) ? items : []
            };
            return this.commandCache.items;
          } catch {
            return [];
          }
        }
        async resolveCommandForEndpoint(commandName) {
          const list = await this.listCommands();
          const names = availableCommandSet(list);
          return resolveCommandFromSet(commandName, names);
        }
        getFinalizeTimeoutConfig() {
          const configured = Math.max(15e3, Number(this.settings.requestTimeoutMs) || 12e4);
          const quietTimeoutMs = configured;
          const maxTotalMs = Math.max(quietTimeoutMs * 3, 10 * 60 * 1e3);
          return { quietTimeoutMs, maxTotalMs };
        }
      }
      const methods = {};
      for (const key of Object.getOwnPropertyNames(RequestSessionMethods.prototype)) {
        if (key === "constructor") continue;
        methods[key] = RequestSessionMethods.prototype[key];
      }
      return methods;
    }
    module2.exports = { createRequestSessionMethods };
  }
});

// runtime/transports/compat/response-recovery-methods.js
var require_response_recovery_methods = __commonJS({
  "runtime/transports/compat/response-recovery-methods.js"(exports2, module2) {
    var {
      isAssistantMessageEnvelopeCompleted
    } = require_completion_signals();
    function createResponseRecoveryMethods(deps = {}) {
      const {
        URL: URL2,
        sleep,
        createLinkedAbortController,
        nodeHttpRequestSse,
        createTransportEventReducer,
        pollAssistantPayload,
        ensureRenderablePayload,
        extractAssistantPayloadFromEnvelope,
        extractErrorText,
        normalizedRenderableText,
        hasRenderablePayload,
        formatSessionStatusText,
        isIntermediateToolCallPayload,
        payloadLooksInProgress,
        hasTerminalPayload,
        chooseRicherResponse,
        rt = (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
          const v = params[k];
          return v === void 0 || v === null ? "" : String(v);
        })
      } = deps;
      const QUESTION_WATCH_POLL_INTERVAL_MS = 5e3;
      const QUESTION_WATCH_ERROR_RETRY_MS = 3e3;
      function normalizeTimestampMs(value) {
        const raw = Number(value || 0);
        if (!Number.isFinite(raw) || raw <= 0) return 0;
        if (raw >= 1e14) return Math.floor(raw / 1e3);
        if (raw >= 1e12) return Math.floor(raw);
        if (raw >= 1e9) return Math.floor(raw * 1e3);
        return Math.floor(raw);
      }
      function envelopeInfo(item) {
        if (!item || typeof item !== "object") return null;
        return item.info && typeof item.info === "object" ? item.info : null;
      }
      function envelopeRole(item) {
        const info = envelopeInfo(item);
        if (!info) return "";
        return typeof info.role === "string" ? info.role.trim().toLowerCase() : "";
      }
      function envelopeCreatedAtMs(item) {
        const info = envelopeInfo(item);
        if (!info) return 0;
        const time = info.time && typeof info.time === "object" ? info.time : {};
        return normalizeTimestampMs(time.created || time.updated || info.created || info.updated || 0);
      }
      function findLatestUserAnchor(messages) {
        const list = Array.isArray(messages) ? messages : [];
        let latest = null;
        let latestCreated = 0;
        for (const item of list) {
          if (envelopeRole(item) !== "user") continue;
          const info = envelopeInfo(item);
          if (!info || typeof info.id !== "string" || !info.id.trim()) continue;
          const created = envelopeCreatedAtMs(item);
          if (!latest || created >= latestCreated) {
            latest = item;
            latestCreated = created;
          }
        }
        return latest;
      }
      function findLatestAssistantAnchoredByUser(messages, userAnchor) {
        const list = Array.isArray(messages) ? messages : [];
        const anchorInfo = envelopeInfo(userAnchor);
        const anchorId = anchorInfo && typeof anchorInfo.id === "string" ? anchorInfo.id.trim() : "";
        const anchorCreated = envelopeCreatedAtMs(userAnchor);
        if (!anchorId && !anchorCreated) return null;
        const assistants = list.filter((item) => envelopeRole(item) === "assistant").sort((a, b) => envelopeCreatedAtMs(b) - envelopeCreatedAtMs(a));
        const byParent = assistants.find((item) => {
          const info = envelopeInfo(item);
          const parentId = info && typeof info.parentID === "string" ? info.parentID.trim() : "";
          return Boolean(anchorId && parentId && parentId === anchorId);
        });
        if (byParent) return byParent;
        if (!anchorCreated) return null;
        return assistants.find((item) => {
          const created = envelopeCreatedAtMs(item);
          return Boolean(created && created >= anchorCreated);
        }) || null;
      }
      function waitWithAbort(ms, signal) {
        const waitMs = Math.max(0, Number(ms) || 0);
        if (!signal || typeof signal.addEventListener !== "function") {
          return sleep(waitMs);
        }
        if (signal.aborted) return Promise.resolve();
        return new Promise((resolve) => {
          let settled = false;
          let timer = null;
          const cleanup = () => {
            if (settled) return;
            settled = true;
            if (timer) {
              clearTimeout(timer);
              timer = null;
            }
            signal.removeEventListener("abort", onAbort);
            resolve();
          };
          const onAbort = () => cleanup();
          signal.addEventListener("abort", onAbort, { once: true });
          timer = setTimeout(cleanup, waitMs);
        });
      }
      class ResponseRecoveryMethods {
        isMessageEnvelopeCompleted(envelope) {
          return isAssistantMessageEnvelopeCompleted(envelope);
        }
        async finalizeAssistantResponse(sessionId, responsePayload, startedAt, signal, preferredMessageId = "") {
          const data = responsePayload && responsePayload.data ? responsePayload.data : responsePayload;
          const initialMessageId = preferredMessageId || (data && data.info ? data.info.id : "");
          const initialPayload = extractAssistantPayloadFromEnvelope(data);
          const timeoutCfg = this.getFinalizeTimeoutConfig();
          const quietTimeoutMs = timeoutCfg.quietTimeoutMs;
          const messageListFetchState = this.createMessageListFetchState();
          const polled = await pollAssistantPayload({
            initialMessageId,
            initialPayload,
            signal,
            quietTimeoutMs,
            maxTotalMs: timeoutCfg.maxTotalMs,
            latestIntervalMs: 1100,
            sleep,
            getByMessageId: async (messageId, requestSignal) => {
              const msgRes = await this.request(
                "GET",
                `/session/${encodeURIComponent(sessionId)}/message/${encodeURIComponent(messageId)}`,
                void 0,
                this.buildSessionDirectoryQuery(sessionId),
                requestSignal
              );
              const messagePayload = this.normalizeMessageEnvelope(msgRes && msgRes.data ? msgRes.data : msgRes);
              const role = messagePayload && messagePayload.info && typeof messagePayload.info.role === "string" ? messagePayload.info.role : "";
              if (role && role !== "assistant") {
                return { payload: null, completed: false, messageId: "" };
              }
              return {
                payload: messagePayload ? extractAssistantPayloadFromEnvelope(messagePayload) : null,
                completed: this.isMessageEnvelopeCompleted(messagePayload),
                messageId
              };
            },
            getLatest: async (requestSignal) => {
              const fetched = await this.fetchSessionMessages(sessionId, {
                signal: requestSignal,
                limit: 50,
                startedAt,
                state: messageListFetchState
              });
              const latest = fetched.latest || this.findLatestAssistantMessage(fetched.list, startedAt);
              if (!latest) return null;
              const createdAt = latest && latest.info && latest.info.time ? Number(latest.info.time.created || 0) : 0;
              return {
                payload: extractAssistantPayloadFromEnvelope(latest),
                completed: this.isMessageEnvelopeCompleted(latest),
                messageId: latest && latest.info ? latest.info.id : "",
                createdAt
              };
            },
            getSessionStatus: (requestSignal) => this.getSessionStatus(sessionId, requestSignal),
            isQuestionPending: (requestSignal) => {
              if (typeof this.hasPendingQuestionsForSession !== "function") return false;
              return this.hasPendingQuestionsForSession(sessionId, requestSignal);
            }
          });
          const hadRenderablePayload = hasRenderablePayload(polled.payload);
          if (polled.timedOut && !hadRenderablePayload) {
            const statusText = formatSessionStatusText(polled.lastStatus);
            const activeModel = String(this.settings && this.settings.defaultModel ? this.settings.defaultModel : "").trim();
            const modelText = activeModel ? rt("\u6A21\u578B {activeModel}", "Model {activeModel}", { activeModel }) : rt("\u5F53\u524D\u6A21\u578B", "Current model");
            throw new Error(rt(
              "{modelText} \u957F\u65F6\u95F4\u65E0\u54CD\u5E94\uFF0C\u53EF\u80FD\u4E0D\u53D7\u5F53\u524D\u8D26\u53F7/API Key \u652F\u6301\uFF08session.status={statusText}\uFF09\u3002\u8BF7\u5207\u6362\u6A21\u578B\u6216\u68C0\u67E5\u767B\u5F55\u914D\u7F6E\u3002",
              "{modelText} has not responded for a long time and may not be supported by current account/API key (session.status={statusText}). Switch model or check login configuration.",
              { modelText, statusText }
            ));
          }
          let payload = await ensureRenderablePayload({
            payload: polled.payload,
            lastStatus: polled.lastStatus,
            getSessionStatus: (requestSignal) => this.getSessionStatus(sessionId, requestSignal),
            signal
          });
          if (polled.timedOut) {
            this.log(`finalize timeout ${JSON.stringify({
              sessionId,
              messageId: polled.messageId,
              idleMs: Date.now() - Number(polled.lastProgressAt || polled.startedAt || Date.now()),
              quietTimeoutMs: polled.quietTimeoutMs,
              maxTotalMs: polled.maxTotalMs,
              textLen: String(payload.text || "").length,
              reasoningLen: String(payload.reasoning || "").length,
              blockCount: Array.isArray(payload.blocks) ? payload.blocks.length : 0,
              terminal: hasTerminalPayload(payload),
              inProgress: payloadLooksInProgress(payload)
            })}`);
          }
          if (isIntermediateToolCallPayload(payload) && normalizedRenderableText(payload.text || "").length <= 1) {
            payload = { ...payload, text: "" };
          }
          return {
            messageId: polled.messageId || "",
            text: payload.text || "",
            reasoning: payload.reasoning || "",
            meta: payload.meta || "",
            blocks: payload.blocks || [],
            completed: Boolean(polled.completed)
          };
        }
        async streamAssistantFromPolling(sessionId, startedAt, signal, handlers, options = {}) {
          const timeoutCfg = this.getFinalizeTimeoutConfig();
          const pollOptions = options && typeof options === "object" ? options : {};
          const quickFallback = Boolean(pollOptions.quickFallback);
          const requireTerminalStatusOnCompletion = Boolean(pollOptions.requireTerminalStatusOnCompletion);
          const quietTimeoutMs = Number.isFinite(Number(pollOptions.quietTimeoutMs)) && Number(pollOptions.quietTimeoutMs) > 0 ? Number(pollOptions.quietTimeoutMs) : quickFallback ? Math.min(timeoutCfg.quietTimeoutMs, 5e3) : timeoutCfg.quietTimeoutMs;
          const maxTotalMs = Number.isFinite(Number(pollOptions.maxTotalMs)) && Number(pollOptions.maxTotalMs) > 0 ? Number(pollOptions.maxTotalMs) : quickFallback ? Math.min(timeoutCfg.maxTotalMs, 8e3) : timeoutCfg.maxTotalMs;
          const latestIntervalMs = Number.isFinite(Number(pollOptions.latestIntervalMs)) && Number(pollOptions.latestIntervalMs) > 0 ? Number(pollOptions.latestIntervalMs) : quickFallback ? 700 : 1100;
          const noMessageTimeoutMs = Number.isFinite(Number(pollOptions.noMessageTimeoutMs)) && Number(pollOptions.noMessageTimeoutMs) > 0 ? Number(pollOptions.noMessageTimeoutMs) : void 0;
          const allowQuestionPending = pollOptions.allowQuestionPending !== false;
          const messageListFetchState = this.createMessageListFetchState();
          const polled = await pollAssistantPayload({
            signal,
            quietTimeoutMs,
            maxTotalMs,
            latestIntervalMs,
            noMessageTimeoutMs,
            sleep,
            requireTerminal: false,
            requireTerminalStatusOnCompletion,
            onToken: handlers && handlers.onToken,
            onReasoning: handlers && handlers.onReasoning,
            onBlocks: handlers && handlers.onBlocks,
            getByMessageId: async (messageId, requestSignal) => {
              const msgRes = await this.request(
                "GET",
                `/session/${encodeURIComponent(sessionId)}/message/${encodeURIComponent(messageId)}`,
                void 0,
                this.buildSessionDirectoryQuery(sessionId),
                requestSignal
              );
              const messagePayload = this.normalizeMessageEnvelope(msgRes && msgRes.data ? msgRes.data : msgRes);
              const role = messagePayload && messagePayload.info && typeof messagePayload.info.role === "string" ? messagePayload.info.role : "";
              if (role && role !== "assistant") {
                return { payload: null, completed: false, messageId: "" };
              }
              return {
                payload: messagePayload ? extractAssistantPayloadFromEnvelope(messagePayload) : null,
                completed: this.isMessageEnvelopeCompleted(messagePayload),
                messageId
              };
            },
            getLatest: async (requestSignal) => {
              const fetched = await this.fetchSessionMessages(sessionId, {
                signal: requestSignal,
                limit: 50,
                startedAt,
                state: messageListFetchState
              });
              const latest = fetched.latest || this.findLatestAssistantMessage(fetched.list, startedAt);
              if (!latest) return null;
              const createdAt = latest && latest.info && latest.info.time ? Number(latest.info.time.created || 0) : 0;
              return {
                payload: extractAssistantPayloadFromEnvelope(latest),
                completed: this.isMessageEnvelopeCompleted(latest),
                messageId: latest && latest.info ? latest.info.id : "",
                createdAt
              };
            },
            getSessionStatus: (requestSignal) => this.getSessionStatus(sessionId, requestSignal),
            isQuestionPending: allowQuestionPending ? (requestSignal) => {
              if (typeof this.hasPendingQuestionsForSession !== "function") return false;
              return this.hasPendingQuestionsForSession(sessionId, requestSignal);
            } : void 0
          });
          return {
            messageId: polled.messageId || "",
            text: polled.payload && polled.payload.text ? polled.payload.text : "",
            reasoning: polled.payload && polled.payload.reasoning ? polled.payload.reasoning : "",
            meta: polled.payload && polled.payload.meta ? polled.payload.meta : "",
            blocks: polled.payload && Array.isArray(polled.payload.blocks) ? polled.payload.blocks : [],
            completed: Boolean(polled.completed)
          };
        }
        async streamAssistantFromEvents(sessionId, startedAt, signal, handlers) {
          const baseUrl = await this.ensureStarted();
          const sessionDirectory = this.getSessionScopedDirectory(sessionId);
          const expectedDirectory = this.normalizeDirectoryForService(sessionDirectory) || sessionDirectory;
          const reducer = createTransportEventReducer({
            sessionId,
            startedAt,
            onToken: handlers && handlers.onToken,
            onReasoning: handlers && handlers.onReasoning,
            onBlocks: handlers && handlers.onBlocks,
            onPermissionRequest: (permission, permissionId) => {
              if (!handlers || typeof handlers.onPermissionRequest !== "function") return;
              Promise.resolve(handlers.onPermissionRequest(permission || {})).then((response) => {
                if (!response || !["once", "always", "reject"].includes(response)) return;
                return this.replyPermission({
                  sessionId,
                  permissionId,
                  response,
                  signal
                });
              }).catch((e) => {
                this.log(`permission handler failed: ${e instanceof Error ? e.message : String(e)}`);
              });
            },
            onQuestionRequest: handlers && handlers.onQuestionRequest,
            onQuestionResolved: handlers && handlers.onQuestionResolved,
            onPromptAppend: handlers && handlers.onPromptAppend,
            onToast: handlers && handlers.onToast,
            permissionEventTypes: ["permission.updated", "permission.asked"]
          });
          const eventCandidates = this.buildEventStreamUrlCandidates(baseUrl, expectedDirectory);
          let lastError = null;
          for (const candidate of eventCandidates) {
            try {
              await nodeHttpRequestSse(
                candidate.url,
                Math.max(3e3, Number(this.settings.requestTimeoutMs) || 12e4),
                signal,
                {
                  onEvent: (raw) => {
                    const root = raw && typeof raw === "object" ? raw : null;
                    if (root && typeof root.directory === "string" && root.directory) {
                      const eventDir = String(root.directory || "").trim();
                      const originDir = String(sessionDirectory || "").trim();
                      if (eventDir !== expectedDirectory && eventDir !== originDir) return;
                    }
                    const event = root && root.payload && typeof root.payload === "object" ? root.payload : root;
                    if (!event || typeof event !== "object") return;
                    reducer.consume(event);
                  },
                  shouldStop: () => reducer.isDone()
                },
                { trace: (line) => this.log(line) }
              );
              return reducer.snapshot();
            } catch (error) {
              if (signal && signal.aborted) throw error;
              lastError = error;
              this.log(
                `event stream endpoint failed (${candidate.path}): ${error instanceof Error ? error.message : String(error)}`
              );
            }
          }
          if (lastError) throw lastError;
          throw new Error(rt("FLOWnote \u4E8B\u4EF6\u6D41\u8FDE\u63A5\u5931\u8D25", "FLOWnote event stream connection failed"));
        }
        buildEventStreamUrlCandidates(baseUrl, directory) {
          const paths = ["/event", "/global/event"];
          const seen = /* @__PURE__ */ new Set();
          const output = [];
          for (const endpointPath of paths) {
            const url = new URL2(baseUrl + endpointPath);
            if (directory) url.searchParams.set("directory", directory);
            const normalized = url.toString();
            if (seen.has(normalized)) continue;
            seen.add(normalized);
            output.push({ path: endpointPath, url: normalized });
          }
          return output;
        }
        createQuestionWatch(sessionId, signal, handlers) {
          const onQuestionRequest = handlers && typeof handlers.onQuestionRequest === "function" ? handlers.onQuestionRequest : null;
          const onQuestionResolved = handlers && typeof handlers.onQuestionResolved === "function" ? handlers.onQuestionResolved : null;
          if (!onQuestionRequest && !onQuestionResolved) {
            return {
              stop: async () => {
              }
            };
          }
          const linked = createLinkedAbortController(signal);
          const watcherPromise = this.watchPendingQuestions(sessionId, linked.controller.signal, {
            onQuestionRequest,
            onQuestionResolved
          }).catch((error) => {
            if (linked.controller.signal.aborted) return;
            this.log(`question watcher failed: ${error instanceof Error ? error.message : String(error)}`);
          });
          return {
            stop: async () => {
              linked.detach();
              linked.controller.abort();
              await watcherPromise;
            }
          };
        }
        async watchPendingQuestions(sessionId, signal, handlers = {}) {
          const onQuestionRequest = typeof handlers.onQuestionRequest === "function" ? handlers.onQuestionRequest : null;
          const onQuestionResolved = typeof handlers.onQuestionResolved === "function" ? handlers.onQuestionResolved : null;
          if (!onQuestionRequest && !onQuestionResolved) return;
          const seenByRequestId = /* @__PURE__ */ new Map();
          let endpointUnavailable = false;
          while (!(signal && signal.aborted)) {
            let listed = [];
            try {
              listed = await this.listQuestions({
                signal,
                sessionId,
                allowDirectoryFallback: false
              });
            } catch (error) {
              if (signal && signal.aborted) break;
              const message = error instanceof Error ? error.message : String(error);
              if (/\b404\b|not found|unknown endpoint|\/question/i.test(message)) {
                if (!endpointUnavailable) {
                  endpointUnavailable = true;
                  this.log(`question watcher disabled: ${message}`);
                }
                break;
              }
              this.log(`question watcher poll failed: ${message}`);
              await waitWithAbort(QUESTION_WATCH_ERROR_RETRY_MS, signal);
              continue;
            }
            const activeByRequestId = /* @__PURE__ */ new Map();
            for (const item of Array.isArray(listed) ? listed : []) {
              if (!item || typeof item !== "object") continue;
              const requestId = String(item.id || item.requestID || item.requestId || "").trim();
              if (!requestId) continue;
              const reqSession = String(item.sessionID || item.sessionId || "").trim();
              if (reqSession && reqSession !== sessionId) continue;
              activeByRequestId.set(requestId, item);
            }
            for (const [requestId, request] of activeByRequestId.entries()) {
              if (!seenByRequestId.has(requestId) && onQuestionRequest) {
                onQuestionRequest(request);
              }
            }
            if (onQuestionResolved) {
              for (const requestId of seenByRequestId.keys()) {
                if (activeByRequestId.has(requestId)) continue;
                onQuestionResolved({
                  requestId,
                  sessionId,
                  answers: [],
                  rejected: false
                });
              }
            }
            seenByRequestId.clear();
            for (const [requestId, request] of activeByRequestId.entries()) {
              seenByRequestId.set(requestId, request);
            }
            await waitWithAbort(QUESTION_WATCH_POLL_INTERVAL_MS, signal);
          }
        }
        isUnknownStatusFallbackText(text) {
          const value = String(text || "").trim();
          if (!value) return false;
          return /^\((无文本返回：|No text returned: )session\.status=/i.test(value);
        }
        async trySyncMessageRecovery(sessionId, messageBody, signal, streamedMessageId = "") {
          const body = messageBody && typeof messageBody === "object" ? { ...messageBody } : null;
          if (!body || !Array.isArray(body.parts) || !body.parts.length) return null;
          const messageListFetchState = this.createMessageListFetchState();
          const placeholderId = String(streamedMessageId || "").trim();
          let parentMessageId = "";
          if (placeholderId) {
            try {
              const placeholderRes = await this.request(
                "GET",
                `/session/${encodeURIComponent(sessionId)}/message/${encodeURIComponent(placeholderId)}`,
                void 0,
                this.buildSessionDirectoryQuery(sessionId),
                signal
              );
              const placeholder = this.normalizeMessageEnvelope(
                placeholderRes && placeholderRes.data ? placeholderRes.data : placeholderRes
              );
              const info = placeholder && placeholder.info && typeof placeholder.info === "object" ? placeholder.info : null;
              if (info && info.role === "assistant" && typeof info.parentID === "string" && info.parentID.trim()) {
                parentMessageId = info.parentID.trim();
              }
            } catch (error) {
              this.log(`sync recovery inspect failed: ${error instanceof Error ? error.message : String(error)}`);
            }
          }
          if (parentMessageId && !body.messageID) {
            body.messageID = parentMessageId;
          }
          if (!body.messageID) {
            try {
              const fetched = await this.fetchSessionMessages(sessionId, {
                signal,
                limit: 50,
                requireRecentTail: true,
                state: messageListFetchState
              });
              const listPayload = Array.isArray(fetched.list) ? fetched.list : [];
              let latestUser = null;
              let latestUserCreated = 0;
              for (const item of listPayload) {
                if (!item || typeof item !== "object") continue;
                const info = item.info && typeof item.info === "object" ? item.info : null;
                if (!info || info.role !== "user" || typeof info.id !== "string" || !info.id.trim()) continue;
                const time = info.time && typeof info.time === "object" ? info.time : {};
                const created = Number(time.created || info.created || info.updated || 0);
                if (created >= latestUserCreated) {
                  latestUserCreated = created;
                  latestUser = info.id.trim();
                }
              }
              if (latestUser) {
                body.messageID = latestUser;
              }
            } catch (error) {
              this.log(`sync recovery user-anchor failed: ${error instanceof Error ? error.message : String(error)}`);
            }
          }
          const pickAssistantEnvelope = (items, anchorMessageId = "") => {
            const list = Array.isArray(items) ? items : [];
            if (!list.length) return null;
            const anchor = String(anchorMessageId || "").trim();
            if (anchor) {
              const anchored = list.filter((row) => {
                const info = row && row.info && typeof row.info === "object" ? row.info : null;
                return Boolean(
                  info && typeof info.parentID === "string" && info.parentID.trim() && info.parentID.trim() === anchor
                );
              }).sort((a, b) => {
                const ai = a && a.info && typeof a.info === "object" ? a.info : {};
                const bi = b && b.info && typeof b.info === "object" ? b.info : {};
                const at = Number(ai.time && ai.time.created || ai.created || 0);
                const bt = Number(bi.time && bi.time.created || bi.created || 0);
                return bt - at;
              });
              if (anchored.length) return anchored[0];
            }
            return this.findLatestAssistantMessage(list, 0);
          };
          try {
            const syncRes = await this.request(
              "POST",
              `/session/${encodeURIComponent(sessionId)}/message`,
              body,
              this.buildSessionDirectoryQuery(sessionId),
              signal
            );
            const raw = syncRes && syncRes.data ? syncRes.data : syncRes;
            const direct = this.normalizeMessageEnvelope(raw);
            if (direct && direct.info && typeof direct.info === "object" && (direct.info.role === "assistant" || direct.info.type === "assistant" || direct.info.error)) {
              const payload = extractAssistantPayloadFromEnvelope(direct);
              return {
                messageId: direct.info && direct.info.id ? String(direct.info.id) : placeholderId,
                text: String(payload.text || ""),
                reasoning: String(payload.reasoning || ""),
                meta: String(payload.meta || ""),
                blocks: Array.isArray(payload.blocks) ? payload.blocks : [],
                completed: true
              };
            }
            const fromResponseList = pickAssistantEnvelope(this.extractMessageList(raw), body.messageID);
            if (fromResponseList) {
              const payload = extractAssistantPayloadFromEnvelope(fromResponseList);
              return {
                messageId: fromResponseList.info && fromResponseList.info.id ? String(fromResponseList.info.id) : placeholderId,
                text: String(payload.text || ""),
                reasoning: String(payload.reasoning || ""),
                meta: String(payload.meta || ""),
                blocks: Array.isArray(payload.blocks) ? payload.blocks : [],
                completed: true
              };
            }
            for (let i = 0; i < 6; i += 1) {
              if (signal && signal.aborted) break;
              await sleep(220);
              const fetched = await this.fetchSessionMessages(sessionId, {
                signal,
                limit: 50,
                requireRecentTail: true,
                state: messageListFetchState
              });
              const listPayload = Array.isArray(fetched.list) ? fetched.list : [];
              const envelope = pickAssistantEnvelope(listPayload, body.messageID);
              if (!envelope) continue;
              const payload = extractAssistantPayloadFromEnvelope(envelope);
              return {
                messageId: envelope.info && envelope.info.id ? String(envelope.info.id) : placeholderId,
                text: String(payload.text || ""),
                reasoning: String(payload.reasoning || ""),
                meta: String(payload.meta || ""),
                blocks: Array.isArray(payload.blocks) ? payload.blocks : [],
                completed: true
              };
            }
            return null;
          } catch (error) {
            this.log(`sync recovery failed: ${error instanceof Error ? error.message : String(error)}`);
            return null;
          }
        }
        async reconcileAssistantResponseQuick(sessionId, currentPayload, startedAt, signal, preferredMessageId = "") {
          let merged = currentPayload && typeof currentPayload === "object" ? { ...currentPayload } : { messageId: "", text: "", reasoning: "", meta: "", blocks: [], completed: false };
          const messageIdHint = String(
            preferredMessageId || (merged && typeof merged.messageId === "string" ? merged.messageId : "")
          ).trim();
          if (messageIdHint) {
            try {
              const msgRes = await this.request(
                "GET",
                `/session/${encodeURIComponent(sessionId)}/message/${encodeURIComponent(messageIdHint)}`,
                void 0,
                this.buildSessionDirectoryQuery(sessionId),
                signal
              );
              const messagePayload = this.normalizeMessageEnvelope(msgRes && msgRes.data ? msgRes.data : msgRes);
              const role = messagePayload && messagePayload.info && typeof messagePayload.info.role === "string" ? messagePayload.info.role : "";
              if (!role || role === "assistant") {
                const payload = messagePayload ? extractAssistantPayloadFromEnvelope(messagePayload) : null;
                if (payload) {
                  merged = chooseRicherResponse(merged, {
                    messageId: messageIdHint,
                    text: String(payload.text || ""),
                    reasoning: String(payload.reasoning || ""),
                    meta: String(payload.meta || ""),
                    blocks: Array.isArray(payload.blocks) ? payload.blocks : [],
                    completed: this.isMessageEnvelopeCompleted(messagePayload)
                  });
                }
              }
            } catch (error) {
              this.log(`quick reconcile by-id failed: ${error instanceof Error ? error.message : String(error)}`);
            }
          }
          try {
            const fetched = await this.fetchSessionMessages(sessionId, {
              signal,
              limit: 20,
              startedAt,
              requireRecentTail: true,
              state: this.createMessageListFetchState()
            });
            const list = Array.isArray(fetched && fetched.list) ? fetched.list : [];
            let latest = fetched.latest || this.findLatestAssistantMessage(list, startedAt);
            if (!latest && list.length) {
              const latestUser = findLatestUserAnchor(list);
              latest = findLatestAssistantAnchoredByUser(list, latestUser) || this.findLatestAssistantMessage(list, 0);
            }
            if (latest) {
              const payload = extractAssistantPayloadFromEnvelope(latest);
              merged = chooseRicherResponse(merged, {
                messageId: latest && latest.info && latest.info.id ? String(latest.info.id) : messageIdHint,
                text: String(payload.text || ""),
                reasoning: String(payload.reasoning || ""),
                meta: String(payload.meta || ""),
                blocks: Array.isArray(payload.blocks) ? payload.blocks : [],
                completed: this.isMessageEnvelopeCompleted(latest)
              });
            }
          } catch (error) {
            this.log(`quick reconcile latest failed: ${error instanceof Error ? error.message : String(error)}`);
          }
          return merged;
        }
      }
      const methods = {};
      for (const key of Object.getOwnPropertyNames(ResponseRecoveryMethods.prototype)) {
        if (key === "constructor") continue;
        methods[key] = ResponseRecoveryMethods.prototype[key];
      }
      return methods;
    }
    module2.exports = { createResponseRecoveryMethods };
  }
});

// runtime/transports/compat/send-message-methods.js
var require_send_message_methods = __commonJS({
  "runtime/transports/compat/send-message-methods.js"(exports2, module2) {
    function createSendMessageMethods(deps = {}) {
      const {
        streamPseudo,
        sleep,
        createLinkedAbortController,
        extractErrorText,
        normalizedRenderableText,
        hasRenderablePayload,
        formatSessionStatusText,
        payloadLooksInProgress,
        extractAssistantPayloadFromEnvelope,
        sessionStatusLooksAuthFailure,
        rt = (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
          const v = params[k];
          return v === void 0 || v === null ? "" : String(v);
        })
      } = deps;
      function isRecoverableRequestError(error) {
        const message = error instanceof Error ? error.message : String(error || "");
        return /请求超时|timeout|timed out|ECONNRESET|ECONNREFUSED|EPIPE|socket hang up|connection reset/i.test(message);
      }
      function delayMs(ms) {
        const waitMs = Number.isFinite(Number(ms)) ? Math.max(0, Number(ms)) : 0;
        if (typeof sleep === "function") return sleep(waitMs);
        return new Promise((resolve) => setTimeout(resolve, waitMs));
      }
      function normalizeTimestampMs(value) {
        const raw = Number(value || 0);
        if (!Number.isFinite(raw) || raw <= 0) return 0;
        if (raw >= 1e14) return Math.floor(raw / 1e3);
        if (raw >= 1e12) return Math.floor(raw);
        if (raw >= 1e9) return Math.floor(raw * 1e3);
        return Math.floor(raw);
      }
      function summarizeEnvelope(envelope) {
        if (!envelope || typeof envelope !== "object") return null;
        const info = envelope.info && typeof envelope.info === "object" ? envelope.info : {};
        const parts = Array.isArray(envelope.parts) ? envelope.parts : [];
        const partTypes = parts.slice(0, 10).map((part) => part && typeof part === "object" && typeof part.type === "string" ? part.type : "unknown");
        const time = info.time && typeof info.time === "object" ? info.time : {};
        return {
          id: typeof info.id === "string" ? info.id : "",
          role: typeof info.role === "string" ? info.role : "",
          parentID: typeof info.parentID === "string" ? info.parentID : "",
          createdAt: normalizeTimestampMs(time.created || info.created || 0),
          completedAt: normalizeTimestampMs(time.completed || 0),
          hasError: Boolean(info.error),
          partCount: parts.length,
          partTypes
        };
      }
      function summarizePayload(payload) {
        if (!payload || typeof payload !== "object") return null;
        return {
          messageId: String(payload.messageId || ""),
          textLen: String(payload.text || "").length,
          reasoningLen: String(payload.reasoning || "").length,
          metaLen: String(payload.meta || "").length,
          blockCount: Array.isArray(payload.blocks) ? payload.blocks.length : 0,
          completed: Boolean(payload.completed),
          inProgress: payloadLooksInProgress(payload)
        };
      }
      function summarizeMessageList(list, startedAt) {
        const rows = Array.isArray(list) ? list : [];
        const normalizedStartedAt = Number(startedAt || 0);
        const mapped = rows.map((item) => {
          const info = item && item.info && typeof item.info === "object" ? item.info : {};
          const role = typeof info.role === "string" ? info.role : "";
          const id = typeof info.id === "string" ? info.id : "";
          const parentID = typeof info.parentID === "string" ? info.parentID : "";
          const time = info.time && typeof info.time === "object" ? info.time : {};
          const createdAt = normalizeTimestampMs(time.created || info.created || 0);
          const hasError = Boolean(info.error);
          const parts = Array.isArray(item && item.parts) ? item.parts : [];
          const partTypes = parts.slice(0, 5).map((part) => part && typeof part === "object" && typeof part.type === "string" ? part.type : "unknown");
          return { role, id, parentID, createdAt, hasError, partCount: parts.length, partTypes };
        });
        const latestAssistant = mapped.filter((row) => row.role === "assistant").sort((a, b) => b.createdAt - a.createdAt)[0] || null;
        const latestAssistantAfterStartedAt = mapped.filter((row) => row.role === "assistant").filter((row) => !normalizedStartedAt || !row.createdAt || row.createdAt >= normalizedStartedAt - 1e3).sort((a, b) => b.createdAt - a.createdAt)[0] || null;
        const latestUser = mapped.filter((row) => row.role === "user").sort((a, b) => b.createdAt - a.createdAt)[0] || null;
        const tail = mapped.slice(Math.max(0, mapped.length - 6));
        return {
          total: mapped.length,
          latestAssistant,
          latestAssistantAfterStartedAt,
          latestUser,
          tail
        };
      }
      function safeJson(value) {
        try {
          return JSON.stringify(value);
        } catch (error) {
          return JSON.stringify({
            error: `json-stringify-failed: ${error instanceof Error ? error.message : String(error)}`
          });
        }
      }
      function buildNoRenderableDiagHint(diag) {
        const d = diag && typeof diag === "object" ? diag : null;
        if (!d) return "";
        const total = Number.isFinite(Number(d.total)) ? Number(d.total) : 0;
        const latestAssistantId = String(d.latestAssistantId || "").trim() || "-";
        const latestAssistantAfterStartedAtId = String(d.latestAssistantAfterStartedAtId || "").trim() || "-";
        const latestUserId = String(d.latestUserId || "").trim() || "-";
        return `diag(total=${total},latestAssistant=${latestAssistantId},latestAssistantAfterStart=${latestAssistantAfterStartedAtId},latestUser=${latestUserId})`;
      }
      function extractSessionId(candidate) {
        const payload = candidate && typeof candidate === "object" ? candidate : null;
        if (!payload) return "";
        const direct = String(payload.id || payload.sessionID || payload.sessionId || "").trim();
        if (direct) return direct;
        const nestedCandidates = [payload.session, payload.data, payload.result, payload.payload, payload.item];
        for (const nested of nestedCandidates) {
          if (!nested || typeof nested !== "object") continue;
          const nestedId = String(nested.id || nested.sessionID || nested.sessionId || "").trim();
          if (nestedId) return nestedId;
        }
        return "";
      }
      class SendMessageMethods {
        async sendMessage(options) {
          const requestedSessionId = String(options && options.sessionId ? options.sessionId : "").trim();
          const sessionId = this.resolveSessionAlias(requestedSessionId) || requestedSessionId;
          if (!sessionId) throw new Error(rt("sessionId \u4E0D\u80FD\u4E3A\u7A7A", "sessionId is required"));
          this.log(`sendMessage start ${JSON.stringify({
            sessionId,
            requestedSessionId: requestedSessionId && requestedSessionId !== sessionId ? requestedSessionId : "",
            transport: "compat",
            streaming: Boolean(this.settings.enableStreaming)
          })}`);
          const deliveredQuestionIds = /* @__PURE__ */ new Set();
          let waitingForQuestion = false;
          let pendingQuestionSyncAt = 0;
          let pendingQuestionSyncResult = false;
          let pendingQuestionSyncPromise = null;
          let isCommandRequest = false;
          const handleQuestionRequest = (request) => {
            const requestId = String(
              request && (request.id || request.requestID || request.requestId) || ""
            ).trim();
            if (requestId && deliveredQuestionIds.has(requestId)) return;
            if (requestId) deliveredQuestionIds.add(requestId);
            waitingForQuestion = deliveredQuestionIds.size > 0;
            if (typeof options.onQuestionRequest === "function") {
              options.onQuestionRequest(request);
            }
          };
          const handleQuestionResolved = (info) => {
            const requestId = String(info && info.requestId || "").trim();
            if (requestId) deliveredQuestionIds.delete(requestId);
            waitingForQuestion = deliveredQuestionIds.size > 0;
            if (typeof options.onQuestionResolved === "function") {
              options.onQuestionResolved(info);
            }
          };
          const syncPendingQuestionsForSession = async (force = false) => {
            if (isCommandRequest) {
              waitingForQuestion = false;
              return false;
            }
            if (!force) {
              if (pendingQuestionSyncPromise) return pendingQuestionSyncPromise;
              if (Date.now() - pendingQuestionSyncAt < 450) return pendingQuestionSyncResult;
            }
            const run = (async () => {
              const listed = await this.listQuestions({
                sessionId,
                signal: options.signal
              });
              const activeRequestIds = /* @__PURE__ */ new Set();
              if (Array.isArray(listed)) {
                for (const request of listed) {
                  if (!request || typeof request !== "object") continue;
                  const reqSession = String(request.sessionID || request.sessionId || "").trim();
                  if (reqSession && reqSession !== sessionId) continue;
                  const requestId = String(request.id || request.requestID || request.requestId || "").trim();
                  if (!requestId) continue;
                  activeRequestIds.add(requestId);
                  handleQuestionRequest(request);
                }
              }
              if (deliveredQuestionIds.size) {
                for (const requestId of Array.from(deliveredQuestionIds)) {
                  if (!activeRequestIds.has(requestId)) {
                    handleQuestionResolved({ requestId, sessionId });
                  }
                }
              }
              waitingForQuestion = activeRequestIds.size > 0;
              pendingQuestionSyncResult = waitingForQuestion;
              pendingQuestionSyncAt = Date.now();
              return waitingForQuestion;
            })();
            pendingQuestionSyncPromise = run;
            try {
              return await run;
            } catch (error) {
              if (deliveredQuestionIds.size > 0 || waitingForQuestion) {
                waitingForQuestion = true;
                pendingQuestionSyncResult = true;
                pendingQuestionSyncAt = Date.now();
                return true;
              }
              this.log(`sync pending questions failed: ${error instanceof Error ? error.message : String(error)}`);
              pendingQuestionSyncResult = false;
              pendingQuestionSyncAt = Date.now();
              return false;
            } finally {
              if (pendingQuestionSyncPromise === run) pendingQuestionSyncPromise = null;
            }
          };
          const checkWaitingForQuestion = async () => {
            if (isCommandRequest) return false;
            if (deliveredQuestionIds.size > 0) {
              return syncPendingQuestionsForSession(true);
            }
            if (waitingForQuestion) {
              return syncPendingQuestionsForSession(false);
            }
            return syncPendingQuestionsForSession(false);
          };
          const questionWatch = this.createQuestionWatch(sessionId, options.signal, {
            onQuestionRequest: handleQuestionRequest,
            onQuestionResolved: handleQuestionResolved
          });
          const emitNoRenderableDiag = (label, payload) => {
            const line = `${label} ${safeJson(payload)}`;
            this.log(line);
          };
          try {
            const startedAt = Date.now();
            const sessionQuery = this.buildSessionDirectoryQuery(sessionId);
            const model = this.parseModel();
            const commandModel = this.parseCommandModel();
            const parsedCommand = this.parseSlashCommand(options.prompt);
            const resolvedCommand = parsedCommand ? await this.resolveCommandForEndpoint(parsedCommand.command) : { use: false, command: "" };
            isCommandRequest = Boolean(parsedCommand && resolvedCommand.use);
            if (isCommandRequest) {
              this.log(`compat command route ${JSON.stringify({
                sessionId,
                command: resolvedCommand.command
              })}`);
            }
            let res = null;
            let streamed = null;
            let recoveredAfterRequestFailure = false;
            const commandBody = isCommandRequest ? {
              command: resolvedCommand.command,
              arguments: parsedCommand.arguments
            } : null;
            if (commandBody && commandModel) commandBody.model = commandModel;
            const effectivePrompt = parsedCommand ? options.prompt.replace(/^\//, "").trim() : options.prompt;
            const messageBody = {
              noReply: false,
              parts: [{ type: "text", text: effectivePrompt || options.prompt }]
            };
            if (model) messageBody.model = model;
            const canRecoverFromRequestFailurePayload = async (candidate) => {
              if (!candidate || !hasRenderablePayload(candidate)) return false;
              if (payloadLooksInProgress(candidate)) return Boolean(await checkWaitingForQuestion());
              if (Boolean(candidate.completed)) return true;
              return Boolean(await checkWaitingForQuestion());
            };
            const recoverFromRequestFailure = async (requestError, handlers = null, recoveryOptions = null) => {
              if (!isRecoverableRequestError(requestError)) return null;
              if (options.signal && options.signal.aborted) return null;
              this.log(
                `request failed and fallback to polling final response: ${requestError instanceof Error ? requestError.message : String(requestError)}`
              );
              const pollRecoveryOptions = recoveryOptions && typeof recoveryOptions === "object" ? recoveryOptions : {};
              try {
                const recovered = await this.streamAssistantFromPolling(
                  sessionId,
                  startedAt,
                  options.signal,
                  handlers,
                  {
                    allowQuestionPending: true,
                    ...pollRecoveryOptions
                  }
                );
                if (await canRecoverFromRequestFailurePayload(recovered)) return recovered;
              } catch (pollError) {
                this.log(
                  `request failure polling recovery failed: ${pollError instanceof Error ? pollError.message : String(pollError)}`
                );
              }
              return null;
            };
            if (this.settings.enableStreaming) {
              const linked = createLinkedAbortController(options.signal);
              const eventSignal = linked.controller.signal;
              let eventStreamStopRequested = false;
              let requestError = null;
              const streamHandlers = {
                onToken: options.onToken,
                onReasoning: options.onReasoning,
                onBlocks: options.onBlocks,
                onPermissionRequest: options.onPermissionRequest,
                onQuestionRequest: handleQuestionRequest,
                onQuestionResolved: handleQuestionResolved,
                onPromptAppend: options.onPromptAppend,
                onToast: options.onToast
              };
              const eventStreamPromise = this.streamAssistantFromEvents(sessionId, startedAt, eventSignal, streamHandlers).catch(async (e) => {
                const message = e instanceof Error ? e.message : String(e);
                if (options.signal && options.signal.aborted) {
                  this.log(`event stream aborted by user: ${message}`);
                  return null;
                }
                if (eventStreamStopRequested && /用户取消了请求|aborted|abort|cancelled|canceled/i.test(message)) {
                  this.log(`event stream closed after request completion: ${message}`);
                  return null;
                }
                this.log(`event stream unavailable: ${message}; fallback to polling stream`);
                try {
                  return await this.streamAssistantFromPolling(
                    sessionId,
                    startedAt,
                    options.signal,
                    streamHandlers,
                    {
                      quickFallback: true,
                      noMessageTimeoutMs: 1800,
                      quietTimeoutMs: 5e3,
                      maxTotalMs: 8e3,
                      latestIntervalMs: 700,
                      allowQuestionPending: false
                    }
                  );
                } catch (pollError) {
                  this.log(`polling stream fallback failed: ${pollError instanceof Error ? pollError.message : String(pollError)}`);
                  return null;
                }
              });
              try {
                if (isCommandRequest) {
                  res = await this.request(
                    "POST",
                    `/session/${encodeURIComponent(sessionId)}/command`,
                    commandBody,
                    sessionQuery,
                    options.signal
                  );
                } else {
                  res = await this.request(
                    "POST",
                    `/session/${encodeURIComponent(sessionId)}/message`,
                    messageBody,
                    sessionQuery,
                    options.signal
                  );
                }
              } catch (error) {
                requestError = error;
              } finally {
                linked.detach();
                eventStreamStopRequested = true;
                linked.controller.abort();
              }
              streamed = await eventStreamPromise;
              if (requestError) {
                const forcePollingRecovery = Boolean(
                  isCommandRequest && isRecoverableRequestError(requestError) && !(options.signal && options.signal.aborted)
                );
                let canRecoverFromStream = false;
                if (!forcePollingRecovery) {
                  canRecoverFromStream = await canRecoverFromRequestFailurePayload(streamed);
                } else {
                  this.log("command request timed out; require polling terminal recovery before completing");
                }
                if (!canRecoverFromStream || forcePollingRecovery) {
                  const recovered = await recoverFromRequestFailure(
                    requestError,
                    streamHandlers,
                    forcePollingRecovery ? { requireTerminalStatusOnCompletion: true } : null
                  );
                  if (recovered) {
                    streamed = recovered;
                    canRecoverFromStream = true;
                  }
                }
                if (!canRecoverFromStream) throw requestError;
                recoveredAfterRequestFailure = true;
                this.log(
                  `request failed but recovered from stream/polling payload: ${requestError instanceof Error ? requestError.message : String(requestError)}`
                );
              }
            } else if (isCommandRequest) {
              try {
                res = await this.request(
                  "POST",
                  `/session/${encodeURIComponent(sessionId)}/command`,
                  commandBody,
                  sessionQuery,
                  options.signal
                );
              } catch (requestError) {
                const recovered = await recoverFromRequestFailure(requestError);
                if (!recovered) throw requestError;
                streamed = recovered;
                recoveredAfterRequestFailure = true;
              }
            } else {
              try {
                res = await this.request(
                  "POST",
                  `/session/${encodeURIComponent(sessionId)}/message`,
                  messageBody,
                  sessionQuery,
                  options.signal
                );
              } catch (requestError) {
                const recovered = await recoverFromRequestFailure(requestError);
                if (!recovered) throw requestError;
                streamed = recovered;
                recoveredAfterRequestFailure = true;
              }
            }
            const data = res && res.data ? res.data : res;
            let envelope = this.normalizeMessageEnvelope(data);
            if (!envelope) {
              const list = this.extractMessageList(data);
              const latest = this.findLatestAssistantMessage(list, startedAt);
              if (latest) envelope = latest;
            }
            let finalized = {
              messageId: "",
              text: "",
              reasoning: "",
              meta: "",
              blocks: [],
              completed: false
            };
            if (envelope) {
              const payload = extractAssistantPayloadFromEnvelope(envelope);
              finalized = {
                messageId: envelope.info && envelope.info.id ? String(envelope.info.id) : "",
                text: String(payload && payload.text ? payload.text : ""),
                reasoning: String(payload && payload.reasoning ? payload.reasoning : ""),
                meta: String(payload && payload.meta ? payload.meta : ""),
                blocks: payload && Array.isArray(payload.blocks) ? payload.blocks : [],
                completed: this.isMessageEnvelopeCompleted(envelope)
              };
            }
            if (recoveredAfterRequestFailure && !hasRenderablePayload(finalized) && streamed && hasRenderablePayload(streamed)) {
              finalized = {
                messageId: String(streamed.messageId || finalized.messageId || ""),
                text: String(streamed.text || ""),
                reasoning: String(streamed.reasoning || ""),
                meta: String(streamed.meta || ""),
                blocks: Array.isArray(streamed.blocks) ? streamed.blocks : [],
                completed: Boolean(streamed.completed)
              };
            }
            if (!hasRenderablePayload(finalized) && !isCommandRequest) {
              const recovered = await this.trySyncMessageRecovery(
                sessionId,
                messageBody,
                options.signal,
                finalized.messageId
              );
              if (recovered && hasRenderablePayload(recovered)) {
                finalized = recovered;
              }
            }
            if (!hasRenderablePayload(finalized) && !isCommandRequest) {
              const reconciled = await this.reconcileAssistantResponseQuick(
                sessionId,
                finalized,
                startedAt,
                options.signal,
                finalized.messageId
              );
              if (reconciled && hasRenderablePayload(reconciled)) {
                finalized = reconciled;
              }
            }
            if (!hasRenderablePayload(finalized) && streamed && hasRenderablePayload(streamed)) {
              finalized = {
                messageId: String(streamed.messageId || finalized.messageId || ""),
                text: String(streamed.text || ""),
                reasoning: String(streamed.reasoning || ""),
                meta: String(streamed.meta || ""),
                blocks: Array.isArray(streamed.blocks) ? streamed.blocks : [],
                completed: Boolean(streamed.completed)
              };
            }
            if (!hasRenderablePayload(finalized) && !isCommandRequest) {
              for (let attempt = 0; attempt < 4 && !hasRenderablePayload(finalized); attempt += 1) {
                if (await checkWaitingForQuestion()) break;
                const reconciled = await this.reconcileAssistantResponseQuick(
                  sessionId,
                  finalized,
                  startedAt,
                  options.signal,
                  finalized.messageId
                );
                if (reconciled && hasRenderablePayload(reconciled)) {
                  finalized = reconciled;
                  break;
                }
                if (attempt < 3) await delayMs(220);
              }
            }
            if (!hasRenderablePayload(finalized)) {
              if (await checkWaitingForQuestion()) {
                const hint = rt("\u7B49\u5F85\u95EE\u9898\u56DE\u7B54\u540E\u7EE7\u7EED\u751F\u6210\u3002", "Waiting for question response before continuing generation.");
                finalized = {
                  messageId: String(finalized.messageId || ""),
                  text: hint,
                  reasoning: "",
                  meta: hint,
                  blocks: [],
                  completed: false
                };
              } else {
                const status = await this.getSessionStatus(sessionId, options.signal);
                const statusText = formatSessionStatusText(status);
                const activeModel = String(this.settings && this.settings.defaultModel ? this.settings.defaultModel : "").trim();
                const modelText = activeModel ? rt("\u6A21\u578B {activeModel}", "Model {activeModel}", { activeModel }) : rt("\u5F53\u524D\u6A21\u578B", "Current model");
                let noRenderableDiagHint = "";
                let noRenderableListSummary = null;
                emitNoRenderableDiag("no-renderable snapshot", {
                  sessionId,
                  startedAt,
                  elapsedMs: Math.max(0, Date.now() - Number(startedAt || Date.now())),
                  statusText,
                  statusRaw: status && typeof status === "object" ? status : null,
                  envelope: summarizeEnvelope(envelope),
                  finalized: summarizePayload(finalized),
                  streamed: summarizePayload(streamed)
                });
                if (sessionStatusLooksAuthFailure(status)) {
                  throw new Error(rt(
                    "{modelText} \u9274\u6743\u5931\u8D25\uFF08session.status={statusText}\uFF09\u3002\u8BF7\u68C0\u67E5 Provider \u767B\u5F55\u6216 API Key\u3002",
                    "{modelText} authentication failed (session.status={statusText}). Check Provider login or API Key.",
                    { modelText, statusText }
                  ));
                }
                try {
                  const fetched = await this.fetchSessionMessages(sessionId, {
                    signal: options.signal,
                    limit: 20,
                    requireRecentTail: true
                  });
                  const listPayload = Array.isArray(fetched.list) ? fetched.list : [];
                  const listSummary = summarizeMessageList(listPayload, startedAt);
                  noRenderableListSummary = listSummary;
                  noRenderableDiagHint = buildNoRenderableDiagHint({
                    total: listSummary.total,
                    latestAssistantId: listSummary.latestAssistant && listSummary.latestAssistant.id,
                    latestAssistantAfterStartedAtId: listSummary.latestAssistantAfterStartedAt && listSummary.latestAssistantAfterStartedAt.id,
                    latestUserId: listSummary.latestUser && listSummary.latestUser.id
                  });
                  emitNoRenderableDiag(
                    "no-renderable message-list",
                    listSummary
                  );
                  const latestErrorEnvelope = [...listPayload].reverse().find((item) => {
                    const info = item && item.info && typeof item.info === "object" ? item.info : null;
                    return Boolean(info && info.error);
                  }) || null;
                  if (latestErrorEnvelope) {
                    const info = latestErrorEnvelope.info && typeof latestErrorEnvelope.info === "object" ? latestErrorEnvelope.info : {};
                    const err = extractErrorText(info.error);
                    if (err) {
                      throw new Error(rt("\u6A21\u578B\u8FD4\u56DE\u9519\u8BEF\uFF1A{err}", "Model returned an error: {err}", { err }));
                    }
                  }
                } catch (error) {
                  if (error instanceof Error && /^(模型返回错误：|Model returned an error:)/.test(error.message)) throw error;
                  this.log(`no-renderable inspect failed: ${error instanceof Error ? error.message : String(error)}`);
                }
                const shouldRetryWithFreshSession = Boolean(
                  !isCommandRequest && !Boolean(options && options._retryFreshSessionAttempted)
                );
                if (shouldRetryWithFreshSession) {
                  this.log(`no-renderable retry decision ${JSON.stringify({
                    sessionId,
                    statusText,
                    total: noRenderableListSummary ? Number(noRenderableListSummary.total || 0) : null,
                    strategy: "recreate-session-once"
                  })}`);
                  try {
                    const replacement = await this.createSession("");
                    const newSessionIdRaw = extractSessionId(replacement);
                    const newSessionId = this.resolveSessionAlias(newSessionIdRaw) || newSessionIdRaw;
                    if (newSessionId && newSessionId !== sessionId) {
                      this.rememberSessionAlias(requestedSessionId || sessionId, newSessionId, "idle-empty-recreate");
                      this.rememberSessionAlias(sessionId, newSessionId, "idle-empty-recreate");
                      this.log(`session auto-recreated after idle-empty ${JSON.stringify({
                        fromSessionId: sessionId,
                        toSessionId: newSessionId,
                        replacementRawId: newSessionIdRaw
                      })}`);
                      return this.sendMessage({
                        ...options,
                        sessionId: newSessionId,
                        _retryFreshSessionAttempted: true
                      });
                    }
                    this.log(`session auto-recreate skipped: invalid replacement id ${JSON.stringify({
                      fromSessionId: sessionId,
                      replacement
                    })}`);
                  } catch (retryError) {
                    this.log(`session auto-recreate failed: ${retryError instanceof Error ? retryError.message : String(retryError)}`);
                  }
                }
                if (noRenderableDiagHint) {
                  throw new Error(rt(
                    "{modelText} \u672A\u8FD4\u56DE\u53EF\u7528\u6D88\u606F\uFF08session.status={statusText}; {hint}\uFF09\u3002",
                    "{modelText} did not return a usable message (session.status={statusText}; {hint}).",
                    { modelText, statusText, hint: noRenderableDiagHint }
                  ));
                }
                throw new Error(rt(
                  "{modelText} \u672A\u8FD4\u56DE\u53EF\u7528\u6D88\u606F\uFF08session.status={statusText}\uFF09\u3002",
                  "{modelText} did not return a usable message (session.status={statusText}).",
                  { modelText, statusText }
                ));
              }
            }
            const explicitModelErrorText = String(finalized && finalized.text ? finalized.text : "").trim();
            if (/^(模型返回错误：|Model returned an error:)/.test(explicitModelErrorText)) {
              throw new Error(explicitModelErrorText);
            }
            if (payloadLooksInProgress(finalized)) {
              if (!await checkWaitingForQuestion()) {
                throw new Error(rt(
                  "\u6A21\u578B\u54CD\u5E94\u672A\u5B8C\u6210\u4E14\u5DF2\u8D85\u65F6\uFF0C\u8BF7\u5207\u6362\u6A21\u578B\u6216\u68C0\u67E5\u8BE5 Provider \u9274\u6743\u3002",
                  "Model response is incomplete and timed out. Switch model or check Provider authentication."
                ));
              }
            }
            if (!Boolean(finalized && finalized.completed)) {
              if (!await checkWaitingForQuestion()) {
                throw new Error(rt(
                  "\u6A21\u578B\u54CD\u5E94\u672A\u6536\u5230\u660E\u786E\u5B8C\u6210\u4FE1\u53F7\uFF08message.updated.completed/finish\uFF09\uFF0C\u5DF2\u7EC8\u6B62\u4EE5\u907F\u514D\u622A\u65AD\u3002",
                  "Model response did not receive an explicit completion signal (message.updated.completed/finish); aborted to avoid truncation."
                ));
              }
            }
            const messageId = finalized.messageId;
            const text = finalized.text || "";
            const reasoning = finalized.reasoning || "";
            const meta = finalized.meta || "";
            const blocks = Array.isArray(finalized.blocks) ? finalized.blocks : [];
            if (this.settings.enableStreaming) {
              if (options.onReasoning) options.onReasoning(reasoning);
              if (options.onToken) options.onToken(text);
              if (options.onBlocks) options.onBlocks(blocks);
            } else {
              if (reasoning && options.onReasoning) {
                await streamPseudo(reasoning, options.onReasoning, options.signal);
              }
              await streamPseudo(text, options.onToken, options.signal);
              if (blocks.length && options.onBlocks) {
                options.onBlocks(blocks);
              }
            }
            this.log(`sendMessage done ${JSON.stringify({
              sessionId,
              requestedSessionId: requestedSessionId && requestedSessionId !== sessionId ? requestedSessionId : "",
              hasText: Boolean(normalizedRenderableText(text)),
              textLen: text ? text.length : 0,
              normalizedTextLen: normalizedRenderableText(text).length,
              reasoningLen: reasoning ? reasoning.length : 0,
              blockCount: blocks.length,
              messageId
            })}`);
            return { messageId, text, reasoning, meta, blocks, sessionId };
          } finally {
            await questionWatch.stop();
          }
        }
      }
      const methods = {};
      for (const key of Object.getOwnPropertyNames(SendMessageMethods.prototype)) {
        if (key === "constructor") continue;
        methods[key] = SendMessageMethods.prototype[key];
      }
      return methods;
    }
    module2.exports = { createSendMessageMethods };
  }
});

// runtime/transports/compat/question-permission-methods.js
var require_question_permission_methods = __commonJS({
  "runtime/transports/compat/question-permission-methods.js"(exports2, module2) {
    function createQuestionPermissionMethods(deps = {}) {
      const {
        parseSlashCommand
      } = deps;
      class QuestionPermissionMethods {
        async replyPermission(options) {
          const sessionIdRaw = String(options && options.sessionId ? options.sessionId : "").trim();
          const sessionId = this.resolveSessionAlias(sessionIdRaw) || sessionIdRaw;
          const response = String(options && options.response ? options.response : "").trim();
          if (!sessionId || !["once", "always", "reject"].includes(response)) return { ok: false };
          await this.request(
            "POST",
            `/session/${encodeURIComponent(sessionId)}/permissions/${encodeURIComponent(options.permissionId)}`,
            { response },
            this.buildSessionDirectoryQuery(sessionId),
            options.signal
          );
          return { ok: true };
        }
        async listQuestions(options = {}) {
          const requestOptions = options && typeof options === "object" ? options : { signal: options };
          const signal = requestOptions.signal;
          const sessionIdRaw = String(requestOptions.sessionId || "").trim();
          const sessionId = this.resolveSessionAlias(sessionIdRaw) || sessionIdRaw;
          const preferredDirectory = String(requestOptions.directory || this.getSessionScopedDirectory(sessionId)).trim();
          const fallbackEnabled = Boolean(
            requestOptions.allowDirectoryFallback !== false && this.launchContext && this.launchContext.mode === "wsl"
          );
          const candidates = fallbackEnabled ? this.collectSessionDirectoryCandidates(sessionId, preferredDirectory) : [preferredDirectory || this.vaultPath];
          let firstList = null;
          let firstError = null;
          for (let i = 0; i < candidates.length; i += 1) {
            const directory = String(candidates[i] || "").trim();
            try {
              const res = await this.request(
                "GET",
                "/question",
                void 0,
                { directory },
                signal
              );
              const payload = res && res.data ? res.data : res;
              const list = Array.isArray(payload) ? payload : [];
              if (firstList === null) firstList = list;
              if (list.length) {
                if (sessionId) {
                  this.rememberSessionDirectoryHint(sessionId, directory, i > 0 ? "question-list-fallback" : "question-list");
                }
                return list;
              }
            } catch (error) {
              if (!firstError) firstError = error;
            }
          }
          if (Array.isArray(firstList)) return firstList;
          if (firstError) throw firstError;
          return [];
        }
        async hasPendingQuestionsForSession(sessionId, options = {}) {
          const sidRaw = String(sessionId || "").trim();
          const sid = this.resolveSessionAlias(sidRaw) || sidRaw;
          if (!sid) return false;
          const requestOptions = options && typeof options === "object" && (Object.prototype.hasOwnProperty.call(options, "signal") || Object.prototype.hasOwnProperty.call(options, "minIntervalMs")) ? options : { signal: options };
          try {
            const listed = await this.listQuestions({ ...requestOptions, sessionId: sid });
            if (!Array.isArray(listed)) return false;
            return listed.some((item) => {
              if (!item || typeof item !== "object") return false;
              const reqSession = String(item.sessionID || item.sessionId || "").trim();
              if (!reqSession || reqSession !== sid) return false;
              const requestId = String(item.id || item.requestID || item.requestId || "").trim();
              return Boolean(requestId);
            });
          } catch (error) {
            this.log(`check pending question failed: ${error instanceof Error ? error.message : String(error)}`);
            return false;
          }
        }
        async replyQuestion(options) {
          const requestId = String(options && options.requestId ? options.requestId : "").trim();
          if (!requestId) return { ok: false };
          const answers = Array.isArray(options && options.answers ? options.answers : []) ? options.answers.map((row) => {
            if (!Array.isArray(row)) return [];
            return row.map((item) => String(item || "").trim()).filter(Boolean);
          }) : [];
          await this.request(
            "POST",
            `/question/${encodeURIComponent(requestId)}/reply`,
            { answers },
            this.buildSessionDirectoryQuery(options && options.sessionId),
            options && options.signal ? options.signal : void 0
          );
          return { ok: true };
        }
        async setDefaultModel(options) {
          const modelID = String(options.model || "").trim();
          if (!modelID) return { ok: true, model: "" };
          try {
            await this.request(
              "PATCH",
              "/config",
              {
                model: modelID
              },
              { directory: this.vaultPath },
              options.signal
            );
          } catch (error) {
            const message = error instanceof Error ? error.message : String(error);
            const isConfigMissing = /config\.json/i.test(message) && /enoent|no such file|cannot find/i.test(message);
            if (!isConfigMissing) throw error;
            this.log(`setDefaultModel fallback (config missing): ${message}`);
            return { ok: true, model: modelID, persisted: false };
          }
          return { ok: true, model: modelID };
        }
        async switchModel(options) {
          return this.setDefaultModel(options);
        }
        parseSlashCommand(prompt) {
          return parseSlashCommand(prompt);
        }
      }
      const methods = {};
      for (const key of Object.getOwnPropertyNames(QuestionPermissionMethods.prototype)) {
        if (key === "constructor") continue;
        methods[key] = QuestionPermissionMethods.prototype[key];
      }
      return methods;
    }
    module2.exports = { createQuestionPermissionMethods };
  }
});

// runtime/compat-transport.js
var require_compat_transport = __commonJS({
  "runtime/compat-transport.js"(exports2, module2) {
    var fs = require("fs");
    var path = require("path");
    var os = require("os");
    var { spawn, execFileSync } = require("child_process");
    var { rt } = (() => {
      try {
        return require_runtime_locale_state();
      } catch (_e) {
        return {
          rt: (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
            const value = params[k];
            return value === void 0 || value === null ? "" : String(value);
          })
        };
      }
    })();
    var {
      ExecutableResolver,
      isNodeScriptPath,
      isWindowsCommandWrapperPath,
      resolveWindowsWrapperNodeScript,
      resolveNodeExecutablePath
    } = require_executable_resolver();
    var {
      nodeHttpRequestJson,
      nodeHttpRequestSse,
      createLinkedAbortController
    } = require_http_utils();
    var {
      parseModel,
      parseCommandModel,
      availableCommandSet,
      resolveCommandFromSet,
      parseSlashCommand,
      findLatestAssistantMessage
    } = require_command_utils();
    var { createTransportEventReducer } = require_event_reducer();
    var {
      pollAssistantPayload,
      ensureRenderablePayload
    } = require_finalizer();
    var {
      extractAssistantPayloadFromEnvelope,
      extractErrorText,
      normalizedRenderableText,
      hasRenderablePayload,
      formatSessionStatusText,
      isIntermediateToolCallPayload,
      payloadLooksInProgress,
      hasTerminalPayload,
      responseRichnessScore,
      chooseRicherResponse
    } = require_assistant_payload_utils();
    var {
      createPathWslMethods
    } = require_path_wsl_utils();
    var {
      createLaunchAttemptMethods
    } = require_launch_attempt_methods();
    var {
      createProcessLifecycleMethods
    } = require_process_lifecycle_methods();
    var {
      createRequestSessionMethods
    } = require_request_session_methods();
    var {
      createResponseRecoveryMethods
    } = require_response_recovery_methods();
    var {
      createSendMessageMethods
    } = require_send_message_methods();
    var {
      createQuestionPermissionMethods
    } = require_question_permission_methods();
    function sleep(ms) {
      return new Promise((resolve) => setTimeout(resolve, ms));
    }
    async function streamPseudo(text, onToken, signal) {
      if (!onToken) return;
      const tokens = text.match(/.{1,16}/g) || [text];
      let current = "";
      for (const t of tokens) {
        if (signal && signal.aborted) throw new Error(rt("\u7528\u6237\u53D6\u6D88\u4E86\u8BF7\u6C42", "Request was cancelled by user"));
        current += t;
        onToken(current);
        await sleep(20);
      }
    }
    var OPENCODE_SERVE_ARGS = [
      "serve",
      "--hostname",
      "127.0.0.1",
      "--port",
      "0",
      "--cors",
      "app://obsidian.md",
      "--print-logs"
    ];
    var STARTUP_TIMEOUT_MS = 15e3;
    var WSL_STARTUP_TIMEOUT_MS = 3e4;
    var WSL_RUNTIME_WORKSPACE_DIR = ".flownote-workspace";
    var WSL_XDG_DATA_DIR = ".flownote-data";
    var WSL_XDG_CONFIG_DIR = ".config";
    var WSL_XDG_STATE_DIR = ".local/state";
    var WSL_XDG_CACHE_DIR = ".cache";
    var SQLITE_LOCK_RE2 = /sqlite|database is locked|sql_busy|readonly database|disk i\/o|locking protocol/i;
    function isWindowsPlatform() {
      return process.platform === "win32";
    }
    function isWindowsCommandWrapper(filePath) {
      return isWindowsCommandWrapperPath(filePath);
    }
    function quotePosixShellArg(value) {
      return `'${String(value || "").replace(/'/g, "'\\''")}'`;
    }
    function toWslPath(inputPath) {
      const raw = String(inputPath || "").trim();
      if (!raw) return "";
      if (raw.startsWith("/")) return raw;
      const normalized = raw.replace(/\\/g, "/");
      const driveMatch = normalized.match(/^([a-zA-Z]):\/(.*)$/);
      if (!driveMatch) return normalized;
      const drive = String(driveMatch[1] || "").toLowerCase();
      const tail = String(driveMatch[2] || "");
      return `/mnt/${drive}/${tail}`;
    }
    function parseWindowsDrivePath(inputPath) {
      const normalized = String(inputPath || "").trim().replace(/\\/g, "/");
      const match = normalized.match(/^([a-zA-Z]):\/(.*)$/);
      if (!match) return null;
      return {
        drive: String(match[1] || "").toLowerCase(),
        tail: String(match[2] || "")
      };
    }
    function parseWslMountPath(inputPath) {
      const normalized = String(inputPath || "").trim().replace(/\\/g, "/");
      const match = normalized.match(/^\/mnt\/([a-zA-Z])\/(.*)$/);
      if (!match) return null;
      return {
        drive: String(match[1] || "").toLowerCase(),
        tail: String(match[2] || "")
      };
    }
    function toWslPathWithFallback(inputPath) {
      const raw = String(inputPath || "").trim();
      if (!raw || !isWindowsPlatform()) return toWslPath(raw);
      const parsed = parseWindowsDrivePath(raw);
      if (!parsed) return toWslPath(raw);
      if (parsed.drive === "c") return toWslPath(raw);
      const direct = toWslPath(raw);
      const normalizedTail = parsed.tail.replace(/\//g, path.sep);
      const candidates = [];
      if (normalizedTail) {
        candidates.push(path.join(`${String("C").toUpperCase()}:\\`, normalizedTail));
        const userProfile = process.env.USERPROFILE || os.homedir();
        if (userProfile) candidates.push(path.join(userProfile, normalizedTail));
      }
      const existing = candidates.find((candidate) => fs.existsSync(candidate));
      return existing ? toWslPath(existing) : direct;
    }
    function shortStableHash(input) {
      const text = String(input || "");
      let hash = 2166136261;
      for (let i = 0; i < text.length; i += 1) {
        hash ^= text.charCodeAt(i);
        hash = Math.imul(hash, 16777619);
      }
      return (hash >>> 0).toString(16);
    }
    function collectOutputTail(tail, source, chunk) {
      const text = String(chunk || "");
      if (!text) return;
      text.split(/\r?\n/).map((line) => String(line || "").trim()).filter(Boolean).forEach((line) => {
        tail.push(`${source}: ${line}`);
        if (tail.length > 20) tail.shift();
      });
    }
    function appendOutputHint(message, tail) {
      if (!Array.isArray(tail) || !tail.length) return message;
      return `${message}
${rt("\u6700\u8FD1\u8F93\u51FA", "Recent output")}:
${tail.join("\n")}`;
    }
    function looksLikeRetryableConnectionError(message) {
      const text = String(message || "");
      if (!text) return false;
      return /(ECONNRESET|ECONNREFUSED|EPIPE|socket hang up|connection reset|network socket disconnected|read ECONNRESET|write EPIPE|broken pipe)/i.test(text);
    }
    function extractSessionStatusHint(status) {
      if (!status || typeof status !== "object") return "";
      const text = [
        typeof status.type === "string" ? status.type : "",
        typeof status.message === "string" ? status.message : "",
        typeof status.error === "string" ? status.error : "",
        typeof status.reason === "string" ? status.reason : ""
      ].filter(Boolean).join(" ");
      return String(text || "").trim();
    }
    function sessionStatusLooksAuthFailure(status) {
      const hint = extractSessionStatusHint(status);
      if (!hint) return false;
      return /401|unauthorized|user not found|forbidden|invalid api key|authentication/i.test(hint);
    }
    var CompatTransport = class {
      constructor(options) {
        this.vaultPath = options.vaultPath;
        this.settings = options.settings;
        this.logger = typeof options.logger === "function" ? options.logger : () => {
        };
        this.process = null;
        this.baseUrl = "";
        this.bootPromise = null;
        this.resolver = new ExecutableResolver();
        this.commandCache = {
          at: 0,
          items: []
        };
        this.processExitListener = null;
        this.launchContext = {
          mode: "native",
          directory: this.vaultPath,
          label: "",
          distro: "",
          wslHome: "",
          wslUserHome: ""
        };
        this.windowsDriveMapCache = {
          at: 0,
          map: /* @__PURE__ */ new Map()
        };
        this.wslDirectoryCache = /* @__PURE__ */ new Map();
        this.wslWorkspaceDir = "";
        this.wslUserHomeDir = "";
        this.wslDataHomeOverride = "";
        this.wslRequestChain = Promise.resolve();
        this.wslFallbackMirrorAt = 0;
        this.wslFallbackMirrorKey = "";
        this.getPreferredLaunch = typeof options.getPreferredLaunch === "function" ? options.getPreferredLaunch : null;
        this.onLaunchSuccess = typeof options.onLaunchSuccess === "function" ? options.onLaunchSuccess : null;
      }
      log(line) {
        this.logger(line);
      }
      updateSettings(settings) {
        this.settings = settings;
        this.wslDirectoryCache.clear();
        this.wslWorkspaceDir = "";
        this.wslUserHomeDir = "";
        this.wslFallbackMirrorAt = 0;
        this.wslFallbackMirrorKey = "";
      }
      async resolveExecutable() {
        if (!this.settings.autoDetectCli && this.settings.cliPath) {
          return this.resolver.resolve(this.settings.cliPath, { onlyCliPath: true });
        }
        return this.resolver.resolve(this.settings.cliPath);
      }
    };
    var compatDeps = {
      fs,
      path,
      os,
      spawn,
      execFileSync,
      URL,
      process,
      ExecutableResolver,
      isNodeScriptPath,
      resolveWindowsWrapperNodeScript,
      resolveNodeExecutablePath,
      nodeHttpRequestJson,
      nodeHttpRequestSse,
      createLinkedAbortController,
      parseModel,
      parseCommandModel,
      availableCommandSet,
      resolveCommandFromSet,
      parseSlashCommand,
      findLatestAssistantMessage,
      createTransportEventReducer,
      pollAssistantPayload,
      ensureRenderablePayload,
      extractAssistantPayloadFromEnvelope,
      extractErrorText,
      normalizedRenderableText,
      hasRenderablePayload,
      formatSessionStatusText,
      isIntermediateToolCallPayload,
      payloadLooksInProgress,
      hasTerminalPayload,
      responseRichnessScore,
      chooseRicherResponse,
      sleep,
      streamPseudo,
      OPENCODE_SERVE_ARGS,
      STARTUP_TIMEOUT_MS,
      WSL_STARTUP_TIMEOUT_MS,
      WSL_RUNTIME_WORKSPACE_DIR,
      WSL_XDG_DATA_DIR,
      WSL_XDG_CONFIG_DIR,
      WSL_XDG_STATE_DIR,
      WSL_XDG_CACHE_DIR,
      SQLITE_LOCK_RE: SQLITE_LOCK_RE2,
      isWindowsPlatform,
      isWindowsCommandWrapper,
      quotePosixShellArg,
      toWslPath,
      parseWindowsDrivePath,
      parseWslMountPath,
      toWslPathWithFallback,
      shortStableHash,
      collectOutputTail,
      appendOutputHint,
      looksLikeRetryableConnectionError,
      sessionStatusLooksAuthFailure,
      rt
    };
    var pathWslMethods = createPathWslMethods(compatDeps);
    var launchAttemptMethods = createLaunchAttemptMethods(compatDeps);
    var processLifecycleMethods = createProcessLifecycleMethods(compatDeps);
    var requestSessionMethods = createRequestSessionMethods(compatDeps);
    var responseRecoveryMethods = createResponseRecoveryMethods(compatDeps);
    var sendMessageMethods = createSendMessageMethods(compatDeps);
    var questionPermissionMethods = createQuestionPermissionMethods(compatDeps);
    Object.assign(
      CompatTransport.prototype,
      pathWslMethods,
      launchAttemptMethods,
      processLifecycleMethods,
      requestSessionMethods,
      responseRecoveryMethods,
      sendMessageMethods,
      questionPermissionMethods
    );
    module2.exports = { CompatTransport };
  }
});

// runtime/sdk-transport.js
var require_sdk_transport = __commonJS({
  "runtime/sdk-transport.js"(exports2, module2) {
    var fs = require("fs");
    var path = require("path");
    var { createLinkedAbortController } = require_http_utils();
    var { rt } = (() => {
      try {
        return require_runtime_locale_state();
      } catch (_e) {
        return {
          rt: (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
            const value = params[k];
            return value === void 0 || value === null ? "" : String(value);
          })
        };
      }
    })();
    var {
      parseModel,
      parseCommandModel,
      availableCommandSet,
      resolveCommandFromSet,
      parseSlashCommand,
      findLatestAssistantMessage
    } = require_command_utils();
    var { createTransportEventReducer } = require_event_reducer();
    var {
      pollAssistantPayload,
      ensureRenderablePayload
    } = require_finalizer();
    var {
      extractAssistantPayloadFromEnvelope,
      normalizedRenderableText,
      hasRenderablePayload,
      payloadLooksInProgress,
      hasTerminalPayload,
      chooseRicherResponse
    } = require_assistant_payload_utils();
    function sleep(ms) {
      return new Promise((resolve) => setTimeout(resolve, ms));
    }
    function loadBundledSdkClientModule() {
      return require_opencode_sdk_v2_client();
    }
    function normalizeBaseUrl(url) {
      return String(url || "").trim().replace(/\/+$/, "");
    }
    function isLikelyNetworkFetchError(error) {
      const message = error instanceof Error ? error.message : String(error || "");
      if (!message) return false;
      return /failed to fetch|err_connection_refused|econnrefused|networkerror|network error/i.test(message);
    }
    var SdkTransport = class {
      constructor(options) {
        this.vaultPath = options.vaultPath;
        this.settings = options.settings;
        this.logger = typeof options.logger === "function" ? options.logger : () => {
        };
        this.getPreferredLaunch = typeof options.getPreferredLaunch === "function" ? options.getPreferredLaunch : null;
        this.onLaunchSuccess = typeof options.onLaunchSuccess === "function" ? options.onLaunchSuccess : null;
        this.client = null;
        this.clientInitPromise = null;
        this.bootstrapTransport = null;
        this.baseUrl = "http://127.0.0.1:4096";
        this.commandCache = {
          at: 0,
          items: []
        };
        this.sdkTraceFile = "";
      }
      log(line) {
        this.logger(line);
      }
      resolveSdkTraceFile() {
        if (this.sdkTraceFile) return this.sdkTraceFile;
        const base = String(this.vaultPath || "").trim();
        if (!base) return "";
        this.sdkTraceFile = path.join(base, ".obsidian", "plugins", "flownote", "sdk-runtime-trace.log");
        return this.sdkTraceFile;
      }
      trace(line) {
        if (!this.settings || !this.settings.debugLogs) return;
        const file = this.resolveSdkTraceFile();
        if (!file) return;
        try {
          fs.appendFileSync(file, `${(/* @__PURE__ */ new Date()).toISOString()} ${line}
`);
        } catch {
        }
      }
      updateSettings(settings) {
        this.settings = settings;
        if (this.bootstrapTransport && typeof this.bootstrapTransport.updateSettings === "function") {
          try {
            this.bootstrapTransport.updateSettings(settings);
          } catch {
          }
        }
      }
      getBootstrapTransport() {
        if (this.bootstrapTransport) return this.bootstrapTransport;
        const { CompatTransport } = require_compat_transport();
        this.bootstrapTransport = new CompatTransport({
          vaultPath: this.vaultPath,
          settings: this.settings,
          logger: (line) => this.log(`sdk bootstrap: ${line}`),
          getPreferredLaunch: this.getPreferredLaunch || void 0,
          onLaunchSuccess: this.onLaunchSuccess || void 0
        });
        return this.bootstrapTransport;
      }
      async probeBaseUrl(baseUrl) {
        const normalized = normalizeBaseUrl(baseUrl);
        if (!normalized) return false;
        if (!globalThis || typeof globalThis.fetch !== "function") return false;
        const ctrl = new AbortController();
        const timer = setTimeout(() => ctrl.abort(), 1200);
        try {
          const url = `${normalized}/global/health`;
          const res = await globalThis.fetch(url, { method: "GET", signal: ctrl.signal });
          return Boolean(res && res.ok);
        } catch {
          return false;
        } finally {
          clearTimeout(timer);
        }
      }
      async ensureServiceBaseUrl(options = {}) {
        const forceStart = Boolean(options && options.forceStart);
        const fallbackUrl = "http://127.0.0.1:4096";
        if (!forceStart) {
          const candidate = normalizeBaseUrl(this.baseUrl || fallbackUrl) || fallbackUrl;
          this.log(`sdk ensureServiceBaseUrl probe ${candidate}`);
          const ok = await this.probeBaseUrl(candidate);
          if (ok) {
            this.baseUrl = candidate;
            this.trace(`sdk ensureServiceBaseUrl probe ok ${candidate}`);
            return this.baseUrl;
          }
          this.log(`sdk ensureServiceBaseUrl probe failed ${candidate}`);
          this.trace(`sdk ensureServiceBaseUrl probe failed ${candidate}`);
        }
        this.log("sdk ensureServiceBaseUrl launch local opencode serve");
        this.trace("sdk ensureServiceBaseUrl launch local opencode serve");
        const bootstrap = this.getBootstrapTransport();
        const launched = normalizeBaseUrl(await bootstrap.ensureStarted());
        this.baseUrl = launched || fallbackUrl;
        this.log(`sdk ensureServiceBaseUrl launch ok ${this.baseUrl}`);
        this.trace(`sdk ensureServiceBaseUrl launch ok ${this.baseUrl}`);
        return this.baseUrl;
      }
      async recoverFromNetworkFailure(actionName, error) {
        const msg = error instanceof Error ? error.message : String(error);
        this.log(`sdk ${actionName}: network failure, recovering transport: ${msg}`);
        this.trace(`sdk ${actionName}: network failure, recovering transport: ${msg}`);
        this.client = null;
        this.clientInitPromise = null;
        this.baseUrl = "http://127.0.0.1:4096";
        if (this.bootstrapTransport && typeof this.bootstrapTransport.stop === "function") {
          try {
            await this.bootstrapTransport.stop();
          } catch {
          }
          this.bootstrapTransport = null;
        }
        await this.ensureServiceBaseUrl({ forceStart: true });
      }
      async ensureClient() {
        if (this.client) return this.client;
        if (this.clientInitPromise) return this.clientInitPromise;
        this.clientInitPromise = (async () => {
          this.log("sdk ensureClient start");
          this.trace("sdk ensureClient start");
          try {
            const nodeVersion = process && process.versions ? process.versions.node : "";
            const electronVersion = process && process.versions ? process.versions.electron : "";
            const chromeVersion = process && process.versions ? process.versions.chrome : "";
            const procType = process && process.type ? process.type : "unknown";
            this.log(`sdk env node=${nodeVersion} electron=${electronVersion} chrome=${chromeVersion} processType=${procType}`);
            this.trace(`sdk env node=${nodeVersion} electron=${electronVersion} chrome=${chromeVersion} processType=${procType}`);
          } catch {
          }
          let clientMod = null;
          const importErrors = [];
          try {
            this.log("sdk ensureClient load bundled opencode sdk begin");
            this.trace("sdk ensureClient load bundled opencode sdk begin");
            clientMod = loadBundledSdkClientModule();
            this.log("sdk ensureClient load bundled opencode sdk ok");
            this.trace("sdk ensureClient load bundled opencode sdk ok");
          } catch (e) {
            importErrors.push(`bundled opencode sdk: ${e instanceof Error ? e.message : String(e)}`);
            this.log(`sdk ensureClient load bundled opencode sdk failed: ${e instanceof Error ? e.message : String(e)}`);
            this.trace(`sdk ensureClient load bundled opencode sdk failed: ${e instanceof Error ? e.message : String(e)}`);
          }
          const looksLikeReadableStream = (value) => Boolean(value) && typeof value === "object" && typeof value.getReader === "function" && typeof value.tee === "function";
          const applyDuplexForStreamingBody = (method, init) => {
            const normalizedMethod = typeof method === "string" ? method.toUpperCase() : "GET";
            if (normalizedMethod === "GET" || normalizedMethod === "HEAD") return;
            if (!init || typeof init !== "object") return;
            if (!Object.prototype.hasOwnProperty.call(init, "body")) return;
            if (init.body === void 0 || init.body === null) return;
            if (typeof init.duplex === "string") return;
            if (looksLikeReadableStream(init.body)) {
              init.duplex = "half";
            }
          };
          const safeFetch = (request, requestInit) => {
            if (!globalThis || typeof globalThis.fetch !== "function") {
              throw new Error(rt("\u5F53\u524D\u73AF\u5883\u4E0D\u652F\u6301 fetch", "fetch is unavailable in current runtime"));
            }
            if (typeof Request !== "undefined" && request instanceof Request && requestInit === void 0) {
              return globalThis.fetch(request);
            }
            if (!request || typeof request !== "object") {
              const init2 = requestInit && typeof requestInit === "object" ? { ...requestInit } : requestInit;
              const method2 = init2 && typeof init2 === "object" && typeof init2.method === "string" ? init2.method : "GET";
              if (init2 && typeof init2 === "object") applyDuplexForStreamingBody(method2, init2);
              return globalThis.fetch(request, init2);
            }
            const method = typeof request.method === "string" ? request.method.toUpperCase() : "GET";
            const init = {
              method,
              headers: request.headers,
              signal: request.signal,
              redirect: request.redirect,
              cache: request.cache,
              credentials: request.credentials,
              integrity: request.integrity,
              keepalive: request.keepalive,
              mode: request.mode,
              referrer: request.referrer,
              referrerPolicy: request.referrerPolicy
            };
            if (method !== "GET" && method !== "HEAD") {
              init.body = request.body;
              if (typeof request.duplex === "string") init.duplex = request.duplex;
            }
            applyDuplexForStreamingBody(method, init);
            return globalThis.fetch(request.url, init);
          };
          if (clientMod && typeof clientMod.createOpencodeClient === "function") {
            const baseUrl = await this.ensureServiceBaseUrl();
            try {
              this.log("sdk ensureClient createOpencodeClient begin");
              this.trace("sdk ensureClient createOpencodeClient begin");
              this.client = clientMod.createOpencodeClient({
                baseUrl,
                directory: this.vaultPath,
                throwOnError: true,
                timeout: this.settings.requestTimeoutMs,
                fetch: safeFetch
              });
              this.log("sdk ensureClient createOpencodeClient ok");
              this.trace("sdk ensureClient createOpencodeClient ok");
              this.log(`sdk client ready ${baseUrl}`);
              this.trace(`sdk client ready ${baseUrl}`);
              return this.client;
            } catch (e) {
              importErrors.push(`createOpencodeClient: ${e instanceof Error ? e.message : String(e)}`);
              this.log(`sdk ensureClient createOpencodeClient failed: ${e instanceof Error ? e.message : String(e)}`);
              this.trace(`sdk ensureClient createOpencodeClient failed: ${e instanceof Error ? e.message : String(e)}`);
            }
          }
          const details = importErrors.length ? `; ${importErrors.join(" | ")}` : "";
          throw new Error(rt(
            "FLOWnote SDK(v2) \u52A0\u8F7D\u5931\u8D25\uFF1AcreateOpencodeClient \u4E0D\u53EF\u7528{details}",
            "FLOWnote SDK(v2) load failed: createOpencodeClient is unavailable{details}",
            { details }
          ));
        })().finally(() => {
          this.clientInitPromise = null;
        });
        return this.clientInitPromise;
      }
      parseModel() {
        return parseModel(this.settings.defaultModel);
      }
      parseCommandModel() {
        return parseCommandModel(this.settings.defaultModel);
      }
      async testConnection() {
        const client = await this.ensureClient();
        await client.path.get({ directory: this.vaultPath });
        return { ok: true, mode: "sdk" };
      }
      async listSessions() {
        const client = await this.ensureClient();
        const res = await client.session.list({ directory: this.vaultPath });
        return res.data || [];
      }
      async listSessionMessages(options = {}) {
        const sessionId = String(options.sessionId || "").trim();
        if (!sessionId) return [];
        const rawLimit = Number(options.limit || 200);
        const limit = Number.isFinite(rawLimit) && rawLimit > 0 ? Math.max(1, Math.floor(rawLimit)) : 200;
        const client = await this.ensureClient();
        const res = await client.session.messages({
          sessionID: sessionId,
          directory: this.vaultPath,
          limit
        }, { signal: options.signal });
        const payload = res && res.data ? res.data : res;
        return Array.isArray(payload) ? payload : [];
      }
      async getSessionDiff(options = {}) {
        const sessionId = String(options.sessionId || "").trim();
        if (!sessionId) return [];
        const messageId = String(options.messageId || options.messageID || "").trim();
        const client = await this.ensureClient();
        const params = {
          sessionID: sessionId,
          directory: this.vaultPath
        };
        if (messageId) params.messageID = messageId;
        const res = await client.session.diff(params, { signal: options.signal });
        const payload = res && Object.prototype.hasOwnProperty.call(res, "data") ? res.data : res;
        return Array.isArray(payload) ? payload : [];
      }
      async createSession(title) {
        const client = await this.ensureClient();
        const res = await client.session.create(title ? { directory: this.vaultPath, title } : { directory: this.vaultPath });
        return res.data;
      }
      async listModels() {
        try {
          const client = await this.ensureClient();
          const res = await client.config.providers({ directory: this.vaultPath });
          const payload = res && Object.prototype.hasOwnProperty.call(res, "data") ? res.data : res;
          const providers = Array.isArray(payload) ? payload : payload && Array.isArray(payload.providers) ? payload.providers : payload && Array.isArray(payload.all) ? payload.all : [];
          this.log(`sdk listModels: providers=${providers.length}`);
          const out = [];
          for (const provider of providers) {
            const providerID = String(provider && provider.id ? provider.id : "").trim();
            if (!providerID) continue;
            const models2 = provider && provider.models;
            if (Array.isArray(models2)) {
              for (const item of models2) {
                const modelID = String(
                  typeof item === "string" ? item : item && typeof item === "object" ? item.id || item.model || item.name || "" : ""
                ).trim();
                if (!modelID) continue;
                out.push(`${providerID}/${modelID}`);
              }
              continue;
            }
            if (models2 && typeof models2 === "object") {
              for (const key of Object.keys(models2)) {
                const modelID = String(key || "").trim();
                if (!modelID) continue;
                out.push(`${providerID}/${modelID}`);
              }
            }
          }
          const models = [...new Set(out)].sort((a, b) => a.localeCompare(b));
          this.log(`sdk listModels: models=${models.length}`);
          return models;
        } catch (e) {
          this.log(`sdk listModels failed: ${e instanceof Error ? e.message : String(e)}`);
          return [];
        }
      }
      async listProviders() {
        this.log("sdk listProviders: ensureClient");
        let client = await this.ensureClient();
        this.log("sdk listProviders: request");
        let res;
        try {
          res = await client.provider.list({ directory: this.vaultPath });
        } catch (e) {
          if (!isLikelyNetworkFetchError(e)) throw e;
          await this.recoverFromNetworkFailure("listProviders", e);
          client = await this.ensureClient();
          res = await client.provider.list({ directory: this.vaultPath });
        }
        this.log("sdk listProviders: response");
        const payload = res && res.data ? res.data : res || {};
        const all = Array.isArray(payload.all) ? payload.all : [];
        const connected = Array.isArray(payload.connected) ? payload.connected : [];
        const defaults = payload && typeof payload.default === "object" && payload.default ? payload.default : {};
        return {
          all,
          connected,
          default: defaults
        };
      }
      async listProviderAuthMethods() {
        let client = await this.ensureClient();
        let res;
        try {
          res = await client.provider.auth({ directory: this.vaultPath });
        } catch (e) {
          if (!isLikelyNetworkFetchError(e)) throw e;
          await this.recoverFromNetworkFailure("listProviderAuthMethods", e);
          client = await this.ensureClient();
          res = await client.provider.auth({ directory: this.vaultPath });
        }
        const payload = res && res.data ? res.data : res || {};
        return payload && typeof payload === "object" ? payload : {};
      }
      async authorizeProviderOauth(options = {}) {
        const providerID = String(options.providerID || "").trim();
        if (!providerID) throw new Error(rt("providerID \u4E0D\u80FD\u4E3A\u7A7A", "providerID is required"));
        const method = Number(options.method);
        if (!Number.isFinite(method) || method < 0) throw new Error(rt("OAuth method \u65E0\u6548", "Invalid OAuth method"));
        const client = await this.ensureClient();
        const res = await client.provider.oauth.authorize({
          providerID,
          directory: this.vaultPath,
          method: Number(method)
        }, { signal: options.signal });
        return res && res.data ? res.data : res;
      }
      async completeProviderOauth(options = {}) {
        const providerID = String(options.providerID || "").trim();
        if (!providerID) throw new Error(rt("providerID \u4E0D\u80FD\u4E3A\u7A7A", "providerID is required"));
        const method = Number(options.method);
        if (!Number.isFinite(method) || method < 0) throw new Error(rt("OAuth method \u65E0\u6548", "Invalid OAuth method"));
        const code = String(options.code || "").trim();
        const params = {
          providerID,
          directory: this.vaultPath,
          method: Number(method)
        };
        if (code) params.code = code;
        const client = await this.ensureClient();
        const res = await client.provider.oauth.callback(params, { signal: options.signal });
        const payload = res && Object.prototype.hasOwnProperty.call(res, "data") ? res.data : res;
        return payload === void 0 ? true : Boolean(payload);
      }
      async setProviderApiKeyAuth(options = {}) {
        const providerID = String(options.providerID || "").trim();
        if (!providerID) throw new Error(rt("providerID \u4E0D\u80FD\u4E3A\u7A7A", "providerID is required"));
        const key = String(options.key || "").trim();
        if (!key) throw new Error(rt("API Key \u4E0D\u80FD\u4E3A\u7A7A", "API Key is required"));
        const client = await this.ensureClient();
        await client.auth.set({
          providerID,
          auth: {
            type: "api",
            key
          }
        }, { signal: options.signal });
        return true;
      }
      async clearProviderAuth(options = {}) {
        const providerID = String(options.providerID || "").trim();
        if (!providerID) throw new Error(rt("providerID \u4E0D\u80FD\u4E3A\u7A7A", "providerID is required"));
        const client = await this.ensureClient();
        await client.auth.remove({ providerID }, { signal: options.signal });
        return true;
      }
      async listCommands() {
        const now = Date.now();
        if (now - this.commandCache.at < 3e4 && this.commandCache.items.length) {
          return this.commandCache.items;
        }
        try {
          const client = await this.ensureClient();
          const res = await client.command.list({ directory: this.vaultPath });
          const payload = res && res.data ? res.data : res || [];
          const items = Array.isArray(payload) ? payload : Array.isArray(payload.commands) ? payload.commands : [];
          this.commandCache = {
            at: now,
            items: Array.isArray(items) ? items : []
          };
          return this.commandCache.items;
        } catch {
          return [];
        }
      }
      async resolveCommandForEndpoint(commandName) {
        const list = await this.listCommands();
        const names = availableCommandSet(list);
        return resolveCommandFromSet(commandName, names);
      }
      parseSlashCommand(prompt) {
        return parseSlashCommand(prompt);
      }
      async getSessionStatus(sessionId, signal) {
        try {
          const client = await this.ensureClient();
          const res = await client.session.status({ directory: this.vaultPath }, { signal });
          const payload = res && res.data ? res.data : res;
          if (!payload || typeof payload !== "object") return null;
          if (payload[sessionId] && typeof payload[sessionId] === "object") return payload[sessionId];
          if (payload.sessions && typeof payload.sessions === "object" && payload.sessions[sessionId]) {
            return payload.sessions[sessionId];
          }
          if (Array.isArray(payload)) {
            const row = payload.find((item) => {
              if (!item || typeof item !== "object") return false;
              return item.id === sessionId || item.sessionID === sessionId || item.sessionId === sessionId;
            });
            if (row && typeof row === "object") {
              if (row.status && typeof row.status === "object") return row.status;
              if (row.state && typeof row.state === "object") return row.state;
              return row;
            }
          }
          if (payload.sessionID === sessionId || payload.sessionId === sessionId || payload.id === sessionId) {
            if (payload.status && typeof payload.status === "object") return payload.status;
            if (payload.state && typeof payload.state === "object") return payload.state;
            return payload;
          }
          if (typeof payload.type === "string" || typeof payload.status === "string" || typeof payload.state === "string" || payload.status && typeof payload.status === "object" || payload.state && typeof payload.state === "object") {
            return payload;
          }
          return null;
        } catch {
          return null;
        }
      }
      findLatestAssistantMessage(messages, startedAt) {
        return findLatestAssistantMessage(messages, startedAt);
      }
      async pollAssistantResult(client, sessionId, startedAt, signal, preferredMessageId = "", handlers, isQuestionPending) {
        const quietTimeoutMs = Math.max(1e4, Number(this.settings.requestTimeoutMs) || 12e4);
        const polled = await pollAssistantPayload({
          initialMessageId: preferredMessageId,
          initialPayload: { text: "", reasoning: "", meta: "", blocks: [] },
          signal,
          quietTimeoutMs,
          maxTotalMs: Math.max(quietTimeoutMs * 2, 5 * 60 * 1e3),
          sleep,
          onToken: handlers && handlers.onToken,
          onReasoning: handlers && handlers.onReasoning,
          onBlocks: handlers && handlers.onBlocks,
          getByMessageId: async (messageId, requestSignal) => {
            const byId = await client.session.message(
              { sessionID: sessionId, messageID: messageId, directory: this.vaultPath },
              { signal: requestSignal }
            );
            const byIdPayload = byId && byId.data ? byId.data : byId;
            const role = byIdPayload && byIdPayload.info && typeof byIdPayload.info.role === "string" ? byIdPayload.info.role : "";
            if (role && role !== "assistant") {
              return { payload: null, completed: false, messageId: "" };
            }
            const completedAt = byIdPayload && byIdPayload.info && byIdPayload.info.time ? Number(byIdPayload.info.time.completed || 0) : 0;
            return {
              payload: extractAssistantPayloadFromEnvelope(byIdPayload),
              completed: completedAt > 0,
              messageId
            };
          },
          getLatest: async (requestSignal) => {
            const listRes = await client.session.messages(
              { sessionID: sessionId, directory: this.vaultPath, limit: 50 },
              { signal: requestSignal }
            );
            const listPayload = listRes && listRes.data ? listRes.data : listRes;
            const latest = this.findLatestAssistantMessage(listPayload, startedAt);
            if (!latest) return null;
            const completedAt = latest && latest.info && latest.info.time ? Number(latest.info.time.completed || 0) : 0;
            const createdAt = latest && latest.info && latest.info.time ? Number(latest.info.time.created || 0) : 0;
            return {
              payload: extractAssistantPayloadFromEnvelope(latest),
              completed: completedAt > 0,
              messageId: latest && latest.info ? latest.info.id : "",
              createdAt
            };
          },
          getSessionStatus: (requestSignal) => this.getSessionStatus(sessionId, requestSignal)
          ,
          isQuestionPending: typeof isQuestionPending === "function" ? (requestSignal) => isQuestionPending(requestSignal) : void 0
        });
        const payload = await ensureRenderablePayload({
          payload: polled.payload,
          lastStatus: polled.lastStatus,
          getSessionStatus: (requestSignal) => this.getSessionStatus(sessionId, requestSignal),
          signal
        });
        return {
          messageId: polled.messageId || "",
          text: payload.text || "",
          reasoning: payload.reasoning || "",
          meta: payload.meta || "",
          blocks: Array.isArray(payload.blocks) ? payload.blocks : [],
          completed: Boolean(polled.completed) || hasTerminalPayload(payload) && !payloadLooksInProgress(payload)
        };
      }
      async streamAssistantFromEvents(client, sessionId, startedAt, signal, handlers) {
        const reducer = createTransportEventReducer({
          sessionId,
          startedAt,
          onToken: handlers && handlers.onToken,
          onReasoning: handlers && handlers.onReasoning,
          onBlocks: handlers && handlers.onBlocks,
          onPermissionRequest: (permission, permissionId) => {
            if (!handlers || typeof handlers.onPermissionRequest !== "function") return;
            Promise.resolve(handlers.onPermissionRequest(permission || {})).then((response) => {
              if (!response || !["once", "always", "reject"].includes(response)) return;
              return this.replyPermission({
                sessionId,
                permissionId,
                response,
                signal
              });
            }).catch((e) => {
              this.log(`permission handler failed: ${e instanceof Error ? e.message : String(e)}`);
            });
          },
          onQuestionRequest: handlers && handlers.onQuestionRequest,
          onQuestionResolved: handlers && handlers.onQuestionResolved,
          onPromptAppend: handlers && handlers.onPromptAppend,
          onToast: handlers && handlers.onToast,
          permissionEventTypes: ["permission.asked"]
        });
        const eventStream = await client.event.subscribe(
          { directory: this.vaultPath },
          { signal, sseMaxRetryAttempts: 3 }
        );
        for await (const raw of eventStream.stream) {
          if (signal && signal.aborted) throw new Error(rt("\u7528\u6237\u53D6\u6D88\u4E86\u8BF7\u6C42", "Request was cancelled by user"));
          const root = raw && typeof raw === "object" ? raw : null;
          const event = root && root.payload && typeof root.payload === "object" ? root.payload : root;
          if (!event || typeof event !== "object") continue;
          reducer.consume(event);
          if (reducer.isDone()) break;
        }
        return reducer.snapshot();
      }
      async sendMessage(options) {
        this.log(`sendMessage start ${JSON.stringify({ sessionId: options.sessionId, transport: "sdk" })}`);
        const client = await this.ensureClient();
        const startedAt = Date.now();
        const model = this.parseModel();
        const commandModel = this.parseCommandModel();
        const parsedCommand = this.parseSlashCommand(options.prompt);
        const resolvedCommand = parsedCommand ? await this.resolveCommandForEndpoint(parsedCommand.command) : { use: false, command: "" };
        const isCommandRequest = Boolean(parsedCommand && resolvedCommand.use);
        const deliveredQuestionIds = /* @__PURE__ */ new Set();
        let waitingForQuestion = false;
        let pendingQuestionSyncAt = 0;
        let pendingQuestionSyncResult = false;
        let pendingQuestionSyncPromise = null;
        const handleQuestionRequest = (request) => {
          const requestId = String(
            request && (request.id || request.requestID || request.requestId) || ""
          ).trim();
          if (requestId && deliveredQuestionIds.has(requestId)) return;
          if (requestId) deliveredQuestionIds.add(requestId);
          waitingForQuestion = deliveredQuestionIds.size > 0;
          if (typeof options.onQuestionRequest === "function") {
            options.onQuestionRequest(request);
          }
        };
        const handleQuestionResolved = (info) => {
          const requestId = String(info && info.requestId || "").trim();
          if (requestId) deliveredQuestionIds.delete(requestId);
          waitingForQuestion = deliveredQuestionIds.size > 0;
          if (typeof options.onQuestionResolved === "function") {
            options.onQuestionResolved(info);
          }
        };
        const syncPendingQuestionsForSession = async (force = false, requestSignal = options.signal) => {
          if (isCommandRequest) {
            waitingForQuestion = false;
            return false;
          }
          if (!force) {
            if (pendingQuestionSyncPromise) return pendingQuestionSyncPromise;
            if (Date.now() - pendingQuestionSyncAt < 450) return pendingQuestionSyncResult;
          }
          const run = (async () => {
            const listed = await this.listQuestions({ signal: requestSignal || options.signal });
            const activeRequestIds = /* @__PURE__ */ new Set();
            for (const request of Array.isArray(listed) ? listed : []) {
              if (!request || typeof request !== "object") continue;
              const reqSession = String(request.sessionID || request.sessionId || "").trim();
              if (reqSession && reqSession !== options.sessionId) continue;
              const requestId = String(request.id || request.requestID || request.requestId || "").trim();
              if (!requestId) continue;
              activeRequestIds.add(requestId);
              handleQuestionRequest(request);
            }
            if (deliveredQuestionIds.size) {
              for (const requestId of Array.from(deliveredQuestionIds)) {
                if (!activeRequestIds.has(requestId)) {
                  handleQuestionResolved({ requestId, sessionId: options.sessionId });
                }
              }
            }
            waitingForQuestion = activeRequestIds.size > 0;
            pendingQuestionSyncResult = waitingForQuestion;
            pendingQuestionSyncAt = Date.now();
            return waitingForQuestion;
          })();
          pendingQuestionSyncPromise = run;
          try {
            return await run;
          } catch (error) {
            if (deliveredQuestionIds.size > 0 || waitingForQuestion) {
              waitingForQuestion = true;
              pendingQuestionSyncResult = true;
              pendingQuestionSyncAt = Date.now();
              return true;
            }
            this.log(`sdk sync pending questions failed: ${error instanceof Error ? error.message : String(error)}`);
            pendingQuestionSyncResult = false;
            pendingQuestionSyncAt = Date.now();
            return false;
          } finally {
            if (pendingQuestionSyncPromise === run) pendingQuestionSyncPromise = null;
          }
        };
        const checkWaitingForQuestion = async (requestSignal) => {
          if (isCommandRequest) return false;
          if (deliveredQuestionIds.size > 0) return syncPendingQuestionsForSession(true, requestSignal);
          if (waitingForQuestion) return syncPendingQuestionsForSession(false, requestSignal);
          return syncPendingQuestionsForSession(false, requestSignal);
        };
        const shouldWatchQuestions = !isCommandRequest && (typeof options.onQuestionRequest === "function" || typeof options.onQuestionResolved === "function");
        if (shouldWatchQuestions) {
          void syncPendingQuestionsForSession(true);
        }
        const questionWatchTimer = shouldWatchQuestions ? setInterval(() => {
          void syncPendingQuestionsForSession(true);
        }, 1600) : 0;
        if (questionWatchTimer && typeof questionWatchTimer.unref === "function") {
          questionWatchTimer.unref();
        }
        try {
          let streamed = null;
          let directResponse = null;
          let usedRealStreaming = false;
          let shouldEmitPollingUpdates = false;
          if (this.settings.enableStreaming) {
            usedRealStreaming = true;
            const linked = createLinkedAbortController(options.signal);
            const eventSignal = linked.controller.signal;
            const streamPromise = this.streamAssistantFromEvents(client, options.sessionId, startedAt, eventSignal, {
              onToken: options.onToken,
              onReasoning: options.onReasoning,
              onBlocks: options.onBlocks,
              onPermissionRequest: options.onPermissionRequest,
              onQuestionRequest: handleQuestionRequest,
              onQuestionResolved: handleQuestionResolved,
              onPromptAppend: options.onPromptAppend,
              onToast: options.onToast
            }).catch((e) => {
              this.log(`sdk event stream fallback: ${e instanceof Error ? e.message : String(e)}`);
              return null;
            });
            try {
              if (parsedCommand && resolvedCommand.use) {
                await client.session.command(
                  {
                    sessionID: options.sessionId,
                    directory: this.vaultPath,
                    command: resolvedCommand.command,
                    arguments: parsedCommand.arguments,
                    model: commandModel
                  },
                  { signal: options.signal }
                );
              } else {
                const effectivePrompt = parsedCommand ? options.prompt.replace(/^\//, "").trim() : options.prompt;
                await client.session.promptAsync(
                  {
                    sessionID: options.sessionId,
                    directory: this.vaultPath,
                    noReply: false,
                    model,
                    parts: [{ type: "text", text: effectivePrompt || options.prompt }]
                  },
                  { signal: options.signal }
                );
              }
              streamed = await streamPromise;
            } finally {
              linked.detach();
              linked.controller.abort();
            }
            const waitingForQuestionAfterStream = await checkWaitingForQuestion();
            shouldEmitPollingUpdates = Boolean(
              !streamed || !hasTerminalPayload(streamed) || !Boolean(streamed && streamed.completed) || waitingForQuestionAfterStream
            );
          } else if (parsedCommand && resolvedCommand.use) {
            const commandRes = await client.session.command(
              {
                sessionID: options.sessionId,
                directory: this.vaultPath,
                command: resolvedCommand.command,
                arguments: parsedCommand.arguments,
                model: commandModel
              },
              { signal: options.signal }
            );
            const data = commandRes && commandRes.data ? commandRes.data : commandRes;
            const payload = extractAssistantPayloadFromEnvelope(data);
            directResponse = {
              messageId: data && data.info ? data.info.id : "",
              text: payload.text || "",
              reasoning: payload.reasoning || "",
              meta: payload.meta || "",
              blocks: Array.isArray(payload.blocks) ? payload.blocks : [],
              completed: true
            };
          } else {
            const promptRes = await client.session.prompt(
              {
                sessionID: options.sessionId,
                directory: this.vaultPath,
                noReply: false,
                model,
                parts: [{ type: "text", text: options.prompt }]
              },
              { signal: options.signal }
            );
            const data = promptRes && promptRes.data ? promptRes.data : promptRes;
            const payload = extractAssistantPayloadFromEnvelope(data);
            directResponse = {
              messageId: data && data.info ? data.info.id : "",
              text: payload.text || "",
              reasoning: payload.reasoning || "",
              meta: payload.meta || "",
              blocks: Array.isArray(payload.blocks) ? payload.blocks : [],
              completed: true
            };
          }
          let finalized = streamed || directResponse;
          const waitingForQuestionBeforeFinalize = await checkWaitingForQuestion();
          if (!finalized || !hasRenderablePayload(finalized) || waitingForQuestionBeforeFinalize || usedRealStreaming && (!hasTerminalPayload(finalized) || !Boolean(finalized && finalized.completed))) {
            const preferredMessageId = finalized && typeof finalized.messageId === "string" ? finalized.messageId : "";
            const polled = await this.pollAssistantResult(
              client,
              options.sessionId,
              startedAt,
              options.signal,
              preferredMessageId,
              usedRealStreaming ? shouldEmitPollingUpdates ? options : null : options,
              (requestSignal) => checkWaitingForQuestion(requestSignal)
            ).catch((e) => {
              this.log(`sdk poll fallback failed: ${e instanceof Error ? e.message : String(e)}`);
              return null;
            });
            finalized = chooseRicherResponse(finalized, polled);
          }
          const ensuredPayload = await ensureRenderablePayload({
            payload: finalized || { text: "", reasoning: "", meta: "", blocks: [] },
            getSessionStatus: (requestSignal) => this.getSessionStatus(options.sessionId, requestSignal),
            signal: options.signal
          });
          finalized = {
            ...finalized || { messageId: "" },
            text: ensuredPayload.text || "",
            reasoning: ensuredPayload.reasoning || "",
            meta: ensuredPayload.meta || "",
            blocks: Array.isArray(ensuredPayload.blocks) ? ensuredPayload.blocks : []
          };
          if (!usedRealStreaming && this.settings.enableStreaming) {
            if (finalized.reasoning && options.onReasoning) options.onReasoning(finalized.reasoning);
            if (options.onToken) options.onToken(finalized.text || "");
            if (Array.isArray(finalized.blocks) && finalized.blocks.length && options.onBlocks) {
              options.onBlocks(finalized.blocks);
            }
          }
          this.log(`sendMessage done ${JSON.stringify({
            sessionId: options.sessionId,
            transport: "sdk",
            hasText: Boolean(normalizedRenderableText(finalized.text || "")),
            textLen: String(finalized.text || "").length,
            normalizedTextLen: normalizedRenderableText(finalized.text || "").length,
            reasoningLen: String(finalized.reasoning || "").length,
            blockCount: Array.isArray(finalized.blocks) ? finalized.blocks.length : 0,
            messageId: finalized.messageId || ""
          })}`);
          return {
            messageId: finalized.messageId || "",
            text: finalized.text || "",
            reasoning: finalized.reasoning || "",
            meta: finalized.meta || "",
            blocks: Array.isArray(finalized.blocks) ? finalized.blocks : []
          };
        } finally {
          if (questionWatchTimer) clearInterval(questionWatchTimer);
        }
      }
      async replyPermission(options) {
        const client = await this.ensureClient();
        const response = String(options && options.response ? options.response : "").trim();
        if (!["once", "always", "reject"].includes(response)) return { ok: false };
        try {
          await client.permission.reply(
            {
              requestID: options.permissionId,
              directory: this.vaultPath,
              reply: response
            },
            { signal: options.signal }
          );
        } catch (e) {
          if (!options.sessionId) throw e;
          await client.permission.respond(
            {
              sessionID: options.sessionId,
              permissionID: options.permissionId,
              directory: this.vaultPath,
              response
            },
            { signal: options.signal }
          );
        }
        return { ok: true };
      }
      async listQuestions(options = {}) {
        const client = await this.ensureClient();
        const signal = options && options.signal ? options.signal : void 0;
        const res = await client.question.list({ directory: this.vaultPath }, { signal });
        const payload = res && res.data ? res.data : res;
        return Array.isArray(payload) ? payload : [];
      }
      async replyQuestion(options) {
        const client = await this.ensureClient();
        const requestID = String(options && options.requestId ? options.requestId : "").trim();
        if (!requestID) return { ok: false };
        const answers = Array.isArray(options && options.answers ? options.answers : []) ? options.answers.map((row) => {
          if (!Array.isArray(row)) return [];
          return row.map((item) => String(item || "").trim()).filter(Boolean);
        }) : [];
        await client.question.reply(
          {
            requestID,
            directory: this.vaultPath,
            answers
          },
          { signal: options && options.signal ? options.signal : void 0 }
        );
        return { ok: true };
      }
      async setDefaultModel(options) {
        const client = await this.ensureClient();
        const modelID = String(options.model || "").trim();
        if (!modelID) return { ok: true, model: "" };
        await client.config.update({
          directory: this.vaultPath,
          config: { model: modelID }
        });
        return { ok: true, model: modelID };
      }
      async stop() {
        const bootstrap = this.bootstrapTransport;
        this.clientInitPromise = null;
        this.client = null;
        this.bootstrapTransport = null;
        this.baseUrl = "http://127.0.0.1:4096";
        if (bootstrap && typeof bootstrap.stop === "function") {
          try {
            await bootstrap.stop();
          } catch (e) {
            this.log(`sdk bootstrap close failed: ${e instanceof Error ? e.message : String(e)}`);
          }
        }
      }
    };
    module2.exports = { SdkTransport };
  }
});

// runtime/session-store.js
var require_session_store = __commonJS({
  "runtime/session-store.js"(exports2, module2) {
    var {
      normalizeSessionTitleInput,
      isPlaceholderSessionTitle,
      deriveSessionTitleFromPrompt
    } = require_session_title();

    class SessionStore {
      constructor(plugin) {
        this.plugin = plugin;
      }

      state() {
        if (!this.plugin.runtimeState) {
          this.plugin.runtimeState = { sessions: [], activeSessionId: "", messagesBySession: {}, deletedSessionIds: [] };
        }
        if (!Array.isArray(this.plugin.runtimeState.deletedSessionIds)) {
          this.plugin.runtimeState.deletedSessionIds = [];
        }
        return this.plugin.runtimeState;
      }

      static normalizeSessionTitleInput(value) {
        return normalizeSessionTitleInput(value);
      }

      static isPlaceholderTitle(title) {
        return isPlaceholderSessionTitle(title);
      }

      static deriveTitleFromPrompt(prompt) {
        return deriveSessionTitleFromPrompt(prompt);
      }

      static normalizeTimestampMs(value) {
        const raw = Number(value || 0);
        if (!Number.isFinite(raw) || raw <= 0) return 0;
        if (raw >= 1e14) return Math.floor(raw / 1000);
        if (raw >= 1e12) return Math.floor(raw);
        if (raw >= 1e9) return Math.floor(raw * 1000);
        return Math.floor(raw);
      }

      static normalizeLinkedContextFiles(value) {
        const list = Array.isArray(value) ? value : [];
        const seen = new Set();
        const normalized = [];
        list.forEach((rawPath) => {
          const next = String(rawPath || "").trim().replace(/^\/+/, "");
          if (!next || seen.has(next)) return;
          seen.add(next);
          normalized.push(next);
        });
        return normalized;
      }

      upsertSession(session) {
        const st = this.state();
        const i = st.sessions.findIndex((s) => s.id === session.id);
        if (i >= 0) st.sessions[i] = Object.assign({}, st.sessions[i], session);
        else st.sessions.unshift(session);

        st.sessions.sort((a, b) => (b.updatedAt || 0) - (a.updatedAt || 0));
      }

      setActiveSession(id) {
        this.state().activeSessionId = id;
      }

      renameSession(sessionId, title) {
        const st = this.state();
        const session = st.sessions.find((s) => s.id === sessionId);
        if (!session) return false;

        const normalized = normalizeSessionTitleInput(title);
        if (!normalized) return false;

        session.title = normalized;
        session.updatedAt = Date.now();
        return true;
      }

      removeSession(sessionId) {
        const st = this.state();
        const before = st.sessions.length;
        st.sessions = st.sessions.filter((s) => s.id !== sessionId);
        const removed = st.sessions.length !== before;
        if (!removed) return false;

        delete st.messagesBySession[sessionId];

        if (!st.deletedSessionIds.includes(sessionId)) {
          st.deletedSessionIds.push(sessionId);
        }

        if (st.activeSessionId === sessionId) {
          st.activeSessionId = st.sessions.length ? st.sessions[0].id : "";
        }

        return true;
      }

      setSessionMessages(sessionId, messages) {
        const st = this.state();
        const sid = String(sessionId || "").trim();
        if (!sid) return false;
        const source = Array.isArray(messages) ? messages : [];
        const normalized = source
          .map((message, index) => {
            const row = message && typeof message === "object" ? message : null;
            if (!row) return null;
            const roleRaw = String(row.role || "").trim().toLowerCase();
            const role = roleRaw === "assistant" ? "assistant" : roleRaw === "user" ? "user" : "";
            if (!role) return null;
            const createdAt = SessionStore.normalizeTimestampMs(row.createdAt || row.updatedAt || row.timestamp || 0);
            const linkedContextFiles = role === "user"
              ? SessionStore.normalizeLinkedContextFiles(row.linkedContextFiles)
              : [];
            return {
              id: String(row.id || `${role}-${Date.now()}-${index}`),
              messageId: String(row.messageId || row.messageID || row.id || "").trim(),
              role,
              text: String(row.text || ""),
              linkedContextFiles,
              reasoning: role === "assistant" ? String(row.reasoning || "") : "",
              meta: role === "assistant" ? String(row.meta || "") : "",
              blocks: role === "assistant" && Array.isArray(row.blocks) ? row.blocks : [],
              pending: false,
              error: String(row.error || ""),
              createdAt: createdAt || Date.now(),
            };
          })
          .filter(Boolean)
          .slice(-200);

        st.messagesBySession[sid] = normalized;

        const session = st.sessions.find((s) => s.id === sid);
        if (session) {
          const latestMessage = normalized.length ? normalized[normalized.length - 1] : null;
          if (latestMessage && Number(latestMessage.createdAt || 0) > 0) {
            session.updatedAt = Number(latestMessage.createdAt || Date.now());
          }
          const latestUserMessage = [...normalized]
            .reverse()
            .find((row) => row && row.role === "user" && String(row.text || "").trim().length > 0);
          if (latestUserMessage) {
            session.lastUserPrompt = String(latestUserMessage.text || "");
            if (isPlaceholderSessionTitle(session.title)) {
              const nextTitle = deriveSessionTitleFromPrompt(session.lastUserPrompt);
              if (nextTitle) session.title = nextTitle;
            }
          }
        }

        return true;
      }

      appendMessage(sessionId, message) {
        const st = this.state();
        const list = st.messagesBySession[sessionId] || [];
        const nextMessage = message && typeof message === "object"
          ? Object.assign({}, message)
          : message;
        if (nextMessage && typeof nextMessage === "object" && nextMessage.role === "user") {
          nextMessage.linkedContextFiles = SessionStore.normalizeLinkedContextFiles(nextMessage.linkedContextFiles);
        }
        list.push(nextMessage);
        st.messagesBySession[sessionId] = list.slice(-200);

        const session = st.sessions.find((s) => s.id === sessionId);
        if (session) {
          session.updatedAt = Date.now();
          if (message.role === "user") {
            const promptText = typeof message.text === "string" ? message.text : "";
            session.lastUserPrompt = promptText;
            if (isPlaceholderSessionTitle(session.title)) {
              const nextTitle = deriveSessionTitleFromPrompt(promptText);
              if (nextTitle) session.title = nextTitle;
            }
          }
        }
      }

      static blockStatusRank(status) {
        const value = String(status || "").trim().toLowerCase();
        if (value === "error") return 4;
        if (value === "completed") return 3;
        if (value === "running") return 2;
        if (value === "pending") return 1;
        return 0;
      }

      static blockMergeKey(block, index) {
        if (!block || typeof block !== "object") return `invalid:${index}`;
        const id = String(block.id || "").trim();
        if (id) return `id:${id}`;
        const type = String(block.type || "").trim();
        const title = String(block.title || "").trim();
        const summary = String(block.summary || "").trim();
        return `fallback:${type}::${title}::${summary}::${index}`;
      }

      static mergeReasoningText(existingReasoning, incomingReasoning) {
        const existing = String(existingReasoning || "");
        const incoming = String(incomingReasoning || "");
        const existingTrimmed = existing.trim();
        const incomingTrimmed = incoming.trim();

        if (!incomingTrimmed) return existing;
        if (!existingTrimmed) return incoming;
        if (incoming === existing) return existing;
        if (incoming.includes(existing)) return incoming;
        if (existing.includes(incoming)) return existing;
        if (incomingTrimmed.startsWith(existingTrimmed) || incomingTrimmed.endsWith(existingTrimmed)) return incoming;
        if (existingTrimmed.startsWith(incomingTrimmed) || existingTrimmed.endsWith(incomingTrimmed)) return existing;

        const overlapSuffixPrefix = (leftText, rightText) => {
          const left = String(leftText || "");
          const right = String(rightText || "");
          const max = Math.min(left.length, right.length, 2048);
          for (let len = max; len >= 16; len -= 1) {
            if (left.slice(left.length - len) === right.slice(0, len)) return len;
          }
          return 0;
        };

        const overlap = overlapSuffixPrefix(existing, incoming);
        if (overlap > 0) return `${existing}${incoming.slice(overlap)}`;
        const reverseOverlap = overlapSuffixPrefix(incoming, existing);
        if (reverseOverlap > 0) return `${incoming}${existing.slice(reverseOverlap)}`;

        return incoming.length >= existing.length ? incoming : `${existing}\n\n${incoming}`;
      }

      static mergeDraftText(existingText, incomingText) {
        const existing = String(existingText || "");
        const incoming = String(incomingText || "");
        const existingTrimmed = existing.trim();
        const incomingTrimmed = incoming.trim();

        if (!incomingTrimmed) return existing;
        if (!existingTrimmed) return incoming;
        if (incoming === existing) return existing;
        if (incoming.includes(existing)) return incoming;
        if (existing.includes(incoming)) return existing;
        if (incomingTrimmed.startsWith(existingTrimmed) || incomingTrimmed.endsWith(existingTrimmed)) return incoming;
        if (existingTrimmed.startsWith(incomingTrimmed) || existingTrimmed.endsWith(incomingTrimmed)) return existing;

        const overlapSuffixPrefix = (leftText, rightText) => {
          const left = String(leftText || "");
          const right = String(rightText || "");
          const max = Math.min(left.length, right.length, 4096);
          for (let len = max; len >= 8; len -= 1) {
            if (left.slice(left.length - len) === right.slice(0, len)) return len;
          }
          return 0;
        };

        const overlap = overlapSuffixPrefix(existing, incoming);
        if (overlap > 0) return `${existing}${incoming.slice(overlap)}`;
        const reverseOverlap = overlapSuffixPrefix(incoming, existing);
        if (reverseOverlap > 0) return `${incoming}${existing.slice(reverseOverlap)}`;

        let sharedPrefixLen = 0;
        const prefixMax = Math.min(existing.length, incoming.length, 512);
        while (sharedPrefixLen < prefixMax && existing[sharedPrefixLen] === incoming[sharedPrefixLen]) {
          sharedPrefixLen += 1;
        }
        if (sharedPrefixLen >= 24) {
          return incoming.length >= existing.length ? incoming : existing;
        }

        const needsSpace = /[A-Za-z0-9]$/.test(existing) && /^[A-Za-z0-9]/.test(incoming);
        return needsSpace ? `${existing} ${incoming}` : `${existing}${incoming}`;
      }

      mergeBlocks(previousBlocks, nextBlocks) {
        const prev = Array.isArray(previousBlocks) ? previousBlocks : [];
        const next = Array.isArray(nextBlocks) ? nextBlocks : [];
        if (!next.length) return prev;

        const merged = prev.slice();
        const indexByKey = new Map();
        merged.forEach((block, idx) => {
          indexByKey.set(SessionStore.blockMergeKey(block, idx), idx);
        });

        next.forEach((block, idx) => {
          if (!block || typeof block !== "object") return;
          const key = SessionStore.blockMergeKey(block, idx);
          if (!indexByKey.has(key)) {
            indexByKey.set(key, merged.length);
            merged.push(block);
            return;
          }

          const existingIndex = Number(indexByKey.get(key));
          const existing = merged[existingIndex];
          const existingRank = SessionStore.blockStatusRank(existing && existing.status);
          const nextRank = SessionStore.blockStatusRank(block.status);
          const existingDetailLen = String(existing && existing.detail ? existing.detail : "").length;
          const nextDetailLen = String(block.detail || "").length;
          const shouldReplace = nextRank > existingRank || (nextRank === existingRank && nextDetailLen >= existingDetailLen);
          if (shouldReplace) {
            merged[existingIndex] = block;
          }
        });

        return merged;
      }

      updateAssistantDraft(sessionId, draftId, text, reasoning, meta, blocks) {
        const list = this.state().messagesBySession[sessionId] || [];
        const t = list.find((x) => x.id === draftId);
        if (!t) return;
        if (typeof text === "string") {
          const nextText = String(text || "");
          const hasExistingText = String(t.text || "").trim().length > 0;
          // Avoid replacing already visible streamed content with transient partial chunks.
          if (nextText.trim().length > 0) {
            t.text = SessionStore.mergeDraftText(t.text, nextText);
          } else if (!hasExistingText) {
            t.text = "";
          }
        }
        if (typeof reasoning === "string") {
          const nextReasoning = String(reasoning || "");
          const hasExistingReasoning = String(t.reasoning || "").trim().length > 0;
          // Avoid wiping already streamed reasoning when transport emits transient empty snapshots.
          if (nextReasoning.trim().length > 0) {
            t.reasoning = SessionStore.mergeReasoningText(t.reasoning, nextReasoning);
          } else if (!hasExistingReasoning) {
            t.reasoning = "";
          }
        }
        if (typeof meta === "string") t.meta = meta;
        if (Array.isArray(blocks)) t.blocks = this.mergeBlocks(t.blocks, blocks);
      }

      finalizeAssistantDraft(sessionId, draftId, text, error) {
        const list = this.state().messagesBySession[sessionId] || [];
        const t = list.find((x) => x.id === draftId);
        const payload =
          text && typeof text === "object"
            ? text
            : {
              text: String(text || ""),
              reasoning: "",
              meta: "",
              blocks: [],
            };
        if (t) {
          const messageId = String(payload.messageId || payload.messageID || "").trim();
          if (messageId) {
            t.messageId = messageId;
          }
          {
            const nextText = String(payload.text || "");
            const hasExistingText = String(t.text || "").trim().length > 0;
            if (nextText.trim().length > 0) {
              t.text = SessionStore.mergeDraftText(t.text, nextText);
            } else if (!hasExistingText) {
              t.text = "";
            }
          }
          {
            const nextReasoning = String(payload.reasoning || "");
            const hasExistingReasoning = String(t.reasoning || "").trim().length > 0;
            // Keep streamed reasoning if final payload omits it, and merge when snapshots are partial.
            if (nextReasoning.trim().length > 0) {
              t.reasoning = SessionStore.mergeReasoningText(t.reasoning, nextReasoning);
            } else if (!hasExistingReasoning) {
              t.reasoning = "";
            }
          }
          {
            const nextMeta = String(payload.meta || "");
            const hasNextMeta = nextMeta.trim().length > 0;
            const hasExistingMeta = String(t.meta || "").trim().length > 0;
            if (hasNextMeta || !hasExistingMeta) {
              t.meta = nextMeta;
            }
          }
          t.blocks = this.mergeBlocks(t.blocks, Array.isArray(payload.blocks) ? payload.blocks : []);
          t.error = error || "";
          t.pending = false;
        }
      }

      getActiveMessages() {
        const st = this.state();
        return st.messagesBySession[st.activeSessionId] || [];
      }
    }


    module2.exports = { SessionStore };
  }
});

// runtime/diagnostics-service.js
var require_diagnostics_service = __commonJS({
  "runtime/diagnostics-service.js"(exports2, module2) {
    var { rt } = (() => {
      try {
        return require_runtime_locale_state();
      } catch (_e) {
        return {
          rt: (_zh, en, params = {}) => String(en || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, k) => {
            const value = params[k];
            return value === void 0 || value === null ? "" : String(value);
          })
        };
      }
    })();
    var DiagnosticsService = class {
      constructor(plugin, ResolverCtor) {
        this.plugin = plugin;
        const Ctor = typeof ResolverCtor === "function" ? ResolverCtor : null;
        this.resolver = Ctor ? new Ctor() : {
          resolve: async () => ({
            ok: false,
            path: "",
            attempted: [],
            hint: rt("Resolver \u672A\u6CE8\u5165", "Resolver is not initialized")
          })
        };
        this.lastResult = null;
        this.inflight = null;
      }
      async runFresh() {
        const executable = await this.resolver.resolve(this.plugin.settings.cliPath);
        const defaultMode = "sdk";
        let connection = { ok: false, mode: defaultMode, error: "" };
        try {
          const result = await this.plugin.opencodeClient.testConnection();
          connection = { ok: true, mode: result.mode || defaultMode, error: "" };
        } catch (e) {
          connection = { ok: false, mode: defaultMode, error: e instanceof Error ? e.message : String(e) };
        }
        this.lastResult = { at: Date.now(), executable, connection };
        return this.lastResult;
      }
      async runCached(ttlMs = 1e4, force = false) {
        const ttl = Math.max(0, Number(ttlMs) || 0);
        const now = Date.now();
        if (!force && this.lastResult && now - Number(this.lastResult.at || 0) <= ttl) {
          return this.lastResult;
        }
        if (this.inflight) return this.inflight;
        this.inflight = this.runFresh().finally(() => {
          this.inflight = null;
        });
        return this.inflight;
      }
      async run() {
        return this.runCached(0, true);
      }
      getLastResult() {
        return this.lastResult;
      }
    };
    module2.exports = { DiagnosticsService };
  }
});

// runtime/settings-utils.js
var require_settings_utils = __commonJS({
  "runtime/settings-utils.js"(exports2, module2) {
    var LINK_RESOLVER_PROVIDER_IDS = ["tianapi", "showapi", "gugudata"];
    var LINK_RESOLVER_DEFAULTS = {
      enabled: true,
      provider: "tianapi",
      providerOrder: [...LINK_RESOLVER_PROVIDER_IDS],
      tianapiKey: "",
      showapiAppKey: "",
      gugudataAppKey: "",
      timeoutMs: 25e3,
      retries: 2,
      maxConcurrency: 2,
      fallbackMode: "ai_then_plain"
    };
    function normalizeProviderOrder(raw, defaults = LINK_RESOLVER_DEFAULTS.providerOrder) {
      const incoming = Array.isArray(raw) ? raw : String(raw || "").split(/[,\s>，]+/).map((x) => x.trim()).filter(Boolean);
      const normalized = [];
      for (const item of incoming) {
        const id = String(item || "").trim().toLowerCase();
        if (!LINK_RESOLVER_PROVIDER_IDS.includes(id)) continue;
        if (normalized.includes(id)) continue;
        normalized.push(id);
      }
      return normalized.length ? normalized : [...defaults];
    }
    function normalizeResolverProviderId(raw, fallback = LINK_RESOLVER_DEFAULTS.provider) {
      const id = String(raw || "").trim().toLowerCase();
      if (LINK_RESOLVER_PROVIDER_IDS.includes(id)) return id;
      const normalizedFallback = String(fallback || "").trim().toLowerCase();
      if (LINK_RESOLVER_PROVIDER_IDS.includes(normalizedFallback)) return normalizedFallback;
      return LINK_RESOLVER_PROVIDER_IDS[0];
    }
    function normalizeLinkResolver(raw) {
      const lr = raw && typeof raw === "object" ? Object.assign({}, LINK_RESOLVER_DEFAULTS, raw) : { ...LINK_RESOLVER_DEFAULTS };
      lr.enabled = typeof lr.enabled === "boolean" ? lr.enabled : LINK_RESOLVER_DEFAULTS.enabled;
      lr.providerOrder = normalizeProviderOrder(lr.providerOrder);
      lr.provider = normalizeResolverProviderId(lr.provider, lr.providerOrder[0]);
      lr.tianapiKey = String(lr.tianapiKey || "").trim();
      lr.showapiAppKey = String(lr.showapiAppKey || "").trim();
      lr.gugudataAppKey = String(lr.gugudataAppKey || "").trim();
      lr.timeoutMs = Math.max(5e3, Number(lr.timeoutMs) || LINK_RESOLVER_DEFAULTS.timeoutMs);
      lr.retries = Math.min(5, Math.max(0, Number.isFinite(Number(lr.retries)) ? Number(lr.retries) : LINK_RESOLVER_DEFAULTS.retries));
      lr.maxConcurrency = Math.min(
        5,
        Math.max(1, Number.isFinite(Number(lr.maxConcurrency)) ? Number(lr.maxConcurrency) : LINK_RESOLVER_DEFAULTS.maxConcurrency)
      );
      lr.fallbackMode = lr.fallbackMode === "ai_then_plain" ? "ai_then_plain" : LINK_RESOLVER_DEFAULTS.fallbackMode;
      return lr;
    }
    var DEFAULT_SETTINGS = {
      uiLanguage: "auto",
      cliPath: "",
      autoDetectCli: true,
      skillsDir: ".opencode/skills",
      skillInjectMode: "summary",
      defaultModel: "",
      requestTimeoutMs: 12e4,
      enableStreaming: true,
      debugLogs: true,
      opencodeHomeDir: ".opencode-runtime",
      launchStrategy: "auto",
      wslDistro: "",
      mobileCapture: {
        provider: "deepseek",
        apiKey: "",
        baseUrl: "",
        model: "",
        dailyNotePath: "01-\u6355\u83B7\u5C42/\u6BCF\u65E5\u7B14\u8BB0",
        ideaSectionHeader: "## \u8BB0\u5F55",
        enableAiCleanup: true,
        enableUrlSummary: true,
        linkResolver: { ...LINK_RESOLVER_DEFAULTS }
      }
    };
    function migrateLegacySettings(raw) {
      const data = raw && typeof raw === "object" ? { ...raw } : {};
      delete data.useCustomApiKey;
      delete data.authMode;
      delete data.customProviderId;
      delete data.customApiKey;
      delete data.customBaseUrl;
      delete data.transportMode;
      delete data.experimentalSdkEnabled;
      if (data.prependSkillPrompt === false && !data.skillInjectMode) data.skillInjectMode = "off";
      if (data.prependSkillPrompt === true && !data.skillInjectMode) data.skillInjectMode = "summary";
      delete data.prependSkillPrompt;
      return data;
    }
    function normalizeSettings(raw) {
      const merged = Object.assign({}, DEFAULT_SETTINGS, migrateLegacySettings(raw));
      {
        const lang = String(merged.uiLanguage || "").trim().toLowerCase();
        if (lang === "auto") merged.uiLanguage = "auto";
        else if (lang === "zh-cn" || lang === "zh_cn" || lang === "zh") merged.uiLanguage = "zh-CN";
        else if (lang.startsWith("en")) merged.uiLanguage = "en";
        else merged.uiLanguage = DEFAULT_SETTINGS.uiLanguage;
      }
      if (!["summary", "full", "off"].includes(merged.skillInjectMode)) merged.skillInjectMode = "summary";
      if (!["auto", "native"].includes(String(merged.launchStrategy || "").trim().toLowerCase())) {
        merged.launchStrategy = "auto";
      }
      merged.requestTimeoutMs = Math.max(1e4, Number(merged.requestTimeoutMs) || DEFAULT_SETTINGS.requestTimeoutMs);
      merged.cliPath = String(merged.cliPath || "").trim();
      if (/^wsl(?::|:\/\/|$|\.exe$)/i.test(merged.cliPath)) merged.cliPath = "";
      merged.skillsDir = String(merged.skillsDir || DEFAULT_SETTINGS.skillsDir).trim();
      merged.defaultModel = String(merged.defaultModel || "").trim();
      merged.launchStrategy = String(merged.launchStrategy || "auto").trim().toLowerCase();
      if (merged.launchStrategy === "wsl") merged.launchStrategy = "auto";
      merged.wslDistro = "";
      const mcDefaults = DEFAULT_SETTINGS.mobileCapture;
      if (!merged.mobileCapture || typeof merged.mobileCapture !== "object") {
        merged.mobileCapture = { ...mcDefaults };
      } else {
        merged.mobileCapture = Object.assign({}, mcDefaults, merged.mobileCapture);
      }
      const mc = merged.mobileCapture;
      mc.provider = String(mc.provider || mcDefaults.provider).trim();
      mc.apiKey = String(mc.apiKey || "").trim();
      mc.baseUrl = String(mc.baseUrl || "").trim();
      mc.model = String(mc.model || "").trim();
      mc.dailyNotePath = String(mc.dailyNotePath || mcDefaults.dailyNotePath).trim();
      if (mc.dailyNotePath === "01-\u6355\u83B7\u5C42/\u8BB0\u5F55") mc.dailyNotePath = "01-\u6355\u83B7\u5C42/\u6BCF\u65E5\u7B14\u8BB0";
      if (mc.dailyNotePath === "01-Capture/Records") mc.dailyNotePath = "01-Capture/Daily Notes";
      mc.ideaSectionHeader = String(mc.ideaSectionHeader || mcDefaults.ideaSectionHeader).trim();
      mc.enableAiCleanup = typeof mc.enableAiCleanup === "boolean" ? mc.enableAiCleanup : mcDefaults.enableAiCleanup;
      mc.enableUrlSummary = typeof mc.enableUrlSummary === "boolean" ? mc.enableUrlSummary : mcDefaults.enableUrlSummary;
      mc.linkResolver = normalizeLinkResolver(mc.linkResolver);
      return merged;
    }
    module2.exports = {
      DEFAULT_SETTINGS,
      LINK_RESOLVER_DEFAULTS,
      LINK_RESOLVER_PROVIDER_IDS,
      migrateLegacySettings,
      normalizeLinkResolver,
      normalizeProviderOrder,
      normalizeResolverProviderId,
      normalizeSettings
    };
  }
});

// runtime/mobile/mobile-ai-service.js
var require_mobile_ai_service = __commonJS({
  "runtime/mobile/mobile-ai-service.js"(exports2, module2) {
    var { requestUrl: requestUrl2 } = require("obsidian");
    var { normalizeSupportedLocale: normalizeSupportedLocale2 } = require_i18n_locale_utils();
    var PROVIDER_PRESETS = {
      deepseek: { name: "DeepSeek", baseUrl: "https://api.deepseek.com", defaultModel: "deepseek-chat", keyUrl: "https://platform.deepseek.com/api_keys" },
      qwen: { name: "Qwen", baseUrl: "https://dashscope.aliyuncs.com/compatible-mode", defaultModel: "qwen-turbo", keyUrl: "https://dashscope.console.aliyun.com/apiKey" },
      moonshot: { name: "Moonshot (Kimi)", baseUrl: "https://api.moonshot.cn", defaultModel: "moonshot-v1-8k", keyUrl: "https://platform.moonshot.cn/console/api-keys" },
      zhipu: { name: "Zhipu (GLM)", baseUrl: "https://open.bigmodel.cn/api/paas", defaultModel: "glm-4-flash", keyUrl: "https://open.bigmodel.cn/usercenter/apikeys" },
      siliconflow: { name: "SiliconFlow", baseUrl: "https://api.siliconflow.cn", defaultModel: "deepseek-ai/DeepSeek-V3", keyUrl: "https://cloud.siliconflow.cn/account/ak" },
      custom: { name: "Custom", baseUrl: "", defaultModel: "", keyUrl: "" }
    };
    var CAPTURE_SYSTEM_PROMPT = [
      "\u4F60\u662F\u4E00\u4E2A\u6587\u5B57\u6E05\u7406\u52A9\u624B\u3002\u4F60\u7684\u552F\u4E00\u4EFB\u52A1\u662F\u53BB\u9664\u53E3\u8BED\u4E2D\u7684\u8BED\u6C14\u8BCD\u548C\u586B\u5145\u8BCD\uFF08\u5982\uFF1A\u55EF\u3001\u554A\u3001\u90A3\u4E2A\u3001\u5C31\u662F\u3001\u7136\u540E\u3001\u5BF9\u3001\u54E6\u3001emmm\u3001\u989D \u7B49\uFF09\uFF0C",
      "\u8BA9\u53E5\u5B50\u66F4\u7B80\u6D01\u3002",
      "\u89C4\u5219\uFF1A",
      "1. \u53EA\u53BB\u9664\u8BED\u6C14\u8BCD\u548C\u586B\u5145\u8BCD",
      "2. \u4E0D\u8981\u6539\u5199\u3001\u6DA6\u8272\u6216\u7F8E\u5316\u539F\u6587",
      "3. \u4E0D\u8981\u6DFB\u52A0\u4EFB\u4F55\u65B0\u5185\u5BB9",
      "4. \u4E0D\u8981\u6539\u53D8\u539F\u6587\u7684\u610F\u601D\u548C\u8868\u8FBE\u65B9\u5F0F",
      "5. \u4FDD\u7559\u6240\u6709\u5B9E\u8D28\u5185\u5BB9\u548C\u539F\u59CB\u7528\u8BCD",
      "6. \u4FDD\u7559\u6240\u6709 URL \u539F\u6837\u4E0D\u53D8",
      "7. \u76F4\u63A5\u8FD4\u56DE\u6E05\u7406\u540E\u7684\u6587\u672C\uFF0C\u4E0D\u8981\u4EFB\u4F55\u89E3\u91CA\u6216\u524D\u7F00"
    ].join("\n");
    var CAPTURE_SYSTEM_PROMPT_EN = [
      "You are a text cleanup assistant. Your only task is to remove filler words from spoken text (such as um, uh, like, you know, etc.) and keep the sentence concise.",
      "Rules:",
      "1. Only remove filler words",
      "2. Do not rewrite, polish, or beautify the text",
      "3. Do not add new content",
      "4. Keep original meaning and wording",
      "5. Preserve all substantive content",
      "6. Preserve all URLs exactly as-is",
      "7. Return cleaned text only, no explanation"
    ].join("\n");
    var URL_SUMMARY_PROMPT = [
      "\u4F60\u662F\u4E00\u4E2A URL \u6458\u8981\u52A9\u624B\u3002\u7528\u6237\u6587\u672C\u4E2D\u5305\u542B URL\uFF0C\u4E14\u9875\u9762\u5185\u5BB9\u5DF2\u6293\u53D6\u3002",
      "\u8F93\u51FA\u65F6\u4FDD\u7559\u539F\u6587\u4E0D\u53D8\uFF0C\u5E76\u8FFD\u52A0\u6458\u8981\u884C\u3002",
      "\u683C\u5F0F\uFF1A\u6BCF\u884C `> \u{1F4CE} \u539F\u59CBURL - \u6458\u8981`",
      "\u89C4\u5219\uFF1A",
      "- \u4FDD\u7559\u6240\u6709 URL \u539F\u6837",
      "- \u6458\u8981\u4E0D\u8D85\u8FC7 50 \u5B57",
      "- \u4E0D\u6539\u52A8 URL \u4E4B\u5916\u5185\u5BB9",
      "- \u5185\u5BB9\u4E0D\u8DB3\u65F6\u7528\u201C\u6682\u65E0\u6CD5\u89E3\u6790\uFF0C\u5DF2\u4FDD\u7559\u539F\u59CB\u94FE\u63A5\u201D",
      "- \u76F4\u63A5\u8FD4\u56DE\u5B8C\u6574\u5904\u7406\u540E\u7684\u6587\u672C"
    ].join("\n");
    var URL_SUMMARY_PROMPT_EN = [
      "You are a URL summary assistant. User text includes URLs and page content has been fetched.",
      "Output the original text unchanged, and append summary lines.",
      "Format: each line is `> \u{1F4CE} OriginalURL - Summary`",
      "Rules:",
      "- Keep all original URLs unchanged",
      "- Summary <= 50 chars",
      "- Do not alter non-URL content",
      '- If insufficient content, use "Unable to resolve, original URL preserved"',
      "- Return full processed text without explanation"
    ].join("\n");
    var URL_FALLBACK_PROMPT = [
      "\u4F60\u662F\u4E00\u4E2A URL \u515C\u5E95\u52A9\u624B\u3002",
      "\u4EFB\u52A1\uFF1A\u5F53\u9875\u9762\u5185\u5BB9\u4E0D\u53EF\u7528\u65F6\uFF0C\u4EC5\u505A\u6700\u5C0F\u5904\u7406\u3002",
      "\u89C4\u5219\uFF1A",
      "1. \u4FDD\u7559\u539F\u6587\u548C\u6240\u6709 URL \u539F\u6837",
      "2. \u4E0D\u6539\u5199\uFF0C\u4E0D\u865A\u6784\u9875\u9762\u5185\u5BB9",
      "3. \u53EA\u5141\u8BB8\u8FFD\u52A0 `> \u{1F4CE} \u539F\u59CBURL - \u6682\u65E0\u6CD5\u89E3\u6790\uFF0C\u5DF2\u4FDD\u7559\u539F\u59CB\u94FE\u63A5`",
      "4. \u76F4\u63A5\u8FD4\u56DE\u5B8C\u6574\u6587\u672C"
    ].join("\n");
    var URL_FALLBACK_PROMPT_EN = [
      "You are a URL fallback assistant.",
      "Task: apply minimal processing when page content is unavailable.",
      "Rules:",
      "1. Preserve original text and all URLs exactly",
      "2. Do not rewrite or fabricate page content",
      "3. You may only append lines like `> \u{1F4CE} OriginalURL - Unable to resolve, original URL preserved`",
      "4. Return processed full text directly"
    ].join("\n");
    function isZh(locale) {
      return normalizeSupportedLocale2(locale) === "zh-CN";
    }
    function getCaptureSystemPrompt(locale) {
      return isZh(locale) ? CAPTURE_SYSTEM_PROMPT : CAPTURE_SYSTEM_PROMPT_EN;
    }
    function getUrlSummaryPrompt(locale) {
      return isZh(locale) ? URL_SUMMARY_PROMPT : URL_SUMMARY_PROMPT_EN;
    }
    function getUrlFallbackPrompt(locale) {
      return isZh(locale) ? URL_FALLBACK_PROMPT : URL_FALLBACK_PROMPT_EN;
    }
    function getAiProviderDisplayName(providerId, fallbackName, locale = "en") {
      const id = String(providerId || "").trim().toLowerCase();
      if (id === "custom") return isZh(locale) ? "\u81EA\u5B9A\u4E49" : "Custom";
      if (id === "qwen") return isZh(locale) ? "\u901A\u4E49\u5343\u95EE" : "Qwen";
      if (id === "zhipu") return isZh(locale) ? "\u667A\u8C31 (GLM)" : "Zhipu (GLM)";
      return fallbackName || String(providerId || "");
    }
    function resolveAiConfig(mcSettings) {
      const providerId = mcSettings.provider || "deepseek";
      const preset = PROVIDER_PRESETS[providerId] || PROVIDER_PRESETS.deepseek;
      const baseUrl = String(mcSettings.baseUrl || preset.baseUrl || "").replace(/\/+$/, "");
      const model = String(mcSettings.model || preset.defaultModel || "").trim();
      const apiKey = String(mcSettings.apiKey || "").trim();
      return { providerId, preset, baseUrl, model, apiKey };
    }
    function hasAiConfig(mcSettings) {
      const ai = resolveAiConfig(mcSettings || {});
      return Boolean(ai.baseUrl && ai.apiKey);
    }
    function pickFirstText(values) {
      for (const value of values) {
        if (typeof value === "string") {
          const text = value.trim();
          if (text) return text;
        }
      }
      return "";
    }
    async function requestAiCompletion(messages, mcSettings, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      const ai = resolveAiConfig(mcSettings || {});
      if (!ai.baseUrl || !ai.apiKey) {
        throw new Error(isZh(locale) ? "AI \u670D\u52A1\u672A\u914D\u7F6E\uFF1A\u7F3A\u5C11 Base URL \u6216 API Key" : "AI is not configured: missing Base URL or API Key");
      }
      const response = await requestUrl2({
        url: `${ai.baseUrl}/v1/chat/completions`,
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${ai.apiKey}`
        },
        body: JSON.stringify({
          model: ai.model,
          temperature: Number.isFinite(Number(options.temperature)) ? Number(options.temperature) : 0.1,
          messages
        }),
        throw: false,
        timeout: Number.isFinite(Number(options.timeoutMs)) ? Number(options.timeoutMs) : 3e4
      });
      if (response.status !== 200) {
        const snippet = JSON.stringify(response.json || response.text).slice(0, 200);
        if (isZh(locale)) throw new Error(`AI \u8BF7\u6C42\u5931\u8D25 (${response.status}): ${snippet}`);
        throw new Error(`AI request failed (${response.status}): ${snippet}`);
      }
      const data = response.json;
      const content = pickFirstText([
        data && data.choices && data.choices[0] && data.choices[0].message && data.choices[0].message.content
      ]);
      if (!content) {
        throw new Error(isZh(locale) ? "AI \u8FD4\u56DE\u5185\u5BB9\u4E3A\u7A7A" : "AI returned empty content");
      }
      return content;
    }
    async function cleanupCapture(text, mcSettings, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      const content = await requestAiCompletion([
        { role: "system", content: getCaptureSystemPrompt(locale) },
        { role: "user", content: String(text || "") }
      ], mcSettings, { locale, temperature: 0.1, timeoutMs: 3e4 });
      return content.trim();
    }
    async function summarizeTextWithAi(text, mcSettings, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      const systemPrompt = options.useFallbackPrompt ? getUrlFallbackPrompt(locale) : getUrlSummaryPrompt(locale);
      const content = await requestAiCompletion([
        { role: "system", content: systemPrompt },
        { role: "user", content: String(text || "") }
      ], mcSettings, { locale, temperature: 0.1, timeoutMs: 45e3 });
      return content.trim();
    }
    async function testConnection(mcSettings, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      try {
        const probe = isZh(locale) ? "\u55EF\uFF0C\u8FD9\u662F\u4E00\u4E2A\u6D4B\u8BD5" : "um, this is a test";
        const result = await cleanupCapture(probe, mcSettings, { locale });
        return {
          ok: true,
          message: isZh(locale) ? `\u8FDE\u63A5\u6210\u529F\uFF0C\u8FD4\u56DE: "${result}"` : `Connected. Response: "${result}"`
        };
      } catch (e) {
        return { ok: false, message: e instanceof Error ? e.message : String(e) };
      }
    }
    module2.exports = {
      PROVIDER_PRESETS,
      CAPTURE_SYSTEM_PROMPT,
      CAPTURE_SYSTEM_PROMPT_EN,
      getAiProviderDisplayName,
      getCaptureSystemPrompt,
      getUrlSummaryPrompt,
      getUrlFallbackPrompt,
      resolveAiConfig,
      hasAiConfig,
      pickFirstText,
      requestAiCompletion,
      cleanupCapture,
      summarizeTextWithAi,
      testConnection
    };
  }
});

// runtime/settings/basic-settings-section-methods.js
var require_basic_settings_section_methods = __commonJS({
  "runtime/settings/basic-settings-section-methods.js"(exports2, module2) {
    var { Setting: Setting2, Notice: Notice2, Platform: Platform2 = {} } = require("obsidian");
    var { tFromContext } = require_i18n_runtime();
    var { normalizeSupportedLocale: normalizeSupportedLocale2 } = require_i18n_locale_utils();
    var {
      LINK_RESOLVER_DEFAULTS,
      normalizeLinkResolver,
      normalizeResolverProviderId
    } = require_settings_utils();
    var LINK_RESOLVER_PROVIDER_PRESETS = {
      tianapi: {
        id: "tianapi",
        name: "TianAPI",
        keyLabel: "TianAPI Key",
        keyPlaceholder: "tianapi key",
        keyUrl: "https://www.tianapi.com/apiview/66",
        docsUrl: "https://www.tianapi.com/apiview/66",
        hint: "Suitable for basic webpage content extraction; dynamic or anti-crawl pages may fail."
      },
      showapi: {
        id: "showapi",
        name: "ShowAPI",
        keyLabel: "ShowAPI AppKey",
        keyPlaceholder: "showapi appKey",
        keyUrl: "https://www.showapi.com/apiGateway/view/3262/1",
        docsUrl: "https://www.showapi.com/apiGateway/view/3262/1",
        hint: "Usage-based billing with some free quota on selected plans; good low-barrier option."
      },
      gugudata: {
        id: "gugudata",
        name: "Gugudata",
        keyLabel: "Gugudata AppKey",
        keyPlaceholder: "gugudata appkey",
        keyUrl: "https://www.gugudata.com/api/details/url2markdown",
        docsUrl: "https://www.gugudata.com/api/details/url2markdown",
        hint: "Stable Markdown quality output; official docs recommend rate control."
      }
    };
    function getResolverProviderPreset(providerId) {
      return LINK_RESOLVER_PROVIDER_PRESETS[normalizeResolverProviderId(providerId)] || LINK_RESOLVER_PROVIDER_PRESETS.tianapi;
    }
    function getResolverProviderKey(linkResolver, providerId) {
      if (providerId === "tianapi") return String(linkResolver.tianapiKey || "").trim();
      if (providerId === "showapi") return String(linkResolver.showapiAppKey || "").trim();
      if (providerId === "gugudata") return String(linkResolver.gugudataAppKey || "").trim();
      return "";
    }
    function setResolverProviderKey(linkResolver, providerId, nextValue) {
      const value = String(nextValue || "").trim();
      if (providerId === "tianapi") {
        linkResolver.tianapiKey = value;
        return;
      }
      if (providerId === "showapi") {
        linkResolver.showapiAppKey = value;
        return;
      }
      if (providerId === "gugudata") {
        linkResolver.gugudataAppKey = value;
      }
    }
    function isWindowsUiPlatform() {
      if (typeof Platform2.isWin === "boolean") return Platform2.isWin;
      if (typeof navigator !== "undefined" && typeof navigator.userAgent === "string") {
        return /windows/i.test(navigator.userAgent);
      }
      return false;
    }
    function getAiProviderDisplayName(providerId, fallbackName, t) {
      return t(`mobile.providers.${String(providerId || "").trim().toLowerCase()}`, fallbackName || String(providerId || ""));
    }
    var BasicSettingsSectionMethods = class {
      display() {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        const { containerEl } = this;
        containerEl.empty();
        if (typeof this.setHeading === "function") this.setHeading();
        containerEl.createEl("p", {
          text: t(
            "settings.basic.intro",
            "\u5E38\u7528\u60C5\u51B5\u4E0B\u53EA\u9700\u8981\u786E\u8BA4\u8FDE\u63A5\u72B6\u6001\u548C Provider \u767B\u5F55\u3002\u5176\u4F59\u9AD8\u7EA7\u9879\u4E00\u822C\u4FDD\u6301\u9ED8\u8BA4\u5373\u53EF\u3002"
          )
        });
        new Setting2(containerEl).setName(t("settings.language.name", "\u754C\u9762\u8BED\u8A00")).setDesc(t(
          "settings.language.desc",
          "\u9ED8\u8BA4\u8DDF\u968F\u8BBE\u5907\u8BED\u8A00\u3002\u5207\u6362\u540E\u754C\u9762\u5373\u65F6\u5237\u65B0\uFF1B\u547D\u4EE4\u540D\u4E0E Ribbon \u63D0\u793A\u91CD\u8F7D\u540E\u751F\u6548\u3002"
        )).addDropdown((dropdown) => {
          dropdown.addOption("auto", t("settings.language.optionAuto", "\u8DDF\u968F\u7CFB\u7EDF\uFF08\u63A8\u8350\uFF09")).addOption("zh-CN", t("settings.language.optionZhCN", "\u7B80\u4F53\u4E2D\u6587")).addOption("en", t("settings.language.optionEn", "English")).setValue(String(this.plugin.settings.uiLanguage || "auto")).onChange(async (value) => {
            const previousLocale = normalizeSupportedLocale2(
              typeof this.plugin.getEffectiveLocale === "function" ? this.plugin.getEffectiveLocale() : "en",
              "en"
            );
            this.plugin.settings.uiLanguage = String(value || "auto");
            await this.plugin.saveSettings();
            if (typeof this.plugin.refreshLocaleUi === "function") this.plugin.refreshLocaleUi();
            const nextLocale = normalizeSupportedLocale2(
              typeof this.plugin.getEffectiveLocale === "function" ? this.plugin.getEffectiveLocale() : "en",
              "en"
            );
            this.display();
            new Notice2(t(
              "notices.languageAppliedReloadTip",
              "\u754C\u9762\u8BED\u8A00\u5DF2\u66F4\u65B0\u3002\u547D\u4EE4\u540D\u548C Ribbon \u63D0\u793A\u5C06\u5728\u91CD\u8F7D\u63D2\u4EF6\u540E\u751F\u6548\u3002"
            ));
            if (previousLocale === nextLocale) return;
            const languageLabel = nextLocale === "zh-CN" ? t("settings.language.optionZhCN", "\u7B80\u4F53\u4E2D\u6587") : t("settings.language.optionEn", "English");
            if (typeof this.showConfirmModal !== "function") return;
            const shouldReinstall = await this.showConfirmModal({
              title: t("settings.language.reinstallPromptTitle", "\u91CD\u88C5\u5BF9\u5E94\u8BED\u8A00 Skills\uFF1F"),
              description: t(
                "settings.language.reinstallPromptDesc",
                "\u5F53\u524D\u8BED\u8A00\u5DF2\u5207\u6362\u4E3A {language}\u3002\u662F\u5426\u73B0\u5728\u91CD\u88C5\u5BF9\u5E94\u8BED\u8A00\u7248\u672C\u7684\u5185\u7F6E Skills \u4E0E\u6A21\u677F\uFF1F",
                { language: languageLabel }
              ),
              submitText: t("settings.language.reinstallPromptConfirm", "\u7ACB\u5373\u91CD\u88C5"),
              cancelText: t("settings.language.reinstallPromptCancel", "\u7A0D\u540E")
            });
            if (!shouldReinstall) return;
            await this.reinstallBundledContentWithPrompt(null, {
              locale: nextLocale,
              replaceAll: true,
              skipConflictPrompt: true
            });
          });
        });
        const isWindows = isWindowsUiPlatform();
        const launchStrategyValue = String(this.plugin.settings.launchStrategy || "auto");
        const launchStrategyForUi = launchStrategyValue === "native" ? "native" : "auto";
        new Setting2(containerEl).setName(t("settings.basic.cliPathName", "FLOWnote CLI \u8DEF\u5F84\uFF08\u53EF\u9009\uFF09")).setDesc(t(
          "settings.basic.cliPathDesc",
          "\u901A\u5E38\u7559\u7A7A\u3002\u63D2\u4EF6\u4F1A\u81EA\u52A8\u63A2\u6D4B\u3002Windows \u8BF7\u5148\u5728\u672C\u673A\u5B89\u88C5 Node.js\uFF0C\u518D\u6267\u884C npm install -g opencode-ai\u3002\u5FC5\u8981\u65F6\u4F18\u5148\u586B\u5199 opencode.exe \u6216 cli.js\uFF08\u4E0D\u8981\u586B opencode.cmd\uFF0C\u4E5F\u4E0D\u8981\u4F7F\u7528 WSL\uFF09\u3002"
        )).addText((text) => {
          text.setPlaceholder("/Users/xxx/.opencode/bin/opencode").setValue(this.plugin.settings.cliPath).onChange(async (v) => {
            this.plugin.settings.cliPath = v.trim();
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("settings.basic.launchStrategyName", "\u8FDE\u63A5\u542F\u52A8\u65B9\u5F0F")).setDesc(
          isWindows ? t(
            "settings.basic.launchStrategyDescWindows",
            "\u81EA\u52A8\uFF08\u63A8\u8350\uFF09\uFF1A\u81EA\u52A8\u68C0\u6D4B\u5E76\u8BB0\u5FC6\u6210\u529F\u7684\u672C\u673A\u8FDE\u63A5\u65B9\u5F0F\u3002\u624B\u52A8\u6A21\u5F0F\u4EC5\u4F7F\u7528 Windows \u672C\u673A\u5B89\u88C5\uFF1B\u4E0D\u518D\u652F\u6301 WSL\u3002"
          ) : t(
            "settings.basic.launchStrategyDesc",
            "\u81EA\u52A8\uFF08\u63A8\u8350\uFF09\uFF1A\u4F18\u5148\u4F7F\u7528\u4E0A\u6B21\u6210\u529F\u65B9\u5F0F\uFF1B\u5931\u8D25\u65F6\u81EA\u52A8\u56DE\u9000\u5230\u5176\u4ED6\u65B9\u5F0F\u3002"
          )
        ).addDropdown((d) => {
          d.addOption("auto", t("settings.basic.launchAuto", "\u81EA\u52A8\uFF08\u63A8\u8350\uFF09"));
          if (isWindows) {
            d.addOption("native", t("settings.basic.launchNativeWindows", "Windows \u672C\u673A\u5B89\u88C5"));
          } else {
            d.addOption("native", t("settings.basic.launchNativeMac", "Mac \u672C\u673A\u5B89\u88C5"));
          }
          d.setValue(launchStrategyForUi).onChange(async (v) => {
            this.plugin.settings.launchStrategy = v;
            await this.plugin.saveSettings();
            this.display();
          });
        });
        new Setting2(containerEl).setName(t("settings.basic.skillInjectModeName", "\u6280\u80FD\u6CE8\u5165\u65B9\u5F0F")).setDesc(t("settings.basic.skillInjectModeDesc", "\u5F53\u4F60\u4F7F\u7528 /skill \u6307\u4EE4\u65F6\uFF0C\u63D2\u4EF6\u5982\u4F55\u628A\u6280\u80FD\u5185\u5BB9\u4F20\u7ED9\u6A21\u578B\u3002")).addDropdown((d) => {
          d.addOption("summary", t("settings.basic.skillInjectModeSummary", "\u6458\u8981\u6CE8\u5165\uFF08\u63A8\u8350\uFF09")).addOption("full", t("settings.basic.skillInjectModeFull", "\u5168\u6587\u6CE8\u5165\uFF08\u66F4\u5B8C\u6574\u4F46\u66F4\u91CD\uFF09")).addOption("off", t("settings.basic.skillInjectModeOff", "\u5173\u95ED\u6CE8\u5165\uFF08\u53EA\u53D1\u9001\u7528\u6237\u8F93\u5165\uFF09")).setValue(this.plugin.settings.skillInjectMode).onChange(async (v) => {
            this.plugin.settings.skillInjectMode = v;
            await this.plugin.saveSettings();
          });
        });
        this.renderProviderAuthSection(containerEl);
        new Setting2(containerEl).setName(t("settings.basic.advancedHeading", "\u9AD8\u7EA7\u8BBE\u7F6E")).setHeading();
        new Setting2(containerEl).setName(t("settings.basic.skillsDirName", "\u5185\u7F6E Skills \u5B89\u88C5\u76EE\u5F55")).setDesc(t(
          "settings.basic.skillsDirDesc",
          "\u9ED8\u8BA4 .opencode/skills\u3002\u63D2\u4EF6\u4F1A\u81EA\u52A8\u5B89\u88C5/\u66F4\u65B0\u5185\u7F6E skills\uFF0C\u4F60\u4E5F\u53EF\u4EE5\u5728\u8BE5\u76EE\u5F55\u65B0\u589E\u6216\u4FEE\u6539 skills\uFF1B\u659C\u6760\u547D\u4EE4\u4F1A\u76F4\u63A5\u8BFB\u53D6\u6B64\u76EE\u5F55\u3002"
        )).addText((text) => {
          text.setValue(this.plugin.settings.skillsDir).onChange(async (v) => {
            this.plugin.settings.skillsDir = v.trim() || ".opencode/skills";
            await this.plugin.saveSettings();
            await this.plugin.reloadSkills();
          });
        });
        new Setting2(containerEl).setName(t("settings.basic.reinstallSkillsName", "\u91CD\u65B0\u5B89\u88C5\u5185\u7F6E Skills \u4E0E\u6A21\u677F")).setDesc(t(
          "settings.basic.reinstallSkillsDesc",
          "\u6309\u5F53\u524D\u754C\u9762\u8BED\u8A00\u5B89\u88C5/\u66F4\u65B0\u5185\u7F6E skills\uFF0C\u5E76\u5C06 Meta/\u6A21\u677F \u540C\u6B65\u5230\u5404 skill \u8D44\u6E90\u76EE\u5F55\u3002\u9047\u5230\u540C\u540D\u51B2\u7A81\u4F1A\u8BE2\u95EE\u66FF\u6362\u6216\u5FFD\u7565\u3002"
        )).addButton((b) => {
          b.setButtonText(t("settings.basic.reinstallSkillsNow", "\u7ACB\u5373\u91CD\u88C5")).onClick(async () => {
            await this.reinstallBundledContentWithPrompt(b, {
              replaceAll: true,
              skipConflictPrompt: true
            });
          });
        });
        new Setting2(containerEl).setName(t("settings.basic.resetTemplateBaselineName", "\u91CD\u7F6E\u6A21\u677F\u57FA\u7EBF")).setDesc(t(
          "settings.basic.resetTemplateBaselineDesc",
          "\u4EC5\u5F53\u4F60\u9700\u8981\u6062\u590D\u9ED8\u8BA4\u6A21\u677F\u65F6\u4F7F\u7528\u3002\u4F1A\u628A\u5185\u7F6E\u6A21\u677F\u5199\u56DE Meta/\u6A21\u677F\uFF08\u51B2\u7A81\u53EF\u9010\u9879\u66FF\u6362\u6216\u5FFD\u7565\uFF09\u3002"
        )).addButton((b) => {
          b.setButtonText(t("settings.basic.resetTemplateBaselineNow", "\u91CD\u7F6E\u6A21\u677F")).onClick(async () => {
            b.setDisabled(true);
            b.setButtonText(t("settings.basic.resetTemplateBaselineBusy", "\u91CD\u7F6E\u4E2D..."));
            try {
              const resetResult = await this.plugin.resetTemplateBaseline({
                resolveConflict: (conflict) => this.promptBundledConflictResolution(conflict),
                defaultConflictAction: "skip"
              });
              if (resetResult.cancelled) {
                new Notice2(t(
                  "settings.basic.resetTemplateBaselineCanceled",
                  "\u5DF2\u53D6\u6D88\u6A21\u677F\u91CD\u7F6E\u3002\u5DF2\u5904\u7406 {synced}/{total}\u3002",
                  resetResult
                ));
              } else if (!resetResult.errors.length) {
                new Notice2(t(
                  "settings.basic.resetTemplateBaselineSuccess",
                  "\u6A21\u677F\u91CD\u7F6E\u5B8C\u6210\uFF1A{synced}/{total}\uFF0C\u76EE\u5F55 {metaRoot}",
                  resetResult
                ));
              } else {
                const msg = resetResult.errors[0];
                new Notice2(t("settings.basic.resetTemplateBaselineFailed", "\u6A21\u677F\u91CD\u7F6E\u5931\u8D25\uFF1A{message}", { message: msg }));
              }
            } catch (e) {
              new Notice2(t(
                "settings.basic.resetTemplateBaselineFailed",
                "\u6A21\u677F\u91CD\u7F6E\u5931\u8D25\uFF1A{message}",
                { message: e instanceof Error ? e.message : String(e) }
              ));
            } finally {
              b.setDisabled(false);
              b.setButtonText(t("settings.basic.resetTemplateBaselineNow", "\u91CD\u7F6E\u6A21\u677F"));
            }
          });
        });
        new Setting2(containerEl).setName(t("settings.basic.diagnosticsName", "\u8FDE\u63A5\u8BCA\u65AD")).setDesc(t("settings.basic.diagnosticsDesc", "\u68C0\u6D4B FLOWnote \u53EF\u6267\u884C\u6587\u4EF6\u4E0E\u8FDE\u63A5\u72B6\u6001\u3002")).addButton((b) => {
          b.setButtonText(t("settings.basic.diagnosticsRun", "\u8FD0\u884C\u8BCA\u65AD")).onClick(async () => {
            b.setDisabled(true);
            b.setButtonText(t("settings.basic.diagnosticsBusy", "\u6D4B\u8BD5\u4E2D..."));
            try {
              const r = await this.plugin.diagnosticsService.run();
              if (r.connection.ok) new Notice2(t("settings.basic.diagnosticsOk", "\u8FDE\u63A5\u6B63\u5E38 ({mode})", r.connection));
              else new Notice2(t("settings.basic.diagnosticsFailed", "\u8FDE\u63A5\u5931\u8D25: {error}", r.connection));
            } catch (e) {
              new Notice2(e instanceof Error ? e.message : String(e));
            } finally {
              b.setDisabled(false);
              b.setButtonText(t("settings.basic.diagnosticsRun", "\u8FD0\u884C\u8BCA\u65AD"));
            }
          });
        });
        if (this.plugin.settings.launchStrategy === "auto") {
          const remembered = typeof this.plugin.getPreferredLaunchProfile === "function" ? this.plugin.getPreferredLaunchProfile() : null;
          const rememberedText = remembered ? t("settings.basic.autoMemoryRememberedNative", "\u5DF2\u8BB0\u5FC6\uFF1A\u672C\u673A {command}", {
            command: remembered.command || "opencode"
          }) : t("settings.basic.autoMemoryNone", "\u5F53\u524D\u672A\u8BB0\u5FC6\u6210\u529F\u8FDE\u63A5\u65B9\u5F0F\u3002");
          new Setting2(containerEl).setName(t("settings.basic.autoMemoryName", "\u81EA\u52A8\u8FDE\u63A5\u8BB0\u5FC6")).setDesc(t("settings.basic.autoMemoryDesc", "{rememberedText} \u6210\u529F\u8FDE\u63A5\u540E\u4F1A\u81EA\u52A8\u66F4\u65B0\u3002", { rememberedText })).addButton((b) => {
            b.setButtonText(t("settings.basic.autoMemoryReset", "\u91CD\u7F6E\u8BB0\u5FC6")).onClick(async () => {
              b.setDisabled(true);
              try {
                if (typeof this.plugin.clearRememberedLaunchProfile === "function") {
                  await this.plugin.clearRememberedLaunchProfile();
                }
                new Notice2(t("settings.basic.autoMemoryResetDone", "\u5DF2\u6E05\u9664\u8BB0\u5FC6\u7684\u8FDE\u63A5\u65B9\u5F0F\u3002"));
                this.display();
              } catch (e) {
                new Notice2(t(
                  "settings.basic.autoMemoryResetFailed",
                  "\u91CD\u7F6E\u5931\u8D25: {message}",
                  { message: e instanceof Error ? e.message : String(e) }
                ));
              } finally {
                b.setDisabled(false);
              }
            });
          });
        }
        this.renderMobileCaptureSection(containerEl);
      }
      async reinstallBundledContentWithPrompt(buttonEl, options = {}) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        const button = buttonEl && typeof buttonEl.setDisabled === "function" ? buttonEl : null;
        const locale = normalizeSupportedLocale2(
          options && options.locale ? options.locale : typeof this.plugin.getEffectiveLocale === "function" ? this.plugin.getEffectiveLocale() : "en",
          "en"
        );
        const replaceAll = Boolean(options && options.replaceAll);
        const skipConflictPrompt = Boolean(options && options.skipConflictPrompt);
        const syncMetaTemplates = !Object.prototype.hasOwnProperty.call(options || {}, "syncMetaTemplates") || Boolean(options.syncMetaTemplates);
        if (button) {
          button.setDisabled(true);
          button.setButtonText(t("settings.basic.reinstallSkillsBusy", "\u91CD\u88C5\u4E2D..."));
        }
        try {
          const syncResult = await this.plugin.reloadSkills({
            force: true,
            syncTemplates: true,
            locale,
            resolveConflict: skipConflictPrompt ? null : (conflict) => this.promptBundledConflictResolution(conflict),
            defaultConflictAction: replaceAll ? "replace" : "skip"
          });
          if (syncResult.cancelled) {
            new Notice2(t(
              "settings.basic.reinstallSkillsCanceled",
              "\u5DF2\u53D6\u6D88\u91CD\u88C5\u3002\u5DF2\u5904\u7406 skills {synced}/{total}\uFF0Ctemplates {syncedTemplates}/{totalTemplates}\u3002",
              syncResult
            ));
            return;
          }
          if (syncResult.errors.length) {
            const msg = syncResult.errors[0] || t("settings.basic.unknownError", "\u672A\u77E5\u9519\u8BEF");
            new Notice2(t("settings.basic.reinstallSkillsFailed", "\u91CD\u88C5\u5931\u8D25\uFF1A{message}", { message: msg }));
            return;
          }
          let metaResult = null;
          if (syncMetaTemplates) {
            metaResult = await this.plugin.resetTemplateBaseline({
              locale,
              resolveConflict: skipConflictPrompt ? null : (conflict) => this.promptBundledConflictResolution(conflict),
              defaultConflictAction: replaceAll ? "replace" : "skip"
            });
            if (metaResult.cancelled) {
              new Notice2(t(
                "settings.basic.resetTemplateBaselineCanceled",
                "\u5DF2\u53D6\u6D88\u6A21\u677F\u91CD\u7F6E\u3002\u5DF2\u5904\u7406 {synced}/{total}\u3002",
                metaResult
              ));
              return;
            }
            if (metaResult.errors.length) {
              const msg = metaResult.errors[0] || t("settings.basic.unknownError", "\u672A\u77E5\u9519\u8BEF");
              new Notice2(t(
                "settings.basic.resetTemplateBaselineFailed",
                "\u6A21\u677F\u91CD\u7F6E\u5931\u8D25\uFF1A{message}",
                { message: msg }
              ));
              return;
            }
          }
          const successPayload = {
            ...syncResult,
            syncedMetaTemplates: Number(metaResult && metaResult.synced ? metaResult.synced : 0),
            totalMetaTemplates: Number(metaResult && metaResult.total ? metaResult.total : 0)
          };
          new Notice2(t(
            "settings.basic.reinstallSkillsSuccessWithMeta",
            "\u91CD\u88C5\u5B8C\u6210\uFF1Askills {synced}/{total}\uFF0Ctemplates {syncedTemplates}/{totalTemplates}\uFF0Cmeta {syncedMetaTemplates}/{totalMetaTemplates}\uFF0C\u76EE\u5F55 {targetRoot}",
            successPayload
          ));
        } catch (e) {
          new Notice2(t(
            "settings.basic.reinstallSkillsFailed",
            "\u91CD\u88C5\u5931\u8D25\uFF1A{message}",
            { message: e instanceof Error ? e.message : String(e) }
          ));
        } finally {
          if (button) {
            button.setDisabled(false);
            button.setButtonText(t("settings.basic.reinstallSkillsNow", "\u7ACB\u5373\u91CD\u88C5"));
          }
        }
      }
      async promptBundledConflictResolution(conflict) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        const kind = String(conflict && conflict.kind ? conflict.kind : "");
        const kindText = kind === "skill" ? t("settings.basic.conflictKindSkill", "\u6280\u80FD") : kind === "template" ? t("settings.basic.conflictKindTemplate", "\u6A21\u677F") : t("settings.basic.conflictKindMetaTemplate", "Meta \u6A21\u677F");
        const title = t("settings.basic.contentConflictTitle", "\u53D1\u73B0\u540C\u540D\u51B2\u7A81");
        const description = t(
          "settings.basic.contentConflictDesc",
          "{kind} `{id}` \u5DF2\u5B58\u5728\u3002\u8BF7\u9009\u62E9\u5904\u7406\u65B9\u5F0F\u3002",
          { kind: kindText, id: String(conflict && conflict.id ? conflict.id : "unknown") }
        );
        const contextLines = [
          t("settings.basic.contentConflictTarget", "\u76EE\u6807\uFF1A{path}", {
            path: String(conflict && conflict.targetPath ? conflict.targetPath : "")
          }),
          t("settings.basic.contentConflictSource", "\u6765\u6E90\uFF1A{path}", {
            path: String(conflict && conflict.sourcePath ? conflict.sourcePath : "")
          })
        ].filter((line) => String(line || "").trim());
        return this.showConflictResolutionModal({
          title,
          description,
          context: contextLines.join("\n"),
          replaceText: t("settings.basic.conflictReplace", "\u66FF\u6362"),
          skipText: t("settings.basic.conflictSkip", "\u5FFD\u7565"),
          replaceAllText: t("settings.basic.conflictReplaceAll", "\u5168\u90E8\u66FF\u6362"),
          skipAllText: t("settings.basic.conflictSkipAll", "\u5168\u90E8\u5FFD\u7565"),
          cancelText: t("settings.basic.conflictCancel", "\u53D6\u6D88")
        });
      }
      renderMobileCaptureSection(containerEl) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        let PROVIDER_PRESETS;
        try {
          PROVIDER_PRESETS = require_mobile_ai_service().PROVIDER_PRESETS;
        } catch (_e) {
          return;
        }
        new Setting2(containerEl).setName(t("settings.mobileCapture.heading", "\u79FB\u52A8\u7AEF\u5FEB\u901F\u6355\u83B7")).setHeading();
        containerEl.createEl("p", {
          text: t("settings.mobileCapture.intro", "\u5728\u684C\u9762\u7AEF\u9884\u5148\u914D\u7F6E\u79FB\u52A8\u7AEF\u6355\u83B7\u8BBE\u7F6E\u3002\u540C\u6B65\u5230\u79FB\u52A8\u7AEF\u540E\u5373\u53EF\u4F7F\u7528\u3002"),
          cls: "setting-item-description"
        });
        const mc = this.plugin.settings.mobileCapture;
        const locale = typeof this.plugin.getEffectiveLocale === "function" ? this.plugin.getEffectiveLocale() : "en";
        mc.linkResolver = normalizeLinkResolver(mc.linkResolver);
        const lr = mc.linkResolver;
        const resolverProvider = getResolverProviderPreset(lr.provider);
        new Setting2(containerEl).setName(t("mobile.settings.providerName", "AI \u63D0\u4F9B\u5546")).setDesc(t("mobile.settings.providerDesc", "\u9009\u62E9\u9884\u8BBE\u63D0\u4F9B\u5546\u6216\u81EA\u5B9A\u4E49\u3002")).addDropdown((d) => {
          for (const [id, preset2] of Object.entries(PROVIDER_PRESETS)) {
            d.addOption(id, getAiProviderDisplayName(id, preset2.name, t));
          }
          d.setValue(mc.provider).onChange(async (v) => {
            mc.provider = v;
            await this.plugin.saveSettings();
            this.display();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.apiKeyName", "API Key")).setDesc(t("mobile.settings.apiKeyDesc", "\u7559\u7A7A\u5219\u8DF3\u8FC7 AI \u6E05\u7406\uFF0C\u76F4\u63A5\u8BB0\u5F55\u539F\u6587\u3002")).addText((text) => {
          text.inputEl.type = "password";
          text.setPlaceholder("sk-...").setValue(mc.apiKey).onChange(async (v) => {
            mc.apiKey = v.trim();
            await this.plugin.saveSettings();
          });
        });
        const preset = PROVIDER_PRESETS[mc.provider] || PROVIDER_PRESETS.deepseek;
        new Setting2(containerEl).setName(t("mobile.settings.baseUrlName", "Base URL\uFF08\u53EF\u9009\uFF09")).setDesc(t("mobile.settings.baseUrlDesc", "\u7559\u7A7A\u4F7F\u7528\u9884\u8BBE: {value}", { value: preset.baseUrl || "(\u65E0)" })).addText((text) => {
          text.setPlaceholder(preset.baseUrl || "https://api.example.com").setValue(mc.baseUrl).onChange(async (v) => {
            mc.baseUrl = v.trim();
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.modelName", "\u6A21\u578B\u540D\uFF08\u53EF\u9009\uFF09")).setDesc(t("mobile.settings.modelDesc", "\u7559\u7A7A\u4F7F\u7528\u9884\u8BBE: {value}", { value: preset.defaultModel || "(\u65E0)" })).addText((text) => {
          text.setPlaceholder(preset.defaultModel || "").setValue(mc.model).onChange(async (v) => {
            mc.model = v.trim();
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.aiCleanupName", "\u542F\u7528 AI \u6E05\u7406")).setDesc(t("mobile.settings.aiCleanupDesc", "\u5F00\u542F\u540E\u81EA\u52A8\u53BB\u9664\u8BED\u6C14\u8BCD\u3002")).addToggle((toggle) => {
          toggle.setValue(mc.enableAiCleanup).onChange(async (v) => {
            mc.enableAiCleanup = v;
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.urlSummaryName", "\u542F\u7528\u94FE\u63A5\u6458\u8981")).setDesc(t(
          "mobile.settings.urlSummaryDesc",
          "\u4F18\u5148\u8D70\u56FD\u5185\u89E3\u6790\u670D\u52A1\uFF08\u5929\u805A/\u4E07\u7EF4\u6613\u6E90/\u5495\u5495\u6570\u636E\uFF09\uFF0C\u5931\u8D25\u540E\u81EA\u52A8\u56DE\u9000 AI\uFF0C\u518D\u56DE\u9000\u7EAF\u6587\u672C\u3002"
        )).addToggle((toggle) => {
          toggle.setValue(mc.enableUrlSummary !== false).onChange(async (v) => {
            mc.enableUrlSummary = v;
            lr.enabled = v;
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.resolverSwitchName", "\u89E3\u6790\u670D\u52A1\u603B\u5F00\u5173")).setDesc(t("mobile.settings.resolverSwitchDesc", "\u5173\u95ED\u540E\u4E0D\u8BF7\u6C42\u4EFB\u4F55\u94FE\u63A5\u89E3\u6790\u670D\u52A1\u3002")).addToggle((toggle) => {
          toggle.setValue(lr.enabled).onChange(async (v) => {
            lr.enabled = v;
            mc.enableUrlSummary = v;
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.resolverProviderName", "\u94FE\u63A5\u89E3\u6790\u670D\u52A1\u5546")).setDesc(t("mobile.settings.resolverProviderDesc", "\u4E09\u9009\u4E00\u914D\u7F6E\u5373\u53EF\uFF0C\u63D2\u4EF6\u53EA\u4F1A\u4F7F\u7528\u5F53\u524D\u9009\u4E2D\u7684\u670D\u52A1\u5546\u3002")).addDropdown((d) => {
          for (const id of Object.keys(LINK_RESOLVER_PROVIDER_PRESETS)) {
            const provider = getResolverProviderPreset(id);
            d.addOption(id, t(`settings.mobileCapture.resolverProvider.${id}.name`, provider.name));
          }
          d.setValue(resolverProvider.id).onChange(async (v) => {
            lr.provider = normalizeResolverProviderId(v);
            await this.plugin.saveSettings();
            this.display();
          });
        });
        const resolverKeySetting = new Setting2(containerEl).setName(t(
          `settings.mobileCapture.resolverProvider.${resolverProvider.id}.keyLabel`,
          resolverProvider.keyLabel
        )).setDesc(t(
          `settings.mobileCapture.resolverHint.${resolverProvider.id}`,
          resolverProvider.hint
        )).addText((text) => {
          text.inputEl.type = "password";
          text.setPlaceholder(resolverProvider.keyPlaceholder).setValue(getResolverProviderKey(lr, resolverProvider.id)).onChange(async (v) => {
            setResolverProviderKey(lr, resolverProvider.id, v);
            await this.plugin.saveSettings();
          });
        });
        {
          const descFrag = document.createDocumentFragment();
          descFrag.appendText(t("mobile.settings.resolverEntryPrefix", "\u914D\u7F6E\u5165\u53E3\uFF1A"));
          const keyLink = descFrag.createEl("a", { text: t("mobile.settings.resolverBuyKey", "\u7533\u8BF7/\u8D2D\u4E70 Key"), href: resolverProvider.keyUrl });
          keyLink.setAttr("target", "_blank");
          descFrag.appendText(" \xB7 ");
          const docLink = descFrag.createEl("a", { text: t("mobile.settings.resolverDocs", "\u63A5\u53E3\u6587\u6863"), href: resolverProvider.docsUrl });
          docLink.setAttr("target", "_blank");
          descFrag.appendText(t(
            "mobile.settings.resolverEntrySuffix",
            "\u3002\u82E5\u76EE\u6807\u7F51\u9875\u53CD\u722C\u6216\u52A8\u6001\u52A0\u8F7D\u5931\u8D25\uFF0C\u5C06\u81EA\u52A8\u964D\u7EA7\u5230 AI\uFF0C\u518D\u964D\u7EA7\u5230\u539F\u6587\u4FDD\u7559\u3002"
          ));
          resolverKeySetting.setDesc(descFrag);
        }
        new Setting2(containerEl).setName(t("mobile.settings.timeoutName", "\u89E3\u6790\u8D85\u65F6(ms)")).setDesc(t("mobile.settings.timeoutDesc", "\u5355\u6B21\u89E3\u6790\u8BF7\u6C42\u8D85\u65F6\uFF0C\u9ED8\u8BA4 25000\u3002")).addText((text) => {
          text.setPlaceholder("25000").setValue(String(lr.timeoutMs)).onChange(async (v) => {
            lr.timeoutMs = Math.max(5e3, Number(v) || LINK_RESOLVER_DEFAULTS.timeoutMs);
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.retriesName", "\u5931\u8D25\u91CD\u8BD5\u6B21\u6570")).setDesc(t("mobile.settings.retriesDesc", "\u5355\u670D\u52A1\u91CD\u8BD5\u6B21\u6570\uFF0C\u9ED8\u8BA4 2\u3002")).addText((text) => {
          text.setPlaceholder("2").setValue(String(lr.retries)).onChange(async (v) => {
            lr.retries = Math.min(5, Math.max(0, Number(v) || LINK_RESOLVER_DEFAULTS.retries));
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.concurrencyName", "\u6700\u5927\u5E76\u53D1")).setDesc(t("mobile.settings.concurrencyDesc", "\u5E76\u53D1\u89E3\u6790 URL \u4E0A\u9650\uFF0C\u9ED8\u8BA4 2\u3002")).addText((text) => {
          text.setPlaceholder("2").setValue(String(lr.maxConcurrency)).onChange(async (v) => {
            lr.maxConcurrency = Math.min(5, Math.max(1, Number(v) || LINK_RESOLVER_DEFAULTS.maxConcurrency));
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.dailyPathName", "\u6BCF\u65E5\u7B14\u8BB0\u8DEF\u5F84")).addText((text) => {
          text.setPlaceholder(locale === "zh-CN" ? "01-\u6355\u83B7\u5C42/\u6BCF\u65E5\u7B14\u8BB0" : "01-Capture/Daily Notes").setValue(mc.dailyNotePath).onChange(async (v) => {
            mc.dailyNotePath = v.trim() || (locale === "zh-CN" ? "01-\u6355\u83B7\u5C42/\u6BCF\u65E5\u7B14\u8BB0" : "01-Capture/Daily Notes");
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.headerName", "\u8BB0\u5F55\u533A\u6807\u9898")).addText((text) => {
          text.setPlaceholder(locale === "zh-CN" ? "## \u8BB0\u5F55" : "## Records").setValue(mc.ideaSectionHeader).onChange(async (v) => {
            mc.ideaSectionHeader = v.trim() || (locale === "zh-CN" ? "## \u8BB0\u5F55" : "## Records");
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.testName", "\u6D4B\u8BD5 AI \u8FDE\u63A5")).addButton((b) => {
          b.setButtonText(t("mobile.settings.testBtn", "\u6D4B\u8BD5")).onClick(async () => {
            if (!mc.apiKey) {
              new Notice2(t("notices.needApiKeyFirst", "\u8BF7\u5148\u586B\u5199 API Key"));
              return;
            }
            b.setDisabled(true);
            b.setButtonText(t("mobile.settings.testBusy", "\u6D4B\u8BD5\u4E2D..."));
            try {
              const { testConnection } = require_mobile_ai_service();
              const locale2 = typeof this.plugin.getEffectiveLocale === "function" ? this.plugin.getEffectiveLocale() : "zh-CN";
              const result = await testConnection(mc, { locale: locale2 });
              new Notice2(result.ok ? `\u2705 ${result.message}` : `\u274C ${result.message}`);
            } catch (e) {
              new Notice2(`\u274C ${e instanceof Error ? e.message : String(e)}`);
            } finally {
              b.setDisabled(false);
              b.setButtonText(t("mobile.settings.testBtn", "\u6D4B\u8BD5"));
            }
          });
        });
      }
    };
    var basicSettingsSectionMethods = {};
    for (const key of Object.getOwnPropertyNames(BasicSettingsSectionMethods.prototype)) {
      if (key === "constructor") continue;
      basicSettingsSectionMethods[key] = BasicSettingsSectionMethods.prototype[key];
    }
    module2.exports = { basicSettingsSectionMethods };
  }
});

// runtime/settings/provider-auth-utils.js
var require_provider_auth_utils = __commonJS({
  "runtime/settings/provider-auth-utils.js"(exports2, module2) {
    var { tFromContext } = require_i18n_runtime();
    var ISO3_TO_ISO2 = {
      CHN: "CN",
      USA: "US",
      GBR: "GB",
      CAN: "CA",
      FRA: "FR",
      DEU: "DE",
      JPN: "JP",
      KOR: "KR",
      SGP: "SG",
      AUS: "AU",
      NLD: "NL",
      CHE: "CH",
      SWE: "SE",
      NOR: "NO",
      FIN: "FI",
      DNK: "DK",
      ITA: "IT",
      ESP: "ES",
      ISR: "IL",
      IND: "IN",
      ARE: "AE"
    };
    var COUNTRY_CODE_ALIASES = {
      cn: "CN",
      china: "CN",
      prc: "CN",
      \u4E2D\u56FD: "CN",
      \u4E2D\u56FD\u5927\u9646: "CN",
      us: "US",
      usa: "US",
      america: "US",
      unitedstates: "US",
      \u7F8E\u56FD: "US",
      gb: "GB",
      uk: "GB",
      british: "GB",
      unitedkingdom: "GB",
      greatbritain: "GB",
      \u82F1\u56FD: "GB",
      ca: "CA",
      canada: "CA",
      \u52A0\u62FF\u5927: "CA",
      fr: "FR",
      france: "FR",
      \u6CD5\u56FD: "FR",
      de: "DE",
      germany: "DE",
      \u5FB7\u56FD: "DE",
      jp: "JP",
      japan: "JP",
      \u65E5\u672C: "JP",
      kr: "KR",
      korea: "KR",
      southkorea: "KR",
      \u97E9\u56FD: "KR",
      sg: "SG",
      singapore: "SG",
      \u65B0\u52A0\u5761: "SG",
      au: "AU",
      australia: "AU",
      \u6FB3\u5927\u5229\u4E9A: "AU",
      nl: "NL",
      netherlands: "NL",
      \u8377\u5170: "NL",
      ch: "CH",
      switzerland: "CH",
      \u745E\u58EB: "CH",
      se: "SE",
      sweden: "SE",
      \u745E\u5178: "SE",
      no: "NO",
      norway: "NO",
      \u632A\u5A01: "NO",
      fi: "FI",
      finland: "FI",
      \u82AC\u5170: "FI",
      dk: "DK",
      denmark: "DK",
      \u4E39\u9EA6: "DK",
      it: "IT",
      italy: "IT",
      \u610F\u5927\u5229: "IT",
      es: "ES",
      spain: "ES",
      \u897F\u73ED\u7259: "ES",
      il: "IL",
      israel: "IL",
      \u4EE5\u8272\u5217: "IL",
      in: "IN",
      india: "IN",
      \u5370\u5EA6: "IN",
      ae: "AE",
      uae: "AE",
      unitedarabemirates: "AE",
      \u963F\u8054\u914B: "AE"
    };
    var PROVIDER_COUNTRY_HINTS = [
      {
        code: "CN",
        hints: [
          "deepseek",
          "qwen",
          "dashscope",
          "alibaba",
          "moonshot",
          "kimi",
          "zhipu",
          "glm",
          "chatglm",
          "hunyuan",
          "tencent",
          "doubao",
          "volcengine",
          "volc",
          "bytedance",
          "minimax",
          "baidu",
          "ernie",
          "siliconflow",
          "stepfun",
          "yi",
          "01ai",
          "\u667A\u8C31",
          "\u901A\u4E49",
          "\u8C46\u5305",
          "\u767E\u5DDD",
          "\u8BAF\u98DE",
          "\u817E\u8BAF",
          "\u767E\u5EA6",
          "\u963F\u91CC",
          "\u6708\u4E4B\u6697\u9762"
        ]
      },
      {
        code: "US",
        hints: [
          "openai",
          "anthropic",
          "xai",
          "grok",
          "google",
          "gemini",
          "vertex",
          "azureopenai",
          "microsoft",
          "meta",
          "groq",
          "together",
          "perplexity",
          "openrouter",
          "cohereforai",
          "fireworks",
          "replicate",
          "scale",
          "awsbedrock",
          "bedrock"
        ]
      },
      {
        code: "FR",
        hints: ["mistral", "mistralai"]
      },
      {
        code: "CA",
        hints: ["cohere"]
      },
      {
        code: "GB",
        hints: ["stability", "stabilityai"]
      },
      {
        code: "SG",
        hints: ["sea-lion", "sealion", "aisingapore"]
      }
    ];
    var COMMON_PROVIDER_HINTS = [
      "openai",
      "deepseek",
      "qwen",
      "dashscope",
      "alibaba",
      "moonshot",
      "kimi",
      "zhipu",
      "glm",
      "chatglm",
      "hunyuan",
      "tencent",
      "doubao",
      "volcengine",
      "volc",
      "bytedance",
      "minimax",
      "baidu",
      "ernie",
      "siliconflow",
      "stepfun",
      "yi",
      "01ai",
      "\u667A\u8C31",
      "\u901A\u4E49",
      "\u8C46\u5305",
      "\u767E\u5DDD",
      "\u8BAF\u98DE",
      "\u817E\u8BAF",
      "\u767E\u5EA6",
      "\u963F\u91CC",
      "\u6708\u4E4B\u6697\u9762"
    ];
    var COUNTRY_NAME_FALLBACKS = {
      CN: { "zh-CN": "\u4E2D\u56FD", en: "China" },
      US: { "zh-CN": "\u7F8E\u56FD", en: "United States" },
      GB: { "zh-CN": "\u82F1\u56FD", en: "United Kingdom" },
      CA: { "zh-CN": "\u52A0\u62FF\u5927", en: "Canada" },
      FR: { "zh-CN": "\u6CD5\u56FD", en: "France" },
      DE: { "zh-CN": "\u5FB7\u56FD", en: "Germany" },
      JP: { "zh-CN": "\u65E5\u672C", en: "Japan" },
      KR: { "zh-CN": "\u97E9\u56FD", en: "South Korea" },
      SG: { "zh-CN": "\u65B0\u52A0\u5761", en: "Singapore" },
      AU: { "zh-CN": "\u6FB3\u5927\u5229\u4E9A", en: "Australia" },
      NL: { "zh-CN": "\u8377\u5170", en: "Netherlands" },
      CH: { "zh-CN": "\u745E\u58EB", en: "Switzerland" },
      SE: { "zh-CN": "\u745E\u5178", en: "Sweden" },
      NO: { "zh-CN": "\u632A\u5A01", en: "Norway" },
      FI: { "zh-CN": "\u82AC\u5170", en: "Finland" },
      DK: { "zh-CN": "\u4E39\u9EA6", en: "Denmark" },
      IT: { "zh-CN": "\u610F\u5927\u5229", en: "Italy" },
      ES: { "zh-CN": "\u897F\u73ED\u7259", en: "Spain" },
      IL: { "zh-CN": "\u4EE5\u8272\u5217", en: "Israel" },
      IN: { "zh-CN": "\u5370\u5EA6", en: "India" },
      AE: { "zh-CN": "\u963F\u8054\u914B", en: "United Arab Emirates" }
    };
    function normalizeSearchToken(value) {
      return String(value || "").trim().toLowerCase().replace(/[^a-z0-9\u4e00-\u9fa5]+/g, "");
    }
    function normalizeCountryCode(value) {
      const raw = String(value || "").trim();
      if (!raw) return "";
      const compact = raw.replace(/[^a-zA-Z]/g, "").toUpperCase();
      if (/^[A-Z]{2}$/.test(compact)) return compact;
      if (/^[A-Z]{3}$/.test(compact) && ISO3_TO_ISO2[compact]) return ISO3_TO_ISO2[compact];
      const byAlias = COUNTRY_CODE_ALIASES[normalizeSearchToken(raw)];
      return byAlias || "";
    }
    var ProviderAuthUtilsMethods = class {
      normalizeSearchText(value) {
        return String(value || "").trim().toLowerCase();
      }
      resolveProviderCountryCode(provider) {
        const directHints = [
          provider && provider.countryCode,
          provider && provider.country_code,
          provider && provider.country,
          provider && provider.region,
          provider && provider.vendorCountry,
          provider && provider.vendor_country,
          provider && provider.location && provider.location.countryCode,
          provider && provider.location && provider.location.country,
          provider && provider.vendor && provider.vendor.countryCode,
          provider && provider.vendor && provider.vendor.country,
          provider && provider.meta && provider.meta.countryCode,
          provider && provider.meta && provider.meta.country,
          provider && provider.meta && provider.meta.region,
          provider && provider.company && provider.company.countryCode,
          provider && provider.company && provider.company.country
        ];
        for (const hint of directHints) {
          const countryCode = normalizeCountryCode(hint);
          if (countryCode) return countryCode;
        }
        const hintText = normalizeSearchToken([
          provider && provider.id,
          provider && provider.name,
          provider && provider.displayName,
          provider && provider.vendor && provider.vendor.name,
          provider && provider.company && provider.company.name
        ].filter(Boolean).join(" "));
        for (const item of PROVIDER_COUNTRY_HINTS) {
          if (!item || !Array.isArray(item.hints)) continue;
          if (item.hints.some((hint) => hintText.includes(normalizeSearchToken(hint)))) return item.code;
        }
        return "ZZ";
      }
      getCountryDisplayLocale() {
        if (this.plugin && typeof this.plugin.getEffectiveLocale === "function") {
          const locale = String(this.plugin.getEffectiveLocale() || "").trim().toLowerCase();
          if (locale.startsWith("zh")) return "zh-CN";
        }
        return "en";
      }
      getCountryDisplayNames() {
        if (typeof Intl === "undefined" || typeof Intl.DisplayNames !== "function") return null;
        const locale = this.getCountryDisplayLocale();
        if (this.__countryDisplayNames && this.__countryDisplayNamesLocale === locale) {
          return this.__countryDisplayNames;
        }
        try {
          this.__countryDisplayNames = new Intl.DisplayNames([locale], { type: "region" });
          this.__countryDisplayNamesLocale = locale;
          return this.__countryDisplayNames;
        } catch {
          this.__countryDisplayNames = null;
          this.__countryDisplayNamesLocale = locale;
          return null;
        }
      }
      resolveProviderCountryLabel(countryCode) {
        const normalizedCode = normalizeCountryCode(countryCode) || "ZZ";
        if (normalizedCode === "ZZ") {
          return tFromContext(this, "settings.providerAuth.groupUnknownCountry", "Unknown Country");
        }
        const displayNames = this.getCountryDisplayNames();
        if (displayNames && typeof displayNames.of === "function") {
          try {
            const label = displayNames.of(normalizedCode);
            if (label && String(label).trim()) return String(label);
          } catch {
          }
        }
        const locale = this.getCountryDisplayLocale();
        const fallback = COUNTRY_NAME_FALLBACKS[normalizedCode];
        if (!fallback) return normalizedCode;
        if (locale === "zh-CN") return fallback["zh-CN"] || fallback.en || normalizedCode;
        return fallback.en || fallback["zh-CN"] || normalizedCode;
      }
      buildProviderEntry(provider, connectedSet, authMap) {
        const providerID = String(provider && provider.id ? provider.id : "").trim();
        const providerName = String(provider && provider.name ? provider.name : providerID || "unknown");
        const methodsRaw = Array.isArray(authMap && authMap[providerID]) ? authMap[providerID] : [];
        const oauthMethods = methodsRaw.map((m, idx) => ({ index: idx, type: String(m && m.type ? m.type : ""), label: String(m && m.label ? m.label : `OAuth ${idx + 1}`) })).filter((m) => m.type === "oauth");
        const supportsApi = methodsRaw.some((m) => String(m && m.type ? m.type : "") === "api");
        const isConnected = connectedSet instanceof Set ? connectedSet.has(providerID) : false;
        const modelCount = provider && provider.models && typeof provider.models === "object" ? Object.keys(provider.models).length : 0;
        const methodText = methodsRaw.length ? methodsRaw.map((m) => String(m && m.label ? m.label : m && m.type ? m.type : "unknown")).join(" / ") : tFromContext(this, "settings.providerAuth.noAuthMethods", "No auth methods");
        const countryCode = this.resolveProviderCountryCode(provider);
        const countryLabel = this.resolveProviderCountryLabel(countryCode);
        return {
          provider,
          providerID,
          providerName,
          methodsRaw,
          oauthMethods,
          supportsApi,
          isConnected,
          modelCount,
          methodText,
          countryCode,
          countryLabel
        };
      }
      providerEntryMatchesQuery(entry, query) {
        if (!query) return true;
        const content = [
          entry.providerName,
          entry.providerID,
          entry.methodText,
          entry.countryLabel,
          entry.countryCode,
          `model ${entry.modelCount}`,
          entry.isConnected ? "connected" : "disconnected"
        ].join(" ").toLowerCase();
        return content.includes(query);
      }
      isCommonProviderEntry(entry) {
        const row = entry && typeof entry === "object" ? entry : {};
        const countryCode = normalizeCountryCode(row.countryCode);
        if (countryCode === "CN") return true;
        const hintText = normalizeSearchToken([
          row.providerID,
          row.providerName,
          row.countryLabel,
          row.methodText,
          row && row.provider && row.provider.displayName,
          row && row.provider && row.provider.name
        ].filter(Boolean).join(" "));
        if (!hintText) return false;
        return COMMON_PROVIDER_HINTS.some((hint) => hintText.includes(normalizeSearchToken(hint)));
      }
    };
    var providerAuthUtilsMethods = {};
    for (const key of Object.getOwnPropertyNames(ProviderAuthUtilsMethods.prototype)) {
      if (key === "constructor") continue;
      providerAuthUtilsMethods[key] = ProviderAuthUtilsMethods.prototype[key];
    }
    module2.exports = { providerAuthUtilsMethods };
  }
});

// runtime/settings/provider-auth-section-methods.js
var require_provider_auth_section_methods = __commonJS({
  "runtime/settings/provider-auth-section-methods.js"(exports2, module2) {
    var { Notice: Notice2 } = require("obsidian");
    var { tFromContext } = require_i18n_runtime();
    var OPENCODE_DOCS_URL = "https://opencode.ai/docs";
    function isWindowsUiPlatform2() {
      if (typeof process !== "undefined" && process && process.platform === "win32") return true;
      if (typeof navigator !== "undefined" && typeof navigator.userAgent === "string") {
        return /windows/i.test(navigator.userAgent);
      }
      return false;
    }
    var ProviderAuthSectionMethods = class {
      classifyProviderAuthError(message) {
        if (!isWindowsUiPlatform2()) return "generic";
        const text = String(message || "").toLowerCase();
        if (!text) return "generic";
        if (/wsl(?:\.exe|\(|:|\/)|windows\s*\+\s*wsl/.test(text)) return "wsl";
        if (/not found|command not found|enoent|executable not found|未找到|找不到|spawn opencode|spawn .*opencode/.test(text)) {
          return "missing";
        }
        return "generic";
      }
      buildProviderAuthErrorSpec(message) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        const detail = String(message || "").trim();
        const kind = this.classifyProviderAuthError(detail);
        const commands = isWindowsUiPlatform2() ? ["node -v", "npm -v", "npm install -g opencode-ai", "opencode --version", "where opencode"] : [];
        const links = [];
        if (kind === "missing" || kind === "wsl") {
          links.push({
            label: t("settings.providerAuth.installDocs", "\u5B98\u65B9\u5B89\u88C5\u6587\u6863"),
            url: OPENCODE_DOCS_URL
          });
        }
        if (kind === "wsl") {
          return {
            kind,
            summary: t("settings.providerAuth.readFailedWsl", "\u68C0\u6D4B\u5230 WSL \u5B89\u88C5\uFF0C\u8BF7\u6539\u4E3A Windows \u672C\u673A\u5B89\u88C5\u3002"),
            intro: "\u8BF7\u4F7F\u7528\u8FBE.js\u8FDB\u884C\u5B89\u88C5\u3002",
            commands,
            links,
            copyText: [
              "\u68C0\u6D4B\u5230 WSL \u5B89\u88C5\uFF0C\u8BF7\u6539\u4E3A Windows \u672C\u673A\u5B89\u88C5\u3002",
              "\u8BF7\u4F7F\u7528\u8FBE.js\u8FDB\u884C\u5B89\u88C5\u3002",
              "",
              ...commands,
              "",
              `\u5B98\u65B9\u5B89\u88C5\u6587\u6863: ${OPENCODE_DOCS_URL}`,
              "",
              `\u539F\u59CB\u62A5\u9519: ${detail}`
            ].join("\n")
          };
        }
        if (kind === "missing") {
          return {
            kind,
            summary: t("settings.providerAuth.readFailedMissing", "\u672A\u68C0\u6D4B\u5230 OpenCode \u5B89\u88C5\uFF0C\u8BF7\u5148\u5B8C\u6210\u5B89\u88C5\u3002"),
            intro: t(
              "settings.providerAuth.readFailedMissingIntro",
              "\u8BF7\u5148\u5728 Windows \u672C\u673A\u5B89\u88C5 OpenCode\uFF0C\u518D\u56DE\u5230\u63D2\u4EF6\u91CC\u5237\u65B0 Provider \u72B6\u6001\u3002"
            ),
            commands,
            links,
            copyText: [
              "\u672A\u68C0\u6D4B\u5230 OpenCode \u5B89\u88C5\uFF0C\u8BF7\u5148\u5B8C\u6210\u5B89\u88C5\u3002",
              "\u8BF7\u5148\u5728 Windows \u672C\u673A\u5B89\u88C5 OpenCode\uFF0C\u518D\u56DE\u5230\u63D2\u4EF6\u91CC\u5237\u65B0 Provider \u72B6\u6001\u3002",
              "",
              ...commands,
              "",
              `\u5B98\u65B9\u5B89\u88C5\u6587\u6863: ${OPENCODE_DOCS_URL}`,
              "",
              `\u539F\u59CB\u62A5\u9519: ${detail}`
            ].join("\n")
          };
        }
        return {
          kind,
          summary: t("settings.providerAuth.readFailed", "\u8BFB\u53D6\u5931\u8D25\uFF1A{message}", { message: detail }),
          intro: t("settings.providerAuth.readFailedDetail", "\u8BFB\u53D6 Provider \u4FE1\u606F\u5931\u8D25\uFF1A{message}", { message: detail }),
          commands: [],
          links: [],
          copyText: detail
        };
      }
      renderProviderAuthErrorPanel(listEl, message) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        const spec = this.buildProviderAuthErrorSpec(message);
        const panel = listEl.createDiv({ cls: "oc-copyable-panel" });
        panel.createEl("strong", { text: spec.summary });
        if (spec.intro) {
          panel.createDiv({
            cls: "setting-item-description oc-copyable-help",
            text: spec.intro
          });
        }
        if (Array.isArray(spec.commands) && spec.commands.length) {
          const commandWrap = panel.createDiv({ cls: "oc-copyable-command-list" });
          spec.commands.forEach((cmd) => {
            const line = commandWrap.createDiv({ cls: "oc-copyable-command-item" });
            line.createEl("code", { text: cmd });
          });
        }
        const actionRow = panel.createDiv({ cls: "oc-copyable-actions" });
        if (Array.isArray(spec.links) && spec.links.length) {
          spec.links.forEach((item) => {
            const link = actionRow.createEl("a", {
              cls: "oc-copyable-link",
              text: String(item.label || item.url || ""),
              href: String(item.url || "")
            });
            link.target = "_blank";
            link.rel = "noopener noreferrer";
          });
        }
        const copyBtn = actionRow.createEl("button", {
          text: t("settings.providerAuth.copyError", "\u590D\u5236\u62A5\u9519\u4FE1\u606F")
        });
        copyBtn.type = "button";
        copyBtn.addEventListener("click", async () => {
          const copied = typeof this.copyToClipboard === "function" ? await this.copyToClipboard(spec.copyText) : false;
          if (copied) {
            new Notice2(t("settings.providerAuth.copyErrorDone", "\u62A5\u9519\u4FE1\u606F\u5DF2\u590D\u5236"));
          } else {
            new Notice2(t("settings.providerAuth.copyErrorManual", "\u590D\u5236\u5931\u8D25\uFF0C\u8BF7\u624B\u52A8\u9009\u62E9\u4E0B\u65B9\u6587\u672C\u590D\u5236\u3002"));
          }
        });
        const detailArea = panel.createEl("textarea", {
          cls: "oc-copyable-textarea",
          attr: {
            readonly: "true",
            spellcheck: "false",
            "aria-label": t("settings.providerAuth.errorDetails", "\u9519\u8BEF\u8BE6\u60C5")
          }
        });
        detailArea.value = spec.copyText;
        detailArea.rows = Math.min(14, Math.max(5, spec.copyText.split(/\r?\n/).length));
        detailArea.addEventListener("focus", () => detailArea.select());
        detailArea.addEventListener("click", () => detailArea.select());
        return spec;
      }
      renderProviderAuthSection(containerEl) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        containerEl.createEl("h3", { text: t("settings.providerAuth.heading", "Provider \u767B\u5F55\u7BA1\u7406\uFF08OAuth / API Key\uFF09") });
        containerEl.createEl("p", {
          text: t(
            "settings.providerAuth.intro",
            "\u7528\u4E8E\u5728\u63D2\u4EF6\u5185\u8FDE\u63A5\u5B98\u65B9/\u7B2C\u4E09\u65B9\u6A21\u578B provider\u3002\u5B8C\u6210\u540E\u4F1A\u5199\u5165\u5F53\u524D\u63D2\u4EF6\u8FD0\u884C\u65F6\u7684 FLOWnote \u51ED\u636E\u3002"
          )
        });
        const controls = containerEl.createDiv();
        controls.style.display = "flex";
        controls.style.alignItems = "stretch";
        controls.style.flexWrap = "wrap";
        controls.style.gap = "8px";
        controls.style.marginBottom = "8px";
        const refreshBtn = controls.createEl("button", { text: t("settings.providerAuth.refresh", "\u5237\u65B0 Provider \u72B6\u6001") });
        const expandBtn = controls.createEl("button", { text: t("settings.providerAuth.expandAll", "\u5168\u90E8\u5C55\u5F00") });
        const collapseBtn = controls.createEl("button", { text: t("settings.providerAuth.collapseAll", "\u5168\u90E8\u6536\u8D77") });
        const searchInput = controls.createEl("input", {
          attr: {
            type: "search",
            placeholder: t("settings.providerAuth.searchPlaceholder", "\u641C\u7D22 Provider / ID / \u9274\u6743\u65B9\u5F0F / \u6A21\u578B\u6570")
          }
        });
        searchInput.style.flex = "1 1 260px";
        searchInput.style.minWidth = "220px";
        searchInput.value = String(this.providerSearchQuery || "");
        const statusEl = containerEl.createDiv({ text: t("settings.providerAuth.loading", "\u52A0\u8F7D\u4E2D...") });
        statusEl.style.fontSize = "12px";
        statusEl.style.color = "var(--text-muted)";
        statusEl.style.marginBottom = "6px";
        const listEl = containerEl.createDiv();
        listEl.style.display = "flex";
        listEl.style.flexDirection = "column";
        listEl.style.gap = "8px";
        listEl.style.marginBottom = "12px";
        const renderFromSnapshot = () => {
          listEl.empty();
          const snapshot = this.providerAuthSnapshot;
          if (!snapshot) {
            statusEl.setText(t("settings.providerAuth.notLoaded", "\u5C1A\u672A\u52A0\u8F7D Provider \u4FE1\u606F\u3002"));
            return;
          }
          const query = this.normalizeSearchText(this.providerSearchQuery);
          let entries = snapshot.providers.map((provider) => this.buildProviderEntry(provider, snapshot.connectedSet, snapshot.authMap));
          if (query) {
            entries = entries.filter((entry) => this.providerEntryMatchesQuery(entry, query));
          }
          const totalProviders = snapshot.providers.length;
          const visibleCount = entries.length;
          if (!entries.length) {
            statusEl.setText(query ? t(
              "settings.providerAuth.loadedNoResult",
              "\u5DF2\u52A0\u8F7D {totalProviders} \u4E2A Provider\uFF0C\u641C\u7D22\u201C{query}\u201D\u65E0\u7ED3\u679C\u3002",
              { totalProviders, query: this.providerSearchQuery }
            ) : t("settings.providerAuth.loaded", "\u5DF2\u52A0\u8F7D {totalProviders} \u4E2A Provider\u3002", { totalProviders }));
            listEl.createDiv({
              text: query ? t("settings.providerAuth.noMatch", "\u6CA1\u6709\u5339\u914D\u7684 Provider\uFF0C\u8BF7\u5C1D\u8BD5\u66F4\u6362\u5173\u952E\u8BCD\u3002") : t("settings.providerAuth.empty", "\u5F53\u524D\u6CA1\u6709\u53EF\u663E\u793A\u7684 Provider\u3002"),
              cls: "setting-item-description"
            });
            return;
          }
          const connectedCount = entries.filter((entry) => entry.isConnected).length;
          statusEl.setText(t(
            "settings.providerAuth.loadedWithCount",
            "\u5DF2\u52A0\u8F7D {totalProviders} \u4E2A Provider\uFF0C\u5F53\u524D\u663E\u793A {visibleCount} \u4E2A\uFF1B\u5DF2\u8FDE\u63A5 {connectedCount} \u4E2A\u3002",
            { totalProviders, visibleCount, connectedCount }
          ));
          const groupMap = /* @__PURE__ */ new Map();
          entries.forEach((entry) => {
            const key = this.isCommonProviderEntry(entry) ? "common" : "others";
            let group = groupMap.get(key);
            if (!group) {
              const label = key === "common" ? t("settings.providerAuth.groupCommon", "\u5E38\u7528") : t("settings.providerAuth.groupOthers", "\u5176\u4ED6");
              group = {
                key,
                label,
                entries: []
              };
              groupMap.set(key, group);
            }
            group.entries.push(entry);
          });
          const groups = Array.from(groupMap.values()).sort((a, b) => {
            if (a.key === b.key) return 0;
            if (a.key === "common") return -1;
            return 1;
          });
          groups.forEach((group) => {
            const inGroup = group.entries;
            inGroup.sort((a, b) => {
              if (a.isConnected !== b.isConnected) return a.isConnected ? -1 : 1;
              return a.providerName.localeCompare(b.providerName);
            });
            const groupDetails = listEl.createEl("details");
            groupDetails.open = Boolean(query);
            groupDetails.style.border = "1px solid var(--background-modifier-border)";
            groupDetails.style.borderRadius = "8px";
            groupDetails.style.padding = "2px 0";
            const groupSummary = groupDetails.createEl("summary", { text: `${group.label} (${inGroup.length})` });
            groupSummary.style.padding = "8px 10px";
            groupSummary.style.cursor = "pointer";
            groupSummary.style.fontWeight = "600";
            const groupBody = groupDetails.createDiv();
            groupBody.style.display = "flex";
            groupBody.style.flexDirection = "column";
            groupBody.style.gap = "6px";
            groupBody.style.padding = "4px 6px 6px";
            inGroup.forEach((entry) => {
              this.renderProviderRow({
                listEl: groupBody,
                entry,
                reload: loadProviders
              });
            });
          });
        };
        const toggleAllDetails = (open) => {
          listEl.querySelectorAll("details").forEach((node) => {
            node.open = open;
          });
        };
        const loadProviders = async () => {
          if (this.plugin && typeof this.plugin.log === "function") this.plugin.log("provider refresh: start");
          refreshBtn.disabled = true;
          refreshBtn.setText(t("settings.providerAuth.refreshBusy", "\u5237\u65B0\u4E2D..."));
          expandBtn.disabled = true;
          collapseBtn.disabled = true;
          statusEl.setText(t("settings.providerAuth.loadingProviders", "\u6B63\u5728\u8BFB\u53D6 Provider \u914D\u7F6E..."));
          listEl.empty();
          try {
            if (this.plugin && typeof this.plugin.log === "function") this.plugin.log("provider refresh: listProviders");
            const providerResult = await this.plugin.opencodeClient.listProviders();
            if (this.plugin && typeof this.plugin.log === "function") this.plugin.log("provider refresh: listProviderAuthMethods");
            const authMapResult = await this.plugin.opencodeClient.listProviderAuthMethods();
            const providers = Array.isArray(providerResult && providerResult.all) ? [...providerResult.all] : [];
            const connectedSet = new Set(
              Array.isArray(providerResult && providerResult.connected) ? providerResult.connected.map((id) => String(id)) : []
            );
            const authMap = authMapResult && typeof authMapResult === "object" ? authMapResult : {};
            providers.sort((a, b) => String(a && a.name ? a.name : a && a.id ? a.id : "").localeCompare(String(b && b.name ? b.name : b && b.id ? b.id : "")));
            this.providerAuthSnapshot = { providers, connectedSet, authMap };
            if (typeof this.plugin.refreshModelCatalog === "function") {
              try {
                if (this.plugin && typeof this.plugin.log === "function") this.plugin.log("provider refresh: refreshModelCatalog");
                await this.plugin.refreshModelCatalog({
                  connectedProviders: connectedSet,
                  providerResult,
                  notifyView: true
                });
              } catch (refreshError) {
                if (this.plugin && typeof this.plugin.log === "function") {
                  this.plugin.log(
                    `refresh model catalog from provider settings failed: ${refreshError instanceof Error ? refreshError.message : String(refreshError)}`
                  );
                }
              }
            }
            if (typeof this.plugin.refreshDiagnosticsStatus === "function") {
              try {
                if (this.plugin && typeof this.plugin.log === "function") this.plugin.log("provider refresh: diagnostics");
                await this.plugin.refreshDiagnosticsStatus({ ttlMs: 0, force: true, applyView: true });
              } catch (diagnosticsError) {
                if (this.plugin && typeof this.plugin.log === "function") {
                  this.plugin.log(
                    `refresh diagnostics from provider settings failed: ${diagnosticsError instanceof Error ? diagnosticsError.message : String(diagnosticsError)}`
                  );
                }
              }
            }
            if (this.plugin && typeof this.plugin.log === "function") this.plugin.log("provider refresh: render");
            renderFromSnapshot();
          } catch (e) {
            const msg = e instanceof Error ? e.message : String(e);
            if (this.plugin && typeof this.plugin.log === "function") {
              this.plugin.log(`provider refresh: failed ${msg}`);
            }
            const spec = this.renderProviderAuthErrorPanel(listEl, msg);
            statusEl.setText(spec.summary);
          } finally {
            if (this.plugin && typeof this.plugin.log === "function") this.plugin.log("provider refresh: done");
            refreshBtn.disabled = false;
            refreshBtn.setText(t("settings.providerAuth.refresh", "\u5237\u65B0 Provider \u72B6\u6001"));
            expandBtn.disabled = false;
            collapseBtn.disabled = false;
          }
        };
        searchInput.addEventListener("input", () => {
          this.providerSearchQuery = String(searchInput.value || "");
          renderFromSnapshot();
        });
        expandBtn.addEventListener("click", () => toggleAllDetails(true));
        collapseBtn.addEventListener("click", () => toggleAllDetails(false));
        refreshBtn.addEventListener("click", loadProviders);
        renderFromSnapshot();
        if (!this.providerAuthSnapshot) {
          void loadProviders();
        }
      }
      renderProviderRow(context) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        const { listEl, entry, reload } = context;
        const {
          providerID,
          providerName,
          methodsRaw,
          oauthMethods,
          supportsApi,
          isConnected,
          modelCount,
          methodText
        } = entry;
        if (!providerID) return;
        const row = listEl.createDiv();
        row.style.border = "1px solid var(--background-modifier-border)";
        row.style.borderRadius = "6px";
        row.style.padding = "8px 10px";
        const titleRow = row.createDiv();
        titleRow.style.display = "flex";
        titleRow.style.alignItems = "center";
        titleRow.style.justifyContent = "space-between";
        titleRow.style.gap = "8px";
        const titleLeft = titleRow.createDiv();
        titleLeft.style.display = "flex";
        titleLeft.style.alignItems = "center";
        titleLeft.style.gap = "8px";
        titleLeft.createEl("strong", { text: providerName });
        titleLeft.createSpan({ text: `(${providerID})`, cls: "setting-item-description" });
        const statusBadge = titleRow.createSpan({
          text: isConnected ? t("settings.providerAuth.connected", "\u5DF2\u8FDE\u63A5") : t("settings.providerAuth.disconnected", "\u672A\u8FDE\u63A5"),
          cls: "setting-item-description"
        });
        statusBadge.style.color = isConnected ? "var(--text-success)" : "var(--text-muted)";
        const desc = row.createDiv({ cls: "setting-item-description" });
        desc.setText(t(
          "settings.providerAuth.providerMeta",
          "\u6A21\u578B\u6570\uFF1A{modelCount}\uFF1B\u9274\u6743\u65B9\u5F0F\uFF1A{methodText}",
          { modelCount, methodText }
        ));
        const actionRow = row.createDiv();
        actionRow.style.display = "flex";
        actionRow.style.flexWrap = "wrap";
        actionRow.style.gap = "6px";
        actionRow.style.marginTop = "8px";
        const rowStatus = row.createDiv({ cls: "setting-item-description" });
        rowStatus.style.marginTop = "6px";
        const runAction = async (button, pendingText, action) => {
          button.disabled = true;
          const originalText = button.textContent || "";
          button.setText(pendingText);
          try {
            await action();
          } catch (e) {
            const msg = e instanceof Error ? e.message : String(e);
            rowStatus.setText(t("settings.providerAuth.actionFailed", "\u64CD\u4F5C\u5931\u8D25\uFF1A{message}", { message: msg }));
            new Notice2(t("settings.providerAuth.actionFailed", "\u64CD\u4F5C\u5931\u8D25\uFF1A{message}", { message: msg }));
          } finally {
            button.disabled = false;
            button.setText(originalText);
          }
        };
        if (oauthMethods.length) {
          const oauthBtn = actionRow.createEl("button", { text: t("settings.providerAuth.oauthLogin", "OAuth \u767B\u5F55") });
          oauthBtn.addEventListener("click", () => {
            void runAction(oauthBtn, t("settings.providerAuth.pending", "\u5904\u7406\u4E2D..."), async () => {
              await this.handleOauthLogin({
                providerID,
                providerName,
                oauthMethods,
                rowStatus
              });
              if (typeof this.plugin.clearUnavailableModels === "function") {
                this.plugin.clearUnavailableModels({ providerID });
              }
              await reload();
            });
          });
        }
        if (supportsApi || !methodsRaw.length) {
          const apiBtn = actionRow.createEl("button", { text: t("settings.providerAuth.setApiKey", "\u8BBE\u7F6E API Key") });
          apiBtn.addEventListener("click", () => {
            void runAction(apiBtn, t("settings.providerAuth.saving", "\u4FDD\u5B58\u4E2D..."), async () => {
              await this.handleApiKeySet({
                providerID,
                providerName,
                rowStatus
              });
              if (typeof this.plugin.clearUnavailableModels === "function") {
                this.plugin.clearUnavailableModels({ providerID });
              }
              await reload();
            });
          });
        }
        if (isConnected) {
          const clearBtn = actionRow.createEl("button", { text: t("settings.providerAuth.clearLogin", "\u6E05\u9664\u767B\u5F55") });
          clearBtn.addEventListener("click", () => {
            void runAction(clearBtn, t("settings.providerAuth.clearing", "\u6E05\u9664\u4E2D..."), async () => {
              const confirmed = await this.showConfirmModal({
                title: t("settings.providerAuth.clearTitle", "\u6E05\u9664 {providerName} \u767B\u5F55", { providerName }),
                description: t("settings.providerAuth.clearDesc", "\u786E\u8BA4\u6E05\u9664\u8BE5 Provider \u7684\u767B\u5F55\u51ED\u636E\uFF1F"),
                submitText: t("settings.providerAuth.clearSubmit", "\u6E05\u9664")
              });
              if (!confirmed) return;
              await this.plugin.opencodeClient.clearProviderAuth({ providerID });
              if (typeof this.plugin.clearUnavailableModels === "function") {
                this.plugin.clearUnavailableModels({ providerID });
              }
              rowStatus.setText(t("settings.providerAuth.clearDone", "\u5DF2\u6E05\u9664\u767B\u5F55\u51ED\u636E\u3002"));
              new Notice2(t("settings.providerAuth.clearNotice", "{providerName} \u51ED\u636E\u5DF2\u6E05\u9664", { providerName }));
              await reload();
            });
          });
        }
      }
    };
    var providerAuthSectionMethods = {};
    for (const key of Object.getOwnPropertyNames(ProviderAuthSectionMethods.prototype)) {
      if (key === "constructor") continue;
      providerAuthSectionMethods[key] = ProviderAuthSectionMethods.prototype[key];
    }
    module2.exports = { providerAuthSectionMethods };
  }
});

// runtime/settings/modals.js
var require_modals2 = __commonJS({
  "runtime/settings/modals.js"(exports2, module2) {
    var { Modal: Modal2 } = require("obsidian");
    var InputPromptModal = class extends Modal2 {
      constructor(app, options, onResolve) {
        super(app);
        this.options = options || {};
        this.onResolve = typeof onResolve === "function" ? onResolve : () => {
        };
        this.resolved = false;
      }
      resolve(value) {
        if (this.resolved) return;
        this.resolved = true;
        this.onResolve(value);
        this.close();
      }
      onOpen() {
        const { contentEl } = this;
        contentEl.empty();
        contentEl.createEl("h2", { text: String(this.options.title || "Input") });
        if (this.options.description) contentEl.createEl("p", { text: String(this.options.description) });
        const input = contentEl.createEl("input", {
          attr: {
            type: this.options.password ? "password" : "text",
            placeholder: String(this.options.placeholder || "")
          }
        });
        input.style.width = "100%";
        input.value = String(this.options.value || "");
        const actions = contentEl.createDiv();
        actions.style.display = "flex";
        actions.style.justifyContent = "flex-end";
        actions.style.gap = "8px";
        actions.style.marginTop = "12px";
        const cancelBtn = actions.createEl("button", { text: String(this.options.cancelText || "Cancel") });
        const submitBtn = actions.createEl("button", { text: String(this.options.submitText || "Confirm"), cls: "mod-cta" });
        cancelBtn.addEventListener("click", () => this.resolve(null));
        submitBtn.addEventListener("click", () => this.resolve(String(input.value || "")));
        input.addEventListener("keydown", (ev) => {
          if (ev.key === "Enter") {
            ev.preventDefault();
            this.resolve(String(input.value || ""));
          }
        });
        setTimeout(() => input.focus(), 0);
      }
      onClose() {
        if (!this.resolved) this.onResolve(null);
        this.contentEl.empty();
      }
    };
    var ConfirmModal = class extends Modal2 {
      constructor(app, options, onResolve) {
        super(app);
        this.options = options || {};
        this.onResolve = typeof onResolve === "function" ? onResolve : () => {
        };
        this.resolved = false;
      }
      resolve(value) {
        if (this.resolved) return;
        this.resolved = true;
        this.onResolve(Boolean(value));
        this.close();
      }
      onOpen() {
        const { contentEl } = this;
        contentEl.empty();
        contentEl.createEl("h2", { text: String(this.options.title || "Confirm Action") });
        if (this.options.description) contentEl.createEl("p", { text: String(this.options.description) });
        const actions = contentEl.createDiv();
        actions.style.display = "flex";
        actions.style.justifyContent = "flex-end";
        actions.style.gap = "8px";
        actions.style.marginTop = "12px";
        const cancelBtn = actions.createEl("button", { text: String(this.options.cancelText || "Cancel") });
        const submitBtn = actions.createEl("button", { text: String(this.options.submitText || "Confirm"), cls: "mod-cta" });
        cancelBtn.addEventListener("click", () => this.resolve(false));
        submitBtn.addEventListener("click", () => this.resolve(true));
      }
      onClose() {
        if (!this.resolved) this.onResolve(false);
        this.contentEl.empty();
      }
    };
    var SelectPromptModal = class extends Modal2 {
      constructor(app, options, onResolve) {
        super(app);
        this.options = options || {};
        this.onResolve = typeof onResolve === "function" ? onResolve : () => {
        };
        this.resolved = false;
      }
      resolve(value) {
        if (this.resolved) return;
        this.resolved = true;
        this.onResolve(value);
        this.close();
      }
      onOpen() {
        const { contentEl } = this;
        const options = Array.isArray(this.options.options) ? this.options.options : [];
        contentEl.empty();
        contentEl.createEl("h2", { text: String(this.options.title || "Select") });
        if (this.options.description) contentEl.createEl("p", { text: String(this.options.description) });
        const select = contentEl.createEl("select");
        select.style.width = "100%";
        select.style.marginTop = "8px";
        options.forEach((item) => {
          select.createEl("option", {
            value: String(item.value),
            text: String(item.label || item.value)
          });
        });
        if (this.options.defaultValue !== void 0 && this.options.defaultValue !== null) {
          select.value = String(this.options.defaultValue);
        }
        const actions = contentEl.createDiv();
        actions.style.display = "flex";
        actions.style.justifyContent = "flex-end";
        actions.style.gap = "8px";
        actions.style.marginTop = "12px";
        const cancelBtn = actions.createEl("button", { text: String(this.options.cancelText || "Cancel") });
        const submitBtn = actions.createEl("button", { text: String(this.options.submitText || "Confirm"), cls: "mod-cta" });
        cancelBtn.addEventListener("click", () => this.resolve(null));
        submitBtn.addEventListener("click", () => this.resolve(String(select.value || "")));
        select.addEventListener("keydown", (ev) => {
          if (ev.key === "Enter") {
            ev.preventDefault();
            this.resolve(String(select.value || ""));
          }
        });
      }
      onClose() {
        if (!this.resolved) this.onResolve(null);
        this.contentEl.empty();
      }
    };
    var ConflictResolutionModal = class extends Modal2 {
      constructor(app, options, onResolve) {
        super(app);
        this.options = options || {};
        this.onResolve = typeof onResolve === "function" ? onResolve : () => {
        };
        this.resolved = false;
      }
      resolve(value) {
        if (this.resolved) return;
        this.resolved = true;
        this.onResolve(String(value || "cancel"));
        this.close();
      }
      onOpen() {
        const { contentEl } = this;
        contentEl.empty();
        contentEl.createEl("h2", { text: String(this.options.title || "Resolve Conflict") });
        if (this.options.description) contentEl.createEl("p", { text: String(this.options.description) });
        if (this.options.context) {
          const pre = contentEl.createEl("pre", { text: String(this.options.context) });
          pre.style.whiteSpace = "pre-wrap";
          pre.style.wordBreak = "break-all";
          pre.style.maxHeight = "240px";
          pre.style.overflow = "auto";
        }
        const actions = contentEl.createDiv();
        actions.style.display = "flex";
        actions.style.flexWrap = "wrap";
        actions.style.justifyContent = "flex-end";
        actions.style.gap = "8px";
        actions.style.marginTop = "12px";
        const skipBtn = actions.createEl("button", { text: String(this.options.skipText || "Skip") });
        const skipAllBtn = actions.createEl("button", { text: String(this.options.skipAllText || "Skip All") });
        const replaceBtn = actions.createEl("button", { text: String(this.options.replaceText || "Replace"), cls: "mod-cta" });
        const replaceAllBtn = actions.createEl("button", { text: String(this.options.replaceAllText || "Replace All") });
        const cancelBtn = actions.createEl("button", { text: String(this.options.cancelText || "Cancel") });
        skipBtn.addEventListener("click", () => this.resolve("skip"));
        skipAllBtn.addEventListener("click", () => this.resolve("skip_all"));
        replaceBtn.addEventListener("click", () => this.resolve("replace"));
        replaceAllBtn.addEventListener("click", () => this.resolve("replace_all"));
        cancelBtn.addEventListener("click", () => this.resolve("cancel"));
      }
      onClose() {
        if (!this.resolved) this.onResolve("cancel");
        this.contentEl.empty();
      }
    };
    module2.exports = {
      InputPromptModal,
      ConfirmModal,
      SelectPromptModal,
      ConflictResolutionModal
    };
  }
});

// runtime/settings/provider-auth-actions.js
var require_provider_auth_actions = __commonJS({
  "runtime/settings/provider-auth-actions.js"(exports2, module2) {
    var { Notice: Notice2, openExternal } = require("obsidian");
    var { tFromContext } = require_i18n_runtime();
    var {
      InputPromptModal,
      ConfirmModal,
      SelectPromptModal,
      ConflictResolutionModal
    } = require_modals2();
    var ProviderAuthActionsMethods = class {
      async pickOauthMethod(providerName, oauthMethods) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        if (!oauthMethods.length) return null;
        if (oauthMethods.length === 1) return oauthMethods[0];
        const options = oauthMethods.map((item) => ({
          value: String(item.index),
          label: String(item.label || `OAuth ${item.index + 1}`)
        }));
        const pickedValue = await this.showSelectModal({
          title: t("settings.providerAuth.pickOauthTitle", "\u9009\u62E9 {providerName} \u7684 OAuth \u767B\u5F55\u65B9\u5F0F", { providerName }),
          description: t("settings.providerAuth.pickOauthDesc", "\u8BF7\u9009\u62E9\u4E00\u79CD OAuth \u9274\u6743\u65B9\u5F0F\u3002"),
          options,
          defaultValue: String(oauthMethods[0].index),
          submitText: t("settings.providerAuth.continue", "\u7EE7\u7EED")
        });
        if (pickedValue === null) return null;
        const picked = oauthMethods.find((item) => String(item.index) === String(pickedValue));
        if (!picked) throw new Error(t("settings.providerAuth.invalidOauthMethod", "\u65E0\u6548\u7684 OAuth \u65B9\u5F0F\u5E8F\u53F7"));
        return picked;
      }
      async handleOauthLogin(context) {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        const { providerID, providerName, oauthMethods, rowStatus } = context;
        const picked = await this.pickOauthMethod(providerName, oauthMethods);
        if (!picked) {
          rowStatus.setText(t("settings.providerAuth.oauthCanceled", "\u5DF2\u53D6\u6D88 OAuth \u767B\u5F55\u3002"));
          return;
        }
        rowStatus.setText(t("settings.providerAuth.oauthCreatingLink", "\u6B63\u5728\u521B\u5EFA\u6388\u6743\u94FE\u63A5..."));
        const authorization = await this.plugin.opencodeClient.authorizeProviderOauth({
          providerID,
          method: picked.index
        });
        const authUrl = String(authorization && authorization.url ? authorization.url : "").trim();
        const mode = String(authorization && authorization.method ? authorization.method : "code").trim();
        const instructions = String(authorization && authorization.instructions ? authorization.instructions : "").trim();
        if (!authUrl) {
          throw new Error(t("settings.providerAuth.oauthMissingUrl", "\u672A\u83B7\u53D6\u5230 OAuth \u6388\u6743\u94FE\u63A5"));
        }
        const opened = await this.openAuthorizationUrl(authUrl);
        if (opened) {
          new Notice2(t("settings.providerAuth.oauthOpened", "\u5DF2\u6253\u5F00 {providerName} \u6388\u6743\u9875\u9762", { providerName }));
        } else {
          await this.copyToClipboard(authUrl);
          new Notice2(t("settings.providerAuth.oauthCopied", "\u65E0\u6CD5\u81EA\u52A8\u6253\u5F00\u6D4F\u89C8\u5668\uFF0C\u6388\u6743\u94FE\u63A5\u5DF2\u590D\u5236\u5230\u526A\u8D34\u677F\u3002"));
        }
        let code = "";
        if (mode === "code") {
          const input = await this.showInputModal({
            title: t("settings.providerAuth.oauthCallbackTitle", "{providerName} OAuth \u56DE\u8C03", { providerName }),
            description: instructions || t("settings.providerAuth.oauthCallbackDesc", "\u8BF7\u5728\u6D4F\u89C8\u5668\u5B8C\u6210\u6388\u6743\u540E\u7C98\u8D34 code\u3002"),
            placeholder: t("settings.providerAuth.oauthCallbackPlaceholder", "\u7C98\u8D34\u6388\u6743 code"),
            submitText: t("settings.providerAuth.submit", "\u63D0\u4EA4")
          });
          if (input === null) {
            rowStatus.setText(t("settings.providerAuth.oauthCanceled", "\u5DF2\u53D6\u6D88 OAuth \u767B\u5F55\u3002"));
            return;
          }
          code = String(input || "").trim();
          if (!code) {
            rowStatus.setText(t("settings.providerAuth.oauthCodeMissing", "\u672A\u586B\u5199\u6388\u6743 code\uFF0C\u5DF2\u53D6\u6D88\u3002"));
            return;
          }
        } else {
          const confirmed = await this.showConfirmModal({
            title: t("settings.providerAuth.oauthCallbackTitle", "{providerName} OAuth \u56DE\u8C03", { providerName }),
            description: `${instructions || t("settings.providerAuth.oauthCompleteInBrowser", "\u8BF7\u5728\u6D4F\u89C8\u5668\u5B8C\u6210\u6388\u6743\u3002")}

${t("settings.providerAuth.oauthThenConfirm", "\u5B8C\u6210\u540E\u70B9\u51FB\u201C\u786E\u8BA4\u201D\u7EE7\u7EED\u3002")}`,
            submitText: t("settings.providerAuth.confirm", "\u786E\u8BA4")
          });
          if (!confirmed) {
            rowStatus.setText(t("settings.providerAuth.oauthCanceled", "\u5DF2\u53D6\u6D88 OAuth \u767B\u5F55\u3002"));
            return;
          }
        }
        rowStatus.setText(t("settings.providerAuth.oauthSubmitting", "\u6B63\u5728\u63D0\u4EA4 OAuth \u56DE\u8C03..."));
        await this.plugin.opencodeClient.completeProviderOauth({
          providerID,
          method: picked.index,
          code: code || void 0
        });
        rowStatus.setText(t("settings.providerAuth.oauthDone", "OAuth \u767B\u5F55\u5B8C\u6210\u3002"));
        new Notice2(t("settings.providerAuth.oauthSuccess", "{providerName} \u767B\u5F55\u6210\u529F", { providerName }));
      }
      async handleApiKeySet(context) {
        const t = (key2, fallback, params = {}) => tFromContext(this, key2, fallback, params);
        const { providerID, providerName, rowStatus } = context;
        const key = await this.showInputModal({
          title: t("settings.providerAuth.apiKeyTitle", "\u8BBE\u7F6E {providerName} API Key", { providerName }),
          description: t(
            "settings.providerAuth.apiKeyDesc",
            "\u8BF7\u8F93\u5165\u8BE5 Provider \u7684 API Key\uFF08\u4EC5\u4FDD\u5B58\u5728\u672C\u5730 FLOWnote \u51ED\u636E\u4E2D\uFF09\u3002"
          ),
          placeholder: t("settings.providerAuth.apiKeyPlaceholder", "\u8F93\u5165 API Key"),
          password: true,
          submitText: t("settings.providerAuth.save", "\u4FDD\u5B58")
        });
        if (key === null) {
          rowStatus.setText(t("settings.providerAuth.apiKeyCanceled", "\u5DF2\u53D6\u6D88 API Key \u8BBE\u7F6E\u3002"));
          return;
        }
        const trimmed = String(key || "").trim();
        if (!trimmed) {
          rowStatus.setText(t("settings.providerAuth.apiKeyEmpty", "API Key \u4E0D\u80FD\u4E3A\u7A7A\u3002"));
          return;
        }
        rowStatus.setText(t("settings.providerAuth.apiKeySaving", "\u6B63\u5728\u4FDD\u5B58 API Key..."));
        await this.plugin.opencodeClient.setProviderApiKeyAuth({
          providerID,
          key: trimmed
        });
        rowStatus.setText(t("settings.providerAuth.apiKeySaved", "API Key \u5DF2\u4FDD\u5B58\u3002"));
        new Notice2(t("settings.providerAuth.apiKeySavedNotice", "{providerName} API Key \u5DF2\u4FDD\u5B58", { providerName }));
      }
      async openAuthorizationUrl(url) {
        const value = String(url || "").trim();
        if (!value) return false;
        try {
          if (typeof openExternal === "function") {
            await openExternal(value);
            return true;
          }
        } catch {
        }
        try {
          if (typeof window !== "undefined" && typeof window.open === "function") {
            window.open(value, "_blank", "noopener,noreferrer");
            return true;
          }
        } catch {
        }
        return false;
      }
      async copyToClipboard(text) {
        const value = String(text || "");
        if (!value) return false;
        try {
          if (typeof navigator !== "undefined" && navigator.clipboard && typeof navigator.clipboard.writeText === "function") {
            await navigator.clipboard.writeText(value);
            return true;
          }
        } catch {
        }
        return false;
      }
      showInputModal(options) {
        return new Promise((resolve) => {
          const modal = new InputPromptModal(this.app, options, resolve);
          modal.open();
        });
      }
      showConfirmModal(options) {
        return new Promise((resolve) => {
          const modal = new ConfirmModal(this.app, options, resolve);
          modal.open();
        });
      }
      showSelectModal(options) {
        return new Promise((resolve) => {
          const modal = new SelectPromptModal(this.app, options, resolve);
          modal.open();
        });
      }
      showConflictResolutionModal(options) {
        return new Promise((resolve) => {
          const modal = new ConflictResolutionModal(this.app, options, resolve);
          modal.open();
        });
      }
    };
    var providerAuthActionsMethods = {};
    for (const key of Object.getOwnPropertyNames(ProviderAuthActionsMethods.prototype)) {
      if (key === "constructor") continue;
      providerAuthActionsMethods[key] = ProviderAuthActionsMethods.prototype[key];
    }
    module2.exports = { providerAuthActionsMethods };
  }
});

// runtime/settings-tab.js
var require_settings_tab = __commonJS({
  "runtime/settings-tab.js"(exports2, module2) {
    var {
      PluginSettingTab: PluginSettingTab2
    } = require("obsidian");
    var {
      basicSettingsSectionMethods
    } = require_basic_settings_section_methods();
    var {
      providerAuthUtilsMethods
    } = require_provider_auth_utils();
    var {
      providerAuthSectionMethods
    } = require_provider_auth_section_methods();
    var {
      providerAuthActionsMethods
    } = require_provider_auth_actions();
    var FLOWnoteSettingsTab = class extends PluginSettingTab2 {
      constructor(app, plugin) {
        super(app, plugin);
        this.plugin = plugin;
        this.providerSearchQuery = "";
        this.providerAuthSnapshot = null;
      }
    };
    Object.assign(
      FLOWnoteSettingsTab.prototype,
      basicSettingsSectionMethods,
      providerAuthUtilsMethods,
      providerAuthSectionMethods,
      providerAuthActionsMethods
    );
    module2.exports = { FLOWnoteSettingsTab };
  }
});

// runtime/skill-service.js
var require_skill_service = __commonJS({
  "runtime/skill-service.js"(exports2, module2) {
    var fs = require("fs");
    var path = require("path");
    function parseFrontmatter(md) {
      const m = md.match(/^---\n([\s\S]*?)\n---\n?/);
      if (!m) return { attrs: {}, body: md };
      const attrs = {};
      const lines = m[1].split(/\r?\n/);
      for (let idx = 0; idx < lines.length; idx += 1) {
        const line = lines[idx];
        const i = line.indexOf(":");
        if (i <= 0) continue;
        const key = line.slice(0, i).trim();
        const rawValue = line.slice(i + 1).trim();
        const blockScalar = rawValue.match(/^([>|])([+-])?$/);
        if (blockScalar) {
          const block = [];
          idx += 1;
          while (idx < lines.length) {
            const next = lines[idx];
            if (!/^\s+/.test(next)) {
              idx -= 1;
              break;
            }
            block.push(next.replace(/^\s+/, ""));
            idx += 1;
          }
          attrs[key] = block.join("\n").trim();
          continue;
        }
        attrs[key] = rawValue.replace(/^['"]|['"]$/g, "");
      }
      return { attrs, body: md.slice(m[0].length) };
    }
    function summarizeBody(body) {
      return body.split(/\r?\n/).map((x) => x.trim()).filter(Boolean).slice(0, 18).join("\n");
    }
    function copyDirectoryRecursive(srcDir, destDir) {
      if (!fs.existsSync(srcDir)) return;
      fs.mkdirSync(destDir, { recursive: true });
      const entries = fs.readdirSync(srcDir, { withFileTypes: true });
      for (const entry of entries) {
        if (!entry || String(entry.name || "").startsWith(".")) continue;
        const srcPath = path.join(srcDir, entry.name);
        const destPath = path.join(destDir, entry.name);
        if (entry.isDirectory()) {
          copyDirectoryRecursive(srcPath, destPath);
          continue;
        }
        if (entry.isFile()) {
          fs.copyFileSync(srcPath, destPath);
        }
      }
    }
    var SkillService = class {
      constructor(vaultPath, settings) {
        this.vaultPath = vaultPath;
        this.settings = settings;
        this.cache = [];
        this.allowedSkillIds = null;
      }
      updateSettings(settings) {
        this.settings = settings;
      }
      setAllowedSkillIds(skillIds) {
        if (!skillIds) {
          this.allowedSkillIds = null;
          return;
        }
        const ids = Array.isArray(skillIds) ? skillIds : Array.from(skillIds);
        const normalized = ids.map((item) => String(item || "").trim()).filter(Boolean);
        this.allowedSkillIds = new Set(normalized);
      }
      loadSkills() {
        const root = path.join(this.vaultPath, this.settings.skillsDir);
        if (!fs.existsSync(root)) {
          this.cache = [];
          return this.cache;
        }
        const entries = fs.readdirSync(root, { withFileTypes: true });
        const skills = [];
        for (const e of entries) {
          if (!e || String(e.name || "").startsWith(".")) continue;
          if (!e.isDirectory()) continue;
          const file = path.join(root, e.name, "SKILL.md");
          if (!fs.existsSync(file)) continue;
          const raw = fs.readFileSync(file, "utf8");
          const parsed = parseFrontmatter(raw);
          skills.push({
            id: e.name,
            name: parsed.attrs.name || e.name,
            description: parsed.attrs.description || "",
            metadata: parsed.attrs,
            content: raw,
            summary: summarizeBody(parsed.body),
            path: file
          });
        }
        skills.sort((a, b) => a.name.localeCompare(b.name));
        this.cache = skills;
        return skills;
      }
      getSkills() {
        return this.cache;
      }
      buildInjectedPrompt(skill, mode, userPrompt) {
        if (!skill || mode === "off") return userPrompt;
        if (mode === "full") {
          return [
            `\u4F60\u5F53\u524D\u8981\u9075\u5FAA\u6280\u80FD ${skill.name}\u3002`,
            "\u6280\u80FD\u6587\u6863\u5982\u4E0B\uFF1A",
            skill.content,
            "\u7528\u6237\u8BF7\u6C42\u5982\u4E0B\uFF1A",
            userPrompt
          ].join("\n\n");
        }
        return [
          `\u4F60\u5F53\u524D\u8981\u9075\u5FAA\u6280\u80FD ${skill.name}\u3002`,
          `\u6280\u80FD\u8BF4\u660E\uFF1A${skill.description || "\u65E0"}`,
          "\u6280\u80FD\u6458\u8981\uFF1A",
          skill.summary,
          "\u7528\u6237\u8BF7\u6C42\u5982\u4E0B\uFF1A",
          userPrompt
        ].join("\n\n");
      }
    };
    module2.exports = {
      SkillService,
      copyDirectoryRecursive
    };
  }
});

// runtime/state-migrations.js
var require_state_migrations = __commonJS({
  "runtime/state-migrations.js"(exports2, module2) {
    function splitLegacyMixedAssistantText(rawText) {
      const text = typeof rawText === "string" ? rawText : "";
      if (!text) return { changed: false, text: "", reasoning: "" };
      const hasLegacyMarkers = /(^|\n)(step-start|step-finish|reasoning|text|stop)(\n|$)/m.test(text);
      const hasLegacyIds = /(^|\n)(prt_|ses_|msg_)[a-zA-Z0-9]+(\n|$)/m.test(text);
      if (!hasLegacyMarkers && !hasLegacyIds) {
        return { changed: false, text, reasoning: "" };
      }
      const reasoningLines = [];
      const outputLines = [];
      const lines = text.split(/\r?\n/);
      let mode = "";
      for (const line of lines) {
        const trimmed = line.trim();
        if (!trimmed) {
          if (mode === "reasoning") reasoningLines.push("");
          if (mode === "text") outputLines.push("");
          continue;
        }
        if (trimmed === "reasoning") {
          mode = "reasoning";
          continue;
        }
        if (trimmed === "text") {
          mode = "text";
          continue;
        }
        if (trimmed === "step-start" || trimmed === "step-finish" || trimmed === "stop") {
          mode = "";
          continue;
        }
        if (/^(prt|ses|msg)_[a-zA-Z0-9]+$/.test(trimmed)) continue;
        if (/^[a-f0-9]{40}$/i.test(trimmed)) continue;
        if (mode === "reasoning") {
          reasoningLines.push(line);
          continue;
        }
        if (mode === "text") {
          outputLines.push(line);
          continue;
        }
      }
      const parsedText = outputLines.join("\n").trim();
      const parsedReasoning = reasoningLines.join("\n").trim();
      if (!parsedText && !parsedReasoning) {
        return { changed: false, text, reasoning: "" };
      }
      return {
        changed: true,
        text: parsedText || text,
        reasoning: parsedReasoning
      };
    }
    function overlapSuffixPrefix(left, right, maxWindow = 2048) {
      const a = String(left || "");
      const b = String(right || "");
      const max = Math.min(a.length, b.length, Math.max(32, Number(maxWindow) || 2048));
      for (let len = max; len >= 16; len -= 1) {
        if (a.slice(a.length - len) === b.slice(0, len)) return len;
      }
      return 0;
    }
    function mergeSnapshotText(existingText, incomingText) {
      const existing = String(existingText || "");
      const incoming = String(incomingText || "");
      if (!incoming.trim()) return existing;
      if (!existing.trim()) return incoming;
      if (existing === incoming) return existing;
      if (incoming.includes(existing)) return incoming;
      if (existing.includes(incoming)) return existing;
      const existingTrimmed = existing.trim();
      const incomingTrimmed = incoming.trim();
      if (incomingTrimmed.startsWith(existingTrimmed) || incomingTrimmed.endsWith(existingTrimmed)) return incoming;
      if (existingTrimmed.startsWith(incomingTrimmed) || existingTrimmed.endsWith(incomingTrimmed)) return existing;
      const overlap = overlapSuffixPrefix(existing, incoming);
      if (overlap > 0) return `${existing}${incoming.slice(overlap)}`;
      const reverseOverlap = overlapSuffixPrefix(incoming, existing);
      if (reverseOverlap > 0) return `${incoming}${existing.slice(reverseOverlap)}`;
      return incoming.length >= existing.length ? incoming : existing;
    }
    function compactInflatedReasoning(reasoningText) {
      const original = typeof reasoningText === "string" ? reasoningText : "";
      if (!original || original.length < 12e3) {
        return { changed: false, text: original };
      }
      const segments = original.split(/\n{2,}/).map((segment) => segment.trim()).filter(Boolean);
      if (segments.length < 3) {
        return { changed: false, text: original };
      }
      let merged = "";
      for (const segment of segments) {
        merged = mergeSnapshotText(merged, segment);
      }
      const compacted = merged.trim();
      if (!compacted) return { changed: false, text: original };
      if (compacted.length >= Math.floor(original.length * 0.9)) {
        return { changed: false, text: original };
      }
      return { changed: true, text: compacted };
    }
    function clampText(value, maxLen = 1200) {
      const raw = String(value || "");
      const limit = Math.max(64, Number(maxLen) || 1200);
      if (raw.length <= limit) return raw;
      return `${raw.slice(0, limit)}...(${raw.length - limit} chars truncated)`;
    }
    function normalizePatchPath(pathLike) {
      return String(pathLike || "").trim();
    }
    function normalizePatchAction(value) {
      const raw = String(value || "").trim().toLowerCase();
      if (!raw) return "";
      if (["a", "add", "added", "new", "create", "created"].includes(raw)) return "added";
      if (["m", "mod", "modify", "modified", "update", "updated", "change", "changed", "edit", "edited"].includes(raw)) {
        return "modified";
      }
      if (["d", "del", "delete", "deleted", "remove", "removed"].includes(raw)) return "deleted";
      if (["r", "ren", "rename", "renamed", "move", "moved"].includes(raw)) return "renamed";
      if (["c", "copy", "copied"].includes(raw)) return "copied";
      return "";
    }
    function compactPatchFileEntry(entry) {
      if (entry === null || entry === void 0) return null;
      if (typeof entry === "string") {
        const text = normalizePatchPath(entry);
        if (!text) return null;
        return { path: clampText(text, 400) };
      }
      if (typeof entry !== "object") {
        const text = normalizePatchPath(entry);
        return text ? { path: clampText(text, 400) } : null;
      }
      const action = normalizePatchAction(entry.action || entry.status || entry.changeType || entry.op || entry.kind || entry.type);
      const from = normalizePatchPath(entry.from || entry.oldPath || entry.previousPath || entry.source || entry.src || "");
      const to = normalizePatchPath(entry.to || entry.newPath || entry.target || entry.dest || entry.dst || "");
      const path = normalizePatchPath(entry.path || entry.file || entry.filePath || entry.filename || entry.name || to || from || "");
      if (!path && !from && !to) return null;
      const out = {
        path: clampText(path, 400)
      };
      if (action) out.action = action;
      if (from) out.from = clampText(from, 400);
      if (to) out.to = clampText(to, 400);
      return out;
    }
    function compactJsonValue(value, depth = 0) {
      if (value === null || value === void 0) return value;
      if (typeof value === "string") return clampText(value, 800);
      if (typeof value === "number" || typeof value === "boolean") return value;
      if (typeof value !== "object") return String(value);
      if (depth >= 3) {
        if (Array.isArray(value)) return `Array(${value.length})`;
        return "Object(...)";
      }
      if (Array.isArray(value)) {
        const out2 = value.slice(0, 24).map((item) => compactJsonValue(item, depth + 1));
        if (value.length > out2.length) out2.push(`...(${value.length - out2.length} more)`);
        return out2;
      }
      const out = {};
      const keys = Object.keys(value).slice(0, 40);
      for (const key of keys) {
        out[key] = compactJsonValue(value[key], depth + 1);
      }
      if (Object.keys(value).length > keys.length) {
        out.__truncated__ = `${Object.keys(value).length - keys.length} keys omitted`;
      }
      return out;
    }
    function compactBlockRaw(raw, type) {
      if (!raw || typeof raw !== "object") return raw;
      const normalizedType = String(type || raw.type || "").toLowerCase();
      const base = {
        id: typeof raw.id === "string" ? raw.id : "",
        type: normalizedType || (typeof raw.type === "string" ? raw.type : ""),
        messageID: typeof raw.messageID === "string" ? raw.messageID : "",
        sessionID: typeof raw.sessionID === "string" ? raw.sessionID : ""
      };
      const time = raw.time && typeof raw.time === "object" ? raw.time : null;
      if (time) {
        base.time = {
          start: Number(time.start || 0) || void 0,
          end: Number(time.end || 0) || void 0,
          created: Number(time.created || 0) || void 0,
          completed: Number(time.completed || 0) || void 0
        };
      }
      if (normalizedType === "tool") {
        const state = raw.state && typeof raw.state === "object" ? raw.state : {};
        const input = state.input && typeof state.input === "object" ? state.input : {};
        base.tool = typeof raw.tool === "string" ? raw.tool : "";
        base.state = {
          status: typeof state.status === "string" ? state.status : "",
          input: compactJsonValue(input, 0),
          error: clampText(state.error || "", 1200)
        };
        return base;
      }
      if (normalizedType === "patch") {
        base.hash = typeof raw.hash === "string" ? raw.hash : "";
        base.files = Array.isArray(raw.files) ? raw.files.slice(0, 200).map((item) => compactPatchFileEntry(item)).filter(Boolean) : [];
        return base;
      }
      if (normalizedType === "file") {
        base.filename = typeof raw.filename === "string" ? raw.filename : "";
        base.url = typeof raw.url === "string" ? clampText(raw.url, 400) : "";
        base.mime = typeof raw.mime === "string" ? raw.mime : "";
        return base;
      }
      if (normalizedType === "retry") {
        base.attempt = Number.isFinite(raw.attempt) ? Number(raw.attempt) : void 0;
        base.error = clampText(raw.error || "", 1200);
        return base;
      }
      return base;
    }
    function compactMessageBlocks(message) {
      const blocks = Array.isArray(message && message.blocks) ? message.blocks : null;
      if (!blocks || !blocks.length) return { changed: false, blocks };
      let changed = false;
      const nextBlocks = blocks.map((block) => {
        if (!block || typeof block !== "object") return block;
        if (!block.raw || typeof block.raw !== "object") return block;
        let rawSize = 0;
        try {
          rawSize = JSON.stringify(block.raw).length;
        } catch {
          rawSize = 0;
        }
        const type = String(block.type || "").toLowerCase();
        if (rawSize <= 3e3 && type !== "reasoning") return block;
        const compactedRaw = compactBlockRaw(block.raw, type);
        let isSame = false;
        try {
          isSame = JSON.stringify(compactedRaw) === JSON.stringify(block.raw);
        } catch {
          isSame = false;
        }
        if (isSame) return block;
        changed = true;
        return Object.assign({}, block, { raw: compactedRaw });
      });
      return { changed, blocks: nextBlocks };
    }
    function migrateLegacyMessages(runtimeState) {
      const st = runtimeState && typeof runtimeState === "object" ? runtimeState : {};
      if (!st.messagesBySession || typeof st.messagesBySession !== "object") return st;
      for (const [sessionId, list] of Object.entries(st.messagesBySession)) {
        if (!Array.isArray(list)) continue;
        const normalizedList = list.length > 200 ? list.slice(-200) : list;
        st.messagesBySession[sessionId] = normalizedList.map((message) => {
          if (!message || typeof message !== "object") return message;
          if (message.role !== "assistant") return message;
          let next = message;
          let changed = false;
          const cleaned = splitLegacyMixedAssistantText(next.text || "");
          if (cleaned.changed) {
            next = Object.assign({}, next, {
              text: cleaned.text,
              reasoning: cleaned.reasoning
            });
            changed = true;
          }
          const compactedReasoning = compactInflatedReasoning(next.reasoning || "");
          if (compactedReasoning.changed) {
            if (!changed) next = Object.assign({}, next);
            next.reasoning = compactedReasoning.text;
            changed = true;
          }
          const compactedBlocks = compactMessageBlocks(next);
          if (compactedBlocks.changed) {
            if (!changed) next = Object.assign({}, next);
            next.blocks = compactedBlocks.blocks;
            changed = true;
          }
          return changed ? next : message;
        });
      }
      return st;
    }
    module2.exports = {
      migrateLegacyMessages
    };
  }
});

// runtime/plugin/module-loader-methods.js
var require_module_loader_methods = __commonJS({
  "runtime/plugin/module-loader-methods.js"(exports2, module2) {
    function createModuleLoaderMethods(options = {}) {
      const defaultViewType = String(options.defaultViewType || "flownote-view");
      return {
        getViewType() {
          const type = this.runtimeModules && typeof this.runtimeModules.VIEW_TYPE === "string" ? this.runtimeModules.VIEW_TYPE : "";
          return type || defaultViewType;
        },
        ensureRuntimeModules() {
          if (this.runtimeModules) return this.runtimeModules;
          const view = require_flownote_view();
          const client = require_flownote_client();
          const sdkTransport = require_sdk_transport();
          const sessionStore = require_session_store();
          const diagnosticsService = require_diagnostics_service();
          const settingsTab = require_settings_tab();
          const executableResolver = require_executable_resolver();
          const skillService = require_skill_service();
          const settingsUtils = require_settings_utils();
          const stateMigrations = require_state_migrations();
          this.runtimeModules = {
            VIEW_TYPE: String(view && view.VIEW_TYPE ? view.VIEW_TYPE : defaultViewType),
            FLOWnoteAssistantView: view && view.FLOWnoteAssistantView ? view.FLOWnoteAssistantView : null,
            FLOWnoteClient: client && client.FLOWnoteClient ? client.FLOWnoteClient : null,
            SdkTransport: sdkTransport && sdkTransport.SdkTransport ? sdkTransport.SdkTransport : null,
            SessionStore: sessionStore && sessionStore.SessionStore ? sessionStore.SessionStore : null,
            DiagnosticsService: diagnosticsService && diagnosticsService.DiagnosticsService ? diagnosticsService.DiagnosticsService : null,
            FLOWnoteSettingsTab: settingsTab && settingsTab.FLOWnoteSettingsTab ? settingsTab.FLOWnoteSettingsTab : null,
            ExecutableResolver: executableResolver && executableResolver.ExecutableResolver ? executableResolver.ExecutableResolver : null,
            SkillService: skillService && skillService.SkillService ? skillService.SkillService : null,
            copyDirectoryRecursive: skillService && skillService.copyDirectoryRecursive ? skillService.copyDirectoryRecursive : null,
            normalizeSettings: settingsUtils && settingsUtils.normalizeSettings ? settingsUtils.normalizeSettings : (raw) => raw || {},
            migrateLegacyMessages: stateMigrations && stateMigrations.migrateLegacyMessages ? stateMigrations.migrateLegacyMessages : (raw) => raw || {}
          };
          const requiredCtors = [
            ["FLOWnoteAssistantView", this.runtimeModules.FLOWnoteAssistantView],
            ["FLOWnoteClient", this.runtimeModules.FLOWnoteClient],
            ["SdkTransport", this.runtimeModules.SdkTransport],
            ["SessionStore", this.runtimeModules.SessionStore],
            ["DiagnosticsService", this.runtimeModules.DiagnosticsService],
            ["FLOWnoteSettingsTab", this.runtimeModules.FLOWnoteSettingsTab],
            ["ExecutableResolver", this.runtimeModules.ExecutableResolver],
            ["SkillService", this.runtimeModules.SkillService],
            ["copyDirectoryRecursive", this.runtimeModules.copyDirectoryRecursive]
          ].filter(([, value]) => !value);
          if (requiredCtors.length) {
            throw new Error(`runtime \u6A21\u5757\u4E0D\u5B8C\u6574: ${requiredCtors.map(([name]) => name).join(", ")}`);
          }
          return this.runtimeModules;
        }
      };
    }
    module2.exports = { createModuleLoaderMethods };
  }
});

// runtime/plugin/runtime-state-methods.js
var require_runtime_state_methods = __commonJS({
  "runtime/plugin/runtime-state-methods.js"(exports2, module2) {
    var runtimeStateMethods = {
      ensureRuntimeStateShape() {
        if (!this.runtimeState || typeof this.runtimeState !== "object") {
          this.runtimeState = {};
        }
        if (!Array.isArray(this.runtimeState.deletedSessionIds)) this.runtimeState.deletedSessionIds = [];
        if (this.runtimeState.modelCatalogCache !== null && this.runtimeState.modelCatalogCache !== void 0 && typeof this.runtimeState.modelCatalogCache !== "object") {
          this.runtimeState.modelCatalogCache = null;
        }
        if (this.runtimeState.modelCatalogCache === void 0) this.runtimeState.modelCatalogCache = null;
        if (!this.runtimeState.migrationFlags || typeof this.runtimeState.migrationFlags !== "object") {
          this.runtimeState.migrationFlags = {};
        }
        this.runtimeState.lastLaunchProfile = this.normalizeLaunchProfile(this.runtimeState.lastLaunchProfile);
      },
      normalizeLaunchProfile(profile) {
        if (!profile || typeof profile !== "object") return null;
        const mode = String(profile.mode || "").trim().toLowerCase() === "native" ? "native" : "";
        const command = String(profile.command || "").trim();
        const shell = Boolean(profile.shell);
        const args = Array.isArray(profile.args) ? profile.args.map((item) => String(item || "")) : [];
        if (mode !== "native" || !command) return null;
        return {
          mode,
          command,
          args,
          shell,
          distro: "",
          at: Number(profile.at || Date.now())
        };
      },
      getPreferredLaunchProfile() {
        if (String(this.settings && this.settings.launchStrategy ? this.settings.launchStrategy : "auto") !== "auto") {
          return null;
        }
        this.ensureRuntimeStateShape();
        return this.normalizeLaunchProfile(this.runtimeState.lastLaunchProfile);
      },
      rememberLaunchProfile(profile) {
        const normalized = this.normalizeLaunchProfile(profile);
        if (!normalized) return;
        this.ensureRuntimeStateShape();
        const current = this.normalizeLaunchProfile(this.runtimeState.lastLaunchProfile);
        if (current && current.mode === normalized.mode && current.command === normalized.command && JSON.stringify(current.args || []) === JSON.stringify(normalized.args || []) && Boolean(current.shell) === Boolean(normalized.shell) && current.distro === normalized.distro) {
          return;
        }
        this.runtimeState.lastLaunchProfile = {
          ...normalized,
          at: Date.now()
        };
        void this.persistState().catch((e) => {
          this.log(`persist launch profile failed: ${e instanceof Error ? e.message : String(e)}`);
        });
      },
      async clearRememberedLaunchProfile() {
        this.ensureRuntimeStateShape();
        if (!this.runtimeState.lastLaunchProfile) return;
        this.runtimeState.lastLaunchProfile = null;
        await this.persistState();
      }
    };
    module2.exports = { runtimeStateMethods };
  }
});

// runtime/plugin/model-catalog-methods.js
var require_model_catalog_methods = __commonJS({
  "runtime/plugin/model-catalog-methods.js"(exports2, module2) {
    var modelCatalogMethods = {
      normalizeModelID(modelID) {
        return String(modelID || "").trim();
      },
      extractProviderFromModelID(modelID) {
        const normalized = this.normalizeModelID(modelID).toLowerCase();
        if (!normalized) return "";
        const slashIndex = normalized.indexOf("/");
        if (slashIndex <= 0) return "";
        return normalized.slice(0, slashIndex).trim();
      },
      isFreeModelID(modelID) {
        const normalized = this.normalizeModelID(modelID).toLowerCase();
        if (!normalized) return false;
        const modelName = normalized.includes("/") ? normalized.slice(normalized.indexOf("/") + 1) : normalized;
        return /(?:^|[-_:.\/])free$/.test(modelName);
      },
      ensureModelCatalogState() {
        if (!this.modelCatalog || typeof this.modelCatalog !== "object") {
          this.modelCatalog = {};
        }
        if (!Array.isArray(this.modelCatalog.allModels)) this.modelCatalog.allModels = [];
        if (!Array.isArray(this.modelCatalog.visibleModels)) this.modelCatalog.visibleModels = [];
        if (!(this.modelCatalog.unavailable instanceof Map)) this.modelCatalog.unavailable = /* @__PURE__ */ new Map();
        if (!(this.modelCatalog.connectedProviders instanceof Set) && this.modelCatalog.connectedProviders !== null) {
          this.modelCatalog.connectedProviders = null;
        }
        if (this.modelCatalog.connectedProviders === void 0) this.modelCatalog.connectedProviders = null;
        return this.modelCatalog;
      },
      getUnavailableModelSet() {
        const state = this.ensureModelCatalogState();
        const out = /* @__PURE__ */ new Set();
        for (const [modelKey, meta] of state.unavailable.entries()) {
          if (!modelKey) continue;
          if (!meta || meta.hidden !== false) out.add(String(modelKey));
        }
        return out;
      },
      isHardUnsupportedModelError(message) {
        const text = String(message || "").toLowerCase();
        if (!text) return false;
        return /unsupported model|no such model|model .* not found|invalid model|unknown model|does not support model/.test(text) || /模型不支持|不支持该模型|模型不可用|模型不存在|无可用模型/.test(text) || /unauthorized|forbidden|permission denied|auth.*failed|鉴权失败|无权限/.test(text) || /(status=|http |请求失败 \()4(01|03|04|22)\b/.test(text) || /长时间无响应.*不受当前账号\/api key 支持/.test(text);
      },
      shouldTrackModelFailure(message) {
        const text = String(message || "").toLowerCase();
        if (!text) return false;
        if (this.isHardUnsupportedModelError(text)) return true;
        return /timeout|timed out|超时|无响应|no response|session\.status=busy|session\.status=idle/.test(text);
      },
      markModelUnavailable(modelID, reason) {
        const normalized = this.normalizeModelID(modelID);
        if (!normalized) return { hidden: false, attempts: 0, model: "" };
        if (!this.shouldTrackModelFailure(reason)) return { hidden: false, attempts: 0, model: normalized };
        const state = this.ensureModelCatalogState();
        const key = normalized.toLowerCase();
        const previous = state.unavailable.get(key) || {};
        const attempts = Math.max(0, Number(previous.attempts || 0)) + 1;
        const hidden = this.isHardUnsupportedModelError(reason) || attempts >= 2;
        state.unavailable.set(key, {
          model: normalized,
          attempts,
          hidden,
          reason: String(reason || ""),
          updatedAt: Date.now()
        });
        if (hidden) this.rebuildVisibleModelCache();
        return { hidden, attempts, model: normalized };
      },
      clearUnavailableModels(options = {}) {
        const state = this.ensureModelCatalogState();
        const providerID = this.extractProviderFromModelID(options.providerID || "");
        if (!providerID) {
          state.unavailable.clear();
          this.rebuildVisibleModelCache();
          return;
        }
        for (const [modelKey, meta] of state.unavailable.entries()) {
          const targetProvider = this.extractProviderFromModelID(meta && meta.model ? meta.model : modelKey);
          if (targetProvider === providerID) state.unavailable.delete(modelKey);
        }
        this.rebuildVisibleModelCache();
      },
      normalizeConnectedProviders(connectedProviders) {
        let out = null;
        if (connectedProviders instanceof Set) {
          out = new Set(
            [...connectedProviders].map((id) => String(id || "").trim().toLowerCase()).filter(Boolean)
          );
        } else if (Array.isArray(connectedProviders)) {
          out = new Set(
            connectedProviders.map((id) => String(id || "").trim().toLowerCase()).filter(Boolean)
          );
        }
        return out;
      },
      filterModelList(models, connectedProviders) {
        const uniq = [...new Set(
          (Array.isArray(models) ? models : []).map((model) => this.normalizeModelID(model)).filter(Boolean)
        )];
        uniq.sort((a, b) => a.localeCompare(b));
        const unavailable = this.getUnavailableModelSet();
        const hasConnectedFilter = connectedProviders instanceof Set;
        return uniq.filter((modelID) => {
          const key = modelID.toLowerCase();
          if (unavailable.has(key)) return false;
          if (this.isFreeModelID(modelID)) return true;
          if (!hasConnectedFilter) return true;
          const providerID = this.extractProviderFromModelID(modelID);
          if (!providerID) return true;
          return connectedProviders.has(providerID);
        });
      },
      rebuildVisibleModelCache() {
        const state = this.ensureModelCatalogState();
        const visible = this.filterModelList(state.allModels, state.connectedProviders);
        state.visibleModels = visible;
        this.cachedModels = visible;
        return visible;
      },
      normalizeModelCachePayload(payload) {
        const raw = payload && typeof payload === "object" ? payload : {};
        const models = [...new Set(
          (Array.isArray(raw.models) ? raw.models : []).map((model) => this.normalizeModelID(model)).filter(Boolean)
        )];
        models.sort((a, b) => a.localeCompare(b));
        const connectedProviders = [...new Set(
          (Array.isArray(raw.connectedProviders) ? raw.connectedProviders : []).map((id) => String(id || "").trim().toLowerCase()).filter(Boolean)
        )];
        return {
          models,
          connectedProviders,
          updatedAt: Number(raw.updatedAt || 0)
        };
      },
      ensureRuntimeStateModelCacheShape() {
        if (!this.runtimeState || typeof this.runtimeState !== "object") {
          this.runtimeState = {};
        }
        const cache = this.runtimeState.modelCatalogCache;
        if (!cache || typeof cache !== "object") {
          this.runtimeState.modelCatalogCache = null;
          return null;
        }
        return cache;
      },
      readModelCacheFromRuntimeState() {
        const cache = this.ensureRuntimeStateModelCacheShape();
        if (!cache) return null;
        return this.normalizeModelCachePayload(cache);
      },
      writeModelCacheToRuntimeState(models, connectedProviders = []) {
        const normalizedModels = [...new Set(
          (Array.isArray(models) ? models : []).map((model) => this.normalizeModelID(model)).filter(Boolean)
        )];
        if (!normalizedModels.length) return false;
        normalizedModels.sort((a, b) => a.localeCompare(b));
        const normalizedProviders = [...new Set(
          (Array.isArray(connectedProviders) ? connectedProviders : []).map((id) => String(id || "").trim().toLowerCase()).filter(Boolean)
        )];
        normalizedProviders.sort((a, b) => a.localeCompare(b));
        const payload = {
          updatedAt: Date.now(),
          models: normalizedModels,
          connectedProviders: normalizedProviders
        };
        this.ensureRuntimeStateModelCacheShape();
        this.runtimeState.modelCatalogCache = payload;
        return true;
      },
      readModelCacheFromTerminalOutput() {
        return this.readModelCacheFromRuntimeState();
      },
      writeModelCacheToTerminalOutput(models, connectedProviders = []) {
        return this.writeModelCacheToRuntimeState(models, connectedProviders);
      },
      loadModelCatalogFromTerminalOutput() {
        const cache = this.readModelCacheFromRuntimeState();
        if (!cache || !Array.isArray(cache.models) || !cache.models.length) return [];
        const state = this.ensureModelCatalogState();
        state.allModels = cache.models.slice();
        const connected = Array.isArray(cache.connectedProviders) && cache.connectedProviders.length ? cache.connectedProviders : null;
        state.connectedProviders = this.normalizeConnectedProviders(connected);
        return this.rebuildVisibleModelCache();
      },
      notifyModelCatalogUpdated() {
        const view = this.getAssistantView();
        if (!view) return;
        try {
          if (typeof view.updateModelSelectOptions === "function") {
            view.updateModelSelectOptions();
            return;
          }
          if (typeof view.render === "function") view.render();
        } catch (e) {
          this.log(`notify model catalog failed: ${e instanceof Error ? e.message : String(e)}`);
        }
      },
      async refreshModelCatalog(options = {}) {
        const state = this.ensureModelCatalogState();
        const cfg = options && typeof options === "object" ? options : {};
        if (cfg.resetUnavailable) this.clearUnavailableModels();
        let incomingModels = Array.isArray(cfg.models) ? cfg.models : null;
        if (!incomingModels) {
          try {
            incomingModels = await this.opencodeClient.listModels();
          } catch (e) {
            this.log(`refresh models failed: ${e instanceof Error ? e.message : String(e)}`);
          }
        }
        if (Array.isArray(incomingModels)) {
          state.allModels = incomingModels.map((model) => this.normalizeModelID(model)).filter(Boolean);
        }
        let connectedProviders = null;
        if (cfg.connectedProviders instanceof Set || Array.isArray(cfg.connectedProviders)) {
          connectedProviders = cfg.connectedProviders;
        } else if (cfg.providerResult && Array.isArray(cfg.providerResult.connected)) {
          connectedProviders = cfg.providerResult.connected;
        } else {
          try {
            const providerResult = await this.opencodeClient.listProviders();
            if (providerResult && Array.isArray(providerResult.connected)) {
              connectedProviders = providerResult.connected;
            }
          } catch (e) {
            this.log(`refresh providers failed: ${e instanceof Error ? e.message : String(e)}`);
          }
        }
        state.connectedProviders = this.normalizeConnectedProviders(connectedProviders);
        const visible = this.rebuildVisibleModelCache();
        const currentDefaultModel = this.normalizeModelID(this.settings && this.settings.defaultModel ? this.settings.defaultModel : "");
        if (currentDefaultModel && !visible.includes(currentDefaultModel)) {
          this.settings.defaultModel = "";
        }
        if (cfg.cacheToTerminalOutput !== false && Array.isArray(state.allModels) && state.allModels.length) {
          const connectedForCache = state.connectedProviders instanceof Set ? [...state.connectedProviders] : [];
          this.writeModelCacheToRuntimeState(state.allModels, connectedForCache);
        }
        if (cfg.notifyView !== false) this.notifyModelCatalogUpdated();
        return visible;
      }
    };
    module2.exports = { modelCatalogMethods };
  }
});

// runtime/plugin/bundled-skills-utils.js
var require_bundled_skills_utils = __commonJS({
  "runtime/plugin/bundled-skills-utils.js"(exports2, module2) {
    var fs = require("fs");
    var path = require("path");
    var TEMPLATE_MAP_FILE = "template-map.json";
    var DEFAULT_META_TEMPLATES_DIR = path.join("Meta", "\u6A21\u677F");
    var DEFAULT_BACKUP_ROOT = path.join(".opencode", "backups", "bundled-content");
    var CANCELLED_ERROR_CODE = "BUNDLED_CONTENT_SYNC_CANCELLED";
    function walkFilesRecursive(rootDir, currentDir = rootDir, output = []) {
      if (!rootDir || !currentDir || !fs.existsSync(currentDir)) return output;
      const entries = fs.readdirSync(currentDir, { withFileTypes: true });
      for (const entry of entries) {
        if (!entry || String(entry.name || "").startsWith(".")) continue;
        const absPath = path.join(currentDir, entry.name);
        if (entry.isDirectory()) {
          walkFilesRecursive(rootDir, absPath, output);
          continue;
        }
        if (!entry.isFile()) continue;
        output.push(path.relative(rootDir, absPath));
      }
      return output;
    }
    function normalizeSafeRelativePath(value) {
      const normalized = String(value || "").replace(/\\/g, "/").replace(/^\/+/, "").trim();
      if (!normalized) return "";
      if (path.isAbsolute(normalized)) return "";
      if (normalized.split("/").some((segment) => segment === ".." || segment === "")) return "";
      return normalized;
    }
    function dedupeNormalizedPaths(paths = []) {
      const seen = /* @__PURE__ */ new Set();
      const output = [];
      for (const value of paths) {
        const normalized = normalizeSafeRelativePath(value);
        if (!normalized || seen.has(normalized)) continue;
        seen.add(normalized);
        output.push(normalized);
      }
      return output;
    }
    function normalizeTemplateLocaleKey(value) {
      const raw = String(value || "").trim();
      if (!raw) return "";
      const lowered = raw.toLowerCase();
      if (lowered === "zh-cn" || lowered.startsWith("zh")) return "zh-CN";
      if (lowered === "en" || lowered.startsWith("en")) return "en";
      return "";
    }
    function normalizeTemplateLocaleVariant(variant) {
      if (!variant || typeof variant !== "object") return null;
      const metaSource = normalizeSafeRelativePath(variant.metaSource);
      const fallbackPath = normalizeSafeRelativePath(variant.fallback);
      const targets = dedupeNormalizedPaths(Array.isArray(variant.targets) ? variant.targets : []);
      if (!metaSource && !fallbackPath && !targets.length) return null;
      return {
        ...metaSource ? { metaSource } : {},
        ...fallbackPath ? { fallback: fallbackPath } : {},
        ...targets.length ? { targets } : {}
      };
    }
    function toFileBuffer(value) {
      try {
        return fs.readFileSync(value);
      } catch {
        return null;
      }
    }
    function filesHaveSameContent(fileA, fileB) {
      const bufA = toFileBuffer(fileA);
      const bufB = toFileBuffer(fileB);
      if (!bufA || !bufB) return false;
      return Buffer.compare(bufA, bufB) === 0;
    }
    function isTemplateContentValid(filePath) {
      try {
        const text = fs.readFileSync(filePath, "utf8");
        return String(text || "").trim().length > 0;
      } catch {
        return false;
      }
    }
    function createCancelledError(stage = "unknown") {
      const err = new Error("bundled content sync cancelled");
      err.code = CANCELLED_ERROR_CODE;
      err.stage = stage;
      return err;
    }
    function isCancelledError(err) {
      return Boolean(err && typeof err === "object" && err.code === CANCELLED_ERROR_CODE);
    }
    function copyFileWithParent(srcFile, destFile) {
      fs.mkdirSync(path.dirname(destFile), { recursive: true });
      fs.copyFileSync(srcFile, destFile);
    }
    function cloneExistingPath(srcPath, backupPath) {
      if (!fs.existsSync(srcPath)) return false;
      fs.mkdirSync(path.dirname(backupPath), { recursive: true });
      const stat = fs.statSync(srcPath);
      if (stat.isDirectory()) {
        fs.cpSync(srcPath, backupPath, { recursive: true });
      } else {
        fs.copyFileSync(srcPath, backupPath);
      }
      return true;
    }
    module2.exports = {
      TEMPLATE_MAP_FILE,
      DEFAULT_META_TEMPLATES_DIR,
      DEFAULT_BACKUP_ROOT,
      CANCELLED_ERROR_CODE,
      walkFilesRecursive,
      normalizeSafeRelativePath,
      dedupeNormalizedPaths,
      normalizeTemplateLocaleKey,
      normalizeTemplateLocaleVariant,
      filesHaveSameContent,
      isTemplateContentValid,
      createCancelledError,
      isCancelledError,
      copyFileWithParent,
      cloneExistingPath
    };
  }
});

// runtime/plugin/bundled-skills-flow-methods.js
var require_bundled_skills_flow_methods = __commonJS({
  "runtime/plugin/bundled-skills-flow-methods.js"(exports2, module2) {
    var fs = require("fs");
    var path = require("path");
    var {
      DEFAULT_META_TEMPLATES_DIR,
      createCancelledError,
      isCancelledError
    } = require_bundled_skills_utils();
    async function resolveBundledSyncAction(conflict, options = {}, state = {}) {
      if (state.globalAction === "replace") return "replace";
      if (state.globalAction === "skip") return "skip";
      const defaultAction = ["replace", "skip"].includes(String(options.defaultConflictAction || "")) ? String(options.defaultConflictAction) : "skip";
      const resolver = options && typeof options.resolveConflict === "function" ? options.resolveConflict : null;
      if (!resolver) return defaultAction;
      let choice = null;
      try {
        choice = await resolver(conflict);
      } catch {
        choice = null;
      }
      const normalized = String(choice || "").trim().toLowerCase();
      if (normalized === "replace_all") {
        state.globalAction = "replace";
        return "replace";
      }
      if (normalized === "skip_all") {
        state.globalAction = "skip";
        return "skip";
      }
      if (normalized === "replace") return "replace";
      if (normalized === "skip") return "skip";
      if (normalized === "cancel") throw createCancelledError(conflict && conflict.kind ? conflict.kind : "unknown");
      return defaultAction;
    }
    async function syncBundledSkills(vaultPath, options = {}) {
      const runtime = this.ensureRuntimeModules();
      const force = Boolean(options && options.force);
      const skillLocale = this.resolveBundledSkillLocale(options.locale);
      const bundledRoot = this.getBundledSkillsRoot();
      const bundledIds = this.listBundledSkillIds(bundledRoot);
      const signature = this.getBundledSkillsContentSignature(bundledRoot, bundledIds);
      const stamp = `${this.getBundledSkillsStamp(bundledIds, signature)}:locale=${skillLocale}`;
      if (this.skillService && typeof this.skillService.setAllowedSkillIds === "function") {
        this.skillService.setAllowedSkillIds(null);
      }
      if (!bundledIds.length) {
        return {
          synced: 0,
          total: 0,
          skippedCount: 0,
          replacedCount: 0,
          conflictCount: 0,
          backupCount: 0,
          targetRoot: path.join(vaultPath, this.settings.skillsDir),
          bundledRoot,
          locale: skillLocale,
          stamp,
          skipped: false,
          stampUpdated: false,
          cancelled: false,
          backupRoot: "",
          errors: [`\u672A\u627E\u5230\u5185\u7F6E skills \u6E90\u76EE\u5F55\u6216\u76EE\u5F55\u4E3A\u7A7A\uFF1A${bundledRoot}`]
        };
      }
      const targetRoot = path.join(vaultPath, this.settings.skillsDir);
      fs.mkdirSync(targetRoot, { recursive: true });
      const shouldSync = this.shouldSyncBundledSkills(targetRoot, bundledIds, stamp, force);
      if (!shouldSync) {
        return {
          synced: 0,
          total: bundledIds.length,
          skippedCount: 0,
          replacedCount: 0,
          conflictCount: 0,
          backupCount: 0,
          targetRoot,
          bundledRoot,
          locale: skillLocale,
          stamp,
          skipped: true,
          stampUpdated: false,
          cancelled: false,
          backupRoot: "",
          errors: []
        };
      }
      const errors = [];
      const conflicts = [];
      const conflictState = {};
      let synced = 0;
      let replacedCount = 0;
      let skippedCount = 0;
      let backupCount = 0;
      let backupRoot = "";
      try {
        for (const skillId of bundledIds) {
          const srcDir = path.join(bundledRoot, skillId);
          const destDir = path.join(targetRoot, skillId);
          let action = "replace";
          if (fs.existsSync(destDir)) {
            action = await this.resolveBundledSyncAction({
              kind: "skill",
              id: skillId,
              sourcePath: srcDir,
              targetPath: destDir
            }, options, conflictState);
            conflicts.push({ kind: "skill", id: skillId, action, targetPath: destDir });
          }
          if (action === "skip") {
            skippedCount += 1;
            continue;
          }
          try {
            if (fs.existsSync(destDir)) {
              if (!backupRoot) backupRoot = this.getBundledContentBackupRoot(vaultPath, options);
              const backed = this.backupPathIfNeeded(destDir, backupRoot, path.join("skills", skillId));
              if (backed) backupCount += 1;
              fs.rmSync(destDir, { recursive: true, force: true });
              replacedCount += 1;
            }
            runtime.copyDirectoryRecursive(srcDir, destDir);
            const localizedDoc = this.applyBundledSkillLocaleDoc(srcDir, destDir, skillLocale);
            if (!localizedDoc.ok) {
              errors.push(`${skillId}: ${localizedDoc.error}`);
              continue;
            }
            this.applyBundledSkillLocaleResources(srcDir, destDir, skillLocale);
            this.removeLocalizedMarkdownVariants(destDir);
            synced += 1;
          } catch (e) {
            errors.push(`${skillId}: ${e instanceof Error ? e.message : String(e)}`);
          }
        }
      } catch (e) {
        if (isCancelledError(e)) {
          return {
            synced,
            total: bundledIds.length,
            skippedCount,
            replacedCount,
            conflictCount: conflicts.length,
            backupCount,
            targetRoot,
            bundledRoot,
            locale: skillLocale,
            stamp,
            skipped: false,
            stampUpdated: false,
            cancelled: true,
            cancelledStage: e.stage,
            backupRoot,
            conflicts,
            errors
          };
        }
        errors.push(e instanceof Error ? e.message : String(e));
      }
      const hasRuntimeState = this.runtimeState && typeof this.runtimeState === "object";
      let stampUpdated = false;
      if (!errors.length && hasRuntimeState) {
        this.runtimeState.bundledSkillsStamp = stamp;
        stampUpdated = true;
      }
      return {
        synced,
        total: bundledIds.length,
        skippedCount,
        replacedCount,
        conflictCount: conflicts.length,
        backupCount,
        targetRoot,
        bundledRoot,
        locale: skillLocale,
        stamp,
        skipped: false,
        stampUpdated,
        cancelled: false,
        backupRoot,
        conflicts,
        errors
      };
    }
    async function syncBundledContent(vaultPath, options = {}) {
      const force = Boolean(options && options.force);
      const syncTemplates = options && Object.prototype.hasOwnProperty.call(options, "syncTemplates") ? Boolean(options.syncTemplates) : true;
      const skillResult = await this.syncBundledSkills(vaultPath, options);
      if (skillResult.cancelled) {
        return {
          synced: Number(skillResult.synced || 0),
          total: Number(skillResult.total || 0),
          targetRoot: skillResult.targetRoot,
          bundledRoot: skillResult.bundledRoot,
          locale: skillResult.locale,
          stamp: skillResult.stamp,
          stampUpdated: false,
          skipped: false,
          cancelled: true,
          cancelledStage: skillResult.cancelledStage || "skill",
          backupRoot: skillResult.backupRoot || "",
          skills: skillResult,
          templates: null,
          errors: Array.isArray(skillResult.errors) ? skillResult.errors : []
        };
      }
      const templateOptions = {
        ...options,
        force,
        targetRoot: skillResult.targetRoot || path.join(vaultPath, this.settings.skillsDir)
      };
      const templateResult = syncTemplates ? await this.syncBundledTemplates(vaultPath, templateOptions) : {
        synced: 0,
        total: 0,
        skippedCount: 0,
        replacedCount: 0,
        conflictCount: 0,
        backupCount: 0,
        fallbackUsed: [],
        missingMeta: [],
        skipped: true,
        cancelled: false,
        targetRoot: templateOptions.targetRoot,
        bundledRoot: this.getBundledSkillsRoot(),
        mapPath: this.getBundledTemplateMapPath(),
        metaRoot: path.join(vaultPath, DEFAULT_META_TEMPLATES_DIR),
        backupRoot: "",
        errors: []
      };
      const cancelled = Boolean(templateResult && templateResult.cancelled);
      const errors = [
        ...Array.isArray(skillResult.errors) ? skillResult.errors : [],
        ...Array.isArray(templateResult.errors) ? templateResult.errors : []
      ];
      return {
        synced: Number(skillResult.synced || 0),
        total: Number(skillResult.total || 0),
        syncedTemplates: Number(templateResult.synced || 0),
        totalTemplates: Number(templateResult.total || 0),
        targetRoot: skillResult.targetRoot,
        bundledRoot: skillResult.bundledRoot,
        locale: skillResult.locale,
        templateMapPath: templateResult.mapPath,
        stamp: skillResult.stamp,
        stampUpdated: Boolean(skillResult.stampUpdated),
        skipped: Boolean(skillResult.skipped) && Boolean(templateResult.skipped),
        cancelled,
        cancelledStage: cancelled ? templateResult.cancelledStage || "template" : "",
        backupRoot: skillResult.backupRoot || templateResult.backupRoot || "",
        skills: skillResult,
        templates: templateResult,
        errors
      };
    }
    module2.exports = {
      resolveBundledSyncAction,
      syncBundledSkills,
      syncBundledContent
    };
  }
});

// runtime/generated/bundled-skills-embedded.js
var require_bundled_skills_embedded = __commonJS({
  "runtime/generated/bundled-skills-embedded.js"(exports2, module2) {
    "use strict";
    var EMBEDDED_BUNDLED_SKILLS_VERSION = "c2cf94c3188269a6abec84148d24ee8e6a1eecb4:99";
    var EMBEDDED_BUNDLED_SKILLS_FILE_COUNT = 99;
    var EMBEDDED_BUNDLED_SKILLS_FILES = Object.freeze({ "ah/SKILL.md": "", "ah-archive/references/path-rules.md": "", "ah-archive/references/workflow-details.md": "", "ah-archive/SKILL.md": "", "ah-capture/references/response-templates.md": "", "ah-capture/references/routing-and-edge-cases.md": "", "ah-capture/references/write-update-rules.md": "", "ah-capture/SKILL.md": "", "ah-card/references/card-template.md": "", "ah-card/references/workflow-and-sync-rules.md": "", "ah-card/references/zettelkasten-principles.md": "", "ah-card/references/\u94FE\u63A5\u89C4\u8303.md": "", "ah-card/SKILL.md": "", "ah-inbox/assets/\u6C38\u4E45\u7B14\u8BB0\u6A21\u677F.md": "", "ah-inbox/references/output-templates.md": "", "ah-inbox/references/workflow-and-routing-rules.md": "", "ah-inbox/SKILL.md": "", "ah-init/references/HOME\u6A21\u677F.md": "", "ah-init/references/kb-manifest\u6A21\u677F.md": "", "ah-init/references/\u4E3B\u9898\u7B14\u8BB0\u6A21\u677F.md": "", "ah-init/references/\u6C38\u4E45\u7B14\u8BB0\u8FC1\u79FB\u683C\u5F0F.md": "", "ah-init/references/\u8BB0\u5FC6\u7CFB\u7EDF\u521D\u59CB\u5316\u6A21\u677F.md": "", "ah-init/references/\u9886\u57DF\u9875\u6A21\u677F.md": "", "ah-init/SKILL.md": "", "ah-legacy/references/\u72B6\u6001\u66F4\u65B0\u89C4\u8303.md": "", "ah-legacy/SKILL.md": "", "ah-memory/references/skill-interface-spec.md": "", "ah-memory/references/status-and-buffer-rules.md": "", "ah-memory/references/task-file-templates.md": "", "ah-memory/SKILL.md": "", "ah-month/references/workflow-and-planning-rules.md": "", "ah-month/references/\u60F3\u6CD5\u6807\u8BB0\u89C4\u8303.md": "", "ah-month/references/\u6570\u636E\u6C47\u603B\u6A21\u677F.md": "", "ah-month/references/\u6708\u62A5\u6A21\u677F.md": "", "ah-month/SKILL.md": "", "ah-note/assets/\u6BCF\u65E5\u7B14\u8BB0\u6A21\u677F.md": "", "ah-note/references/workflow-and-context-rules.md": "", "ah-note/references/\u56DE\u987E\u63D0\u9192\u89C4\u5219.md": "", "ah-note/SKILL.md": "", "ah-project/assets/\u9879\u76EE\u6A21\u677F.md": "", "ah-project/references/output-templates.md": "", "ah-project/references/workflow-and-domain-rules.md": "", "ah-project/SKILL.md": "", "ah-read/assets/extract_weread_outline.py": "", "ah-read/assets/\u8FDB\u5EA6\u6A21\u677F.md": "", "ah-read/references/note-template.md": "", "ah-read/references/wechat-export-outline-workflow.md": "", "ah-read/references/\u6587\u732E\u7B14\u8BB0\u6A21\u677F.md": "", "ah-read/references/\u68C0\u67E5\u6E05\u5355.md": "", "ah-read/SKILL.md": "", "ah-review/references/workflow-and-handoff-rules.md": "", "ah-review/references/\u5BF9\u8BDD\u793A\u4F8B.md": "", "ah-review/references/\u60F3\u6CD5\u6807\u8BB0\u89C4\u8303.md": "", "ah-review/SKILL.md": "", "ah-think/references/active-recall.md": "", "ah-think/references/argument-mapping.md": "", "ah-think/references/atomicity.md": "", "ah-think/references/bloom-taxonomy.md": "", "ah-think/references/causal-loop.md": "", "ah-think/references/circle-of-competence.md": "", "ah-think/references/cornell-question-column.md": "", "ah-think/references/counterexample-thinking.md": "", "ah-think/references/evidence-grading.md": "", "ah-think/references/expected-value.md": "", "ah-think/references/feynman-technique.md": "", "ah-think/references/first-principles.md": "", "ah-think/references/five-whys.md": "", "ah-think/references/iceberg-model.md": "", "ah-think/references/interleaving.md": "", "ah-think/references/inversion.md": "", "ah-think/references/leverage-points.md": "", "ah-think/references/mece.md": "", "ah-think/references/model-library.md": "", "ah-think/references/ooda-loop.md": "", "ah-think/references/opportunity-cost.md": "", "ah-think/references/pre-mortem.md": "", "ah-think/references/progressive-summarization.md": "", "ah-think/references/second-order-thinking.md": "", "ah-think/references/socratic-questioning.md": "", "ah-think/references/spaced-repetition.md": "", "ah-think/references/sq3r.md": "", "ah-think/references/steelman.md": "", "ah-think/references/zettelkasten-linking.md": "", "ah-think/SKILL.md": "", "ah-week/references/\u5468\u62A5\u6A21\u677F.md": "", "ah-week/references/\u60F3\u6CD5\u6807\u8BB0\u89C4\u8303.md": "", "ah-week/SKILL.md": "", "ah-year/references/\u5E74\u62A5\u6A21\u677F.md": "", "ah-year/SKILL.md": "", "defuddle/SKILL.md": "", "json-canvas/references/EXAMPLES.md": "", "json-canvas/SKILL.md": "", "obsidian-bases/references/FUNCTIONS_REFERENCE.md": "", "obsidian-bases/SKILL.md": "", "obsidian-cli/SKILL.md": "", "obsidian-markdown/references/CALLOUTS.md": "", "obsidian-markdown/references/EMBEDS.md": "", "obsidian-markdown/references/PROPERTIES.md": "", "obsidian-markdown/SKILL.md": "" });
    module2.exports = {
      EMBEDDED_BUNDLED_SKILLS_VERSION,
      EMBEDDED_BUNDLED_SKILLS_FILE_COUNT,
      EMBEDDED_BUNDLED_SKILLS_FILES
    };
  }
});

// runtime/plugin/bundled-skills-methods.js
var require_bundled_skills_methods = __commonJS({
  "runtime/plugin/bundled-skills-methods.js"(exports2, module2) {
    var fs = require("fs");
    var path = require("path");
    var crypto = require("crypto");
    var { normalizeSupportedLocale: normalizeSupportedLocale2 } = require_i18n_locale_utils();
    var {
      TEMPLATE_MAP_FILE,
      DEFAULT_META_TEMPLATES_DIR,
      DEFAULT_BACKUP_ROOT,
      walkFilesRecursive,
      normalizeSafeRelativePath,
      dedupeNormalizedPaths,
      normalizeTemplateLocaleKey,
      normalizeTemplateLocaleVariant,
      filesHaveSameContent,
      isTemplateContentValid,
      isCancelledError,
      copyFileWithParent,
      cloneExistingPath
    } = require_bundled_skills_utils();
    var {
      resolveBundledSyncAction: resolveBundledSyncActionImpl,
      syncBundledSkills: syncBundledSkillsImpl,
      syncBundledContent: syncBundledContentImpl
    } = require_bundled_skills_flow_methods();
    var embeddedBundledSkillsModule = (() => {
      try {
        return require_bundled_skills_embedded();
      } catch {
        return {};
      }
    })();
    var EMBEDDED_BUNDLED_SKILLS_VERSION = String(
      embeddedBundledSkillsModule && embeddedBundledSkillsModule.EMBEDDED_BUNDLED_SKILLS_VERSION ? embeddedBundledSkillsModule.EMBEDDED_BUNDLED_SKILLS_VERSION : ""
    ).trim();
    var EMBEDDED_BUNDLED_SKILLS_FILES = embeddedBundledSkillsModule && embeddedBundledSkillsModule.EMBEDDED_BUNDLED_SKILLS_FILES && typeof embeddedBundledSkillsModule.EMBEDDED_BUNDLED_SKILLS_FILES === "object" ? embeddedBundledSkillsModule.EMBEDDED_BUNDLED_SKILLS_FILES : null;
    function hasEmbeddedBundledSkillsFiles() {
      if (!EMBEDDED_BUNDLED_SKILLS_FILES) return false;
      return Object.keys(EMBEDDED_BUNDLED_SKILLS_FILES).length > 0;
    }
    function ensureEmbeddedBundledSkillsRoot(pluginRootDir) {
      if (!pluginRootDir) return "";
      if (!hasEmbeddedBundledSkillsFiles()) return "";
      const embeddedRoot = path.join(pluginRootDir, ".bundled-skills-embedded");
      const stampPath = path.join(embeddedRoot, ".stamp");
      const version = EMBEDDED_BUNDLED_SKILLS_VERSION || "embedded";
      try {
        if (fs.existsSync(stampPath)) {
          const currentStamp = String(fs.readFileSync(stampPath, "utf8") || "").trim();
          if (currentStamp === version && fs.existsSync(embeddedRoot)) {
            return embeddedRoot;
          }
        }
      } catch {
      }
      try {
        fs.rmSync(embeddedRoot, { recursive: true, force: true });
        fs.mkdirSync(embeddedRoot, { recursive: true });
        const entries = Object.entries(EMBEDDED_BUNDLED_SKILLS_FILES);
        for (const [rawRelativePath, rawBase64] of entries) {
          const safeRelativePath = normalizeSafeRelativePath(rawRelativePath);
          if (!safeRelativePath) continue;
          const targetPath = path.join(embeddedRoot, safeRelativePath);
          fs.mkdirSync(path.dirname(targetPath), { recursive: true });
          fs.writeFileSync(targetPath, Buffer.from(String(rawBase64 || ""), "base64"));
        }
        fs.writeFileSync(stampPath, `${version}
`, "utf8");
        return embeddedRoot;
      } catch {
        return "";
      }
    }
    function createBundledSkillsMethods(options = {}) {
      const pluginDirname = String(options.pluginDirname || "");
      return {
        resolveBundledSkillLocale(localeHint) {
          const fallbackLocale = typeof this.getEffectiveLocale === "function" ? this.getEffectiveLocale() : "en";
          return normalizeSupportedLocale2(localeHint || fallbackLocale, "en");
        },
        toCanonicalLocalizedMdPath(filePath) {
          const value = String(filePath || "");
          if (!value.endsWith(".md")) return value;
          if (value.endsWith(".zh-CN.md")) return `${value.slice(0, -".zh-CN.md".length)}.md`;
          if (value.endsWith(".en.md")) return `${value.slice(0, -".en.md".length)}.md`;
          return value;
        },
        localizedMdPathByToken(basePath, token) {
          const canonical = this.toCanonicalLocalizedMdPath(basePath);
          if (!canonical.endsWith(".md")) return canonical;
          if (token === "base") return canonical;
          if (token === "zh-CN" || token === "en") return `${canonical.slice(0, -".md".length)}.${token}.md`;
          return canonical;
        },
        resolveLocalizedMarkdownSource(basePath, locale, options2 = {}) {
          const normalizedLocale = normalizeSupportedLocale2(locale, "en");
          const defaultOrder = normalizedLocale === "zh-CN" ? ["zh-CN", "base", "en"] : ["en", "base", "zh-CN"];
          const order = Array.isArray(options2.order) && options2.order.length ? options2.order : defaultOrder;
          for (const token of order) {
            const candidate = this.localizedMdPathByToken(basePath, token);
            if (!candidate || !fs.existsSync(candidate)) continue;
            try {
              if (fs.statSync(candidate).isFile()) return candidate;
            } catch {
            }
          }
          return "";
        },
        applyBundledSkillLocaleResources(srcDir, destDir, locale) {
          if (!srcDir || !destDir) return { applied: 0 };
          const relFiles = walkFilesRecursive(srcDir);
          const canonicalTargets = /* @__PURE__ */ new Set();
          for (const relPath of relFiles) {
            const normalizedRel = String(relPath || "").replace(/\\/g, "/");
            if (!normalizedRel.endsWith(".md")) continue;
            const isReference = normalizedRel.startsWith("references/");
            const isTemplate = normalizedRel.startsWith("assets/templates/");
            if (!isReference && !isTemplate) continue;
            canonicalTargets.add(this.toCanonicalLocalizedMdPath(normalizedRel));
          }
          let applied = 0;
          for (const canonicalRel of canonicalTargets) {
            const sourcePath = this.resolveLocalizedMarkdownSource(path.join(srcDir, canonicalRel), locale);
            if (!sourcePath) continue;
            copyFileWithParent(sourcePath, path.join(destDir, canonicalRel));
            applied += 1;
          }
          return { applied };
        },
        removeLocalizedMarkdownVariants(rootDir) {
          if (!rootDir || !fs.existsSync(rootDir)) return { removed: 0 };
          const relFiles = walkFilesRecursive(rootDir);
          let removed = 0;
          for (const relPath of relFiles) {
            const normalizedRel = String(relPath || "").replace(/\\/g, "/");
            if (!normalizedRel.endsWith(".en.md") && !normalizedRel.endsWith(".zh-CN.md")) continue;
            const absPath = path.join(rootDir, normalizedRel);
            try {
              if (fs.existsSync(absPath) && fs.statSync(absPath).isFile()) {
                fs.rmSync(absPath, { force: true });
                removed += 1;
              }
            } catch {
            }
          }
          return { removed };
        },
        resolveBundledSkillDocSource(skillDir, locale) {
          if (!skillDir) return "";
          const normalizedLocale = normalizeSupportedLocale2(locale, "en");
          const order = normalizedLocale === "zh-CN" ? ["zh-CN", "base", "en"] : ["en", "base", "zh-CN"];
          return this.resolveLocalizedMarkdownSource(path.join(skillDir, "SKILL.md"), normalizedLocale, { order });
        },
        applyBundledSkillLocaleDoc(srcDir, destDir, locale) {
          const sourcePath = this.resolveBundledSkillDocSource(srcDir, locale);
          if (!sourcePath) {
            return {
              ok: false,
              error: `\u672A\u627E\u5230\u53EF\u7528\u7684 SKILL \u6587\u4EF6\uFF08locale=${locale}\uFF09`
            };
          }
          const targetPath = path.join(destDir, "SKILL.md");
          copyFileWithParent(sourcePath, targetPath);
          return { ok: true, sourcePath, targetPath };
        },
        getPluginRootDir() {
          const vaultPath = this.getVaultPath();
          const configDir = this.app && this.app.vault && this.app.vault.configDir ? String(this.app.vault.configDir) : ".obsidian";
          const id = this.manifest && this.manifest.id ? String(this.manifest.id) : "flownote";
          const candidates = [
            path.join(vaultPath, configDir, "plugins", id),
            this.manifest && this.manifest.dir ? String(this.manifest.dir) : "",
            pluginDirname,
            pluginDirname ? path.resolve(pluginDirname, "..") : ""
          ].filter(Boolean);
          for (const dir of candidates) {
            if (fs.existsSync(path.join(dir, "manifest.json"))) return dir;
          }
          return candidates[0] || pluginDirname;
        },
        getBundledSkillsRoot() {
          const pluginRootDir = this.getPluginRootDir();
          const bundledRoot = path.join(pluginRootDir, "bundled-skills");
          if (fs.existsSync(bundledRoot)) return bundledRoot;
          const embeddedRoot = ensureEmbeddedBundledSkillsRoot(pluginRootDir);
          if (embeddedRoot) return embeddedRoot;
          return bundledRoot;
        },
        getBundledTemplateMapPath(rootDir = this.getBundledSkillsRoot()) {
          return path.join(rootDir, TEMPLATE_MAP_FILE);
        },
        getBundledSkillsContentSignature(rootDir = this.getBundledSkillsRoot(), skillIds = []) {
          if (!rootDir || !fs.existsSync(rootDir)) return "";
          const ids = [...new Set(
            (Array.isArray(skillIds) ? skillIds : []).map((id) => String(id || "").trim()).filter(Boolean)
          )].sort((a, b) => a.localeCompare(b));
          if (!ids.length) return "";
          const hash = crypto.createHash("sha1");
          for (const skillId of ids) {
            const skillDir = path.join(rootDir, skillId);
            if (!fs.existsSync(skillDir)) continue;
            const relFiles = walkFilesRecursive(skillDir).sort((a, b) => a.localeCompare(b));
            hash.update(`${skillId}
`);
            for (const relFile of relFiles) {
              const absFile = path.join(skillDir, relFile);
              try {
                hash.update(`${relFile}
`);
                hash.update(fs.readFileSync(absFile));
              } catch (_e) {
              }
            }
          }
          return hash.digest("hex");
        },
        getBundledSkillsStamp(skillIds = [], contentSignature = "") {
          const version = this.manifest && this.manifest.version ? String(this.manifest.version) : "0";
          const ids = [...new Set(
            (Array.isArray(skillIds) ? skillIds : []).map((id) => String(id || "").trim()).filter(Boolean)
          )].sort((a, b) => a.localeCompare(b));
          const signature = String(contentSignature || "").trim();
          return `${version}:${ids.join(",")}:${signature}`;
        },
        hasSyncedBundledSkills(targetRoot, bundledIds) {
          if (!targetRoot || !fs.existsSync(targetRoot)) return false;
          return bundledIds.every((skillId) => fs.existsSync(path.join(targetRoot, skillId, "SKILL.md")));
        },
        shouldSyncBundledSkills(targetRoot, bundledIds, stamp, force = false) {
          if (force) return true;
          const st = this.runtimeState && typeof this.runtimeState === "object" ? this.runtimeState : {};
          const previousStamp = String(st.bundledSkillsStamp || "");
          if (!previousStamp || previousStamp !== String(stamp || "")) return true;
          return !this.hasSyncedBundledSkills(targetRoot, bundledIds);
        },
        listBundledSkillIds(rootDir = this.getBundledSkillsRoot()) {
          if (!fs.existsSync(rootDir)) return [];
          return fs.readdirSync(rootDir, { withFileTypes: true }).filter((entry) => entry && entry.isDirectory() && !String(entry.name || "").startsWith(".")).map((entry) => String(entry.name || "").trim()).filter(Boolean).filter((id) => fs.existsSync(path.join(rootDir, id, "SKILL.md"))).sort((a, b) => a.localeCompare(b));
        },
        loadBundledTemplateMap(rootDir = this.getBundledSkillsRoot()) {
          const mapPath = this.getBundledTemplateMapPath(rootDir);
          const fallback = {
            path: mapPath,
            version: 1,
            metaTemplatesDir: DEFAULT_META_TEMPLATES_DIR,
            entries: []
          };
          if (!fs.existsSync(mapPath)) return fallback;
          try {
            const raw = fs.readFileSync(mapPath, "utf8");
            const parsed = JSON.parse(raw);
            const entries = Array.isArray(parsed.entries) ? parsed.entries : [];
            const normalizedEntries = entries.map((entry, index) => {
              const id = String(entry && entry.id ? entry.id : `template-${index + 1}`).trim();
              const metaSource = normalizeSafeRelativePath(entry && entry.metaSource);
              const fallbackPath = normalizeSafeRelativePath(entry && entry.fallback);
              const targets = dedupeNormalizedPaths(Array.isArray(entry && entry.targets) ? entry.targets : []);
              const locales = {};
              if (entry && entry.locales && typeof entry.locales === "object") {
                for (const [rawLocale, variant] of Object.entries(entry.locales)) {
                  const locale = normalizeTemplateLocaleKey(rawLocale);
                  if (!locale) continue;
                  const normalizedVariant = normalizeTemplateLocaleVariant(variant);
                  if (!normalizedVariant) continue;
                  locales[locale] = {
                    ...locales[locale] || {},
                    ...normalizedVariant
                  };
                }
              }
              if (!id || !metaSource || !fallbackPath || !targets.length) return null;
              return {
                id,
                metaSource,
                fallback: fallbackPath,
                targets,
                locales
              };
            }).filter(Boolean);
            const metaTemplatesDir = normalizeSafeRelativePath(parsed.metaTemplatesDir) || DEFAULT_META_TEMPLATES_DIR;
            return {
              path: mapPath,
              version: Number(parsed.version || 1),
              metaTemplatesDir,
              entries: normalizedEntries
            };
          } catch (e) {
            return {
              ...fallback,
              errors: [`template-map \u89E3\u6790\u5931\u8D25\uFF1A${e instanceof Error ? e.message : String(e)}`]
            };
          }
        },
        resolveTemplateEntryByLocale(entry, locale = "en") {
          if (!entry || typeof entry !== "object") {
            return {
              id: "",
              metaSource: "",
              fallback: "",
              targets: [],
              staleTargets: [],
              staleMetaSources: []
            };
          }
          const normalizedLocale = this.resolveBundledSkillLocale(locale);
          const localeVariant = entry.locales && typeof entry.locales === "object" ? entry.locales[normalizedLocale] : null;
          const metaSource = normalizeSafeRelativePath(localeVariant && localeVariant.metaSource) || normalizeSafeRelativePath(entry.metaSource);
          const fallbackPath = normalizeSafeRelativePath(localeVariant && localeVariant.fallback) || normalizeSafeRelativePath(entry.fallback);
          const targets = dedupeNormalizedPaths(
            Array.isArray(localeVariant && localeVariant.targets) && localeVariant.targets.length ? localeVariant.targets : entry.targets
          );
          const allTargets = dedupeNormalizedPaths([
            ...Array.isArray(entry.targets) ? entry.targets : [],
            ...Object.values(entry.locales && typeof entry.locales === "object" ? entry.locales : {}).flatMap((variant) => Array.isArray(variant && variant.targets) ? variant.targets : [])
          ]);
          const allMetaSources = dedupeNormalizedPaths([
            entry.metaSource,
            ...Object.values(entry.locales && typeof entry.locales === "object" ? entry.locales : {}).map((variant) => variant && variant.metaSource)
          ]);
          return {
            id: String(entry.id || "").trim(),
            locale: normalizedLocale,
            metaSource,
            fallback: fallbackPath,
            targets,
            staleTargets: allTargets.filter((target) => !targets.includes(target)),
            staleMetaSources: allMetaSources.filter((meta) => meta !== metaSource)
          };
        },
        getBundledContentBackupRoot(vaultPath, options2 = {}) {
          const root = normalizeSafeRelativePath(options2.backupDir || "") || DEFAULT_BACKUP_ROOT;
          const ts = options2.timestamp || (/* @__PURE__ */ new Date()).toISOString().replace(/[:.]/g, "-");
          return path.join(vaultPath, root, ts);
        },
        backupPathIfNeeded(srcPath, backupRoot, backupRelativePath) {
          if (!srcPath || !backupRoot || !fs.existsSync(srcPath)) return false;
          const normalizedRel = normalizeSafeRelativePath(backupRelativePath) || path.basename(srcPath);
          const backupPath = path.join(backupRoot, normalizedRel);
          return cloneExistingPath(srcPath, backupPath);
        },
        resolveTemplateSource(vaultPath, bundledRoot, templateMap, entry, locale = "en") {
          const normalizedLocale = this.resolveBundledSkillLocale(locale);
          const localizedEntry = this.resolveTemplateEntryByLocale(entry, normalizedLocale);
          const metaRoot = path.join(vaultPath, templateMap.metaTemplatesDir || DEFAULT_META_TEMPLATES_DIR);
          const metaBase = path.join(metaRoot, localizedEntry.metaSource);
          const fallbackBase = path.join(bundledRoot, localizedEntry.fallback);
          const metaOrders = normalizedLocale === "zh-CN" ? [["zh-CN", "base", "en"]] : [["en"], ["base", "zh-CN"]];
          const fallbackOrders = normalizedLocale === "zh-CN" ? [["zh-CN", "base", "en"]] : [["en"], ["base", "zh-CN"]];
          const tryResolve = (basePath, sourceType, orders) => {
            for (const order of orders) {
              const resolved = this.resolveLocalizedMarkdownSource(basePath, normalizedLocale, { order });
              if (!resolved || !fs.existsSync(resolved) || !isTemplateContentValid(resolved)) continue;
              return { sourceType, sourcePath: resolved };
            }
            return null;
          };
          const firstMeta = tryResolve(metaBase, "meta", [metaOrders[0]]);
          if (firstMeta) {
            return {
              ok: true,
              entry: localizedEntry,
              sourceType: "meta",
              sourcePath: firstMeta.sourcePath,
              metaRoot,
              fallbackPath: fallbackBase
            };
          }
          const firstFallback = tryResolve(fallbackBase, "fallback", [fallbackOrders[0]]);
          if (firstFallback) {
            return {
              ok: true,
              entry: localizedEntry,
              sourceType: "fallback",
              sourcePath: firstFallback.sourcePath,
              metaRoot,
              fallbackPath: fallbackBase,
              missingMeta: !this.resolveLocalizedMarkdownSource(metaBase, normalizedLocale, { order: metaOrders[0] }),
              invalidMeta: Boolean(
                this.resolveLocalizedMarkdownSource(metaBase, normalizedLocale, { order: metaOrders[0] }) && !isTemplateContentValid(this.resolveLocalizedMarkdownSource(metaBase, normalizedLocale, { order: metaOrders[0] }))
              )
            };
          }
          const secondaryMeta = metaOrders.length > 1 ? tryResolve(metaBase, "meta", [metaOrders[1]]) : null;
          if (secondaryMeta) {
            return {
              ok: true,
              entry: localizedEntry,
              sourceType: "meta",
              sourcePath: secondaryMeta.sourcePath,
              metaRoot,
              fallbackPath: fallbackBase
            };
          }
          const secondaryFallback = fallbackOrders.length > 1 ? tryResolve(fallbackBase, "fallback", [fallbackOrders[1]]) : null;
          if (secondaryFallback) {
            return {
              ok: true,
              entry: localizedEntry,
              sourceType: "fallback",
              sourcePath: secondaryFallback.sourcePath,
              metaRoot,
              fallbackPath: fallbackBase,
              missingMeta: !this.resolveLocalizedMarkdownSource(metaBase, normalizedLocale),
              invalidMeta: Boolean(
                this.resolveLocalizedMarkdownSource(metaBase, normalizedLocale) && !isTemplateContentValid(this.resolveLocalizedMarkdownSource(metaBase, normalizedLocale))
              )
            };
          }
          const metaCandidates = [
            this.localizedMdPathByToken(metaBase, "zh-CN"),
            this.localizedMdPathByToken(metaBase, "base"),
            this.localizedMdPathByToken(metaBase, "en")
          ];
          const fallbackCandidates = [
            this.localizedMdPathByToken(fallbackBase, "zh-CN"),
            this.localizedMdPathByToken(fallbackBase, "base"),
            this.localizedMdPathByToken(fallbackBase, "en")
          ];
          const metaDesc = metaCandidates.filter(Boolean).join(" | ");
          const fallbackDesc = fallbackCandidates.filter(Boolean).join(" | ");
          return {
            ok: false,
            entry: localizedEntry,
            metaRoot,
            metaSource: metaBase,
            fallbackPath: fallbackBase,
            error: `\u6A21\u677F\u6E90\u7F3A\u5931\uFF1A${localizedEntry.id}\uFF08Meta: ${metaDesc}\uFF0CFallback: ${fallbackDesc}\uFF09`
          };
        },
        async resolveBundledSyncAction(conflict, options2 = {}, state = {}) {
          return resolveBundledSyncActionImpl.call(this, conflict, options2, state);
        },
        async syncBundledSkills(vaultPath, options2 = {}) {
          return syncBundledSkillsImpl.call(this, vaultPath, options2);
        },
        async syncBundledTemplates(vaultPath, options2 = {}) {
          const skillLocale = this.resolveBundledSkillLocale(options2.locale);
          const bundledRoot = this.getBundledSkillsRoot();
          const templateMap = this.loadBundledTemplateMap(bundledRoot);
          const targetRoot = String(options2.targetRoot || "") || path.join(vaultPath, this.settings.skillsDir);
          const metaRoot = path.join(vaultPath, templateMap.metaTemplatesDir || DEFAULT_META_TEMPLATES_DIR);
          const entries = Array.isArray(templateMap.entries) ? templateMap.entries : [];
          const errors = Array.isArray(templateMap.errors) ? [...templateMap.errors] : [];
          if (!entries.length) {
            return {
              synced: 0,
              total: 0,
              skippedCount: 0,
              replacedCount: 0,
              conflictCount: 0,
              backupCount: 0,
              fallbackUsed: [],
              missingMeta: [],
              skipped: true,
              cancelled: false,
              locale: skillLocale,
              targetRoot,
              bundledRoot,
              mapPath: templateMap.path,
              metaRoot,
              backupRoot: "",
              errors
            };
          }
          fs.mkdirSync(targetRoot, { recursive: true });
          let synced = 0;
          let skippedCount = 0;
          let replacedCount = 0;
          let backupCount = 0;
          let backupRoot = "";
          const conflicts = [];
          const conflictState = {};
          const fallbackUsed = /* @__PURE__ */ new Set();
          const missingMeta = /* @__PURE__ */ new Set();
          let cleanedStaleCount = 0;
          const total = entries.reduce((sum, entry) => {
            const localizedEntry = this.resolveTemplateEntryByLocale(entry, skillLocale);
            return sum + localizedEntry.targets.length;
          }, 0);
          try {
            for (const entry of entries) {
              const source = this.resolveTemplateSource(vaultPath, bundledRoot, templateMap, entry, skillLocale);
              if (!source.ok) {
                errors.push(source.error);
                continue;
              }
              if (source.sourceType === "fallback") fallbackUsed.add(entry.id);
              if (source.missingMeta || source.invalidMeta) missingMeta.add(entry.id);
              for (const targetRelativePath of source.entry.targets) {
                const destFile = path.join(targetRoot, targetRelativePath);
                const exists = fs.existsSync(destFile);
                if (exists && filesHaveSameContent(destFile, source.sourcePath)) {
                  continue;
                }
                let action = "replace";
                if (exists) {
                  action = await this.resolveBundledSyncAction({
                    kind: "template",
                    id: entry.id,
                    sourceType: source.sourceType,
                    sourcePath: source.sourcePath,
                    targetPath: destFile,
                    targetRelativePath,
                    metaSourcePath: path.join(source.metaRoot, source.entry.metaSource)
                  }, options2, conflictState);
                  conflicts.push({ kind: "template", id: entry.id, action, targetPath: destFile });
                }
                if (action === "skip") {
                  skippedCount += 1;
                  continue;
                }
                try {
                  if (exists) {
                    if (!backupRoot) backupRoot = this.getBundledContentBackupRoot(vaultPath, options2);
                    const backed = this.backupPathIfNeeded(destFile, backupRoot, path.join("templates", targetRelativePath));
                    if (backed) backupCount += 1;
                    replacedCount += 1;
                  }
                  copyFileWithParent(source.sourcePath, destFile);
                  synced += 1;
                } catch (e) {
                  errors.push(`${entry.id}: ${e instanceof Error ? e.message : String(e)}`);
                }
              }
              for (const staleRelativePath of source.entry.staleTargets) {
                const staleFile = path.join(targetRoot, staleRelativePath);
                if (!fs.existsSync(staleFile)) continue;
                try {
                  if (!backupRoot) backupRoot = this.getBundledContentBackupRoot(vaultPath, options2);
                  const backed = this.backupPathIfNeeded(staleFile, backupRoot, path.join("templates", staleRelativePath));
                  if (backed) backupCount += 1;
                  fs.rmSync(staleFile, { force: true });
                  cleanedStaleCount += 1;
                } catch (e) {
                  errors.push(`${entry.id}: \u6E05\u7406\u65E7\u6A21\u677F\u5931\u8D25 ${staleRelativePath} -> ${e instanceof Error ? e.message : String(e)}`);
                }
              }
            }
          } catch (e) {
            if (isCancelledError(e)) {
              return {
                synced,
                total,
                skippedCount,
                replacedCount,
                conflictCount: conflicts.length,
                backupCount,
                cleanedStaleCount,
                fallbackUsed: [...fallbackUsed],
                missingMeta: [...missingMeta],
                skipped: false,
                cancelled: true,
                cancelledStage: e.stage,
                locale: skillLocale,
                targetRoot,
                bundledRoot,
                mapPath: templateMap.path,
                metaRoot,
                backupRoot,
                conflicts,
                errors
              };
            }
            errors.push(e instanceof Error ? e.message : String(e));
          }
          return {
            synced,
            total,
            skippedCount,
            replacedCount,
            conflictCount: conflicts.length,
            backupCount,
            cleanedStaleCount,
            fallbackUsed: [...fallbackUsed],
            missingMeta: [...missingMeta],
            skipped: false,
            cancelled: false,
            locale: skillLocale,
            targetRoot,
            bundledRoot,
            mapPath: templateMap.path,
            metaRoot,
            backupRoot,
            conflicts,
            errors
          };
        },
        async resetMetaTemplateBaseline(vaultPath, options2 = {}) {
          const skillLocale = this.resolveBundledSkillLocale(options2.locale);
          const bundledRoot = this.getBundledSkillsRoot();
          const templateMap = this.loadBundledTemplateMap(bundledRoot);
          const metaRoot = path.join(vaultPath, templateMap.metaTemplatesDir || DEFAULT_META_TEMPLATES_DIR);
          const entries = Array.isArray(templateMap.entries) ? templateMap.entries : [];
          const errors = Array.isArray(templateMap.errors) ? [...templateMap.errors] : [];
          if (!entries.length) {
            return {
              synced: 0,
              total: 0,
              skippedCount: 0,
              replacedCount: 0,
              conflictCount: 0,
              backupCount: 0,
              skipped: true,
              cancelled: false,
              locale: skillLocale,
              metaRoot,
              mapPath: templateMap.path,
              backupRoot: "",
              errors
            };
          }
          fs.mkdirSync(metaRoot, { recursive: true });
          const uniqueEntries = [];
          const seen = /* @__PURE__ */ new Set();
          for (const entry of entries) {
            const localizedEntry = this.resolveTemplateEntryByLocale(entry, skillLocale);
            if (!localizedEntry.metaSource || !localizedEntry.fallback || !localizedEntry.targets.length) continue;
            if (seen.has(localizedEntry.metaSource)) continue;
            seen.add(localizedEntry.metaSource);
            uniqueEntries.push({ original: entry, localized: localizedEntry });
          }
          let synced = 0;
          let skippedCount = 0;
          let replacedCount = 0;
          let backupCount = 0;
          let cleanedStaleCount = 0;
          let backupRoot = "";
          const conflicts = [];
          const conflictState = {};
          try {
            for (const pair of uniqueEntries) {
              const entry = pair.original;
              const localizedEntry = pair.localized;
              const srcFile = this.resolveLocalizedMarkdownSource(
                path.join(bundledRoot, localizedEntry.fallback),
                skillLocale
              );
              if (!fs.existsSync(srcFile) || !isTemplateContentValid(srcFile)) {
                errors.push(`\u6A21\u677F\u57FA\u7EBF\u7F3A\u5931\uFF1A${entry.id} -> ${srcFile}`);
                continue;
              }
              const destFile = path.join(metaRoot, localizedEntry.metaSource);
              const exists = fs.existsSync(destFile);
              if (!exists || !filesHaveSameContent(srcFile, destFile)) {
                let action = "replace";
                if (exists) {
                  action = await this.resolveBundledSyncAction({
                    kind: "meta-template",
                    id: entry.id,
                    sourcePath: srcFile,
                    targetPath: destFile,
                    targetRelativePath: localizedEntry.metaSource
                  }, options2, conflictState);
                  conflicts.push({ kind: "meta-template", id: entry.id, action, targetPath: destFile });
                }
                if (action === "skip") {
                  skippedCount += 1;
                  continue;
                }
                try {
                  if (exists) {
                    if (!backupRoot) backupRoot = this.getBundledContentBackupRoot(vaultPath, options2);
                    const backed = this.backupPathIfNeeded(destFile, backupRoot, path.join("meta-templates", localizedEntry.metaSource));
                    if (backed) backupCount += 1;
                    replacedCount += 1;
                  }
                  copyFileWithParent(srcFile, destFile);
                  synced += 1;
                } catch (e) {
                  errors.push(`${entry.id}: ${e instanceof Error ? e.message : String(e)}`);
                }
              }
              for (const staleMetaRelativePath of localizedEntry.staleMetaSources) {
                const staleFile = path.join(metaRoot, staleMetaRelativePath);
                if (!fs.existsSync(staleFile)) continue;
                try {
                  if (!backupRoot) backupRoot = this.getBundledContentBackupRoot(vaultPath, options2);
                  const backed = this.backupPathIfNeeded(
                    staleFile,
                    backupRoot,
                    path.join("meta-templates", staleMetaRelativePath)
                  );
                  if (backed) backupCount += 1;
                  fs.rmSync(staleFile, { force: true });
                  cleanedStaleCount += 1;
                } catch (e) {
                  errors.push(`${entry.id}: \u6E05\u7406\u65E7\u6A21\u677F\u5931\u8D25 ${staleMetaRelativePath} -> ${e instanceof Error ? e.message : String(e)}`);
                }
              }
            }
          } catch (e) {
            if (isCancelledError(e)) {
              return {
                synced,
                total: uniqueEntries.length,
                skippedCount,
                replacedCount,
                conflictCount: conflicts.length,
                backupCount,
                cleanedStaleCount,
                skipped: false,
                cancelled: true,
                cancelledStage: e.stage,
                locale: skillLocale,
                metaRoot,
                mapPath: templateMap.path,
                backupRoot,
                conflicts,
                errors
              };
            }
            errors.push(e instanceof Error ? e.message : String(e));
          }
          return {
            synced,
            total: uniqueEntries.length,
            skippedCount,
            replacedCount,
            conflictCount: conflicts.length,
            backupCount,
            cleanedStaleCount,
            skipped: false,
            cancelled: false,
            locale: skillLocale,
            metaRoot,
            mapPath: templateMap.path,
            backupRoot,
            conflicts,
            errors
          };
        },
        async syncBundledContent(vaultPath, options2 = {}) {
          return syncBundledContentImpl.call(this, vaultPath, options2);
        }
      };
    }
    module2.exports = { createBundledSkillsMethods };
  }
});

// runtime/plugin/session-bootstrap-methods.js
var require_session_bootstrap_methods = __commonJS({
  "runtime/plugin/session-bootstrap-methods.js"(exports2, module2) {
    var {
      extractAssistantPayloadFromEnvelope
    } = require_assistant_payload_utils();
    var RUNTIME_SCHEMA_VERSION = 1;
    function normalizeTimestampMs(value) {
      const raw = Number(value || 0);
      if (!Number.isFinite(raw) || raw <= 0) return 0;
      if (raw >= 1e14) return Math.floor(raw / 1e3);
      if (raw >= 1e12) return Math.floor(raw);
      if (raw >= 1e9) return Math.floor(raw * 1e3);
      return Math.floor(raw);
    }
    function mergeSnapshotText(existingText, incomingText) {
      const existing = String(existingText || "");
      const incoming = String(incomingText || "");
      if (!incoming.trim()) return existing;
      if (!existing.trim()) return incoming;
      if (existing === incoming) return existing;
      if (incoming.includes(existing)) return incoming;
      if (existing.includes(incoming)) return existing;
      return incoming.length >= existing.length ? incoming : existing;
    }
    function extractMergedPartText(parts, targetType = "text") {
      const list = Array.isArray(parts) ? parts : [];
      const byPart = /* @__PURE__ */ new Map();
      list.forEach((part, index) => {
        const item = part && typeof part === "object" ? part : null;
        if (!item || String(item.type || "").trim().toLowerCase() !== String(targetType || "").toLowerCase()) return;
        const key = String(item.id || `${targetType}:${index}`);
        const current = String(byPart.get(key) || "");
        let next = current;
        if (typeof item.delta === "string") next = mergeSnapshotText(next, item.delta);
        if (typeof item.text === "string") next = mergeSnapshotText(next, item.text);
        byPart.set(key, next);
      });
      return Array.from(byPart.values()).map((text) => String(text || "").trim()).filter(Boolean).join("\n\n");
    }
    function toLocalSessionMessage(envelope, index, extractAssistantPayload) {
      const row = envelope && typeof envelope === "object" ? envelope : null;
      if (!row) return null;
      const info = row.info && typeof row.info === "object" ? row.info : {};
      const parts = Array.isArray(row.parts) ? row.parts : [];
      const roleRaw = String(info.role || info.type || "").trim().toLowerCase();
      const role = roleRaw === "assistant" ? "assistant" : roleRaw === "user" ? "user" : "";
      if (!role) return null;
      const id = String(info.id || `${role}-${index}`);
      const createdAt = normalizeTimestampMs(
        info.time && info.time.created || info.created || info.updated || 0
      );
      if (role === "assistant") {
        const payload = typeof extractAssistantPayload === "function" ? extractAssistantPayload({ info, parts }) || { text: "", reasoning: "", meta: "", blocks: [] } : {
          text: extractMergedPartText(parts, "text"),
          reasoning: extractMergedPartText(parts, "reasoning"),
          meta: "",
          blocks: []
        };
        const hasVisiblePayload = Boolean(
          String(payload.text || "").trim() || String(payload.reasoning || "").trim() || String(payload.meta || "").trim() || Array.isArray(payload.blocks) && payload.blocks.length
        );
        if (!hasVisiblePayload) return null;
        return {
          id,
          role,
          text: String(payload.text || ""),
          reasoning: String(payload.reasoning || ""),
          meta: String(payload.meta || ""),
          blocks: Array.isArray(payload.blocks) ? payload.blocks : [],
          createdAt,
          pending: false,
          error: "",
          __sortIndex: index
        };
      }
      const text = extractMergedPartText(parts, "text");
      if (!text.trim()) return null;
      return {
        id,
        role,
        text,
        reasoning: "",
        meta: "",
        blocks: [],
        createdAt,
        pending: false,
        error: "",
        __sortIndex: index
      };
    }
    var sessionBootstrapMethods = {
      getAssistantView() {
        const leaves = this.app.workspace.getLeavesOfType(this.getViewType());
        if (!leaves.length) return null;
        return leaves[0].view;
      },
      async activateView() {
        const leaves = this.app.workspace.getLeavesOfType(this.getViewType());
        if (leaves.length) {
          await this.app.workspace.revealLeaf(leaves[0]);
        } else {
          const leaf = this.app.workspace.getRightLeaf(false);
          await leaf.setViewState({ type: this.getViewType(), active: true });
          await this.app.workspace.revealLeaf(leaf);
        }
        void this.bootstrapData({ waitRemote: false, startRemote: true }).catch((e) => {
          this.log(`activate view bootstrap failed: ${e instanceof Error ? e.message : String(e)}`);
        });
        void this.refreshDiagnosticsStatus({ ttlMs: 1e4, force: false, applyView: true });
      },
      async refreshDiagnosticsStatus(options = {}) {
        const service = this.diagnosticsService;
        if (!service || typeof service.runCached !== "function") return null;
        const ttlMs = Math.max(0, Number(options.ttlMs || 1e4));
        const force = Boolean(options.force);
        let result = null;
        try {
          result = await service.runCached(ttlMs, force);
        } catch (e) {
          this.log(`refresh diagnostics failed: ${e instanceof Error ? e.message : String(e)}`);
          return null;
        }
        const applyView = options.applyView !== false;
        if (applyView) {
          const view = this.getAssistantView();
          if (view && typeof view.applyStatus === "function") {
            view.applyStatus(result);
          }
        }
        return result;
      },
      async loadPersistedData() {
        const runtime = this.ensureRuntimeModules();
        const raw = await this.loadData() || {};
        const rawSchemaVersion = Number(raw && raw.schemaVersion ? raw.schemaVersion : 0);
        this.schemaVersion = Number.isFinite(rawSchemaVersion) && rawSchemaVersion > 0 ? Math.floor(rawSchemaVersion) : RUNTIME_SCHEMA_VERSION;
        if (raw.settings || raw.runtimeState) {
          const rawSettings = raw.settings || {};
          this.settings = runtime.normalizeSettings(rawSettings);
          const runtimeStateRaw = raw.runtimeState || { sessions: [], activeSessionId: "", messagesBySession: {} };
          let beforeSnapshot = "";
          try {
            beforeSnapshot = JSON.stringify(runtimeStateRaw);
          } catch {
            beforeSnapshot = "";
          }
          this.runtimeState = runtime.migrateLegacyMessages(runtimeStateRaw);
          let afterSnapshot = "";
          try {
            afterSnapshot = JSON.stringify(this.runtimeState);
          } catch {
            afterSnapshot = "";
          }
          this.runtimeStateMigrationDirty = Boolean(beforeSnapshot && afterSnapshot && beforeSnapshot !== afterSnapshot);
          this.ensureRuntimeStateShape();
          this.ensureModelCatalogState();
          return;
        }
        this.settings = runtime.normalizeSettings(raw);
        this.runtimeState = runtime.migrateLegacyMessages({ sessions: [], activeSessionId: "", messagesBySession: {} });
        this.runtimeStateMigrationDirty = false;
        this.ensureRuntimeStateShape();
        this.ensureModelCatalogState();
      },
      async saveSettings() {
        const runtime = this.ensureRuntimeModules();
        this.settings = runtime.normalizeSettings(this.settings);
        if (this.skillService) this.skillService.updateSettings(this.settings);
        if (this.opencodeClient) this.opencodeClient.updateSettings(this.settings);
        await this.persistState();
      },
      async persistState() {
        this.schemaVersion = RUNTIME_SCHEMA_VERSION;
        await this.saveData({
          schemaVersion: this.schemaVersion,
          settings: this.settings,
          runtimeState: this.runtimeState
        });
      },
      async reloadSkills(options = {}) {
        if (!this.skillService) return null;
        const vaultPath = this.getVaultPath();
        const locale = String(
          options && options.locale ? options.locale : typeof this.getEffectiveLocale === "function" ? this.getEffectiveLocale() : "en"
        ).trim() || "en";
        const syncResult = await this.syncBundledContent(vaultPath, {
          force: Object.prototype.hasOwnProperty.call(options, "force") ? Boolean(options.force) : true,
          syncTemplates: Object.prototype.hasOwnProperty.call(options, "syncTemplates") ? Boolean(options.syncTemplates) : true,
          locale,
          resolveConflict: typeof options.resolveConflict === "function" ? options.resolveConflict : null,
          defaultConflictAction: String(options.defaultConflictAction || "replace"),
          backupDir: options.backupDir
        });
        this.log(
          `bundled content reload: skills=${syncResult.synced || 0}/${syncResult.total || 0}, templates=${syncResult.syncedTemplates || 0}/${syncResult.totalTemplates || 0}, source=${syncResult.bundledRoot || "unknown"} target=${syncResult.targetRoot || "unknown"}`
        );
        if (syncResult.errors.length) this.log(`bundled content sync: ${syncResult.errors.join("; ")}`);
        this.skillService.loadSkills();
        if (syncResult.stampUpdated) {
          try {
            await this.persistState();
          } catch (e) {
            this.log(`persist bundled skills stamp failed: ${e instanceof Error ? e.message : String(e)}`);
          }
        }
        const view = this.getAssistantView();
        if (view) view.render();
        return syncResult;
      },
      async resetTemplateBaseline(options = {}) {
        const vaultPath = this.getVaultPath();
        const locale = String(
          options && options.locale ? options.locale : typeof this.getEffectiveLocale === "function" ? this.getEffectiveLocale() : "en"
        ).trim() || "en";
        return this.resetMetaTemplateBaseline(vaultPath, {
          locale,
          resolveConflict: typeof options.resolveConflict === "function" ? options.resolveConflict : null,
          defaultConflictAction: String(options.defaultConflictAction || "skip"),
          backupDir: options.backupDir
        });
      },
      async createSession(title) {
        const created = await this.opencodeClient.createSession(title || "");
        const sessionId = String(
          created && (created.id || created.sessionID || created.sessionId) || created && created.session && (created.session.id || created.session.sessionID || created.session.sessionId) || ""
        ).trim();
        if (!sessionId) {
          throw new Error("FLOWnote \u521B\u5EFA\u4F1A\u8BDD\u5931\u8D25\uFF1A\u8FD4\u56DE\u6570\u636E\u7F3A\u5C11\u4F1A\u8BDD ID");
        }
        const session = {
          id: sessionId,
          title: created.title || title || "\u65B0\u4F1A\u8BDD",
          updatedAt: Date.now()
        };
        if (this.runtimeState && Array.isArray(this.runtimeState.deletedSessionIds)) {
          this.runtimeState.deletedSessionIds = this.runtimeState.deletedSessionIds.filter((id) => id !== session.id);
        }
        this.sessionStore.upsertSession(session);
        await this.persistState();
        return session;
      },
      async deleteSession(sessionId) {
        if (!this.sessionStore || typeof this.sessionStore.removeSession !== "function") return false;
        const removed = this.sessionStore.removeSession(sessionId);
        if (!removed) return false;
        await this.persistState();
        return true;
      },
      normalizeRemoteSessionMessages(messages) {
        const list = Array.isArray(messages) ? messages : [];
        const extractAssistantPayload = extractAssistantPayloadFromEnvelope;
        return list.map((item, index) => toLocalSessionMessage(item, index, extractAssistantPayload)).filter(Boolean).sort((a, b) => {
          const at = Number(a.createdAt || 0);
          const bt = Number(b.createdAt || 0);
          if (at !== bt) return at - bt;
          return Number(a.__sortIndex || 0) - Number(b.__sortIndex || 0);
        }).map((item) => ({
          id: item.id,
          role: item.role,
          text: item.text,
          reasoning: item.reasoning,
          meta: item.meta,
          blocks: item.blocks,
          pending: false,
          error: item.error,
          createdAt: Number(item.createdAt || 0)
        }));
      },
      async ensureSessionMessagesLoaded(sessionId, options = {}) {
        const sid = String(sessionId || "").trim();
        if (!sid || !this.sessionStore || !this.opencodeClient) return false;
        const force = Boolean(options.force);
        const st = this.sessionStore.state();
        const currentMessages = Array.isArray(st.messagesBySession && st.messagesBySession[sid]) ? st.messagesBySession[sid] : [];
        if (!force && currentMessages.length) return false;
        let remoteMessages = [];
        try {
          remoteMessages = await this.opencodeClient.listSessionMessages({
            sessionId: sid,
            limit: Number(options.limit || 200),
            signal: options.signal
          });
        } catch (error) {
          this.log(`load session messages failed (${sid}): ${error instanceof Error ? error.message : String(error)}`);
          return false;
        }
        const normalizedMessages = this.normalizeRemoteSessionMessages(remoteMessages);
        if (!normalizedMessages.length && !force) return false;
        return this.sessionStore.setSessionMessages(sid, normalizedMessages);
      },
      async syncSessionsFromRemote() {
        try {
          const st = this.sessionStore.state();
          const remote = await this.opencodeClient.listSessions();
          const deletedSet = new Set(
            Array.isArray(this.runtimeState && this.runtimeState.deletedSessionIds) ? this.runtimeState.deletedSessionIds : []
          );
          let changed = false;
          remote.forEach((s) => {
            if (!s || deletedSet.has(s.id)) return;
            const nextSession = {
              id: s.id,
              title: s.title || "\u672A\u547D\u540D\u4F1A\u8BDD",
              updatedAt: s.time && s.time.updated || Date.now()
            };
            const existing = st.sessions.find((row) => row.id === nextSession.id);
            if (!existing) {
              changed = true;
            } else if (String(existing.title || "") !== String(nextSession.title || "") || Number(existing.updatedAt || 0) !== Number(nextSession.updatedAt || 0)) {
              changed = true;
            }
            this.sessionStore.upsertSession({
              id: nextSession.id,
              title: nextSession.title,
              updatedAt: nextSession.updatedAt
            });
          });
          if (!st.activeSessionId && st.sessions.length) {
            st.activeSessionId = st.sessions[0].id;
            changed = true;
          }
          if (changed) await this.persistState();
        } catch {
        }
      },
      async bootstrapLocalData(options = {}) {
        if (this.bootstrapLocalDone && !options.force) return;
        try {
          this.loadModelCatalogFromTerminalOutput();
        } catch (e) {
          this.log(`load model cache failed: ${e instanceof Error ? e.message : String(e)}`);
        }
        const vaultPath = this.getVaultPath();
        const locale = String(
          options && options.locale ? options.locale : typeof this.getEffectiveLocale === "function" ? this.getEffectiveLocale() : "en"
        ).trim() || "en";
        const syncResult = await this.syncBundledContent(vaultPath, {
          force: Boolean(options.force),
          syncTemplates: true,
          locale,
          defaultConflictAction: "replace"
        });
        if (!syncResult.errors.length && !syncResult.skipped) {
          this.log(
            `bundled content bootstrap: skills=${syncResult.synced || 0}/${syncResult.total || 0}, templates=${syncResult.syncedTemplates || 0}/${syncResult.totalTemplates || 0}, source=${syncResult.bundledRoot || "unknown"} target=${syncResult.targetRoot || "unknown"}`
          );
        }
        if (syncResult.errors.length) this.log(`bundled content bootstrap sync: ${syncResult.errors.join("; ")}`);
        this.skillService.loadSkills();
        this.bootstrapLocalDone = true;
        if (syncResult.stampUpdated) {
          try {
            await this.persistState();
          } catch (e) {
            this.log(`persist bundled skills stamp failed: ${e instanceof Error ? e.message : String(e)}`);
          }
        }
      },
      async runBootstrapRemote() {
        await this.refreshModelCatalog();
        await this.syncSessionsFromRemote();
        this.bootstrapRemoteDone = true;
        this.bootstrapRemoteAt = Date.now();
      },
      async bootstrapData(options = {}) {
        const force = Boolean(options && options.force);
        const waitRemote = Boolean(options && options.waitRemote);
        const startRemote = Object.prototype.hasOwnProperty.call(options || {}, "startRemote") ? Boolean(options.startRemote) : true;
        if (force) {
          this.bootstrapLocalDone = false;
          this.bootstrapRemoteDone = false;
        }
        await this.bootstrapLocalData({ force });
        const shouldStartRemote = startRemote && (force || !this.bootstrapRemoteDone);
        if (shouldStartRemote && !this.bootstrapInflight) {
          this.bootstrapInflight = (async () => {
            try {
              await this.runBootstrapRemote();
            } catch (e) {
              this.bootstrapRemoteDone = false;
              throw e;
            } finally {
              this.bootstrapInflight = null;
            }
          })().catch((e) => {
            this.log(`bootstrap remote failed: ${e instanceof Error ? e.message : String(e)}`);
            return null;
          });
        }
        if (waitRemote && this.bootstrapInflight) {
          await this.bootstrapInflight;
        }
        return {
          localDone: Boolean(this.bootstrapLocalDone),
          remoteDone: Boolean(this.bootstrapRemoteDone),
          remoteAt: Number(this.bootstrapRemoteAt || 0),
          remoteInflight: Boolean(this.bootstrapInflight),
          remoteSkipped: !startRemote
        };
      }
    };
    module2.exports = { sessionBootstrapMethods };
  }
});

// runtime/mobile/mobile-settings-utils.js
var require_mobile_settings_utils = __commonJS({
  "runtime/mobile/mobile-settings-utils.js"(exports2, module2) {
    var {
      normalizeUiLanguage: normalizeUiLanguage2,
      normalizeSupportedLocale: normalizeSupportedLocale2,
      resolveLocaleFromNavigator: resolveLocaleFromNavigator2,
      DEFAULT_UI_LOCALE: DEFAULT_UI_LOCALE2
    } = require_i18n_locale_utils();
    var LINK_RESOLVER_PROVIDER_IDS = ["tianapi", "showapi", "gugudata"];
    var LINK_RESOLVER_PROVIDER_PRESETS = {
      tianapi: {
        id: "tianapi",
        name: "TianAPI",
        keyField: "tianapiKey",
        keyLabel: "TianAPI Key",
        keyPlaceholder: "tianapi key",
        keyUrl: "https://www.tianapi.com/apiview/66",
        docsUrl: "https://www.tianapi.com/apiview/66",
        hint: "Suitable for basic webpage content extraction; dynamic or anti-crawl pages may fail."
      },
      showapi: {
        id: "showapi",
        name: "ShowAPI",
        keyField: "showapiAppKey",
        keyLabel: "ShowAPI AppKey",
        keyPlaceholder: "showapi appKey",
        keyUrl: "https://www.showapi.com/apiGateway/view/3262/1",
        docsUrl: "https://www.showapi.com/apiGateway/view/3262/1",
        hint: "Usage-based billing with some free quota on selected plans; good low-barrier option."
      },
      gugudata: {
        id: "gugudata",
        name: "Gugudata",
        keyField: "gugudataAppKey",
        keyLabel: "Gugudata AppKey",
        keyPlaceholder: "gugudata appkey",
        keyUrl: "https://www.gugudata.com/api/details/url2markdown",
        docsUrl: "https://www.gugudata.com/api/details/url2markdown",
        hint: "Stable Markdown quality output; official docs recommend rate control."
      }
    };
    var LINK_RESOLVER_DEFAULTS = {
      enabled: true,
      provider: "tianapi",
      providerOrder: [...LINK_RESOLVER_PROVIDER_IDS],
      tianapiKey: "",
      showapiAppKey: "",
      gugudataAppKey: "",
      timeoutMs: 25e3,
      retries: 2,
      maxConcurrency: 2,
      fallbackMode: "ai_then_plain"
    };
    var MOBILE_CAPTURE_DEFAULTS = {
      provider: "deepseek",
      apiKey: "",
      baseUrl: "",
      model: "",
      dailyNotePath: "01-\u6355\u83B7\u5C42/\u6BCF\u65E5\u7B14\u8BB0",
      ideaSectionHeader: "## \u8BB0\u5F55",
      enableAiCleanup: true,
      enableUrlSummary: true,
      linkResolver: { ...LINK_RESOLVER_DEFAULTS }
    };
    function resolveEffectiveLocaleFromSettings2(settings, navigatorLike) {
      const preferred = normalizeUiLanguage2(settings && settings.uiLanguage);
      if (preferred === "auto") {
        return resolveLocaleFromNavigator2(
          navigatorLike || (typeof navigator !== "undefined" ? navigator : null),
          DEFAULT_UI_LOCALE2
        );
      }
      return normalizeSupportedLocale2(preferred, DEFAULT_UI_LOCALE2);
    }
    function normalizeProviderOrder(raw, defaults = LINK_RESOLVER_DEFAULTS.providerOrder) {
      const incoming = Array.isArray(raw) ? raw : String(raw || "").split(/[,\s>，]+/).map((x) => x.trim()).filter(Boolean);
      const normalized = [];
      for (const item of incoming) {
        const id = String(item || "").trim().toLowerCase();
        if (!LINK_RESOLVER_PROVIDER_IDS.includes(id)) continue;
        if (normalized.includes(id)) continue;
        normalized.push(id);
      }
      return normalized.length ? normalized : [...defaults];
    }
    function normalizeResolverProviderId(raw, fallback = LINK_RESOLVER_DEFAULTS.provider) {
      const id = String(raw || "").trim().toLowerCase();
      if (LINK_RESOLVER_PROVIDER_IDS.includes(id)) return id;
      const normalizedFallback = String(fallback || "").trim().toLowerCase();
      if (LINK_RESOLVER_PROVIDER_IDS.includes(normalizedFallback)) return normalizedFallback;
      return LINK_RESOLVER_PROVIDER_IDS[0];
    }
    function getResolverProviderPreset(providerId) {
      return LINK_RESOLVER_PROVIDER_PRESETS[normalizeResolverProviderId(providerId)] || LINK_RESOLVER_PROVIDER_PRESETS[LINK_RESOLVER_DEFAULTS.provider];
    }
    function normalizeLinkResolver(raw) {
      const lr = raw && typeof raw === "object" ? Object.assign({}, LINK_RESOLVER_DEFAULTS, raw) : { ...LINK_RESOLVER_DEFAULTS };
      lr.enabled = typeof lr.enabled === "boolean" ? lr.enabled : LINK_RESOLVER_DEFAULTS.enabled;
      lr.providerOrder = normalizeProviderOrder(lr.providerOrder);
      lr.provider = normalizeResolverProviderId(lr.provider, lr.providerOrder[0]);
      lr.tianapiKey = String(lr.tianapiKey || "").trim();
      lr.showapiAppKey = String(lr.showapiAppKey || "").trim();
      lr.gugudataAppKey = String(lr.gugudataAppKey || "").trim();
      lr.timeoutMs = Math.max(5e3, Number(lr.timeoutMs) || LINK_RESOLVER_DEFAULTS.timeoutMs);
      lr.retries = Math.min(5, Math.max(0, Number.isFinite(Number(lr.retries)) ? Number(lr.retries) : LINK_RESOLVER_DEFAULTS.retries));
      lr.maxConcurrency = Math.min(
        5,
        Math.max(1, Number.isFinite(Number(lr.maxConcurrency)) ? Number(lr.maxConcurrency) : LINK_RESOLVER_DEFAULTS.maxConcurrency)
      );
      lr.fallbackMode = lr.fallbackMode === "ai_then_plain" ? "ai_then_plain" : LINK_RESOLVER_DEFAULTS.fallbackMode;
      return lr;
    }
    function defaultDailyNotePathByLocale(locale) {
      const normalized = normalizeSupportedLocale2(locale, DEFAULT_UI_LOCALE2);
      return normalized === "zh-CN" ? "01-\u6355\u83B7\u5C42/\u6BCF\u65E5\u7B14\u8BB0" : "01-Capture/Daily Notes";
    }
    function defaultIdeaSectionHeaderByLocale(locale) {
      const normalized = normalizeSupportedLocale2(locale, DEFAULT_UI_LOCALE2);
      return normalized === "zh-CN" ? "## \u8BB0\u5F55" : "## Records";
    }
    function normalizeMobileSettings(raw) {
      const data = raw && typeof raw === "object" ? { ...raw } : {};
      data.uiLanguage = normalizeUiLanguage2(data.uiLanguage);
      const locale = resolveEffectiveLocaleFromSettings2(data);
      const mcDefaults = {
        ...MOBILE_CAPTURE_DEFAULTS,
        dailyNotePath: defaultDailyNotePathByLocale(locale),
        ideaSectionHeader: defaultIdeaSectionHeaderByLocale(locale)
      };
      if (!data.mobileCapture || typeof data.mobileCapture !== "object") {
        data.mobileCapture = { ...mcDefaults };
      } else {
        data.mobileCapture = Object.assign({}, mcDefaults, data.mobileCapture);
      }
      const mc = data.mobileCapture;
      mc.provider = String(mc.provider || mcDefaults.provider).trim();
      mc.apiKey = String(mc.apiKey || "").trim();
      mc.baseUrl = String(mc.baseUrl || "").trim();
      mc.model = String(mc.model || "").trim();
      mc.dailyNotePath = String(mc.dailyNotePath || mcDefaults.dailyNotePath).trim();
      if (mc.dailyNotePath === "01-\u6355\u83B7\u5C42/\u8BB0\u5F55") mc.dailyNotePath = "01-\u6355\u83B7\u5C42/\u6BCF\u65E5\u7B14\u8BB0";
      if (mc.dailyNotePath === "01-Capture/Records") mc.dailyNotePath = "01-Capture/Daily Notes";
      mc.ideaSectionHeader = String(mc.ideaSectionHeader || mcDefaults.ideaSectionHeader).trim();
      mc.enableAiCleanup = typeof mc.enableAiCleanup === "boolean" ? mc.enableAiCleanup : mcDefaults.enableAiCleanup;
      mc.enableUrlSummary = typeof mc.enableUrlSummary === "boolean" ? mc.enableUrlSummary : mcDefaults.enableUrlSummary;
      mc.linkResolver = normalizeLinkResolver(mc.linkResolver);
      return data;
    }
    function getResolverProviderKey(linkResolver, providerId) {
      if (providerId === "tianapi") return String(linkResolver.tianapiKey || "").trim();
      if (providerId === "showapi") return String(linkResolver.showapiAppKey || "").trim();
      if (providerId === "gugudata") return String(linkResolver.gugudataAppKey || "").trim();
      return "";
    }
    function setResolverProviderKey(linkResolver, providerId, nextValue) {
      const value = String(nextValue || "").trim();
      if (providerId === "tianapi") {
        linkResolver.tianapiKey = value;
        return;
      }
      if (providerId === "showapi") {
        linkResolver.showapiAppKey = value;
        return;
      }
      if (providerId === "gugudata") {
        linkResolver.gugudataAppKey = value;
      }
    }
    module2.exports = {
      LINK_RESOLVER_PROVIDER_IDS,
      LINK_RESOLVER_PROVIDER_PRESETS,
      LINK_RESOLVER_DEFAULTS,
      MOBILE_CAPTURE_DEFAULTS,
      resolveEffectiveLocaleFromSettings: resolveEffectiveLocaleFromSettings2,
      normalizeProviderOrder,
      normalizeResolverProviderId,
      getResolverProviderPreset,
      normalizeLinkResolver,
      defaultDailyNotePathByLocale,
      defaultIdeaSectionHeaderByLocale,
      normalizeMobileSettings,
      getResolverProviderKey,
      setResolverProviderKey
    };
  }
});

// runtime/mobile/mobile-url-summary-service.js
var require_mobile_url_summary_service = __commonJS({
  "runtime/mobile/mobile-url-summary-service.js"(exports2, module2) {
    var { requestUrl: requestUrl2 } = require("obsidian");
    var { normalizeSupportedLocale: normalizeSupportedLocale2 } = require_i18n_locale_utils();
    var {
      hasAiConfig,
      resolveAiConfig,
      summarizeTextWithAi,
      pickFirstText
    } = require_mobile_ai_service();
    var {
      LINK_RESOLVER_PROVIDER_IDS,
      normalizeLinkResolver,
      getResolverProviderPreset,
      getResolverProviderKey
    } = require_mobile_settings_utils();
    var URL_REGEX = /https?:\/\/[^\s)\]>，。！？]+/g;
    var URL_TRAILING_ASCII_PUNCTUATION_REGEX = /[.,;:!?]+$/;
    var LOCALE_MESSAGES = {
      "zh-CN": {
        errors: {
          resolverBodyEmpty: "\u6B63\u6587\u4E3A\u7A7A",
          resolverUnsupported: "\u4E0D\u652F\u6301\u7684\u89E3\u6790\u670D\u52A1: {providerId}",
          resolverInvalidJson: "\u54CD\u5E94\u4E0D\u662F\u6709\u6548 JSON",
          resolverFailed: "\u89E3\u6790\u5931\u8D25",
          resolverFailedGeneral: "\u94FE\u63A5\u89E3\u6790\u5931\u8D25",
          resolverRateLimited: "\u9891\u7387\u53D7\u9650",
          resolverTimeout: "\u670D\u52A1\u8D85\u65F6",
          resolverUnavailable: "\u670D\u52A1\u4E0D\u53EF\u7528"
        },
        url: {
          statusProviderMissing: "\u26A0\uFE0F {providerName} key missing, fallback to AI",
          statusNoResolverOrAi: "\u26A0\uFE0F Resolver and AI not configured, fallback to plain text",
          statusAiSummary: "\u{1F916} Generating URL summary...",
          statusFallbackAi: "\u26A0\uFE0F {hint}, fallback to AI",
          statusFallbackPlain: "\u26A0\uFE0F {hint}, fallback to plain text",
          statusResolverNoAi: "\u26A0\uFE0F URL resolved but AI not configured, fallback to plain text",
          statusAiSummaryFailed: "\u26A0\uFE0F AI summary failed, fallback to plain text",
          statusPartialResolverFailed: "\u26A0\uFE0F Some URLs failed, partial result applied"
        },
        parser: {
          summaryFallback: "\u6682\u65E0\u6CD5\u89E3\u6790\uFF0C\u5DF2\u4FDD\u7559\u539F\u59CB\u94FE\u63A5",
          untitled: "\uFF08\u672A\u547D\u540D\uFF09"
        }
      },
      en: {
        errors: {
          resolverBodyEmpty: "Response body is empty",
          resolverUnsupported: "Unsupported resolver provider: {providerId}",
          resolverInvalidJson: "Response is not valid JSON",
          resolverFailed: "Resolver failed",
          resolverFailedGeneral: "URL resolver failed",
          resolverRateLimited: "Rate limited",
          resolverTimeout: "Resolver timeout",
          resolverUnavailable: "Resolver unavailable"
        },
        url: {
          statusProviderMissing: "\u26A0\uFE0F {providerName} key missing, fallback to AI",
          statusNoResolverOrAi: "\u26A0\uFE0F Resolver and AI not configured, fallback to plain text",
          statusAiSummary: "\u{1F916} Generating URL summary...",
          statusFallbackAi: "\u26A0\uFE0F {hint}, fallback to AI",
          statusFallbackPlain: "\u26A0\uFE0F {hint}, fallback to plain text",
          statusResolverNoAi: "\u26A0\uFE0F URL resolved but AI not configured, fallback to plain text",
          statusAiSummaryFailed: "\u26A0\uFE0F AI summary failed, fallback to plain text",
          statusPartialResolverFailed: "\u26A0\uFE0F Some URLs failed, partial result applied"
        },
        parser: {
          summaryFallback: "Unable to resolve, original URL preserved",
          untitled: "(Untitled)"
        }
      }
    };
    function isZh(locale) {
      return normalizeSupportedLocale2(locale) === "zh-CN";
    }
    function resolveLocalePack(locale) {
      return isZh(locale) ? LOCALE_MESSAGES["zh-CN"] : LOCALE_MESSAGES.en;
    }
    function interpolate(message, params = {}) {
      return String(message || "").replace(/\{([a-zA-Z0-9_]+)\}/g, (_m, key) => {
        if (!Object.prototype.hasOwnProperty.call(params, key)) return "";
        const value = params[key];
        return value === void 0 || value === null ? "" : String(value);
      });
    }
    function t(locale, path, params = {}) {
      const pack = resolveLocalePack(locale);
      const keys = String(path || "").split(".");
      let cursor = pack;
      for (const key of keys) {
        if (!cursor || typeof cursor !== "object" || !(key in cursor)) return path;
        cursor = cursor[key];
      }
      return interpolate(cursor, params);
    }
    function inferTitleFromUrl(url) {
      try {
        const parsed = new URL(String(url || ""));
        return String(parsed.hostname || "").replace(/^www\./i, "").trim();
      } catch (_e) {
        return "";
      }
    }
    function safeJsonParse(rawText) {
      if (typeof rawText !== "string" || !rawText.trim()) return null;
      try {
        return JSON.parse(rawText);
      } catch (_e) {
        return null;
      }
    }
    function getResponsePayload(response) {
      if (response && response.json && typeof response.json === "object") return response.json;
      return safeJsonParse(response && response.text ? response.text : "");
    }
    function normalizeResolverText(input, maxLen = 2400) {
      const text = typeof input === "string" ? input.trim() : "";
      return text ? text.slice(0, maxLen) : "";
    }
    function stripTrailingUrlPunctuation(rawUrl) {
      return String(rawUrl || "").trim().replace(URL_TRAILING_ASCII_PUNCTUATION_REGEX, "");
    }
    function extractUrlsFromText(text) {
      const matches = String(text || "").match(URL_REGEX) || [];
      const seen = /* @__PURE__ */ new Set();
      const urls = [];
      for (const raw of matches) {
        const cleaned = stripTrailingUrlPunctuation(raw);
        if (!cleaned || seen.has(cleaned)) continue;
        let parsed;
        try {
          parsed = new URL(cleaned);
        } catch (_e) {
          continue;
        }
        const protocol = String(parsed.protocol || "").toLowerCase();
        if (protocol !== "http:" && protocol !== "https:") continue;
        seen.add(cleaned);
        urls.push(cleaned);
      }
      return urls;
    }
    function ensureUrlsPreserved(originalText, candidateText) {
      const output = String(candidateText || "").trim();
      if (!output) return "";
      const urls = extractUrlsFromText(originalText);
      for (const url of urls) {
        if (!output.includes(url)) return "";
      }
      return output;
    }
    function appendLinesToText(text, lines) {
      const clean = Array.isArray(lines) ? lines.map((line) => String(line || "").trim()).filter(Boolean) : [];
      if (!clean.length) return String(text || "");
      const base = String(text || "").trimEnd();
      return `${base}

${clean.join("\n")}`;
    }
    function truncatePlainSummary(body, maxLen = 50, locale = "zh-CN") {
      const normalized = String(body || "").replace(/\s+/g, " ").replace(/[#>*`[\]_]/g, "").trim();
      if (!normalized) return t(locale, "parser.summaryFallback");
      return normalized.length > maxLen ? `${normalized.slice(0, maxLen - 1)}\u2026` : normalized;
    }
    function buildResolverSummaryLines(urlContents, failedUrls, locale = "zh-CN") {
      const lines = [];
      for (const item of urlContents || []) {
        lines.push(`> \u{1F4CE} ${item.url} - ${truncatePlainSummary(item.body || item.title || "", 50, locale)}`);
      }
      for (const url of failedUrls || []) {
        lines.push(`> \u{1F4CE} ${url} - ${t(locale, "parser.summaryFallback")}`);
      }
      return lines;
    }
    async function sleepMs(ms) {
      await new Promise((resolve) => setTimeout(resolve, Math.max(0, Number(ms) || 0)));
    }
    async function mapWithConcurrency(items, limit, mapper) {
      const output = new Array(items.length);
      const workerCount = Math.max(1, Math.min(items.length, Number(limit) || 1));
      let cursor = 0;
      const workers = Array.from({ length: workerCount }, async () => {
        while (true) {
          const idx = cursor++;
          if (idx >= items.length) return;
          output[idx] = await mapper(items[idx], idx);
        }
      });
      await Promise.all(workers);
      return output;
    }
    function parseTianapiPayload(payload, locale = "zh-CN") {
      const code = Number(payload && payload.code);
      if (Number.isFinite(code) && code !== 200) {
        return { ok: false, error: pickFirstText([payload && payload.msg, payload && payload.message]) || `code=${code}` };
      }
      const result = payload && payload.result && typeof payload.result === "object" ? payload.result : {};
      const title = pickFirstText([result.title, result.name, payload && payload.title]);
      const body = normalizeResolverText(
        pickFirstText([result.content, result.text, result.desc, result.markdown, payload && payload.content, payload && payload.text])
      );
      if (!body) return { ok: false, error: t(locale, "errors.resolverBodyEmpty") };
      return { ok: true, title: title || inferTitleFromUrl(result.url || payload && payload.url), body };
    }
    function parseShowapiPayload(payload, locale = "zh-CN") {
      const code = Number(payload && payload.showapi_res_code);
      if (Number.isFinite(code) && code !== 0) {
        return { ok: false, error: pickFirstText([payload && payload.showapi_res_error, payload && payload.message]) || `code=${code}` };
      }
      const bodyPayload = payload && payload.showapi_res_body && typeof payload.showapi_res_body === "object" ? payload.showapi_res_body : {};
      const title = pickFirstText([bodyPayload.title, payload && payload.title]);
      const body = normalizeResolverText(
        pickFirstText([bodyPayload.output, bodyPayload.content, bodyPayload.text, bodyPayload.markdown])
      );
      if (!body) return { ok: false, error: t(locale, "errors.resolverBodyEmpty") };
      return { ok: true, title: title || inferTitleFromUrl(bodyPayload.url || payload && payload.url), body };
    }
    function parseGugudataPayload(payload, locale = "zh-CN") {
      const codeRaw = payload && (payload.code ?? payload.status ?? payload.errCode ?? payload.errcode);
      if (codeRaw !== void 0 && codeRaw !== null && codeRaw !== "") {
        const code = String(codeRaw).trim().toLowerCase();
        const success = code === "0" || code === "200" || code === "ok" || code === "success" || code === "true";
        if (!success) {
          return { ok: false, error: pickFirstText([payload && payload.msg, payload && payload.message, payload && payload.error]) || `code=${code}` };
        }
      }
      const dataObj = payload && payload.data && typeof payload.data === "object" ? payload.data : payload && payload.result && typeof payload.result === "object" ? payload.result : payload && payload.Data && typeof payload.Data === "object" ? payload.Data : {};
      const title = pickFirstText([dataObj.title, dataObj.name, payload && payload.title]);
      const body = normalizeResolverText(
        pickFirstText([
          dataObj.markdown,
          dataObj.content,
          dataObj.text,
          dataObj.desc,
          payload && payload.markdown,
          payload && payload.content,
          payload && payload.text
        ])
      );
      if (!body) return { ok: false, error: t(locale, "errors.resolverBodyEmpty") };
      return { ok: true, title: title || inferTitleFromUrl(dataObj.url || payload && payload.url), body };
    }
    async function resolveUrlWithProvider(url, providerId, appKey, timeoutMs, locale = "zh-CN") {
      const pid = String(providerId || "").trim().toLowerCase();
      if (!LINK_RESOLVER_PROVIDER_IDS.includes(pid)) {
        return { ok: false, status: 0, error: t(locale, "errors.resolverUnsupported", { providerId: pid }) };
      }
      let request;
      if (pid === "tianapi") {
        request = {
          url: `https://apis.tianapi.com/htmltext/index?key=${encodeURIComponent(appKey)}&url=${encodeURIComponent(url)}`,
          method: "GET"
        };
      } else if (pid === "showapi") {
        request = {
          url: "https://route.showapi.com/3262-1",
          method: "POST",
          headers: { "Content-Type": "application/x-www-form-urlencoded" },
          body: `showapi_appid=2409036&showapi_sign=${encodeURIComponent(appKey)}&url=${encodeURIComponent(url)}`
        };
      } else {
        request = {
          url: "https://api.gugudata.com/url2md",
          method: "POST",
          headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${appKey}`
          },
          body: JSON.stringify({ url })
        };
      }
      let response;
      try {
        response = await requestUrl2({
          ...request,
          throw: false,
          timeout: timeoutMs
        });
      } catch (error) {
        return {
          ok: false,
          status: 0,
          error: error instanceof Error ? error.message : String(error)
        };
      }
      const payload = getResponsePayload(response);
      if (!payload || typeof payload !== "object") {
        if (response.status !== 200) {
          return {
            ok: false,
            status: response.status,
            error: String(response.text || "").slice(0, 200)
          };
        }
        return { ok: false, status: response.status, error: t(locale, "errors.resolverInvalidJson") };
      }
      let parsed;
      if (pid === "tianapi") parsed = parseTianapiPayload(payload, locale);
      else if (pid === "showapi") parsed = parseShowapiPayload(payload, locale);
      else parsed = parseGugudataPayload(payload, locale);
      if (!parsed.ok) {
        return {
          ok: false,
          status: response.status,
          error: parsed.error,
          payload
        };
      }
      return {
        ok: true,
        status: response.status,
        title: parsed.title,
        body: parsed.body,
        payload
      };
    }
    async function resolveUrlsWithContextByResolver(text, mcSettings, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      const urlList = extractUrlsFromText(text);
      if (!urlList.length) return { urls: [], failures: [] };
      const mc = mcSettings && typeof mcSettings === "object" ? mcSettings : {};
      const linkResolver = normalizeLinkResolver(mc.linkResolver);
      if (!linkResolver.enabled) return { urls: [], failures: [] };
      const providerOrder = Array.isArray(linkResolver.providerOrder) && linkResolver.providerOrder.length ? linkResolver.providerOrder : [linkResolver.provider || LINK_RESOLVER_PROVIDER_IDS[0]];
      const timeoutMs = Math.max(5e3, Number(linkResolver.timeoutMs) || 25e3);
      const retries = Math.max(0, Math.min(5, Number(linkResolver.retries) || 2));
      const concurrency = Math.max(1, Math.min(5, Number(linkResolver.maxConcurrency) || 2));
      const results = await mapWithConcurrency(urlList, concurrency, async (url) => {
        for (const providerId of providerOrder) {
          const key = getResolverProviderKey(linkResolver, providerId);
          if (!key) continue;
          for (let attempt = 0; attempt <= retries; attempt += 1) {
            const result = await resolveUrlWithProvider(url, providerId, key, timeoutMs, locale);
            if (result.ok) {
              return {
                ok: true,
                provider: providerId,
                url,
                title: result.title,
                body: result.body
              };
            }
            const errorText = String(result.error || "").toLowerCase();
            const shouldRetry = attempt < retries && /timeout|timed out|network|ecconn|econn|socket|429|rate/i.test(errorText);
            if (shouldRetry) {
              await sleepMs(200 * (attempt + 1));
              continue;
            }
            if (attempt >= retries) {
              return {
                ok: false,
                provider: providerId,
                url,
                error: String(result.error || t(locale, "errors.resolverFailed"))
              };
            }
          }
        }
        return {
          ok: false,
          provider: providerOrder[0] || "",
          url,
          error: t(locale, "errors.resolverFailed")
        };
      });
      const urls = [];
      const failures = [];
      for (const result of results) {
        if (result && result.ok) urls.push(result);
        else if (result) failures.push(result);
      }
      return { urls, failures };
    }
    function buildResolverFailureHint(failures, locale = "zh-CN") {
      if (!Array.isArray(failures) || failures.length === 0) return t(locale, "errors.resolverFailedGeneral");
      const text = failures.map((item) => String(item && item.error ? item.error : "").toLowerCase()).join(" ");
      if (/(429|rate|limit|quota|频率|限流)/i.test(text)) return t(locale, "errors.resolverRateLimited");
      if (/(timeout|timed out|超时)/i.test(text)) return t(locale, "errors.resolverTimeout");
      return t(locale, "errors.resolverUnavailable");
    }
    async function enrichUrlsWithContextByAi(text, urlContents, mcSettings, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      if (!urlContents.length) return "";
      const context = urlContents.map((item) => {
        if (isZh(locale)) {
          return `--- URL: ${item.url} ---
\u6807\u9898: ${item.title || t(locale, "parser.untitled")}
\u5185\u5BB9\u6458\u8981:
${item.body}`;
        }
        return `--- URL: ${item.url} ---
Title: ${item.title || t(locale, "parser.untitled")}
Summary:
${item.body}`;
      }).join("\n\n");
      const prompt = `${text}

${context}`;
      let aiText = "";
      try {
        aiText = await summarizeTextWithAi(prompt, mcSettings, { locale, useFallbackPrompt: false });
      } catch (_error) {
        return "";
      }
      const preserved = ensureUrlsPreserved(text, aiText);
      return preserved || "";
    }
    async function enrichUrlsInText(text, mcSettings, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      const onStatus = typeof options.onStatus === "function" ? options.onStatus : () => {
      };
      const urls = extractUrlsFromText(text);
      if (!urls.length) return { text, statusHint: "" };
      const mc = mcSettings && typeof mcSettings === "object" ? mcSettings : {};
      const linkResolver = normalizeLinkResolver(mc.linkResolver);
      const aiReady = hasAiConfig(mc);
      if (!linkResolver.enabled && !aiReady) {
        return { text, statusHint: t(locale, "url.statusNoResolverOrAi") };
      }
      const selectedProviderId = String(linkResolver.provider || LINK_RESOLVER_PROVIDER_IDS[0]).trim().toLowerCase();
      const selectedProviderPreset = getResolverProviderPreset(selectedProviderId);
      const selectedProviderKey = getResolverProviderKey(linkResolver, selectedProviderId);
      const selectedProviderName = selectedProviderPreset.name;
      if (!selectedProviderKey) {
        if (aiReady) {
          const hint = t(locale, "url.statusProviderMissing", { providerName: selectedProviderName });
          const fallbackLines = buildResolverSummaryLines([], urls, locale);
          onStatus(hint);
          const aiText = await summarizeTextWithAi(
            appendLinesToText(text, fallbackLines),
            mc,
            { locale, useFallbackPrompt: true }
          ).catch(() => "");
          const preserved = ensureUrlsPreserved(text, aiText);
          if (preserved) return { text: preserved, statusHint: "" };
          return { text: appendLinesToText(text, fallbackLines), statusHint: hint };
        }
        return { text, statusHint: t(locale, "url.statusNoResolverOrAi") };
      }
      onStatus(t(locale, "url.statusAiSummary"));
      const { urls: resolvedUrls, failures } = await resolveUrlsWithContextByResolver(text, mc, { locale });
      if (!resolvedUrls.length) {
        const failureHint = buildResolverFailureHint(failures, locale);
        if (aiReady) {
          const hint = t(locale, "url.statusFallbackAi", { hint: failureHint });
          const fallbackLines = buildResolverSummaryLines([], urls, locale);
          const aiText = await summarizeTextWithAi(
            appendLinesToText(text, fallbackLines),
            mc,
            { locale, useFallbackPrompt: true }
          ).catch(() => "");
          const preserved = ensureUrlsPreserved(text, aiText);
          if (preserved) return { text: preserved, statusHint: "" };
          return { text: appendLinesToText(text, fallbackLines), statusHint: hint };
        }
        return {
          text: appendLinesToText(text, buildResolverSummaryLines([], urls, locale)),
          statusHint: t(locale, "url.statusFallbackPlain", { hint: failureHint })
        };
      }
      const failedUrls = failures.map((item) => item.url).filter(Boolean);
      if (!aiReady) {
        return {
          text: appendLinesToText(text, buildResolverSummaryLines(resolvedUrls, failedUrls, locale)),
          statusHint: t(locale, "url.statusResolverNoAi")
        };
      }
      const summarized = await enrichUrlsWithContextByAi(text, resolvedUrls, mc, { locale });
      if (!summarized) {
        return {
          text: appendLinesToText(text, buildResolverSummaryLines(resolvedUrls, failedUrls, locale)),
          statusHint: t(locale, "url.statusAiSummaryFailed")
        };
      }
      if (failures.length > 0) {
        return {
          text: appendLinesToText(summarized, buildResolverSummaryLines([], failedUrls, locale)),
          statusHint: t(locale, "url.statusPartialResolverFailed")
        };
      }
      return { text: summarized, statusHint: "" };
    }
    module2.exports = {
      extractUrlsFromText,
      stripTrailingUrlPunctuation,
      ensureUrlsPreserved,
      appendLinesToText,
      buildResolverSummaryLines,
      parseTianapiPayload,
      parseShowapiPayload,
      parseGugudataPayload,
      resolveUrlWithProvider,
      resolveUrlsWithContextByResolver,
      buildResolverFailureHint,
      enrichUrlsWithContextByAi,
      enrichUrlsInText
    };
  }
});

// runtime/mobile/daily-note-service.js
var require_daily_note_service = __commonJS({
  "runtime/mobile/daily-note-service.js"(exports2, module2) {
    var { normalizePath: normalizePath2 } = require("obsidian");
    var { normalizeSupportedLocale: normalizeSupportedLocale2 } = require_i18n_locale_utils();
    var URL_REGEX = /https?:\/\/[^\s)\]>，。！？]+/g;
    var URL_TRAILING_ASCII_PUNCTUATION_REGEX = /[.,;:!?]+$/;
    var URL_SUMMARY_LINE_REGEX = /^\s*>\s*📎\s*(https?:\/\/\S+|原始URL|OriginalURL)\s*-\s*(.+?)\s*$/i;
    var INLINE_URL_SUMMARY_REGEX = />\s*📎\s*(https?:\/\/\S+|原始URL|OriginalURL)\s*-\s*(.+?)\s*$/i;
    var DEFAULT_SKILLS_DIR = ".opencode/skills";
    var DAILY_NOTE_TEMPLATE = `---
\u521B\u5EFA\u65F6\u95F4: {{date}}
\u7C7B\u578B: \u6BCF\u65E5\u7B14\u8BB0
---

# {{date}}

## \u4ECA\u5929\u6700\u91CD\u8981\u7684\u4E8B

## \u4EFB\u52A1

## \u8BB0\u5F55

## \u665A\u95F4\u56DE\u987E

## \u660E\u65E5\u8BA1\u5212
`;
    var DAILY_NOTE_TEMPLATE_EN = `---
Created: {{date}}
Type: Daily Note
---

# {{date}}

## Most Important Today

## Tasks

## Records

## Evening Review

## Tomorrow Plan
`;
    function isZh(locale) {
      return normalizeSupportedLocale2(locale) === "zh-CN";
    }
    function summaryFallback(locale) {
      return isZh(locale) ? "\u6682\u65E0\u6CD5\u89E3\u6790\uFF0C\u5DF2\u4FDD\u7559\u539F\u59CB\u94FE\u63A5" : "Unable to resolve, original URL preserved";
    }
    function summaryPrefix(locale) {
      return isZh(locale) ? "\u94FE\u63A5\u6458\u8981" : "URL Summary";
    }
    function emptyValue(locale) {
      return isZh(locale) ? "\uFF08\u7A7A\uFF09" : "(Empty)";
    }
    function originalPrefix(locale) {
      return isZh(locale) ? "\u539F\u6587" : "Original";
    }
    function linkLabel(index, locale) {
      return isZh(locale) ? `\u94FE\u63A5${index}` : `Link ${index}`;
    }
    function recordHeading(locale) {
      return isZh(locale) ? "## \u8BB0\u5F55" : "## Records";
    }
    function normalizeHeadingForCompare(value) {
      return String(value || "").replace(/\s+/g, " ").trim().toLowerCase();
    }
    function isRecordSectionHeading(value) {
      const normalized = normalizeHeadingForCompare(value);
      if (!normalized) return false;
      return [
        "## \u8BB0\u5F55",
        "## \u4ECA\u65E5\u8BB0\u5F55",
        "## \u{1F4DD} \u4ECA\u65E5\u8BB0\u5F55",
        "## records",
        "## today notes",
        "## \u{1F4DD} today notes"
      ].includes(normalized);
    }
    function getDailyNoteTemplate(locale) {
      return isZh(locale) ? DAILY_NOTE_TEMPLATE : DAILY_NOTE_TEMPLATE_EN;
    }
    function getWeekdayByDateStr(dateStr) {
      const match = String(dateStr || "").match(/^(\d{4})-(\d{2})-(\d{2})$/);
      if (!match) return null;
      const year = Number(match[1]);
      const month = Number(match[2]);
      const day = Number(match[3]);
      if (!Number.isFinite(year) || !Number.isFinite(month) || !Number.isFinite(day)) return null;
      const date = new Date(year, month - 1, day);
      if (Number.isNaN(date.getTime())) return null;
      return date.getDay();
    }
    function renderDailyNoteTemplate(template, dateStr, locale = "zh-CN") {
      const localeCode = normalizeSupportedLocale2(locale || "en");
      const date = String(dateStr || "");
      let rendered = String(template || "");
      if (!rendered.trim()) return rendered;
      rendered = rendered.replace(/\{\{\s*date\s*\}\}/gi, date);
      rendered = rendered.replace(/YYYY-MM-DD/g, date);
      const weekday = getWeekdayByDateStr(date);
      if (weekday !== null) {
        const zhWeekdays = ["\u65E5", "\u4E00", "\u4E8C", "\u4E09", "\u56DB", "\u4E94", "\u516D"];
        const enWeekdays = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
        const weekdayZh = zhWeekdays[weekday] || "X";
        const weekdayEn = enWeekdays[weekday] || "";
        rendered = rendered.replace(/星期[一二三四五六日天Xx]/g, `\u661F\u671F${weekdayZh}`);
        rendered = rendered.replace(/\bWeekday\b/g, localeCode === "zh-CN" ? `\u661F\u671F${weekdayZh}` : weekdayEn);
      }
      return rendered;
    }
    function localizedTemplateCandidates(basePath, locale = "en") {
      const normalizedLocale = normalizeSupportedLocale2(locale || "en");
      const canonicalBase = normalizePath2(String(basePath || ""));
      if (!canonicalBase || !canonicalBase.endsWith(".md")) return [];
      const suffixless = canonicalBase.slice(0, -".md".length);
      const tokenOrder = normalizedLocale === "zh-CN" ? ["zh-CN", "base", "en"] : ["en", "base", "zh-CN"];
      const candidates = [];
      for (const token of tokenOrder) {
        if (token === "base") candidates.push(`${suffixless}.md`);
        else candidates.push(`${suffixless}.${token}.md`);
      }
      return [...new Set(candidates.map((value) => normalizePath2(value)).filter(Boolean))];
    }
    async function readVaultTextByPath(vault, path) {
      const targetPath = normalizePath2(String(path || ""));
      if (!targetPath || !vault) return "";
      const adapter = vault.adapter;
      if (adapter && typeof adapter.exists === "function" && typeof adapter.read === "function") {
        try {
          if (await adapter.exists(targetPath)) {
            const raw = await adapter.read(targetPath);
            return String(raw || "");
          }
        } catch {
        }
      }
      if (typeof vault.getAbstractFileByPath === "function" && typeof vault.read === "function") {
        try {
          const file = vault.getAbstractFileByPath(targetPath);
          if (file) {
            const raw = await vault.read(file);
            return String(raw || "");
          }
        } catch {
        }
      }
      return "";
    }
    async function resolveSkillDailyNoteTemplate(vault, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      const skillsDir = normalizePath2(String(options.skillsDir || DEFAULT_SKILLS_DIR).trim() || DEFAULT_SKILLS_DIR);
      const candidates = [
        ...localizedTemplateCandidates(`${skillsDir}/ah-note/assets/\u6BCF\u65E5\u7B14\u8BB0\u6A21\u677F.md`, locale),
        ...localizedTemplateCandidates(`${skillsDir}/ah-note/assets/Daily-Note-Template.md`, locale),
        // Compatibility fallback for template-map targets that may use assets/templates/.
        ...localizedTemplateCandidates(`${skillsDir}/ah-note/assets/templates/\u6BCF\u65E5\u7B14\u8BB0\u6A21\u677F.md`, locale),
        ...localizedTemplateCandidates(`${skillsDir}/ah-note/assets/templates/Daily-Note-Template.md`, locale)
      ];
      for (const candidate of [...new Set(candidates)]) {
        const text = await readVaultTextByPath(vault, candidate);
        if (String(text || "").trim()) return text;
      }
      return "";
    }
    function formatDateStr(date) {
      const d = date || /* @__PURE__ */ new Date();
      const y = d.getFullYear();
      const m = String(d.getMonth() + 1).padStart(2, "0");
      const day = String(d.getDate()).padStart(2, "0");
      return `${y}-${m}-${day}`;
    }
    function formatTimeStr(date) {
      const d = date || /* @__PURE__ */ new Date();
      const h = String(d.getHours()).padStart(2, "0");
      const m = String(d.getMinutes()).padStart(2, "0");
      return `${h}:${m}`;
    }
    function normalizeSingleLine(text, fallback = "") {
      const normalized = String(text || "").replace(/\s+/g, " ").trim();
      return normalized || fallback;
    }
    function stripTrailingUrlPunctuation(rawUrl) {
      return String(rawUrl || "").trim().replace(URL_TRAILING_ASCII_PUNCTUATION_REGEX, "");
    }
    function extractUrlsFromText(text) {
      const matches = String(text || "").match(URL_REGEX) || [];
      const seen = /* @__PURE__ */ new Set();
      const urls = [];
      for (const raw of matches) {
        const cleaned = stripTrailingUrlPunctuation(raw);
        if (!cleaned || seen.has(cleaned)) continue;
        try {
          const parsed = new URL(cleaned);
          const protocol = String(parsed.protocol || "").toLowerCase();
          if (protocol !== "http:" && protocol !== "https:") continue;
        } catch (_e) {
          continue;
        }
        seen.add(cleaned);
        urls.push(cleaned);
      }
      return urls;
    }
    function parseSummaryItemFromMatch(match, linePrefix = "", locale = "zh-CN") {
      const rawTarget = String(match && match[1] ? match[1] : "").trim();
      const summary = normalizeSingleLine(match && match[2] ? match[2] : "", summaryFallback(locale));
      const isPlaceholder = /^(原始url|originalurl)$/i.test(rawTarget);
      const directUrl = isPlaceholder ? "" : stripTrailingUrlPunctuation(rawTarget);
      const hints = extractUrlsFromText(String(linePrefix || ""));
      const urlHint = hints.length ? hints[hints.length - 1] : "";
      return {
        url: directUrl,
        urlHint,
        summary,
        hasSummary: true
      };
    }
    function inferTitleFromUrl(url) {
      try {
        const parsed = new URL(String(url || ""));
        return String(parsed.hostname || "").replace(/^www\./i, "").trim();
      } catch (_e) {
        return "";
      }
    }
    function parseCaptureTextSections(text, locale = "zh-CN") {
      const raw = String(text || "").replace(/\r\n?/g, "\n");
      const lines = raw.split("\n");
      const bodyLines = [];
      const summaryItems = [];
      for (const line of lines) {
        const pureMatch = line.match(URL_SUMMARY_LINE_REGEX);
        if (pureMatch) {
          summaryItems.push(parseSummaryItemFromMatch(pureMatch, "", locale));
          continue;
        }
        const inlineMatch = line.match(INLINE_URL_SUMMARY_REGEX);
        if (inlineMatch) {
          const markerStart = line.search(/>\s*📎\s*(https?:\/\/\S+|原始URL|OriginalURL)\s*-\s*/i);
          const prefix = markerStart >= 0 ? line.slice(0, markerStart).trimEnd() : String(line || "").trimEnd();
          if (prefix.trim()) bodyLines.push(prefix);
          summaryItems.push(parseSummaryItemFromMatch(inlineMatch, prefix, locale));
          continue;
        }
        bodyLines.push(line);
      }
      const body = bodyLines.join("\n").trim();
      const orderedUrls = extractUrlsFromText(body);
      const resolvedSummaryItems = [];
      const summaryByUrl = /* @__PURE__ */ new Map();
      for (const item of summaryItems) {
        let targetUrl = String(item && item.url ? item.url : "").trim();
        if (!targetUrl) {
          const hint = String(item && item.urlHint ? item.urlHint : "").trim();
          if (hint && orderedUrls.includes(hint)) {
            targetUrl = hint;
          } else {
            targetUrl = orderedUrls.find((url) => !summaryByUrl.has(url)) || "";
          }
        }
        if (!targetUrl) continue;
        const resolved = { url: targetUrl, summary: item.summary, hasSummary: true };
        resolvedSummaryItems.push(resolved);
        if (!summaryByUrl.has(targetUrl)) summaryByUrl.set(targetUrl, item.summary);
      }
      const resolverItems = [];
      const seen = /* @__PURE__ */ new Set();
      for (const url of orderedUrls) {
        const explicitSummary = summaryByUrl.get(url) || "";
        resolverItems.push({
          url,
          summary: explicitSummary,
          hasSummary: Boolean(explicitSummary)
        });
        seen.add(url);
      }
      for (const item of resolvedSummaryItems) {
        if (seen.has(item.url)) continue;
        resolverItems.push(item);
        seen.add(item.url);
      }
      return {
        body: body || raw.trim(),
        resolverItems
      };
    }
    function normalizeCaptureParagraph(text, locale = "zh-CN") {
      let normalized = String(text || "").replace(/\r\n?/g, "\n").trim();
      if (!normalized) return "";
      const prefix = originalPrefix(locale);
      normalized = normalized.replace(new RegExp(`^(${prefix}|\u539F\u6587|Original)[:\uFF1A]\\s*`, "i"), "");
      normalized = normalized.split("\n").map((line) => line.trim()).filter(Boolean).join(" ");
      return normalized.replace(/\s+/g, " ").trim();
    }
    function formatResolverInlineSummary(resolverItems, locale = "zh-CN") {
      const items = Array.isArray(resolverItems) ? resolverItems.filter((item) => item && item.hasSummary) : [];
      if (!items.length) return "";
      if (items.length === 1) {
        return `(${summaryPrefix(locale)}: ${normalizeSingleLine(items[0].summary, summaryFallback(locale))})`;
      }
      const usedLabels = /* @__PURE__ */ new Map();
      const parts = items.map((item, index) => {
        const base = inferTitleFromUrl(item.url) || linkLabel(index + 1, locale);
        const count = (usedLabels.get(base) || 0) + 1;
        usedLabels.set(base, count);
        const label = count > 1 ? `${base}#${count}` : base;
        return `${label}: ${normalizeSingleLine(item.summary, summaryFallback(locale))}`;
      });
      return `(${summaryPrefix(locale)}: ${parts.join("; ")})`;
    }
    function formatCaptureEntry(timeStr, text, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      const { body, resolverItems } = parseCaptureTextSections(text, locale);
      const paragraph = normalizeCaptureParagraph(body || text, locale) || emptyValue(locale);
      const inlineSummary = formatResolverInlineSummary(resolverItems, locale);
      return `- ${timeStr} ${paragraph}${inlineSummary ? ` ${inlineSummary}` : ""}`;
    }
    async function ensureFolders(vault, filePath) {
      const parts = filePath.split("/");
      parts.pop();
      let current = "";
      for (const part of parts) {
        current = current ? `${current}/${part}` : part;
        const normalized = normalizePath2(current);
        const existing = vault.getAbstractFileByPath(normalized);
        if (!existing) {
          try {
            await vault.createFolder(normalized);
          } catch (_e) {
          }
        }
      }
    }
    async function findOrCreateDailyNote(vault, dailyNotePath, dateStr, options = {}) {
      const locale = normalizeSupportedLocale2(options.locale || "en");
      const date = dateStr || formatDateStr();
      const filePath = normalizePath2(`${dailyNotePath}/${date}.md`);
      const existing = vault.getAbstractFileByPath(filePath);
      if (existing) return existing;
      await ensureFolders(vault, filePath);
      let template = String(options.template || "");
      if (!template.trim()) {
        template = await resolveSkillDailyNoteTemplate(vault, {
          locale,
          skillsDir: options.skillsDir
        });
      }
      if (!template.trim()) template = getDailyNoteTemplate(locale);
      const content = renderDailyNoteTemplate(template, date, locale);
      return await vault.create(filePath, content);
    }
    async function appendToIdeaSection(vault, file, entry, sectionHeader) {
      let content = await vault.read(file);
      const headerIdx = content.indexOf(sectionHeader);
      if (headerIdx !== -1) {
        const afterHeader = headerIdx + sectionHeader.length;
        const restContent = content.slice(afterHeader);
        const nextHeadingMatch = restContent.match(/\n(#{1,6} )/);
        let insertPos;
        if (nextHeadingMatch) {
          insertPos = afterHeader + nextHeadingMatch.index;
        } else {
          insertPos = content.length;
        }
        const sectionContent = content.slice(afterHeader, insertPos);
        const lastDashIdx = sectionContent.lastIndexOf("\n- ");
        if (lastDashIdx !== -1) {
          const lineStart = afterHeader + lastDashIdx + 1;
          const lineEnd = content.indexOf("\n", lineStart + 1);
          let actualEnd = lineEnd === -1 ? content.length : lineEnd;
          while (actualEnd < content.length) {
            const nextLineEnd = content.indexOf("\n", actualEnd + 1);
            const nextLine = content.slice(actualEnd + 1, nextLineEnd === -1 ? content.length : nextLineEnd);
            if (nextLine.startsWith("  >") || nextLine.startsWith("  ") && !nextLine.startsWith("- ")) {
              actualEnd = nextLineEnd === -1 ? content.length : nextLineEnd;
            } else {
              break;
            }
          }
          content = content.slice(0, actualEnd) + "\n" + entry + content.slice(actualEnd);
        } else {
          const headerLineEnd = content.indexOf("\n", headerIdx);
          if (headerLineEnd !== -1) {
            content = content.slice(0, headerLineEnd) + "\n" + entry + content.slice(headerLineEnd);
          } else {
            content = content + "\n" + entry;
          }
        }
      } else {
        const recordAnchors = [
          "## \u8BB0\u5F55",
          "## Records",
          "## \u{1F4DD} \u4ECA\u65E5\u8BB0\u5F55",
          "## \u{1F4DD} Today Notes",
          recordHeading("zh-CN"),
          recordHeading("en")
        ];
        const recordIdx = recordAnchors.reduce((acc, heading) => {
          if (acc !== -1) return acc;
          return content.indexOf(String(heading || ""));
        }, -1);
        const insertBlock = "\n" + sectionHeader + "\n" + entry + "\n";
        if (recordIdx !== -1) {
          const lineEnd = content.indexOf("\n", recordIdx);
          if (lineEnd !== -1) {
            if (isRecordSectionHeading(sectionHeader)) {
              content = content.slice(0, lineEnd) + "\n" + entry + content.slice(lineEnd);
            } else {
              content = content.slice(0, lineEnd) + "\n" + insertBlock + content.slice(lineEnd);
            }
          } else {
            if (isRecordSectionHeading(sectionHeader)) {
              content = content + "\n" + entry;
            } else {
              content = content + "\n" + insertBlock;
            }
          }
        } else {
          content = content + "\n" + insertBlock;
        }
      }
      await vault.modify(file, content);
    }
    module2.exports = {
      DAILY_NOTE_TEMPLATE,
      DAILY_NOTE_TEMPLATE_EN,
      formatDateStr,
      formatTimeStr,
      formatCaptureEntry,
      findOrCreateDailyNote,
      appendToIdeaSection,
      getDailyNoteTemplate,
      renderDailyNoteTemplate,
      resolveSkillDailyNoteTemplate,
      parseCaptureTextSections,
      extractUrlsFromText,
      summaryFallback
    };
  }
});

// runtime/mobile/capture-modal.js
var require_capture_modal = __commonJS({
  "runtime/mobile/capture-modal.js"(exports2, module2) {
    var { Modal: Modal2, Notice: Notice2, Platform: Platform2 } = require("obsidian");
    var { tFromContext } = require_i18n_runtime();
    var { resolveEffectiveLocaleFromSettings: resolveEffectiveLocaleFromSettings2 } = require_mobile_settings_utils();
    var { cleanupCapture, hasAiConfig } = require_mobile_ai_service();
    var { enrichUrlsInText } = require_mobile_url_summary_service();
    var {
      findOrCreateDailyNote,
      appendToIdeaSection,
      formatCaptureEntry,
      formatTimeStr
    } = require_daily_note_service();
    var captureInFlight = false;
    var CaptureModal = class extends Modal2 {
      constructor(app, plugin) {
        super(app);
        this.plugin = plugin;
      }
      onOpen() {
        const { contentEl } = this;
        contentEl.addClass("oc-capture-modal");
        const locale = typeof this.plugin.getEffectiveLocale === "function" ? this.plugin.getEffectiveLocale() : resolveEffectiveLocaleFromSettings2(this.plugin.settings);
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        contentEl.createEl("div", { cls: "oc-capture-drag-handle" });
        contentEl.createEl("div", {
          cls: "oc-capture-title",
          text: t("mobile.capture.title", "\u{1F4A1} Quick Capture")
        });
        const inputEl = contentEl.createEl("textarea", {
          cls: "oc-capture-input",
          attr: { placeholder: t("mobile.capture.inputPlaceholder", "Type your thought..."), rows: "4" }
        });
        const statusEl = contentEl.createEl("div", { cls: "oc-capture-status" });
        const footerEl = contentEl.createEl("div", { cls: "oc-capture-footer" });
        footerEl.createEl("span", {
          cls: "oc-capture-hint",
          text: Platform2.isMobile ? "" : t("mobile.capture.hintSend", "Ctrl/Cmd + Enter to submit")
        });
        const actionsEl = footerEl.createEl("div", { cls: "oc-capture-actions" });
        const cancelBtn = actionsEl.createEl("button", {
          text: t("mobile.capture.cancel", "Cancel"),
          cls: "oc-capture-btn oc-capture-btn-cancel mod-muted"
        });
        const submitBtn = actionsEl.createEl("button", {
          text: t("mobile.capture.submit", "Capture"),
          cls: "oc-capture-btn oc-capture-btn-submit mod-cta"
        });
        cancelBtn.type = "button";
        submitBtn.type = "button";
        cancelBtn.addEventListener("click", () => this.close());
        const doCapture = async () => {
          if (captureInFlight) return;
          const raw = inputEl.value.trim();
          if (!raw) {
            new Notice2(t("mobile.capture.emptyInput", "Please input content"));
            return;
          }
          captureInFlight = true;
          submitBtn.disabled = true;
          cancelBtn.disabled = true;
          submitBtn.textContent = t("mobile.capture.submitBusy", "Processing...");
          try {
            const mc = this.plugin.settings.mobileCapture;
            let finalText = raw;
            if (mc.enableAiCleanup && hasAiConfig(mc)) {
              statusEl.textContent = t("mobile.capture.statusAiCleanup", "\u{1F916} Cleaning text...");
              try {
                finalText = await cleanupCapture(raw, mc, { locale });
              } catch (_error) {
                statusEl.textContent = t("mobile.capture.statusAiCleanupFailed", "\u26A0\uFE0F AI cleanup failed, using original text");
                finalText = raw;
              }
            }
            if (mc.enableUrlSummary !== false) {
              const hasUrl = /https?:\/\//i.test(finalText);
              if (hasUrl) {
                statusEl.textContent = t("mobile.capture.statusResolver", "\u{1F517} Resolving URLs...");
                try {
                  const enriched = await enrichUrlsInText(finalText, mc, {
                    locale,
                    onStatus: (hint) => {
                      if (hint) statusEl.textContent = hint;
                    }
                  });
                  finalText = enriched.text;
                  if (enriched.statusHint) statusEl.textContent = enriched.statusHint;
                } catch (error) {
                  statusEl.textContent = t("mobile.capture.statusResolverFailed", "\u26A0\uFE0F URL resolve failed: {message}", {
                    message: error instanceof Error ? error.message : String(error)
                  });
                }
              }
            }
            statusEl.textContent = t("mobile.capture.statusWriteNote", "\u{1F4DD} Writing note...");
            const vault = this.app.vault;
            const dailyNote = await findOrCreateDailyNote(vault, mc.dailyNotePath, void 0, {
              locale,
              skillsDir: this.plugin && this.plugin.settings && typeof this.plugin.settings.skillsDir === "string" ? this.plugin.settings.skillsDir : ".opencode/skills"
            });
            const timeStr = formatTimeStr();
            const entry = formatCaptureEntry(timeStr, finalText, { locale });
            await appendToIdeaSection(vault, dailyNote, entry, mc.ideaSectionHeader);
            new Notice2(t("notices.captureSaved", "\u2705 Saved"));
            this.close();
          } catch (e) {
            const msg = e instanceof Error ? e.message : String(e);
            statusEl.textContent = `\u274C ${msg}`;
            new Notice2(t("notices.captureFailed", "Capture failed: {message}", { message: msg }));
          } finally {
            captureInFlight = false;
            submitBtn.disabled = false;
            cancelBtn.disabled = false;
            submitBtn.textContent = t("mobile.capture.submit", "Capture");
          }
        };
        submitBtn.addEventListener("click", doCapture);
        inputEl.addEventListener("keydown", (e) => {
          if (e.key === "Enter" && (e.ctrlKey || e.metaKey)) {
            e.preventDefault();
            doCapture();
          }
        });
        if (Platform2.isMobile) {
          requestAnimationFrame(() => {
            const modalEl = contentEl.closest(".modal");
            if (!modalEl) return;
            const vv = typeof window !== "undefined" && window.visualViewport ? window.visualViewport : null;
            const ua = typeof navigator !== "undefined" ? String(navigator.userAgent || "") : "";
            const isLikelyIOS = Boolean(
              Platform2 && (Platform2.isIosApp || Platform2.isIos) || /iPad|iPhone|iPod/i.test(ua)
            );
            let rafId = 0;
            let baselineBottom = 0;
            const listeners = [];
            const getViewportBottom = () => {
              if (vv) return Number(vv.height || 0) + Number(vv.offsetTop || 0);
              return Number(window.innerHeight || 0);
            };
            const applyKeyboardOffset = (keyboardHeight) => {
              const offset = Math.max(0, Math.round(Number(keyboardHeight) || 0));
              const inputFocused = typeof document !== "undefined" && document.activeElement === inputEl;
              if (isLikelyIOS && inputFocused) {
                modalEl.style.setProperty("top", "max(8px, env(safe-area-inset-top, 0px))", "important");
                modalEl.style.setProperty("bottom", "auto", "important");
                modalEl.toggleClass("oc-capture-top-mode", true);
                modalEl.toggleClass("oc-capture-kb-open", true);
                contentEl.style.setProperty("--oc-capture-keyboard-offset", `${offset}px`);
                return;
              }
              modalEl.toggleClass("oc-capture-top-mode", false);
              modalEl.style.setProperty("bottom", offset > 0 ? `${offset}px` : "0", "important");
              modalEl.style.setProperty("top", "auto", "important");
              modalEl.toggleClass("oc-capture-kb-open", offset > 0);
              contentEl.style.setProperty("--oc-capture-keyboard-offset", `${offset}px`);
            };
            const recalc = () => {
              const currentBottom = getViewportBottom();
              if (!baselineBottom || currentBottom > baselineBottom) baselineBottom = currentBottom;
              const keyboardHeight = Math.max(0, baselineBottom - currentBottom);
              applyKeyboardOffset(keyboardHeight);
            };
            const scheduleRecalc = (delay = 0) => {
              if (rafId) cancelAnimationFrame(rafId);
              if (delay > 0) {
                window.setTimeout(() => {
                  rafId = requestAnimationFrame(recalc);
                }, delay);
                return;
              }
              rafId = requestAnimationFrame(recalc);
            };
            const bind = (target, eventName, handler, options) => {
              target.addEventListener(eventName, handler, options);
              listeners.push(() => target.removeEventListener(eventName, handler, options));
            };
            baselineBottom = getViewportBottom();
            scheduleRecalc();
            scheduleRecalc(80);
            scheduleRecalc(180);
            if (vv) {
              bind(vv, "resize", () => scheduleRecalc());
              bind(vv, "scroll", () => scheduleRecalc());
            }
            bind(window, "resize", () => {
              baselineBottom = Math.max(baselineBottom, getViewportBottom());
              scheduleRecalc();
            });
            bind(inputEl, "focus", () => scheduleRecalc(50));
            bind(inputEl, "blur", () => scheduleRecalc(120));
            bind(document, "focusin", () => scheduleRecalc(30));
            bind(document, "focusout", () => scheduleRecalc(120));
            this._vpCleanup = () => {
              if (rafId) cancelAnimationFrame(rafId);
              for (const dispose of listeners) dispose();
              modalEl.style.removeProperty("bottom");
              modalEl.style.removeProperty("top");
              modalEl.removeClass("oc-capture-kb-open");
              modalEl.removeClass("oc-capture-top-mode");
              contentEl.style.removeProperty("--oc-capture-keyboard-offset");
            };
          });
        }
        setTimeout(() => inputEl.focus(), 80);
      }
      onClose() {
        if (this._vpCleanup) {
          this._vpCleanup();
          this._vpCleanup = null;
        }
        this.contentEl.empty();
      }
    };
    module2.exports = { CaptureModal };
  }
});

// runtime/mobile/mobile-settings-tab.js
var require_mobile_settings_tab = __commonJS({
  "runtime/mobile/mobile-settings-tab.js"(exports2, module2) {
    var { PluginSettingTab: PluginSettingTab2, Setting: Setting2, Notice: Notice2 } = require("obsidian");
    var { tFromContext } = require_i18n_runtime();
    var { normalizeUiLanguage: normalizeUiLanguage2 } = require_i18n_locale_utils();
    var {
      LINK_RESOLVER_PROVIDER_IDS,
      LINK_RESOLVER_DEFAULTS,
      normalizeResolverProviderId,
      getResolverProviderPreset,
      normalizeLinkResolver,
      defaultDailyNotePathByLocale,
      defaultIdeaSectionHeaderByLocale,
      getResolverProviderKey,
      setResolverProviderKey,
      resolveEffectiveLocaleFromSettings: resolveEffectiveLocaleFromSettings2
    } = require_mobile_settings_utils();
    var {
      PROVIDER_PRESETS,
      getAiProviderDisplayName,
      testConnection
    } = require_mobile_ai_service();
    var MobileSettingsTab = class extends PluginSettingTab2 {
      constructor(app, plugin) {
        super(app, plugin);
        this.plugin = plugin;
      }
      display() {
        const { containerEl } = this;
        containerEl.empty();
        const locale = typeof this.plugin.getEffectiveLocale === "function" ? this.plugin.getEffectiveLocale() : resolveEffectiveLocaleFromSettings2(this.plugin.settings);
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        if (typeof this.setHeading === "function") this.setHeading();
        containerEl.createEl("p", { text: t("settings.mobile.intro", "Configure AI service and daily note path for mobile quick capture.") });
        new Setting2(containerEl).setName(t("settings.language.name", "UI Language")).setDesc(t("settings.language.desc", "Follows system by default. UI updates immediately after switching.")).addDropdown((d) => {
          d.addOption("auto", t("settings.language.optionAuto", "Follow system (recommended)"));
          d.addOption("zh-CN", t("settings.language.optionZhCN", "\u7B80\u4F53\u4E2D\u6587"));
          d.addOption("en", t("settings.language.optionEn", "English"));
          d.setValue(normalizeUiLanguage2(this.plugin.settings.uiLanguage)).onChange(async (value) => {
            this.plugin.settings.uiLanguage = normalizeUiLanguage2(value);
            await this.plugin.saveSettings();
            this.display();
            new Notice2(t("notices.languageAppliedReloadTip", "Language updated. Command names and ribbon tooltip update after reload."));
          });
        });
        const mc = this.plugin.settings.mobileCapture;
        mc.linkResolver = normalizeLinkResolver(mc.linkResolver);
        const lr = mc.linkResolver;
        const preset = PROVIDER_PRESETS[mc.provider] || PROVIDER_PRESETS.deepseek;
        const resolverProvider = getResolverProviderPreset(lr.provider);
        new Setting2(containerEl).setName(t("mobile.settings.providerName", "AI Provider")).setDesc(t("mobile.settings.providerDesc", "Choose preset provider or custom endpoint.")).addDropdown((d) => {
          for (const [id, p] of Object.entries(PROVIDER_PRESETS)) {
            d.addOption(id, getAiProviderDisplayName(id, p.name, locale));
          }
          d.setValue(mc.provider).onChange(async (v) => {
            mc.provider = v;
            await this.plugin.saveSettings();
            this.display();
          });
        });
        const apiKeySetting = new Setting2(containerEl).setName(t("mobile.settings.apiKeyName", "API Key")).addText((text) => {
          text.inputEl.type = "password";
          text.inputEl.style.width = "100%";
          text.setPlaceholder("sk-...").setValue(mc.apiKey).onChange(async (v) => {
            mc.apiKey = v.trim();
            await this.plugin.saveSettings();
          });
        });
        {
          const descFrag = document.createDocumentFragment();
          descFrag.appendText(t("mobile.settings.apiKeyDesc", "Provider API key. Leave empty to skip AI cleanup."));
          if (preset.keyUrl) {
            descFrag.appendText(" ");
            const link = descFrag.createEl("a", {
              text: t("mobile.settings.providerKeyLinkPrefix", "Get key for {name}", {
                name: getAiProviderDisplayName(mc.provider, preset.name, locale)
              }),
              href: preset.keyUrl
            });
            link.setAttr("target", "_blank");
          }
          apiKeySetting.setDesc(descFrag);
        }
        const effectiveUrl = mc.baseUrl || preset.baseUrl || "(Not set)";
        new Setting2(containerEl).setName(t("mobile.settings.baseUrlName", "Base URL (optional)")).setDesc(t("mobile.settings.baseUrlDesc", "Leave empty to use preset. Current: {value}", { value: effectiveUrl })).addText((text) => {
          text.setPlaceholder(preset.baseUrl || "https://api.example.com").setValue(mc.baseUrl).onChange(async (v) => {
            mc.baseUrl = v.trim();
            await this.plugin.saveSettings();
          });
        });
        const effectiveModel = mc.model || preset.defaultModel || "(Not set)";
        new Setting2(containerEl).setName(t("mobile.settings.modelName", "Model (optional)")).setDesc(t("mobile.settings.modelDesc", "Leave empty to use preset. Current: {value}", { value: effectiveModel })).addText((text) => {
          text.setPlaceholder(preset.defaultModel || "model-name").setValue(mc.model).onChange(async (v) => {
            mc.model = v.trim();
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.aiCleanupName", "Enable AI cleanup")).setDesc(t("mobile.settings.aiCleanupDesc", "Auto remove filler words from capture text.")).addToggle((toggle) => {
          toggle.setValue(mc.enableAiCleanup).onChange(async (v) => {
            mc.enableAiCleanup = v;
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.urlSummaryName", "Enable URL summary")).setDesc(t("mobile.settings.urlSummaryDesc", "Resolve URLs and append inline summary.")).addToggle((toggle) => {
          toggle.setValue(mc.enableUrlSummary !== false).onChange(async (v) => {
            mc.enableUrlSummary = v;
            if (mc.linkResolver && typeof mc.linkResolver === "object") {
              mc.linkResolver.enabled = v;
            }
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.resolverSwitchName", "Enable resolver")).setDesc(t("mobile.settings.resolverSwitchDesc", "Enable external URL resolver providers.")).addToggle((toggle) => {
          toggle.setValue(lr.enabled).onChange(async (v) => {
            lr.enabled = v;
            mc.enableUrlSummary = v;
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.resolverProviderName", "Resolver provider")).setDesc(t("mobile.settings.resolverProviderDesc", "Choose URL resolver provider.")).addDropdown((d) => {
          for (const id of LINK_RESOLVER_PROVIDER_IDS) {
            const provider = getResolverProviderPreset(id);
            d.addOption(id, t(`settings.mobileCapture.resolverProvider.${id}.name`, provider.name));
          }
          d.setValue(resolverProvider.id).onChange(async (v) => {
            lr.provider = normalizeResolverProviderId(v);
            await this.plugin.saveSettings();
            this.display();
          });
        });
        const resolverKeySetting = new Setting2(containerEl).setName(t(`settings.mobileCapture.resolverProvider.${resolverProvider.id}.keyLabel`, resolverProvider.keyLabel)).setDesc(t(`settings.mobileCapture.resolverHint.${resolverProvider.id}`, resolverProvider.hint)).addText((text) => {
          text.inputEl.type = "password";
          text.setPlaceholder(resolverProvider.keyPlaceholder).setValue(getResolverProviderKey(lr, resolverProvider.id)).onChange(async (v) => {
            setResolverProviderKey(lr, resolverProvider.id, v);
            await this.plugin.saveSettings();
          });
        });
        {
          const descFrag = document.createDocumentFragment();
          descFrag.appendText(t("mobile.settings.resolverEntryPrefix", "Resolver setup:"));
          const keyLink = descFrag.createEl("a", {
            text: t("mobile.settings.resolverBuyKey", "Get API key"),
            href: resolverProvider.keyUrl
          });
          keyLink.setAttr("target", "_blank");
          descFrag.appendText(" \xB7 ");
          const docLink = descFrag.createEl("a", {
            text: t("mobile.settings.resolverDocs", "Docs"),
            href: resolverProvider.docsUrl
          });
          docLink.setAttr("target", "_blank");
          descFrag.appendText(t("mobile.settings.resolverEntrySuffix", " Configure and test before capture."));
          resolverKeySetting.setDesc(descFrag);
        }
        new Setting2(containerEl).setName(t("mobile.settings.timeoutName", "Resolver timeout (ms)")).setDesc(t("mobile.settings.timeoutDesc", "Timeout for each resolver request.")).addText((text) => {
          text.setPlaceholder("25000").setValue(String(lr.timeoutMs)).onChange(async (v) => {
            lr.timeoutMs = Math.max(5e3, Number(v) || LINK_RESOLVER_DEFAULTS.timeoutMs);
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.retriesName", "Resolver retries")).setDesc(t("mobile.settings.retriesDesc", "Retry count when resolver fails.")).addText((text) => {
          text.setPlaceholder("2").setValue(String(lr.retries)).onChange(async (v) => {
            lr.retries = Math.min(5, Math.max(0, Number(v) || LINK_RESOLVER_DEFAULTS.retries));
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.concurrencyName", "Resolver concurrency")).setDesc(t("mobile.settings.concurrencyDesc", "Concurrent URL resolver requests.")).addText((text) => {
          text.setPlaceholder("2").setValue(String(lr.maxConcurrency)).onChange(async (v) => {
            lr.maxConcurrency = Math.min(5, Math.max(1, Number(v) || LINK_RESOLVER_DEFAULTS.maxConcurrency));
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.dailyPathName", "Daily note path")).setDesc(t("mobile.settings.dailyPathDesc", "Relative folder path for daily notes.")).addText((text) => {
          text.setPlaceholder(defaultDailyNotePathByLocale(locale)).setValue(mc.dailyNotePath).onChange(async (v) => {
            mc.dailyNotePath = v.trim() || defaultDailyNotePathByLocale(locale);
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.headerName", "Record section header")).setDesc(t("mobile.settings.headerDesc", "Heading used to store captured content.")).addText((text) => {
          text.setPlaceholder(defaultIdeaSectionHeaderByLocale(locale)).setValue(mc.ideaSectionHeader).onChange(async (v) => {
            mc.ideaSectionHeader = v.trim() || defaultIdeaSectionHeaderByLocale(locale);
            await this.plugin.saveSettings();
          });
        });
        new Setting2(containerEl).setName(t("mobile.settings.testName", "Test AI connection")).setDesc(t("mobile.settings.testDesc", "Verify current AI provider configuration.")).addButton((b) => {
          b.setButtonText(t("mobile.settings.testBtn", "Test")).onClick(async () => {
            if (!mc.apiKey) {
              new Notice2(t("notices.needApiKeyFirst", "Please fill API key first"));
              return;
            }
            b.setDisabled(true);
            b.setButtonText(t("mobile.settings.testBusy", "Testing..."));
            try {
              const result = await testConnection(mc, { locale });
              new Notice2(result.ok ? `\u2705 ${result.message}` : `\u274C ${result.message}`);
            } catch (e) {
              new Notice2(`\u274C ${e instanceof Error ? e.message : String(e)}`);
            } finally {
              b.setDisabled(false);
              b.setButtonText(t("mobile.settings.testBtn", "Test"));
            }
          });
        });
      }
    };
    module2.exports = { MobileSettingsTab };
  }
});

// runtime/mobile/mobile-capture-methods.js
var require_mobile_capture_methods = __commonJS({
  "runtime/mobile/mobile-capture-methods.js"(exports2, module2) {
    var { Notice: Notice2 } = require("obsidian");
    var { tFromContext } = require_i18n_runtime();
    var { setRuntimeLocale: setRuntimeLocale2 } = require_runtime_locale_state();
    var { normalizeMobileSettings } = require_mobile_settings_utils();
    var { CaptureModal } = require_capture_modal();
    var { MobileSettingsTab } = require_mobile_settings_tab();
    var FLOWNOTE_ICON_ID2 = "flownote-journal-glow";
    var mobileCaptureMethodsMixin = {
      /**
       * Mobile-only onload — called instead of desktop onload on mobile platforms.
       */
      async onloadMobile() {
        const t = (key, fallback, params = {}) => tFromContext(this, key, fallback, params);
        try {
          await this.loadMobilePersistedData();
          this.addRibbonIcon(FLOWNOTE_ICON_ID2, t("commands.mobileQuickCapture", "\u5FEB\u901F\u6355\u83B7\u60F3\u6CD5"), () => {
            this.openCaptureModal();
          });
          this.addCommand({
            id: "mobile-quick-capture",
            name: t("commands.mobileQuickCapture", "\u5FEB\u901F\u6355\u83B7\u60F3\u6CD5"),
            callback: () => {
              this.openCaptureModal();
            }
          });
          this.addSettingTab(new MobileSettingsTab(this.app, this));
        } catch (e) {
          const msg = e instanceof Error ? e.message : String(e);
          console.error("[FLOWnote] mobile load failed", e);
          new Notice2(t("notices.mobileLoadFailed", "FLOWnote \u79FB\u52A8\u7AEF\u52A0\u8F7D\u5931\u8D25: {message}", { message: msg }));
        }
      },
      /**
       * Open the capture modal.
       */
      openCaptureModal() {
        new CaptureModal(this.app, this).open();
      },
      /**
       * Simplified data loading for mobile — no Node.js dependencies.
       */
      async loadMobilePersistedData() {
        const raw = await this.loadData();
        const data = raw && typeof raw === "object" ? raw : {};
        this.settings = normalizeMobileSettings(data.settings || {});
        setRuntimeLocale2(typeof this.getEffectiveLocale === "function" ? this.getEffectiveLocale() : "en");
      },
      /**
       * Save settings (shares data.json format with desktop).
       */
      async saveMobileSettings() {
        const raw = await this.loadData() || {};
        raw.settings = this.settings;
        await this.saveData(raw);
        setRuntimeLocale2(typeof this.getEffectiveLocale === "function" ? this.getEffectiveLocale() : "en");
      },
      /**
       * Alias for save — used by settings tab.
       */
      async saveSettings() {
        await this.saveMobileSettings();
      }
    };
    module2.exports = { mobileCaptureMethodsMixin };
  }
});

// main.js
var obsidianModule = require("obsidian");
var { setRuntimeLocale } = (() => {
  try {
    return require_runtime_locale_state();
  } catch (_e) {
    return { setRuntimeLocale: () => "en" };
  }
})();
var {
  Modal = class {
  },
  Notice = class {
  },
  Plugin = class {
  },
  Platform = { isMobile: false },
  addIcon = () => {
  },
  PluginSettingTab = class {
  },
  Setting = class {
  },
  normalizePath = (value) => String(value || ""),
  requestUrl = async () => ({ status: 500, text: "", json: null })
} = obsidianModule;
var FLOWNOTE_ICON_ID = "flownote-journal-glow";
var FLOWNOTE_ICON_SVG = [
  '<g fill="none" stroke="currentColor" stroke-width="6" stroke-linecap="round" stroke-linejoin="round">',
  '<path d="M16 79V34c11-7 23-7 34 0v45c-11-7-23-7-34 0z" />',
  '<path d="M84 79V34c-11-7-23-7-34 0v45c11-7 23-7 34 0z" />',
  '<path d="M50 34v45" />',
  '<path d="M40 20c0-5.5 4.5-10 10-10s10 4.5 10 10c0 5-2.6 8-5.4 10.9-1.5 1.5-2.6 3-2.6 5.1h-4c0-2.1-1.1-3.6-2.6-5.1C42.6 28 40 25 40 20z" />',
  '<path d="M46 40h8" />',
  '<path d="M47 46h6" />',
  '<path d="M50 4v4" />',
  '<path d="M36 8l2.8 2.8" />',
  '<path d="M64 8l-2.8 2.8" />',
  '<path d="M30 16h4" />',
  '<path d="M66 16h4" />',
  "</g>"
].join("");
var flownoteIconRegistered = false;
function registerFLOWnoteIcons(addIconFn) {
  if (flownoteIconRegistered) return;
  if (typeof addIconFn !== "function") return;
  addIconFn(FLOWNOTE_ICON_ID, FLOWNOTE_ICON_SVG);
  flownoteIconRegistered = true;
}
function resolveRuntimeModuleAbsolutePath(relativePath) {
  let fsMod;
  let pathMod;
  try {
    fsMod = require("fs");
    pathMod = require("path");
  } catch (_error) {
    return "";
  }
  const relative = String(relativePath || "").replace(/^\/+/, "");
  if (!relative) return "";
  const candidates = [];
  if (typeof __dirname === "string" && __dirname) candidates.push(__dirname);
  if (typeof process !== "undefined" && process && typeof process.cwd === "function") {
    const cwd = String(process.cwd() || "");
    if (cwd) {
      candidates.push(cwd);
      candidates.push(pathMod.join(cwd, ".obsidian", "plugins", "flownote"));
    }
  }
  const seen = /* @__PURE__ */ new Set();
  for (const candidate of candidates) {
    const normalized = String(candidate || "").trim();
    if (!normalized || seen.has(normalized)) continue;
    seen.add(normalized);
    const modulePath = pathMod.join(normalized, "runtime", `${relative}.js`);
    if (fsMod.existsSync(modulePath)) return modulePath;
  }
  return "";
}
function normalizeRuntimeRelativePath(relativePath) {
  return String(relativePath || "").replace(/^\/+/, "").replace(/\.js$/i, "");
}
var LOCAL_RUNTIME_MODULE_LOADERS = {
  "i18n-locale-utils": () => require_i18n_locale_utils(),
  "i18n-messages": () => require_i18n_messages()
};
function requireRuntimeModuleSafe(relativePath) {
  const normalizedPath = normalizeRuntimeRelativePath(relativePath);
  try {
    const loader = LOCAL_RUNTIME_MODULE_LOADERS[normalizedPath];
    if (typeof loader !== "function") {
      throw new Error(`unknown runtime module: ${normalizedPath}`);
    }
    return loader();
  } catch (primaryError) {
    const fallbackPath = resolveRuntimeModuleAbsolutePath(normalizedPath);
    if (fallbackPath) return require(fallbackPath);
    throw primaryError;
  }
}
function fallbackNormalizeUiLanguage(value) {
  const raw = String(value || "").trim().toLowerCase();
  if (raw === "auto") return "auto";
  if (raw === "zh-cn" || raw === "zh_cn" || raw === "zh") return "zh-CN";
  if (raw.startsWith("en")) return "en";
  return "auto";
}
function fallbackNormalizeSupportedLocale(value, fallback = "en") {
  const raw = String(value || "").trim().toLowerCase();
  if (raw === "zh-cn" || raw === "zh_cn" || raw === "zh" || raw.startsWith("zh-")) return "zh-CN";
  if (raw.startsWith("en")) return "en";
  if (fallback === null || fallback === void 0) return "en";
  return String(fallback);
}
function fallbackResolveLocaleFromNavigator(navigatorLike, fallback = "en") {
  const nav = navigatorLike && typeof navigatorLike === "object" ? navigatorLike : null;
  const candidates = [];
  if (nav && Array.isArray(nav.languages)) {
    for (const item of nav.languages) {
      if (typeof item === "string" && item.trim()) candidates.push(item.trim());
    }
  }
  if (nav && typeof nav.language === "string" && nav.language.trim()) {
    candidates.push(nav.language.trim());
  }
  if (!candidates.length) return fallbackNormalizeSupportedLocale(fallback, fallback);
  for (const locale of candidates) {
    const normalized = fallbackNormalizeSupportedLocale(locale, "");
    if (normalized === "zh-CN" || normalized === "en") return normalized;
  }
  return fallbackNormalizeSupportedLocale(fallback, fallback);
}
function fallbackGetMessageByPath(messages, path) {
  if (!messages || typeof messages !== "object") return void 0;
  const keys = String(path || "").split(".");
  let cursor = messages;
  for (const key of keys) {
    if (!cursor || typeof cursor !== "object" || !(key in cursor)) return void 0;
    cursor = cursor[key];
  }
  return cursor;
}
function fallbackInterpolateTemplate(message, params = {}) {
  const template = String(message || "");
  return template.replace(/\{([a-zA-Z0-9_]+)\}/g, (_match, name) => {
    if (!Object.prototype.hasOwnProperty.call(params, name)) return "";
    const value = params[name];
    return value === void 0 || value === null ? "" : String(value);
  });
}
var localeUtilsModule = (() => {
  try {
    return requireRuntimeModuleSafe("i18n-locale-utils");
  } catch {
    return {};
  }
})();
var i18nMessagesModule = (() => {
  try {
    return requireRuntimeModuleSafe("i18n-messages");
  } catch {
    return {};
  }
})();
var DEFAULT_UI_LOCALE = typeof localeUtilsModule.DEFAULT_UI_LOCALE === "string" ? localeUtilsModule.DEFAULT_UI_LOCALE : "en";
var normalizeUiLanguage = typeof localeUtilsModule.normalizeUiLanguage === "function" ? localeUtilsModule.normalizeUiLanguage : fallbackNormalizeUiLanguage;
var normalizeSupportedLocale = typeof localeUtilsModule.normalizeSupportedLocale === "function" ? localeUtilsModule.normalizeSupportedLocale : fallbackNormalizeSupportedLocale;
var resolveLocaleFromNavigator = typeof localeUtilsModule.resolveLocaleFromNavigator === "function" ? localeUtilsModule.resolveLocaleFromNavigator : fallbackResolveLocaleFromNavigator;
var getMessageByPath = typeof localeUtilsModule.getMessageByPath === "function" ? localeUtilsModule.getMessageByPath : fallbackGetMessageByPath;
var interpolateTemplate = typeof localeUtilsModule.interpolateTemplate === "function" ? localeUtilsModule.interpolateTemplate : fallbackInterpolateTemplate;
var I18N_MESSAGES = i18nMessagesModule && typeof i18nMessagesModule.I18N_MESSAGES === "object" ? i18nMessagesModule.I18N_MESSAGES : { "zh-CN": {}, en: {} };
var DEFAULT_VIEW_TYPE = "flownote-view";
function i18nLookup(locale, key, params = {}, options = {}) {
  const normalizedLocale = normalizeSupportedLocale(locale, DEFAULT_UI_LOCALE);
  const fallbackLocale = normalizeSupportedLocale(options.fallbackLocale || DEFAULT_UI_LOCALE, DEFAULT_UI_LOCALE);
  const defaultValue = Object.prototype.hasOwnProperty.call(options, "defaultValue") ? options.defaultValue : key;
  const fromLocale = getMessageByPath(I18N_MESSAGES[normalizedLocale], key);
  const fromFallback = getMessageByPath(I18N_MESSAGES[fallbackLocale], key);
  const message = fromLocale !== void 0 ? fromLocale : fromFallback !== void 0 ? fromFallback : defaultValue;
  if (typeof message !== "string") return String(message);
  return interpolateTemplate(message, params);
}
function resolveEffectiveLocaleFromSettings(settings, navigatorLike) {
  const preferred = normalizeUiLanguage(settings && settings.uiLanguage);
  if (preferred === "auto") {
    return resolveLocaleFromNavigator(navigatorLike || (typeof navigator !== "undefined" ? navigator : null), DEFAULT_UI_LOCALE);
  }
  return normalizeSupportedLocale(preferred, DEFAULT_UI_LOCALE);
}
function resolveFacadeModuleAbsolutePath(plugin, relativePath) {
  let fsMod;
  let pathMod;
  try {
    fsMod = require("fs");
    pathMod = require("path");
  } catch (_error) {
    return "";
  }
  const candidates = [];
  if (plugin && plugin.manifest && plugin.manifest.dir) {
    candidates.push(String(plugin.manifest.dir));
  }
  const adapter = plugin && plugin.app && plugin.app.vault ? plugin.app.vault.adapter : null;
  const byMethod = adapter && typeof adapter.getBasePath === "function" ? adapter.getBasePath() : "";
  const byField = adapter && adapter.basePath ? adapter.basePath : "";
  const basePath = byMethod || byField;
  const configDir = plugin && plugin.app && plugin.app.vault && plugin.app.vault.configDir ? String(plugin.app.vault.configDir) : ".obsidian";
  const pluginId = plugin && plugin.manifest && plugin.manifest.id ? String(plugin.manifest.id) : "flownote";
  if (basePath) {
    candidates.push(pathMod.join(basePath, configDir, "plugins", pluginId));
  }
  if (typeof __dirname === "string" && __dirname) {
    candidates.push(__dirname);
  }
  const seen = /* @__PURE__ */ new Set();
  for (const candidate of candidates) {
    const normalized = String(candidate || "").trim();
    if (!normalized || seen.has(normalized)) continue;
    seen.add(normalized);
    const modulePath = pathMod.join(normalized, "runtime", `${String(relativePath || "").replace(/^\/+/, "")}.js`);
    if (fsMod.existsSync(modulePath)) return modulePath;
  }
  return "";
}
function requireFacadeModule(plugin, relativePath) {
  try {
    switch (relativePath) {
      case "plugin/module-loader-methods":
        return require_module_loader_methods();
      case "plugin/runtime-state-methods":
        return require_runtime_state_methods();
      case "plugin/model-catalog-methods":
        return require_model_catalog_methods();
      case "plugin/bundled-skills-methods":
        return require_bundled_skills_methods();
      case "plugin/session-bootstrap-methods":
        return require_session_bootstrap_methods();
      case "mobile/mobile-capture-methods":
        return require_mobile_capture_methods();
      default:
        throw new Error(`unknown facade module: ${relativePath}`);
    }
  } catch (primaryError) {
    const fallbackPath = resolveFacadeModuleAbsolutePath(plugin, relativePath);
    if (fallbackPath) return requireFacadeModuleFromAbsolutePath(fallbackPath);
    throw primaryError;
  }
}
var OBSIDIAN_REQUIRE_SHIM_KEY = "__flownoteObsidianRequireShim";
function ensureObsidianRequireShim() {
  let moduleLoader = null;
  try {
    moduleLoader = require("module");
  } catch (_error) {
    return false;
  }
  if (!moduleLoader || typeof moduleLoader._load !== "function") return false;
  if (moduleLoader[OBSIDIAN_REQUIRE_SHIM_KEY]) return true;
  const originalLoad = moduleLoader._load;
  const patchedLoad = function patchedLoad2(request, parent, isMain) {
    if (request === "obsidian") return obsidianModule;
    return originalLoad.call(this, request, parent, isMain);
  };
  moduleLoader._load = patchedLoad;
  moduleLoader[OBSIDIAN_REQUIRE_SHIM_KEY] = {
    originalLoad,
    patchedLoad
  };
  return true;
}
function removeObsidianRequireShim() {
  let moduleLoader = null;
  try {
    moduleLoader = require("module");
  } catch (_error) {
    return;
  }
  if (!moduleLoader || typeof moduleLoader._load !== "function") return;
  const state = moduleLoader[OBSIDIAN_REQUIRE_SHIM_KEY];
  if (!state || typeof state !== "object") return;
  if (moduleLoader._load === state.patchedLoad && typeof state.originalLoad === "function") {
    moduleLoader._load = state.originalLoad;
  }
  delete moduleLoader[OBSIDIAN_REQUIRE_SHIM_KEY];
}
function requireFacadeModuleFromAbsolutePath(modulePath) {
  ensureObsidianRequireShim();
  return require(modulePath);
}
var FLOWnoteAssistantPlugin = class _FLOWnoteAssistantPlugin extends Plugin {
  constructor(app, manifest) {
    super(app, manifest);
    this.__pluginFacadeMethodsLoaded = false;
    this.__mobileMethodsLoaded = false;
  }
  getDeviceLocale() {
    return resolveLocaleFromNavigator(typeof navigator !== "undefined" ? navigator : null, DEFAULT_UI_LOCALE);
  }
  getEffectiveLocale() {
    return resolveEffectiveLocaleFromSettings(this.settings || {}, typeof navigator !== "undefined" ? navigator : null);
  }
  t(key, params = {}, options = {}) {
    const locale = options && options.locale ? options.locale : this.getEffectiveLocale();
    return i18nLookup(locale, key, params, options);
  }
  refreshLocaleUi() {
    try {
      setRuntimeLocale(this.getEffectiveLocale());
    } catch {
    }
    try {
      if (typeof this.getAssistantView === "function") {
        const view = this.getAssistantView();
        if (view && typeof view.render === "function") view.render();
      }
    } catch {
    }
  }
  ensureFacadeMethodsLoaded() {
    if (this.__pluginFacadeMethodsLoaded) return;
    const {
      createModuleLoaderMethods
    } = requireFacadeModule(this, "plugin/module-loader-methods");
    const {
      runtimeStateMethods
    } = requireFacadeModule(this, "plugin/runtime-state-methods");
    const {
      modelCatalogMethods
    } = requireFacadeModule(this, "plugin/model-catalog-methods");
    const {
      createBundledSkillsMethods
    } = requireFacadeModule(this, "plugin/bundled-skills-methods");
    const {
      sessionBootstrapMethods
    } = requireFacadeModule(this, "plugin/session-bootstrap-methods");
    const moduleLoaderMethods = createModuleLoaderMethods({
      defaultViewType: DEFAULT_VIEW_TYPE
    });
    const bundledSkillsMethods = createBundledSkillsMethods({
      pluginDirname: this.manifest && this.manifest.dir ? String(this.manifest.dir) : typeof __dirname === "string" ? __dirname : ""
    });
    Object.assign(
      _FLOWnoteAssistantPlugin.prototype,
      moduleLoaderMethods,
      runtimeStateMethods,
      modelCatalogMethods,
      bundledSkillsMethods,
      sessionBootstrapMethods
    );
    this.__pluginFacadeMethodsLoaded = true;
  }
  ensureMobileMethodsLoaded() {
    if (this.__mobileMethodsLoaded) return;
    const { mobileCaptureMethodsMixin } = requireFacadeModule(this, "mobile/mobile-capture-methods");
    Object.assign(
      _FLOWnoteAssistantPlugin.prototype,
      mobileCaptureMethodsMixin || {}
    );
    this.__mobileMethodsLoaded = true;
  }
  async onload() {
    registerFLOWnoteIcons(addIcon);
    if (Platform.isMobile) {
      this.ensureMobileMethodsLoaded();
      await this.onloadMobile();
      return;
    }
    try {
      this.ensureFacadeMethodsLoaded();
      this.runtimeStateMigrationDirty = false;
      this.bootstrapInflight = null;
      this.bootstrapLocalDone = false;
      this.bootstrapRemoteDone = false;
      this.bootstrapRemoteAt = 0;
      const runtime = this.ensureRuntimeModules();
      await this.loadPersistedData();
      setRuntimeLocale(this.getEffectiveLocale());
      this.sessionStore = new runtime.SessionStore(this);
      const vaultPath = this.getVaultPath();
      this.skillService = new runtime.SkillService(vaultPath, this.settings);
      this.opencodeClient = new runtime.FLOWnoteClient({
        vaultPath,
        settings: this.settings,
        logger: (line) => this.log(line),
        getPreferredLaunch: () => this.getPreferredLaunchProfile(),
        onLaunchSuccess: (profile) => this.rememberLaunchProfile(profile),
        SdkTransport: runtime.SdkTransport
      });
      this.diagnosticsService = new runtime.DiagnosticsService(this, runtime.ExecutableResolver);
      this.registerView(this.getViewType(), (leaf) => new runtime.FLOWnoteAssistantView(leaf, this));
      this.addRibbonIcon(FLOWNOTE_ICON_ID, "FLOWnote", () => this.activateView());
      this.addCommand({
        id: "open-flownote",
        name: this.t("commands.open"),
        callback: () => this.activateView()
      });
      this.addCommand({
        id: "flownote-send-selected-text",
        name: this.t("commands.sendSelectedText"),
        editorCallback: async (editor) => {
          const text = editor.getSelection().trim();
          if (!text) return new Notice(this.t("notices.pickTextFirst"));
          await this.activateView();
          const view = this.getAssistantView();
          if (view) await view.sendPrompt(text);
        }
      });
      this.addCommand({
        id: "flownote-new-session",
        name: this.t("commands.newSession"),
        callback: async () => {
          const session = await this.createSession("");
          this.sessionStore.setActiveSession(session.id);
          await this.persistState();
          const view = this.getAssistantView();
          if (view) view.render();
        }
      });
      this.addSettingTab(new runtime.FLOWnoteSettingsTab(this.app, this));
      if (this.runtimeStateMigrationDirty) {
        this.runtimeStateMigrationDirty = false;
        void this.persistState().catch((e) => {
          this.log(`persist migrated runtime state failed: ${e instanceof Error ? e.message : String(e)}`);
        });
      }
      void this.bootstrapData({ waitRemote: false, startRemote: true }).then(() => {
        if (typeof this.refreshDiagnosticsStatus === "function") {
          return this.refreshDiagnosticsStatus({ ttlMs: 0, force: true, applyView: true });
        }
        return null;
      }).catch((e) => {
        this.log(`startup bootstrap failed: ${e instanceof Error ? e.message : String(e)}`);
      });
    } catch (e) {
      const msg = e instanceof Error ? e.message : String(e);
      console.error("[FLOWnote] load failed", e);
      new Notice(this.t("notices.pluginLoadFailed", { message: msg }));
    }
  }
  async onunload() {
    if (this.opencodeClient) await this.opencodeClient.stop();
    if (typeof this.getViewType === "function") {
      this.app.workspace.detachLeavesOfType(this.getViewType());
    }
    removeObsidianRequireShim();
  }
  log(line) {
    if (!this.settings || !this.settings.debugLogs) return;
    console.log("[FLOWnote]", line);
  }
  getVaultPath() {
    const adapter = this.app.vault.adapter;
    const byMethod = adapter && typeof adapter.getBasePath === "function" ? adapter.getBasePath() : "";
    const byField = adapter && adapter.basePath ? adapter.basePath : "";
    const resolved = byMethod || byField;
    if (!resolved) throw new Error(this.t("errors.localFsOnly"));
    return resolved;
  }
  /* --- Mobile-only compatibility wrappers --- */
  async _onloadMobile() {
    this.ensureMobileMethodsLoaded();
    if (typeof this.onloadMobile === "function") {
      await this.onloadMobile();
    }
  }
  _openCaptureModal() {
    this.ensureMobileMethodsLoaded();
    if (typeof this.openCaptureModal === "function") {
      this.openCaptureModal();
    }
  }
  async _loadMobileData() {
    this.ensureMobileMethodsLoaded();
    if (typeof this.loadMobilePersistedData === "function") {
      await this.loadMobilePersistedData();
    }
  }
  async saveSettings() {
    if (Platform.isMobile && typeof this.saveMobileSettings === "function") {
      await this.saveMobileSettings();
      return;
    }
    const raw = await this.loadData() || {};
    raw.settings = this.settings;
    await this.saveData(raw);
    setRuntimeLocale(this.getEffectiveLocale());
  }
};
module.exports = FLOWnoteAssistantPlugin;
